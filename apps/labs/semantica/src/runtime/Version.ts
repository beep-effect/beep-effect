/**
 * Version pinned into Semantica-owned parser and prompt identities.
 *
 * **Example** (Read the lab version)
 *
 * ```ts
 * import { SEMANTICA_VERSION } from "@/runtime/Version"
 *
 * console.log(SEMANTICA_VERSION) // "0.0.0"
 * ```
 *
 * @category configuration
 * @since 0.0.0
 */
export const SEMANTICA_VERSION = "0.0.0";
