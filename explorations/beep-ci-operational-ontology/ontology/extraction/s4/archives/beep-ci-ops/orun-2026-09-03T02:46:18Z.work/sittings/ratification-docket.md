# Final Ratification Sitting Docket — ontology-foundational-auditor run 2 (`beep-ci-ops`)

This is an analyst-assembled steward docket, not a ratification. Each row is read from the proposal’s current bytes and its numerically highest review round; a bare `.review.yaml` is round 1. Category disputes are surfaced separately and do not silently change the proposal state.

Census reconciliation: `work/proposals/` contains **45** proposal YAML files. **44** have a highest-round review bound to their current bytes and appear in the three sitting tables below (**17 + 6 + 21 = 44**). The remaining physical proposal is isolated under **NEEDS-ATTENTION** because its current bytes have no current review. The YAML docket retains that anomaly as a forty-fifth machine-visible row so it cannot disappear from the audit trail.

## RATIFY-CANDIDATES (17)

Each candidate has a current-byte `PASS`, `foundational_status: analyzed`, and no current-proposal withdrawal in its revision semantics. A category dispute may still require the steward’s explicit choice.

| Proposal id | Preferred label | Local name | Category | One-line definition | Latest verdict / round |
| --- | --- | --- | --- | --- | --- |
| `otp:jv-failure-signature:001` | failure signature | `FailureSignature` | `information_object` | A repeatable classification information object that combines a failed-step identifier with a failure-kind identifier under one governed vocabulary and provenance context so distinct failure occurrences sharing that tuple can be grouped by CQ-013. | `PASS / r2` |
| `otp:jv-verification-attempt:001` | verification attempt | `VerificationAttempt` | `information_object` | A recorded-attempt information object that identifies one verification attempt and records its start and terminal bounds, target and assurance context, and success, failure, cancellation, or interruption outcome; it does not assert that the journal record is the unfolding process itself. | `PASS / r2` |
| `otp:ov-has-step:001` | has step | `hasStep` | `none` | A proposal-relative ordered-membership relation that holds from a ScheduleProposal information object to a ScheduleStep information object exactly when that step is a member of the proposal's ordered content under the proposal's provenance and projection interpretation. | `PASS / r2` |
| `otp:ov-step-index:001` | step index | `stepIndex` | `none` | A proposal-relative ordinal-value relation that assigns a ScheduleStep the numeric position it occupies in its containing ScheduleProposal's ordered sequence under an explicit ordinal convention. | `PASS / r2` |
| `otp:pa-admission-request:001` | seat request | `SeatRequest` | `information_object` | A durable queued admission-ticket information artifact created by one enqueue act, ordered and aged while its Agent-owned contender waits for machine-wide admission, and removed when a grant is staged or the wait ends; live input DTOs and replay-derived views are separate representations unless their carrier lineage is explicitly joined. | `PASS / r2` |
| `otp:pa-docgen-work-unit:001` | docgen affected work unit | `DocgenAffectedWorkUnit` | `information_object` | The ratified WorkUnitSpecification individual that prescribes local documentation verification over the affected edit-loop scope, distinct from every dated execution and from full-repository docgen proof. | `PASS / r2` |
| `otp:pa-fallow-audit-evidence:002` | Fallow audit lane | `FallowAuditLane` | `information_object` | The ratified WorkUnitSpecification individual that prescribes the Fallow new-only boundary audit against a resolved base and target tree, distinct from any execution and the result artifact that execution produces. | `PASS / r2` |
| `otp:pa-package-dependency-closure:001` | depends on transitively | `dependsOnTransitive` | `none` | A context-indexed package relation that holds from one workspace package to another exactly when the latter is reachable from the former by the declared Datalog fixpoint over direct dependency edges in one pinned graph. | `PASS / r2` |
| `otp:pa-turbo-task-specification:001` | work unit specification | `WorkUnitSpecification` | `information_object` | A repeatable work-prescription information object whose identity-bearing content fixes task semantics, input surface, upstream dependency rule, persistence behavior, outputs, and completion conditions independently of any execution or configuration-entry carrier. | `PASS / r2` |
| `otp:pa-turbo-task-specification:002` | verification lane | `VerificationLane` | `information_object` | A governed verification-procedure information object that coordinates one or more replaceable work prescriptions under a lane kind, input and scope semantics, completion conditions, and failure behavior, independently of any dated execution. | `PASS / r2` |
| `otp:pb-admission-policy:001` | admission policy | `AdmissionPolicy` | `information_object` | A governed policy-specification information object that normatively fixes admission ordering, capacity, aging, class-cap, starvation, applicability, and revision semantics for queued SeatRequests. | `PASS / r2` |
| `otp:pb-admission-priority-aging:001` | admission policy | `AdmissionPolicy` | `information_object` | A governed policy-specification information object that normatively fixes admission ordering, capacity, aging, class-cap, starvation, applicability, and revision semantics for queued SeatRequests. | `PASS / r2` |
| `otp:pb-package-dependency:001` | depends on | `dependsOn` | `none` | A context-indexed package relation that holds from one importing workspace package to one imported workspace package when a direct dependency edge connects that ordered pair in one pinned repository dependency graph. | `PASS / r2` |
| `otp:pb-resolved-cache-posture:001` | cache posture | `CachePosture` | `information_object` | A governed cache-posture specification used to classify the effective local and remote cache authority of a WorkUnit execution as caller-controlled, local-only, or local-write-remote-read under explicit caller-override semantics. | `PASS / r2` |
| `otp:pb-review-fix-class-cap:001` | admission policy | `AdmissionPolicy` | `information_object` | A governed policy-specification information object that normatively fixes admission ordering, capacity, aging, class-cap, starvation, applicability, and revision semantics for queued SeatRequests. | `PASS / r2` |
| `otp:pb-scheduler-process-owner:001` | agent | `Agent` | `role` | An admission-owner role borne by one causally continuous operating-system process while that process owns a queued SeatRequest or active SeatGrant. PID and process-start coordinates are reidentification evidence for the bearer; the role can cover changing owned items without being identical to that process or minting one Agent per item. | `PASS / r3` |
| `otp:pc-token-capacity-policy:001` | admission policy | `AdmissionPolicy` | `information_object` | A governed policy-specification information object that normatively fixes admission ordering, capacity, aging, class-cap, starvation, applicability, and revision semantics for queued SeatRequests. | `PASS / r2` |

## FLAGGED-SUBMITS (6)

These remain submittable only with the deferral visible: their proposal has `foundational_status: explicitly_deferred`, and two also have a substantive latest-review `INDETERMINATE`. A quoted evidence requirement is included verbatim as the YAML scalar value, with folded whitespace normalized to one line.

| Proposal id | Preferred label | Local name | Category | One-line definition | Latest verdict / round | Flag trigger | Quoted deferral / evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `otp:jv-merge-readiness-assessment:001` | verification evidence | `VerificationEvidence` | `information_object` | A provenance-bearing information object used to justify a verification conclusion within a bounded target, tree, epoch, policy, and evidence context; merge-readiness assessments are instances of this broad ratified class rather than its defining extension. | `PASS / r2` | `explicitly_deferred` | “Authoritative generation, issuance, custody, copy, correction, and claim-realization provenance across heterogeneous VerificationEvidence instances is required to decide content versus carrier identity.” (needed_evidence in work/foundational/fa-jv-merge-readiness-assessment-001.yaml) |
| `otp:pa-projection-limitation-report:001` | verification result artifact | `VerificationResultArtifact` | `information_object` | An information object recording the outcome of a verification execution in a form that may bear the contingent VerificationEvidence role when policy-required applicability and provenance are present. A result report containing projection-applicability caveats is one possible instance; its caveat section may instead be a proper component and does not define this broad class or establish an independent limitation-report identity. | `PASS / r3` | `explicitly_deferred` | “Authoritative result formation, issuance, producer, custody, copy, correction, and revocation provenance across heterogeneous VerificationResultArtifact records is required to choose content, record-token, or carrier-lineage identity. A separate limitation-report term additionally requires independent issuance and a Must/Should CQ that consumes it apart from the containing result.” (needed_evidence in work/foundational/fa-pa-projection-limitation-report-001.yaml) |
| `otp:pa-yeet-verification-workflow:001` | verification plan specification | `VerificationPlanSpecification` | `information_object` | A repeatable information object prescribing an ordered verification WorkUnit sequence and its applicable scope independently of any dated execution. Yeet proof-plan selection, monitoring, and merge-ready behavior may occur in an instance or constituent specification, but they are not necessary membership conditions on this broad class. | `PASS / r3` | `explicitly_deferred` | “An authoritative identity contract across heterogeneous verification plans must define content, contextual-copy, replacement, and revision identity. A separate Yeet specialization additionally requires governed authority and version lineage deciding whether cheap-gates, review-fix, and monitor are one plan or coordinated subplans, plus a Must/Should CQ requiring it.” (needed_evidence in work/foundational/fa-pa-yeet-verification-workflow-001.yaml) |
| `otp:pb-admission-priority-class:001` | admission priority class | `AdmissionPriorityClass` | `information_object` | A governed controlled-vocabulary information object whose verify and publish members classify queued SeatRequests for canonical admission ordering; the request's later effective aging rank is a separate derived value and does not reclassify the recorded request. | `INDETERMINATE / r2` | `explicitly_deferred + INDETERMINATE` | “Run-3 evidence must identify the authoritative priority-class registry, version and revision lineage, membership-change rules, and whether planner versions share one governed domain or issue semantically distinct copies.” (needed_evidence in work/proposals/otp-pb-admission-priority-class-001-r2.review.yaml) |
| `otp:pb-planned-lane-status:001` | verification result artifact | `VerificationResultArtifact` | `information_object` | An information object recording the outcome of a verification execution in a form that may bear the contingent VerificationEvidence role when policy-required applicability and provenance are present. A retained planned-lane status record may be one instance, but lane identity, repair instructions, or duration fields are not necessary membership conditions on this broad class. | `PASS / r3` | `explicitly_deferred` | “Authoritative result formation, issuance, producer, custody, copy, correction, and revocation provenance across heterogeneous VerificationResultArtifact records is required to choose content, record-token, or carrier-lineage identity. A separate planned-lane-status term additionally requires retained issuance, parent-attempt and execution joins, and a Must/Should CQ that consumes it separately.” (needed_evidence in work/foundational/fa-pb-planned-lane-status-001.yaml) |
| `otp:pb-verification-evidence-receipt:001` | verification evidence | `VerificationEvidence` | `information_object` | A provenance-bearing information object used to justify a verification conclusion within a bounded subject, tree, epoch, policy, and evidence context; digest-bound replay receipts are instances of this broad ratified class rather than its defining extension. | `INDETERMINATE / r2` | `explicitly_deferred + INDETERMINATE` | “Run-3 must provide authoritative claim-formation and realization rules plus issuance, producer, custody, copy, retention, correction, and revocation provenance across heterogeneous VerificationEvidence records.” (needed_evidence in work/proposals/otp-pb-verification-evidence-receipt-001-r2.review.yaml) |

## WITHDRAWN (21)

These proposal bytes record a concession or withdrawal of the current proposed commitment. The evidence is quoted from the proposal itself; references to a withdrawn narrower specialization were not used to withdraw a repaired broad exact-reuse proposal.

| Proposal id | Preferred label | Local name | Category | One-line definition | Latest verdict / round | Quoted concession / withdrawal evidence |
| --- | --- | --- | --- | --- | --- | --- |
| `otp:jv-actual-wall-duration:001` | recorded wall-duration measurement | `RecordedWallDurationMeasurement` | `information_object` | A recorded-value information object that states one wall-duration value in milliseconds for the execution or attempt extent identified by the same journal record, without asserting an independently issued carrier token or collapsing a lane duration into whole-attempt elapsed time. | `INDETERMINATE / r3` | “Round 3 concedes the remaining warrant defect: CQ-007 reads materialized duration data properties directly and does not require a reified RecordedWallDurationMeasurement individual. The class is withdrawn from run-2 admission pending the named post-run decision evidence.” (revision_log[2].disposition) |
| `otp:jv-actual-wall-duration:002` | wall-duration measurement record | `WallDurationMeasurementRecord` | `information_object` | A measurement-record information object that is one issued carrier token for a recorded wall-duration assertion and is distinguished by its issuance and custody provenance even when another token carries equal content. | `INDETERMINATE / r2` | “Run-2 sitting 1 conceded the issued-carrier proposal as unresolved; the demonstrated attacks are answered by withdrawal pending the named run-3 issuance, custody, and decision evidence.” (revision_log[1].disposition) |
| `otp:jv-admission-lifecycle:001` | admission lifecycle | `AdmissionLifecycle` | `process` | An admission process that begins with one request's enqueue handling, continues through any capacity-holding grant interval, and ends in release, cancellation, or eviction as one causally continuous operational history. | `INDETERMINATE / r2` | “Run-2 sitting 1 struck the cross-journal attack and conceded the process proposal as unresolved because neither process continuity nor a decision need is established by the present records.” (revision_log[1].disposition) |
| `otp:jv-lane-diagnostic-comparison:001` | lane diagnostic comparison | `LaneDiagnosticComparison` | `information_object` | A comparison information object that relates standalone and lane-rerun wall-duration measurements for one task, package, diagnostic policy, and detection context while preserving the compared executions' provenance. | `INDETERMINATE / r2` | “Run-2 sitting 1 struck the carrier-only attack and conceded the standalone comparison helper as unresolved pending joined execution identities and a decision CQ.” (revision_log[1].disposition) |
| `otp:jv-verification-step-execution:001` | work unit execution | `WorkUnitExecution` | `unresolved` | A verification process whose one continuous realization executes a WorkUnit specification within one parent VerificationAttempt and has actual execution boundaries; a not-run disposition is excluded from this class until the step-record contract distinguishes non-occurrence records. | `INDETERMINATE / r2` | “Run-2 sitting 1 struck the already-answered not-run attack and conceded the execution-process proposal as unresolved because the surviving status record lacks the required parent and execution boundaries.” (revision_log[1].disposition) |
| `otp:ov-has-scope:001` | has scope | `hasScope` | `none` | A relation from a ScheduleStep information object to the Scope designation that qualifies the scheduled content of that step within one containing ScheduleProposal and projection interpretation; WorkUnitSpecification, proposal-level, and evidence-scope uses are not asserted by this revision. | `INDETERMINATE / r3` | “ROUND-3 CONCESSION — the ScheduleStep-to-Scope relation is withdrawn from run-2 admission until requirements reconciles the pinned query with the S7 SeatRequest/step projection. Run-3 evidence required: the CQ-020 wording amendment (WorkUnit -> SeatRequest ordering) queued for the post-run CQ revision, together with an executable query and emitted facts that make the ScheduleStep scope association decision-bearing.” (open_issues[3]) |
| `otp:ov-schedule-step:001` | schedule step | `ScheduleStep` | `information_object` | An information object that is one proposal-relative member of a ScheduleProposal's ordered content and records an ordinal membership, a scheduled operational-target reference, and a Scope designation under one projection interpretation. | `INDETERMINATE / r2` | “Run-2 sitting 1 conceded the ScheduleStep content-versus-carrier boundary as unresolved pending the named run-3 provenance evidence.” (revision_log[1].disposition) |
| `otp:ov-schedules-seat-request:001` | schedules seat request | `schedulesSeatRequest` | `none` | A schedule-content reference relation that holds from a ScheduleStep information object to the queued SeatRequest ticket whose admission that step orders within the containing ScheduleProposal and projection context. | `INDETERMINATE / r2` | “Run-2 sitting 1 struck the carrier-only attack and conceded this provisional relation as unresolved until a decision CQ requires the SeatRequest target.” (revision_log[1].disposition) |
| `otp:pa-admission-lease-lifecycle:001` | admission lease lifecycle | `AdmissionLeaseLifecycle` | `process` | An admission process that begins when one SeatGrant becomes active, continues while that grant holds scheduler capacity, and ends when the same grant is released, rolled back, or evicted after owner-liveness failure. | `INDETERMINATE / r2` | “Run-2 sitting 1 conceded the lease-relator, carrier-lineage, and lifecycle-process boundary as unresolved pending joined provenance and an independently warranted decision need.” (revision_log[1].disposition) |
| `otp:pa-fallow-audit-evidence:001` | verification evidence | `VerificationEvidence` | `information_object` | A verification-evidence information object that can carry a provenance-bound Fallow boundary-audit result for an exact procedure, base, target tree, execution, time, and result custody context. | `INDETERMINATE / r2` | “Run-2 sitting 1 struck the exact-reuse warrant attack and conceded the unsupported composite Fallow evidence package as unresolved pending a provenance-bearing result join.” (revision_log[1].disposition) |
| `otp:pa-fallow-audit-evidence:003` | verification result artifact | `VerificationResultArtifact` | `information_object` | A verification-result information artifact that records the outcome of one identified Fallow audit execution over an exact base and target tree; the quoted zero count alone is not sufficient execution provenance. | `INDETERMINATE / r2` | “Run-2 sitting 1 conceded the separate Fallow result artifact as unresolved because the corpus supplies neither its producing-execution join nor an independent decision need.” (revision_log[1].disposition) |
| `otp:pa-projection-conformance-evidence:001` | verification evidence | `VerificationEvidence` | `information_object` | A verification-evidence information object that preserves a projection's property-suite identity, implementation bytes, frozen inputs, replay execution provenance, complete results, and applicability limits as one governed evidence package. | `INDETERMINATE / r2` | “Run-2 sitting 1 conceded the composite projection-evidence package as unresolved pending a contract that joins its independently variable suite, build, inputs, execution, result, custody, and limits.” (revision_log[1].disposition) |
| `otp:pa-projection-conformance-evidence:002` | projection conformance test specification | `ProjectionConformanceTestSpecification` | `information_object` | A test-specification information object that prescribes determinism, admissibility, totality, priority-aging, and differential-replay checks for a schedule projection over identified implementations and frozen inputs. | `INDETERMINATE / r2` | “Run-2 sitting 1 struck the carrier-only attack and conceded the separate property-suite term as unresolved pending independent governance and a decision CQ that requires it.” (revision_log[1].disposition) |
| `otp:pa-projection-contract:001` | admission projection specification | `AdmissionProjectionSpecification` | `information_object` | A governed information object that prescribes deterministic admission-order schedule projection, including input interpretation, policy and ledger reconstruction, canonical ordering, explicit projection instant, output vocabulary, provenance digests, replay and failure conditions, and non-goals under one authority, version, and applicability scope. | `INDETERMINATE / r3` | “Round 3 concedes that the manifest-pinned CQ-020 can be answered from its WorkUnit-shaped schedule A-Box without an AdmissionProjectionSpecification individual. The consolidated contract term is withdrawn from run-2 admission rather than laundering that gap through an unnecessary support edge.” (revision_log[2].disposition) |
| `otp:pa-workspace-package:001` | package reference | `PackageRef` | `unresolved` | A repository-contextual information object that designates one workspace package for dependency and affected-package decisions while preserving the repository, revision, name history, manifest boundary, and designation provenance needed for reidentification. | `INDETERMINATE / r2` | “Run-2 sitting 1 conceded workspace-package lineage as unresolved pending the named run-3 continuity policy and provenance evidence.” (revision_log[1].disposition) |
| `otp:pb-admission-capacity-state:001` | admission snapshot | `AdmissionSnapshot` | `information_object` | A recorded-value information object that groups the observed memAvailableGib, hardFloorEngaged, and capacityTokens values at one admission-state observation boundary; it does not assert a persisting world-side capacity quality or an unrecorded pre-grant admission join. | `INDETERMINATE / r3` | “Round 3 concedes that CQ-010 reads chargeAtAdmissionTokens and capacityAtAdmissionTokens from the SeatGrant and does not require an AdmissionSnapshot individual. The snapshot proposal is withdrawn rather than disguising that missing necessity as a support edge.” (revision_log[2].disposition) |
| `otp:pb-docgen-affected-scope:001` | docgen affected scope | `DocgenAffectedScope` | `unresolved` | A repository-selection situation whose members are the documentation inputs selected for one verification context by a pinned origin/main to HEAD interval together with one dirty-file observation boundary. | `INDETERMINATE / r2` | “Run-2 sitting 1 conceded the docgen-specific extensional-scope candidate as unresolved pending a pinned selection result and a decision CQ that requires the specialization.” (revision_log[1].disposition) |
| `otp:pb-docgen-affected-scope:002` | docgen affected scope specification | `DocgenAffectedScopeSpecification` | `unresolved` | A selection-specification information object that prescribes how a local docgen verification context derives its affected input membership from a base revision, head revision, and dirty-file snapshot. | `INDETERMINATE / r2` | “Run-2 sitting 1 conceded the docgen selection-rule candidate as unresolved pending independent rule authority, lineage, and a decision CQ requiring the specification.” (revision_log[1].disposition) |
| `otp:pb-schedule-projection-specification:001` | admission projection specification | `AdmissionProjectionSpecification` | `information_object` | A governed information object that prescribes deterministic admission-order schedule projection, including input interpretation, policy and ledger reconstruction, canonical ordering, explicit projection instant, output vocabulary, provenance digests, replay and failure conditions, and non-goals under one authority, version, and applicability scope. | `INDETERMINATE / r3` | “Round 3 concedes that the manifest-pinned CQ-020 can be answered from its WorkUnit-shaped schedule A-Box without an AdmissionProjectionSpecification individual. This co-denoting chain is withdrawn with the canonical contract rather than receiving a cosmetic support edge.” (revision_log[2].disposition) |
| `otp:pb-yeet-proof-tier:001` | Yeet proof-planning tier | `YeetProofTier` | `information_object` | An information-object classification scheme whose full, cheap-gates, and review-fix members select contextually expanded Yeet verification plans while remaining distinct from assurance obligations and their tiers. | `INDETERMINATE / r2` | “Run-2 sitting 1 struck the carrier-only attack and conceded the implementation-usefulness helper as unresolved pending classification governance and a decision CQ.” (revision_log[1].disposition) |
| `otp:pc-projection-contract:001` | admission projection specification | `AdmissionProjectionSpecification` | `information_object` | A governed information object that prescribes deterministic admission-order schedule projection, including input interpretation, policy and ledger reconstruction, canonical ordering, explicit projection instant, output vocabulary, provenance digests, replay and failure conditions, and non-goals under one authority, version, and applicability scope. | `INDETERMINATE / r3` | “Round 3 concedes that the manifest-pinned CQ-020 can be answered from its WorkUnit-shaped schedule A-Box without an AdmissionProjectionSpecification individual. This co-denoting chain is withdrawn with the canonical contract rather than receiving a cosmetic support edge.” (revision_log[2].disposition) |

## DISPUTES

The gate comparison is mechanical: the primary and blinded-alternative `IdentityCard.ufo_analysis.candidate_category` values are joined on `hypothesis_ref`. The identity-card file is therefore the controlling file for what that seat actually claims; the paired foundational-analysis file supplies the grounds.

The five supplied alternative-vs-primary shorthands are verified exactly: `docgen-affected-scope-002` is **situation vs unresolved**, `package-dependency-001` is **relator vs none**, `resolved-cache-posture-001` is **mode vs information_object**, `scheduler-process-owner-001` is **kind vs role**, and `yeet-proof-tier-001` is **quality vs information_object**. In every case the first category comes from the `-alt` identity card, not the primary card or proposal. Direct comparison of all current seat pairs finds **15 proposal-level flags across 14 hypothesis pairs**; the additional rows below are included rather than suppressing gate-visible divergence. The two docgen proposal branches share one primary/alternative analysis pair and therefore both receive the same gate flag.

| Proposal id | State | Primary seat: category, files, and one-line grounds | Alternative seat: category, files, and one-line grounds |
| --- | --- | --- | --- |
| `otp:jv-verification-attempt:001` | `RATIFY-CANDIDATE` | **information_object** — `work/foundational/ic-jv-verification-attempt-001.yaml` + `work/foundational/fa-jv-verification-attempt-001.yaml`. The journal supports an authoritative attempt-record lineage with attempt identifier, bounds, target, context, and outcome, but does not independently join those records to one world-side causal process. | **process** — `work/alternative/ic-jv-verification-attempt-alt-001.yaml` + `work/alternative/fa-jv-verification-attempt-alt-001.yaml`. The referent is one bounded verification unfolding on the same checkout and work from start to terminal outcome; attempt and run identifiers are only reidentification evidence. |
| `otp:jv-verification-step-execution:001` | `WITHDRAWN` | **unresolved** — `work/foundational/ic-jv-verification-step-execution-001.yaml` + `work/foundational/fa-jv-verification-step-execution-001.yaml`. The not-run row cannot be an execution occurrence, and the present corpus does not supply the parent-attempt and start/end boundaries needed to choose an execution, disposition situation, or result record. | **information_object** — `work/alternative/ic-jv-verification-step-execution-alt-001.yaml` + `work/alternative/fa-jv-verification-step-execution-alt-001.yaml`. The covering referent is a status-and-outcome assertion about one planned step in one attempt, so a not-run row remains an information object even when no execution occurred. |
| `otp:ov-has-scope:001` | `WITHDRAWN` | **none** — `work/foundational/ic-ov-has-scope-001.yaml` + `work/foundational/fa-ov-has-scope-001.yaml`. The proposal is an object property whose fact is fixed by the ScheduleStep, Scope designation, containing proposal, and projection interpretation; it supplies no independent individual identity. | **quality** — `work/alternative/ic-ov-has-scope-alt-001.yaml` + `work/alternative/fa-ov-has-scope-alt-001.yaml`. The scope classification is a quality borne by each ScheduleStep, with the serialized scope tag serving only as evidence of that quality value. |
| `otp:ov-step-index:001` | `RATIFY-CANDIDATE` | **none** — `work/foundational/ic-ov-step-index-001.yaml` + `work/foundational/fa-ov-step-index-001.yaml`. The proposal is a data-property assertion relative to a step, containing proposal sequence, ordinal convention, and number, not an independently identified positional particular. | **quality** — `work/alternative/ic-ov-step-index-alt-001.yaml` + `work/alternative/fa-ov-step-index-alt-001.yaml`. Ordinal position is a relational quality of a ScheduleStep within one ScheduleProposal sequence; the integer field records its value rather than constituting it. |
| `otp:pa-admission-lease-lifecycle:001` | `WITHDRAWN` | **process** — `work/foundational/ic-pa-admission-lease-lifecycle-001.yaml` + `work/foundational/fa-pa-admission-lease-lifecycle-001.yaml`. The candidate is a lifecycle process spanning activation, heartbeat, and release or eviction, although the current corpus does not authoritatively join those stages into one continuous process. | **relator** — `work/alternative/ic-pa-admission-lease-lifecycle-alt-001.yaml` + `work/alternative/fa-pa-admission-lease-lifecycle-alt-001.yaml`. The active lease is a relator that mediates one admitted request or work item and held scheduler capacity; eviction or removal terminates that mediation. |
| `otp:pa-package-dependency-closure:001` | `RATIFY-CANDIDATE` | **none** — `work/foundational/ic-pa-package-dependency-closure-001.yaml` + `work/foundational/fa-pa-package-dependency-closure-001.yaml`. Transitive dependency is a derived relation or fact between an ordered package pair under one graph and closure rule, with no separate relator identity. | **relator** — `work/alternative/ic-pa-package-dependency-closure-alt-001.yaml` + `work/alternative/fa-pa-package-dependency-closure-alt-001.yaml`. Each reachable importer-to-imported pair in one graph snapshot is a reified reachability mediation, while a non-reified formal predicate remains an acknowledged rival. |
| `otp:pa-workspace-package:001` | `WITHDRAWN` | **unresolved** — `work/foundational/ic-pa-workspace-package-001.yaml` + `work/foundational/fa-pa-workspace-package-001.yaml`. Continuing artifact, immutable version, workspace role, and package-reference models all remain viable because no policy settles identity across rename, move, fork, version, or delete-recreate. | **kind** — `work/alternative/ic-pa-workspace-package-alt-001.yaml` + `work/alternative/fa-pa-workspace-package-alt-001.yaml`. The package is a rigid versioned workspace-member software artifact tracked by package lineage; printed names, dependency declarations, and topological rows are evidence only. |
| `otp:pb-admission-capacity-state:001` | `WITHDRAWN` | **information_object** — `work/foundational/ic-pb-admission-capacity-state-001.yaml` + `work/foundational/fa-pb-admission-capacity-state-001.yaml`. AdmissionSnapshot is recorded-value information from one coordinated capture, but capture-act identity and correlation to the pre-grant boundary remain unresolved. | **quality** — `work/alternative/ic-pb-admission-capacity-state-alt-001.yaml` + `work/alternative/fa-pb-admission-capacity-state-alt-001.yaml`. Available token capacity is a changing quality inhering in the scheduler or machine, and each snapshot field is merely evidence of its current value. |
| `otp:pb-admission-priority-class:001` | `FLAGGED-SUBMIT` | **information_object** — `work/foundational/ic-pb-admission-priority-class-001.yaml` + `work/foundational/fa-pb-admission-priority-class-001.yaml`. AdmissionPriorityClass is a governed controlled-vocabulary information object, but authority, version lineage, and whether planners share or copy the domain remain unresolved. | **quality** — `work/alternative/ic-pb-admission-priority-class-alt-001.yaml` + `work/alternative/fa-pb-admission-priority-class-alt-001.yaml`. Verify or publish classification is a quality borne by an admission request; priority aging may change rank without changing that classification. |
| `otp:pb-docgen-affected-scope:001` | `WITHDRAWN` | **unresolved** — `work/foundational/ic-pb-docgen-affected-scope-001.yaml` + `work/foundational/fa-pb-docgen-affected-scope-001.yaml`. The evidence does not choose between a run-specific extensional input selection and a reusable selection specification, so identity and category remain unresolved. | **situation** — `work/alternative/ic-pb-docgen-affected-scope-alt-001.yaml` + `work/alternative/fa-pb-docgen-affected-scope-alt-001.yaml`. Affected scope is the extensional file-set situation for one verification run under the same base-to-head interval and dirty-file boundary. |
| `otp:pb-docgen-affected-scope:002` | `WITHDRAWN` | **unresolved** — `work/foundational/ic-pb-docgen-affected-scope-001.yaml` + `work/foundational/fa-pb-docgen-affected-scope-001.yaml`. The evidence does not choose between a run-specific extensional input selection and a reusable selection specification, so identity and category remain unresolved. | **situation** — `work/alternative/ic-pb-docgen-affected-scope-alt-001.yaml` + `work/alternative/fa-pb-docgen-affected-scope-alt-001.yaml`. Affected scope is the extensional file-set situation for one verification run under the same base-to-head interval and dirty-file boundary. |
| `otp:pb-package-dependency:001` | `RATIFY-CANDIDATE` | **none** — `work/foundational/ic-pb-package-dependency-001.yaml` + `work/foundational/fa-pb-package-dependency-001.yaml`. Direct dependency is a directed relation or fact between importer and imported package under one graph snapshot; manifest declarations remain separate information objects. | **relator** — `work/alternative/ic-pb-package-dependency-alt-001.yaml` + `work/alternative/fa-pb-package-dependency-alt-001.yaml`. The dependency is a relator mediating the importer and imported package in one manifest snapshot, and removing the import terminates that relator. |
| `otp:pb-resolved-cache-posture:001` | `RATIFY-CANDIDATE` | **information_object** — `work/foundational/ic-pb-resolved-cache-posture-001.yaml` + `work/foundational/fa-pb-resolved-cache-posture-001.yaml`. CachePosture is governed specification content assigning local and remote cache authority across invocations, rather than the token returned by one resolver call. | **mode** — `work/alternative/ic-pb-resolved-cache-posture-alt-001.yaml` + `work/alternative/fa-pb-resolved-cache-posture-alt-001.yaml`. Resolved cache posture is an invocation-borne mode characterizing that run’s cache access; the return object only encodes the mode. |
| `otp:pb-scheduler-process-owner:001` | `RATIFY-CANDIDATE` | **role** — `work/foundational/ic-pb-scheduler-process-owner-001.yaml` + `work/foundational/fa-pb-scheduler-process-owner-001.yaml`. Agent is an anti-rigid admission-owner role borne by one causally continuous operating-system process while it owns live requests or grants, and it carries the bearer’s identity. | **kind** — `work/alternative/ic-pb-scheduler-process-owner-alt-001.yaml` + `work/alternative/fa-pb-scheduler-process-owner-alt-001.yaml`. The owner is the rigid operating-system process instantiation itself; process identifier and start time are reidentification evidence for that kind-individual. |
| `otp:pb-yeet-proof-tier:001` | `WITHDRAWN` | **information_object** — `work/foundational/ic-pb-yeet-proof-tier-001.yaml` + `work/foundational/fa-pb-yeet-proof-tier-001.yaml`. YeetProofTier is classification-scheme information governed by planner policy, although shared-scheme versus copied-domain identity remains unresolved. | **quality** — `work/alternative/ic-pb-yeet-proof-tier-alt-001.yaml` + `work/alternative/fa-pb-yeet-proof-tier-alt-001.yaml`. Full, cheap-gates, or review-fix is a quality borne by a planned verification; LiteralKit members encode the value and do not form a taxonomy. |

## NEEDS-ATTENTION (1)

### `otp:pa-projection-conformance-evidence:003` — verification result artifact

- **Round 1:** `work/proposals/otp-pa-projection-conformance-evidence-003.review.yaml` is the only review. It records `FAIL` against proposal digest `61902062de79ecf11e691b1cc4f69641c8d2204b1903c7bc86eeb3b6f1e46764` and lands `identity-card-cross-wired-to-composite-evidence`, `support-term-not-required-by-generic-evidence`, `discriminator-true-of-test-report`.
- **Sitting-1 revision:** the proposal was changed to digest `d283a9282d2ad062197a0385e14961b969ef5211da08f9ac2b9f69cdc8292f11`. Its revision log names the failed digest, addresses all three landed rules, marks `discriminator-true-of-test-report` struck, and says: “Run-2 sitting 1 struck the carrier-only attack and conceded the separate replay-result term as unresolved pending execution, issuance, custody, and decision provenance.”
- **Round 2:** no `otp-pa-projection-conformance-evidence-003-r2.review.yaml` exists. No round-3 review exists either. Consequently the numerically latest review is still the round-1 `FAIL`, and its `target_sha256` does not bind the current proposal bytes.
- **Disposition:** `NEEDS-ATTENTION`, not a ratify candidate, flagged submit, or docketed withdrawal. The current bytes require the missing contiguous round-2 review before any steward submission; the stale round-1 review cannot adjudicate the revision.

No other proposal has a review-round gap, target-id mismatch, or latest-review digest mismatch.
