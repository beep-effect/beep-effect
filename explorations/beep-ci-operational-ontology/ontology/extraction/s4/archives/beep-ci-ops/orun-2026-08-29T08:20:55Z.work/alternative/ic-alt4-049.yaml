id: ic:alt4:049
hypothesis_ref: dh:turbo-cache:013
identity:
  supplies_identity: true
  identity_criterion: Two Turbo codegen task specifications are the same when they occupy the same pipeline slot with the same upstream codegen dependency selector; the key spelling is evidence, never the criterion.
  reidentification_key:
    - codegen
ontoclean:
  rigidity: rigid
  carries_identity: true
  supplies_identity: true
  unity: The codegen pipeline slot together with its upstream codegen selector compose one task prescription.
  dependence: none
ufo_analysis:
  candidate_category: information_object
  alternatives:
    - kind
  existential_dependencies:
    - turbo pipeline
temporality:
  exists_during_interval: true
  may_change_state_without_losing_identity: false
  terminal_states: []
counterexamples:
  - description: "Calling this a verification lane overclaims: the cited facts show only a configured dependency selector."
  - description: A codegen execution's generated files are outputs of a run, not this specification.
status: proposed
