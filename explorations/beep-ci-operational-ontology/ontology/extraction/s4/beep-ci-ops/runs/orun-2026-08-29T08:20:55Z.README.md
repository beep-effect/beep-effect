# Archived run orun-2026-08-29T08:20:55Z — replay notes

Archived beside this file, per the skill's next-run rotation:

- `orun-2026-08-29T08:20:55Z.manifest.yaml` / `.index.yaml` — the ratified run's
  manifest and total dispositions index (the next run's `prior_index`).
- `orun-2026-08-29T08:20:55Z.observations/` — the run-scoped SourceObservation and
  ProseObservation records (their canonical ids embed the corpus pin and adapter
  version, so a new run regenerates rather than re-validates them).
- `history/orun-2026-08-29T08:20:55Z.engine/` — the exact engine that judged the run:
  `ontology-foundational-auditor/scripts/validate_artifacts.py` (sha256_12
  `6aec64cde23f`), the `_shared/` contracts closure (`e1338e75966c`, 21 members),
  the five seat prompts, and the two templates — byte-identical to the digests the
  manifest pins. The repository-shipped skill under `.claude/skills/` has moved on
  since (validator `c036e5316511`), so replays must use this snapshot. It lives under
  a `history/` segment because it is captured provenance: the knowledge refs gate
  exempts archival directories, and the vendored contract bytes must not be rewritten.

## Corpus pin

Every record cites `repository.commit` `c1558f6ca9b1…`, the run's pin commit on the
`ontology-s4b-normalization` branch. Squash-merging orphans that commit, so it is
retained by the tag `evidence/beep-ci-ops/orun-2026-08-29T08-20-55Z-pin`
(`git fetch origin tag evidence/beep-ci-ops/orun-2026-08-29T08-20-55Z-pin`).

## Replaying the mechanical gate

```sh
RUN=explorations/beep-ci-operational-ontology/ontology/extraction/s4/beep-ci-ops/runs
RID=orun-2026-08-29T08:20:55Z
VAL=$RUN/history/$RID.engine/ontology-foundational-auditor/scripts/validate_artifacts.py
git fetch origin tag evidence/beep-ci-ops/orun-2026-08-29T08-20-55Z-pin
# rebuild the singleton work/ layout the validator expects from the archive
cp $RUN/$RID.manifest.yaml  $RUN/../work/run-manifest.yaml
cp $RUN/$RID.index.yaml     $RUN/../work/dispositions.index.yaml
cp -r $RUN/$RID.observations/observations $RUN/$RID.observations/prose-observations $RUN/../work/
uv run --with pyyaml python $VAL --gate $RUN/.. --repo .
```

The manifest's `repository.commit` must equal `HEAD` for the corpus-fidelity checks,
so replay from a checkout of the pin tag (or pass the archived tree to a validator
build that accepts an explicit pin). Reviews and analyses are content-addressed;
only the latest review round's `target_sha256` / `chain_sha256` are reconstructible
from the committed bytes (see the friction ledger in `research/OPPORTUNITIES.md`).

## 2026-09-03 relocation note (run-2 launch)

`orun-2026-08-29T08:20:55Z.observations/` now lives at
`../archives/beep-ci-ops/orun-2026-08-29T08:20:55Z.observations/` (bytes and
inner layout untouched, moved with git history). The v13 validator scans every
`*.yaml` under the ontology root by record-name prefix with no archive
exemption, so the archived `so-`/`po-` records read as live unpinned evidence
under any later run's manifest — the rotation recipe's own destination poisons
the very scan it exists to protect. Skill follow-up queued: teach the
validator (or the rotation recipe) an archive shelter inside the root.

The carried `work/` seat trees (`hypotheses/`, `foundational/`,
`alternative/`, `proposals/`) moved beside it as
`orun-2026-08-29T08:20:55Z.work/` on the same date: their whole reference
closure names run-1 observation ids, which regenerate under a new corpus pin
by design, so under the scanner they read as 1,896 dangling references
rather than reusable authority. The carry-forward policy's "still-valid
authority" is only reachable when the corpus pin and adapter versions are
unchanged; a real second run always re-derives.

`governance/ratifications/` (rat-001..031) moved beside them as
`orun-2026-08-29T08:20:55Z.governance/ratifications/` for the same reason:
each ratification binds an archived proposal digest, so in-root they scan as
31 unresolvable proposal references. Their authority is already projected
into the packet's S5/S6 status files; run-2 sittings continue the numbering
at rat-032 so identifiers never collide across runs.
