# Archived run orun-2026-09-10T02:10:52Z — auditor run 3

Archived per the next-run rotation:

- `orun-2026-09-10T02:10:52Z.manifest.yaml` / `.index.yaml` — the ratified run's manifest and total
  dispositions index (run 4's `prior_index`; `prior_index_sha256_12` for run 4: `b9c140ccd31b`).
- `../../archives/beep-ci-ops/orun-2026-09-10T02:10:52Z.observations/` — the run-scoped SourceObservation
  (152, adapter-journal v1.1.0 over the four run-3 corpus pins) and ProseObservation (64) records,
  at the v14 sibling shelter outside the scan root.
- The run's seat trees (`work/hypotheses`, `foundational`, `alternative`, `proposals` with their
  reviews), the sittings docket and entries (`work/sittings/`), the review-validity audit and gate
  logs (`work/review-audit/`), and `governance/ratifications/rat-053..rat-070` stay in place under
  the carry-forward policy; run 4 relocates them byte-identically to the shelter when its pin
  stales their closures.

Result: 18 ratifications (rat-053..rat-070), all flagged accepts — the thirteen-member ordering
cluster ratified together (ScheduleProposal and SeatRequest reuse, ScheduleStep, hasStep,
stepIndex zero-based, schedulesSeatRequest, hasScopeTag, VerificationEpisode, hasCurrentProposal,
AdmissionProjectionSpecification, hasProjectionSpecification, hasOriginKey and
admissionChargeTokens reuse) plus five recorded-value reuse mappings (SeatGrant ×2, SeatRequest,
VerificationAttempt, VerificationResultArtifact at assessment-origin grain); 8 proposals withdrawn
with named run-4 evidence; the 68 carried run-2 rows adjudicated at sitting 2 (14 retired, 54
re-parked with fresh evidence); unresolved fraction 84/198 = 42% (no waiver). Gate:
ARTIFACTS VALID — GATE PASSED at the pin. Engine: validator v14 `45e4f856a371`, contracts
`db5aefe804fe`, adapter-journal v1.1.0 `028dcb1d7953`, transcriber `3341d7bcbc5a`, CQ suite
`e99e30cd8015` (26), pin `1c7cd98289` (tag `evidence/beep-ci-ops/orun-2026-09-10T02-10-52Z-pin`),
VAL under Python 3.12. Seats: gpt-6-astra at max (denotation, foundational, synthesis; adversary in an
independent context) and grok-4.6 blinded via a headless proxy session. Sittings 1–3 are logged in
the packet `DECISIONS.md` (2026-09-10 entries).

## Corpus pin

Every record cites `repository.commit` `1c7cd98289150f481fd40721efa6b7fe6109e711`, the run's pin
commit on `ontology-run3`, retained by the tag above (a squash merge orphans it). The branch moved
past the pin before close (a doc-path fix for the semantic-delta gate), so the post-scribe gate ran
in a detached worktree checked out at the tag, with the untracked run tree synced in.
