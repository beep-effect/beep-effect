# Archived run orun-2026-09-03T02:46:18Z — auditor run 2

Archived per the next-run rotation:

- `orun-2026-09-03T02:46:18Z.manifest.yaml` / `.index.yaml` — the ratified run's
  manifest and total dispositions index (run 3's `prior_index`;
  `prior_index_sha256_12` for run 3: `a207a106de68`).
- `../../archives/beep-ci-ops/orun-2026-09-03T02:46:18Z.observations/` — the
  run-scoped SourceObservation and ProseObservation records. The RELOCATED
  destination is deliberate: the v13 validator scans every record-named YAML
  under the ontology root with no archive exemption (see the relocation notes
  in `orun-2026-08-29T08:20:55Z.README.md`), so observation archives live
  outside the scan root until the queued skill fix lands.

Result: 21 ratifications (rat-032..rat-052; 15 clean accepts + 6 flagged reuse
mappings with run-3 deferrals), 24 proposals withdrawn/deferred with named
run-3 evidence, the 149 carried run-1 rows fully adjudicated (146 retired,
3 kept open), unresolved-fraction waiver 56% steward-ratified. Gate:
ARTIFACTS VALID — GATE PASSED. Engine: validator v13 `c036e5316511`,
contracts `db5aefe804fe`, pin `341cfef8b6e8`, VAL under Python 3.12.
Sittings 1–3 are logged in the packet `DECISIONS.md` (2026-09-03 entries).

## 2026-09-09 relocation note (run-3 launch)

The run-2 `work/` trees (`alternative/`, `foundational/`, `hypotheses/`,
`proposals/`, `denotation-batches/`, `review-audit/`, and `sittings/`) now
live beside the observations in
`../../archives/beep-ci-ops/orun-2026-09-03T02:46:18Z.work/`.
Validator v14 requires this sibling shelter outside the live scan root.
Observation identities and proposal reference closures regenerate under a
new pin; leaving predecessor records in `work/` would validate them as live
run-3 evidence. The seven trees contain 365 files, moved with `git mv` and
verified byte-identical. `work/` is empty before the observe pass.

All 21 ratifications (rat-032..rat-052) moved to
`../../archives/beep-ci-ops/orun-2026-09-03T02:46:18Z.governance/ratifications/`,
also byte-identically. Seventeen of them already have accepted S5/S6 status
projections (TAXONOMY.yaml / ABOX.yaml entries). Four do not: rat-032
(FailureSignature) and rat-033 (VerificationAttempt) still read `parked-run-2`
in `extraction/s5/DISPOSITIONS.yaml`, rat-037 (dependsOnTransitive) still reads
`parked-run-2` in `extraction/s6/PREDICATES.yaml`, and rat-039 (VerificationLane
reuse) is still deferred by `docs/s6-abox-contract.md` §5. The run-2 closeout
owed those projections; they were not made. The archived records remain the
authority (digest-bound to the archived proposals), and the projection of every
run-2 ratification into the S5/S6 status surface is carried as a run-3 closeout
item (see `research/auditor-run3-intake.md`, carry-forwards), to be verified by
the S5/S6 scripts together with the run-3 ratifications. The engine report
(`research/run3-lanes/run3-pin-engine-report.md`) records the exact pre-move
scan violations.
