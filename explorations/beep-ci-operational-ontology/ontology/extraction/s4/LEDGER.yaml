# GENERATED by merge_s4_lanes.py from the lane outputs beside this file — do not
# hand-edit. Contract: s4-lane-contract.md §5. Statuses transition only through
# S5 ratification.
- id: fallow-laws-I06
  kind: ambiguity
  claim: Architecture enforcement lanes denote doctrine status and enforcement posture, not the executable
    VerificationLane/LaneKind domain.
  evidence: standards/ARCHITECTURE.md:1918-1925 — 'Architecture rules use four enforcement lanes'
  suggested_disposition: Keep Doctrine, Generated Default, Review Gate, and Hard Check in a distinct source
    domain unless human ratification supplies an explicit mapping.
  status: open
  source_lanes:
  - s4-fallow-laws
- id: literalkits-I01
  kind: ambiguity
  claim: CiLaneClass encodes local parity classes, not the frozen LaneKind distinction between turbo task,
    script, and collected gate.
  evidence: packages/tooling/tool/cli/src/commands/Ci/CiLane.ts:57 — 'export const CI_LANE_CLASS_VALUES
    = ["cli-runnable", "workflow-gated", "ci-native"] as const;'
  suggested_disposition: Keep the source domain distinct and require an explicit mapping property before
    any LaneKind alignment.
  status: open
  source_lanes:
  - s4-literalkits
- id: admission-scheduler-I08
  kind: ambiguity
  claim: Configured token weights are nominal defaults because small hosts may clamp the persisted charge
    or bypass machine-wide admission.
  evidence: packages/tooling/tool/cli/src/internal/repo-run/QualityScheduler.ts:1144-1150 — 'const weightTokens
    = Math.min(request.weightTokens, machineCeiling);'
  suggested_disposition: Use ticket or lease weightTokens for instance admissionChargeTokens and keep
    admissionTokenWeight assertions as policy defaults.
  status: open
  source_lanes:
  - s4-admission-scheduler
- id: literalkits-I03
  kind: ambiguity
  claim: DocgenLaneMode mixes absence of a scheduled lane with affected and full scope choices.
  evidence: packages/tooling/tool/cli/src/commands/Ci/CiLane.ts:242 — 'export const DOCGEN_LANE_MODE_VALUES
    = ["none", "affected", "full"] as const;'
  suggested_disposition: Model lane omission separately from Scope before mapping affected or full to
    canonical scope members.
  status: open
  source_lanes:
  - s4-literalkits
- id: literalkits-I09
  kind: ambiguity
  claim: DocgenProofManifestStatus says current, missing, or stale without binding that status to CacheEpoch
    and TreeState.
  evidence: packages/tooling/tool/docgen/src/ProofManifest.ts:111-114 — 'LiteralKit(["current", "missing",
    "stale"])'
  suggested_disposition: Treat these as manifest observations, not VerificationEvidence validity, until
    epoch and tree provenance are attached.
  status: open
  source_lanes:
  - s4-literalkits
- id: fallow-laws-I02
  kind: ambiguity
  claim: Fallow's new-only audit gate attributes findings against a base but does not explicitly say that
    the execution itself has AffectedScope.
  evidence: 'standards/fallow.pilot.inventory.jsonc:67-73 — ''"command": "bun run fallow:audit -- --base
    origin/main --gate new-only"'''
  suggested_disposition: Distinguish result-attribution scope from execution scope when ratifying the
    candidate hasScope assertion.
  status: open
  source_lanes:
  - s4-fallow-laws
- id: literalkits-I11
  kind: ambiguity
  claim: GateArtifactKind names cached file forms, which are information artifacts and are not automatically
    VerificationEvidence.
  evidence: packages/tooling/tool/cli/src/commands/Yeet/internal/GateStaleness.ts:88-91 — 'LiteralKit(["baseline",
    "inventory", "manifest", "snapshot", "tsbuildinfo"])'
  suggested_disposition: Require evidence-production and epoch-tree provenance before mapping an artifact
    instance to VerificationEvidence.
  status: open
  source_lanes:
  - s4-literalkits
- id: literalkits-I21
  kind: ambiguity
  claim: GithubCheckLaneRunStatus not-run-early-stop establishes non-execution but does not state whether
    the cancellation is clean or dirty.
  evidence: packages/tooling/tool/cli/src/commands/Quality/Quality.schemas.ts:895-898 — 'LiteralKit(["passed",
    "failed", "not-run-early-stop"])'
  suggested_disposition: Map it to execution state only; derive CancelClass independently from step mutability.
  status: open
  source_lanes:
  - s4-literalkits
- id: literalkits-I19
  kind: ambiguity
  claim: GithubCheckMode and RepoProofSurface overlap on four spellings while denoting a command mode
    and a named proof surface respectively.
  evidence: packages/tooling/tool/cli/src/internal/repo-run/RepoRun.proofs.ts:74-77 — 'LiteralKit(["cheap-gates",
    "quality", "review-fix", "pre-push"])'
  suggested_disposition: Keep source_domain-qualified members distinct and ratify the mode-to-surface
    relation.
  status: open
  source_lanes:
  - s4-literalkits
- id: literalkits-I12
  kind: ambiguity
  claim: ProofLockDisposition classifies fail-closed actions on proof-lock generations, not SeatGrant
    lifecycle states.
  evidence: packages/tooling/tool/cli/src/commands/Yeet/internal/ProofState.ts:126-129 — 'LiteralKit(["replace-stale",
    "refuse-active", "refuse-legacy", "refuse-unreadable"])'
  suggested_disposition: Keep proof-lock actions distinct from scheduler grants and model only a checked
    relation to MachineProofLock contention.
  status: open
  source_lanes:
  - s4-literalkits
- id: admission-scheduler-I06
  kind: ambiguity
  claim: Scheduler quarantine classifies undecodable ticket or lease files as corrupt records; dead owners
    are reaped through a separate branch.
  evidence: 'packages/tooling/tool/cli/src/internal/repo-run/QualityScheduler.ts:518-522 — ''outcome.kind
    === "malformed" ? quarantineEntry ... : outcome.kind === "dead" ? reapDeadAdmissionEntry'''
  suggested_disposition: Model corrupt-record isolation separately from Agent state and StarvationException;
    do not mint QuarantineException.
  status: open
  source_lanes:
  - s4-admission-scheduler
- id: turbo-tasks-I03
  kind: ambiguity
  claim: Several tasks omit explicit inputs, outputs, or cache declarations, so their effective values
    depend on defaults not stated in this corpus.
  evidence: 'turbo.json:227-229 — ''"transit": {'''
  suggested_disposition: Resolve Turborepo default semantics before asserting effective hash or cache
    posture for omitted fields.
  status: open
  source_lanes:
  - s4-turbo-tasks
- id: admission-scheduler-I01
  kind: ambiguity
  claim: Source AdmissionRequest is an in-memory run-mode DTO, whereas ontology SeatRequest denotes the
    durable queued YeetAdmissionTicket.
  evidence: packages/tooling/tool/cli/src/internal/repo-run/QualityScheduler.schemas.ts:285-320 — 'One
    admission request derived from a Yeet run mode.'
  suggested_disposition: Map SeatRequest only to YeetAdmissionTicket and keep AdmissionRequest as an implementation
    artifact.
  status: open
  source_lanes:
  - s4-admission-scheduler
- id: fallow-laws-I09
  kind: ambiguity
  claim: The allowed sources do not establish which AssuranceTier, if any, requires a Fallow lane.
  evidence: 'standards/fallow.pilot.inventory.jsonc:125-136 — ''"blockingQualityGate": "none-proven-in-live-pre-push"'''
  suggested_disposition: Do not mint Fallow requiresLane facts here; reconcile tier obligations from the
    separately owned planner and scheduler evidence during S5.
  status: open
  source_lanes:
  - s4-fallow-laws
- id: package-topology-I03
  kind: ambiguity
  claim: The dependencies token at index 2 is an output artifact, not a package name.
  evidence: explorations/beep-ci-operational-ontology/ontology/extraction/s4/inputs/topo-sort.txt:3 —
    'dependencies 2'
  suggested_disposition: Exclude the token from PackageRef candidates and retain this source anomaly for
    S5 review.
  status: open
  source_lanes:
  - s4-package-topology
- id: package-topology-I01
  kind: ambiguity
  claim: The devDependencies token at index 0 is an output artifact, not a package name.
  evidence: explorations/beep-ci-operational-ontology/ontology/extraction/s4/inputs/topo-sort.txt:1 —
    'devDependencies 0'
  suggested_disposition: Exclude the token from PackageRef candidates and retain this source anomaly for
    S5 review.
  status: open
  source_lanes:
  - s4-package-topology
- id: package-topology-I04
  kind: ambiguity
  claim: The optionalDependencies token at index 3 is an output artifact, not a package name.
  evidence: explorations/beep-ci-operational-ontology/ontology/extraction/s4/inputs/topo-sort.txt:4 —
    'optionalDependencies 3'
  suggested_disposition: Exclude the token from PackageRef candidates and retain this source anomaly for
    S5 review.
  status: open
  source_lanes:
  - s4-package-topology
- id: package-topology-I02
  kind: ambiguity
  claim: The peerDependencies token at index 1 is an output artifact, not a package name.
  evidence: explorations/beep-ci-operational-ontology/ontology/extraction/s4/inputs/topo-sort.txt:2 —
    'peerDependencies 1'
  suggested_disposition: Exclude the token from PackageRef candidates and retain this source anomaly for
    S5 review.
  status: open
  source_lanes:
  - s4-package-topology
- id: turbo-tasks-I07
  kind: ambiguity
  claim: The source distinguishes env from passThroughEnv but does not state the latter's cache-key semantics.
  evidence: 'turbo.json:19 — ''"passThroughEnv": ['''
  suggested_disposition: Keep pass-through variables separate from hash-environment variables and verify
    semantics before reasoning about transferability.
  status: open
  source_lanes:
  - s4-turbo-tasks
- id: fallow-laws-I11
  kind: ambiguity
  claim: The supplied boundary-provenance artifact is a schema pointing to a generated config, not an
    instance containing the concrete generated rule rows.
  evidence: 'standards/fallow.boundaries.provenance.schema.json:27-44 — ''"generatedConfigRef": { "const":
    "standards/fallow.boundaries.generated.jsonc" }'''
  suggested_disposition: Treat only the provenance record shape as extractable; do not assert concrete
    boundary-rule A-Box values without an allowed instance source.
  status: open
  source_lanes:
  - s4-fallow-laws
- id: turbo-tasks-I04
  kind: ambiguity
  claim: The three enabled future flags are named and valued but their operational transformations are
    not defined by turbo.json.
  evidence: 'turbo.json:4 — ''"affectedUsingTaskInputs": true,'''
  suggested_disposition: Ratify their denotation against the pinned Turbo schema before formal axiomatization.
  status: open
  source_lanes:
  - s4-turbo-tasks
- id: package-topology-I07
  kind: ambiguity
  claim: The topology name @beep/api-docs has no matching name in the allowed packages/**/package.json
    manifest corpus, so this lane cannot assert its outgoing dependency edges.
  evidence: explorations/beep-ci-operational-ontology/ontology/extraction/s4/inputs/topo-sort.txt:39 —
    '@beep/api-docs 38'
  suggested_disposition: Keep the PackageRef candidate and resolve outgoing dependencies only through
    a future lane contract that admits the package manifest location.
  status: open
  source_lanes:
  - s4-package-topology
- id: package-topology-I16
  kind: ambiguity
  claim: The topology name @beep/architecture-lab-proof has no matching name in the allowed packages/**/package.json
    manifest corpus, so this lane cannot assert its outgoing dependency edges.
  evidence: explorations/beep-ci-operational-ontology/ontology/extraction/s4/inputs/topo-sort.txt:118
    — '@beep/architecture-lab-proof 117'
  suggested_disposition: Keep the PackageRef candidate and resolve outgoing dependencies only through
    a future lane contract that admits the package manifest location.
  status: open
  source_lanes:
  - s4-package-topology
- id: package-topology-I15
  kind: ambiguity
  claim: The topology name @beep/infra has no matching name in the allowed packages/**/package.json manifest
    corpus, so this lane cannot assert its outgoing dependency edges.
  evidence: explorations/beep-ci-operational-ontology/ontology/extraction/s4/inputs/topo-sort.txt:99 —
    '@beep/infra 98'
  suggested_disposition: Keep the PackageRef candidate and resolve outgoing dependencies only through
    a future lane contract that admits the package manifest location.
  status: open
  source_lanes:
  - s4-package-topology
- id: package-topology-I09
  kind: ambiguity
  claim: The topology name @beep/oip-web has no matching name in the allowed packages/**/package.json
    manifest corpus, so this lane cannot assert its outgoing dependency edges.
  evidence: explorations/beep-ci-operational-ontology/ontology/extraction/s4/inputs/topo-sort.txt:56 —
    '@beep/oip-web 55'
  suggested_disposition: Keep the PackageRef candidate and resolve outgoing dependencies only through
    a future lane contract that admits the package manifest location.
  status: open
  source_lanes:
  - s4-package-topology
- id: package-topology-I12
  kind: ambiguity
  claim: The topology name @beep/practice-kg-mcp has no matching name in the allowed packages/**/package.json
    manifest corpus, so this lane cannot assert its outgoing dependency edges.
  evidence: explorations/beep-ci-operational-ontology/ontology/extraction/s4/inputs/topo-sort.txt:66 —
    '@beep/practice-kg-mcp 65'
  suggested_disposition: Keep the PackageRef candidate and resolve outgoing dependencies only through
    a future lane contract that admits the package manifest location.
  status: open
  source_lanes:
  - s4-package-topology
- id: package-topology-I14
  kind: ambiguity
  claim: The topology name @beep/professional-desktop has no matching name in the allowed packages/**/package.json
    manifest corpus, so this lane cannot assert its outgoing dependency edges.
  evidence: explorations/beep-ci-operational-ontology/ontology/extraction/s4/inputs/topo-sort.txt:90 —
    '@beep/professional-desktop 89'
  suggested_disposition: Keep the PackageRef candidate and resolve outgoing dependencies only through
    a future lane contract that admits the package manifest location.
  status: open
  source_lanes:
  - s4-package-topology
- id: package-topology-I06
  kind: ambiguity
  claim: The topology name @beep/root has no matching name in the allowed packages/**/package.json manifest
    corpus, so this lane cannot assert its outgoing dependency edges.
  evidence: explorations/beep-ci-operational-ontology/ontology/extraction/s4/inputs/topo-sort.txt:10 —
    '@beep/root 9'
  suggested_disposition: Keep the PackageRef candidate and resolve outgoing dependencies only through
    a future lane contract that admits the package manifest location.
  status: open
  source_lanes:
  - s4-package-topology
- id: package-topology-I11
  kind: ambiguity
  claim: The topology name @beep/scratchpad has no matching name in the allowed packages/**/package.json
    manifest corpus, so this lane cannot assert its outgoing dependency edges.
  evidence: explorations/beep-ci-operational-ontology/ontology/extraction/s4/inputs/topo-sort.txt:64 —
    '@beep/scratchpad 63'
  suggested_disposition: Keep the PackageRef candidate and resolve outgoing dependencies only through
    a future lane contract that admits the package manifest location.
  status: open
  source_lanes:
  - s4-package-topology
- id: package-topology-I08
  kind: ambiguity
  claim: The topology name @beep/semantica has no matching name in the allowed packages/**/package.json
    manifest corpus, so this lane cannot assert its outgoing dependency edges.
  evidence: explorations/beep-ci-operational-ontology/ontology/extraction/s4/inputs/topo-sort.txt:54 —
    '@beep/semantica 53'
  suggested_disposition: Keep the PackageRef candidate and resolve outgoing dependencies only through
    a future lane contract that admits the package manifest location.
  status: open
  source_lanes:
  - s4-package-topology
- id: package-topology-I10
  kind: ambiguity
  claim: The topology name @beep/storybook has no matching name in the allowed packages/**/package.json
    manifest corpus, so this lane cannot assert its outgoing dependency edges.
  evidence: explorations/beep-ci-operational-ontology/ontology/extraction/s4/inputs/topo-sort.txt:57 —
    '@beep/storybook 56'
  suggested_disposition: Keep the PackageRef candidate and resolve outgoing dependencies only through
    a future lane contract that admits the package manifest location.
  status: open
  source_lanes:
  - s4-package-topology
- id: package-topology-I13
  kind: ambiguity
  claim: The topology name @beep/trustgraph-workbench has no matching name in the allowed packages/**/package.json
    manifest corpus, so this lane cannot assert its outgoing dependency edges.
  evidence: explorations/beep-ci-operational-ontology/ontology/extraction/s4/inputs/topo-sort.txt:80 —
    '@beep/trustgraph-workbench 79'
  suggested_disposition: Keep the PackageRef candidate and resolve outgoing dependencies only through
    a future lane contract that admits the package manifest location.
  status: open
  source_lanes:
  - s4-package-topology
- id: package-topology-I17
  kind: ambiguity
  claim: The topology name @beep/tsgo-shim has no matching name in the allowed packages/**/package.json
    manifest corpus, so this lane cannot assert its outgoing dependency edges.
  evidence: explorations/beep-ci-operational-ontology/ontology/extraction/s4/inputs/topo-sort.txt:123
    — '@beep/tsgo-shim 122'
  suggested_disposition: Keep the PackageRef candidate and resolve outgoing dependencies only through
    a future lane contract that admits the package manifest location.
  status: open
  source_lanes:
  - s4-package-topology
- id: turbo-tasks-I06
  kind: ambiguity
  claim: Turbo selector tokens and negated globs are configuration-language literals, not filesystem individuals.
  evidence: 'turbo.json:38 — ''"inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/.env*", "!.beep/**"],'''
  suggested_disposition: Preserve selector spellings as xsd:string facts until a selector grammar is separately
    modeled.
  status: open
  source_lanes:
  - s4-turbo-tasks
- id: yeet-internals-I01
  kind: ambiguity
  claim: YeetProofTier selects local proof plans, but this corpus does not state a one-to-one mapping
    from those source tiers to AssuranceTier individuals.
  evidence: packages/tooling/tool/cli/src/commands/Yeet/internal/Planner.ts:380-385 — 'YeetProofTier.$match(tier,
    {'
  suggested_disposition: Ratify an explicit source-tier-to-assurance mapping in S5, preserving YeetProofTier
    and AssuranceTierId as distinct domains.
  status: open
  source_lanes:
  - s4-yeet-internals
- id: admission-scheduler-I07
  kind: assumption
  claim: A stale heartbeat is only a suspicion diagnostic and does not release or invalidate an active
    grant.
  evidence: goals/ship-velocity/research/d1-admission-scheduler.md:64-67 — 'stale heartbeats mark suspicion
    but never kill.'
  suggested_disposition: Derive live grant membership from pid plus process-start liveness and lease presence,
    not heartbeat age.
  status: open
  source_lanes:
  - s4-admission-scheduler
- id: turbo-tasks-I01
  kind: assumption
  claim: A Turbo task declaration does not by itself prove that the task denotes an in-scope VerificationLane.
  evidence: 'turbo.json:237 — ''"persistent": true,'''
  suggested_disposition: Ratify task individuals against actual yeet/CI use; reject persistent or support
    tasks that are implementation artifacts only.
  status: open
  source_lanes:
  - s4-turbo-tasks
- id: fallow-laws-I12
  kind: assumption
  claim: AGENTS.md names merge gates and merge-ready monitoring but not TierCiMergeGreen; the requiresLane
    fact below is a candidate frozen-vocabulary mapping.
  evidence: 'AGENTS.md:52-56 — ''run `bun run beep yeet monitor` until it reports `merge-ready: yes`'''
  suggested_disposition: S5 should ratify the mapping to TierCiMergeGreen or leave the source obligation
    unmapped.
  status: open
  source_lanes:
  - s4-fallow-laws
- id: literalkits-I20
  kind: assumption
  claim: FleetCheckoutKind distinguishes clone from linked worktree but supplies no deployed admission-exclusion
    identity.
  evidence: packages/tooling/tool/cli/src/commands/Worktree/Worktree.schemas.ts:616-619 — 'LiteralKit(["clone",
    "linked-worktree"])'
  suggested_disposition: Use the members only to constrain Checkout; never infer origin or checkout exclusion
    from them.
  status: open
  source_lanes:
  - s4-literalkits
- id: admission-scheduler-I05
  kind: assumption
  claim: PublishWork is deployed as a schema member and weight default but has no caller; publish proofs
    request FullProofWork at PublishPriority.
  evidence: goals/ship-velocity/research/d1-admission-scheduler.md:41-46 — 'The publish kind (1 token)
    remains in the schema ... both follow-ups.'
  suggested_disposition: Retain the closed-domain member while marking it reserved and uninstantiated
    by current admission call sites.
  status: open
  source_lanes:
  - s4-admission-scheduler
- id: fallow-laws-I01
  kind: assumption
  claim: The named sources do not declare frozen LaneKind mappings; command-shaped activities and aggregate
    gates are mapped below only as candidate interpretations.
  evidence: AGENTS.md:39-41 — 'Yeet is the canonical repo-quality path'
  suggested_disposition: S5 should ratify or reject each ScriptLane and CollectedGateLane fact independently.
  status: open
  source_lanes:
  - s4-fallow-laws
- id: fallow-laws-I03
  kind: assumption
  claim: The pilot inventory is a dated snapshot, so its tool version, issue counts, timings, and advisory
    status are not current-policy facts.
  evidence: 'standards/fallow.pilot.inventory.jsonc:2-7 — ''"recordedAt": "2026-06-08"'''
  suggested_disposition: If retained in an A-Box, identify the snapshot by date and never project its
    measurements onto the pinned corpus as current state.
  status: open
  source_lanes:
  - s4-fallow-laws
- id: admission-scheduler-I02
  kind: missing-concept
  claim: Admission deletes the ticket after creating the lease, so no persisted ticket-to-lease edge survives
    in the live store.
  evidence: 'packages/tooling/tool/cli/src/internal/repo-run/QualityScheduler.ts:961-967 — ''yield* fs.remove(ticketPath,
    { force: true }).pipe(Effect.ignore);'''
  suggested_disposition: Do not propose grantedFrom; model queued tickets and active leases as disjoint
    snapshot carriers unless telemetry is extended.
  status: open
  source_lanes:
  - s4-admission-scheduler
- id: literalkits-I08
  kind: missing-concept
  claim: QualityHardwareProfile and CiRunnerClass condition scheduling measurements, but the frozen glossary
    has no execution-environment bearer or attachment property.
  evidence: packages/tooling/tool/cli/src/commands/Quality/Quality.schemas.ts:341-344 — 'LiteralKit(["current",
    "workstation", "ci"])'
  suggested_disposition: Ratify an environment model only if cost calibration requires it; define the
    context-dependent current member explicitly.
  status: open
  source_lanes:
  - s4-literalkits
- id: admission-scheduler-I04
  kind: missing-concept
  claim: The deployed aging window promotes sort rank but does not provide the declared starvationBoundMs
    required by CQ-023.
  evidence: 'packages/tooling/tool/cli/src/internal/repo-run/QualityScheduler.ts:600-601 — ''nowMillis
    - ticket.enqueuedAtMillis >= config.publishAgingSeconds * 1000 ? 0 : 1'''
  suggested_disposition: Keep publishAgingSeconds as a policy fact and source starvationBoundMs from the
    separately declared operational policy.
  status: open
  source_lanes:
  - s4-admission-scheduler
- id: fallow-laws-I05
  kind: missing-concept
  claim: The sourceClass and enforcementScope enum members have no corresponding frozen literal domain,
    so this lane cannot propose them as unqualified members.
  evidence: 'standards/fallow.boundaries.provenance.schema.json:54-59 — ''"sourceClass": { "enum": ["manifest-derived",
    "doctrine-pinned"] }'''
  suggested_disposition: Use the R5 extend and re-run loop only if a Must or Should CQ, or a named semantic-support
    dependency, licenses qualified source domains.
  status: open
  source_lanes:
  - s4-fallow-laws
- id: admission-scheduler-I09
  kind: missing-concept
  claim: YeetAdmissionLease carries originKey but no contended-resource identifier, so contendsFor is
    not a direct lease-record lookup.
  evidence: 'packages/tooling/tool/cli/src/internal/repo-run/QualityScheduler.schemas.ts:111-120 — ''priority:
    AdmissionPriority, originKey: S.String, checkoutRoot: S.String'''
  suggested_disposition: Define an explicit ETL derivation from admission kind and call-site gate, or
    add a resource identifier before treating contendsFor as stored.
  status: open
  source_lanes:
  - s4-admission-scheduler
- id: fallow-laws-I10
  kind: missing-cq
  claim: Architecture promotion gates and migration buckets are binding governance laws, but no Must or
    Should CQ requires their taxonomy as CI-operations terms.
  evidence: standards/ARCHITECTURE.md:1939-1947 — 'The migration posture has five buckets'
  suggested_disposition: Retain the laws as source evidence; admit ontology terms only after a CQ or checkable
    support dependency names the projection decision they serve.
  status: open
  source_lanes:
  - s4-fallow-laws
- id: literalkits-I16
  kind: missing-cq
  claim: Changeset applicability, check registration, optional-artifact reads, and monitor termination
    are operational control states without a CQ warrant.
  evidence: packages/tooling/tool/cli/src/commands/Quality/ChangesetStatus.ts:154-157 — 'LiteralKit(["lab-exempt",
    "enforced"])'
  suggested_disposition: Leave these values in runtime telemetry unless a Must or Should CQ needs the
    control-state distinction.
  status: open
  source_lanes:
  - s4-literalkits
- id: literalkits-I02
  kind: missing-cq
  claim: CiLaneReplay carries exact, approximate, and no-replay fidelity, but no Must or Should CQ asks
    for replay fidelity as a T-Box term.
  evidence: packages/tooling/tool/cli/src/commands/Ci/CiLane.ts:110 — 'export const CI_LANE_REPLAY_VALUES
    = ["exact", "approximate", "none"] as const;'
  suggested_disposition: Retain as source telemetry unless a CQ needs replay fidelity for tier or transferability
    decisions.
  status: open
  source_lanes:
  - s4-literalkits
- id: literalkits-I10
  kind: missing-cq
  claim: Docgen proof-manifest format and schema-version markers are verification representations with
    no CQ or support dependency licensing T-Box members.
  evidence: packages/tooling/tool/docgen/src/ProofManifest.ts:37-39 — 'LiteralKit(["docgen-proof-manifest"])'
  suggested_disposition: Keep the format markers in ingestion metadata rather than minting ontology terms.
  status: open
  source_lanes:
  - s4-literalkits
- id: fallow-laws-I07
  kind: missing-cq
  claim: Fallow entry roots, exclusions, dependency waivers, health ceilings, audit mode, and suppression
    policy are configuration observations but no Must or Should CQ asks for them.
  evidence: '.fallowrc.jsonc:302-312 — ''"maxCognitive": 8'''
  suggested_disposition: Keep these values outside the T-Box and A-Box until a qualifying CQ or checkable
    support dependency requires them.
  status: open
  source_lanes:
  - s4-fallow-laws
- id: literalkits-I14
  kind: missing-cq
  claim: Fallow envelope status and defect domains describe producer and report handling, not a CQ-licensed
    failure or invalidation taxonomy.
  evidence: packages/tooling/tool/cli/src/commands/Quality/internal/FallowEnvelope.schema.ts:99-107 —
    '"tool-failed", "invalid-json", "invalid-report", "base-resolution-failed"'
  suggested_disposition: Keep them as lane-output diagnostics until a decision CQ requires their distinctions.
  status: open
  source_lanes:
  - s4-literalkits
- id: literalkits-I05
  kind: missing-cq
  claim: GitHub lane stage, wave, and failure-policy labels configure execution but are not queried as
    ontology terms by a Must or Should CQ.
  evidence: packages/tooling/tool/cli/src/commands/Quality/Quality.schemas.ts:777-806 — 'LiteralKit(["fail-fast",
    "collect-all"]) ... LiteralKit(["preflight", "heavy", "test", "documentation"])'
  suggested_disposition: Keep them as projection configuration unless a CQ requires their semantics beyond
    materialized stepIndex.
  status: open
  source_lanes:
  - s4-literalkits
- id: literalkits-I13
  kind: missing-cq
  claim: QualityIssue category, severity, parser-confidence, and attribution domains normalize diagnostics
    but are not required by any Must or Should CQ.
  evidence: packages/tooling/tool/cli/src/commands/Yeet/Yeet.schemas.ts:29-49 — 'export const QualityIssueCategory
    = LiteralKit(['
  suggested_disposition: Retain them in diagnostic payloads unless a CQ explicitly requires issue taxonomy
    or attribution.
  status: open
  source_lanes:
  - s4-literalkits
- id: admission-scheduler-I10
  kind: missing-cq
  claim: suspectAfterSeconds and progressSeconds are deployed diagnostic cadence knobs with no Must or
    Should CQ warrant.
  evidence: packages/tooling/tool/cli/src/internal/repo-run/QualityScheduler.schemas.ts:274-276 — 'suspectAfterSeconds
    ... progressSeconds'
  suggested_disposition: Keep these values outside the ontology unless requirements add a decision that
    consumes them.
  status: open
  source_lanes:
  - s4-admission-scheduler
- id: yeet-internals-I02
  kind: missing-cq
  claim: The attempt journal enforces a 50-attempt retention limit, but no Must/Should CQ admits a retention-policy
    term or fact.
  evidence: packages/tooling/tool/cli/src/commands/Yeet/internal/AttemptJournal.ts:24-25 — 'const RETAINED_ATTEMPTS
    = 50;'
  suggested_disposition: Either classify retention as implementation-only or extend the requirements if
    CQ window completeness must expose this limit.
  status: open
  source_lanes:
  - s4-yeet-internals
- id: turbo-tasks-I05
  kind: missing-cq
  claim: The audit task's new-only output-log mode has no Must/Should CQ warrant.
  evidence: 'turbo.json:225 — ''"outputLogs": "new-only"'''
  suggested_disposition: Leave output-log presentation outside the T-Box unless a scheduling or evidence
    decision requires it.
  status: open
  source_lanes:
  - s4-turbo-tasks
- id: package-topology-I05
  kind: missing-cq
  claim: The numeric topology indexes are observed, but no Must/Should CQ licenses a topological-index
    property.
  evidence: explorations/beep-ci-operational-ontology/ontology/extraction/s4/inputs/topo-sort.txt:5 —
    '@beep/hubspot 4'
  suggested_disposition: Retain the indexes only as capture ordering unless ontology requirements add
    a decision that needs them; do not mint a property in this lane.
  status: open
  source_lanes:
  - s4-package-topology
- id: fallow-laws-I08
  kind: missing-cq
  claim: The required introduced/inherited/unrelated/environment-only failure attribution controls repair
    behavior, but no Must or Should CQ queries that attribution.
  evidence: AGENTS.md:49-51 — 'Attribute verification failures before repairing — introduced / inherited
    / unrelated / environment-only'
  suggested_disposition: Add a decision CQ only if the schedule projection must choose repair, rebase,
    or report from this attribution; otherwise retain it as procedural law.
  status: open
  source_lanes:
  - s4-fallow-laws
- id: turbo-tasks-I02
  kind: schema-conflict
  claim: A task-level cache boolean is not a member of the closed CachePosture domain.
  evidence: 'turbo.json:82 — ''"cache": false,'''
  suggested_disposition: Keep taskCacheEnabled separate from LocalOnly, LocalWriteRemoteRead, and CallerControlled.
  status: open
  source_lanes:
  - s4-turbo-tasks
- id: affected-typenames-I02
  kind: schema-conflict
  claim: An affected task may omit its reason, so this decoder does not guarantee the typed outcome completeness
    that CQ-019 expects for every AffectedComputation.
  evidence: 'packages/tooling/tool/cli/src/commands/Yeet/internal/TurboQuery.ts:49-55 — ''reason: S.optionalKey(TurboQueryAffectedReason),'''
  suggested_disposition: Define an ingestion rule for missing task reasons before asserting a total hasAffectedOutcome
    relation.
  status: open
  source_lanes:
  - s4-affected-typenames
- id: merge-conflict-candidate-dependsOn
  kind: schema-conflict
  claim: candidate 'dependsOn' (property) has 2 conflicting definitions across lanes ['s4-fallow-laws',
    's4-package-topology'] — never silently merged; S5 rules
  evidence: ontology/extraction/s4/CANDIDATES.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks or reconciles one definition
  status: open
- id: literalkits-I04
  kind: schema-conflict
  claim: FallowQualityFeatureFamily contains matrix-only members beyond the smaller FallowFeatureFamily
    implemented by the quality wrapper.
  evidence: packages/tooling/tool/cli/src/commands/Quality/Quality.schemas.ts:533-543 — '"runtime-coverage",
    "editor-mcp-hooks"'
  suggested_disposition: Preserve the two source domains separately and ratify which matrix rows denote
    deployed VerificationLanes.
  status: open
  source_lanes:
  - s4-literalkits
- id: merge-conflict-fact-LaneAudit-hasHashEnvironmentVariable
  kind: schema-conflict
  claim: literal fact (LaneAudit, hasHashEnvironmentVariable) has 15 conflicting values across lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneAudit-hasTaskInputPattern
  kind: schema-conflict
  claim: literal fact (LaneAudit, hasTaskInputPattern) has 6 conflicting values across lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneAudit-hasTaskOutputPattern
  kind: schema-conflict
  claim: literal fact (LaneAudit, hasTaskOutputPattern) has 12 conflicting values across lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneBuild-hasHashEnvironmentVariable
  kind: schema-conflict
  claim: literal fact (LaneBuild, hasHashEnvironmentVariable) has 12 conflicting values across lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneBuild-hasTaskInputPattern
  kind: schema-conflict
  claim: literal fact (LaneBuild, hasTaskInputPattern) has 3 conflicting values across lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneBuild-hasTaskOutputPattern
  kind: schema-conflict
  claim: literal fact (LaneBuild, hasTaskOutputPattern) has 12 conflicting values across lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneCheck-hasTaskInputPattern
  kind: schema-conflict
  claim: literal fact (LaneCheck, hasTaskInputPattern) has 3 conflicting values across lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneCodegen-hasTaskInputPattern
  kind: schema-conflict
  claim: literal fact (LaneCodegen, hasTaskInputPattern) has 2 conflicting values across lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneCoverage-hasHashEnvironmentVariable
  kind: schema-conflict
  claim: literal fact (LaneCoverage, hasHashEnvironmentVariable) has 4 conflicting values across lanes
    ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneCoverage-hasTaskInputPattern
  kind: schema-conflict
  claim: literal fact (LaneCoverage, hasTaskInputPattern) has 3 conflicting values across lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneDocgen-hasTaskInputPattern
  kind: schema-conflict
  claim: literal fact (LaneDocgen, hasTaskInputPattern) has 2 conflicting values across lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneDocgen-hasTaskOutputPattern
  kind: schema-conflict
  claim: literal fact (LaneDocgen, hasTaskOutputPattern) has 2 conflicting values across lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneLint-hasTaskInputPattern
  kind: schema-conflict
  claim: literal fact (LaneLint, hasTaskInputPattern) has 7 conflicting values across lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneLintFix-hasTaskInputPattern
  kind: schema-conflict
  claim: literal fact (LaneLintFix, hasTaskInputPattern) has 4 conflicting values across lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneStorybookBuild-hasTaskInputPattern
  kind: schema-conflict
  claim: literal fact (LaneStorybookBuild, hasTaskInputPattern) has 10 conflicting values across lanes
    ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneTest-hasPassThroughEnvironmentVariable
  kind: schema-conflict
  claim: literal fact (LaneTest, hasPassThroughEnvironmentVariable) has 2 conflicting values across lanes
    ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneTest-hasTaskInputPattern
  kind: schema-conflict
  claim: literal fact (LaneTest, hasTaskInputPattern) has 3 conflicting values across lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneTestIntegration-hasHashEnvironmentVariable
  kind: schema-conflict
  claim: literal fact (LaneTestIntegration, hasHashEnvironmentVariable) has 5 conflicting values across
    lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneTestIntegration-hasTaskInputPattern
  kind: schema-conflict
  claim: literal fact (LaneTestIntegration, hasTaskInputPattern) has 3 conflicting values across lanes
    ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneTestIntegrationParallel-hasHashEnvironmentVariable
  kind: schema-conflict
  claim: literal fact (LaneTestIntegrationParallel, hasHashEnvironmentVariable) has 5 conflicting values
    across lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneTestIntegrationParallel-hasTaskInputPattern
  kind: schema-conflict
  claim: literal fact (LaneTestIntegrationParallel, hasTaskInputPattern) has 3 conflicting values across
    lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneTestIntegrationSerial-hasTaskInputPattern
  kind: schema-conflict
  claim: literal fact (LaneTestIntegrationSerial, hasTaskInputPattern) has 3 conflicting values across
    lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneTestProperty-hasHashEnvironmentVariable
  kind: schema-conflict
  claim: literal fact (LaneTestProperty, hasHashEnvironmentVariable) has 2 conflicting values across lanes
    ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneTestProperty-hasTaskInputPattern
  kind: schema-conflict
  claim: literal fact (LaneTestProperty, hasTaskInputPattern) has 4 conflicting values across lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-LaneTestStorybook-hasTaskInputPattern
  kind: schema-conflict
  claim: literal fact (LaneTestStorybook, hasTaskInputPattern) has 8 conflicting values across lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-TurboConfiguration469136d2a872-hasGlobalHashEnvironmentVariable
  kind: schema-conflict
  claim: literal fact (TurboConfiguration469136d2a872, hasGlobalHashEnvironmentVariable) has 2 conflicting
    values across lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-TurboConfiguration469136d2a872-hasGlobalInputPattern
  kind: schema-conflict
  claim: literal fact (TurboConfiguration469136d2a872, hasGlobalInputPattern) has 6 conflicting values
    across lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: merge-conflict-fact-TurboConfiguration469136d2a872-hasGlobalPassThroughEnvironmentVariable
  kind: schema-conflict
  claim: literal fact (TurboConfiguration469136d2a872, hasGlobalPassThroughEnvironmentVariable) has 11
    conflicting values across lanes ['s4-turbo-tasks']
  evidence: ontology/extraction/s4/FACTS.yaml — the variants sit adjacent under one merge key
  suggested_disposition: S5 picks the deployed value with evidence
  status: open
- id: literalkits-I15
  kind: schema-conflict
  claim: PrCloseoutGateStatus mixes outcome states with written, which denotes artifact production rather
    than pass or block state.
  evidence: packages/tooling/tool/cli/src/commands/Yeet/internal/closeout/Closeout.schemas.ts:66-69 —
    'LiteralKit(["passed", "blocked", "unknown", "written"])'
  suggested_disposition: Split evidence production from gate outcome before any mapping to VerificationEvidence
    or ExecutionState.
  status: open
  source_lanes:
  - s4-literalkits
- id: admission-scheduler-I03
  kind: schema-conflict
  claim: Remaining capacity before grant is evaluated transiently but is not persisted on YeetAdmissionLease
    as capacityAtAdmissionTokens.
  evidence: packages/tooling/tool/cli/src/internal/repo-run/QualityScheduler.ts:661-664 — 'activeTokenTotal(state)
    + ticket.weightTokens <= capacityTokens'
  suggested_disposition: Require ETL or new telemetry to capture the pre-grant remainder; never treat
    a post-grant snapshot as the carrier.
  status: open
  source_lanes:
  - s4-admission-scheduler
- id: yeet-internals-I05
  kind: schema-conflict
  claim: RemoteReadTurboCache accepts the whole two-member TurboCacheMode schema even though the resolver
    constructs remote-read only from local:rw,remote:r.
  evidence: 'packages/tooling/tool/cli/src/internal/cli/TurboCache.ts:397-402 — ''mode: TurboCacheMode,'''
  suggested_disposition: Constrain or validate the remote-read variant to LocalWriteRemoteRead before
    using decoded plans as CachePosture assertions.
  status: open
  source_lanes:
  - s4-yeet-internals
- id: literalkits-I07
  kind: schema-conflict
  claim: RepoGraphHealthStatus is a trust-level enum and does not state the typed FailOpenOutcome or FailClosedOutcome
    required by CQ-019.
  evidence: packages/tooling/tool/cli/src/internal/repo-run/RepoRun.models.ts:154-157 — 'LiteralKit(["ok",
    "warning", "error"])'
  suggested_disposition: Require a deterministic outcome mapping before graph-health status can affect
    scopedByComputation.
  status: open
  source_lanes:
  - s4-literalkits
- id: literalkits-I17
  kind: schema-conflict
  claim: RepoPlanStepResume permits fingerprint-based skipping without stating the CacheEpoch by TreeState
    validity conditions required for reusable evidence.
  evidence: packages/tooling/tool/cli/src/internal/repo-run/RepoRun.models.ts:126-129 — 'LiteralKit(["never",
    "fingerprint-match"])'
  suggested_disposition: Treat fingerprint-match as a candidate carrier only; prohibit direct proof-validity
    equivalence until the fingerprint semantics are audited.
  status: open
  source_lanes:
  - s4-literalkits
- id: literalkits-I06
  kind: schema-conflict
  claim: RepoPlanStepScope uses package, repo, and git, which is not the frozen ScopeKind domain of full,
    affected, filter, and shard scopes.
  evidence: packages/tooling/tool/cli/src/internal/repo-run/RepoRun.models.ts:98-101 — 'LiteralKit(["package",
    "repo", "git"])'
  suggested_disposition: Do not equate these domains; define a checked mapping or retain RepoPlanStepScope
    as implementation metadata.
  status: open
  source_lanes:
  - s4-literalkits
- id: literalkits-I18
  kind: schema-conflict
  claim: Source flake and infrastructure classifiers name failure signatures that are not visibly normalized
    to the frozen failedStepId by failureKind identity.
  evidence: packages/tooling/tool/cli/src/internal/github/JobShape.ts:76-80 — 'LiteralKit(["setup-5xx",
    "runner-loss", "install-failure"])'
  suggested_disposition: Ratify a mapping or a separate infrastructure-signature model; do not silently
    merge the identities.
  status: open
  source_lanes:
  - s4-literalkits
- id: affected-typenames-I01
  kind: schema-conflict
  claim: The affected-reason decoder admits an unconstrained typename string, so the exact source establishes
    neither a closed member inventory nor the fail-open partition required by CQ-019.
  evidence: 'packages/tooling/tool/cli/src/commands/Yeet/internal/TurboQuery.ts:31-34 — ''__typename:
    S.String,'''
  suggested_disposition: Keep reason members unproposed until a pinned closed typename schema and fail-open
    mapping are supplied from an admitted source.
  status: open
  source_lanes:
  - s4-affected-typenames
- id: fallow-laws-I04
  kind: schema-conflict
  claim: The boundary-provenance schema validates two sourceClass and two enforcementScope values while
    its x-enumValues metadata advertises broader sets.
  evidence: 'standards/fallow.boundaries.provenance.schema.json:54-59,111-125 — ''"enum": ["manifest-derived",
    "doctrine-pinned"] ... "ruleSourceClass": [ ... "external-service" ]'''
  suggested_disposition: Align validation enums and metadata before treating either list as the authoritative
    closed source domain.
  status: open
  source_lanes:
  - s4-fallow-laws
- id: yeet-internals-I04
  kind: schema-conflict
  claim: The lane-level durationMs source schema admits any finite number, while actualWallMs is frozen
    as an xsd:integer measured duration.
  evidence: 'packages/tooling/tool/cli/src/commands/Yeet/internal/Verdict.ts:143-152 — ''durationMs: S.optionalKey(S.Finite),'''
  suggested_disposition: Require ETL validation to a non-negative integer and preserve durationMs as lane-grain
    cost, using elapsedMs only for whole-attempt executions.
  status: open
  source_lanes:
  - s4-yeet-internals
- id: affected-typenames-I03
  kind: schema-conflict
  claim: turboPlanTaskFromAffectedTask drops the decoded reason when it constructs TurboPlanTask, so plan-task
    records cannot carry affected-outcome provenance.
  evidence: packages/tooling/tool/cli/src/commands/Yeet/internal/TurboQuery.ts:221-231 — 'return TurboPlanTask.make({'
  suggested_disposition: Ingest affected outcomes from raw turbo query JSON before this boundary; do not
    derive the A-Box outcome from TurboPlanTask.
  status: open
  source_lanes:
  - s4-affected-typenames
- id: yeet-internals-I03
  kind: schema-conflict
  claim: YeetLaneStatus is a terminal planned-lane status domain and does not directly encode the RunningExecution,
    CompletedExecution, and CancelledExecution lifecycle.
  evidence: packages/tooling/tool/cli/src/commands/Yeet/internal/Verdict.ts:48-53 — 'export const YeetLaneStatus
    = LiteralKit(["passed", "failed", "skipped", "not-run", "not-run-early-stop"]).pipe('
  suggested_disposition: Specify the ETL mapping explicitly, excluding never-executed lanes and sourcing
    running or cancelled state without treating not-run as cancellation.
  status: open
  source_lanes:
  - s4-yeet-internals
