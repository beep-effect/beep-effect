# Round 1 Seat B — cross-artifact coherence

## Findings

- [BLOCKER] `ontology/docs/orsd.md:22,73`; `ontology/docs/scope.md:61-62` — both contracts still make the namespace provisional even though only the term set remains for S8 ratification — evidence: `orsd.md` says `"Namespace (provisional until S8 ratification)"` and `"Namespace provisional; ratify ... at S8"`; `scope.md` says `"Namespace (provisional, ratify at S8 ...)"`; `DECISIONS.md:144-149` says `"https://oip.law/ontology/ci-ops# ... the namespace itself is settled."`
- [WARN] `ontology/docs/orsd.md:6-9` — the purpose-level KPI omits the required tier and epoch coordinates; `scope.md` repairs the omission, but the ORSD alone defines an unqualified distribution — evidence: `"fleet-aggregated P50/P95 time-to-certainty per verification episode"`; `DECISIONS.md:19-22` adds `"tier-relative ... and epoch-relative (a proof is a fact about CacheEpoch × tree)."`
- [BLOCKER] `ontology/docs/scope.md:34-35` — `cheap-gates` appears in an unnamed four-member "tiered membership" list beside the settled three-member CertaintyTier domain; without a separate lane-group domain, S4 would conflate the two — evidence: `"tiered membership (repair / cheap-gates / full proof / CI-merge)"`; `DECISIONS.md:20-22` names `"repair loop / local full proof / CI merge"`; `pre-glossary.csv:7` names `"TierRepairGreen | TierLocalFullProof | TierCiMergeGreen"`.
- [BLOCKER] `ontology/docs/orsd.md:8-9` — the blanket ban on hand-picked levers excludes the amendment's evidence-backed quick levers before the projection exists — evidence: `"levers are derived as projections, not hand-picked"`; `DECISIONS.md:117-118` allows them `"as tagged control interventions"` with KPI time-series before/after.
- [WARN] `ontology/docs/orsd.md:46-48` — the priority census predates CQ-019: the file now has 14 Must CQs, not 13 — evidence: `"13 Must, 4 Should, 1 Could"`; `competency-questions.yaml:364-380` adds `"id: CQ-019"` with `"priority: must_have"`.
- [WARN] `ontology/docs/orsd.md:52-54` — the pre-glossary census is stale: the CSV contains 28 classes and 41 properties, not 26 and 36 — evidence: `"26 candidate classes, 36 candidate properties"`; the CSV now ends with `"AffectedComputation,class,CQ-019"`, `"FailOpenOutcome,class,CQ-019"`, `"hasAffectedOutcome,property,CQ-019"`, and `"scopedByComputation,property,CQ-019"` at lines 71-74.
- [WARN] `ontology/docs/orsd.md:52-54`; `ontology/docs/pre-glossary.csv:27` — the claim that every glossary row traces to a CQ is false until the Agent admission gap is repaired — evidence: ORSD says `"every row traceable to a CQ"`; the glossary says `"Agent,class,UC-001"`.
- [WARN] `ontology/docs/pre-glossary.csv:3-5,7,10,19,22` — six prose unions need closed LiteralKit domains before S4 extraction: `LaneKind`, `ScopeKind`, `CertaintyTierId`, `CostProvenance`, `GrantState`, and `ContendedResourceId` — evidence: `"TurboTaskLane | ScriptLane | CollectedGateLane"`; `"FullRepoScope | AffectedScope | FilterScope | ShardScope"`; `"TierRepairGreen | TierLocalFullProof | TierCiMergeGreen"`; `"measured | census | default"`; `"ActiveGrant | WaitingGrant | ReleasedGrant"`; `"MachineProofLock | TurboDaemon | CpuCores | MemoryPool | SharedCache"`.
- [WARN] `ontology/docs/pre-glossary.csv:5`; `ontology/docs/scope.md:36-37` — the ScopeKind domain has no settled exact name for its affected member, which would make a LiteralKit extraction choose between `AffectedScope` and an affected-from-base spelling — evidence: glossary has `"AffectedScope"`; scope has `"affected-from-base"`.
- [BLOCKER] `ontology/docs/pre-glossary.csv:22,26` — the proposed `ContendedResourceId` domain reuses `SharedCache`, while the glossary declares `SharedCache` as a class; S4 cannot assign one unqualified name both categories without an explicit punning ruling — evidence: `"ContendedResource,class,... MachineProofLock | ... | SharedCache"` and `"SharedCache,class,CQ-015"`.
- [WARN] `ontology/docs/pre-glossary.csv:4`; `DECISIONS.md:73-78` — the WorkUnit note misuses the constitution's term "admission law" for grant cost-bounding — evidence: `"WorkUnit ... cost-bounded by admission law"`; the ruling defines the law as `"every class/property must serve a KPI-derived CQ to enter the T-Box"`.
- [NIT] `ontology/docs/pre-glossary.csv:19-20` — the closed state members are attached to `SeatGrant`, while the `GrantState` row only says `"grant lifecycle state"`; place the union on its own domain — evidence: `"SeatGrant ... states: ActiveGrant | WaitingGrant | ReleasedGrant"` followed by `"GrantState,class,CQ-008,grant lifecycle state"`.
- [WARN] `ontology/docs/pre-glossary.csv:27`; `ontology/docs/use-cases.yaml:61-64` — the Agent note overstates its reach; Agent comes from UC-001, not "all UCs" — evidence: `"a coding agent driving episodes (implicit in all UCs)"`; UC-005's actor is `"Operator; the ontology pipeline itself"`.
- [BLOCKER] `ontology/docs/use-cases.yaml:5-7`; `DECISIONS.md:19-27` — UC-001 makes expected time-to-first-actionable-failure the projection objective, while the constitution makes tier- and epoch-relative time-to-certainty the sole KPI — evidence: `"Compute the WorkUnit sequence minimizing expected time-to-first-actionable-failure"`; the ruling specifies `"P50/P95 of time-to-certainty per verification episode"` and says `"The KPI is a distribution, not a scalar"`.
- [BLOCKER] `ontology/docs/use-cases.yaml:7,13,16` — UC-001 promises an ordered, cost-bounded projected sequence, but no listed CQ header covers ordering and the obviously covering cost-bound CQ-010 is absent from `related_cqs` — evidence: `"Projection returns ordered, cost-bounded WorkUnits (diff-touched first, topo tie-break, ascending cost)"`; related list is `[CQ-001, CQ-002, CQ-003, CQ-004, CQ-013, CQ-018]`; `competency-questions.yaml:195` asks whether an admitted WorkUnit exceeds maximum grant cost.
- [BLOCKER] `ontology/docs/use-cases.yaml:22-30` — UC-002 promises starvation freedom and deficit-round-robin queue drainage, but CQ-008/009/010 headers cover resource holders, checkout exclusion, and maximum grant cost only; no CQ covers per-Agent request queues or fairness — evidence: `"without starvation"`, `"drains per-agent queues fairly (deficit round-robin by cost)"`, and `"related_cqs: [CQ-008, CQ-009, CQ-010]"`.
- [BLOCKER] `ontology/docs/use-cases.yaml:33-45`; `ontology/docs/competency-questions.yaml:364-380` — UC-003 omits its own Must CQ-019 from `related_cqs` — evidence: UC-003 lists `[CQ-004, CQ-005, CQ-011, CQ-014]`; CQ-019 declares `"source_use_case: UC-003"` and asks whether any proof or schedule trusts a fail-open affected set.
- [WARN] `ontology/docs/use-cases.yaml:62-72` — UC-005 promises to rank candidate levers by measured KPI impact, but none of its related CQ headers asks for a cross-lever ranking — evidence: `"rank candidate levers"` and `"Levers are ranked by measured KPI impact"`; related list is `[CQ-007, CQ-011, CQ-012, CQ-013, CQ-016]`.
- [WARN] `ontology/docs/traceability-matrix.csv:3`; `ontology/docs/competency-questions.yaml:39-47` — CQ-002's matrix row overstates direct query usage by adding `VerificationEpisode` and `CertaintyTier`; the query names only `WorkUnit` plus its four listed properties — evidence: matrix has `"WorkUnit;VerificationEpisode;CertaintyTier;admissibleFor;executesLane;targetsTier;requiresLane"`; query has `"rdf:type ciops:WorkUnit"`, `"admissibleFor"`, `"executesLane"`, `"targetsTier"`, and `"requiresLane"`.
- [BLOCKER] `ontology/docs/traceability-matrix.csv:7`; `ontology/docs/competency-questions.yaml:121-128` — CQ-006 omits `provesTree` and `validInEpoch` while adding class IRIs the query never names — evidence: matrix has `"CertaintyTier;Proof;requiresLane;dischargesObligation"`; query also uses `"ciops:provesTree"` and `"ciops:validInEpoch"` and contains no `ciops:CertaintyTier` or `ciops:Proof`.
- [BLOCKER] `ontology/docs/traceability-matrix.csv:11`; `ontology/docs/competency-questions.yaml:201-206` — CQ-010 omits `hasCostEstimate` and `p95Ms` while adding three class IRIs the query never names — evidence: matrix has `"WorkUnit;SeatGrant;ResourceBudget;admittedBy;hasBudget;maxGrantCostMs"`; query also has `"ciops:hasCostEstimate"` and `"ciops:p95Ms"` and no class IRI.
- [BLOCKER] `ontology/docs/traceability-matrix.csv:14`; `ontology/docs/competency-questions.yaml:260-264` — CQ-013 omits `hasCostEstimate` and `p50Ms` while adding two class IRIs the query never names — evidence: matrix has `"FailureSignature;VerificationLane;surfacedByLane"`; query also has `"ciops:hasCostEstimate"` and `"ciops:p50Ms"` and no class IRI.

## Admission-law verdicts

- Scope: admit-with-new-CQ-candidate, serving the `projection function` decision.
  Candidate: "For each projected WorkUnit, which Scope variant and concrete scope operands does it carry?"
  WorkUnit is explicitly `Lane × Scope`; folding or dropping Scope erases the schedule's second axis.
- SeatRequest: admit-with-new-CQ-candidate, serving the fairness requirement inside the `kpi-shape` decision.
  Candidate: "Which queued SeatRequests belong to each Agent, what WorkUnit/cost do they request, and which became SeatGrants?"
  A queued ask precedes admission; folding it into SeatGrant destroys queue wait and starvation evidence.
- Agent: admit-with-new-CQ-candidate, serving UC-001 and the `projection function` decision.
  Candidate: "For an Agent's change set, epoch, and target tier, what ordered WorkUnit sequence does projection return?"
  Agent is the UC-001 actor and the owner needed to distinguish concurrent episodes and projected schedules.

## Checked clean

- `ontology/docs/scope.md:15-21` has the settled fleet distribution and both tier-relative and epoch-relative KPI coordinates.
- `DECISIONS.md:127-137` and `ontology/docs/orsd.md:5-6,33-34` agree on projection inputs/output and the S7 property-test stage.
- `ontology/docs/scope.md:29,44-45` retains tagged control interventions and before/after KPI attribution; the drift is confined to the ORSD's blanket lever sentence.
- The namespace IRI and `ciops:` prefix agree in every named artifact that states them; only the provisional qualifiers flagged above are stale.
- LaneKind labels agree between the glossary and CQ-001's natural-language header; CostProvenance members agree between the glossary and UC-001. Other than the ScopeKind spelling, certainty-tier list, and `SharedCache` category collision above, no member mismatch was found.
- No true glossary duplicates remain after preserving the intended distinctions: VerificationLane/LaneKind, WorkUnit/Scope, and SeatRequest/SeatGrant are entity/classifier, product axes, and pre-/post-admission pairs.
- UC-004's held-tier and missing-obligation flow is directionally covered by CQ-005, CQ-006, CQ-015, and CQ-017.
- Traceability spot-check covered exactly CQ-002, CQ-006, CQ-010, CQ-013, and CQ-016; CQ-016 is exact (`ControlIntervention;landedAt`), and the other four discrepancies are above.
