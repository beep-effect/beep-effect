# Round 2 Seat F — post-fix coherence & reality attack (docs)

## Findings

- [WARN] `ontology/docs/orsd.md` — the post-fix census regressed to the pre-admission property count, so the declared S4 hand-off does not describe its own glossary — evidence: ORSD 6 says "`pre-glossary.csv` — 28 candidate classes, 41 candidate properties, 4 named individuals" (lines 54-58), while `pre-glossary.csv` contains 28 `class`, 46 `property`, and 4 `individual` rows (the five appended properties are `queueWaitMs` through `hasCurrentEpoch`, lines 75-79); `research/reviews/pre-s4/round1-triage.md` itself records "28 classes / 46 properties / 4 individuals."
- [BLOCKER] `research/scripts/kpi_baseline_probe.py` + `research/kpi-baseline-2026-08-27.md` — probe v3 cannot reproduce a fleet distribution keyed by `(checkout, branch)` because it discards checkout identity and merges same-named branches across roots — evidence: the report claims "244 (checkout, branch) pairs" (baseline lines 117-120), but `load()` stores only `"branch": v.get("branch")` (probe lines 94-105) and fleet episode construction uses `by_branch.setdefault(a["branch"], [])` (lines 160-164); no root/checkout field survives. Live inputs prove collisions are real: `chore/semantic-web-dead-surface` exists in beep-effect13 and beep-effect18, and `feat/openai-driver` in beep-effect19 and beep-effect19-openai-driver, allowing one checkout's green to close another's red streak.
- [BLOCKER] `research/scripts/kpi_baseline_probe.py` + `research/kpi-baseline-2026-08-27.md` — the documented fleet-v0.5 command cannot reproduce the section it says its flags "produced" — evidence: baseline lines 90-96 and probe lines 14-17 claim the `--modes verify,repair,publish --exclude-bounces` path produced the published numbers, while the report's green/red rows total all 2,433 raw attempts and separately count 416 bounces (baseline lines 117-130). Probe applies both filters before attempt distributions and failed-step counts (lines 125-158), and never emits checkout count, `(checkout, branch)` count, date window, or the 292-hour sum. A current exact rerun made the mismatch visible: 2,436 loaded → 1,994 selected modes → 1,578 after bounces. Round-1 chronology proves the table predated the flags (`round1-seat-c.md` line 6) and v3 merely "grew" them afterward (`round1-triage.md` fleet-reproducibility disposition); the document does not disclose that its one recipe cannot have generated all retained rows.
- [BLOCKER] `ontology/docs/orsd.md` + pre-intervention baseline — acceptance criterion 2 is currently unsatisfied for the artifact it names after round 1 admitted queue wait — evidence: ORSD requires "The S0 baseline A-Box (attempt journals) can answer CQ-007 and CQ-012 for the 2026-08 window" (lines 62-65), and generated `ontology/tests/cq-007.sparql` requires `queueWaitMs`, `lockWaitMs`, execution, repair, and CI wait on every episode (lines 5-9). But the baseline declares every journal number PRE-INTERVENTION and queue wait only a post-#870 hypothesis (baseline lines 3-13), while probe v3 ingests no request/grant timestamp or component fields (probe lines 94-105); the report calls decomposition a v1 fix (baseline lines 104-113). AC2 needs an explicit pre-#870 mapping or an additional/post-intervention telemetry source, neither of which the named S0 A-Box supplies.
- [BLOCKER] `ontology/docs/s4-lane-contract.md` + `literal-domains.md` — the S4 record/merge schema cannot preserve literal-domain identity for the newly overlapping enums — evidence: `YeetProofTier` has `review-fix`, `AdmissionWorkKind` has `review-fix`, and `AdmissionWorkKind` plus `AdmissionPriority` both have `publish` (origin/main `packages/tooling/tool/cli/src/commands/Yeet/internal/Planner.ts` lines 95-120; `packages/tooling/tool/cli/src/internal/repo-run/QualityScheduler.schemas.ts` lines 22-80). Yet a lane can emit only `{ candidate: <name>, kind: literal-domain-member }` with no `source_domain` (contract lines 81-92), and the orchestrator merges on `(kind, candidate)` (lines 107-110). Unit 4 owns all verification `LiteralKit` sites while units 5/7 re-own these exact files despite "disjoint ownership" (lines 24,31-34). If emitted, identical `review-fix`/`publish` members cannot be distinguished and `(kind, candidate)` would collapse them; unit 7 is insufficient to prevent the confused taxonomy.
- [BLOCKER] `ontology/docs/use-cases.yaml` UC-002 — the deployed-vs-prospective fix lives only in YAML comments; the structured use-case still asserts the prospective algorithm as its current main flow — evidence: `main_flow` says "Scheduler validates ... MaxGrantCost, one grant per checkout" then "drains per-agent queues fairly (deficit round-robin by cost)" (lines 26-30). Comments later say deployed #870 is "NOT deficit round-robin" (lines 36-42), while origin/main `packages/tooling/tool/cli/src/internal/repo-run/QualityScheduler.ts` specifies effective-priority + FIFO admission (header lines 1-14) and the design record specifies priority/enqueue/pid/nonce ordering plus capacity and class-cap skips (origin/main `goals/ship-velocity/research/d1-admission-scheduler.md` lines 47-60). Comments are outside the YAML data model, so ordinary semantic loading loses the only correction.
- [BLOCKER] `ontology/docs/scope.md` — `one-grant-per-checkout` is presented as a current backpressure invariant even though deployed #870 deliberately permits concurrent review-fix leases without an origin/checkout gate — evidence: scope lines 43-45 names "one-grant-per-checkout"; origin/main `packages/tooling/tool/cli/src/commands/Yeet/internal/Handler.ts` says review-fix is class-capped at three, never takes the per-origin proof lock, sends `originKey: ""`, and uses `noAdmissionOriginGate` (lines 360-376). The complete origin/main scheduler skip predicate checks non-empty same-origin leases, the temporary origin-busy stamp, and the review-fix class cap—but never `checkoutRoot` (`packages/tooling/tool/cli/src/internal/repo-run/QualityScheduler.ts` lines 614-640). The ontology would reject a valid deployed state with two review-fix leases from one checkout.
- [WARN] `research/kpi-baseline-2026-08-27.md` — the intervention header says #870 "replac[ed] the bounce-fail proof lock," contradicting both the glossary and deployed design: the lock remains a live per-origin gate; only new contenders' behavior is queued — evidence: baseline lines 3-9 versus origin/main `packages/tooling/tool/cli/src/internal/repo-run/QualityScheduler.ts` lines 12-14 ("per-origin full-proof lock ... is retained") and origin/main `goals/ship-velocity/research/d1-admission-scheduler.md` lines 21-29/47-52 (`acquireFullProofLock` remains unchanged; admission converts `refuse-active` into staying queued). `pre-glossary.csv` line 67 is therefore accurate; the baseline's replacement claim is not.
- [BLOCKER] `research/kpi-baseline-2026-08-27.md` — merge time is not a valid fleet-wide intervention boundary, so the proposed before/after attribution confounds staggered checkout adoption — evidence: the header asserts "Contenders now QUEUE," sets a single `landedAt 2026-08-27T19:52:03Z`, and says the KPI series partitions there (lines 3-13), while origin/main `goals/ship-velocity/research/d1-admission-scheduler.md` explicitly says old Yeet binaries in sibling checkouts keep fail-fast behavior (lines 47-52). The report itself says this checkout predates #870, and probe v3 neither retains checkout HEAD/tool version nor tests ancestry (attempt projection at probe lines 94-105). Post-merge timestamps from an unre-based checkout are still pre-intervention behavior; intervention membership must be version/adoption-qualified, not wall-clock-only.
- [WARN] `research/kpi-baseline-2026-08-27.md` + probe v3 — bounce exclusion does not remove lock waiting from the episode clock as claimed — evidence: the caveat says `--exclude-bounces` means "Lock WAIT therefore does not accumulate into these episode times" (baseline lines 104-111), but the probe merely deletes bounce records (lines 131-135) and still measures each episode as next-green end minus first retained red start (lines 160-180). Bounce/idle wall time between those endpoints remains in the delta, so the 292-hour "toil pool" can already include the delay #870 attacks and cannot support the asserted decomposition.
- [WARN] `research/scripts/kpi_baseline_probe.py` + baseline — the right-censor fix labels an underdefined lower-bound activity span as an open streak's "age so far" — evidence: for a still-red branch the probe stops at that branch's last recorded event (`last = ... rs[-1]`, lines 181-186), then labels the span "open-streak age so far" (lines 198-205; baseline lines 72-75), but defines neither a common observation cutoff nor abandonment semantics. Last activity is defensible as last observation; it is not self-evidently age "so far," and quiet streaks may be materially understated under that label.
- [WARN] `research/scripts/kpi_baseline_probe.py` — the claimed schema assertion is a successful skip, and the embedded verdict schema remains unchecked — evidence: the v3 header says "schemaVersion asserted per event" (lines 20-22), but unexpected envelope versions only increment `bad_schema` and are skipped (lines 79-82), while `verdict` is read through `.get(..., {})` with no `yeet-verdict/v2` check (lines 93-105). Schema drift can silently remove or deform observations while the probe exits zero.
- [WARN] `research/scripts/kpi_baseline_probe.py` + baseline — the central bounce class still rests on an undocumented prose-string heuristic — evidence: probe line 39 classifies only the exact substring `Another Yeet full proof` and promises "see report caveat," but the baseline documents no wording-drift/classifier caveat. A message change silently moves attempts out of the reported 27%/17% contention class.
- [WARN] `research/kpi-baseline-2026-08-27.md` — the CI-tier sketch infers an additive post-local-green delay from an unjoined workflow-duration sample — evidence: it gives only "From `gh run list` durations" with no command/run ids/cutoff (lines 137-145), then says CI certainty "adds ~12m P50 after local green" while conceding that the per-PR head-push→checks-green join is future work (lines 147-149). Workflow duration alone cannot establish an interval after local green.
- [NIT] `research/kpi-baseline-2026-08-27.md` — the introduction was not updated after the fleet extension — evidence: lines 16-18 still say "One checkout ... fleet-wide extension is the named next step," while lines 115-135 claim that extension is complete.
- [WARN] `ontology/docs/scope.md` + `ontology/docs/orsd.md` — the derived requirements still present bounce-era telemetry without the newly mandatory pre-intervention qualifier — evidence: scope calls "17% ... lock-contention bounces" the baseline with only the ambiguous date 2026-08-27 (lines 17-21), and ORSD says agent needs are derived from "59% red attempts, 17% lock bounces" (lines 71-76); the corrected evidence source says every number is PRE-INTERVENTION and #870 changed contender behavior (baseline lines 3-13). The same-day date cannot disambiguate the epoch because #870 landed that day.
- [BLOCKER] `DECISIONS.md` + KPI baseline — the constitution still mandates the wrong S0 evidence vein after the investigation proved it cannot supply history — evidence: locked `baseline-first` says S0 ETL runs over `.beep/yeet/runs/*/verdict.json` (DECISIONS lines 29-39), while the baseline says each `verdict.json` is last-write-only and "the journal is the episode source" (baseline lines 29-47). The packet's S0/durable-ETL recipe cannot obey both the constitution and the corrected evidence document.
- [BLOCKER] `ontology/docs/s4-lane-contract.md` unit 7 — the contract orders extraction of deployed A-Box/configuration facts into a schema that cannot represent any assertion or configuration value — evidence: unit 7 must extract "tickets, leases, work kinds, priorities, capacity policy" and treats deployed-policy facts as "A-Box/configuration candidates" (lines 34-41), but the only candidate record has term name/kind/definition plus property domain/range and no subject, predicate, object/value, source domain, or deployed/reserved status (lines 81-92). Origin/main supplies facts such as work-kind token weights (`packages/tooling/tool/cli/src/internal/repo-run/QualityScheduler.schemas.ts` lines 83-109) and numeric `AdmissionConfig` defaults (lines 248-282); forcing them into prose `definition` fields makes them neither machine-mergeable nor mechanically validatable.
- [BLOCKER] `ontology/docs/s4-lane-contract.md` + `DECISIONS.md` — extraction unit 3 names a command that does not exist, so a lane cannot capture its promised source — evidence: contract line 30 and DECISIONS lines 60-63 prescribe `bun run beep topo sort`; origin/main `packages/tooling/tool/cli/src/commands/TopoSort/TopoSort.command.ts` defines only `topo-sort` (lines 35-37), and origin/main `package.json` maps `bun run topo-sort` to `bun run beep topo-sort` (line 374). A live prescribed invocation exited 1 with `Unknown subcommand "topo"`. Even the corrected command only logs sorted package names (origin/main `TopoSort.command.ts` lines 66-68), not the "workspace dependency edges" the unit promises, and ephemeral stdout cannot satisfy the required repo-path/line evidence format (contract lines 89-90,121-125).
- [BLOCKER] `ontology/docs/s4-lane-contract.md` — the prescribed telemetry is incomplete for its own freeze/corpus-pin requirements — evidence: §1 names five frozen artifacts and says every sha256 is recorded (lines 8-20), and the corpus rule requires the pinned commit in every lane's telemetry (lines 36-40), but `telemetry.frozen_inputs` shows only two of five artifact rows and has no corpus/source-commit field (lines 58-71). No digest command or canonical byte/path procedure exists; the `origin/main` alternative for units 5/7 is not materialized, while lanes are forbidden git commands (lines 39,54-56). "Telemetry fully populated" (lines 121-124) therefore requires inventing fields and procedure.
- [BLOCKER] `ontology/docs/s4-lane-contract.md` — the promised mechanical completion gate has no executable carrier — evidence: lines 121-125 say the orchestrator mechanically runs checks by extending "`round0_check.py`-style validation," but no command is specified and no `round0_check.py` exists anywhere in the repo (`rg --files --hidden -uu | rg 'round0.*check'` returned none). This incompletely fixes round-1's explicit "validation commands" gap: prose predicates are not an executable validator.
- [BLOCKER] `ontology/docs/s4-lane-contract.md` — the S4→S5 handoff remains schema-less despite claiming exact execution — evidence: lines 107-117 name `LEDGER.yaml`, source-lane ids, `(kind, normalized claim)`, and open-state resolution, but define neither a ledger entry structure nor claim normalization, and give no path/schema for the separately mentioned merged candidate output. Thus the orchestrator cannot deterministically merge, validate, or prove that every candidate—not merely every issue—received an S5 disposition.
- [WARN] `ontology/docs/s4-lane-contract.md` — the "source manifest" still requires lane improvisation — evidence: unit 5 gives bare filenames even though `TurboCache.ts` is at `packages/tooling/tool/cli/src/internal/cli/TurboCache.ts`, unit 6 says only "fallow boundary config + CLAUDE.md / standards/ verification laws," and unit 7 says "the PR #870 design record" rather than `goals/ship-velocity/research/d1-admission-scheduler.md` (lines 22-34). That is not a finite path manifest for a contract whose header says "improvising nothing" (lines 3-6).
- [WARN] `ontology/docs/s4-lane-contract.md` — runner/model provenance is self-contradictory — evidence: the prescribed/example telemetry value says `runner: "codex exec gpt-5.6-sol"` (lines 59-63), but the prescribed runner command has no `-m/--model` argument (lines 130-133). A lane can obey the command under a different configured default model and still be unable to truthfully populate the mandated header.

## Attacks that failed (fixes held)

- `pre-glossary.csv`'s `MachineProofLock` description held: origin/main retains exactly a per-origin machine-wide lock beneath admission; #870 changes waiting behavior, not that resource's identity.
- The direct `CertaintyTierId`/`YeetProofTier` separation held in `scope.md` and `literal-domains.md`; neither text equates `full` with `TierLocalFullProof` (the failure is loss of the two new admission-domain identities).
- The #870 corpus pin held: local `origin/main` is exactly `debbbb51f77ae10015788dec0b819f12b96c3552`, and that tree contains both scheduler sources and `goals/ship-velocity/research/d1-admission-scheduler.md`.
- Unit 1's live-reality claims held: origin/main `turbo.json` contains `global.inputs` and all three named `futureFlags`.
- README's narrative repair held: it identifies #870 as deployed weighted admission, keeps DRR prospective, and records the branch/corpus drift; the stale algorithm survives in structured UC-002, not README.
- UC-004 held: its structured flow stays on tree/epoch certainty and remaining obligations and does not conflate `AdmissionWorkKind` with a certainty or Yeet proof tier.
- ORSD's CQ census held at 19 total (14 Must / 4 Should / 1 Could), with 18 Must/Should test-manifest entries and three recorded Won'ts; every use-case `related_cqs` id exists.
- All actual Markdown links in ORSD, scope, and README resolve.
- The ring-buffer correction held: baseline and probe both limit claims to the retained window and state that durable history requires continuous snapshots.
- Probe v3's single-checkout path held: it still reproduces the published 186-attempt/27-episode table; wrong-root failure, duplicate/unmatched counts, parsed timestamp ordering, and zero/missing-elapsed handling are explicit.
- Right-censored streaks are no longer silently discarded: v3 reports aggregate censored-branch and red-attempt counts; only the observation-clock label/semantics remain underdefined.
