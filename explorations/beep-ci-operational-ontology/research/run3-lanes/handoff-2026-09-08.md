# Handoff — beep-ci-operational-ontology, run-3 execution (written 2026-09-08 ~22:50 CT)

You are continuing the `explorations/beep-ci-operational-ontology` packet in the beep-effect
repo. Steward: Benjamin. You orchestrate; Codex lanes implement. Read this file, then the
memory file `beep-ci-operational-ontology-packet-state.md`, then the packet's
`DECISIONS.md` section "2026-09-03 — run-3 corpora design grill" (sixteen rulings) before
acting. Do not re-litigate settled rulings. Do not resume the old run-2 launch plan file if
the harness resurfaces it — run 2 is complete.

## Where things stand (all verified live at handoff)

- **All three run-3 PRs are merged.** Main is `20ad99a87f`.
  - #1024 emission v2 (`@beep/ciops`): `hasScopeTag`, typed `AdmissionProjectionSpecification`
    + `policyDigest`/`journalPrefixDigest`, typed `VerificationEpisode` as the
    `hasCurrentProposal` subject (required caller-owned `episodeId`; replay derives
    `replay-<journalDigest>-<eventIndex>`), step-less deferred tail with one provisional
    `defersSeatRequest` edge, nonce carried as `scheduledUnitRef`, `s7-projection-contract.md`
    refreshed and labeled instrumentation-not-ratification.
  - #1025 v3 journal events (`@beep/repo-cli`): `admission-enqueued`, `admission-withdrawn`,
    v3 released/lease-evicted/ticket-evicted carrying `checkoutRoot`+`branch`,
    `lastHeartbeatAtMillis` on lease eviction; per-tag schema-derived guards (versions share
    tags); `AdmissionJournalReader` service proves v1/v2 readers preserve v3 rows byte-for-byte;
    **eviction emission now requires the protocol marker `yeet-admission-protocol/v2`** (pre-v3
    workers fail closed and leave claims pending); retained known history capped at 2,400 rows
    (200 admissions × 3 lifecycle rows × 4) with the admitted ring reserved first; ciops replay
    union decodes v3 and treats queue events as ledger-neutral but index-counting.
  - #1027 Stage A pins: `corpus/run3-fleet/` (1,873 files) and `corpus/run3-checkout-identity/`
    (217 files) from `etl_run3_fleet_corpus.py` / `etl_run3_checkout_identity.py`
    (+ `test_run3_generators.py`, 15 tests). Verify mode recomputes receipt bounds; residue
    scans reject home/tmp/runtime/proc/shm/uid/hostname classes; checkout labels are
    percent-encoded into one path segment so `.gitignore` cannot swallow payloads;
    `biome.jsonc` excludes both pin dirs. Run-2 generator and `run2-fleet/` byte-identical.
    Ruling-6 riders: failure-signature evidence PRESENT; cache-plan-resolution ABSENT (verdict
    schema never serializes it). Proof-ledger rows exist in NO checkout.
- Checkouts: `~/YeeBois/projects/beep-effect8-s5` is detached at `20ad99a87f`, clean except
  this handoff file (commit it with the next packet PR). `beep-effect5` is HANDS-OFF (his
  codex session). Merged lane worktrees `beep-effect8-worktrees/ontology-v3-journal` and
  `ontology-emission-v2` still exist (nothing unmerged; `graft/` is a local cache); Benjamin
  declined their forced removal this session — leave them unless he asks.
- Live journal at handoff: canonical `~/.beep/runtime/beep-admit-uid-<uid>/journal.ndjson`
  ~187 rows, all v1/v2 — **no v3 rows yet** (v3 writers only run from checkouts that pulled
  main). Protocol marker on this workstation is still v1.

## Standing directives (Benjamin's, verbatim in force)

Never merge PRs (steward merges). No force-push; reconcile divergence by merging
`origin/main` in. Never `git add -A`. Push every fix the moment it is committed — never queue
behind sibling PRs or local proofs; hosted proves, local checks are `--filter`-scoped and run
in parallel. Public repo: no `/home/`, hostnames, uids, pids, session ids in committed bytes.
Frozen artifacts (`etl_fleet_corpus.py`, `run2-fleet/`, `extraction/s5|s6|s7`, run pins) are
byte-immutable; new work = new sibling files. Codex-first to preserve Fable quota
(`gpt-6-astra` xhigh); Ultracode is ON but Workflow children in a direct `claude` session
would be Fable → use `codex exec` lanes under `systemd-run --user … -p Slice=agent-runs.slice`.
A codex lane that must commit needs `--add-dir "$(git -C <wd> rev-parse --path-format=absolute
--git-common-dir)"` AND `-c 'sandbox_workspace_write.network_access=true'` (1Password signing
socket); lanes never push. Deliver grill questions via AskUserQuestion (≤3 per call,
recommended arm first). 1Password only via the `op` shim; announce sudo before running.

## Next steps, in order

1. **Protocol v2 marker (steward step — ask, don't just run).** Once his active checkouts have
   pulled main, publish `yeet-admission-protocol/v2` with eviction `on`. Find the operator
   command by grepping `setAdmissionEvictionProtocol` / `AdmissionEvictionEmission` under
   `packages/tooling/tool/cli/src/commands/Quality/`. Until published, eviction rows stay
   deferred (claims pending) by design — that is the mixed-fleet fence from #1025.
2. **Let Stage B evidence accumulate.** Watch the canonical journal for v3 rows
   (`grep -o '"schemaVersion":"[^"]*"' … | sort | uniq -c`) and for `proof-ledger.ndjson`
   under `<checkout>/.beep/yeet/` (needs post-merge yeet traffic). Ring: 200 admissions;
   known-row cap 2,400 — capture before the loss population wraps.
3. **Stage B capture (Rulings 8, 9, 10, 11).** Short grill first (AskUserQuestion): (a) new
   sibling pins `run3b-*` via the existing run-3 generators with a stage flag vs new scripts;
   (b) the fixture-induced eviction + withdrawal scenario under `RuntimeRootTestOverride`,
   pinned with explicit synthetic-provenance labels; (c) whether to wait for organic
   proof-ledger rows or seed them with a bounded yeet run. Then brief a codex lane exactly
   like `research/run3-lanes/stage-a-brief.md` (it worked), review its report, commit/push,
   babysit to merge-ready. Fresh-worktree verify and residue scans are mandatory before push.
4. **Run 3 proper.** Choreography precedent: DECISIONS.md "auditor run-2 launch grill" (nine
   rulings) — pin commit → frozen HEAD run with `work/` untracked → observe → seats (run-2
   assignment: codex den/fa/synth, codex adversary in independent context, grok blinded with
   `-alt` ids) → `VAL --gate` → clustered ratification sittings → one end-of-run PR. Docket:
   the ordering cluster ratifies together (ScheduleStep, AdmissionProjectionSpecification,
   VerificationEpisode + six properties + `ciops-prov:` namespace re-proposal), the
   provenance corpora flags (rat-047..052), Ruling-6 riders, the docket consistency items
   (CQ-019 `required_properties`/traceability alignment; seed.ttl:112 and the CQ-020 sample
   to 0-based stepIndex), TS-adapter non-trigger recorded. Engine: validator v14; pin
   `prior_index_sha256_12: a207a106de68`.

## Gotchas learned this session (all in memory too)

- Lint Policy "Hoist Schema.is/decodeEffect to module scope" is Oxlint; prove with
  `bunx oxlint --quiet --disable-nested-config <files>`. `knowledge:semantic-delta` resolves
  tracked paths from the REPO ROOT and reads HEAD (commit before re-running).
- `gh run view --job <id> --log` is withheld while sibling jobs run; use `--log-failed` or the
  run-level log. `yeet monitor --watch` dies with `poll-error` under two concurrent watches —
  use a REST `commits/<ref>/check-runs` poll that only emits on red/settled.
- The Biome pre-commit hook rewrites JSON payloads: any new fidelity pin dir must be added to
  `biome.jsonc` `files.includes` negations in the same PR.
- `bun run beep worktree remove --archive` fails once the remote branch is deleted on merge
  (`inspect-upstream` bad revision) — retire lanes before the PR merges, or use git.
- Codex sandbox: uv cache is read-only (use a lane-local `UV_CACHE_DIR` + offline), Node
  `spawnSync EPERM` can hit package-verify (attribute environment-only, prove with the
  Bun-hosted `tools/tsgo-shim/tsgo.js`), fresh worktrees need `mise trust`.

## Files to open first

`DECISIONS.md` (run-3 grill), `research/run3-corpora-design-brief.md`,
`research/run3-lanes/{stage-a,v3-journal,emission-v2}-{brief,report}.md` and the two
`*-review-fixes-*` pairs, `ops/manifest.json` (open questions), `README.md` (trail),
`ontology/extraction/s4/beep-ci-ops/corpus/etl_run3_*.py`, `ontology/docs/s7-projection-contract.md`.
Memory: `beep-ci-operational-ontology-packet-state`, `codex-linked-worktree-commit-sandbox`,
`feedback-push-fixes-immediately`, `feedback-push-first-hosted-proves-filtered-local-checks`,
`worktree-archive-retirement`, `lint-policy-lane-attribution-and-markdown-noop`.
