# Run-3 Stage A implementation report

> **Refresh note (orchestrator, 2026-09-08):** the pins below were re-captured once with
> `--refresh` before publication because the lefthook Biome pre-commit hook had rewritten
> 37 pinned JSON payloads (receipt in `research/OPPORTUNITIES.md`; the pin directories are
> now excluded in `biome.jsonc`). Generator digests are unchanged; capture instants, the
> custody salt, and counts moved: `run3-fleet` capture `2026-09-09T02:26:47.140Z`, 89 checkouts, 177
> admission rows; `run3-checkout-identity` capture `2026-09-09T02:27:10.125Z`, 109 bindings. The manifest
> excerpts and tallies in the lane's sections describe the first capture.

Date: 2026-09-08 (America/Chicago; capture instants below are UTC).

Both standalone generators and their sibling corpora are implemented and pinned.
The implementation lane's sandbox could not write the linked worktree's Git
metadata, so the orchestrator (Fable) staged the paths listed in the handoff
section by name and committed them; the sandbox recipe fix is recorded in the
operator doctrine. The report separates working-tree proof from HEAD-only lint.

## Pinned manifests

First 40 lines of each generated manifest, pasted verbatim.

### run3-fleet/MANIFEST.yaml

```yaml
# GENERATED by etl_run3_fleet_corpus.py; do not hand-edit.
# Public run-3 Stage A capture; source descriptors are portable.
schema_version: beep-ci-ops-run3-fleet-corpus/v1
generated_by: etl_run3_fleet_corpus.py
generator_sha256: 18e634648c08847e669ead52c8aea0d3d2e3e6b54da0024b080ca2a122e4a8d2
corpus_commit: 663904610cce2a38c06b0619a8c414646b69361c
capture_instant: '2026-09-09T02:02:53.049Z'
capture_finished_at: '2026-09-09T02:02:56.823Z'
stage: A
capture_instant_basis: capture start; files read once over the recorded interval, not an atomic fleet
  snapshot
custody:
  rule: ownerRef = sha12(f"{pid}:{procStart}:{captureSalt}")
  capture_salt_representation: hexadecimal encoding of 32 random bytes
  salt_policy: per-capture, unrecorded, unlinkable across captures
  missing_procStart_rule: pid:<absent>:salt; weaker key tallied separately
  missing_procStart_caveat: weak references cannot be joined to full custody references by ownerRef; nonce
    remains available
admission_roots:
- label: canonical
  status: present
  complete_within: named admission root at capture
  world: closed
  journal:
    path: admission/canonical/journal.ndjson
    source: <canonical-admission>/journal.ndjson
    world: closed
    status: present
    complete_within: one read of the named source during this capture; retained window only
    observed_at: '2026-09-09T02:02:53.049Z'
    history_outside_window: unknown
    observed_rows: 171
    retained_rows: 171
    retained_source_lines:
    - 1
    - 2
    - 3
    - 4
    - 5
    - 6
```

### run3-checkout-identity/MANIFEST.yaml

```yaml
# GENERATED by etl_run3_checkout_identity.py; do not hand-edit.
# Public run-3 Stage A capture; source descriptors are portable.
schema_version: beep-ci-ops-checkout-identity/v1
generated_by: etl_run3_checkout_identity.py
generator_sha256: 39b87cc465a61bd12d491e05a04904e6098f404309af0753ef80402745b2c122
corpus_commit: 663904610cce2a38c06b0619a8c414646b69361c
capture_instant: '2026-09-09T02:14:24.554Z'
capture_instant_basis: FleetSnapshot.scannedAt
capture_started_at: '2026-09-09T02:14:22.761Z'
capture_finished_at: '2026-09-09T02:14:28.317Z'
stage: A
snapshot_shape:
  value: FleetSnapshot
  source:
    file: packages/tooling/tool/cli/src/commands/Worktree/Worktree.schemas.ts
    line: 1129
    sha256: d723d71d33f69b007b78c5824756c3a2c3519947fd0bf88f487c347249c3a168
snapshot_source:
  source: bun run beep worktree fleet --json
  world: closed
  status: present
  complete_within: one read of the named source during this capture; retained window only
  observed_at: '2026-09-09T02:14:24.554Z'
  history_outside_window: unknown
  coverage:
    clonesDiscovered: 22
    checkoutsDiscovered: 106
    checkoutsDegraded: 1
    processesScanned: 5
    processesUnreadable: 0
  snapshot_sha256: 6d76b4ccb535f4124d3c9cf9f575cc6b6eb3c4f7c0c219139ea2f170a42d310f
checkout_counts:
  linked-worktree: 84
  clone: 22
bindings:
- checkout: beep-effect
  identity_key: <fleet>/beep-effect
  kind: clone
  path: bindings/beep-effect.json
  source: <fleet>/beep-effect
```

## Corpus counts and scope

| Corpus | Clones | Linked worktrees | Raw payload files | Properties files | Total files | Source records | Bytes |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| run3-fleet | 22 | 66 | 933 | 933 | 1867 | 7569 | 18079775 |
| run3-checkout-identity | 22 | 84 | 107 | 107 | 215 | 107 | 882483 |

The fleet generator covers the two requested filesystem patterns, requiring a
Git checkout marker and including checkout receipts even when no run exists.
The identity generator preserves the command's same-origin registered-worktree
inventory. These discovery mechanisms and capture instants differ:

- Fleet capture: `2026-09-09T02:02:53.049Z` through `2026-09-09T02:02:56.823Z`.
- Identity snapshot: `2026-09-09T02:14:24.554Z`; probes end at `2026-09-09T02:14:28.317Z`.
- Checkout labels common to both inventories: 85.
- Identity-only labels: 21; fleet-only labels: 3.

A missing cross-corpus match is an inventory/window boundary, not proof that a
checkout had no activity. Neither capture freezes the running fleet.

### Fleet families

| Family | Raw files | Source records |
| --- | ---: | ---: |
| admission | 2 | 173 |
| attempts | 347 | 6812 |
| verdict | 580 | 580 |
| live | 4 | 4 |
| ledger | 0 | 0 |

Every retained raw payload has one deterministic `.properties` sibling.
Attempt record counts include both lifecycle ends and any retained compaction
receipts; they are not a count of unique verification episodes.

### Admission root by version and event

| Root | Status | schemaVersion | _tag | Rows |
| --- | --- | --- | --- | ---: |
| canonical | present | yeet-admission-journal/v1 | admission-admitted | 88 |
| canonical | present | yeet-admission-journal/v1 | admission-released | 82 |
| canonical | present | yeet-admission-journal/v2 | admission-lease-evicted | 1 |
| system-tmp | absent | none | none | 0 |
| session-tmp | present | yeet-admission-journal/v1 | admission-admitted | 1 |
| session-tmp | present | yeet-admission-journal/v1 | admission-released | 1 |

No v3 rows were present. The generator accepts v1/v2/v3 envelopes; malformed
JSON, non-object rows, unknown admission versions, and invalid envelopes receive
classified exclusion receipts. No excluded source bytes are emitted.

Excluded source rows/files in these pins: **0**.
Reason-class counts: `{}`. Synthetic tests
exercise exclusions that the live capture did not contain.

| Root | ownerRef minted | Without procStart | Leases | Queue | Quarantine |
| --- | ---: | ---: | ---: | ---: | ---: |
| canonical | 171 | 83 | 2 | 2 | 0 |
| system-tmp | 0 | 0 | 0 | 0 | 0 |
| session-tmp | 2 | 1 | 0 | 0 | 0 |

Custody counts in this table concern journal rows; each captured live state
source has its own redaction tally. Missing-procStart owner references are
weaker keys and do not equal the corresponding full pid/start reference.
Nonce joins remain available. The salt is 32 random bytes rendered as hex
only inside the hash input; it is never persisted or returned.

### Retention and known loss

The manifests preserve all six requested loss-class labels and cite their
current implementation. The source now retains 200 admitted transitions, not
200 total admission rows. Attempts retain 50 terminal attempts, while active,
protected, and undecodable rows may survive beyond that cap. Each journal
receipt includes observed row counts beside both the brief's nominal cap and
the actual writer unit. Being below the cap does not prove no earlier wrap.

The old claim-race loss class is explicitly qualified: the current scheduler
persists reap claims and acknowledges both sinks. The manifest does not claim
a zero-loss guarantee. Eviction time remains reap time, and v2 eviction
serialization still omits the lease heartbeat.

### Checkout binding and cache evidence

The snapshot binds 106 checkouts (22 clones, 84 linked worktrees).
Local Turbo caches are present in 22; 0 have explicit enabled
`remoteCache` configuration, and 0 have `remoteCache.signature: true`.
These configuration booleans do not establish runtime cache activation.
Environment presence is recorded only as booleans and labeled generator-process
scope. No environment values, signature keys, or separate install-root inventory
are captured. Git-common-dir linkage is labeled a checkout-binding fact.

Degraded supplementary binding(s): 1. Their failed Git probes
have status/exit-code receipts and null values; the snapshot rows are retained.
The missing legacy system-temp checkout is not silently converted to a valid
origin or Git-directory binding. Other completed Git probes matched the snapshot
branch/head. A single logical capture is recorded; change-across-time evidence
and task-hash-grain proof-cache reuse remain deferred.

Merged-preview process-directory tokens aliased: 3.
Distinct capture-local aliases preserve checkout separation while removing the
process numbers from snapshot paths, binding keys, and Git-directory linkage.

## Ruling-6 rider verdict

- `pa-failure-signature`: **present**, 1858 serialized occurrences
  with `failureKind` and `failedStepId` in the same object. This count includes
  embedded and standalone verdict occurrences and does not deduplicate them.
  The run-2 index asks for this classified-failure join. Source citations cover
  `YeetFailureKind`, the verdict fields, and `YeetVerdictJson.encode` in the
  handler; raw values and eligible property occurrences survive redaction.
- `pa-cache-plan-resolution`: **absent**, zero structured resolver-result
  occurrences in the attempts/verdict payloads. The governed resolver tags
  exist in `TurboCache.ts`, and `Quality/Tasks.ts` feeds the resolved plan into
  command construction. The verdict schema does not serialize that result.
  Cache configuration and unrelated failure fields do not fabricate the join.
- `proof-ledger.ndjson`: absent in all captured checkout roots. Materialization
  remains owned by Stage B.

## Verification

The installed uv cache was read-only. The lane reconstructed the required wheel
archives from already installed local distributions into a writable temporary
directory, then ran uv offline with `--no-index --find-links <lane-wheel-dir>`
and `UV_CACHE_DIR=<lane-cache>`. No network installation or original-cache write
was used. The documented generator dependency remains stdlib plus PyYAML.

| Corpus | First successful pin | Ordinary verify | Corrupted verify | Restored verify | Byte-identical after restore |
| --- | ---: | ---: | ---: | ---: | --- |
| run3-fleet | 0 | 0 | 1 | 0 | True |
| run3-checkout-identity | 0 | 0 | 1 | 0 | True |

Exit codes above come from actual CLI invocations. A `.properties` payload
was deliberately corrupted, the CLI refused it, and a `finally` block restored
the exact original bytes before successful verification. The whole-tree hashes
include every relative file name and file content with 8-byte length framing:

- `run3-fleet`: `9d5e7066345ae7493f34326150a446e913034019b600be67e1507dd8fdeefc4f`.
- `run3-checkout-identity`: `da2ae761b997598b94631da3efdf092026f14834b53a845773e25e4f5cd4c139`.

- Generator regression suite: **11 tests pass**, including v1/v2/v3 rows,
  weaker custody, new-salt unlinkability, malformed exclusions, nested process
  fields, distinct session temp roots, credential guards, projections, preview
  path aliases, and a pinned rerun with capture/source/subprocess/salt calls
  replaced by failures.
- Both built-in residue scans: **PASS**. Independent recursive fixed-string
  scans for the absolute-home marker, current hostname, and sha12(hostname)
  each returned grep exit 1 with zero output bytes. Preview process-number
  path scans are also empty. All source and generator digest checks pass.
- Packet validator: **0 blockers, 0 warns**; 26 CQs and 25 SPARQL files parsed.
- CQ suite: **0 failures across 25 seed tests and 20 fixtures**. This proves
  the existing CQ suite, not run-3 ontology ratification. Its golden coverage
  limitation remains visible in the suite output.
- `bun run beep knowledge refs --check`: exit 0, zero live gated observations.
- `bun run beep knowledge semantic-delta`: exit 0, zero introduced findings;
  491 inherited findings remain unchanged.
- `bun run beep laws effect-imports --mode markdown --check`: exit 0, advisory
  mode; 920 files and 310 fences scanned. Its 13 suggested files and two parser
  warnings are inherited, outside this change. No advisory rewrites were applied.
- Knowledge refs and semantic-delta read **HEAD archives**, so their green
  results cover the source commit below. They must run again after this work is
  committed. The new untracked corpus has direct generator/test/residue proof.
- No workspace package was edited; package-verify is not applicable.

## Frozen-byte proof

`etl_fleet_corpus.py` SHA-256 remains:

```text
2b6fb03d818986fffcbc8951ec43b6b63dfb0d302c3a3c5a7ba45ecb7777e424
```

The frozen generator was copied as source patterns, never imported or edited.
Git reports no changes under `run2-fleet/`, `extraction/s5/`, `extraction/s6/`,
or `extraction/s7/`. `DECISIONS.md` and `ATLAS.md` were not changed.

## Deviations and remaining questions

1. **Commit blocked by the session environment.** The explicit-path staging
   attempt exited 128 with `Unable to create .../index.lock: Read-only file
   system`. No SHA was minted. The inherited orchestrator receipt in
   `research/OPPORTUNITIES.md` is preserved; lane receipts append after it.
2. **Source drift is qualified.** Retention units, the moved attempt-journal
   module, and durable reap recovery differ from the stale design references.
   Citations were resolved from this checkout, checked against HEAD bytes, and
   carry source file digests. No new ruling or ontology term was introduced.
3. **Registered external worktrees need portable aliases.** Bindings use
   `external/<portable-root>/<relative-path>` for snapshot rows outside the
   fleet root. `snapshot_path` retains the rewritten observed path. These are
   corpus-local labels, not synthetic stable identity claims. Registered
   temporary checkout rows are retained as inventory rows; install roots are
   not separately inventoried as cache mounts.
4. **Additional redaction protects process-directory names.** The identity pin
   was deliberately refreshed after adding preview aliases. A salted custody
   key is used only in the fleet corpus; preview aliases are capture-local
   ordinals and their raw mapping is never recorded. Hostname digests inside
   historical proof-lock messages become `<host>`; lock files are never read.
5. **Two snapshot times and two discovery populations.** The required glob
   capture and the registered-worktree command produce the scope differences
   listed below. No absent cross-corpus match has been interpreted as no work.
6. **One degraded binding is explicit.** Missing Git probe values are null
   with failed-operation receipts. Substituting the fleet-wide origin would
   have fabricated a per-checkout probe result.

Stage B still needs the v3 journal PR, organic traffic, proof-ledger issuance
rows, and the synthetic eviction/withdrawal scenario. The cache-plan rider needs
a serialized execution join. N-instant evidence, task-hash cache joins, and
rigidity across rename remain open. No TS adapter, fixture eviction, S7 change,
or S8 work was performed.

### Identity-only checkout labels

- `beep-effect/.claude/worktrees/adhd-skill-followup`.
- `beep-effect12-pr874/.beep/yeet/merged-preview-owner-0001`.
- `beep-effect12-pr874/.beep/yeet/merged-preview-owner-0002`.
- `beep-effect15/.claude/worktrees/codex-ui-linting`.
- `beep-effect18/.beep/yeet/merged-preview-owner-0000`.
- `beep-effect2/.claude/worktrees/vigilant-hellman-36c686`.
- `beep-effect20/.claude/worktrees/docs-packet-core-preclose`.
- `beep-effect21/.claude/worktrees/festive-payne-b3da29`.
- `beep-effect3/.claude/worktrees/endgame`.
- `beep-effect5/.claude/worktrees/unruffled-hodgkin-8194a8`.
- `beep-effect5/.claude/worktrees/vigilant-khorana-0c8658`.
- `beep-effect8/.claude/worktrees/elegant-grothendieck-0168b7`.
- `beep-effect8/.claude/worktrees/identity-annote-class`.
- `beep-effect9/.claude/worktrees/quizzical-shamir-3cac3e`.
- `external/operator-home/.codex/worktrees/660d/beep-effect17`.
- `external/operator-home/.codex/worktrees/77f1/beep-effect20`.
- `external/operator-home/.codex/worktrees/90b4/beep-effect10`.
- `external/operator-home/.codex/worktrees/946b/beep-effect7`.
- `external/operator-home/.codex/worktrees/f488/beep-effect7`.
- `external/operator-home/.codex/worktrees/fbd1/beep-effect16`.
- `external/system-temp/beep-pr869`.

### Fleet-only checkout labels

- `beep-effect-worktrees/pr-992-closeout`.
- `beep-effect-worktrees/ship-velocity-proof-c`.
- `beep-effect-worktrees/ship-velocity-proof-d`.

## Commit SHA(s) and handoff

**Implementation commit: created by the orchestrator from the handoff below (see the PR).**
Current branch: `ontology-run3-stage-a`. Source/base HEAD: `663904610cce2a38c06b0619a8c414646b69361c`.
Both manifests record this source commit independently from their generator
SHA-256 pins. The requested implementation commit message remains:

```text
feat(explorations): pin the run-3 Stage A corpora (fleet + checkout identity)
```

From a lane authorized to write this worktree's Git metadata, stage only these
paths. The original lane briefs and `graft/` remain unrelated untracked work.

```sh
stage_a_packet=explorations/beep-ci-operational-ontology
stage_a_corpus="$stage_a_packet/ontology/extraction/s4/beep-ci-ops/corpus"
git add -- \
  "$stage_a_corpus/etl_run3_fleet_corpus.py" \
  "$stage_a_corpus/etl_run3_checkout_identity.py" \
  "$stage_a_corpus/test_run3_generators.py" \
  "$stage_a_corpus/run3-fleet" \
  "$stage_a_corpus/run3-checkout-identity" \
  "$stage_a_packet/ops/manifest.json" \
  "$stage_a_packet/README.md" \
  "$stage_a_packet/research/OPPORTUNITIES.md" \
  "$stage_a_packet/research/run3-lanes/stage-a-report.md"
git commit -m "feat(explorations): pin the run-3 Stage A corpora (fleet + checkout identity)"
```

Before that commit, update the pending-commit sentences in this report and the
packet mirrors to reflect the resumed lane. Record the actual implementation
SHA in the follow-up report, then rerun the HEAD-based knowledge gates.
Publishing remains with Fable; this implementation lane did not push or open a PR.
