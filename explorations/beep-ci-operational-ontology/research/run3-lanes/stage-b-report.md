# Run-3 Stage B implementation report

## Current security repair proof — CSF-012 and CSF-013

After Stage B landed in PR #1034, CSF-012 validation found the same quoted-PID
bypass in its fourth generator. The existing repair now handles the organic and
synthetic pins independently, preserving their population-specific projections.
It sanitizes 11 organic raw files (13 PID occurrences); synthetic payloads are
unchanged. Both full pin verifiers pass, all 36 combined generator tests pass,
and a second repair verifies all five pins unchanged. Capture metadata, nonce
chains, custody references, and loss-population counts are preserved.

Committed-history tests now repair and replay both Stage B populations,
asserting independent roots and exact population-specific projections. The
projection scanner permits an embedded redacted PID null value; raw process
identity members and numeric PID residue still fail. The later generator
revision appends zero-change updates without replacing initial repair counts.

These hashes and totals supersede the historical capture evidence below.
Whole-tree hashes include sorted complete relative POSIX names and contents,
each framed by an eight-byte big-endian length, including the manifest.

| Pin | Whole-tree SHA-256 | Files | Records | Bytes |
| --- | --- | ---: | ---: | ---: |
| run3b-fleet | `602bc3f234371033b2265705bef06cb118946224ada52c57513b6d272e0f6774` | 719 | 7186 | 15586514 |
| run3b-synthetic | `fb1193da55a0bf6220e47183300bc3bd0125452ab075feb4dae40965345e69c2` | 9 | 10 | 41294 |

Current generator SHA-256: `3a2c28b5fa064e19ee5c9cde687b5e48995b8a89cfa3ca589e6825cee88a6b9c`.

The current five-pin repair can be replayed from pre-security source commit
`86990e28f9` with `resanitize-corpora.py --source-ref 86990e28f9`.

CSF-013 adds schema process-member removal and a fail-closed byte guard to all
four generators. The subsequent repair removes 178 fields from 26 Stage A raw
files and 220 fields from 32 Stage B raw files. Their projections are regenerated;
all other raw populations remain unchanged. Original CSF-012 receipts retain
their initial counts and append the new finding's updates. All five full pin
verifiers pass, and a repeat repair is unchanged. The tables above describe
these current repaired bytes; the original capture reports below are historical.

## Historical Stage B capture report — before CSF-012

Capture date: 2026-09-08. Handoff date: 2026-09-09 (America/Chicago).
Capture instants below are UTC.

The standalone `etl_run3b_fleet_corpus.py` implements the organic loss-population
capture and the READY-gated synthetic import. The roots pin and verify independently.
Ordinary replay uses committed payloads and the generator self-pin. It does not
read scheduler state, the producer export, source citations, Git, or a new salt.

Synthetic status: **pinned**. The producer metadata is copied from `scenario.json`.
READY polling began at `2026-09-09T04:43:14Z` and ran every 60 seconds.
Poll 12 observed READY at `2026-09-09T04:55:14Z`; import started after that marker.
The existing fleet pin retained whole-tree hash
`562efb950efb492cac7041f71392080d9d4cc24401b99efdd992adca873aeec9` throughout import.
Producer path: `packages/tooling/tool/cli/test/quality-scheduler-synthetic-scenario.test.ts`.
Producer SHA-256: `d411cc141e357283df105ec19ad491c1fbba19a17e8e64b44edb99de3fce8cac`.
Original `scenario.json` SHA-256: `b6a1aaaf41f30c9569f2a62d2f605d78184394bae03105aff428f4b9fbbb229f`.

## Pinned manifest heads

First 40 lines of each final manifest, including the generator self-pin and capture instant.

### run3b-fleet/MANIFEST.yaml

```yaml
# GENERATED by etl_run3b_fleet_corpus.py; do not hand-edit.
# Public run-3 Stage B capture; source descriptors are portable.
schema_version: beep-ci-ops-run3b-fleet-corpus/v1
generated_by: etl_run3b_fleet_corpus.py
generator_sha256: 137d54a7e8e31ed93d20ca8fa3595187c2e7b1da9e489da40fc8d9a0cde80e03
corpus_commit: ff1cf545ef2c4a43806dc50323259f300218e573
capture_instant: '2026-09-09T04:46:24.032Z'
capture_finished_at: '2026-09-09T04:46:30.704Z'
stage: B
provenance: organic
capture_instant_basis: capture start; files read once over the recorded interval, not an atomic fleet
  snapshot
custody:
  rule: ownerRef = sha12(f"{pid}:{procStart}:{captureSalt}")
  capture_salt_representation: hexadecimal encoding of 32 random bytes
  salt_policy: per-capture, unrecorded, unlinkable across captures
  missing_procStart_rule: pid:<absent>:salt; weaker key tallied separately
  missing_procStart_caveat: weak references cannot join full references by ownerRef; nonce remains available
  nested_payloads: each object containing pid receives its own ownerRef before pid/procStart removal
admission_roots:
- label: canonical
  status: present
  complete_within: named admission root at capture
  world: closed
  journal:
    path: admission/canonical/journal.ndjson
    source: <canonical-admission>/journal.ndjson
    world: closed
    status: present
    complete_within: one read of the named source during this capture; retained window only
    observed_at: '2026-09-09T04:46:24.102Z'
    history_outside_window: unknown
    observed_rows: 233
    retained_rows: 233
    retained_source_lines:
    - 1
    - 2
    - 3
    - 4
    - 5
```

### run3b-synthetic/MANIFEST.yaml

```yaml
# GENERATED by etl_run3b_fleet_corpus.py; do not hand-edit.
# Public run-3 Stage B capture; source descriptors are portable.
schema_version: beep-ci-ops-run3b-synthetic-corpus/v1
generated_by: etl_run3b_fleet_corpus.py
generator_sha256: 137d54a7e8e31ed93d20ca8fa3595187c2e7b1da9e489da40fc8d9a0cde80e03
corpus_commit: ff1cf545ef2c4a43806dc50323259f300218e573
capture_instant: '2026-09-09T04:55:51.186Z'
capture_finished_at: '2026-09-09T04:55:51.253Z'
stage: B
provenance: synthetic
capture_instant_basis: capture start; files read once over the recorded interval, not an atomic fleet
  snapshot
producer:
  path: packages/tooling/tool/cli/test/quality-scheduler-synthetic-scenario.test.ts
  sha256: d411cc141e357283df105ec19ad491c1fbba19a17e8e64b44edb99de3fce8cac
scenario_sha256: b6a1aaaf41f30c9569f2a62d2f605d78184394bae03105aff428f4b9fbbb229f
scenario_captured_at: '2026-09-09T04:54:25.357Z'
scenario_steps:
- Publish protocol v2 with eviction on under the runtime-root test override.
- Admit contender A with an attemptId, checkout root, and branch into the only slot; hold its lease.
- Wait for contender B to enqueue behind A, then interrupt B while waiting.
- Write synthetic dead-owner lease and ticket fixtures with distinct checkout roots and attemptIds.
- Reap both fixtures through the real eviction and attempt-termination writers; acknowledge all claims.
- Finish A, assert the complete per-nonce chains, then reap again to prove idempotence and lock cleanup.
expected:
  tags:
    admission-enqueued: 2
    admission-admitted: 1
    admission-withdrawn: 1
    admission-lease-evicted: 1
    admission-ticket-evicted: 1
    admission-released: 1
  chains:
  - label: contender-a
    nonce: 99cba5c3-890d-43f9-aa5a-11f47b9695e9
    tags:
    - admission-enqueued
    - admission-admitted
    - admission-released
  - label: contender-b
```

## Capture scope and counts

| Population | Capture start (UTC) | Capture finish (UTC) | Payloads | All files | Records | Bytes |
| --- | --- | --- | ---: | ---: | ---: | ---: |
| fleet | 2026-09-09T04:46:24.032Z | 2026-09-09T04:46:30.704Z | 718 | 719 | 7186 | 15598637 |
| synthetic | 2026-09-09T04:55:51.186Z | 2026-09-09T04:55:51.253Z | 8 | 9 | 10 | 40762 |

All-file counts include the manifest; payload counts include one projection per raw file.
Records count raw JSON objects once. Neither capture freezes the scheduler or the fleet.

| Population | Raw family | Files | Records |
| --- | --- | ---: | ---: |
| fleet | admission | 2 | 235 |
| fleet | protocol | 1 | 1 |
| fleet | attempts | 354 | 6948 |
| fleet | live | 2 | 2 |
| synthetic | admission | 1 | 7 |
| synthetic | protocol | 1 | 1 |
| synthetic | attempts | 2 | 2 |

The fleet inventory covers 22 clones and 71 linked worktrees.
Checkout labels use the same percent-encoded single output component as the Stage A
review fix. Labels cross-reference the Stage A identity/verdict pins, with their
different observation windows kept explicit. No verdict family, ledger family, or
checkout-identity capture is emitted in Stage B.

Every root and live family has a closed-world receipt, including absent roots and
empty directories. Lock files and sidecars are counted as exclusions and never read.

**fleet roots and live state.**

- `canonical`: present; protocol present; leases=2 (locks excluded 0), queue=0 (locks excluded 0), claims=0 (locks excluded 1), quarantine=0 (locks excluded 0).
- `system-tmp`: absent; protocol absent; leases=0 (locks excluded 0), queue=0 (locks excluded 0), claims=0 (locks excluded 0), quarantine=0 (locks excluded 0).
- `session-tmp`: present; protocol absent; leases=0 (locks excluded 0), queue=0 (locks excluded 0), claims=0 (locks excluded 0), quarantine=0 (locks excluded 0).

Excluded undecodable rows/files by reason: `{}`.

**synthetic roots and live state.**

- `synthetic`: present; protocol present; leases=0 (locks excluded 0), queue=0 (locks excluded 0), claims=0 (locks excluded 0), quarantine=0 (locks excluded 0).

Excluded undecodable rows/files by reason: `{}`.

## Loss-population census

| Population | Root | schemaVersion | _tag | Rows |
| --- | --- | --- | --- | ---: |
| fleet | canonical | yeet-admission-journal/v1 | admission-admitted | 106 |
| fleet | canonical | yeet-admission-journal/v1 | admission-released | 94 |
| fleet | canonical | yeet-admission-journal/v2 | admission-lease-evicted | 1 |
| fleet | canonical | yeet-admission-journal/v3 | admission-enqueued | 14 |
| fleet | canonical | yeet-admission-journal/v3 | admission-lease-evicted | 4 |
| fleet | canonical | yeet-admission-journal/v3 | admission-released | 4 |
| fleet | canonical | yeet-admission-journal/v3 | admission-ticket-evicted | 2 |
| fleet | canonical | yeet-admission-journal/v3 | admission-withdrawn | 8 |
| fleet | system-tmp | none | none | 0 |
| fleet | session-tmp | yeet-admission-journal/v1 | admission-admitted | 1 |
| fleet | session-tmp | yeet-admission-journal/v1 | admission-released | 1 |
| synthetic | synthetic | yeet-admission-journal/v1 | admission-admitted | 1 |
| synthetic | synthetic | yeet-admission-journal/v3 | admission-enqueued | 2 |
| synthetic | synthetic | yeet-admission-journal/v3 | admission-lease-evicted | 1 |
| synthetic | synthetic | yeet-admission-journal/v3 | admission-released | 1 |
| synthetic | synthetic | yeet-admission-journal/v3 | admission-ticket-evicted | 1 |
| synthetic | synthetic | yeet-admission-journal/v3 | admission-withdrawn | 1 |

| Population | win | withdrawn | lease-evicted | ticket-evicted | in-flight | pre-v3 | unclassified |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| fleet | 4 | 8 | 5 | 2 | 2 | 96 | 0 |
| synthetic | 1 | 1 | 1 | 1 | 0 | 0 | 0 |

The manifests retain every root/nonce chain, its source-order tag sequence, and
zero-based record indexes. Verification recomputes that full census from pinned bytes.
`pre-v3` means admitted/released without a retained enqueue; it does not prove the
writer version. Partial or conflicting terminal chains retain null classification.
No wait or hold durations are pinned. The manifests record the subtraction rule
and leave arithmetic to the auditor.

- fleet: heartbeat <= eviction checked on 4 v3 lease-evicted rows; 0 violations; 1 legacy eviction rows lack a heartbeat.
- synthetic: heartbeat <= eviction checked on 1 v3 lease-evicted rows; 0 violations; 0 legacy eviction rows lack a heartbeat.

Known-loss receipts cite best-effort drops, claim races, malformed-state quarantine,
reap-time timestamps, heartbeat version differences, bounded admission/attempt history,
and deferred protocol-off claims. The deployed caps are 200 admitted transitions,
2,400 known admission rows, and 50 terminal attempts per branch. Below-cap counts
do not establish complete history.

## Proof-ledger receipt

Ruling 17 is recorded as `re-parked to run 4`. Ledger files were observed in 0 fleet checkouts.
The generator checks existence only and reads no ledger contents.
The receipt cites `goals/time-to-certainty/PLAN.md` by the C2 needle
`not yet wired into any lane`, with its current line and source SHA-256.
Issuance-custody evidence remains deferred to run 4.

## Verification

- Generator regression suite: **14 tests pass**. Coverage includes nested claims,
  per-capture salt and weaker custody, schema exclusions, all chain classes,
  synthetic checkout labels, READY refusal, expected-chain mismatch, independent
  refreshes, residue positives/negatives, same-length receipt/census mutations,
  an unlisted nested manifest, and replay with live/source/export/salt access disabled.
- Packet validator: **0 blockers, 0 warns**, 26 CQs and 25 SPARQL files parsed.
  The first invocation lacked `rdflib`; the documented `pyyaml,rdflib` environment passed.
- CQ suite: **0 failures across 25 seed tests and 20 fixtures**. Existing golden
  coverage remains 1/25 status-covered, with no populated golden antecedent.
  This is existing CQ proof, not run-3 ontology ratification.
- No workspace package was edited; package-verify is not applicable.
- The initial commit hook rejected an invented API-key assignment in a negative
  residue test. The test now constructs that value at runtime; all 14 tests pass
  again. The generator and both pins were unchanged by this test-only fix.

Commands were run from the trusted lane with `UV_CACHE_DIR=~/.cache/beep/uv-cache`.
The generator and its tests use `uv run --offline --with pyyaml python`.
The packet validator uses `uv run --offline --with pyyaml,rdflib python`,
and the CQ suite uses `uv run --offline --with pyoxigraph python`.

| Population | First pin/refresh | Ordinary verify | Corrupted payload | Restored verify | Exact restoration |
| --- | ---: | ---: | ---: | ---: | --- |
| fleet | 0 | 0 | 1 | 0 | yes |
| synthetic | 0 | 0 | 1 | 0 | yes |

Each corrupted `.properties` file was restored in a `finally` block. Whole-tree
digests before and after the exercise are identical. Independent scans of file
names and contents found zero home, hostname, hostname-digest, raw pid, uid token,
runtime/proc/shared-memory, or process-bearing state-filename residues.
The built-in verifier also passes its full credential and absolute-path checks.
Excluded raw bytes, original source-line positions, and the unrecorded salt cannot
be reconstructed from a redacted pin. Those remain capture observations; replay
checks their internal accounting and all fields derivable from retained payloads.

Whole-tree SHA-256 uses sorted complete relative POSIX names and contents,
including the manifest. Each name and content is preceded by its length as an
8-byte big-endian integer.

- `run3b-fleet`: `562efb950efb492cac7041f71392080d9d4cc24401b99efdd992adca873aeec9`.
- `run3b-synthetic`: `95a38c4750ed389db338c5b4712663868e672812e0075c726c4bd3a3957caab3`.

## Frozen bytes

| Generator | SHA-256 |
| --- | --- |
| etl_fleet_corpus.py | 2b6fb03d818986fffcbc8951ec43b6b63dfb0d302c3a3c5a7ba45ecb7777e424 |
| etl_run3_fleet_corpus.py | 7d711673d80791ce2aa1c9a1d1d8da6e1ff1867a55962806355fdf44ffd378e3 |
| etl_run3_checkout_identity.py | bc3dbfb84ab802f8a25b8e3b7532856b00729ec76671c001934f4265c1705d39 |

The three frozen generators were read as source patterns, never imported or edited.
No changes exist under `run2-fleet/`, `run3-fleet/`, `run3-checkout-identity/`,
or `extraction/s5/`, `s6/`, `s7/`. `DECISIONS.md` and `ATLAS.md` are unchanged.

## Detached-worktree and committed-file proof

The detached worktree was created from implementation commit
`d5970819fe4f125cc3a2764a49119c53e9e87f53` with:

```sh
git worktree add --quiet --detach .beep/stage-b-verify-worktrees/d5970819fe4f HEAD
```

The trusted lane's offline PyYAML process stayed alive while `sys.executable`
ran the committed generator with the detached worktree as its working directory.
No command relied on the fresh worktree's untrusted `mise.toml`, and no trust
setting changed. The generator ran in ordinary verify mode, without an export
path or refresh flag. Exit code: **0**.

```json
{
  "fleet": {
    "bytes_emitted": 15598637,
    "events": 7186,
    "files_emitted": 719,
    "payload_bytes": 14345706,
    "payload_files": 718,
    "status": "verified"
  },
  "synthetic": {
    "bytes_emitted": 40762,
    "events": 10,
    "files_emitted": 9,
    "payload_bytes": 6575,
    "payload_files": 8,
    "status": "verified"
  },
  "verification": "PASS"
}
```

| Pin | Tracked payloads | Manifest payloads | All tracked files | Missing | Extra |
| --- | ---: | ---: | ---: | ---: | ---: |
| run3b-fleet | 718 | 718 | 719 | 0 | 0 |
| run3b-synthetic | 8 | 8 | 9 | 0 | 0 |

This inventory records the original detached-worktree capture proof. Current
security-repair hashes and byte totals are in the opening proof table.

The exact path sets match, not only their counts. Every committed pin byte
matches the final lane capture, including both manifests; the whole-tree hashes
match those recorded above. The detached worktree was clean and was removed
with `git worktree remove` after verification. No capture ran there.

HEAD-based knowledge and Markdown proof at the implementation commit:

- `bun run beep knowledge refs --check`: exit 0; zero live gated observations.
- `bun run beep knowledge semantic-delta`: exit 0; zero introduced findings,
  zero resolved findings, 491 unchanged inherited findings.
- `bun run beep laws effect-imports --mode markdown --check`: exit 0, dry-run;
  921 files and 310 fences scanned. Its 13 suggested files and two parser warnings
  are inherited and outside the changed paths. No suggested rewrites were applied.
- Commit hooks: Biome, gitleaks, typos, and commitlint passed. Biome applied no
  changes to the pinned bytes.

The report-only follow-up changes no generator or corpus bytes. Ordinary
verification and the HEAD-based knowledge gates are rerun at final HEAD before
the lane hands off.

## Deviations and remaining questions

1. The producer has a fourth observed checkout root, `contender-b`, for the
   withdrawn request. The consumer preserves it as a fourth synthetic token.
   Only the three attempt directories named in the brief are imported.
2. One fleet refresh replaced the initial implementation capture after final
   review found that the copied inventory predicate exempted nested manifests.
   The added regression and verifier fix preceded this final capture. The
   counts, excerpts, and tree hashes above describe the replacement pin.
3. The generator retains an explicit unclassified bucket for partial or
   conflicting terminal evidence. This avoids asserting an unsupported outcome.
   It is empty in the captured populations.
4. The detached verification worktree stays under this checkout's ignored
   `.beep/` directory to respect the lane boundary and writable roots. No
   full checkout is created in system-temp storage.

The proof-ledger writer remains a run-4 dependency. The pins do not resolve
cross-capture identity rigidity, absent task-hash cache joins, or future window loss.
Run 3 proper remains with the steward after PR-1 and PR-2 publication.

## Commits and handoff

Source/citation commit: `ff1cf545ef2c4a43806dc50323259f300218e573`.
Implementation commit: `d5970819fe4f125cc3a2764a49119c53e9e87f53`.

```text
feat(explorations): pin the run-3 Stage B corpora (loss population + synthetic scenario)
```

This report's committed-tree evidence is finalized in a documentation follow-up.
The final handoff identifies that report commit separately.
Paths are staged explicitly; `.claude/settings.json` and the parallel
`synthetic-fixture-report.md` remain outside this lane's commits.
No push, PR creation, or merge is performed. Fable owns publication.
