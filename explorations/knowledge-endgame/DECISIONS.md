# Decisions

<!--
Stage 2 log: dated Question / Answer / Rationale entries, including rejected
options. Parking and killing decisions land here too.
-->

## 2026-08-25 — Parked at capture (by operator intent)

**Question:** Should this synthesis become an active exploration workstream?

**Answer:** No — park immediately after capture.

**Rationale:** Operator, verbatim intent: "Keep it simple I'm not going to
pivot project direction but this is damn cool" — the capture exists so the
exploration is not lost, not to open a sixth front. The synthesis's own
conclusion supports parking: its critical path is almost entirely carried by
already-queued work in other packets — `explorations/packet-system-redesign`
MAP Amendments H (typed work plan), I (JSON-LD projection; acceptance = one
concrete cross-graph join), J (gate certificates, which are also the
forge-disaggregation prerequisite from Part IV), plus
`explorations/protocol-as-value` wave 1/2. The two genuinely new items are
recorded as reopen triggers, not goals: (a) the four-ring substrate/instance
workspace-geometry partition (Part IV's clone-and-go prerequisite; relates
to `explorations/computable-workspace-geometry`), and (b) the cold-agent
eval — a fresh agent in a fresh clone, counting the human facts it needs —
as the falsifiable core of the agents-as-FDEs claim (Part III).

**Reopen when:** H/I/J are ruled on; or the substrate/instance partition
becomes a live question; or the cold-agent eval is wanted as a harness; or
the training-data-flywheel coda (public projection site with linked-data/
AEO/GEO structure) is picked up as a real docs-surface goal.

**Rejected:** advancing to research (the ten lane reports exist but are
machine-local in the private internal docs area; promoting them into a public
RESEARCH.md is real redaction work that parking does not justify), and
graduating anything directly (violates the exploration pipeline and the
operator's stated non-pivot).
