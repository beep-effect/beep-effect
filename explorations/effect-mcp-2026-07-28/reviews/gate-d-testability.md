# Gate D — Acceptance testability review (2026-09-22)

Headless grok reviewer (`grok-4.6`, effort high, no web search) over `BRIEF.md`, `MAP.md`, `DECISIONS.md`, the two graduated goal packets, and the cited code. Note: the reviewer's checkout predated the #1177 merge, so citations into `RESEARCH.md` may reference pre-rc.117 wording.

**Verdict:** `not-ready`

Both GOAL.md files are under the 4000-character hard cap (3588 and 3997). Named beep commands such as `quality package-verify`, `ci lane test-integration`, and `lint reflection-artifacts` exist. An executor still cannot prove either SPEC: the matrix commands already pass on main, Goal A's host row is a placeholder, several criteria have no command, and Goal B's leak check and two-launch criterion would fail or stay ambiguous under a correct implementation.

## Findings

### AT-1 — blocker — Verification-matrix commands already pass on main and cannot prove the new criteria

- **Evidence:** Goal A matrix rows 'Kit rebase + client + conformance' and 'Each flipped host' are only `bun run beep quality package-verify` (goals/mcp-stateless-kit-and-drivers/SPEC.md:99-102). Goal B rows 'Gate keyed on launch' and 'Config field' are the same command for @beep/epistemic-server and @beep/ontology-config (goals/ontology-sidecar-stateless-identity/SPEC.md:91-93). Default package-verify runs beep:audit then docgen (packages/tooling/tool/cli/src/commands/Quality/internal/PackageVerify.ts:288-297). mcp-kit's audit path includes `beep:test:integration`: "bunx --bun vitest run test/integration --passWithNoTests" (packages/foundation/capability/mcp-kit/package.json:42) and that package has no test/integration directory. Driver packages use the same --passWithNoTests integration script. Those commands are green today without dual-read, client, conformance, InvalidParams, host pins, grant-expired permanence, or never-evict tests.
- **Proposal:** For every behavioral AC, add a matrix row that names the test file and exact `it(...)` title, plus `rg -n '<title>' <file>` (must match) and `bunx vitest run <file> -t '<title>'` (or package-verify after those files exist). Minimum Goal A titles: 2026-only tools/call with McpRequestContext present / McpServerClient absent; 2025 dual-read still passes; stdio discover then tools/call; HTTP SSE unwrap; api_key_required non-error; invalid arguments InvalidParams; each flipped host initialize returns JSON-RPC -32022. Minimum Goal B titles: keys the run on the launch; grant-expired permanence; never-evict. Do not treat a bare package-verify of a --passWithNoTests package as proof.
- **Grill question:** Which existing command, run against origin/main right now, would fail if dual-read, the 2026 client, or launch-keyed freeze were still missing?

### AT-2 — blocker — Goal A host proof is a non-command placeholder, and GOAL.md only verifies m365

- **Evidence:** SPEC matrix: "Each flipped host | `bun run beep quality package-verify <@beep/host-package>` | Passes" (goals/mcp-stateless-kit-and-drivers/SPEC.md:102). That token is not a workspace name. Real names are @beep/m365-mcp, @beep/uspto-mcp, @beep/gov-legal-mcp, @beep/nlp-mcp, @beep/law-practice-server, @beep/practice-kg-mcp. GOAL.md verification lists only mcp-kit and m365-mcp (goals/mcp-stateless-kit-and-drivers/GOAL.md:64-70) while the AC requires m365, uspto, gov-legal, and practice-kg pins plus initialize -32022 (SPEC.md:89-91). There is no rg for leftover `v2025_06_18` and no named -32022 assertion. PLAN's practice-kg `.mcpb` smoke is `bun run smoke:compiled` in apps/practice-kg-mcp/package.json:37 and is absent from SPEC.
- **Proposal:** Replace the placeholder with six explicit commands: package-verify @beep/m365-mcp, @beep/uspto-mcp, @beep/gov-legal-mcp, @beep/law-practice-server, @beep/practice-kg-mcp, and @beep/nlp-mcp (or skip nlp-mcp when the exception-ledger hold is present). Add `rg -n 'v2025_06_18' packages/drivers/{m365,uspto,gov-legal}-mcp packages/law-practice/server apps/practice-kg-mcp` with required result no matches; add `bun run --filter @beep/practice-kg-mcp smoke:compiled` if `.mcpb` remains in scope; copy that list into GOAL.md verification.
- **Grill question:** If an executor runs only the GOAL.md verification block, which flipped host has actually been proven?

### AT-3 — blocker — Goal B's two-launch plus initialize criterion cannot be proven as specified

- **Evidence:** AC: "a second launch of the sidecar yields a different run id, and `initialize` is rejected per the adapter" (goals/ontology-sidecar-stateless-identity/SPEC.md:71-73). D-run-key keys on `launch:<digest>` of the verified bearer (explorations/effect-mcp-2026-07-28/DECISIONS.md:170-176). The HTTP harness reuses one fixture: `export const token = Redacted.make("ontology-mcp-http-test-token")` (apps/professional-desktop/test/integration/support/ontology-mcp-harness.ts:37). Two in-process servers with that token must share a digest. Lane 21-r2: a 2026-only HTTP `initialize` with an id is classified as stateless and missing `MCP-Protocol-Version` returns HTTP 400 `-32020`, while stdio `initialize` is `-32022` (explorations/effect-mcp-2026-07-28/research/21-r2-hosts.md:23-42). Matrix proof is `bun run beep ci lane test-integration --affected --base origin/main` (SPEC.md:92), which already runs ontology-mcp-http.test.ts including initialize on 2025 (apps/professional-desktop/test/integration/ontology-mcp-http.test.ts:189-204). GOAL.md omits that lane entirely (goals/ontology-sidecar-stateless-identity/GOAL.md:70-76).
- **Proposal:** Split the AC. Prove bearer-digest keying with two harness tokens (same token → same run id; different tokens → different run ids) in a named ontology-mcp-http.test.ts case. Drop "second launch yields a different run id" unless the test starts two processes that each mint a bearer. Specify the initialize request (headers, body) and the exact status/code: HTTP 400 `-32020` without MCP-Protocol-Version, or JSON-RPC `-32022` on a well-formed 2026 initialize. Put `bun run beep ci lane test-integration --affected --base origin/main` in GOAL.md verification.
- **Grill question:** With the fixture token reused across withHttpServer calls, what observable would show that a second launch minted a new run rather than that the digest of one bearer is stable?

### AT-4 — blocker — The raw-bearer leak command would fail a correct digest and already fails today

- **Evidence:** Matrix: "No raw bearer leak | `rg -n 'Redacted.value' apps/professional-desktop/server packages/epistemic/server/src` | Only the verifier" (goals/ontology-sidecar-stateless-identity/SPEC.md:94). Live hit is not the verifier: `export const rpcSessionAuthorizationHeader = (token: Redacted.Redacted): string => \`Bearer ${Redacted.value(token)}\`` (apps/professional-desktop/server/RpcSessionAuth.ts:44). `requireRpcSessionToken` (RpcSessionAuth.ts:88-119) never reads the token for a digest. SPEC target surface: RpcSessionAuth.ts "exposes the verified-bearer digest to the run-key layer without exposing the token" (SPEC.md:40-41). Computing `launch:<digest>` requires Redacted.value (or equivalent) on the bearer. Required result "Only the verifier" already fails and will fail harder after the intended change. Epistemic server currently has zero Redacted.value hits, so a digest added there would also fail the row.
- **Proposal:** Replace the row with a leak proof that can pass: `rg -n 'Redacted.value' apps/professional-desktop/server packages/epistemic/server/src` must list only the header helper, the digest function, and the verifier; add `rg -n 'launch:' packages/epistemic/server apps/professional-desktop/server` asserting the ledger/span/log sites interpolate the digest string, not `Redacted.value`; add a test that the run-key service never equals the raw token. Name the digest helper so the allowlist is a closed set of files.
- **Grill question:** After `launch:<digest>` is composed from the bearer, which Redacted.value sites are still allowed, and how does the matrix say so without a human judging the rg output?

### AT-5 — major — nlp-mcp capture evidence is a glob, not a command, and sits outside the packet path

- **Evidence:** Goal A matrix: "Vendor capture | `history/<date>-nlp-mcp-capture.md` with the first stdio frame verbatim | Exists" (goals/mcp-stateless-kit-and-drivers/SPEC.md:104). From repo root that path is not goals/mcp-stateless-kit-and-drivers/history/. `<date>` is unsubstituted, so `test -f` cannot be run. AC still requires the file even when nlp-mcp is held (SPEC.md:92-93). GOAL.md verification does not mention the capture (GOAL.md:64-70). PLAN says record under `history/` after launching Claude Code via `.mcp.json` (PLAN.md:30-33). There is no beep subcommand that captures the first stdio frame. Exception ledger is "None yet" (SPEC.md:119-121) with no check that a hold row exists when nlp-mcp stays on 2025.
- **Proposal:** Pin the evidence path to `goals/mcp-stateless-kit-and-drivers/history/nlp-mcp-capture.md` (stable name). Add `test -f goals/mcp-stateless-kit-and-drivers/history/nlp-mcp-capture.md` and `rg -n 'initialize|server/discover' that file`. Add a second row: either `rg -n 'v2026_07_28' packages/drivers/nlp-mcp/src/Server.ts` matches, or `rg -n 'D-cli-contract' goals/mcp-stateless-kit-and-drivers/SPEC.md` matches an exception-ledger row. Document the capture recipe (Claude Code against a 2026-only nlp-mcp build) in PLAN as operator-attended evidence, not a beep command.
- **Grill question:** If no daily CLI speaks 2026, which single command proves both that the capture was taken and that the nlp-mcp hold was recorded?

### AT-6 — major — Several acceptance criteria have no matrix command at all

- **Evidence:** Goal A AC "mcp-kit README consumer table names the six consumers with their protocol state" (SPEC.md:94) has no row. Goal B ACs for OntologyChangeActor (SPEC.md:78), CORS header set (SPEC.md:79-81), "live-mcp-client.ts runs on the kit client" (SPEC.md:82), and decision 10 wording (SPEC.md:83-84) have no command. live-mcp-client.ts still does `client.initialize({ protocolVersion: "2025-06-18", ...})` and echoes `Mcp-Session-Id` (goals/ontology-agent-surface/ops/live-mcp-client.ts:24-60) and needs ONTOLOGY_MCP_URL / BEEP_DESKTOP_RPC_SESSION_TOKEN. Actor today is `urn:beep:desktop-rpc-session:mcp-client:${identity.clientId}` (packages/ontology/server/src/tools/OntologyToolHandlers.ts:91). Neither GOAL.md nor the matrix runs package-verify @beep/ontology-server or @beep/professional-desktop. professional-desktop beep:audit is "bun run beep:build && bun run beep:check && bun run beep:test && bun run beep:lint" with no integration tests (apps/professional-desktop/package.json:31).
- **Proposal:** Add rows: `rg -n 'm365-mcp|uspto-mcp|gov-legal-mcp|nlp-mcp|practice-kg|OntologyMcpTransport' packages/foundation/capability/mcp-kit/README.md`; `rg -n 'mcp-client:' packages/ontology/server/src` required result no matches and a named OntologyChangeActor test; `rg -n 'mcp-session-id' apps/professional-desktop/server/OntologyMcpTransport.ts` no matches plus a CORS test listing mcp-protocol-version, mcp-method, mcp-name; `rg -n '@beep/mcp-kit/client|server/discover' goals/ontology-agent-surface/ops/live-mcp-client.ts`; `rg -n 'a run is a sidecar launch' goals/agent-execution-authority/SPEC.md`. Add package-verify @beep/ontology-server. State that live-mcp-client is operator-attended with those env vars, or drop it from acceptance.
- **Grill question:** Which of those ACs would an executor be allowed to tick after only the commands already printed in GOAL.md?

### AT-7 — major — No mcp-session-id read is checked with a string search that hits comments and the header constant

- **Evidence:** Goal A matrix: "No session-id read | `rg -n -i 'mcp-session-id' packages/foundation/capability/mcp-kit/src` | No matches" (SPEC.md:103). Live hits include the comment "the `mcp-session-id` header minted at `initialize`" (packages/foundation/capability/mcp-kit/src/McpCaller.ts:25) and `const mcpSessionIdHeader = "mcp-session-id"` (SanitizedSpan.ts:202). Dropping the read while documenting the drop would still fail the row. The AC is "No `mcp-session-id` read remains" (SPEC.md:84).
- **Proposal:** Point rg at the read site, not the token: `rg -n 'mcpSessionIdHeader|Headers.get\(.*mcp-session-id' packages/foundation/capability/mcp-kit/src` with no matches. Keep a comment that the kit must not read the header. Add a 2026 dispatch test that `sessionId` is None even when the request carries mcp-session-id.
- **Grill question:** Is a comment that says the kit no longer reads mcp-session-id a failure of this row?

### AT-8 — major — Attacker OPTIONS → 403 has no named test and research says CORS may answer 204

- **Evidence:** Goal B AC: "Attacker-Origin `OPTIONS` answers 403" (SPEC.md:79-81). ontology-mcp-http.test.ts has no OPTIONS case; the origin test POSTs initialize (apps/professional-desktop/test/integration/ontology-mcp-http.test.ts:189-204). Transport comment: "`HttpMiddleware.cors` in `security` short-circuits every OPTIONS request with a 204 plus CORS headers" (apps/professional-desktop/server/OntologyMcpTransport.ts:258-262). CORS allowedHeaders still include mcp-session-id (OntologyMcpTransport.ts:103). Lane 24-r5: "UNVERIFIED by test: ontology-mcp-http.test.ts has no OPTIONS case" and "HttpMiddleware.cors short-circuits OPTIONS to HTTP 204" (explorations/effect-mcp-2026-07-28/research/24-r5-http-security-observability.md:189-210). test-integration is already green without an OPTIONS assertion. rpc-session-auth.test.ts still says "allows unauthenticated CORS preflight while guarding POST" (apps/professional-desktop/test/rpc-session-auth.test.ts:35-38).
- **Proposal:** Name ontology-mcp-http.test.ts cases: attacker-Origin OPTIONS status 403; allowed-Origin OPTIONS status (204 or 403, pick one and match D-origin); Origin-less POST 403; CORS allow-headers equal mcp-protocol-version, mcp-method, mcp-name and not mcp-session-id. Put those titles in the matrix. If CORS 204 is the live onion, change middleware order in the same PR so the OPTIONS 403 AC is not unsatisfiable.
- **Grill question:** If CORS still short-circuits OPTIONS to 204, is the 403 AC a test the executor writes, or a transport change that must land first?

### AT-9 — major — @beep/mcp-kit/client as a directory is not what the existing export map resolves, and no command proves the import

- **Evidence:** SPEC: `packages/foundation/capability/mcp-kit/src/client/**` exported as `@beep/mcp-kit/client` (goals/mcp-stateless-kit-and-drivers/SPEC.md:43-49). package.json exports `"./*": "./src/*.ts"` (packages/foundation/capability/mcp-kit/package.json:46-48), which resolves `@beep/mcp-kit/client` to `src/client.ts`, not `src/client/index.ts`. D-client-home claims "the `./*` export exists" (explorations/effect-mcp-2026-07-28/DECISIONS.md:191-192). Goal A AC that the client drives discover over stdio and HTTP is proven only by package-verify of mcp-kit (SPEC.md:85-86, 101). Relative imports inside mcp-kit would typecheck while consumers (`ontology-mcp-harness.ts`, `live-mcp-client.ts`, m365 Server.test.ts) fail.
- **Proposal:** Either require a barrel `src/client.ts` matching `./*`, or add `"./client": "./src/client/index.ts"` (and `./client/*`) to package.json and list that file in the matrix. Add a proving import in mcp-kit tests: `import { ... } from "@beep/mcp-kit/client"`. Add consumer rows that rg `@beep/mcp-kit/client` in the harness, live-mcp-client.ts, and m365 Server.test.ts.
- **Grill question:** After PR 1, which command fails if src/client/** exists but `@beep/mcp-kit/client` does not resolve?

### AT-10 — major — SPEC source hierarchy points at RESEARCH.md sections that do not exist, so adapter expected results cannot be read

- **Evidence:** Goal A source hierarchy: "RESEARCH.md §2 (adapter semantics), §3.1–3.2 (kit, hosts)" (goals/mcp-stateless-kit-and-drivers/SPEC.md:30-31) and "DECISIONS.md (G1–G9, 2026-09-21, 2026-09-22 rulings)" (SPEC.md:29). Goal B: "RESEARCH.md §3.3–3.4 with lanes 22-r3, 24-r5" (goals/ontology-sidecar-stateless-identity/SPEC.md:30). PLAN B P0 exit is that same §3.3–3.4 (PLAN.md:11). explorations/effect-mcp-2026-07-28/RESEARCH.md is still the template: "## External Landscape" / "<Prior art...>". There is no 2026-09-21 DECISIONS.md entry (only MAP.md:4-5 and BRIEF.md:4 mention a 2026-09-21 release update). research/sizing.md, cited by BRIEF.md:24 and MAP.md:4 for S1–S5a, is absent. Dual-read, -32022 vs -32020, and OPTIONS 204 vs 403 live in lane files, not in RESEARCH.md.
- **Proposal:** Point SPEC source hierarchy at the lane files that actually contain the numbered sections (20-r1, 21-r2, 22-r3, 24-r5, 11-u2), or write RESEARCH.md §2–§6 as a synthesis that quotes those lanes. Remove the 2026-09-21 DECISIONS citation or add that entry. Stop citing research/sizing.md until the file exists. Encode the adapter expected results the tests must assert (stdio -32022; HTTP initialize without MCP-Protocol-Version is 400 -32020) in SPEC so an executor does not need the missing sections.
- **Grill question:** If RESEARCH.md stays a stub, which on-disk document is the normative expected result for initialize on the sidecar?

### AT-11 — minor — The session-key test title in SPEC does not match the live it() string

- **Evidence:** Goal B constraint: delete the test "keys the run on the session, not the per-request client id" (goals/ontology-sidecar-stateless-identity/SPEC.md:59-61). Live title is "keys the run on the session, not on the per-request client id" (packages/epistemic/server/test/GovernedTierGate.test.ts:321). An rg of the SPEC string misses the test.
- **Proposal:** Quote the live title exactly, and add `rg -n 'keys the run on the launch' packages/epistemic/server/test/GovernedTierGate.test.ts` plus `rg -n 'keys the run on the session, not on the per-request client id' ...` with required result no matches.

## Disposition

| # | Disposition |
| --- | --- |
| AT-1 | fixed: matrix rows name test files and it() titles with vitest -t commands |
| AT-2 | fixed: six explicit package-verify commands; v2025_06_18 grep over flipped hosts |
| AT-3 | fixed: two-bearer / same-bearer cases; initialize rejection shapes specified |
| AT-4 | fixed: leak row lists the allowed Redacted.value sites and a launch: interpolation check |
| AT-5 | fixed: capture path pinned to history/nlp-mcp-capture.md; either/or outcome row |
| AT-6 | fixed: rows for README consumers, actor, CORS, live client, decision 10 |
| AT-7 | fixed: rg on mcpSessionIdHeader (moved to Goal B with the deletion) + stray-header test |
| AT-8 | fixed: OPTIONS/Origin-less cases named; Origin middleware ordered before CORS |
| AT-9 | fixed: explicit ./client export + consumer import rows |
| AT-10 | stale checkout: sections exist on main; hierarchy now also names the lane files |
| AT-11 | fixed: live it() title quoted exactly |
