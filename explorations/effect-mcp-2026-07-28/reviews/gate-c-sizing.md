# Gate C — Sizing skeptic review (2026-09-17)

Headless grok reviewer (`grok-4.6`, effort high, no web search) over the Stage 2 synthesis drafts, the packet, and the Effect clone at `a7a71921de`.

Line references to `RESEARCH.md`, `research/impact-matrix.md`, and `research/SOURCES.md` point at the
pre-revision drafts the reviewer read; the committed files were rewritten against these findings
(see the Disposition table).

**Verdict:** `not-ready`

The six-PR / 13–20 agent-day train is a mixed-list schedule wearing G4 clothes. P0 cannot be hosted-green at 2–3 days: Heavy / Test Integration is required and already 8-red on SSE framing, and the platform-node-shared patch is unverified. The 2026 client stack (no public ClientRpcs, header injector, SSE codec, stdio discover helper) has no owner, while P1 steals P4’s identity redesign. Corrected train (same columns): P0 snapshot+2025 SSE shim large 5–8 (none; grill native PgClient + patch re-roll); P1 kit fork rebase without identity large 5–7 (P0; 20-r1 api_key_required/InvalidParams); P1b beep 2026 client kit large 4–6 (P0; G4 — shrinks to optional if mixed); P2 nlp proving host medium 3–4 (P1+P1b+G4; includes Gate A #25 live probe); P3 remaining stdio hosts medium 3–4 (P1+P1b; parallel with P2); P4 sidecar identity+2026 HTTP extra-large 8–12 (P1+P1b+22-r3 Q1/Q2/Q4/Q8/Q9; identity lands with the sidecar pin); P5 docs small 1, RC swap medium 3–5 if a published RC exists (P0–P4). Two goals: A=P0–P3+P1b, B=P4, P5 shared. G4-as-written total ~28–45 days; mixed-list total ~16–24.

## Findings

### SIZING-B1 — blocker — P0 cannot be hosted-green at medium 2–3 days

- **Evidence:** Draft P0 is “medium (2–3)” and “Depends on: none”, and claims the pin plus SSE client fix plus patch verify plus native sql-pg audit fit that box (`RESEARCH.md:259-260`). Spike Wave 2 only proved `check` 251/251 after two edits (`explorations/effect-mcp-2026-07-28/research/25-r4-spike-census.md:41-43`). Wave 4 left `@beep/professional-desktop` integration **8 failed / 22 passed**, every failure `RpcClientDefect: Error decoding HTTP response` (`explorations/effect-mcp-2026-07-28/research/25-r4-spike-census.md:60-72`). Mechanism is snapshot `layerHttp` rewriting multi-message bodies to SSE (`effect:packages/effect/src/unstable/ai/McpServer.ts:1610-1616`, `effect:packages/effect/src/unstable/ai/McpServer.ts:1708-1727`) against a harness that is `RpcClient.layerProtocolHttp` + `RpcSerialization.layerJsonRpc()` (`apps/professional-desktop/test/integration/support/ontology-mcp-harness.ts:183-197`). Generic HTTP decode raises `protocolDefect("Error decoding HTTP response")` on non-JSON (`effect:packages/effect/src/unstable/rpc/RpcClient.ts:922-926`). `quality:test-integration` is a required Heavy lane (`packages/tooling/tool/cli/src/commands/Ci/CiLane.ts:411-418`; `packages/tooling/tool/cli/src/commands/Quality/internal/GithubChecks.ts:440-445`). A catalog bump is not docs-only, so Heavy runs once `ready-for-heavy` lands; tier-1 green after the two type fixes is a trap. The node-shared write patch is still keyed to `4.0.0-rc.115` (`package.json:328-331`); dist apply to the pkg.pr.new tarball is UNVERIFIED and a failed patch silently restores the write-offset bug (`explorations/effect-mcp-2026-07-28/research/23-r4-snapshot-blast-radius.md:152-160`). Default hosted SQL tests are in-process PGlite and will not catch Date/OID decode (`explorations/effect-mcp-2026-07-28/research/23-r4-snapshot-blast-radius.md:65-76`). Spike explicitly did not run packages outside the filter, Testcontainers Postgres, coverage, or docgen (`explorations/effect-mcp-2026-07-28/research/25-r4-spike-census.md:96-98`; `explorations/effect-mcp-2026-07-28/research/23-r4-snapshot-blast-radius.md:220-223`). G7 only promised hosts stay *compiling* (`explorations/effect-mcp-2026-07-28/DECISIONS.md:91-97`); Yeet mergeable requires hosted checks green.
- **Proposal:** Resize P0 to large (5–8) and make hosted-green a hard exit: (1) 16 catalog URLs + the two proven type fixes; (2) re-roll and re-key the platform-node-shared patch against the snapshot dist and assert `bun install` applied it (do not trust the rc.115 key even if the tarball still *reports* rc.115); (3) a 2025-only SSE-stripping JSON-RPC decode shim for the ontology harness and `live-mcp-client.ts` — keep `initialize`, do not invent a 2026 transport here; (4) a native sql-pg timestamp/enum proof or a written waiver that production is PGlite-only; (5) docgen TS7056 check. Do not call P0 mergeable on Wave 2 `check` green. If you refuse the SSE shim in P0, P0 cannot merge.
- **Grill question:** Is P0 allowed to merge on tier-1 check+unit only (G7 letter), or must Heavy / Test Integration be green before the pin lands (Yeet letter)?

### SIZING-B2 — blocker — 13–20 days is the mixed-list number; the 2026 client stack has no owner

- **Evidence:** Draft total is “six PRs, roughly 13–20 agent-days” (`RESEARCH.md:267`). G4 is still `v2026_07_28` only (`explorations/effect-mcp-2026-07-28/DECISIONS.md:54-62`). Under G4 every in-repo wire client dies on `initialize` (`explorations/effect-mcp-2026-07-28/research/21-r2-hosts.md:358-371`). A 2026 rewrite needs all six of: a client RPC group with `server/discover` (public `McpSchema.ClientRpcs` is still Initialize-only, `effect:packages/effect/src/unstable/ai/McpSchema.ts:2982-3034`; the 2026 group is `@internal`, `effect:packages/effect/src/unstable/ai/internal/mcpSchema/v2026_07_28.ts:614-625`); HTTP injection of `MCP-Protocol-Version` / `Mcp-Method` / `Mcp-Name` (generic `layerProtocolHttp` does not set them, `explorations/effect-mcp-2026-07-28/research/21-r2-hosts.md:64-69`); `_meta` protocolVersion+clientCapabilities on every request; drop session replay; CORS `mcp-method`/`mcp-name`; replace stdio `initialize` sequences (`explorations/effect-mcp-2026-07-28/research/21-r2-hosts.md:373-385`). There is no public Effect helper that builds a 2026 `RpcClient` (`effect:packages/effect/src/unstable/ai/McpProtocol.ts:196`; `explorations/effect-mcp-2026-07-28/research/21-r2-hosts.md:382-385`). Lane 21 said that injector “belongs next to `sanitizedToolkit`” (`explorations/effect-mcp-2026-07-28/research/21-r2-hosts.md:504-507`). Draft P0 only names SSE framing of the *current* 2025 harness (`RESEARCH.md:260`); P1 names a “2026 test seam” as one bullet inside a 4–6 day kit PR (`:261`); P4 names “harness on the 2026 wire” (`:264`). That is one missing product (beep-owned 2026 client) split across three PRs and sized as a footnote. If G4 is relaxed to a mixed list, today’s harnesses survive and 13–20 becomes plausible; the draft never states that fork.
- **Proposal:** Add P1b “2026 client kit” as a first-class PR: beep `ClientRpcs` including `server/discover`, HTTP header+_meta injector, stdio NDJSON helper that can send discover, reuse the P0 SSE decode. Size large (4–6) under G4; optional/deferred if the grill picks mixed lists. Recost the train on that fork: G4-as-written ~28–45 agent-days; mixed-list ~16–24 with P2/P3 shrinking to mechanical. Do not publish 13–20 as the G4 number.
- **Grill question:** Is G4 still 2026-only on every host? If mixed lists win, P1b/P2/P3 collapse; if G4 stands, the client kit is a large PR the current train does not contain.

### SIZING-B3 — blocker — P1 identity redesign is P4 work; the two-goal split forces a dual-path kit

- **Evidence:** P1 includes “identity from `McpRequestContext`; `McpCallerIdentity` redesign” and depends on “grill items 1, 2, 4” (`RESEARCH.md:261`). P4 is “run identity anchor” at large 4–6, “the only one that cannot start until the identity anchor is decided”, with goals split P0–P3 vs P4 (`:264-269`). The kit today mints `CurrentMcpCaller` only when `McpServerClient` is `Some` (`packages/foundation/capability/mcp-kit/src/SanitizedSpan.ts:302-322`). Under G4 that service is never installed; the gate refuses (`explorations/effect-mcp-2026-07-28/research/22-r3-governance-identity.md:193-201`; Gate B correction `explorations/effect-mcp-2026-07-28/research/verification/README.md:47,51-53`). 22-r3 Q9 is whether identity must land in the *same PR* as the sidecar protocol flip (`explorations/effect-mcp-2026-07-28/research/22-r3-governance-identity.md:248`). G9’s rationale is that leaving any host on the session protocol keeps the `McpServerClient` path alive inside the kit (`explorations/effect-mcp-2026-07-28/DECISIONS.md:113-124`). Goal A flipping drivers to 2026 while Goal B still needs sessions therefore requires the kit to serve both identities until P4 — the complexity G4 was meant to remove. P1 shipping `sessionId` retired / `clientId` from `McpRequestContext` also pre-empts 22-r3 options C (signed run token) and D (per-launch bearer) (`explorations/effect-mcp-2026-07-28/research/22-r3-governance-identity.md:209-214`). P4 then rewrites P1.
- **Proposal:** Strip identity *redesign* out of P1. P1 only rebases the fork and dual-reads `McpServerClient` *or* `McpRequestContext` so 2026 is not `None`. Implement the chosen run key in P4 in the same PR as the sidecar protocol pin (22-r3 Q9). Two-goal split: Goal A = snapshot + kit rebase + 2026 client kit + stdio hosts; Goal B = sidecar identity. Do not start Goal A’s kit identity schema until the grill names the anchor.
- **Grill question:** 22-r3 Q1/Q2/Q9: is a run still an MCP conversation, does G4 apply to the sidecar, and must that answer land in the same PR as the sidecar pin?

### SIZING-M1 — major — P3 “mechanical 1–2 total” erases the real stdio canaries

- **Evidence:** Draft P3 is “mechanical pin flips, `instructions`, uspto array `structuredContent`, JSDoc” at “small (1–2 total)” depending on P2 (`RESEARCH.md:263`). Impact matrix C tags `initialize` rejection as **forced rewrite** on clients (`research/impact-matrix.md:47`). m365 is the only driver with a real stdio JSON-RPC conversation test and “**is** the migration canary for stdio framing” (`explorations/effect-mcp-2026-07-28/research/21-r2-hosts.md:456-458`, `:299-314`). practice-kg `smoke.ts` + `SmokeInitializeResponse` are load-bearing for the `.mcpb` artifact; a G4 flip without rewriting them fails `smoke:compiled` (`explorations/effect-mcp-2026-07-28/research/21-r2-hosts.md:316-327`, `:471-474`). uspto `S.Array` success is the only confirmed non-object `structuredContent` host (`explorations/effect-mcp-2026-07-28/research/21-r2-hosts.md:136-142`, `:461-465`). P2 takes nlp (no wire test today) as the proving host and serializes P3 behind it (`explorations/effect-mcp-2026-07-28/research/21-r2-hosts.md:448-454`). G2’s kit→proving-host→rest order (`explorations/effect-mcp-2026-07-28/DECISIONS.md:28-39`) does not make the rest mechanical under G4.
- **Proposal:** Resize P3 to medium (3–4). Name the m365 wire rewrite and practice-kg smoke/.mcpb as forced, not “mechanical”. After P1+P1b, P3 can run parallel with P2; nlp is the launcher blast radius, m365 is the stdio canary — they are not a pipeline. If G4 is mixed-list, then and only then is P3 actually 1–2.

### SIZING-M2 — major — P1 4–6 days concatenates three PRs

- **Evidence:** Draft P1 is one “large (4–6)” row covering: rebase `sanitizedToolkit` onto snapshot `registerToolkit` (Exclude `McpRequestContext`, `outputSchema`, strict decode, failure classification); identity from `McpRequestContext`; `McpCallerIdentity` redesign; `api_key_required` and invalid-argument projections; a 2026 test seam that does not go through `server.callTool`; README/comments (`RESEARCH.md:261`). The break table is a full fork drift: exclusions, `callTool` still stateful-only, no typed 2026 stub, `outputSchema` missing, `Tool.Strict` not passed, `Stream.runLast`, `omitRequestServices`, `isError`/`structuredContent` policy (`explorations/effect-mcp-2026-07-28/research/20-r1-mcp-kit.md:88-112`, `:141-146`). `server.callTool` cannot prove 2026 (`effect:packages/effect/src/unstable/ai/McpServer.ts:222-224`; `explorations/effect-mcp-2026-07-28/research/20-r1-mcp-kit.md:258-262`). `SanitizedSpan.ts` is the coverage ratchet’s hottest file (86.36%, 9 uncovered) (`explorations/effect-mcp-2026-07-28/research/20-r1-mcp-kit.md:234-244`). Heavy / Coverage Regression is required (`packages/tooling/tool/cli/src/commands/Ci/CiLane.ts:419-428`). Identity redesign is P4 (B3). The 2026 test seam *is* P1b (B2). What remains is still a large rebase plus two forced kit decisions plus a coverage rewrite.
- **Proposal:** Keep P1 as the fork rebase + dual identity *read* + `api_key_required`/`InvalidParams` decisions + coverage ratchet; size 5–7. Move identity schema to P4. Move the 2026 HTTP/stdio seam to P1b. Do not advertise 4–6 for the concatenated blob.

### SIZING-M3 — major — P5 RC swap is a second snapshot campaign, not a 1-day closeout

- **Evidence:** Draft P5 is “swap snapshot to the published RC, optional fork docs PR, mcp-kit promotion record and architecture docs 03/09/12” at “small (1)” depending on P0–P4 (`RESEARCH.md:265`). G1 already says the swap happens when an RC carries the adapter (`explorations/effect-mcp-2026-07-28/DECISIONS.md:14-21`); that RC does not exist yet. Precedent #1060 re-rolled the node-shared patch, lockstep-pinned 16 URLs, and fought docgen TS7056 (`explorations/effect-mcp-2026-07-28/research/23-r4-snapshot-blast-radius.md:191-207`). The next RC repeats patch re-key, sql-pg decode, and whatever else lands after `a7a71921de`. Architecture 03/09/12 identity language is **forced when the anchor changes** (`research/impact-matrix.md:82`) — that is P4 output, not a closeout chore. Fork `MCP.md` is G8 optional/non-blocking (`explorations/effect-mcp-2026-07-28/DECISIONS.md:104-111`).
- **Proposal:** Split P5: (P5a) promotion record + architecture identity language, small (1), depends on P4; (P5b) RC swap, sized as a new P0-class pin (medium 3–5) when a published RC exists, not on the MCP train’s critical path; (P5c) fork docs, optional. Do not block closeout on an unpublished RC.

### SIZING-M4 — major — Depends-on “grill items 1, 2, 4 / 9 / 8” are unbound

- **Evidence:** P1 depends on “grill items 1, 2, 4”; P2 on “grill item 9”; P4 on “grill items 1, 8” (`RESEARCH.md:261-264`). Synthesis §5 lists four unnumbered G4 options (`:249-252`). 22-r3 grill questions are numbered 1–9 (`explorations/effect-mcp-2026-07-28/research/22-r3-governance-identity.md:234-248`). 20-r1 has its own 1–5 (`explorations/effect-mcp-2026-07-28/research/20-r1-mcp-kit.md:285-297`). 24-r5 has 1–8 (`explorations/effect-mcp-2026-07-28/research/24-r5-http-security-observability.md:456-465`). Gate A finding 27 listed six unowned grill decisions (`explorations/effect-mcp-2026-07-28/research/00-plan-review.md:58`). If P2’s “item 9” is 22-r3 Q9 (identity in the same PR as the protocol flip), nlp-mcp has no `GovernedTierGate` (`explorations/effect-mcp-2026-07-28/research/22-r3-governance-identity.md:8-9`) and must not wait on sidecar identity. The Depends-on column cannot be executed as written.
- **Proposal:** Replace numbered “grill items” with named decisions: G4 posture; 20-r1 `api_key_required`/`InvalidParams`; 22-r3 run-key (Q1/Q2/Q4/Q8/Q9); 24-r5 Origin/CORS browser scope. Bind each PR to those names. Drop P2’s dependency on sidecar Q9.

### SIZING-M5 — major — Required coverage/docgen work has no owner; spike holes are treated as sized

- **Evidence:** Any `sanitizedToolkit` replacement must keep the span ratchet, a protocol-matching caller ratchet, the `api_key_required` envelope-or-decision, and host `SanitizedSpan` proofs (`explorations/effect-mcp-2026-07-28/research/20-r1-mcp-kit.md:251-256`). Coverage is a required Heavy lane (`packages/tooling/tool/cli/src/commands/Ci/CiLane.ts:419-428`). No PR row names the ratchet. #1060 had to exclude vendored effect-ontology from docgen for TS7056; that risk is UNVERIFIED on `a7a71921de` (`explorations/effect-mcp-2026-07-28/research/23-r4-snapshot-blast-radius.md:171-175`, `:220-223`). Spike Wave 3/4 filtered to 12 packages and skipped Testcontainers (`explorations/effect-mcp-2026-07-28/research/25-r4-spike-census.md:46-50`, `:96-98`). Draft P0 still prices the pin as if Wave 2 `check` were the hosted surface (`RESEARCH.md:260`).
- **Proposal:** Put the mcp-kit coverage ratchet in P1’s exit criteria. Put a docgen TS7056 run in P0. Do not treat the filtered spike as a hosted-proof. If native sql-pg is in play, the proof test is P0 scope, not an audit comment.

### SIZING-N1 — minor — P4 CORS `mcp-method`/`mcp-name` may be n/a for in-repo clients

- **Evidence:** P4 lists “CORS `mcp-method`/`mcp-name`” (`RESEARCH.md:264`). Impact matrix tags it **forced** for any browser-origin client, natural otherwise (`research/impact-matrix.md:63`). 24-r5 found no renderer `src/` caller of `/mcp`; in-repo HTTP tests use Effect `HttpClient`/`toWebHandler` and skip CORS (`explorations/effect-mcp-2026-07-28/research/24-r5-http-security-observability.md:91-97`, `:61-62`). Synthesis §4 also logs no in-repo browser-origin client (`RESEARCH.md:229`).
- **Proposal:** Keep CORS as a one-line allow-list edit inside P4, not a size driver, unless the grill answers 24-r5 Q1 that Tauri/portless/workbench `/mcp` is in scope.

### SIZING-N2 — minor — P4 omits grant-expired permanence and actor/ledger alignment; kit protocol helper is unowned

- **Evidence:** Impact matrix D tags grant-expired permanence as natural and `OntologyChangeActor` alignment as natural (`research/impact-matrix.md:70-71`). 22-r3: grant-expired permanence is comment-enforced and untested (`explorations/effect-mcp-2026-07-28/research/22-r3-governance-identity.md:65`); actor URNs are already per-request `clientId` while the run is per-session (`:67`). Draft P4 lists run identity, CORS, Origin, 2026 harness, span correlation — not those two (`RESEARCH.md:264`). 21-r2’s kit `stdioProtocols`/`httpProtocols` constant is “the actual G4 switch” (`explorations/effect-mcp-2026-07-28/research/21-r2-hosts.md:496-500`) and sits in no PR.
- **Proposal:** Add to P4 exit criteria: grant-expired+never-evict test, actor URN keyed to the new run id. Put the kit protocol-list helper in P1 (it is the G4 switch, six host literals otherwise).

## Disposition

| # | Disposition |
| --- | --- |
| SIZING-B1 | Fixed. S0 resized large 5–8 with hosted Heavy / Test Integration as the exit; SSE shim, patch assert, sql-pg proof, docgen listed. |
| SIZING-B2 | Fixed. S1b 2026 client kit added; totals given per fork (28–45 vs 16–24). |
| SIZING-B3 | Fixed. Identity out of S1; S4 lands identity with the sidecar pin; goal split conditioned on D-run-key. |
| SIZING-M1 | Fixed. S3 medium 3–4; m365 named as the stdio canary; S2/S3 parallel. |
| SIZING-M2 | Fixed. S1 5–7 with the 2026 seam moved to S1b. |
| SIZING-M3 | Fixed. S5 split into S5a docs and S5b RC swap off the critical path. |
| SIZING-M4 | Fixed. Named decisions replace grill items. |
| SIZING-M5 | Fixed. Coverage ratchet in S1 exit; docgen in S0; spike holes named in S0 scope. |
| SIZING-N1 | Accepted. CORS is a one-line edit inside S4 unless D-origin brings browser clients in scope. |
| SIZING-N2 | Fixed. Grant-expired test and actor alignment are S4 exit criteria; protocol-list helper in S1. |
