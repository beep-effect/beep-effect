# Gate C — Pre-mortem review (2026-09-17)

Headless grok reviewer (`grok-4.6`, effort high, no web search) over the Stage 2 synthesis drafts, the packet, and the Effect clone at `a7a71921de`.

Line references to `RESEARCH.md`, `research/impact-matrix.md`, and `research/SOURCES.md` point at the
pre-revision drafts the reviewer read; the committed files were rewritten against these findings
(see the Disposition table).

**Verdict:** `not-ready`

If this synthesis is shaped into a goal and shipped, the most likely six-week failure is not a type error. It is that G4 is still open, the PR train pretends it is a per-host pin, and both answers (keep 2026-only, or relax to mixed lists) kill or fake the proving host. Five load-bearing claims were checked against Effect a7a71921de and the repo: initialize rejection 404/-32601 with 2026 headers else 400/-32020 / stdio -32022 is true; HTTP clientId is per-POST and no McpRequestContext field is server-minted and stable is true; HTTP notifications/cancelled does not interrupt another POST on 2026 is true; public McpSchema.ClientRpcs still has Initialize and no server/discover is true; kit identity is skipped when McpServerClient is absent is true. Two claims that the grill and PR train actually rest on are overstated or omitted: mixed lists do not deliver 2026 to initialize clients (they mint a 2025 session even when protocolVersion is 2026-07-28), and P1/matrix identity from McpRequestContext.clientId is the run key the gate already forbids.

## Findings

### PM-1 — blocker — Mixed-list G4 relaxation is a 2025 session trap, not a 2026 adoption path

- **Evidence:** RESEARCH.md:84-87 and :241-245 sell mixed lists as an upstream-designed dual-era feature that 'keep initialize alive through the first stateful adapter.' They do not say what that initialize does. effect:packages/effect/src/unstable/ai/internal/mcpRuntime.ts:182-187 selectStatefulProtocol only considers runtime._tag === 'Stateful', then falls through to the first stateful adapter. effect:packages/effect/test/unstable/ai/McpServer/ProtocolAdapters.test.ts:704-721: initialize offering protocolVersion 2026-07-28 returns 200, mints Mcp-Session-Id, and negotiates 2025-11-25. explorations/effect-mcp-2026-07-28/research/11-u2-stateless-runtime.md:209-213 already recorded this. Default Claude Code / Codex / grok stdio still send initialize (RESEARCH.md:42-46). Six weeks after 'relax G4 to mixed,' every real launcher is still on a minted 2025 session; the 2026 kit path is untested; a later G4 cut then dies on initialize.
- **Proposal:** Rewrite §5 option 2/3 so mixed lists are described as 'initialize clients stay stateful, even if they offered 2026-07-28; 2026 is reached only by skipping initialize and sending server/discover plus headers/_meta.' Do not let the grill pick mixed as the way to keep .mcp.json working and also 'adopt 2026.' If mixed is chosen, the proving-host acceptance test is a 2026 client that never calls initialize, not a green initialize handshake.
- **Grill question:** If the grill picks mixed lists, do you accept that Claude Code, Codex, and grok — all of which still send initialize — will keep getting a minted 2025 session even when they offer protocolVersion 2026-07-28, and that this does not count as 2026 adoption?

### PM-2 — blocker — Keep-G4 proving host ships a server the operator's own CLIs cannot speak

- **Evidence:** G4 is v2026_07_28 only (explorations/effect-mcp-2026-07-28/DECISIONS.md:54-62). G6 says external agent clients do not gate the flip (DECISIONS.md:78-85). RESEARCH.md:235-237 then admits .mcp.json launches nlp-mcp for those same binaries. nlp-mcp is still protocols: [v2025_06_18] (packages/drivers/nlp-mcp/src/Server.ts:115) and is the only in-repo server entry (.mcp.json:25-28). A 2026-only list rejects initialize: HTTP 404/-32601 with 2026 headers (effect:packages/effect/src/unstable/ai/internal/mcpRuntime.ts:427-434; effect:packages/effect/test/unstable/ai/McpServer/v2026_07_28.test.ts:131-138) or HTTP 400/-32020 on bare initialize (mcpRuntime.ts:61-65, :227-230; v2026_07_28.test.ts:158-180); stdio -32022 without data.supported (mcpRuntime.ts:33, :319-326). grok has no stdio modern opt-in (RESEARCH.md:42-46; explorations/effect-mcp-2026-07-28/research/14-s2-ecosystem.md:164-166). Live first-message capture is still UNVERIFIED (RESEARCH.md:221-222; Gate A finding 25 at explorations/effect-mcp-2026-07-28/research/00-plan-review.md:57). P2 is 'proving host nlp-mcp' sized small 1–2 days including an operator contract (RESEARCH.md:262). Six weeks later unit tests are green, .mcp.json nlp is dead, and G6 is the excuse.
- **Proposal:** Make Gate A #25 a P2 entrance criterion, not a footnote. If G4 is kept, P2 must include: captured first-message from Claude Code, Codex, and grok against a 2026-only nlp-mcp; the exact env/flag contract; and an explicit grok unsupported decision in the host README and .mcp.json comments. Drop G6 as a reason to skip that proof for the proving host. Do not size that as 1–2 agent-days.
- **Grill question:** Is nlp-mcp allowed to flip to v2026_07_28 while grok cannot opt in, Claude/Codex still default to initialize, and no live first-message capture exists — and if yes, what is the operator-visible definition of 'proving host'?

### PM-3 — blocker — P1 'identity from McpRequestContext' is the run key the gate already forbids; P4 is too late

- **Evidence:** RESEARCH.md:88-90: HTTP clientId is minted per POST; no McpRequestContext field is server-minted and stable. Confirmed: McpRequestContext.clientId (effect:packages/effect/src/unstable/ai/McpSchema.ts:2824-2835) is the only server-minted field; McpServerClient.protocolVersion is StatefulProtocolVersion, which excludes 2026-07-28 (effect:packages/effect/src/unstable/ai/McpProtocol.ts:33; McpSchema.ts:2848-2860). Kit identity is built only when McpServerClient is Some (packages/foundation/capability/mcp-kit/src/SanitizedSpan.ts:303-322); on 2026, ClientRequestRpcs never installs that middleware (effect:packages/effect/src/unstable/ai/internal/mcpSchema/v2026_07_28.ts:614-625), so CurrentMcpCaller is None and GovernedTierGate refuses (packages/epistemic/server/src/GovernedTierGate/GovernedTierGate.gate.ts:345-348). The gate already documents that clientId cannot key a run because it would open a new run per dispatch and collapse every chain to a genesis row (gate.ts:188-197). OntologyChangeActor already attributes by clientId, so provenance and ledger disagree today (packages/ontology/server/src/tools/OntologyToolHandlers.ts:80-91). Against that, impact-matrix.md:29 marks 'Identity from McpRequestContext (clientId, clientInfo, requestMetadata)' **forced** for the sidecar gate key, and RESEARCH.md:261 puts 'identity from McpRequestContext; McpCallerIdentity redesign' in P1 while RESEARCH.md:264-269 puts the run-anchor decision in P4 and claims P4 'cannot start until the identity anchor is decided' and is the only design-heavy goal. Six weeks later either mutations are fail-closed (G9 sidecar in DECISIONS.md:113-124 is a dead surface) or the ledger is one-row chains keyed on per-POST ids / attacker-controlled requestState.
- **Proposal:** Collapse kit identity and GovernedTierGate run-key into one grill decision before P1. Strike clientId / clientInfo / requestMetadata as the HTTP run key. Name the mint (per-launch bearer vs shell token vs HMAC requestState) and the freeze/TTL/hash-chain tests that must move with it. Do not start P1's McpCallerIdentity redesign, and do not split P0–P3 vs P4, until that anchor exists. Align OntologyChangeActor in the same change.
- **Grill question:** What mints the GovernedTierGate run key on HTTP after initialize and mcp-session-id are gone, given clientId is per-POST, and why would P1 be allowed to redesign McpCallerIdentity before that answer exists?

### PM-4 — major — §6 PR train is unexecutable and hides the G4/identity critical path

- **Evidence:** RESEARCH.md:249-252: 'no recommendation recorded' on G4, then four unnumbered options including split G9. RESEARCH.md:261-264: P1 depends on 'grill items 1, 2, 4'; P2 on 'grill item 9'; P4 on 'grill items 1, 8'. Those numbers do not exist in RESEARCH.md, impact-matrix.md, or explorations/effect-mcp-2026-07-28/ops/manifest.json:8-12 (four unnumbered openQuestions). A repo-wide grep for 'grill item' in the packet is empty. Split G9 is listed as a grill option (RESEARCH.md:252) without saying it reopens G9's own rationale that any leftover stateful host keeps the McpServerClient path alive in the kit (DECISIONS.md:122-124). RESEARCH.md:267-269 then asserts 'the evidence supports a two-goal train' that starts P0–P3 before the identity anchor. Total 13–20 agent-days (RESEARCH.md:267) prices identity redesign, six hosts, a 2026 client that Effect does not publish (RESEARCH.md:162-164; public ClientRpcs still has Initialize at effect:packages/effect/src/unstable/ai/McpSchema.ts:2982-3034), SSE harness rewrite, and an operator CLI contract as if they were mechanical. Six weeks later the train is mid-rebuild because P1 shipped a placeholder identity and P2 shipped a pin the grill had not actually chosen.
- **Proposal:** Replace the dependency column with named decisions (G4 posture, HTTP run key, 2026 client home, grok/CLI contract, conformance waiver). Delete 'grill items N'. Delete the two-goal split until those decisions exist. If split G9 stays as an option, state that the kit then permanently carries both identity machines. Re-size after the grill, not before.
- **Grill question:** What do 'grill items 1, 2, 4, 8, 9' refer to, and how can P1 start while G4 and the run key are still open questions in ops/manifest.json?

### PM-5 — major — G4 servers fail the only external 2026 referee and dual-era clients that probe -32022

- **Evidence:** RESEARCH.md:51-53: official conformance --requirements 2026-07-28 expects initialize HTTP 404/-32601; Effect answers HTTP 400/-32020 and stdio -32022. That HTTP 400 path is headerMismatch HEADER_MISMATCH_ERROR_CODE -32020 (effect:packages/effect/src/unstable/ai/McpSchema.ts:595; mcpRuntime.ts:61-65, :227-230). Stdio initialize ProtocolError is -32022 with no data.supported (mcpRuntime.ts:319-326). The other -32022 path, already-modern wrong version, does attach supported (mcpRuntime.ts:341-346). Python negotiate_auto treats -32022 without a parseable modern-only supported list as fall-through to initialize (RESEARCH.md:48-50; 14-s2-ecosystem.md:183-209). impact-matrix.md:57 tags 'initialize error carries the supported list (spec SHOULD)' as optional. Mixed lists fail the same conformance check because initialize succeeds (14-s2-ecosystem.md:196-201). Nothing in RESEARCH.md:258-265 owns a waiver or an Effect-side 404/-32601 change. Six weeks later 'stateless adoption' is neither conformant nor reachable from the official dual-era probe.
- **Proposal:** Promote initialize rejection shape and data.supported to a grill item with two concrete outcomes: waive sep-2575-http-server-method-not-found-404-initialize and document why, or change Effect/host mapping so G4 HTTP initialize is 404/-32601 and stdio -32022 carries supported: ['2026-07-28']. Put that in P0 or P1, not optional.
- **Grill question:** Will a G4 host be required to pass modelcontextprotocol/conformance --requirements 2026-07-28, and if Effect's 400/-32020 and -32022-without-supported cannot, who owns the waiver or the error-shape change?

### PM-6 — major — P0 pins 'latest main' against a frozen census; patch and native sql-pg proofs are still UNVERIFIED

- **Evidence:** Research is frozen at Effect a7a71921de and repo ca7362278c (RESEARCH.md:15-17). G7 says pin the newest upstream main at implementation start (DECISIONS.md:91-97). RESEARCH.md:260 P0 is '16 catalog entries to latest upstream main' plus 're-key and verify the platform-node-shared patch' plus 'native sql-pg audit (or a native-wire proof test).' RESEARCH.md:210-212: patch application to the pkg.pr.new tarball is UNVERIFIED; a failed patch silently restores the write-offset bug. RESEARCH.md:201-205, :224-225: timestamp/timestamptz now decode to Date and unregistered OIDs to UTF-8 text; whether any production path uses native @effect/sql-pg against those columns is UNVERIFIED; PGlite tests will not see it. Spike typecheck-green after two edits (explorations/effect-mcp-2026-07-28/research/25-r4-spike-census.md:35-43) does not cover a newer SHA. Six weeks later the shipped pin is not the researched snapshot, the write patch may be a no-op, and postgres timestamp/enum values are Date/text with no in-repo proof.
- **Proposal:** P0 must name the SHA it pins. If it is not a7a71921de, re-run the 23-r4/25-r4 census on that SHA before merge. Make patch apply-and-prove a required P0 check (fail closed, not silent restore). Make the native sql-pg timestamp/enum proof a required P0 test or an explicit 'no native PgClient in production' statement with the call-site census attached.
- **Grill question:** If implementation pins a SHA newer than a7a71921de, is the existing blast-radius census void, and is a silent patch miss or native timestamp decode an accepted ship risk?

### PM-7 — minor — Gate B required dissent on 12 split claims; the synthesis cites none

- **Evidence:** explorations/effect-mcp-2026-07-28/research/verification/README.md:32: 'Split 1–2 claims survive but carry one refuting vote; synthesis cites them with the dissent.' Tally is 12 splits (README.md:30). RESEARCH.md never mentions split, dissent, or 1–2. A reader of §5–§6 treats every surviving claim as unanimous. That is how an overstated mixed-list or identity clause survives into the PR train.
- **Proposal:** Add a short dissent appendix, or footnote each split claim in the section that uses it. Do not claim Gate B completeness until that contract is met.

### PM-8 — minor — HTTP cancellation and discover-advertised subscribe are logged then dropped from the train

- **Evidence:** HTTP notifications/cancelled is 202-acked and does not interrupt another POST's in-flight 2026 call because session/binding is undefined on the stateless path (effect:packages/effect/src/unstable/ai/McpServer.ts:1071-1088; RESEARCH.md:91-93). impact-matrix.md:56 tags that optional. 2026 discover still starts capabilities at { completions: {}, logging: {} } and, when resources are present, sets subscribe: supportsSubscriptions (effect:packages/effect/src/unstable/ai/internal/mcpProtocol/v2026_07_28.ts:321-326) while resources/subscribe is absent from 2026 ClientRequestRpcs (internal/mcpSchema/v2026_07_28.ts:614-625). addResource cannot return InputRequired (McpServer.ts:230-237) even though the 2026 schema allows it. Hosts currently register no resources (RESEARCH.md:146-147), so subscribe is latent. Sidecar HTTP mutations after a G4 cut inherit uncancelable POSTs and, if resources are added under G5, a discover lie.
- **Proposal:** Record HTTP cancellation as an accepted Effect gap on the sidecar host page, not as optional noise. If G5 later wires resources, stop advertising resources.subscribe unless subscriptions/listen is the documented meaning. Do not treat either as in-scope P4 work unless the grill says so.

## Disposition

| # | Disposition |
| --- | --- |
| PM-1 | Fixed. §2.2 and §5 now state that a mixed list mints a 2025 session even for a client offering 2026-07-28; §5 item 5 makes the proving-host acceptance a client that never calls `initialize`. |
| PM-2 | Grill (D-cli-contract). Conflicts with D-M7; both positions recorded in §5. `research/sizing.md` S2 carries the live capture as a conditional entrance criterion. |
| PM-3 | Fixed. Identity redesign removed from the kit stage; `clientId` struck as a run key; D-run-key named with the anchor list and the test to delete; sidecar identity lands with the sidecar pin. |
| PM-4 | Fixed. Numbered grill items replaced by named decisions; the train moved to `research/sizing.md` with per-fork sizes; the goal split is a hypothesis for `MAP.md`. |
| PM-5 | Fixed. D-conformance named; rejection shape and `supported` list are a forced decision in the matrix. |
| PM-6 | Fixed. D-pin-sha named; S0 requires patch apply asserted, native sql-pg proof or census, docgen run. |
| PM-7 | Fixed. Dissent appendix added to RESEARCH.md for all 12 split claims. |
| PM-8 | Fixed. HTTP cancellation recorded as an accepted gap; `resources.subscribe` advertisement noted in §2.2. |
