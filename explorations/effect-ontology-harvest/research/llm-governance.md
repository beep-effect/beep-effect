# LLM Governance & Control Inventory

The mined package is on Effect 3 and `@effect/ai`, while beep-effect7 pins
Effect 4; every candidate below is therefore an adaptation target rather than a
copy-forward API (`packages/@core-v2/package.json:36-65`; `package.json:155-169`).
Routing and novelty judgments follow the shared brief's specific-home-first and
honesty rules (`explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:17-23`,
`explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:25-41`).

### Transparent LanguageModel governance decorator
- **Source:** `packages/@core-v2/src/Runtime/RateLimitedLanguageModel.ts:33-188` (effect-ontology)
- **What:** A scoped layer transparently decorates `generateObject`, `generateText`, and `streamText` with provider-selected per-second/per-minute admission, circuit protection, logs, spans, and call identifiers (`packages/@core-v2/src/Runtime/RateLimitedLanguageModel.ts:67-107`, `packages/@core-v2/src/Runtime/RateLimitedLanguageModel.ts:109-188`). Its strongest idea is one provider-agnostic policy boundary; its streaming branch protects only creation of the stream object, so later pull failures never reach the breaker (`packages/@core-v2/src/Runtime/RateLimitedLanguageModel.ts:124-149`, `packages/@core-v2/src/Runtime/RateLimitedLanguageModel.ts:179-188`).
- **Category:** capability (`packages/@core-v2/src/Runtime/RateLimitedLanguageModel.ts:45-58`).
- **Proposed beep home:** NET-NEW `foundation/capability/llm-governance` — this is reusable behavior above multiple provider drivers, not another external-engine boundary (`explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:31-40`).
- **beep overlap & design delta:** beep already has v4 `LanguageModel` adapters and typed HTTP-status-to-`AiError` normalization for xAI and Venice (`packages/drivers/xai/src/XAiLanguageModel.service.ts:135-163`, `packages/drivers/xai/src/XAiLanguageModel.service.ts:241-255`, `packages/drivers/venice-ai/src/VeniceAiLanguageModel.service.ts:143-170`, `packages/drivers/venice-ai/src/VeniceAiLanguageModel.service.ts:236-250`), plus an Anthropic retry plan (`packages/drivers/anthropic/src/Anthropic.service.ts:103-131`). The mined layer adds cross-provider policy composition, but hard-codes three provider limits, falls unknown providers back to Anthropic, and keeps its call counter in module-global state (`packages/@core-v2/src/Runtime/RateLimitedLanguageModel.ts:33-43`, `packages/@core-v2/src/Runtime/RateLimitedLanguageModel.ts:62-75`).
- **Goal linkage:** `agentic-professional-runtime` needs a governed multi-model workspace and append-only provider/model/token/latency/cost records (`goals/agentic-professional-runtime/README.md:65-71`, `goals/agentic-professional-runtime/docs/data-model-shared-core.md:132-147`); `ontology-agent-surface` requires a budgeted, attributed agent boundary (`goals/ontology-agent-surface/README.md:9-14`).
- **v3→v4 notes:** Replace `@effect/ai` and `LanguageModel.LanguageModel.of` with `effect/unstable/ai` and a v4 `Context.Service`/`LanguageModel.make` design; v4 derives structured generation from `generateText` plus its codec transformer, so the decorator must preserve that path and govern the full Stream lifetime (`packages/@core-v2/src/Runtime/RateLimitedLanguageModel.ts:18-23`, `packages/@core-v2/src/Runtime/RateLimitedLanguageModel.ts:179-188`, `effect-smol@f643dbb/packages/effect/src/unstable/ai/LanguageModel.ts:71-105`, `effect-smol@f643dbb/packages/effect/src/unstable/ai/LanguageModel.ts:730-768`). Keep unstable imports local and use explicit Layer composition (`explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:117-128`).
- **Effort:** L — the service shape, error mapping, Layer topology, and streaming lifetime all change (`packages/@core-v2/src/Runtime/RateLimitedLanguageModel.ts:67-188`).
- **Verdict hint:** adapt-improve — preserve the single transparent governance seam, but replace hard-coded/global state and protect stream consumption (`packages/@core-v2/src/Runtime/RateLimitedLanguageModel.ts:33-75`, `packages/@core-v2/src/Runtime/RateLimitedLanguageModel.ts:179-188`).

### Multi-dimensional LLM admission controller
- **Source:** `packages/@core-v2/src/Service/LlmControl/RateLimiter.ts:24-323`; `packages/@core-v2/src/Domain/Error/Circuit.ts:20-50` (effect-ontology)
- **What:** One service combines request-per-minute and token-per-minute ceilings, a concurrency semaphore, three-state circuit metrics, failure/recovery thresholds, and typed retry metadata (`packages/@core-v2/src/Service/LlmControl/RateLimiter.ts:24-71`, `packages/@core-v2/src/Service/LlmControl/RateLimiter.ts:105-150`). The implementation is a fixed reset window despite its “sliding window” documentation, and admission checks, semaphore acquisition, and quota reservation are separate operations rather than one atomic transition (`packages/@core-v2/src/Service/LlmControl/RateLimiter.ts:78-84`, `packages/@core-v2/src/Service/LlmControl/RateLimiter.ts:175-182`, `packages/@core-v2/src/Service/LlmControl/RateLimiter.ts:185-248`).
- **Category:** service (`packages/@core-v2/src/Service/LlmControl/RateLimiter.ts:75-152`).
- **Proposed beep home:** NET-NEW `foundation/capability/llm-governance` — request, token, and concurrency admission is reusable across the existing provider drivers, while provider quota facts remain driver configuration (`explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:31-40`, `explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:65-73`).
- **beep overlap & design delta:** `@beep/api-transport` already composes v4 native HTTP rate limiting, jittered transient retry, and observable quota-header snapshots (`packages/foundation/capability/api-transport/src/Transport.ts:231-291`), and ontology mutations use bracketed `Semaphore.withPermit` (`packages/ontology/use-cases/src/tools/OntologyToolService.ts:335-355`, `packages/ontology/use-cases/src/tools/OntologyToolService.ts:440-456`). The mined controller is broader at the LLM/token level, but its split `acquire`/`release` API can leak a permit on interruption and never applies `actualTokens` to its accounting (`packages/@core-v2/src/Service/LlmControl/RateLimiter.ts:118-131`, `packages/@core-v2/src/Service/LlmControl/RateLimiter.ts:239-254`).
- **Goal linkage:** `agentic-professional-runtime` directly requires governed model use and usage/cost attribution (`goals/agentic-professional-runtime/README.md:65-71`, `goals/agentic-professional-runtime/docs/data-model-shared-core.md:132-147`); `ontology-agent-surface` requires server-owned, non-caller-overridable budgets (`goals/ontology-agent-surface/PLAN.md:9-13`).
- **v3→v4 notes:** Replace `Context.Tag` and `Effect.makeSemaphore` with `Context.Service`, `Semaphore.make`, and a bracketed operation API; v4 semaphores automatically release permits when the wrapped effect exits (`packages/@core-v2/src/Service/LlmControl/RateLimiter.ts:14-15`, `packages/@core-v2/src/Service/LlmControl/RateLimiter.ts:105-173`, `effect-smol@f643dbb/packages/effect/src/Semaphore.ts:20-80`). Remodel config/state/errors with `S.Class`, `LiteralKit`, per-field annotations, `Effect.fn`, and match-based transitions (`explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:117-128`).
- **Effort:** L — safe reservation, actual-usage reconciliation, interruption, and circuit transitions must become one coherent contract (`packages/@core-v2/src/Service/LlmControl/RateLimiter.ts:185-303`).
- **Verdict hint:** adapt-improve — keep the multi-dimensional policy, but use atomic reservation and bracketed release instead of porting the manual lifecycle (`packages/@core-v2/src/Service/LlmControl/RateLimiter.ts:211-254`).

### Request-scoped stage token ledger
- **Source:** `packages/@core-v2/src/Service/LlmControl/TokenBudget.ts:27-263`; `packages/@core-v2/src/Cluster/ExtractionEntityHandler.ts:242-380` (effect-ontology)
- **What:** A `Ref` ledger divides one request budget among entity extraction, relation extraction, grounding, property scoping, and fallback work, then exposes affordability, usage, remaining-budget, snapshot, and reset operations (`packages/@core-v2/src/Service/LlmControl/TokenBudget.ts:27-55`, `packages/@core-v2/src/Service/LlmControl/TokenBudget.ts:91-145`, `packages/@core-v2/src/Service/LlmControl/TokenBudget.ts:155-204`). It is advisory: `recordUsage` increments unconditionally, and its concrete caller logs an exceeded-budget warning but still performs the LLM call and records an estimate (`packages/@core-v2/src/Service/LlmControl/TokenBudget.ts:171-188`, `packages/@core-v2/src/Cluster/ExtractionEntityHandler.ts:336-380`).
- **Category:** capability (`packages/@core-v2/src/Service/LlmControl/TokenBudget.ts:61-146`).
- **Proposed beep home:** NET-NEW `foundation/capability/llm-governance` — the request ledger is provider-neutral; extraction-stage allocation policy should be supplied by the existing `packages/foundation/capability/langextract` rather than baked into the generic engine (`explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:31-40`, `packages/foundation/capability/langextract/src/Service/index.ts:1-30`).
- **beep overlap & design delta:** beep has schema-backed per-call Anthropic output limits (`packages/drivers/anthropic/src/Anthropic.config.ts:64-83`, `packages/drivers/anthropic/src/Anthropic.config.ts:211-225`), provider usage models (`packages/drivers/openai-compat/src/OpenAiCompat.models.ts:933-972`), append-only provider/model/token/latency/cost attribution (`packages/epistemic/domain/src/entities/UsageRecord/UsageRecord.model.ts:31-129`), and typed static ontology-tool ceilings/refusals (`packages/ontology/use-cases/src/tools/OntologyToolkit.ts:63-98`, `packages/ontology/use-cases/src/tools/OntologyToolkit.ts:148-169`). Those cover call configuration, observed/persisted usage, and tool-result bounds, not a cumulative LLM-stage ledger; the mined design supplies that missing shape but has weaker `stage: string` typing and non-enforcing accounting (`packages/@core-v2/src/Service/LlmControl/TokenBudget.ts:101-145`, `packages/@core-v2/src/Service/LlmControl/TokenBudget.ts:180-203`).
- **Goal linkage:** `ontology-agent-surface` already treats budgets and recoverable refusals as part of the tool contract (`goals/ontology-agent-surface/README.md:38-48`); `agentic-professional-runtime` requires token and cost attribution (`goals/agentic-professional-runtime/docs/data-model-shared-core.md:132-147`).
- **v3→v4 notes:** Replace exported interfaces and the ad-hoc stage union with annotated `S.Class` models and `LiteralKit`, replace `Context.Tag` with `Context.Service`, and expose `Effect.fn` operations (`packages/@core-v2/src/Service/LlmControl/TokenBudget.ts:27-44`, `packages/@core-v2/src/Service/LlmControl/TokenBudget.ts:91-146`, `explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:117-128`). Inference: the live ledger should be request-scoped because the source provides one resettable service `Ref`, while the captured handler resets it for every request (`packages/@core-v2/src/Service/LlmControl/TokenBudget.ts:163-210`, `packages/@core-v2/src/Cluster/ExtractionEntityHandler.ts:171-184`, `packages/@core-v2/src/Cluster/ExtractionEntityHandler.ts:242-243`).
- **Effort:** M — the policy is small, but enforcement, scope, typed stages, and actual-usage reconciliation change the contract (`packages/@core-v2/src/Service/LlmControl/TokenBudget.ts:155-210`).
- **Verdict hint:** adapt-improve — retain stage allocation and observability, but fail/refuse or degrade explicitly instead of warning and proceeding (`packages/@core-v2/src/Cluster/ExtractionEntityHandler.ts:356-374`).

### Three-state circuit-breaker decorator
- **Source:** `packages/@core-v2/src/Runtime/CircuitBreaker.ts:22-226`; `packages/@core-v2/test/Runtime/CircuitBreaker.test.ts:5-69` (effect-ontology)
- **What:** `makeCircuitBreaker` protects any `Effect<A, E, R>`, preserves `E`, adds a typed `CircuitOpenError`, counts consecutive failures/successes, logs open/half-open/closed transitions, and exposes state/reset (`packages/@core-v2/src/Runtime/CircuitBreaker.ts:67-141`, `packages/@core-v2/src/Runtime/CircuitBreaker.ts:143-217`). Tests establish closed pass-through, threshold opening, typed fail-fast rejection, and reset of consecutive failures after success (`packages/@core-v2/test/Runtime/CircuitBreaker.test.ts:5-69`). The half-open state admits every caller rather than an exclusive or bounded recovery probe (`packages/@core-v2/src/Runtime/CircuitBreaker.ts:143-167`).
- **Category:** capability (`packages/@core-v2/src/Runtime/CircuitBreaker.ts:61-67`, `packages/@core-v2/src/Runtime/CircuitBreaker.ts:169-217`).
- **Proposed beep home:** NET-NEW `foundation/capability/llm-governance` — the decorator is domain-agnostic behavior and directly composes around the existing LLM drivers (`explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:31-40`, `explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:65-73`).
- **beep overlap & design delta:** NOT FOUND — the closest reusable substrate, `@beep/api-transport`, composes rate limiting and transient retry but no closed/open/half-open failure gate (`packages/foundation/capability/api-transport/src/Transport.ts:231-291`). The mined decorator is a useful starting policy, but its unbounded half-open admission must not be copied (`packages/@core-v2/src/Runtime/CircuitBreaker.ts:151-167`).
- **Goal linkage:** `agentic-professional-runtime` needs provider failures to remain governed and auditable rather than cascade through a local professional workspace (`goals/agentic-professional-runtime/README.md:65-82`, `goals/agentic-professional-runtime/docs/data-model-shared-core.md:125-147`); `ontology-agent-surface` requires a gated and recoverably failing sidecar surface (`goals/ontology-agent-surface/README.md:9-14`).
- **v3→v4 notes:** Replace v3 `Effect.tapBoth` with exit-aware v4 handling so success, typed failure, and interruption are deliberate, and migrate `CircuitOpenError` to the repo's schema-backed tagged-error boundary (`packages/@core-v2/src/Runtime/CircuitBreaker.ts:176-200`, `packages/@core-v2/src/Domain/Error/Circuit.ts:20-32`, `effect-smol@f643dbb/packages/effect/src/Effect.ts:6914-6949`, `explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:117-128`). Use namespace-first `Duration`/`Option` helpers and match-based state transitions (`explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:117-128`).
- **Effort:** M — the core state machine is compact, but concurrent half-open probes and exit accounting require redesign (`packages/@core-v2/src/Runtime/CircuitBreaker.ts:80-167`).
- **Verdict hint:** adapt-improve — preserve the generic typed decorator and add exclusive/bounded recovery probes (`packages/@core-v2/src/Runtime/CircuitBreaker.ts:143-167`).

### Dual soft/hard stage deadlines
- **Source:** `packages/@core-v2/src/Service/LlmControl/StageTimeout.ts:29-234`; `packages/@core-v2/test/Service/Extraction.timeout.test.ts:19-188` (effect-ontology)
- **What:** Per-stage policy emits a soft-warning effect and interrupts with a typed hard-timeout error, with unknown-stage defaults, a prediction helper, and an overridable test layer (`packages/@core-v2/src/Service/LlmControl/StageTimeout.ts:29-78`, `packages/@core-v2/src/Service/LlmControl/StageTimeout.ts:104-140`, `packages/@core-v2/src/Service/LlmControl/StageTimeout.ts:147-234`). Tests verify quick completion, hard failure metadata, soft callback behavior, cancellation of an unnecessary warning, defaults, and prediction (`packages/@core-v2/test/Service/Extraction.timeout.test.ts:21-118`, `packages/@core-v2/test/Service/Extraction.timeout.test.ts:121-187`).
- **Category:** service (`packages/@core-v2/src/Service/LlmControl/StageTimeout.ts:80-141`).
- **Proposed beep home:** `packages/foundation/capability/langextract` — the named extraction-stage policy improves an existing provider-neutral extraction capability; its live Layer remains outside slice `use-cases` (`packages/foundation/capability/langextract/src/Service/index.ts:1-30`, `explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:31-35`).
- **beep overlap & design delta:** `@beep/langextract` already enforces a 30-second model-generation timeout with a typed extraction error (`packages/foundation/capability/langextract/src/Service/index.ts:24-30`, `packages/foundation/capability/langextract/src/Service/index.ts:275-300`), and the filing classifier independently has the same hard timeout plus a safe inbox fallback (`packages/documents/server/src/aggregates/Document/FilingDecisionLlm.ts:113-139`). The mined service adds soft-warning and named-stage hard-deadline policy, but uses string stage lookup and raw millisecond records rather than schema-backed stage/duration values (`packages/@core-v2/src/Service/LlmControl/StageTimeout.ts:29-62`, `packages/@core-v2/src/Service/LlmControl/StageTimeout.ts:115-139`).
- **Goal linkage:** `ontology-agent-surface` requires bounded, recoverably failing tool behavior (`goals/ontology-agent-surface/PLAN.md:9-14`); `agentic-professional-runtime` records latency alongside model/tool usage (`goals/agentic-professional-runtime/docs/data-model-shared-core.md:132-147`).
- **v3→v4 notes:** Replace `Context.Tag`, `Data.TaggedError`, and removed `Effect.timeoutFail` with `Context.Service`, the repo's tagged schema error, `Effect.fn`, and v4 `Effect.timeoutOrElse`; the v4 timeout branch interrupts the source before running the fallback (`packages/@core-v2/src/Service/LlmControl/StageTimeout.ts:20-21`, `packages/@core-v2/src/Service/LlmControl/StageTimeout.ts:71-78`, `packages/@core-v2/src/Service/LlmControl/StageTimeout.ts:104-170`, `effect-smol@f643dbb/packages/effect/src/Effect.ts:4563-4626`). Model stages with `LiteralKit` and config with `S.Class` (`explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:117-128`).
- **Effort:** M — timeout mechanics are direct, but the policy/error/service boundary and Layer placement need redesign (`packages/@core-v2/src/Service/LlmControl/StageTimeout.ts:104-191`).
- **Verdict hint:** adapt-improve — retain the two-threshold contract and test matrix, with typed stage policy and one owner per timed operation (`packages/@core-v2/test/Service/Extraction.timeout.test.ts:19-188`).

### Bracketed LLM concurrency guard
- **Source:** `packages/@core-v2/src/Runtime/LlmSemaphore.ts:20-88`; `packages/@core-v2/test/Runtime/LlmSemaphore.test.ts:12-81` (effect-ontology)
- **What:** A config-driven service brackets LLM effects with one semaphore permit and exposes a typed timeout plus configured-limit introspection (`packages/@core-v2/src/Runtime/LlmSemaphore.ts:37-83`). Its documented “permit acquisition” timeout actually wraps the admitted operation too, and `availablePermits` always returns the configured maximum rather than live availability (`packages/@core-v2/src/Runtime/LlmSemaphore.ts:48-77`); the corresponding test only asserts that value is positive (`packages/@core-v2/test/Runtime/LlmSemaphore.test.ts:47-53`).
- **Category:** pattern (`packages/@core-v2/src/Runtime/LlmSemaphore.ts:25-37`).
- **Proposed beep home:** NET-NEW `foundation/capability/llm-governance` — absorb the bracketed concurrency primitive into the admission/decorator contract rather than create a second standalone service (`explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:31-40`).
- **beep overlap & design delta:** beep already demonstrates the v4 primitive correctly by creating a mutation semaphore and wrapping each write with `withPermit` (`packages/ontology/use-cases/src/tools/OntologyToolService.ts:335-355`, `packages/ontology/use-cases/src/tools/OntologyToolService.ts:440-456`, `packages/ontology/use-cases/src/tools/OntologyToolService.ts:501-503`). The only useful mined delta is LLM-specific configuration and queue-timeout policy; its availability metric and timeout placement are misleading (`packages/@core-v2/src/Runtime/LlmSemaphore.ts:48-77`).
- **Goal linkage:** `agentic-professional-runtime` benefits from bounded provider concurrency as part of its governed model runtime (`goals/agentic-professional-runtime/README.md:65-82`); `ontology-agent-surface` already treats serialized mutations as a safety property (`packages/ontology/use-cases/src/tools/OntologyToolService.ts:440-456`).
- **v3→v4 notes:** Replace `Effect.Service` and `Effect.makeSemaphore` with `Context.Service`, explicit Layers, and `Semaphore.make`; v4 `withPermits` guarantees release when the effect exits (`packages/@core-v2/src/Runtime/LlmSemaphore.ts:37-42`, `effect-smol@f643dbb/packages/effect/src/Semaphore.ts:20-80`, `explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:117-128`). Separate queue acquisition timing from operation deadlines instead of wrapping both in one timeout (`packages/@core-v2/src/Runtime/LlmSemaphore.ts:48-71`).
- **Effort:** S — the v4 primitive already exists; only policy and integration are worth retaining (`effect-smol@f643dbb/packages/effect/src/Semaphore.ts:20-80`).
- **Verdict hint:** skip — do not port this as a standalone service; fold its sound bracketed-concurrency idea into the central governance decorator (`packages/@core-v2/src/Runtime/LlmSemaphore.ts:37-83`).

### Typed provider-retry policy
- **Source:** `packages/@core-v2/src/Service/Retry.ts:18-173`; `packages/@core-v2/test/Service/Retry.test.ts:5-32` (effect-ontology)
- **What:** A shared factory combines exponential backoff, a maximum delay, jitter, an attempt bound, retryability filtering, and per-attempt logging (`packages/@core-v2/src/Service/Retry.ts:122-173`). Its classifier defaults unknown and non-`Error` failures to retryable and otherwise inspects status/code/message strings; it also checks the tag `CircuitBreakerOpenError`, while the actual source error is tagged `CircuitOpenError` (`packages/@core-v2/src/Service/Retry.ts:63-120`, `packages/@core-v2/src/Domain/Error/Circuit.ts:20-25`). The network tests use bare error messages without `.code`, so they pass via the permissive default rather than the intended network-code branch (`packages/@core-v2/test/Service/Retry.test.ts:16-31`, `packages/@core-v2/src/Service/Retry.ts:94-119`).
- **Category:** pattern (`packages/@core-v2/src/Service/Retry.ts:122-149`).
- **Proposed beep home:** NET-NEW `foundation/capability/llm-governance` — share execution-plan orchestration above drivers, while each driver remains responsible for normalizing provider failures into typed `AiError` reasons (`explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:31-40`, `packages/drivers/openai-compat/src/OpenAiCompatClient.service.ts:118-159`).
- **beep overlap & design delta:** basic retry is already covered: Anthropic's v4 `ExecutionPlan` retries only typed `AiError`s whose reason is retryable (`packages/drivers/anthropic/src/Anthropic.service.ts:103-131`), and `@beep/api-transport` uses jittered transient HTTP retry (`packages/foundation/capability/api-transport/src/Transport.ts:261-289`). The mined schedule adds cap/logging ideas, but its reflective classifier is weaker than v4 `AiError.isRetryable` and `retryAfter` (`effect-smol@f643dbb/packages/effect/src/unstable/ai/AiError.ts:1426-1488`).
- **Goal linkage:** `agentic-professional-runtime` needs predictable, attributable provider behavior (`goals/agentic-professional-runtime/README.md:65-82`, `goals/agentic-professional-runtime/docs/data-model-shared-core.md:132-147`); `ontology-agent-surface` requires recoverable failures rather than silent retries without policy (`goals/ontology-agent-surface/README.md:38-48`).
- **v3→v4 notes:** Re-express v3 `Schedule.intersect`/`delayed`/`whileInput`/`tapOutput` with v4 `Schedule.upTo`, `modifyDelay`, `while`, and metadata-aware `tap`, or prefer a v4 `ExecutionPlan` at the provider-acquisition boundary (`packages/@core-v2/src/Service/Retry.ts:149-173`, `effect-smol@f643dbb/packages/effect/src/Schedule.ts:1562-1626`, `effect-smol@f643dbb/packages/effect/src/Schedule.ts:1828-1995`, `effect-smol@f643dbb/packages/effect/src/Schedule.ts:2030-2050`, `packages/drivers/anthropic/src/Anthropic.service.ts:125-131`). Use typed reason matching instead of native `Error`/message inspection (`explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:117-128`).
- **Effort:** M — scheduling is straightforward, but retry taxonomy and provider-normalization ownership are the real work (`packages/@core-v2/src/Service/Retry.ts:44-120`).
- **Verdict hint:** adapt-improve — reuse cap, jitter, attempt metadata, and logging while keeping beep's typed v4 retryability semantics (`packages/@core-v2/src/Service/Retry.ts:149-173`, `packages/drivers/anthropic/src/Anthropic.service.ts:125-131`).

### Observable structured-generation execution envelope
- **Source:** `packages/@core-v2/src/Service/LlmWithRetry.ts:29-167` (effect-ontology)
- **What:** One wrapper normalizes prompts, fingerprints the output schema, applies a per-attempt timeout and bounded retry, counts retries, logs token usage, annotates success, and emits an enclosing span (`packages/@core-v2/src/Service/LlmWithRetry.ts:67-115`, `packages/@core-v2/src/Service/LlmWithRetry.ts:116-166`). Because `tapErrorCause` follows `Effect.retry`, its “failed, will retry” log runs only after the retry effect terminates, not before each retry (`packages/@core-v2/src/Service/LlmWithRetry.ts:110-130`).
- **Category:** capability (`packages/@core-v2/src/Service/LlmWithRetry.ts:62-70`).
- **Proposed beep home:** NET-NEW `foundation/capability/llm-governance` — it is a cross-provider execution envelope, with telemetry emitted through existing `@beep/observability` rather than source-local attribute globals (`explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:31-40`).
- **beep overlap & design delta:** beep already has a schema-validated filing call with a hard timeout and safe product fallback (`packages/documents/server/src/aggregates/Document/FilingDecisionLlm.ts:100-142`), typed Anthropic acquisition retry (`packages/drivers/anthropic/src/Anthropic.service.ts:103-131`), and v4 provider usage normalization (`packages/drivers/openai-compat/src/OpenAiCompatLanguageModel.service.ts:281-305`). The mined wrapper usefully composes those concerns, but assumes v3 numeric token fields and hashes a v3 `JSONSchema.make` result (`packages/@core-v2/src/Service/LlmWithRetry.ts:102-109`, `packages/@core-v2/src/Service/LlmWithRetry.ts:131-155`).
- **Goal linkage:** `agentic-professional-runtime` explicitly models provider, model, tokens/units, latency, cost, credential reference, and actor attribution (`goals/agentic-professional-runtime/docs/data-model-shared-core.md:132-147`); `ontology-agent-surface` benefits from one bounded execution envelope around future LLM-backed tools (`goals/ontology-agent-surface/README.md:9-14`).
- **v3→v4 notes:** Move `@effect/ai` to `effect/unstable/ai`, use `Effect.fn`, generate schema fingerprints from v4 `S.toJsonSchemaDocument`, and read nested optional `Response.Usage.inputTokens/outputTokens` fields (`packages/@core-v2/src/Service/LlmWithRetry.ts:16-24`, `packages/@core-v2/src/Service/LlmWithRetry.ts:102-109`, `effect-smol@f643dbb/packages/effect/src/Schema.ts:13399-13429`, `effect-smol@f643dbb/packages/effect/src/unstable/ai/Response.ts:2350-2402`). Retry should consume typed `AiError` metadata rather than the source's native heuristics (`effect-smol@f643dbb/packages/effect/src/unstable/ai/AiError.ts:1426-1488`).
- **Effort:** L — structured-generation APIs, retry instrumentation, usage shapes, schema hashing, and observability ownership all cross the v3/v4 boundary (`packages/@core-v2/src/Service/LlmWithRetry.ts:67-166`).
- **Verdict hint:** adapt-improve — keep one observable execution envelope, but separate provider retry, per-attempt events, and product fallback policy (`packages/@core-v2/src/Service/LlmWithRetry.ts:95-166`).

### In-flight idempotency-key single-flight registry
- **Source:** `packages/@core-v2/src/Service/ExecutionDeduplicator.ts:17-141`; `packages/@core-v2/test/Service/ExecutionDeduplicator.test.ts:5-43` (effect-ontology)
- **What:** A `Ref<HashMap>` atomically elects one execution per key with `Ref.modify`; followers receive the same `Deferred`, and explicit complete/fail operations wake all waiters (`packages/@core-v2/src/Service/ExecutionDeduplicator.ts:27-72`, `packages/@core-v2/src/Service/ExecutionDeduplicator.ts:75-119`). It is hard-wired to `KnowledgeGraph` and plain `Error`, retains terminal entries until explicit cleanup, and records `startedAt` without expiry (`packages/@core-v2/src/Service/ExecutionDeduplicator.ts:17-21`, `packages/@core-v2/src/Service/ExecutionDeduplicator.ts:47-54`, `packages/@core-v2/src/Service/ExecutionDeduplicator.ts:121-141`); its “concurrent” test performs sequential lookups and does not exercise the race branch (`packages/@core-v2/test/Service/ExecutionDeduplicator.test.ts:5-29`).
- **Category:** service (`packages/@core-v2/src/Service/ExecutionDeduplicator.ts:23-30`, `packages/@core-v2/src/Service/ExecutionDeduplicator.ts:132-141`).
- **Proposed beep home:** design-reference → `ontology/server` — only a server/runtime boundary should own an in-memory registry; current stateless ontology use-cases must not acquire hidden live state (`explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:33-35`, `goals/ontology-agent-surface/PLAN.md:9-13`).
- **beep overlap & design delta:** NOT FOUND for an explicit LLM/ontology in-flight election service. beep does have bounded per-operation `effect/Cache` memoization and a process-local result store with GC/statistics (`packages/foundation/capability/nlp-processing/src/Backend/Composition.ts:198-230`, `packages/foundation/capability/nlp-processing/src/Backend/Composition.ts:273-310`, `packages/foundation/capability/nlp-processing/src/Graph/GraphOperations/ResultStore.ts:159-207`, `packages/foundation/capability/nlp-processing/src/Graph/GraphOperations/ResultStore.ts:283-310`), while ontology tooling reopens saved state, fingerprints it, and uses explicit semantic CAS (`packages/ontology/use-cases/src/tools/OntologyToolService.ts:85-138`). Single-flight is not a replacement for those result-cache or durable/CAS contracts, and ontology V1 explicitly forbids a cache/session repository (`goals/ontology-agent-surface/SPEC.md:68-80`, `goals/ontology-agent-surface/SPEC.md:113-118`).
- **Goal linkage:** `agentic-professional-runtime` could avoid duplicate model/tool cost while retaining one activity/usage result per elected execution (`goals/agentic-professional-runtime/docs/data-model-shared-core.md:125-147`); current `ontology-agent-surface` constrains, rather than demands, this pattern (`goals/ontology-agent-surface/PLAN.md:9-13`).
- **v3→v4 notes:** Replace `Effect.Service` and generated `.Default` wiring with `Context.Service` plus an explicit Layer, use namespace-first helper imports and `Effect.fn`, parameterize success/failure types, and replace `Error` with a boundary-appropriate tagged schema error (`packages/@core-v2/src/Service/ExecutionDeduplicator.ts:10-21`, `packages/@core-v2/src/Service/ExecutionDeduplicator.ts:102-141`, `explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:117-128`). Add scope/finalizer or TTL cleanup before any live use (`packages/@core-v2/src/Service/ExecutionDeduplicator.ts:121-128`).
- **Effort:** M — generic typing is small, but lifecycle, cancellation, expiry, and authority semantics need proof (`packages/@core-v2/src/Service/ExecutionDeduplicator.ts:27-130`).
- **Verdict hint:** design-reference — retain the atomic election/Deferred pattern, not the domain hard-coding or unbounded registry (`packages/@core-v2/src/Service/ExecutionDeduplicator.ts:27-72`, `packages/@core-v2/src/Service/ExecutionDeduplicator.ts:121-141`).

### Provider-aware prompt-cache breakpoint
- **Source:** `packages/@core-v2/src/Service/PromptCache.ts:14-72` (effect-ontology)
- **What:** The useful idea is to separate stable system instructions from variable user input so a provider can place a cache breakpoint (`packages/@core-v2/src/Service/PromptCache.ts:14-32`, `packages/@core-v2/src/Service/PromptCache.ts:41-55`). The implementation does not enable caching: `_enableCaching` is unused, and the file acknowledges that actual cache control belongs at the Anthropic client boundary (`packages/@core-v2/src/Service/PromptCache.ts:36-47`).
- **Category:** design-idea (`packages/@core-v2/src/Service/PromptCache.ts:1-8`).
- **Proposed beep home:** `drivers/anthropic` — cache-control metadata is an Anthropic provider concern, while prompt structure stays on the v4 AI surface (`explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:32-40`).
- **beep overlap & design delta:** beep's Anthropic layer currently exposes schema-backed model and maximum-output-token options but no cache-control option (`packages/drivers/anthropic/src/Anthropic.config.ts:186-225`, `packages/drivers/anthropic/src/Anthropic.service.ts:47-80`). The installed v4 Anthropic adapter already supports `options.anthropic.cacheControl` on system/user messages and translates it to provider `cache_control` (`effect-smol@f643dbb/packages/ai/anthropic/src/AnthropicLanguageModel.ts:101-144`, `effect-smol@f643dbb/packages/ai/anthropic/src/AnthropicLanguageModel.ts:824-857`, `effect-smol@f643dbb/packages/ai/anthropic/src/AnthropicLanguageModel.ts:2816-2825`), so the target delta is a real driver option/helper rather than the mined no-op boolean.
- **Goal linkage:** `agentic-professional-runtime` records tokens and cost, making observable prompt-cache reads/writes directly relevant (`goals/agentic-professional-runtime/docs/data-model-shared-core.md:132-147`); ontology prompts with stable schemas could be a later `ontology-agent-surface` consumer (`goals/ontology-agent-surface/README.md:9-14`).
- **v3→v4 notes:** Move `Prompt` from `@effect/ai` to `effect/unstable/ai`, preserve separate messages, and attach typed Anthropic provider options at the chosen cache breakpoint (`packages/@core-v2/src/Service/PromptCache.ts:11-12`, `effect-smol@f643dbb/packages/ai/anthropic/src/AnthropicLanguageModel.ts:105-144`). Do not add a generic cache service or preserve the ignored flag (`packages/@core-v2/src/Service/PromptCache.ts:36-47`).
- **Effort:** S — v4 already exposes the provider metadata; the work is a typed driver-facing option plus tests/usage telemetry (`effect-smol@f643dbb/packages/ai/anthropic/src/AnthropicLanguageModel.ts:105-144`, `effect-smol@f643dbb/packages/effect/src/unstable/ai/Response.ts:2363-2402`).
- **Verdict hint:** adapt-improve — keep the stable/variable partition, implement an actual provider breakpoint, and discard the no-op API (`packages/@core-v2/src/Service/PromptCache.ts:36-55`).

### Validation-feedback structured-generation loop
- **Source:** `packages/@core-v2/src/Service/GenerateWithFeedback.ts:25-249`; `packages/@core-v2/test/Service/GenerateWithFeedback.test.ts:4-12` (effect-ontology)
- **What:** A bounded loop retries structured generation and appends validation feedback after `MalformedOutput`, while retaining optional per-attempt timeouts and repeating other failures without feedback (`packages/@core-v2/src/Service/GenerateWithFeedback.ts:84-100`, `packages/@core-v2/src/Service/GenerateWithFeedback.ts:116-210`). A supplied `retrySchedule` is never interpreted and only switches on a hard-coded exponential delay, every non-schema error is retried, and the supposedly generic feedback text embeds ontology-specific constraints (`packages/@core-v2/src/Service/GenerateWithFeedback.ts:102-128`, `packages/@core-v2/src/Service/GenerateWithFeedback.ts:157-199`, `packages/@core-v2/src/Service/GenerateWithFeedback.ts:217-240`). Its test constructs a schedule without invoking the helper (`packages/@core-v2/test/Service/GenerateWithFeedback.test.ts:4-12`).
- **Category:** pattern (`packages/@core-v2/src/Service/GenerateWithFeedback.ts:62-84`).
- **Proposed beep home:** design-reference → `packages/agents/server` — beep already owns a product repair tail there; ontology-specific feedback remains in `ontology/use-cases` rather than a generic helper (`explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:33-40`).
- **beep overlap & design delta:** beep's assistant-turn repair path already batches validation issue reports, performs at most two attempts, validates the repair envelope and every repaired block, ignores unexpected/duplicate results, records metrics, and visibly drops exhausted failures (`packages/agents/server/src/AssistantTurn/BlockRepair.ts:61-136`, `packages/agents/server/src/AssistantTurn/BlockRepair.ts:423-552`, `packages/agents/server/src/AssistantTurn/BlockRepair.ts:554-603`). The mined loop contributes the idea of feeding schema diagnostics into the next prompt, but is less trustworthy because retry policy is approximated, errors are not classified, feedback is domain-hard-coded, and behavior is untested (`packages/@core-v2/src/Service/GenerateWithFeedback.ts:102-128`, `packages/@core-v2/src/Service/GenerateWithFeedback.ts:157-240`, `packages/@core-v2/test/Service/GenerateWithFeedback.test.ts:4-12`).
- **Goal linkage:** `agentic-professional-runtime` requires reviewable candidate output and explicit revision rather than silent authoritative writes (`goals/agentic-professional-runtime/README.md:93-102`, `goals/agentic-professional-runtime/docs/data-model-shared-core.md:151-160`); `ontology-agent-surface` needs typed, visible recoverable failures (`goals/ontology-agent-surface/README.md:38-48`).
- **v3→v4 notes:** Replace `@effect/ai`, `MalformedOutput`, `Prompt.merge`, and `effect/Cause`'s v3 timeout type with v4 `effect/unstable/ai`, reason matching on `InvalidOutputError`/`StructuredOutputError`, `Prompt.concat`, and `Cause.TimeoutError` (`packages/@core-v2/src/Service/GenerateWithFeedback.ts:12-18`, `packages/@core-v2/src/Service/GenerateWithFeedback.ts:157-189`, `effect-smol@f643dbb/packages/effect/src/unstable/ai/AiError.ts:680-824`, `effect-smol@f643dbb/packages/effect/src/unstable/ai/Prompt.ts:2047-2143`, `effect-smol@f643dbb/packages/effect/src/Cause.ts:1407-1473`). Replace the mutable `while` loop and conditional chain with `Effect.fn` recursion and typed match helpers (`explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:117-128`).
- **Effort:** L — the feedback protocol, retry semantics, error taxonomy, prompt history, and test contract all need redesign (`packages/@core-v2/src/Service/GenerateWithFeedback.ts:84-240`).
- **Verdict hint:** design-reference — use its schema-diagnostic retry idea to improve the existing bounded repair path, not as a new generic port (`packages/agents/server/src/AssistantTurn/BlockRepair.ts:423-603`).

### Rule-aware schema-feedback compiler
- **Source:** `packages/@core-v2/src/Prompt/FeedbackGenerator.ts:17-395` (effect-ontology)
- **What:** Pure helpers attempt to turn schema parse issues into path-bearing violations, map them to extraction rules, render concise/tree diagnostics, add matched-rule reminders, build a correction prompt, and classify likely repairability (`packages/@core-v2/src/Prompt/FeedbackGenerator.ts:17-150`, `packages/@core-v2/src/Prompt/FeedbackGenerator.ts:163-213`, `packages/@core-v2/src/Prompt/FeedbackGenerator.ts:227-395`). The walker recurses on the same object whenever `ast.key` is present, so that input shape can recurse indefinitely; matching also chooses the first rule in a category rather than the most specific rule (`packages/@core-v2/src/Prompt/FeedbackGenerator.ts:67-73`, `packages/@core-v2/src/Prompt/FeedbackGenerator.ts:112-149`).
- **Category:** capability (`packages/@core-v2/src/Prompt/FeedbackGenerator.ts:31-41`, `packages/@core-v2/src/Prompt/FeedbackGenerator.ts:168-194`).
- **Proposed beep home:** `packages/foundation/capability/langextract` — rule categories such as type mapping, property usage, reference integrity, and mention format are extraction policy, not generic `@beep/schema` substrate (`packages/@core-v2/src/Prompt/FeedbackGenerator.ts:92-149`, `packages/foundation/capability/langextract/src/Service/index.ts:1-30`, `explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:31-40`).
- **beep overlap & design delta:** beep already carries bounded issue reports into a schema-validated assistant repair tail (`packages/agents/server/src/AssistantTurn/BlockRepair.ts:76-136`, `packages/agents/server/src/AssistantTurn/BlockRepair.ts:229-249`, `packages/agents/server/src/AssistantTurn/BlockRepair.ts:423-528`), while LangExtract currently parses model text only after the bounded generation call (`packages/foundation/capability/langextract/src/Service/index.ts:275-302`). The mined compiler adds rule-aware path/reminder generation, while beep's assistant implementation is stronger on typed envelopes, revalidation, deduplication, and metrics (`packages/agents/server/src/AssistantTurn/BlockRepair.ts:443-528`, `packages/agents/server/src/AssistantTurn/BlockRepair.ts:531-603`); any adaptation must replace the recursive AST introspection (`packages/@core-v2/src/Prompt/FeedbackGenerator.ts:41-90`).
- **Goal linkage:** `ontology-agent-surface` benefits from typed, actionable recovery guidance (`goals/ontology-agent-surface/README.md:38-48`); `agentic-professional-runtime` requires revisions and reviewer-visible candidate flows (`goals/agentic-professional-runtime/docs/data-model-shared-core.md:151-160`).
- **v3→v4 notes:** Replace `effect/ParseResult`/`TreeFormatter` with v4 `SchemaError.issue` and the tagged `SchemaIssue.Issue` tree/formatter APIs; v4 issues already preserve pointer paths for standard-schema formatting (`packages/@core-v2/src/Prompt/FeedbackGenerator.ts:11-15`, `effect-smol@f643dbb/packages/effect/src/SchemaIssue.ts:80-116`, `effect-smol@f643dbb/packages/effect/src/SchemaIssue.ts:962-1007`). Replace the exported interface with `S.Class`, use namespace-first helpers and match combinators, and remove the synchronous-decoder product example under repo law (`packages/@core-v2/src/Prompt/FeedbackGenerator.ts:17-29`, `packages/@core-v2/src/Prompt/FeedbackGenerator.ts:178-190`, `explorations/effect-ontology-harvest/research/MAPPING-CONTEXT.md:117-128`).
- **Effort:** M — the rule/prompt idea is reusable inside the slice, but issue traversal and selection logic must be rebuilt (`packages/@core-v2/src/Prompt/FeedbackGenerator.ts:41-150`, `packages/@core-v2/src/Prompt/FeedbackGenerator.ts:227-395`).
- **Verdict hint:** adapt-improve — retain rule-linked diagnostics and correction prompts, replacing the unsafe walker and first-match heuristics (`packages/@core-v2/src/Prompt/FeedbackGenerator.ts:41-150`, `packages/@core-v2/src/Prompt/FeedbackGenerator.ts:285-335`).

## Sources appended

`eo-llm-01`, `eo-llm-02`, `eo-llm-03`, `eo-llm-04`, `eo-llm-05`,
`eo-llm-06`, `eo-llm-07`, `eo-llm-08`, `eo-llm-09`, `eo-llm-10`,
`eo-llm-11`, `eo-llm-12`, `eo-llm-13`, `eo-llm-14`, `eo-llm-15`,
`eo-llm-16`, `eo-llm-17`, `eo-llm-18`, `eo-llm-19`, `eo-llm-20`.
