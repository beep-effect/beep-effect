# Assets

- `vendor/` — gitignored working copies of fetched third-party material
  (links.md page scrapes, ontology files). Never committed (public repo);
  reproducible from `research/00-catalog.json` link rows (L01–L14) and the
  corpus `links.md`.
- Committed record = this README + catalog rows; per wave-1 precedent
  (`../../legal-ontology-landscape/assets/`).
