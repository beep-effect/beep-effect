# Capture

## 2026-08-17

Give ontology curation a consequence-visible review protocol: accept/decline/exclude/quarantine operations with immutable plans and rollback, plus a hard rule that generated DELETE/INSERT never executes directly. Review-order optimization and consequence previews are useful without granting the machine truth authority. Two high-priority wave-2 findings converge here (review governance; generated-change quarantine).

Evidence: `ae13bba3d165`, `156702192320`, `96acfbb4f208`, `e7e99c921d03`, `14dc43d7249e` — `id`-keyed into the machine-local academia-2026-07 research corpus
(`text/`, `meta/`, `notes/`; DOIs/OA PDFs where recoverable via
`resolved-index.jsonl`).

Routed here from the `academia-corpus-mining` wave-2 synthesis routing table;
the operator ratified spawning this packet 2026-08-17.
