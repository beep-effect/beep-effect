Event sourcing for small control planes and workflow state - practitioner lessons, not theory. Schema evolution and upcasting pain in production event-sourced systems; projection rebuilds; when teams regret event sourcing. Per-event immutable files with parent digests (git-like hash chains, CAS, fork detection) vs single append-only JSONL journals - real implementations and merge-conflict experience in repo-stored state. Temporal / Restate / DBOS determinism constraints and versioning lessons. How agent frameworks (LangGraph, Claude Code, OpenAI agents, etc.) persist run/checkpoint state and what breaks. Repo-as-database patterns: storing workflow state in git, CRDT vs event-log approaches.
