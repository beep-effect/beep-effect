Requirements-to-test traceability that does not rot. Research and tools for automated trace-link generation and continuous trace maintenance (Mader & Gotel lineage and successors; modern LLM-based traceability). EARS requirement syntax adoption reports outside aerospace. Trace decay: evidence that hand-maintained matrices become false confidence. Conventions that let traceability be DERIVED: stable IDs in commit trailers, test titles, file paths; tools that lint orphaned requirements or stale evidence. LLM-based spec-conformance checking of diffs (does this diff implement REQ-X and nothing else). Orphaned-evidence failure modes in docs-as-code repos.
