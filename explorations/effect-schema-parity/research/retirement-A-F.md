# Retirement audit A–F

Baseline: local Effect main `51d4a2f08a5c7691dc876415bc9fc0ecf467e153`; pin `effect@4.0.0-rc.115` (`package.json:161`). Read-only inspection, no network, no source changes. Upstream citations below are relative to `.repos/effect/`; Beep citations are relative to the repository root. Dispositions apply to whole top-level concepts, including `FileDiff.schema.ts` as its own concept.

## Counts and method

| Scope | RETIRE | ADAPT | KEEP | UNSURE |
|---|---:|---:|---:|---:|
| 61 concepts | 16 | 1 | 44 | 0 |

Count commands (run from repository root):

```sh
rg --files packages/foundation/modeling/schema/src | rg '/src/[A-F][^/]*(/|\.ts$)' | sed -E 's@.*/src/([^/]+).*@\1@;s/\.ts$//' | sort -u | wc -l
for disposition in RETIRE ADAPT KEEP UNSURE; do
  count=$(rg -c "[|] [0-9]+ [|] $disposition [|]" explorations/effect-schema-parity/research/retirement-A-F.md || true)
  printf '%s %s\n' "$disposition" "${count:-0}"
done
```

Import topology: `packages/foundation/modeling/schema/README.md:9`; root flat exports at `packages/foundation/modeling/schema/src/index.ts:13`, namespace exports such as Conformance at `packages/foundation/modeling/schema/src/index.ts:60`, explicit aliases for Duration at `packages/foundation/modeling/schema/src/index.ts:110`. Root names are resolved through the actual barrels, not guessed from concept names. For example Cuid has a subpath but no root re-export; ContinentName is exported through ContinentCode.

Consumer census commands:

```sh
rg -l '@beep/schema' packages apps --glob '*.{ts,tsx,js,jsx,mjs,cjs,mts,cts}' --glob '!**/node_modules/**' --glob '!**/dist/**' --glob '!**/.repos/**' --glob '!packages/foundation/modeling/schema/**' > explorations/effect-schema-parity/research/retirement-A-F-import-candidates.txt
node explorations/effect-schema-parity/research/retirement-A-F-count.cjs
node explorations/effect-schema-parity/research/retirement-A-F-build.cjs
# Each concept/file pair appears once; rg verifies the per-concept counts:
for concept in $(rg --files packages/foundation/modeling/schema/src | rg '/src/[A-F][^/]*(/|\.ts$)' | sed -E 's@.*/src/([^/]+).*@\1@;s/\.ts$//' | sort -u); do
  count=$(rg -c "^${concept}[[:space:]]" explorations/effect-schema-parity/research/retirement-A-F-consumer-ledger.tsv || true)
  printf '%s %s\n' "$concept" "${count:-0}"
done
```

`retirement-A-F-consumers.json` contains every counted file and the root-name mapping. Counts mean direct code-file import/re-export dependencies: named imports (including aliases/type imports), concept subpaths, and root namespace member references. AST parsing removes JSDoc/example false positives and deduplicates files per concept. A namespace import is attributed to the concepts referenced in that file, not every root export. Transitive imports through other packages, dynamic module-name construction, namespace escape/destructuring, non-code documentation, and schema-package internal dependencies are outside this metric; exhaustive semantic reachability is **UNVERIFIED**. Zero external consumers does not mean safe deletion without migrating package-internal references.

Search commands used for upstream coverage (hits inspected as code, not treated as equivalence by name):

```sh
rg -n '^(export|const).*' .repos/effect/packages/effect/src/Schema*.ts .repos/effect/packages/effect/src/JsonSchema.ts
rg -n -i 'abortsignal|arraybuffer|float16array|float32array|float64array|bigdecimal|duration|datetime|bytesize|email|cuid|csv|conformance|evm|ethereum|atproto|did:|continent|currency|fileextension|filename|reactnode|cssproperties' .repos/effect/packages/effect/src --glob '*.ts' --glob '!**/internal/**' --glob '!**/Scalar.ts' --max-columns 250
rg -n -i 'content-security-policy|expect-ct|x-frame-options|cross-origin-(opener|embedder|resource)-policy|strict-transport-security' .repos/effect/packages/effect/src/Schema*.ts .repos/effect/packages/effect/src/unstable/{http,encoding,net,arbitrary,schema} --glob '*.ts' --max-columns 250
rg -n 'export|readonly (stat|basename|extname)' .repos/effect/packages/effect/src/{BigDecimal,Duration,DateTime,ByteSize,Path,FileSystem,Encoding}.ts
```

`NONE` means no same-intent public implementation found in the inspected public source; it does not mean no generic combinator can build one. Pure aliases/preset compositions retire. Domain vocabularies and specialized parsers remain when upstream supplies only construction primitives. Binary byte storage can move from ArrayBuffer to Uint8Array under the locked behavior-change rule; native floating-point arrays retain their distinct numerical representation and codecs. No performance improvement is claimed: type-check/runtime deltas are **UNVERIFIED** until implementation measurement.

## Lead commits checked

Command: `git -C .repos/effect show --stat 46d83101e8 a63dcbf04e 0a08ae0626`.

| Commit | Observed changes | A–F implication and current source |
|---|---|---|
| `46d83101e8` | HTTP schemas moved from Cookies/Headers/UrlParams to Schema | `packages/effect/src/Schema.ts:12983`, `packages/effect/src/Schema.ts:13027`, `packages/effect/src/Schema.ts:13113`, `packages/effect/src/Schema.ts:13165`; no security-policy formatter replacement. |
| `a63dcbf04e` | Native arbitrary API, schema interpreter and annotation changes; old FastCheck surface removed | `packages/effect/src/unstable/arbitrary/Arbitrary.ts:346` derives arbitrary values from schemas; floating-array annotations already use codec links, not an obsolete standalone generator. |
| `0a08ae0626` | NetAddress, IpInterface, IpNetwork and corresponding Schema values/codecs | `packages/effect/src/Schema.ts:11399`, `packages/effect/src/Schema.ts:11446`, `packages/effect/src/Schema.ts:11587`, `packages/effect/src/Schema.ts:11728`; IP/MAC/network intent does not cover DID/EVM/wallet domains. |

## Concept inventory

| Concept | What (own code/JSDoc) | Upstream cite | Concrete Beep delta | Consumers | Disposition | Reason |
|---|---|---|---|---:|---|---|
| AbortSignal | Validate AbortSignal runtime instances — `packages/foundation/modeling/schema/src/AbortSignal.ts:48` | `packages/effect/src/Schema.ts:6373` (instanceOf) | Own guard and identifier; no brand/codec/arbitrary | 0 | RETIRE | Direct instanceOf composition covers the whole runtime contract. |
| Age | Age in years — `packages/foundation/modeling/schema/src/Age/Age.schema.ts:28` | NONE | Age brand and integer range 1–150 | 0 | KEEP | Generic integer bounds do not supply an age policy. |
| ArrayBuffer | Attached native byte buffer with base64 JSON — `packages/foundation/modeling/schema/src/ArrayBuffer.ts:98` | `packages/effect/src/Schema.ts:9946` (Uint8Array), `packages/effect/src/Schema.ts:9986` (Uint8ArrayFromBase64) | ArrayBuffer representation; detached rejection; byte equivalence; JSON link | 0 | RETIRE | Upstream binary-byte value and base64 codec cover intent; migrate representation to Uint8Array. |
| ArrayOf | String/number/integer array presets — `packages/foundation/modeling/schema/src/ArrayOf.ts:29` | `packages/effect/src/Schema.ts:4450` (Array), `packages/effect/src/Schema.ts:4528` (NonEmptyArray), `packages/effect/src/Schema.ts:8555` (NonEmptyString), `packages/effect/src/Schema.ts:7097` (Finite), `packages/effect/src/Schema.ts:7581` (Int) | Identifier/description presets only | 6 | RETIRE | All preset shapes are direct upstream compositions. |
| AtURI | Normalized AT Protocol Lexicon URI — `packages/foundation/modeling/schema/src/AtURI.ts:240` | NONE | AtUri brand; handle/DID/NSID/record-key grammar; percent-escape restrictions; constrained arbitrary | 0 | KEEP | Generic URL cannot validate AT Protocol component grammar. |
| BigDecimal | Convert finite number to BigDecimal — `packages/foundation/modeling/schema/src/BigDecimal.ts:37` | `packages/effect/src/BigDecimal.ts:1257` (fromNumber), `packages/effect/src/BigDecimal.ts:1221` (fromNumberUnsafe), `packages/effect/src/BigDecimal.ts:1500` (toNumberUnsafe), `packages/effect/src/Schema.ts:10221` (BigDecimal) | Bidirectional number codec glue and identifier | 0 | RETIRE | Upstream value conversion covers intent; choose direct conversion or consumer-local codec. |
| BinaryFileExtension | Classify binary extensions/content — `packages/foundation/modeling/schema/src/BinaryFileExtension.ts:181` | NONE | Dotted extension catalog, extension normalization, NUL/nonprintable sample heuristic | 0 | KEEP | No upstream binary-versus-text classifier found. |
| BufferEncoding | Node Buffer encoding-name literals — `packages/foundation/modeling/schema/src/BufferEncoding.ts:28` | NONE | Alias spellings plus LiteralKit statics | 0 | KEEP | Encoding codecs do not provide the BufferEncoding name domain. |
| Bytes | Protobuf byte payload as Uint8Array — `packages/foundation/modeling/schema/src/Bytes.ts:50` | `packages/effect/src/Schema.ts:9946` (Uint8Array) | Bytes brand; unsigned-length ceiling; identifiers | 0 | RETIRE | Binary payload intent covered; drop protobuf brand/length wrapper under same-intent rule. |
| CardinalDirection | Cardinal direction words and abbreviations — `packages/foundation/modeling/schema/src/CardinalDirection/CardinalDirection.schema.ts:28` | NONE | LiteralKit word and abbreviation domains | 0 | KEEP | No upstream direction vocabulary found. |
| Color | Canonical hex/RGB/OKLCH colors and conversions — `packages/foundation/modeling/schema/src/Color/Color.hex.ts:174` | NONE | Branded components, clamp/normalize codecs, mixing/lightness/alpha and scale generation, codec statics | 4 | KEEP | No upstream color value model or conversions found. |
| CommonTextSchemas | Trimmed text, delimited lists, boolean text — `packages/foundation/modeling/schema/src/CommonTextSchemas.ts:37` | `packages/effect/src/Schema.ts:9706` (Trim), `packages/effect/src/Schema.ts:8555` (NonEmptyString), `packages/effect/src/SchemaGetter.ts:1270` (split), `packages/effect/src/Config.ts:1315` (Boolean) | Trimming and blank dropping; permissive unknown-to-false boolean semantics; annotations | 3 | RETIRE | Public upstream normalization/splitting/config parsing cover intent; accept stricter boolean behavior and changed entry point. |
| Conformance | Specification/profile/invariant conformance metadata — `packages/foundation/modeling/schema/src/Conformance/Conformance.annotations.ts:138` | NONE | Versioned registries, annotation consistency, AST collector, policies and reports | 35 | KEEP | Generic schema annotations/issues are not specification provenance and conformance policy. |
| ContinentCode | CLDR continent codes/names and reversible mapping — `packages/foundation/modeling/schema/src/ContinentCode.ts:32` | NONE | Territory data and mapped-literal statics | 0 | KEEP | No upstream CLDR territory vocabulary found. |
| ContinentName | Continent display-name alias — `packages/foundation/modeling/schema/src/ContinentName.ts:25` | NONE | Re-exports local ContinentCode name schema | 0 | KEEP | Local duplication is not upstream parity; consolidate separately if desired. |
| CountryCode | Territory country code plus emoji flag conversion — `packages/foundation/modeling/schema/src/CountryCode.ts:89` | NONE | Territory schemas, name codecs, regional-indicator emoji static | 0 | KEEP | No upstream country-code data or conversion found. |
| CountryName | Territory display-name schema alias — `packages/foundation/modeling/schema/src/CountryName.ts:26` | NONE | Alias to local territory name model | 0 | KEEP | No upstream territory vocabulary found. |
| CrossOriginEmbedderPolicy | COEP options to response header — `packages/foundation/modeling/schema/src/CrossOriginEmbedderPolicy/CrossOriginEmbedderPolicy.schema.ts:158` | NONE | Policy literals, false/undefined behavior, one-way codec, create/createValue statics | 0 | KEEP | Generic Headers does not implement COEP option semantics. |
| CrossOriginOpenerPolicy | COOP options to response header — `packages/foundation/modeling/schema/src/CrossOriginOpenerPolicy/CrossOriginOpenerPolicy.schema.ts:157` | NONE | Policy literals, default/disabled behavior, one-way codec and creator statics | 0 | KEEP | Generic Headers does not implement COOP policy. |
| CrossOriginResourcePolicy | CORP options to response header — `packages/foundation/modeling/schema/src/CrossOriginResourcePolicy/CrossOriginResourcePolicy.schema.ts:154` | NONE | Policy literals, defaults, disabled state, typed header and creator statics | 0 | KEEP | Generic Headers does not implement CORP policy. |
| CryptoTxnHash | Canonical EVM/Bitcoin/Solana transaction identifier — `packages/foundation/modeling/schema/src/CryptoTxnHash/CryptoTxnHash.schema.ts:62` | NONE | Network-specific checks, brand, redacted companion | 0 | KEEP | Network address values concern IP/MAC/socket addresses, not blockchain identifiers. |
| CryptoWalletAddress | Canonical EVM/Bitcoin/Solana wallet address — `packages/foundation/modeling/schema/src/CryptoWalletAddress/CryptoWalletAddress.schema.ts:136` | NONE | Chain-family validation/checksums, brand, redacted companion | 0 | KEEP | No upstream cryptocurrency wallet address domain found. |
| Csp | Content Security Policy directive/config/header model — `packages/foundation/modeling/schema/src/Csp/Csp.schema.ts:670` | NONE | Directive classes, report-only selection, option/default formatting, one-way codec and statics | 0 | KEEP | Headers container and HTTP schema move do not supply CSP grammar/policy. |
| Csv | CSV text to typed row arrays — `packages/foundation/modeling/schema/src/Csv/Csv.schema.ts:315` | NONE | Branded text, row-schema factory, header/column validation and codec options | 1 | KEEP | SchemaGetter.split is delimiter splitting, not quoted CSV document decoding. |
| CsvCodecOptions | CSV parsing/formatting configuration — `packages/foundation/modeling/schema/src/CsvCodecOptions/CsvCodecOptions.schema.ts:51` | NONE | Single-character controls and schema-backed options/defaults | 0 | KEEP | No upstream CSV document codec options found. |
| CsvError | Typed CSV processing failures — `packages/foundation/modeling/schema/src/CsvError/CsvError.errors.ts:30` | NONE | CSV-specific fields and error constructor helper | 0 | KEEP | TaggedError is an error builder, not an upstream CSV error domain. |
| CsvFormatter | Format CSV headers/rows/documents — `packages/foundation/modeling/schema/src/CsvFormatter/CsvFormatter.formatter.ts:186` | NONE | CSV quoting/escaping and option-aware formatting | 0 | KEEP | SQL Statement.csv joins SQL fragments; it is not CSV file formatting. |
| CsvParser | Parse CSV into fields and rows — `packages/foundation/modeling/schema/src/CsvParser/CsvParser.parser.ts:415` | NONE | Stateful quoted-field parsing and parsed field/row models | 2 | KEEP | Generic split does not cover quoted CSV parser intent. |
| Cuid | CUID validation and generation — `packages/foundation/modeling/schema/src/Cuid.ts:64` | NONE | Brand, seed model, state service, SHA-512-backed generation, is static | 10 | KEEP | No upstream CUID generator/model found. |
| CurrencyCode | Active ISO 4217 currencies — `packages/foundation/modeling/schema/src/CurrencyCode.ts:43` | NONE | Code/name data, common currency constants, guard | 0 | KEEP | BigDecimal supplies arithmetic, not currency vocabulary. |
| DateTimeUtcFromValid | Valid DateTime input to UTC with UI adapters — `packages/foundation/modeling/schema/src/DateTimeUtcFromValid/DateTimeUtcFromValid.schema.ts:483` | `packages/effect/src/DateTime.ts:793` (make), `packages/effect/src/DateTime.ts:917` (toUtc), `packages/effect/src/Schema.ts:11012` (DateTimeUtcFromString), `packages/effect/src/Schema.ts:11047` (DateTimeUtcFromMillis) | Tagged transport variants; canonical tagged ISO encoding; timezone/UI empty-input adapters | 2 | RETIRE | Upstream DateTime input normalization covers intent; adapt transport and picker integration. |
| Did | W3C DID Core identifier grammar — `packages/foundation/modeling/schema/src/Did.ts:79` | NONE | DID syntax check, brand and annotations | 0 | KEEP | Generic URL/URI and IP address models do not implement DID grammar. |
| DomCssProperties | React CSSProperties runtime declaration — `packages/foundation/modeling/schema/src/DomCssProperties/DomCssProperties.schema.ts:51` | NONE | React typing with plain-object guard only; no CSS property validation | 0 | KEEP | No upstream React CSSProperties model found; weak guard merits separate correctness review. |
| DomDragEvent | Validate DragEvent instances — `packages/foundation/modeling/schema/src/DomDragEvent/DomDragEvent.schema.ts:44` | `packages/effect/src/Schema.ts:6373` (instanceOf) | Named guard, identifier and aliases only | 0 | RETIRE | Whole predicate is instanceOf(DragEvent). |
| DomEvent | Validate Event instances — `packages/foundation/modeling/schema/src/DomEvent/DomEvent.schema.ts:44` | `packages/effect/src/Schema.ts:6373` (instanceOf) | Named guard, identifier and aliases only | 0 | RETIRE | Whole predicate is instanceOf(Event). |
| DomHtmlElement | Validate HTMLElement instances — `packages/foundation/modeling/schema/src/DomHtmlElement/DomHtmlElement.schema.ts:44` | `packages/effect/src/Schema.ts:6373` (instanceOf) | Named guard, identifier and aliases only | 0 | RETIRE | Whole predicate is instanceOf(HTMLElement). |
| DomMouseEvent | Validate MouseEvent instances — `packages/foundation/modeling/schema/src/DomMouseEvent/DomMouseEvent.schema.ts:44` | `packages/effect/src/Schema.ts:6373` (instanceOf) | Named guard, identifier and aliases only | 0 | RETIRE | Whole predicate is instanceOf(MouseEvent). |
| DomReactNode | React renderable nodes and refs — `packages/foundation/modeling/schema/src/DomReactNode/DomReactNode.schema.ts:58` | NONE | React node predicate plus generic React.Ref declaration | 1 | KEEP | No upstream React node/ref domain found. |
| Double | IEEE-754 binary64 protobuf number — `packages/foundation/modeling/schema/src/Double.ts:52` | `packages/effect/src/Schema.ts:3012` (Number) | Double brand; redundant number predicate; ProtobufNumber custom arbitrary link | 0 | RETIRE | Schema.Number already accepts NaN and infinities and defines JSON handling. |
| Duration | Normalize heterogeneous Duration inputs — `packages/foundation/modeling/schema/src/Duration/Duration.input.ts:289` | `packages/effect/src/Duration.ts:343` (fromInput), `packages/effect/src/Schema.ts:12061` (Duration), `packages/effect/src/Schema.ts:12136` (DurationFromString), `packages/effect/src/Schema.ts:12165` (DurationFromNanos), `packages/effect/src/Schema.ts:12196` (DurationFromMillis) | Input union, nonnegative/integer restrictions, additive-object class, unit statics, forbidden encoder | 2 | RETIRE | Underlying normalization already calls upstream fromInput; remove concept and adapt boundary shape. |
| EffectSchema | Validate Effect runtime values — `packages/foundation/modeling/schema/src/EffectSchema.ts:85` | `packages/effect/src/Effect.ts:230` (isEffect), `packages/effect/src/Schema.ts:562` (declare) | Generic phantom success/error/context types, strict-reference equivalence, formatter and generation metadata | 5 | RETIRE | Upstream guard plus declaration covers runtime detection; no checking of Effect channels existed. |
| Email | Normalized branded/redacted email addresses — `packages/foundation/modeling/schema/src/Email.ts:33` | NONE | Trim/lowercase, regex and length checks, Email brand, redaction | 5 | KEEP | Email examples/JSON format metadata are not a shipped email validator. |
| EthAmount | Nonnegative ETH amount as BigDecimal — `packages/foundation/modeling/schema/src/EthAmount/EthAmount.schema.ts:56` | NONE; arithmetic substrate `packages/effect/src/BigDecimal.ts:1257` | Finite nonnegative input, EthAmount brand and denomination-specific annotation | 0 | KEEP | Decimal conversion does not supply ETH-denomination policy. |
| EthereumValidatorPublicKey | Canonical compressed Ethereum validator key — `packages/foundation/modeling/schema/src/EthereumValidatorPublicKey/EthereumValidatorPublicKey.schema.ts:53` | NONE | Lowercase hex width check, brand and redacted companion | 0 | KEEP | No upstream BLS validator-key domain found. |
| EvmAddress | Canonical mainnet EVM address — `packages/foundation/modeling/schema/src/EvmAddress/EvmAddress.schema.ts:50` | NONE | Lowercase/EIP-55 checks, brand and redacted variant | 0 | KEEP | No upstream EVM address intent found. |
| ExpectCt | Expect-CT options to response header — `packages/foundation/modeling/schema/src/ExpectCt/ExpectCt.schema.ts:232` | NONE | Max-age/enforce/report URI config, default and disabled modes, one-way codec/statics | 0 | KEEP | Generic Headers does not implement certificate-transparency header policy. |
| FileDiff.schema | Added/deleted/modified file summaries — `packages/foundation/modeling/schema/src/FileDiff.schema.ts:155` | NONE | Status classes, optional file/patch and addition/deletion counters | 0 | KEEP | JSON patch differ is not a file-level diff summary model. |
| FileExtension | MIME-derived extension vocabulary — `packages/foundation/modeling/schema/src/FileExtension.ts:360` | NONE | MIME category literals and extraction helpers | 3 | KEEP | Path.extname extracts a suffix but does not validate a known-extension vocabulary. |
| FileInfo | Filesystem stat metadata discriminated by type — `packages/foundation/modeling/schema/src/FileInfo.ts:145` | `packages/effect/src/FileSystem.ts:950` (File.Info), `packages/effect/src/FileSystem.ts:304` (stat) | Schema decoding, case constructors, default None stat fields and annotations | 1 | RETIRE | Upstream stat type has the same fields and byte-size representation; adapt to value/service API. |
| FileName | Portable basename with known extension — `packages/foundation/modeling/schema/src/FileName.ts:153` | NONE | Stem/extension grammar and template-literal type | 1 | KEEP | Path.basename extracts names; it does not enforce portable known-extension names. |
| FilePath | Validated POSIX/Windows path string — `packages/foundation/modeling/schema/src/FilePath/FilePath.schema.ts:162` | NONE | Path family classification, Windows root/segment checks, NUL/leaf checks and brand | 6 | KEEP | Path service operations do not validate this portable-path policy. |
| FileTypeChecker | Detect/validate file type from signatures — `packages/foundation/modeling/schema/src/FileTypeChecker/FileTypeChecker.behavior.ts:287` | NONE | Magic-byte catalogs, offsets/signature matching, content/options/result models | 0 | KEEP | MIME-by-extension and Schema.File do not perform signature-based detection. |
| Fixed32 | Unsigned protobuf fixed32 integer — `packages/foundation/modeling/schema/src/Fixed32.ts:68` | `packages/effect/src/Schema.ts:7654` (isUint32) | Fixed32 brand, filter group, error metadata | 0 | RETIRE | Uint32 filter covers the exact unsigned integer range intent. |
| Fixed64 | Normalize protobufjs fixed64 input to unsigned bigint — `packages/foundation/modeling/schema/src/Fixed64.ts:58` | NONE; scalar substrate `packages/effect/src/Schema.ts:7984` (isBetweenBigInt), `packages/effect/src/Schema.ts:9664` (BigIntFromString) | Long/number/string/bigint transport normalization, uint64 range and brand | 0 | KEEP | No public protobufjs Long-aware uint64 normalization found. |
| Float | Protobuf binary32-range number including special values — `packages/foundation/modeling/schema/src/Float.ts:57` | `packages/effect/src/Schema.ts:3012` (Number); NONE for binary32 range | Range/special-value check, Float brand; redundant ProtobufNumber declaration/arbitrary | 0 | ADAPT | Keep binary32 domain check/brand but replace the generic number declaration with upstream Number. |
| Float16Array | Native Float16Array with numeric transport/model fields — `packages/foundation/modeling/schema/src/Float16Array.ts:86` | NONE; building blocks `packages/effect/src/Schema.ts:562` (declare), `packages/effect/src/unstable/schema/Model.ts:132` (Field), `packages/effect/src/unstable/arbitrary/Arbitrary.ts:346` (schema) | Feature detection/typed runtime error, native conversion, bounded arbitrary codec, create/update variants | 0 | KEEP | No shipped Float16Array value/codec; native arbitrary generation does not invent its constructor conversion. |
| Float32Array | Native Float32Array with numeric transport/model fields — `packages/foundation/modeling/schema/src/Float32Array.ts:44` | NONE; building blocks `packages/effect/src/Schema.ts:6373` (instanceOf), `packages/effect/src/unstable/schema/Model.ts:132` (Field), `packages/effect/src/unstable/arbitrary/Arbitrary.ts:346` (schema) | Finite array conversion, bounded arbitrary codec, insert/update and JSON variants | 3 | KEEP | No shipped Float32Array codec; keep native representation required by consumers. |
| Float64Array | Native Float64Array with numeric transport/model fields — `packages/foundation/modeling/schema/src/Float64Array.ts:44` | NONE; building blocks `packages/effect/src/Schema.ts:6373` (instanceOf), `packages/effect/src/unstable/schema/Model.ts:132` (Field), `packages/effect/src/unstable/arbitrary/Arbitrary.ts:346` (schema) | Finite array conversion, bounded arbitrary codec, insert/update and JSON variants | 0 | KEEP | No shipped Float64Array codec found. |
| Fn | Runtime function schema with input/output/error contracts — `packages/foundation/modeling/schema/src/Fn/Fn.schema.ts:593` | NONE | No-arg/unary overloads; implement/implementSync/implementEffect statics; error-cause and output validation | 22 | KEEP | Generic function guards/Effect.fn do not supply schema-backed call contracts. |
| ForceHttpsRedirect | HSTS options to response header — `packages/foundation/modeling/schema/src/ForceHttpsRedirect/ForceHttpsRedirect.schema.ts:191` | NONE | Max-age/includeSubDomains/preload, default/disabled behavior, one-way codec and statics | 0 | KEEP | Name is misleading: code creates HSTS, not an HTTP redirect; upstream generic headers do not implement policy. |
| FrameGuard | X-Frame-Options policy/header codec — `packages/foundation/modeling/schema/src/FrameGuard/FrameGuard.schema.ts:243` | NONE | deny/sameorigin/allow-from, URI handling, default/disabled modes and statics | 0 | KEEP | No upstream frame-guard policy builder found. |

## Recipes

All RETIRE recipes remove the concept and its facade/subpath exports in the same consumer-migration change. ADAPT retains only the stated policy. `S` below means `import * as S from "effect/Schema"`; `G` means `import * as G from "effect/SchemaGetter"`. These are source-checked migration shapes, not executed consumer migrations. Full consumer type-checking, transport compatibility, browser behavior, and generated-schema snapshots are **UNVERIFIED**.

| Concept | Before import and call | After import and call | Risk 1 | Risk 2 |
|---|---|---|---|---|
| AbortSignal | `import { AbortSig, isAbortSignal } from "@beep/schema/AbortSignal"`; `S.decodeUnknownSync(AbortSig)(x)` | Import `S`; use `S.decodeUnknownSync(S.instanceOf(globalThis.AbortSignal))(x)` and `S.is(S.instanceOf(globalThis.AbortSignal))(x)`; define the schema once at the consumer boundary | Constructor must exist where schema initializes; do not move browser-only construction into server modules | Own identifiers disappear; cross-realm instanceof limitations persist |
| ArrayBuffer | `import { ArrayBuf } from "@beep/schema/ArrayBuffer"`; `S.decodeUnknownSync(ArrayBuf)(buffer)` | Import `S`; change value/field types to `Uint8Array`; `S.decodeUnknownSync(S.Uint8Array)(new Uint8Array(buffer))`; base64 transport becomes `S.decodeUnknownSync(S.Uint8ArrayFromBase64)(text)` / `S.encodeSync(S.Uint8ArrayFromBase64)(bytes)` | Views can alias memory and have nonzero offsets; do not pass `.buffer` as the logical payload without respecting byteOffset/byteLength or copying | Detached checks, byte equivalence and schema-generated JSON behavior must be deliberately reviewed; conversion may throw before decoding |
| ArrayOf | `import { ArrayOfStrings, NonEmptyArrayOfInts } from "@beep/schema"`; use constants as field schemas | Import `S`; replace with `S.Array(S.String)`, `S.NonEmptyArray(S.Int)`; complete mapping below | Numeric presets use `S.Finite`, not unconstrained `S.Number` | Named schema identifiers/descriptions disappear from diagnostics and generated JSON |
| BigDecimal | `import { BigDecimalFromNumber } from "@beep/schema"`; `S.decodeUnknownSync(BigDecimalFromNumber)(n)` | `import * as BigDecimal from "effect/BigDecimal"`; `BigDecimal.fromNumber(n)` returns Option. For schema boundaries: `S.Finite.pipe(S.decodeTo(S.BigDecimal, { decode: G.transform(BigDecimal.fromNumberUnsafe), encode: G.transform(BigDecimal.toNumberUnsafe) }))`; no replacement shared alias | Option failures replace SchemaError unless boundary codec is retained | Number conversion is lossy; reverse conversion can overflow finite-number constraints |
| Bytes | `import { Bytes } from "@beep/schema/Bytes"`; `S.decodeUnknownSync(Bytes)(bytes)` | Import `S`; `S.decodeUnknownSync(S.Uint8Array)(bytes)`; type becomes `Uint8Array<ArrayBufferLike>` | Brand distinction and protobuf length ceiling disappear | Upstream JSON representation/equivalence must replace any consumer expectations attached to the old annotation |
| CommonTextSchemas | `import { TrimmedNonEmptyText, CommaSeparatedList, NormalizedBooleanString } from "@beep/schema/CommonTextSchemas"`; decode each | Import `S`, `G`; text becomes `S.Trim.check(S.isNonEmpty())`; list can become `S.String.pipe(S.decodeTo(S.Array(S.Trim.check(S.isNonEmpty())), { decode: G.split(), encode: G.transform((xs) => xs.join(",")) }))`; config booleans use `import * as Config from "effect/Config"; Config.Boolean("FLAG")` in a ConfigProvider context | Proposed list rejects blank entries instead of dropping them; boolean parser rejects unknown spellings and requires config context | Do not import `S.BooleanLiterals`: it is `@internal` at `packages/effect/src/Schema.ts:5438`. Non-config text boundaries must adopt an explicit public literal/boolean transport, not silently call a private schema |
| DateTimeUtcFromValid | `import { DateTimeUtcFromValid, createDateTimeWithTimezone, applyTimezone } from "@beep/schema/DateTimeUtcFromValid"`; decode heterogeneous/tagged inputs | `import * as DateTime from "effect/DateTime"; import * as Option from "effect/Option"`; `Option.map(DateTime.make(input), DateTime.toUtc)` for valid upstream inputs. Schema transport: `S.DateTimeUtcFromString` or `S.DateTimeUtcFromMillis`. Replace tagged primitive wrappers with their raw value; remove Parts tag before calling upstream make. UI timezone logic belongs in the existing picker adapter using `DateTime.setZone(value, DateTime.zoneMakeLocal())`, `DateTime.toUtc(value)`, or named-zone constructors | Encoded tagged ISO form becomes plain ISO/millis; update persistence/API readers with writers | Picker nullable/invalid sentinel and `default`/`system` timezone handling must be migrated explicitly; runtime MUI integration is unverified |
| DomDragEvent | `import { DOMDragEvent, isDragEvent } from "@beep/schema/DomDragEvent"`; decode/guard | Import `S`; `const event = S.instanceOf(globalThis.DragEvent)`; use `S.decodeUnknownSync(event)` / `S.is(event)` | Eager constructor access fails in runtimes without DragEvent | Identifier changes and cross-realm instanceof behavior |
| DomEvent | `import { DOMEvent, isEvent } from "@beep/schema/DomEvent"`; decode/guard | Import `S`; `const event = S.instanceOf(globalThis.Event)`; use upstream decoder/guard | DOM and server Event classes can differ | Generated identifiers and diagnostics change |
| DomHtmlElement | `import { DOMHtmlElement, isHTMLElement } from "@beep/schema/DomHtmlElement"`; decode/guard | Import `S`; `const element = S.instanceOf(globalThis.HTMLElement)`; use upstream decoder/guard | No HTMLElement on server; instantiate only in browser boundary | Iframe realm identity and old metadata expectations |
| DomMouseEvent | `import { DOMMouseEvent, isMouseEvent } from "@beep/schema/DomMouseEvent"`; decode/guard | Import `S`; `const event = S.instanceOf(globalThis.MouseEvent)`; use upstream decoder/guard | No MouseEvent in some test/server environments | Cross-realm event handling and identifier changes |
| Double | `import { Double } from "@beep/schema/Double"`; `S.decodeUnknownSync(Double)(n)` | Import `S`; `S.decodeUnknownSync(S.Number)(n)`; type becomes `number` | Do not substitute `S.Finite`: NaN/infinities are explicitly valid | Brand/custom arbitrary disappear; upstream special-number JSON strings may alter snapshots |
| Duration | `import * as Duration from "@beep/schema/Duration"`; `S.decodeUnknownSync(Duration.FromInput)(x)` | `import * as Duration from "effect/Duration"`; `Duration.fromInput(x)` returns Option for typed `Duration.Input`. Runtime schema becomes `S.Duration`; select string/millis/nanos transport with `S.DurationFromString`, `S.DurationFromMillis`, `S.DurationFromNanos`. Replace `new Duration.Object({hours:1})` with `{hours:1}` as upstream input; unit statics become direct literals or upstream constructors such as `Duration.hours(1)` | fromInput is typed, not an unknown-input validator; choose a transport schema before accepting unknown data | Negative/fractional/infinite inputs and reverse encoding differ from the old restricted one-way codec |
| EffectSchema | `import { EffectSchema, isEffect } from "@beep/schema/EffectSchema"`; `EffectSchema<A,E,R>()` | `import * as Effect from "effect/Effect"`; use `Effect.isEffect(x)` directly, or `S.declare<Effect.Effect<A,E,R>>(Effect.isEffect)` when embedding a runtime value in a struct | Generic A/E/R remain unchecked phantom claims; declaration never executes or validates Effect channels | Strict-reference equivalence, `[Effect]` formatter and generation import metadata disappear; supply local annotations only where a demonstrated interpreter requires them |
| FileInfo | `import { FileInfo } from "@beep/schema/FileInfo"`; `FileInfo.guards.Directory(info)` / `.guards.File(info)` | `import type * as FileSystem from "effect/FileSystem"`; `info: FileSystem.File.Info`; `info.type === "Directory"` / `info.type === "File"`. Existing `FileSystem.stat` results already have that type | Upstream type is not a runtime decoder; actual untrusted transport boundaries need explicit boundary schemas | Case constructors/default-None behavior disappear; manually constructed fixtures must fill required Option fields |
| Fixed32 | `import { Fixed32 } from "@beep/schema/Fixed32"`; decode | Import `S`; `S.decodeUnknownSync(S.Number.check(S.isUint32()))(x)` | Fixed32 brand and protobuf error identifiers disappear | Verify downstream schema generation does not rely on the old named filter group |
| Float (ADAPT) | `import { Float } from "@beep/schema/Float"`; decode; implementation uses local `ProtobufNumber` | Consumer import stays. Inside existing Float.ts use `S.Number.check(FloatChecks).pipe(S.brand("Float"), existingAnnotations)`; retain binary32 range/special-value predicate, remove dependency on local ProtobufNumber | Upstream arbitrary/JSON handling replaces the custom declaration's interpreter behavior | Existing predicate admits finite values within range, not only exactly representable binary32 values; do not accidentally introduce rounding/exactness changes |

ArrayOf full mapping (same imports as recipe):

| Old export | Replacement |
|---|---|
| ArrayOfStrings | `S.Array(S.String)` |
| NonEmptyArrayOfStrings | `S.NonEmptyArray(S.String)` |
| ArrayOfNonEmptyStrings | `S.Array(S.NonEmptyString)` |
| NonEmptyArrayOfNonEmptyStrings | `S.NonEmptyArray(S.NonEmptyString)` |
| ArrayOfNumbers | `S.Array(S.Finite)` |
| NonEmptyArrayOfNumbers | `S.NonEmptyArray(S.Finite)` |
| ArrayOfInts | `S.Array(S.Int)` |
| NonEmptyArrayOfInts | `S.NonEmptyArray(S.Int)` |

Additional recipe anchors: `packages/effect/src/Schema.ts:5387` (decodeTo overload), `packages/effect/src/DateTime.ts:936` (setZone), `packages/effect/src/DateTime.ts:1103` (zoneMakeLocal), `packages/effect/src/DateTime.ts:749` (makeZoned). The picker-specific behavior being relocated is at `packages/foundation/modeling/schema/src/DateTimeUtcFromValid/DateTimeUtcFromValid.adapter.ts:78`, `packages/foundation/modeling/schema/src/DateTimeUtcFromValid/DateTimeUtcFromValid.adapter.ts:112`, and `packages/foundation/modeling/schema/src/DateTimeUtcFromValid/DateTimeUtcFromValid.adapter.ts:143`.

## Open questions for align

| Question | Evidence / choice needed |
|---|---|
| Which transport should replace heterogeneous DateTime and Duration codecs at each actual boundary? | Choose ISO/millis/nanos or upstream typed input; retirements remain locked, but wire shape needs an explicit consumer decision. |
| Can all ArrayBuffer consumers use Uint8Array internally, or do platform calls require exact ArrayBuffer output? | Binary intent is covered; preserve offsets/ownership when converting at platform calls. The external census does not include schema-package internal FileTypeChecker dependencies. |
| What is the authoritative count for migration planning: direct imports or transitive symbol reachability? | This report supplies reproducible direct-file counts; compiler-backed transitive closure remains unverified. |
| Must runtime function/Effect values support generated equivalence/formatting after migration? | EffectSchema custom formatter/equivalence are deliberate deltas; Fn's call-contract behavior has no upstream replacement. |
| Should unused CommonText boolean/list exports disappear outright, with any future boolean configuration using Config.Boolean? | Current external concept consumers use TrimmedNonEmptyText; avoid replacing a deleted public wrapper with an upstream private export. |
| Should protobuf Float and Fixed64 remain foundation schemas or move closer to protobuf consumers? | Float only retains binary32 policy; Fixed64 has protobufjs-specific normalization. This is placement work, not an upstream equivalence claim. |
| What should the parity detector recognize as covered composition versus merely reusable substrate? | Pure instance wrappers and ArrayOf presets retire; CSV parsing, React values and specification domains do not retire merely because declare/Struct/filters exist. |
| Which fixture should capture per-change type-check and runtime numbers? | No performance results were measured by this lane; attach numbers to implementation before claiming optimization. |

## Verification boundary and friction

| Item | Result |
|---|---|
| Concept membership | Derived from rg file inventory; every top-level A–F concept has a row. |
| Disposition/consumer counts | Recomputed from rows and deduplicated concept/file ledger; commands above. |
| Source citations | Checked against local source line bounds; declaration anchors corrected before handoff. |
| Runtime/API recipes | Source-checked; selected public compositions smoke-tested separately below. Complete consumer migration remains **UNVERIFIED**. |
| Graft | Skill read; queries skipped because the skill says queries can refresh its cache, violating this lane's write-only-research constraint. Read-only rg substituted. |
| Tool friction | A Node child-process read produced `spawnSync /bin/sh EPERM`; switched to direct shell rg and Node filesystem reads. No escalation or source mutation. |
| Existing work | No git mutation, build, full test suite, dependency install, or network operation. |

Selected API smoke results: **9 PASS**, executed with `bun -e` importing the local `.repos/effect/packages/effect/src/` modules directly. Receipt: `retirement-A-F-smoke.txt`. Checks exercised the recipe expressions for trimmed text; list decode and encode; infinite double; uint32 upper bound; base64 and decimal round trips; Duration object input; Effect declaration. This proves those examples at runtime, not browser integration or consumer compatibility.

Verification count commands:

```sh
rg -c ": PASS$" explorations/effect-schema-parity/research/retirement-A-F-smoke.txt
sed -n "/^## Recipes/,/^ArrayOf full mapping/p" explorations/effect-schema-parity/research/retirement-A-F.md | rg -c "^\| [A-Z][^|]* \| .*@beep/schema"
```

The recipe-row count is **17** (all RETIRE/ADAPT concepts). Companion `.cjs` scripts only write A–F evidence under this research directory; the report itself is authored judgment, not automatically regenerated by those scripts.
