# Retirement audit G–Z

## Evidence and counting

| Item | Verified basis |
| --- | --- |
| Upstream | Repo pin `package.json:161` is `effect@4.0.0-rc.115`; `git -C .repos/effect rev-parse HEAD` → `51d4a2f08a5c7691dc876415bc9fc0ecf467e153`; all upstream citations below are relative to `.repos/effect/`. |
| Repo topology | `packages/foundation/modeling/schema/README.md:12`; root facade `packages/foundation/modeling/schema/src/index.ts:234` onward mixes flat exports with namespace exports. `Http/` is present as internal support despite retired topical public paths; it remains in the requested physical-entry inventory. |
| Decision boundary | `explorations/effect-schema-parity/DECISIONS.md:72` same intent, same-change retirement; `explorations/effect-schema-parity/DECISIONS.md:117` Role B exclusion. `UNSURE` records that conflict where coverage is exclusively Role B, not a proposed deprecation window. |
| Scope semantics | One row per top-level G–Z file/directory, including non-root-exported entries. KEEP means no verified same-intent public API, not no reusable generic combinator. For numeric primitives, same-intent compositions replace aliases; domain policies such as percentages, ports and security headers remain distinct. |
| Validation limit | Source/signature review plus the bounded upstream probes recorded below. Consumer migration execution, full wire compatibility, before/after performance, and unprobed recipes remain **UNVERIFIED**. No full builds/test suites, network calls or git mutations were run. |

Commands (once for this section):

```sh
rg --files packages/foundation/modeling/schema/src | awk -F/src/ '{print $2}' | cut -d/ -f1 | sed 's/\.ts$//' | sort -u | rg '^[G-Z]' | wc -l
rg -l '@beep/schema' packages/ apps/ --glob '*.{ts,tsx,js,jsx,mjs,cjs,mts,cts}' --glob '!**/node_modules/**' --glob '!**/dist/**' --glob '!**/.repos/**' --glob '!packages/foundation/modeling/schema/**'
node explorations/effect-schema-parity/research/retirement-G-Z-counts.cjs
```

The counter uses the displayed `rg` inventory/candidate commands, then TypeScript syntax to exclude documentation strings, map root aliases through barrels, and attribute namespace properties/qualified types. Counts are **unique direct importing source files**, including type-only and concept-subpath dynamic imports; package-internal files, re-export-only files and transitive consumers are excluded. A file can count toward multiple concepts. Exact file witnesses and root-symbol mappings are in `retirement-G-Z-counts-proof.json`; dynamic root/computed namespace access is reported by the counter (none in this snapshot). Wildcard namespace passing/alias reassignment and nonliteral runtime module paths remain **UNVERIFIED**, so these counts are not a transitive migration blast radius.

Targeted upstream searches cover the whole public source tree, including Schema/SchemaAST/SchemaGetter/SchemaRepresentation/JsonSchema, unstable schema/arbitrary/http/encoding/net and core value modules. `retirement-G-Z-searches.json` stores each concept's regex and matching **locations only**, not copied upstream bodies. Search command template:

```sh
rg -n -i "$concept_pattern" .repos/effect/packages/effect/src --glob '*.ts' --glob '!**/internal/**' --glob '!**/HttpApiSwagger.ts' --glob '!**/HttpApiScalar.ts' --max-columns 200
```

`NONE` is the result of targeted semantic/name searches and inspection of adjacent APIs, not a mathematical proof of absence. Adjacent/non-covering symbols are explicitly labeled.

## Lead commits checked

```sh
for sha in 46d83101e8 a63dcbf04e 0a08ae0626; do git -C .repos/effect show --stat "$sha"; done
```

| Commit | Relevant result from stat plus current source |
| --- | --- |
| `46d83101e8` | Moves Cookies/Headers/UrlParams schemas into `Schema.ts`; `Schema.Headers` at `packages/effect/src/Schema.ts:13113` normalizes records. It does not supply the secure-header policy suite or move HttpMethod/HttpStatus. |
| `a63dcbf04e` | Adds native arbitrary implementation and `unstable/arbitrary/Arbitrary.ts`; schema derivation is `packages/effect/src/unstable/arbitrary/Arbitrary.ts:346`. Native Graph already provides `toCodecArbitrary` at `packages/effect/src/Schema.ts:12593`; retaining a hand-built graph solely for generation is unnecessary. |
| `0a08ae0626` | Adds NetAddress/IpInterface/IpNetwork plus Schema codecs. NetAddress private port check at `packages/effect/src/unstable/net/NetAddress.ts:908` accepts zero; host safety policy is not provided by the address classifiers. |

## Summary counts

Count verification command (after generation):

```sh
sed -n '/^## Concept inventory/,/^## Recipes/p' explorations/effect-schema-parity/research/retirement-G-Z.md | rg '^\| [A-Za-z][A-Za-z0-9]* \|' | rg '\| (RETIRE|ADAPT|KEEP|UNSURE) \|' | awk -F'[|]' '{for(i=1;i<=NF;i++) {gsub(/^ +| +$/,"",$i); if($i ~ /^(RETIRE|ADAPT|KEEP|UNSURE)$/) n[$i]++}} END {for(k in n) print k,n[k]}'
```

| Disposition | Concepts |
| --- | ---: |
| RETIRE | 36 |
| ADAPT | 1 |
| KEEP | 33 |
| UNSURE | 6 |
| Total | 76 |

## Concept inventory

| Concept | What (repo source) | Upstream coverage (relative to `.repos/effect/`) | Concrete beep delta | Consumers | Disposition | Reason |
| --- | --- | --- | --- | ---: | --- | --- |
| Glob | Portable Bun-accepted glob-pattern brand (`packages/foundation/modeling/schema/src/Glob/Glob.schema.ts:2`) | NONE; nearby FileSystem.glob is file discovery, packages/effect/src/FileSystem.ts:133 | Bun constructor acceptance; rejects backslashes, empty/oversized patterns; brand and diagnostics | 2 | KEEP | Pattern-validation policy is absent upstream; file discovery is a different intent. |
| Graph | Graph/edge schemas, wire codecs and mutable variants (`packages/foundation/modeling/schema/src/Graph/Graph.transforms.ts:181`) | packages/effect/src/Schema.ts:12536; packages/effect/src/Schema.ts:12593 | Separate edge/index schemas, mutable codecs, formatter/equivalence, reconstruction and encoded snapshots | 0 | RETIRE | Upstream Graph plus JSON codec owns graph validation/serialization; migrate mutable callers to immutable snapshots. |
| HashSet | Array-backed immutable HashSet codec (`packages/foundation/modeling/schema/src/HashSet.ts:2`) | packages/effect/src/Schema.ts:12739; packages/effect/src/Schema.ts:14524 | Explicit array transform, .value/from/to metadata and beep identifier | 4 | RETIRE | Native HashSet codec derivation already supplies array serialization and arbitrary support. |
| Html | Nominal trusted HTML fragment string (`packages/foundation/modeling/schema/src/Html.ts:2`) | NONE; packages/effect/src/unstable/http/Template.ts:116 is rendering, not a trust brand | HtmlFragment brand only; explicitly no sanitizer | 1 | KEEP | No upstream trusted-fragment value/schema was found; text rendering is different intent. |
| Http | Shared header option/rendering support (`packages/foundation/modeling/schema/src/Http/Http.headers.shared.ts:2`) | packages/effect/src/Schema.ts:13113 (adjacent only; NONE for secure-header option semantics) | ResponseHeader class, URI encoding, max-age check, option renderer and forbidden-encode issue helpers | 0 | KEEP | Header records do not replace secure option-to-header policy; this directory is internal support, not a canonical facade. |
| HttpHeaders | Facade of secure HTTP header concepts (`packages/foundation/modeling/schema/src/HttpHeaders/HttpHeaders.schema.ts:2`) | packages/effect/src/Schema.ts:13113 (adjacent only; NONE for policy suite) | Namespace exports for policy-specific codecs and error/options classes | 0 | KEEP | Schema.Headers normalizes record names; this facade models secure-header policies. |
| HttpMethod | HTTP method literal kit with body classifications (`packages/foundation/modeling/schema/src/HttpMethod/HttpMethod.schema.ts:2`) | packages/effect/src/unstable/http/HttpMethod.ts:18; packages/effect/src/unstable/http/HttpMethod.ts:57; packages/effect/src/unstable/http/HttpMethod.ts:71; packages/effect/src/unstable/http/HttpMethod.ts:121 | LiteralKit statics, schema NoBody/WithBody, HashSet instead of ReadonlySet | 2 | UNSURE | Same-intent coverage exists only in Role B; exclusion conflicts with retirement rule. Align must choose, not preserve an alias. |
| HttpProtocol | HTTP versus HTTPS literal domain (`packages/foundation/modeling/schema/src/HttpProtocol/HttpProtocol.schema.ts:2`) | NONE (no dedicated HTTP-only protocol schema/value domain found) | LiteralKit for http/https with Schema alias and identifier | 0 | KEEP | General URL support admits other schemes; it does not own this closed protocol domain. |
| HttpStatus | Status codes, named mappings and category families (`packages/foundation/modeling/schema/src/HttpStatus/HttpStatus.schema.ts:2`) | packages/effect/src/unstable/http/HttpStatus.ts:100; packages/effect/src/Schema.ts:7458 (bounds only) | Three-digit HttpStatusCode, named/category codecs, unofficial status families, literal statics | 11 | UNSURE | Role B named-code value module is same intent; its use as an adoption target needs align resolution. |
| Int | Branded integer and sign/serial-range schemas (`packages/foundation/modeling/schema/src/Int.ts:2`) | packages/effect/src/Schema.ts:7581; packages/effect/src/Schema.ts:7335; packages/effect/src/Schema.ts:7458 | Nominal Int/sign brands, finite check, PostgreSQL serial bounds, PosInt.decodeEffect | 143 | RETIRE | Integer/refinement intent is native; bounds and brands do not justify a parallel primitive family. |
| Int64 | Signed 64-bit bigint and decimal-string codec (`packages/foundation/modeling/schema/src/Int64.ts:2`) | packages/effect/src/Schema.ts:7984; packages/effect/src/Schema.ts:9664 | Signed bounds, brand, named check and diagnostics | 1 | RETIRE | Native bigint string codec plus range check covers fixed-width numeric validation. |
| JSONSchema | Draft-2020-12 node/document model and local references (`packages/foundation/modeling/schema/src/JSONSchema/JSONSchema.schema.ts:2`) | packages/effect/src/JsonSchema.ts:30; packages/effect/src/JsonSchema.ts:129; packages/effect/src/JsonSchema.ts:319; packages/effect/src/JsonSchema.ts:684; packages/effect/src/SchemaRepresentation.ts:2880 | Recursive Class, Option keywords, boolean subschemas, lossless extension bag and strict keyword validation/local resolvers | 0 | RETIRE | Document modeling/import intent is upstream; losing strict Node runtime validation is an accepted but explicit migration cost. |
| Json | JSON object/array schemas and text adapters (`packages/foundation/modeling/schema/src/Json.ts:2`) | packages/effect/src/Schema.ts:14913; packages/effect/src/Schema.ts:14887; packages/effect/src/Schema.ts:9209 | Annotations and pre-bound decode/encode functions | 17 | RETIRE | Native JsonObject, Array(Json) and UnknownFromJsonString cover the complete intent. |
| Jsonc | Comment/trailing-comma JSON parser codec (`packages/foundation/modeling/schema/src/Jsonc.ts:2`) | NONE | jsonc-parser diagnostics class; parse-only transform; typed target-schema decoder | 16 | KEEP | Plain JSON parsing does not accept the JSONC dialect. |
| Jsonl | JSON Lines text parser and target decoder (`packages/foundation/modeling/schema/src/Jsonl.ts:2`) | packages/effect/src/unstable/encoding/Ndjson.ts:174; packages/effect/src/unstable/encoding/Ndjson.ts:259 | Whole-string/array interface, per-line parse handling and unsupported encode | 0 | UNSURE | NDJSON is same intent but Role B; stream/channel migration would also change the call shape. |
| KebabStr | Branded kebab-case text boundary (`packages/foundation/modeling/schema/src/KebabStr.ts:2`) | packages/effect/src/String.ts:1408; packages/effect/src/Schema.ts:5387 | Strict lowercase-leading regex, trimming and brand rather than normalization | 1 | RETIRE | Core kebabCase owns case conversion intent; adopt normalization and record changed acceptance. |
| LiteralKit | Literal union with guards, maps and match helpers (`packages/foundation/modeling/schema/src/LiteralKit/LiteralKit.schema.ts:2`) | packages/effect/src/Schema.ts:4768; packages/effect/src/Schema.ts:4800; packages/effect/src/Schema.ts:6276 | Enum/custom-key mapping, Options, HashSet, per-member is/thunk, pick/omit, $match, tagged-union helpers, collision/coverage errors | 536 | RETIRE | Literals owns finite literal domains; helper ergonomics do not justify retaining the concept. |
| LocalDate | Calendar-only year/month/day value and arithmetic (`packages/foundation/modeling/schema/src/LocalDate/LocalDate.schema.ts:2`) | NONE for calendar-only value; adjacent packages/effect/src/DateTime.ts:2673 | Calendar-only Class, ISO date codec, equality/hash, date arithmetic, leap/month helpers; year bounds | 5 | KEEP | DateTime models instants/zones, not a date-only value. Arithmetic delegation is possible without declaring intent parity. |
| Logs | Effect log levels and severity literal kits (`packages/foundation/modeling/schema/src/Logs.ts:2`) | packages/effect/src/LogLevel.ts:67; packages/effect/src/LogLevel.ts:87; packages/effect/src/LogLevel.ts:114 | Schema and LiteralKit statics on the upstream string domains | 6 | RETIRE | Canonical core LogLevel owns these exact domains; compose S.Literals at boundaries. |
| MappedLiteralKit | Reversible literal-pair schema toolkit (`packages/foundation/modeling/schema/src/MappedLiteralKit/MappedLiteralKit.schema.ts:2`) | packages/effect/src/Schema.ts:4780; packages/effect/src/Schema.ts:4813 | From/To kits, reverse Enum tables, helper statics, duplicate-source/target rejection | 5 | RETIRE | Literals.transform directly owns reversible literal mapping; migrate convenience accessors. |
| Markdown | Validated Markdown and HTML rendering codecs (`packages/foundation/modeling/schema/src/Markdown.ts:2`) | NONE | Bun/micromark GFM fallback, brand, render options, parse/target helpers, unsupported reverse rendering | 4 | KEEP | No upstream Markdown parser/renderer schema found; documentation Markdown is not coverage. |
| MimeType | Closed MIME registry with category schemas (`packages/foundation/modeling/schema/src/MimeType.ts:2`) | packages/effect/src/unstable/http/Mime.ts:38; packages/effect/src/unstable/http/Mime.ts:58; packages/effect/src/unstable/http/Mime.ts:71 (lookup only) | Data-derived LiteralKits, category .kinds and extracted category members | 4 | UNSURE | Role B MIME lookup overlaps intent but lacks the closed category schema; align must settle target eligibility and domain loss. |
| MutableHashMap | Mutable map validation and entry-array codec (`packages/foundation/modeling/schema/src/MutableHashMap.ts:2`) | packages/effect/src/MutableHashMap.ts:89; packages/effect/src/MutableHashMap.ts:220; packages/effect/src/Schema.ts:4235 | Element-validating declaration, delegated value guard, equality/formatter, codec and generation annotations | 1 | RETIRE | Core mutable map owns the value; decode entry arrays with Schema then construct upstream values directly. |
| MutableHashSet | Mutable set validation and array codec (`packages/foundation/modeling/schema/src/MutableHashSet.ts:2`) | packages/effect/src/MutableHashSet.ts:90; packages/effect/src/MutableHashSet.ts:185; packages/effect/src/Schema.ts:4427 | Element-validating declaration, delegated value guard, equality/formatter, codec and generation annotations | 0 | RETIRE | Core mutable set plus boundary Array validation supplies same intent without retaining a schema concept. |
| NoOpen | X-Download-Options option-to-header codec (`packages/foundation/modeling/schema/src/NoOpen/NoOpen.schema.ts:2`) | NONE | noopen value, false/undefined options, response Class, default rendering and typed errors | 0 | KEEP | No upstream download-option policy renderer found. |
| NoSniff | X-Content-Type-Options option-to-header codec (`packages/foundation/modeling/schema/src/NoSniff/NoSniff.schema.ts:2`) | NONE | nosniff literal, option/default handling, response Class and typed failures | 0 | KEEP | No upstream content-sniffing policy schema/renderer found. |
| Number | Numeric checks and nonnegative/finite codecs (`packages/foundation/modeling/schema/src/Number.ts:2`) | packages/effect/src/Schema.ts:7335; packages/effect/src/Schema.ts:7365; packages/effect/src/Schema.ts:7395; packages/effect/src/Schema.ts:7425; packages/effect/src/Schema.ts:9624; packages/effect/src/Schema.ts:7603 | Reusable check instances, finite/integer composition, brands, serial check, decoder statics | 361 | RETIRE | Native numeric predicates/codecs cover intent; replace helper names with upstream calls. |
| Opaque | Defect/unknown schemas excluded from equality (`packages/foundation/modeling/schema/src/Opaque.ts:2`) | packages/effect/src/Schema.ts:8732; packages/effect/src/Schema.ts:2946; packages/effect/src/Schema.ts:14305 | Always-true toEquivalence on both Defect and Unknown plus identifiers | 103 | RETIRE | Unknown/Defect intent exists; move any identity-exclusion annotation to the owning field. |
| Options | Optional-nullish-key to Option wrapper (`packages/foundation/modeling/schema/src/Options.ts:2`) | packages/effect/src/Schema.ts:13389 | Renamed overloaded facade around OptionFromOptionalNullOr; forwards onNoneEncoding | 1 | RETIRE | Direct native equivalent is already underneath. |
| ParserOptions | CSV parser option classes and callback schemas (`packages/foundation/modeling/schema/src/ParserOptions/ParserOptions.schema.ts:2`) | NONE | CSV header transforms, derived option getters, validation and error class | 2 | KEEP | SchemaAST.ParseOptions configures schema parsing, not CSV parsing. |
| PascalStr | Branded PascalCase text boundary (`packages/foundation/modeling/schema/src/PascalStr.ts:2`) | packages/effect/src/String.ts:1315; packages/effect/src/Schema.ts:5387 | Trim/nonempty, strict Pascal pattern and nominal brand | 0 | RETIRE | Adopt native Pascal normalization and migrate callers that relied on rejection. |
| Percentage | Bounded percentage value with arithmetic helpers (`packages/foundation/modeling/schema/src/Percentage.ts:2`) | NONE for percentage domain; packages/effect/src/Schema.ts:7458 is only a generic bound check | Finite [0,100] brand, named constants, complement, decimal conversion and formatting | 2 | KEEP | No upstream percentage semantic value/module found; generic checks are implementation substrate. |
| PermissionsPolicy | Permissions-Policy directive and header codecs (`packages/foundation/modeling/schema/src/PermissionsPolicy/PermissionsPolicy.schema.ts:2`) | NONE | Directive grammar, allowlisted/quoted origins, option and response classes, header formatting/errors | 0 | KEEP | Upstream header records do not implement permissions policy grammar. |
| PermittedCrossDomainPolicies | X-Permitted-Cross-Domain-Policies codec (`packages/foundation/modeling/schema/src/PermittedCrossDomainPolicies/PermittedCrossDomainPolicies.schema.ts:2`) | NONE | Literal values, false/undefined defaults, response Class and encode failures | 0 | KEEP | No upstream cross-domain-policy codec found. |
| Port | Branded transport port and decimal-string codec (`packages/foundation/modeling/schema/src/Port.ts:2`) | packages/effect/src/unstable/net/NetAddress.ts:908 (private check); packages/effect/src/unstable/net/NetAddress.ts:916; packages/effect/src/Schema.ts:7458 | Public standalone 1..65535 domain, decimal-only string parse, brand and decodeEffect | 0 | KEEP | No public standalone port API: upstream check is private and accepts zero. InetAddress is a different value and Role B. |
| PosixPath | Slash-only path brand and separator normalization (`packages/foundation/modeling/schema/src/PosixPath.ts:2`) | NONE for separator-only normalization; packages/effect/src/Path.ts:94; packages/effect/src/Path.ts:867 are adjacent | Rejects backslashes; replacement-only codec and sync normalizePath; brand/static decoder | 67 | KEEP | Path.normalize resolves dot segments and does not promise Windows-backslash conversion on POSIX; different intent. |
| Primitive | Union of ordinary non-symbol primitive values (`packages/foundation/modeling/schema/src/Primitive.ts:2`) | packages/effect/src/Schema.ts:4756; packages/effect/src/Schema.ts:7097 | Explicit String/Finite/Boolean/BigInt/Null/Undefined membership and annotation | 0 | RETIRE | Native primitive schemas and Union express this boundary directly; no independent behavior remains. |
| PromiseSchema | Native-Promise-like runtime schema (`packages/foundation/modeling/schema/src/PromiseSchema.ts:2`) | packages/effect/src/Predicate.ts:1326; packages/effect/src/Schema.ts:562 | Requires finally and Promise object tag; reference equivalence, formatter and generation annotation | 0 | RETIRE | Use upstream promise predicate/declaration, accepting its broader then/catch test. |
| Record | Unknown string-keyed record schema (`packages/foundation/modeling/schema/src/Record/Record.schema.ts:2`) | packages/effect/src/Schema.ts:3815 | Identifier/description on Record(String, Unknown) | 32 | RETIRE | Exact native composition; remove alias. |
| ReferrerPolicy | Referrer-Policy value/fallback/header schemas (`packages/foundation/modeling/schema/src/ReferrerPolicy/ReferrerPolicy.schema.ts:2`) | NONE | Closed tokens, fallback lists, false/undefined defaults, response Class, renderer/error | 0 | KEEP | No native referrer-policy domain or rendering codec found. |
| RegExp | Valid regex text and one-way RegExp codec (`packages/foundation/modeling/schema/src/RegExp.ts:2`) | packages/effect/src/Schema.ts:8918; packages/effect/src/Schema.ts:14524 | Pattern string brand, constructor-check diagnostic, unsupported encode | 0 | RETIRE | Native RegExp JSON codec validates source/flags and supports both directions; consumers accept object wire shape. |
| SafeObject | Nominal string-keyed record and object normalization (`packages/foundation/modeling/schema/src/SafeObject/SafeObject.schema.ts:2`) | packages/effect/src/Schema.ts:3815; packages/effect/src/Schema.ts:3110; packages/effect/src/Schema.ts:5387 | SafeObject brand; shallow spread of object-keyword values catches accessor throws | 0 | RETIRE | Plain record validation covers intent; explicitly adapt normalization-only inputs at their boundary. |
| SafeRemoteHost | SSRF policy and optional DNS-resolver guard (`packages/foundation/modeling/schema/src/SafeRemoteHost.ts:2`) | NONE for policy; packages/effect/src/unstable/net/NetAddress.ts:574; packages/effect/src/unstable/net/NetAddress.ts:755; packages/effect/src/unstable/net/NetAddress.ts:789; packages/effect/src/unstable/net/NetAddress.ts:804; packages/effect/src/unstable/net/NetAddress.ts:816; packages/effect/src/unstable/net/NetAddress.ts:852 are partial helpers | Host allowlist, metadata names, fail-closed URL guard, injected resolution, any-private-address rejection, BlockedHostError | 2 | KEEP | Address parsing/classification cannot replace host/DNS/allowlist security policy; Role B helpers are follow-up evidence only. |
| SchemaUtils | Codec/default/statics and schema traversal utilities (`packages/foundation/modeling/schema/src/SchemaUtils/collectAnnotationsAt.ts:149`) | packages/effect/src/Schema.ts:5651; packages/effect/src/Schema.ts:5769; packages/effect/src/Schema.ts:14305; packages/effect/src/SchemaAST.ts:4994 | Dual encoder order, statics installation/rebuild preservation, defaults, pluck/split and recursive annotation collection | 483 | ADAPT | Remove redundant codec/default/statics facades; retain only distinct recursive annotation collection after leaf-by-leaf pruning. |
| SecureHeader | Closed set of secure-header concept names (`packages/foundation/modeling/schema/src/SecureHeader/SecureHeader.schema.ts:2`) | NONE | LiteralKit domain with identity annotations | 0 | KEEP | No upstream closed secure-header concept registry found. |
| SecureHeaderError | Discriminated errors for secure-header construction (`packages/foundation/modeling/schema/src/SecureHeaderError/SecureHeaderError.errors.ts:2`) | NONE; packages/effect/src/Schema.ts:14201 is only the TaggedError substrate | Per-policy TaggedErrors plus discriminated aggregate; policy-specific payload semantics | 0 | KEEP | Generic TaggedError does not own secure-header error taxonomy. |
| SecureHeaderOptions | Aggregate secure-header configuration and generation (`packages/foundation/modeling/schema/src/SecureHeaderOptions/SecureHeaderOptions.schema.ts:2`) | NONE | Class fields/defaults, entry Class, createHeadersObject and createSecureHeaders Effects | 0 | KEEP | No upstream equivalent secure-header configuration/generator found. |
| SemanticVersion | Strict MAJOR.MINOR.PATCH string schema (`packages/foundation/modeling/schema/src/SemanticVersion.ts:2`) | NONE | Template-literal segments reject leading zeros; decodeUnknownOption static | 10 | KEEP | No semantic-version validation API found; generic TemplateLiteral is substrate. |
| Semver | Semantic version Class, parser and comparison (`packages/foundation/modeling/schema/src/Semver.ts:2`) | NONE | Major/minor/patch, prerelease/build handling, semantic comparison and string codec | 0 | KEEP | No upstream semver value/parser/comparison API found. |
| SeverityLevel | Low/medium/high/critical severity domain (`packages/foundation/modeling/schema/src/SeverityLevel.ts:2`) | NONE | LiteralKit statics and annotated closed vocabulary | 0 | KEEP | Effect log severity is a different vocabulary and purpose. |
| Sex | Male/female literal schema (`packages/foundation/modeling/schema/src/Sex/Sex.schema.ts:2`) | NONE | LiteralKit vocabulary and namespace Schema alias | 0 | KEEP | No upstream sex domain found; policy vocabulary is repo-owned. |
| Sfixed32 | Signed fixed-width protobuf number schema (`packages/foundation/modeling/schema/src/Sfixed32.ts:2`) | packages/effect/src/Schema.ts:7623 | Signed range/integer filter group, finite check, protobuf brand and diagnostic annotations | 0 | RETIRE | No protobuf wire encoder exists here; numeric acceptance is native isInt32. |
| Sfixed64 | Signed fixed-width protobuf bigint codec (`packages/foundation/modeling/schema/src/Sfixed64.ts:2`) | packages/effect/src/Schema.ts:7984; packages/effect/src/Schema.ts:9664 | Bigint/number/string input adapter, exact signed bounds, protobuf brand | 0 | RETIRE | Numeric-width intent is a bounded BigInt codec; switch accepted inputs to decimal strings. |
| Sha256 | Lowercase SHA-256 digest and byte codecs (`packages/foundation/modeling/schema/src/Sha256.ts:2`) | packages/effect/src/Schema.ts:10045 (hex substrate only; NONE for digest domain) | Digest-specific length/lowercase brand; byte and hex-byte inputs are hashed through Crypto.Crypto.digest("SHA-256") (packages/foundation/modeling/schema/src/Sha256.ts:30), not merely hex-encoded; reverse encoding forbidden | 146 | KEEP | Generic hex conversion does not own fixed SHA-256 digest semantics; hashing is a different operation. |
| Sint32 | Signed protobuf zig-zag integer value schema (`packages/foundation/modeling/schema/src/Sint32.ts:2`) | packages/effect/src/Schema.ts:7623 | Signed range/integer checks and nominal protobuf brand; no zig-zag encoder | 0 | RETIRE | Runtime behavior is signed 32-bit validation already provided upstream. |
| Sint64 | Signed protobuf zig-zag bigint value codec (`packages/foundation/modeling/schema/src/Sint64.ts:2`) | packages/effect/src/Schema.ts:7984; packages/effect/src/Schema.ts:9664 | Multi-form exact bigint input, signed bounds and protobuf brand; no zig-zag wire encoding | 0 | RETIRE | Adopt native bigint bounds/string decoding; nominal protocol label is not extra runtime intent. |
| Slug | Canonical lowercase single-path-segment slug (`packages/foundation/modeling/schema/src/Slug.ts:2`) | NONE | Lowercase grammar/brand, no leading/trailing/repeated hyphens; SlugFromStr is only NonEmptyString | 1 | KEEP | No canonical safe URL-segment slug schema found. |
| SnakeStr | Branded snake_case text boundary (`packages/foundation/modeling/schema/src/SnakeStr.ts:2`) | packages/effect/src/String.ts:1427; packages/effect/src/Schema.ts:5387 | Trim/nonempty, strict snake pattern and brand | 0 | RETIRE | Native snakeCase covers case normalization; accept changed rejection behavior. |
| StatusCauseError | Status/cause field bundle and curried error builders (`packages/foundation/modeling/schema/src/StatusCauseError.ts:2`) | packages/effect/src/Schema.ts:14201; packages/effect/src/Schema.ts:8732; packages/effect/src/Schema.ts:13330 | Status/message fields, Option normalization, input Classes, multiple constructor/curry forms | 1 | RETIRE | Native TaggedError field declarations and constructors cover error modeling; inline payload defaults at consumers. |
| String | Normalized/nullable/UUID/stringification schemas (`packages/foundation/modeling/schema/src/String.ts:2`) | packages/effect/src/Schema.ts:9706; packages/effect/src/Schema.ts:6696; packages/effect/src/Schema.ts:12898; packages/effect/src/Inspectable.ts:187 | Brands, statics, Option constructor default, Error.message and hostile-object stringify fallbacks | 21 | RETIRE | Upstream string/Option normalization and inspection cover intent; document stringify behavior changes. |
| TerritoryCode | CLDR territory code/name mappings (`packages/foundation/modeling/schema/src/TerritoryCode.ts:2`) | NONE | Dataset-backed literal domains and reversible code/name codec | 0 | KEEP | No upstream CLDR territory vocabulary found. |
| TerritoryName | CLDR display-name schema alias (`packages/foundation/modeling/schema/src/TerritoryName.ts:2`) | NONE | Alias of TerritoryCode module name schema | 0 | KEEP | No upstream territory-name intent; internal duplication is a separate cleanup, not parity retirement. |
| Thunk | Branded function/thunk schemas and guard constructor (`packages/foundation/modeling/schema/src/Thunk.ts:2`) | packages/effect/src/Predicate.ts:753; packages/effect/src/Schema.ts:562 | Brand/TypeId, generic helper and is static; supplied returnSchema is not evaluated | 0 | RETIRE | Actual runtime intent is function membership; upstream predicate/declaration covers it. |
| Timestamp | UTC timestamp Class, ISO/epoch schemas and arithmetic (`packages/foundation/modeling/schema/src/Timestamp/Timestamp.schema.ts:2`) | packages/effect/src/Schema.ts:11012; packages/effect/src/Schema.ts:11047; packages/effect/src/DateTime.ts:1617; packages/effect/src/DateTime.ts:2308; packages/effect/src/DateTime.ts:2647 | epochMillis Class, ISO/epoch brands, nonnegative epoch, overloaded arithmetic/comparison and EPOCH | 16 | RETIRE | Native DateTimeUtc codecs and core value module own timestamps/arithmetic. |
| Timezone | Dataset-backed IANA timezone literal kit (`packages/foundation/modeling/schema/src/Timezone.ts:2`) | packages/effect/src/Schema.ts:11166 | Static data vocabulary, literal union, annotations and kit helpers | 0 | RETIRE | Native named time-zone validation owns the domain; migrate to TimeZone.Named values. |
| Toml | TOML parse-only schema transform (`packages/foundation/modeling/schema/src/Toml.ts:2`) | packages/effect/src/unstable/encoding/Toml.ts:506 | Bun.TOML.parse lookup only (no fallback), SchemaIssue adaptation, target decoder and unsupported encode | 2 | UNSURE | Same-intent upstream parser is Role B; adoption needs align decision. |
| Transformations | Lossy transform with passthrough encoding (`packages/foundation/modeling/schema/src/Transformations.ts:2`) | packages/effect/src/Schema.ts:5387; packages/effect/src/SchemaGetter.ts:612; packages/effect/src/SchemaGetter.ts:572 | Input decoder invocation, thrown-transform issue conversion, output typed with Unknown AST, passthrough encode | 0 | RETIRE | Native decodeTo/getters cover one-way transformation; require an honest output schema at each consumer. |
| URL | URL text brands and HTTPS-specific validator (`packages/foundation/modeling/schema/src/URL.ts:2`) | packages/effect/src/Schema.ts:9021; packages/effect/src/Schema.ts:8979 | URLStr remains string, HttpsUrl rejects non-HTTPS, brands and codec statics | 38 | RETIRE | Native URL codec owns URL validity; keep HTTPS policy on the consumer field without a beep concept alias. |
| Uint32 | Unsigned protobuf 32-bit number schema (`packages/foundation/modeling/schema/src/Uint32.ts:2`) | packages/effect/src/Schema.ts:7654 | Finite/integer/range group, brand and diagnostics | 0 | RETIRE | Native isUint32 covers numerical acceptance; no protobuf serializer exists here. |
| Uint64 | Unsigned protobuf 64-bit bigint codec (`packages/foundation/modeling/schema/src/Uint64.ts:2`) | packages/effect/src/Schema.ts:7984; packages/effect/src/Schema.ts:9664 | Bigint/number/string input normalization, unsigned bounds and brand | 0 | RETIRE | Use native bigint-from-string plus unsigned range; migrate input shapes. |
| UnitInterval | Finite [0,1] probability/ratio value (`packages/foundation/modeling/schema/src/UnitInterval.ts:2`) | NONE for ratio domain; packages/effect/src/Schema.ts:7458 is generic substrate | UnitInterval brand, ZERO/ONE, predicate and complement | 28 | KEEP | No upstream ratio/probability semantic value found. |
| Unknown | Unknown value and JSON-string boundary (`packages/foundation/modeling/schema/src/Unknown.ts:2`) | packages/effect/src/Schema.ts:2946; packages/effect/src/Schema.ts:9209 | Bound decoder/encoder statics and identifiers; plain Unknown has no implicit serialization | 128 | RETIRE | Native Unknown and UnknownFromJsonString supply intent directly. |
| Version | Package version constant (`packages/foundation/modeling/schema/src/Version.ts:2`) | NONE | Beep package VERSION = 0.0.0 | 0 | KEEP | Package metadata is repo-specific, not an upstream schema concept. |
| Xml | XML parser-to-unknown schema and target decoder (`packages/foundation/modeling/schema/src/Xml.ts:2`) | NONE for parsing; packages/effect/src/Schema.ts:14676 only encodes XML | Bun/parser fallback, parse issues, target decoder and rejected reverse encode | 2 | KEEP | Upstream XML encoder is opposite direction; it cannot replace this parser. |
| XssProtection | X-XSS-Protection option/header codec (`packages/foundation/modeling/schema/src/XssProtection/XssProtection.schema.ts:2`) | NONE | Mode/report schemas, report URI, defaults, response Class and errors | 0 | KEEP | No upstream XSS-protection policy codec found. |
| Yaml | YAML parse-only schema transform (`packages/foundation/modeling/schema/src/Yaml.ts:2`) | packages/effect/src/unstable/encoding/Yaml.ts:539 | Bun/dynamic parser fallback, parseYaml export, target decoder and unsupported encode | 4 | UNSURE | Same-intent upstream parser is Role B; adoption requires align resolution. |

## Recipes

Every RETIRE row removes its module/root exports/subpath in the same change that migrates all importers; no compatibility re-export remains. Recipes show target shapes, not tested drop-in replacements. `Input`, `Output`, `Item`, `K`, `V`, `N`, `E`, `raw` and `text` stand for consumer schemas/values. `S` is `effect/Schema` when a before-example already uses it. Each risk cell contains the two principal migration risks.

Recipe coverage command: `rg '^\| [A-Za-z][A-Za-z0-9]* \| `import' explorations/effect-schema-parity/research/retirement-G-Z.md | wc -l` → 37.

| Concept | Before import and call | After import and call | Top risks |
| --- | --- | --- | --- |
| Graph | `import * as G from "@beep/schema/Graph"; G.DirectedGraph({ node: N, edge: E })` | `import * as S from "effect/Schema"; S.toCodecJson(S.Graph("directed", N, E)); use S.Graph(...) for from-self boundaries` | Wire shape changes from beep snapshots; mutable values must pass through effect/Graph.endMutation (packages/effect/src/Graph.ts:730) before encoding. |
| HashSet | `import { HashSet } from "@beep/schema/HashSet"; HashSet(Item)` | `import * as S from "effect/Schema"; S.toCodecJson(S.HashSet(Item))` | Derived JSON codec lowers nested item encodings; .value/from/to helper metadata disappears. |
| Int | `import { Int, PosInt, PostgresSerialInt } from "@beep/schema"; PosInt.decodeEffect(n)` | `import * as S from "effect/Schema"; S.decodeEffect(S.Int.check(S.isGreaterThan(0)))(n); use S.Int or S.Natural as appropriate; serial adds S.isBetween({minimum:1,maximum:2147483647})` | Brands vanish from signatures; ensure each sign/serial constraint survives instead of replacing every variant with bare Int. |
| Int64 | `import { Int64FromString } from "@beep/schema"; S.decodeSync(Int64FromString)(text)` | `import * as S from "effect/Schema"; S.decodeSync(S.BigIntFromString.pipe(S.decodeTo(S.BigInt.check(S.isBetweenBigInt({minimum:-9223372036854775808n,maximum:9223372036854775807n})))))(text)` | Nominal identity changes; encoded bigint must remain string at JSON boundaries. |
| JSONSchema | `import { JSONSchema as J } from "@beep/schema"; S.decodeUnknownSync(J.NodeCodec)(raw)` | `import * as J from "effect/JsonSchema"; import * as S from "effect/Schema"; J.fromSchemaDraft2020_12(S.decodeUnknownSync(S.Record(S.String,S.Unknown))(raw))` | Open record does not validate keyword semantics or produce Node/Option fields; migrate local reference/extension handling explicitly and test hostile keys before deleting the strict codec. |
| Json | `import { JsonObject, JsonArray, decodeJsonString } from "@beep/schema"; decodeJsonString(text)` | `import * as S from "effect/Schema"; S.decodeUnknownEffect(S.UnknownFromJsonString)(text); use S.JsonObject and S.Array(S.Json); encode with S.encodeUnknownEffect(S.UnknownFromJsonString)` | Named bound functions disappear; Unknown JSON parsing is not a target-domain validation step. |
| KebabStr | `import { KebabCaseStr } from "@beep/schema"; S.decodeSync(KebabCaseStr)(text)` | `import * as Str from "effect/String"; Str.kebabCase(text); at a schema boundary use S.String.pipe(S.decodeTo(S.String, {decode: G.transform(Str.kebabCase), encode: G.passthrough()})) with S from "effect/Schema" and G from "effect/SchemaGetter"` | Invalid case input now normalizes instead of failing; trim/nonempty/brand guarantees must be reconsidered at each consumer. |
| LiteralKit | `import { LiteralKit } from "@beep/schema"; const K = LiteralKit(["a","b"]); K.is.a(x); K.pickOptions(["a"])` | `import * as S from "effect/Schema"; const K = S.Literals(["a","b"]); S.is(S.Literal("a"))(x); K.pick(["a"]); use K.literals/members and effect/Match or S.TaggedUnion for helpers` | Custom Enum keys and mixed-literal collisions are no longer checked by beep; exhaustive match/omit/thunk/tagged-union call sites need rewrites, not textual import substitution. |
| Logs | `import { LogLevel } from "@beep/schema"; S.decodeSync(LogLevel)(level)` | `import * as L from "effect/LogLevel"; import * as S from "effect/Schema"; S.decodeSync(S.Literals(L.values))(level); severity uses the matching subset` | LiteralKit statics disappear; filtering values may widen inferred types unless the Severity domain is kept explicit. |
| MappedLiteralKit | `import { MappedLiteralKit } from "@beep/schema"; const K = MappedLiteralKit([["OK",200],["ERR",500]])` | `import * as S from "effect/Schema"; const K = S.Literals(["OK","ERR"]).transform([200,500]); S.decodeSync(K)("OK"); S.encodeSync(K)(200)` | Duplicate target mappings can make reverse decoding ambiguous; From/To/Enum/pick/match helper consumers must use the schema or explicit lookup tables. |
| MutableHashMap | `import { MutableHashMap } from "@beep/schema/MutableHashMap"; S.decodeSync(MutableHashMap({key:K,value:V}))(raw)` | `import * as M from "effect/MutableHashMap"; import * as S from "effect/Schema"; M.fromIterable(S.decodeSync(S.Array(S.Tuple([K,V])))(raw)); encode Array.from(value) through the same array schema` | Consumers storing the custom schema itself must move to array wire fields and construct at the boundary; custom structural equivalence/from-self payload validation must be replaced explicitly where used. |
| MutableHashSet | `import { MutableHashSet } from "@beep/schema/MutableHashSet"; S.decodeSync(MutableHashSet(Item))(raw)` | `import * as M from "effect/MutableHashSet"; import * as S from "effect/Schema"; M.fromIterable(S.decodeSync(S.Array(Item))(raw)); encode Array.from(value) through the same array schema` | Consumers storing the custom schema itself must move to array wire fields and construct at the boundary; custom structural equivalence/from-self payload validation must be replaced explicitly where used. |
| Number | `import { NonNegNum, NonNegativeInt, FiniteFromString } from "@beep/schema"; S.decodeSync(NonNegativeInt)(n)` | `import * as S from "effect/Schema"; S.decodeSync(S.Natural)(n); use S.Finite.check(S.isGreaterThanOrEqualTo(0)) and S.FiniteFromString; replace sign helper constants with native check calls` | NonNegNum and integer domains differ; brands, messages and bound static call syntax change. |
| Opaque | `import { Defect, OpaqueUnknown } from "@beep/schema"; Defect({includeStack:true})` | `import * as S from "effect/Schema"; S.Defect({includeStack:true}); S.Unknown; if identity exclusion is required, annotate the owning field with {toEquivalence:()=>()=>true}` | Default upstream equality is not beep always-true equality; defect serialization is lossy and must not be mistaken for original object identity. |
| Options | `import { OptionFromOptionalNullishKey } from "@beep/schema"; OptionFromOptionalNullishKey(Item)` | `import * as S from "effect/Schema"; S.OptionFromOptionalNullOr(Item); preserve any onNoneEncoding option` | None encoding choice must be forwarded; field omission, undefined and null must remain distinct where the application needs them. |
| PascalStr | `import { PascalCaseStr } from "@beep/schema"; S.decodeSync(PascalCaseStr)(text)` | `import * as Str from "effect/String"; Str.pascalCase(text); at a schema boundary use S.String.pipe(S.decodeTo(S.String, {decode: G.transform(Str.pascalCase), encode: G.passthrough()})) with S from "effect/Schema" and G from "effect/SchemaGetter"` | Invalid case input now normalizes instead of failing; trim/nonempty/brand guarantees must be reconsidered at each consumer. |
| Primitive | `import { Primitive } from "@beep/schema"; S.decodeSync(Primitive)(x)` | `import * as S from "effect/Schema"; S.decodeSync(S.Union([S.String,S.Finite,S.Boolean,S.BigInt,S.Null,S.Undefined]))(x)` | Do not accidentally admit Symbol or nonfinite numbers; serialization of BigInt/Undefined needs a separate wire codec. |
| PromiseSchema | `import { PromiseSchema } from "@beep/schema"; S.is(PromiseSchema)(x)` | `import * as S from "effect/Schema"; import * as P from "effect/Predicate"; S.is(S.declare(P.isPromise))(x)` | Upstream accepts then/catch objects without finally/Promise tag; custom formatter/equivalence metadata must move to a consumer declaration if needed. |
| Record | `import { UnknownRecord } from "@beep/schema"; S.decodeSync(UnknownRecord)(x)` | `import * as S from "effect/Schema"; S.decodeSync(S.Record(S.String,S.Unknown))(x)` | Identifier-based generated references change; this validates string-keyed record shape, not deep content safety. |
| RegExp | `import { RegExpFromStr } from "@beep/schema"; S.decodeSync(RegExpFromStr)(source)` | `import * as S from "effect/Schema"; S.decodeSync(S.toCodecJson(S.RegExp))({source,flags:""})` | Encoded shape becomes source/flags object; encode becomes supported and preserved flags differ from the former source-only path. |
| SafeObject | `import { SafeObject, SafeObjectFromObjectKeyword } from "@beep/schema"; S.decodeSync(SafeObject)(x)` | `import * as S from "effect/Schema"; S.decodeSync(S.Record(S.String,S.Unknown))(x); normalize object-keyword inputs at their owning boundary before validation` | Direct replacement may reject arrays/functions previously spread into records; getter exceptions/prototype behavior need boundary tests if normalization remains. |
| SchemaUtils | `import { SchemaUtils as U } from "@beep/schema"; U.withCodecStatics(["decodeSync"])(T).decodeSync(x); U.withKeyDefaults(T,v)` | `import * as S from "effect/Schema"; S.decodeSync(T)(x); T.pipe(S.withConstructorDefault(Effect.succeed(v)),S.withDecodingDefaultTypeKey(Effect.succeed(v))) with Effect from "effect"; retain import {collectAnnotationsAt} from "@beep/schema/SchemaUtils/collectAnnotationsAt" for recursive collection` | Defaults distinguish missing keys, undefined and encoded/type values; recursive collection traverses checks/context/encoding links and must not be replaced with a single SchemaAST.resolveAt call. |
| Sfixed32 | `import { Sfixed32 } from "@beep/schema"; S.decodeSync(Sfixed32)(n)` | `import * as S from "effect/Schema"; S.decodeSync(S.Number.check(S.isInt32()))(n)` | Protobuf nominal brands disappear; schema format/diagnostics may differ even with the same numeric bounds. |
| Sfixed64 | `import { Sfixed64 } from "@beep/schema"; S.decodeSync(Sfixed64)(input)` | `import * as S from "effect/Schema"; S.decodeSync(S.BigIntFromString.pipe(S.decodeTo(S.BigInt.check(S.isBetweenBigInt({minimum:-9223372036854775808n,maximum:9223372036854775807n})))))(decimalText)` | Number/bigint inputs must become explicit decimal text or take a separate native BigInt boundary; JSON representation and protocol-specific brands change. |
| Sint32 | `import { Sint32 } from "@beep/schema"; S.decodeSync(Sint32)(n)` | `import * as S from "effect/Schema"; S.decodeSync(S.Number.check(S.isInt32()))(n)` | Protobuf nominal brands disappear; schema format/diagnostics may differ even with the same numeric bounds. |
| Sint64 | `import { Sint64 } from "@beep/schema"; S.decodeSync(Sint64)(input)` | `import * as S from "effect/Schema"; S.decodeSync(S.BigIntFromString.pipe(S.decodeTo(S.BigInt.check(S.isBetweenBigInt({minimum:-9223372036854775808n,maximum:9223372036854775807n})))))(decimalText)` | Number/bigint inputs must become explicit decimal text or take a separate native BigInt boundary; JSON representation and protocol-specific brands change. |
| SnakeStr | `import { SnakeCaseStr } from "@beep/schema"; S.decodeSync(SnakeCaseStr)(text)` | `import * as Str from "effect/String"; Str.snakeCase(text); at a schema boundary use S.String.pipe(S.decodeTo(S.String, {decode: G.transform(Str.snakeCase), encode: G.passthrough()})) with S from "effect/Schema" and G from "effect/SchemaGetter"` | Invalid case input now normalizes instead of failing; trim/nonempty/brand guarantees must be reconsidered at each consumer. |
| StatusCauseError | `import { makeStatusCauseError, StatusCauseFields } from "@beep/schema"; makeStatusCauseError(AppError)({message,status})(cause)` | `import * as S from "effect/Schema"; class AppError extends S.TaggedError<AppError>()("AppError",{message:S.String,status:S.Finite,cause:S.OptionFromOptionalKey(S.Defect({includeStack:true}))}) {}; new AppError({message,status,cause:O.fromUndefinedOr(cause)}) with O from "effect/Option"` | Cause normalization and always-true equivalence change unless deliberately preserved; curried constructor sites and consumers of the input Classes require source edits. |
| String | `import { NonEmptyTrimmedStr, UUID, StrFromUnknown } from "@beep/schema"; S.decodeSync(NonEmptyTrimmedStr)(x)` | `import * as S from "effect/Schema"; S.decodeSync(S.Trim.check(S.isNonEmpty()))(x); UUID adds S.isUUID(); nullable uses S.NullOr(S.String), Option uses S.OptionFromNullOr(S.String); import * as I from "effect/Inspectable" for I.toStringUnknown(x,0)` | Inspectable does not promise Error.message or hostile-object fallback parity; trim/brand/constructor-default policy must be carried at actual boundaries. |
| Thunk | `import { ThunkUnknown } from "@beep/schema/Thunk"; ThunkUnknown.is(x)` | `import * as S from "effect/Schema"; import * as P from "effect/Predicate"; S.is(S.declare(P.isFunction))(x); use S.declare(customGuard) when return typing is known` | Function membership proves neither arity nor return type; brand and generic/static helpers disappear. |
| Timestamp | `import * as T from "@beep/schema/Timestamp"; T.fromString(text); T.addMillis(value,n)` | `import * as S from "effect/Schema"; import * as D from "effect/DateTime"; S.decodeUnknownEffect(S.DateTimeUtcFromString)(text); D.add(value,{milliseconds:n}); D.toEpochMillis(value); D.formatIso(value)` | epochMillis Class/wire representation changes; pre-epoch acceptance and ISO normalization differ from beep restrictions. |
| Timezone | `import { Timezone } from "@beep/schema"; S.decodeSync(Timezone)(name)` | `import * as S from "effect/Schema"; S.decodeSync(S.TimeZoneNamedFromString)(name)` | Result is TimeZone.Named rather than string; runtime IANA availability/canonicalization replaces a fixed dataset vocabulary. |
| Transformations | `import { destructiveTransform } from "@beep/schema"; Input.pipe(destructiveTransform(f))` | `import * as S from "effect/Schema"; import * as G from "effect/SchemaGetter"; Input.pipe(S.decodeTo(Output,{decode:G.transform(f),encode:G.forbiddenEncoding}))` | Encoding intentionally becomes forbidden instead of passthrough; throwing/effectful f needs G.transformEffect with typed issue handling and an honest Output schema. |
| URL | `import { URLStr, HttpsUrl } from "@beep/schema"; S.decodeSync(HttpsUrl)(text)` | `import * as S from "effect/Schema"; S.decodeSync(S.URLFromString)(text); for HTTPS, decodeTo(S.URL.check(S.makeFilter(u=>u.protocol==="https:"))) at the consumer boundary` | Decoded value is URL rather than branded text; protocol restrictions and URL canonicalization affect persisted values/equality. |
| Uint32 | `import { Uint32 } from "@beep/schema"; S.decodeSync(Uint32)(n)` | `import * as S from "effect/Schema"; S.decodeSync(S.Number.check(S.isUint32()))(n)` | Protobuf nominal brands disappear; schema format/diagnostics may differ even with the same numeric bounds. |
| Uint64 | `import { Uint64 } from "@beep/schema"; S.decodeSync(Uint64)(input)` | `import * as S from "effect/Schema"; S.decodeSync(S.BigIntFromString.pipe(S.decodeTo(S.BigInt.check(S.isBetweenBigInt({minimum:0n,maximum:18446744073709551615n})))))(decimalText)` | Number/bigint inputs must become explicit decimal text or take a separate native BigInt boundary; JSON representation and protocol-specific brands change. |
| Unknown | `import { UnknownFromJsonString } from "@beep/schema"; UnknownFromJsonString.encodeUnknownEffect(value)` | `import * as S from "effect/Schema"; S.encodeUnknownEffect(S.UnknownFromJsonString)(value); use S.Unknown for plain unknown fields` | Attached statics disappear; unknown identity and JSON serializability remain separate contracts. |

SchemaUtils leaf migration details (same concept row; no aliases retained for removed helpers):

| Leaf family | Upstream route | Remaining distinction |
| --- | --- | --- |
| `encoders`, `isCodecDataFirst`, `toEquivalence` | `S.encode*` (e.g. `packages/effect/src/Schema.ts:2173`), `S.toEquivalence` (`packages/effect/src/Schema.ts:14305`) | Rewrite dual argument order/currying; use native factory calls at the call site. |
| `withCodecStatics`, `classStatics`, `withStatics`, `withLiteralKitStatics` | `S.decode*/encode*/is`, ordinary named helpers, `S.Literals` | Remove static installation and rebuild monkey-patching. No claimed upstream static-installer equivalent; consumers abandon that API. |
| Constructor/key/value defaults | `S.withConstructorDefault` (`packages/effect/src/Schema.ts:5651`), `S.withDecodingDefaultTypeKey` (`packages/effect/src/Schema.ts:5769`), `S.withDecodingDefaultType` (`packages/effect/src/Schema.ts:5877`) | Preserve key-vs-value and encoded-vs-type semantics individually. |
| `optional`, `optionalKeyWithDefaults`, `withEncodeDefault` | Native optional/default combinators plus `SchemaGetter.required` (`packages/effect/src/SchemaGetter.ts:438`) | Verify omitted key versus explicit undefined and how encoding drops/preserves defaults. |
| `split`, `pluck` | `decodeTo` plus native getters/transformations; `SchemaGetter.split` (`packages/effect/src/SchemaGetter.ts:1270`) and property selection | These are consumer compositions, not renamed package helpers. Native split decodes empty text to [] whereas the beep String.split path yields [""]. Pluck's narrowed encoded struct must be preserved or intentionally changed. |
| `collectAnnotationsAt` | Retain thin traversal over public SchemaAST; single-node resolver is `packages/effect/src/SchemaAST.ts:4994` | Repo implementation traverses structural children, checks, context, constructor defaults and encoding links (`packages/foundation/modeling/schema/src/SchemaUtils/collectAnnotationsAt.ts:49`); root-only lookup is not equivalent. Further generic traversal API replacement is **UNVERIFIED**. |

Additional recipe API evidence: `S.declare` at `packages/effect/src/Schema.ts:562`; `S.decodeTo` at `packages/effect/src/Schema.ts:5387`; `S.Natural` at `packages/effect/src/Schema.ts:7603`; `SchemaGetter.transform`/`transformEffect` at `packages/effect/src/SchemaGetter.ts:572` and `packages/effect/src/SchemaGetter.ts:612`; `SchemaGetter.forbiddenEncoding` at `packages/effect/src/SchemaGetter.ts:251`. Native arbitrary use after retirement is `import * as A from "effect/unstable/arbitrary/Arbitrary"; A.schema(schema)` (`packages/effect/src/unstable/arbitrary/Arbitrary.ts:346`), not a beep companion.

## Bounded upstream probes

These probes import the local reference source directly, not the repo-installed package. They corroborate selected after-shapes and semantic deltas; they do not establish complete consumer migrations or performance parity.

```sh
bun run explorations/effect-schema-parity/research/retirement-G-Z-smoke.ts > explorations/effect-schema-parity/research/retirement-G-Z-smoke.json
rg -c '^check\(' explorations/effect-schema-parity/research/retirement-G-Z-smoke.ts
```

Result: **13 checks passed**, exit 0. Machine-readable values: `retirement-G-Z-smoke.json`; executable witness: `retirement-G-Z-smoke.ts`.

| Probe | Observed result | Upstream source |
| --- | --- | --- |
| Literal mapping | Decode OK to 200; encode 404 to NO | `packages/effect/src/Schema.ts:4780` |
| HashSet iso codec | Duplicate array elements collapse on round trip | `packages/effect/src/Schema.ts:12739`; `packages/effect/src/Schema.ts:14544` |
| Graph iso codec | Native snapshot has type/nodes/edges; nodes have index/data | `packages/effect/src/Schema.ts:12536`; `packages/effect/src/Schema.ts:14544` |
| RegExp JSON codec | Object source/flags round-trips the i flag | `packages/effect/src/Schema.ts:8918` |
| Defaults | Constructor and missing-key decoder both produce the declared default | `packages/effect/src/Schema.ts:5651`; `packages/effect/src/Schema.ts:5769` |
| Split | Empty input becomes an empty array | `packages/effect/src/SchemaGetter.ts:1270` |
| Promise guard | Plain then/catch object is accepted without finally/native Promise tag | `packages/effect/src/Predicate.ts:1326` |
| Timestamp | Epoch -1 is accepted and retained | `packages/effect/src/Schema.ts:11047` |
| Signed and unsigned bounds | Both native 32-bit checks accept endpoint/reject out-of-range probes | `packages/effect/src/Schema.ts:7623`; `packages/effect/src/Schema.ts:7654` |
| Arbitrary | HashSet schema produces a native Arbitrary value; generation quality not exercised | `packages/effect/src/unstable/arbitrary/Arbitrary.ts:346` |
| JSON Schema import | Draft-2020-12 string document imports to a runtime string schema | `packages/effect/src/JsonSchema.ts:319`; `packages/effect/src/SchemaRepresentation.ts:2880` |
| One-way transform | Decode succeeds; corrected forbidden callback rejects encoding | `packages/effect/src/SchemaGetter.ts:215` |

Independent count corroboration: `retirement-G-Z-consumers.cjs`/`.json` agrees with `retirement-G-Z-counts-proof.json` for every concept. It does not resolve the transitive/namespace limitations documented above.

## Open questions for align

1. Resolve the locked same-intent rule versus Role B exclusion for HttpMethod, HttpStatus, Jsonl, MimeType, Toml and Yaml. If Role B adoption is allowed, retire rather than retain aliases; MIME category-domain loss needs an explicit decision.
2. Confirm that literal-case normalization is the accepted replacement for strict Kebab/Pascal/Snake validation; enumerate consumers where rejection itself is application policy.
3. Confirm JSONSchema's open-record document migration may drop strict keyword validation/Option-wrapped Node values and how consumers requiring hostile-key preservation are tested.
4. Decide where Opaque's identity-exclusion annotation belongs per owning field; do not silently change equality for classes containing defects.
5. Ratify the proposed SchemaUtils survivor (`collectAnnotationsAt`) against the active selective-codec-statics goal; retire the rest without a parallel compatibility API.
6. Confirm mutable-map/set wire boundaries may use decoded arrays plus direct value construction, and Graph mutable serialization may become immutable snapshot serialization.
7. Ratify the semantic-domain boundary for KEEP entries (calendar-only LocalDate, standalone nonzero Port, Percentage, UnitInterval and trust/security policies); generic schema constructors alone are not evidence of a dedicated domain implementation.
8. Before implementation, obtain the required type-check/runtime before-and-after performance numbers. This research supplies no performance claim.
9. Expand the direct-import witnesses through package barrels and runtime namespace aliases when planning the one-PR migration; zero external direct importers does not mean zero internal dependents.
