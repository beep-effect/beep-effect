# Sources — bitemporal-goal-roadmap

- `goals/knowledge-surface-automation/SPEC.md` — ratifying interview record; Workstream
  D scope boundary (now-view engine only; --as-of deferred here).
- `goals/epistemic-bitemporal-edge-core/` — the bitemporal edge kernel this exploration
  is a named sibling of.
- `packages/tooling/tool/cli/src/commands/Goals/` — the manifest/index tooling the
  ledger would be derived from.
