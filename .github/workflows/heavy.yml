name: Heavy

on:
  workflow_call:

permissions:
  contents: read

jobs:
  verify:
    name: ${{ matrix.name }}
    runs-on: beep-ec2-heavy
    timeout-minutes: ${{ matrix.timeout_minutes }}
    environment: ${{ github.event_name == 'push' && 'turbo-cache-write' || null }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - id: lint-policy
            name: Lint Policy
            timeout_minutes: 50
            uses_turbo: "false"
            install_typos: "true"
          - id: check
            name: Check
            timeout_minutes: 120
            uses_turbo: "true"
            install_typos: "false"
          - id: test-integration
            name: Test Integration
            timeout_minutes: 40
            uses_turbo: "true"
            install_typos: "false"
          - id: coverage
            name: Coverage Regression
            timeout_minutes: 80
            uses_turbo: "true"
            install_typos: "false"
          # Quality-lane audit D12: pull requests prove the tsc emit + babel
          # build affected-scoped with remote-cache read; trusted main pushes
          # run unscoped and stay the only cache writer. The lane lives here
          # rather than in check.yml because the beep-ec2-heavy runner group
          # admits only heavy.yml@main and check.yml@main: check.yml calls
          # this workflow at @main, so its jobs carry the main ref on pull
          # requests, while a job declared in check.yml carries the PR ref
          # and never receives a runner.
          - id: build
            name: Build
            timeout_minutes: 40
            uses_turbo: "true"
            install_typos: "false"
          - id: docgen
            name: Docgen
            timeout_minutes: 60
            uses_turbo: "false"
            install_typos: "false"
            docgen_concurrency: 6
          - id: doctest
            name: Doctest
            timeout_minutes: 60
            uses_turbo: "false"
            install_typos: "false"
    env:
      BEEP_DOCGEN_CONCURRENCY: ${{ matrix.docgen_concurrency || 3 }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0
        with:
          fetch-depth: 0
          filter: blob:none
          persist-credentials: false

      - name: Fetch pull request base
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          git fetch --quiet --no-tags origin "+refs/heads/${GITHUB_BASE_REF:-main}:refs/remotes/origin/${GITHUB_BASE_REF:-main}"

      - name: Determine lane execution
        id: lane-gate
        shell: bash
        run: |
          set -euo pipefail
          should_run=true
          doctest_mode=full
          eval "$(scripts/ci-change-profile.sh "origin/${GITHUB_BASE_REF:-main}")"
          if [[ "$goals_only" == "true" ]]; then
            should_run=false
          fi

          if [[ "${{ matrix.id }}" == "docgen" && "$GITHUB_EVENT_NAME" == "pull_request" ]]; then
            changed_files="$(git diff --name-only "origin/${GITHUB_BASE_REF:-main}...HEAD")"
            if [[ -z "$changed_files" ]] || ! grep -Eq '(^apps/|^packages/|^infra/|/docgen\.json$|\.(ts|tsx|mts|cts|md)$)' <<< "$changed_files"; then
              should_run=false
            fi
          fi

          if [[ "${{ matrix.id }}" == "doctest" && "$GITHUB_EVENT_NAME" == "pull_request" ]]; then
            doctest_mode=affected
            base_ref="origin/${GITHUB_BASE_REF:-main}"
            if ! git rev-parse --verify "$base_ref" >/dev/null 2>&1; then
              git fetch --quiet --no-tags --depth=100 origin \
                "${GITHUB_BASE_REF:-main}:refs/remotes/origin/${GITHUB_BASE_REF:-main}"
            fi

            changed_files="$(git diff --name-only "${base_ref}...HEAD")"
            if grep -Eq '(^vitest\.docs\.ts$|^vitest\.shared\.ts$|^package\.json$|^bun\.lock$|^\.github/workflows/heavy\.yml$|^packages/tooling/tool/cli/src/commands/Docgen/|^packages/tooling/tool/cli/src/internal/jsdoc/|^packages/tooling/tool/cli/src/commands/Ci/CiLane\.ts$)' <<< "$changed_files"; then
              doctest_mode=full
            else
              # Any package/app source or package-level config can
              # affect marked examples in that workspace or its dependents.
              # The CLI owns package-graph expansion, existence filtering,
              # and marker discovery.
              mapfile -t affected_inputs < <(
                git diff --name-only "${base_ref}...HEAD" -- \
                  ':(glob)packages/**/src/**/*.ts' \
                  ':(glob)packages/**/src/**/*.tsx' \
                  ':(glob)apps/**/src/**/*.ts' \
                  ':(glob)apps/**/src/**/*.tsx' \
                  ':(glob)packages/**/package.json' \
                  ':(glob)packages/**/docgen.json' \
                  ':(glob)packages/**/tsconfig*.json' \
                  ':(glob)apps/**/package.json' \
                  ':(glob)apps/**/docgen.json' \
                  ':(glob)apps/**/tsconfig*.json'
              )

              if (( ${#affected_inputs[@]} == 0 )); then
                should_run=false
              fi
            fi
          fi

          echo "should_run=$should_run" >> "$GITHUB_OUTPUT"
          echo "doctest_mode=$doctest_mode" >> "$GITHUB_OUTPUT"

      - name: Setup monorepo CI
        if: steps.lane-gate.outputs.should_run == 'true'
        uses: ./.github/actions/setup-monorepo-ci
        with:
          cache-turbo: ${{ matrix.uses_turbo }}
          cache-write: "false"
          turbo-cache-key-suffix: -${{ matrix.id }}
          free-disk: "true"
          turbo-remote-cache: "true"
          app-secrets: "true"
          turbo-api: ${{ vars.TURBO_API }}
          turbo-team: ${{ vars.TURBO_TEAM }}
          turbo-token: ${{ github.event_name == 'push' && secrets.TURBO_TOKEN || '' }}
          turbo-read-token: ${{ secrets.TURBO_READ_TOKEN }}
          database-url: ${{ github.event_name == 'push' && secrets.DATABASE_URL || '' }}
          database-url-unpooled: ${{ github.event_name == 'push' && secrets.DATABASE_URL_UNPOOLED || '' }}
          email-resend-api-key: ${{ github.event_name == 'push' && secrets.EMAIL_RESEND_API_KEY || '' }}
          auth-secret: ${{ github.event_name == 'push' && secrets.AUTH_SECRET || '' }}
          better-auth-secret: ${{ github.event_name == 'push' && secrets.BETTER_AUTH_SECRET || '' }}
          better-auth-url: ${{ github.event_name == 'push' && secrets.BETTER_AUTH_URL || '' }}
          security-trusted-origins: ${{ github.event_name == 'push' && secrets.SECURITY_TRUSTED_ORIGINS || '' }}
          liveblocks-secret-key: ${{ github.event_name == 'push' && secrets.LIVEBLOCKS_SECRET_KEY || '' }}

      # PRs may restore the post-main local fallback without saving it. Same-
      # repository PRs can also read the remote cache; forks remain tokenless.
      - name: Restore main Docgen Turbo cache
        if: matrix.id == 'docgen' && steps.lane-gate.outputs.should_run == 'true' && github.event_name == 'pull_request'
        id: docgen-turbo-cache-restore
        continue-on-error: true
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .turbo/cache
          key: turbo-${{ runner.os }}-docgen-main-${{ hashFiles('bun.lock') }}-${{ github.sha }}
          restore-keys: |
            turbo-${{ runner.os }}-docgen-main-${{ hashFiles('bun.lock') }}-

      - name: Install typos
        if: steps.lane-gate.outputs.should_run == 'true' && matrix.install_typos == 'true'
        uses: taiki-e/install-action@91ddec75689c4c78665b598d188dc821c5a43e5c # v2.85.9
        with:
          tool: typos-cli@1.44.0

      - name: Run verification lane
        if: steps.lane-gate.outputs.should_run == 'true'
        run: |
          set -euo pipefail
          # Lane bodies live in the beep CLI (one-round-loop P0 inversion;
          # same-SHA parity proof recorded in
          # goals/one-round-loop/history/p0-parity-evidence.md). This case
          # block carries only GitHub-context orchestration: the
          # event-dependent shape flags each lane receives.
          #
          # Orphaned lane grandchildren (stuck bun wrappers, daemon sockets)
          # inherit this step's stdio pipes, and the runner does not end a
          # step until those pipes close — so a lane can print success, exit,
          # and still wedge the job for its full timeout (ship-velocity SPEC
          # A7; proven on Docgen run 31797064763). Run each lane in its own
          # session and reap the entire process group once the lane process
          # finishes, success and failure alike.
          run_lane() {
            setsid bun run beep "$@" < /dev/null &
            local pgid=$!
            local status=0
            wait "$pgid" || status=$?
            kill -TERM -- "-$pgid" 2>/dev/null || true
            sleep 2
            kill -KILL -- "-$pgid" 2>/dev/null || true
            return "$status"
          }

          shape_args=()

          if [[ "$GITHUB_EVENT_NAME" == "pull_request" ]]; then
            shape_args+=(--affected --base "origin/${GITHUB_BASE_REF:-main}")
          fi

          shape_args+=(--summarize)

          case "${{ matrix.id }}" in
            check|test-integration|coverage|build)
              run_lane ci lane "${{ matrix.id }}" "${shape_args[@]}"
              ;;
            lint-policy)
              run_lane ci lane "${{ matrix.id }}"
              ;;
            docgen)
              if [[ "$GITHUB_EVENT_NAME" == "pull_request" ]]; then
                run_lane ci lane docgen --mode auto --base "origin/${GITHUB_BASE_REF:-main}" --head HEAD
              else
                run_lane ci lane docgen --mode full
              fi
              ;;
            doctest)
              if [[ "$GITHUB_EVENT_NAME" == "pull_request" ]]; then
                run_lane ci lane doctest \
                  --mode "${{ steps.lane-gate.outputs.doctest_mode }}" \
                  --base "origin/${GITHUB_BASE_REF:-main}" \
                  --head HEAD
              else
                run_lane ci lane doctest --mode full
              fi
              ;;
          esac

      # Only trusted main pushes publish a cache entry. Pull requests have no
      # save step, so restored main artifacts cannot be poisoned by PR code.
      - name: Save main Docgen Turbo cache
        if: matrix.id == 'docgen' && steps.lane-gate.outputs.should_run == 'true' && github.event_name == 'push'
        continue-on-error: true
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .turbo/cache
          key: turbo-${{ runner.os }}-docgen-main-${{ hashFiles('bun.lock') }}-${{ github.sha }}

      - name: Append Turbo summary
        if: matrix.uses_turbo == 'true' && steps.lane-gate.outputs.should_run == 'true' && always()
        run: |
          if [[ "${{ matrix.id }}" == "coverage" ]]; then
            bun run beep ci append-turbo-summary --all
          else
            bun run beep ci append-turbo-summary
          fi

      - name: Save post-lane Turbo fallback
        if: github.event_name == 'push' && matrix.uses_turbo == 'true' && steps.lane-gate.outputs.should_run == 'true' && success()
        continue-on-error: true
        uses: actions/cache/save@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
        with:
          path: .turbo/cache
          key: turbo-${{ runner.os }}-${{ startsWith(runner.name, 'beep-ci-') && 'fleet' || 'shared' }}-${{ github.job }}-${{ matrix.id }}-${{ hashFiles('turbo.json', 'package.json', 'bun.lock') }}-${{ github.sha }}

      - name: Append Turbo summary (docgen)
        if: matrix.id == 'docgen' && steps.lane-gate.outputs.should_run == 'true' && always()
        run: bun run beep ci append-turbo-summary

      - name: Skip lane
        if: steps.lane-gate.outputs.should_run != 'true'
        run: echo "Skipping ${{ matrix.name }} for this change set."
