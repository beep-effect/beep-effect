name: Official Data Sync

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 15 * *"

permissions:
  contents: write
  pull-requests: write

jobs:
  sync-data:
    name: Sync Official Data
    runs-on: ubuntu-24.04
    timeout-minutes: 40
    env:
      TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
      TURBO_TEAM: ${{ vars.TURBO_TEAM || secrets.TURBO_TEAM }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          fetch-depth: 0

      - name: Setup monorepo CI
        uses: ./.github/actions/setup-monorepo-ci
        with:
          cache-turbo: true
          cache-write: "true"

      - name: Sync official datasets
        env:
          BEEP_ISO3166_PART1_CSV_URL: ${{ secrets.BEEP_ISO3166_PART1_CSV_URL }}
          BEEP_ISO3166_PART2_CSV_URL: ${{ secrets.BEEP_ISO3166_PART2_CSV_URL }}
          BEEP_ISO3166_AUTH_HEADER: ${{ secrets.BEEP_ISO3166_AUTH_HEADER }}
        run: bun run beep sync-data-to-ts --all --include-authenticated --report-dir "$RUNNER_TEMP/data-sync-report"

      - name: Upload data sync report
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: data-sync-report
          path: ${{ runner.temp }}/data-sync-report
          if-no-files-found: error

      - name: Run focused check verification
        id: focused-check
        run: bun run check -- --filter=@beep/repo-cli --filter=@beep/data --filter=@beep/law-practice-domain --filter=@beep/schema --filter=@beep/md --filter=@beep/file-processing --filter=@beep/libpff --summarize

      - name: Append Turbo summary (check)
        if: always() && steps.focused-check.outcome != 'skipped'
        run: bun run beep ci append-turbo-summary

      - name: Run focused unit verification
        id: focused-test
        if: steps.focused-check.outcome == 'success'
        run: bun run test -- --unit --filter=@beep/repo-cli --filter=@beep/data --filter=@beep/law-practice-domain --filter=@beep/schema --filter=@beep/md --filter=@beep/file-processing --filter=@beep/libpff --summarize

      - name: Prepare pull request body
        id: pr-body
        if: always()
        run: |
          delimiter="EOF_$(openssl rand -hex 16)"
          report="$RUNNER_TEMP/data-sync-report/data-sync-report.md"
          {
            echo "body<<${delimiter}"
            if [ -f "$report" ]; then
              cat "$report"
            else
              echo "_Data sync report unavailable: a sync or verification step failed._"
            fi
            echo
            echo 'This PR was opened automatically by `.github/workflows/data-sync.yml`.'
            echo "${delimiter}"
          } >> "$GITHUB_OUTPUT"

      - name: Append Turbo summary (test)
        if: always() && steps.focused-test.outcome != 'skipped'
        run: bun run beep ci append-turbo-summary

      - name: Create or update pull request
        uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1 # v8.1.1
        with:
          token: ${{ secrets.BEEP_DATA_SYNC_TOKEN != '' && secrets.BEEP_DATA_SYNC_TOKEN || github.token }}
          branch: automation/data-sync
          delete-branch: true
          commit-message: "chore(data): sync official datasets"
          title: "chore(data): sync official datasets"
          body: ${{ steps.pr-body.outputs.body }}
