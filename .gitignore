coverage/
*.tsbuildinfo
node_modules/
.DS_Store
tmp/
dist/
build/
.direnv/
.playwright-mcp
# Nested IDE dirs stay machine-local; the root .idea is tracked and curates
# its own contents via the whitelist .idea/.gitignore
**/.idea/
!/.idea/
# Environment and secrets - NEVER commit these
.env
.env.*
!.env.example
!.env.*.example
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.keys
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
credentials.json
secrets.json
service-account*.json
**/secrets/
**/.secrets/
.serena/
.playwright-cli/
.firecrawl/
# Client
stats.html
map.html
.env.tmp
.aider*
# Turborepo
.turbo
.vercel
**/.next/
**/next-env.d.ts
**/docs
!goals/**/docs/
!goals/**/docs/**
# Root docs/ is tracked authored documentation; generated + private stay out
!/docs/
# Legacy root docgen aggregate paths; current aggregate output is /docs/generated/
/docs/agent-capability/
/docs/agents/
/docs/architecture-lab/
/docs/architecture-lab-proof/
/docs/drivers/
/docs/epistemic/
/docs/foundation/
/docs/infra/
/docs/law-practice/
/docs/oip-web/
/docs/professional-desktop/
/docs/shared/
/docs/storybook/
/docs/tooling/
/docs/workspace/
/docs/generated/
/docs/_internal/
# Docgen analysis artifacts
**/JSDOC_ANALYSIS.md
**/JSDOC_ANALYSIS.json
**/JSDOC_QUALITY.md
**/JSDOC_QUALITY.json
trace

# Playwright
e2e/.auth/
playwright-report/
test-results/

# Storybook build output
storybook-static/

# Rust/Tauri local build output
**/src-tauri/target/
**/src-tauri/gen/

# Lost Pixel
.lostpixel/current/
.lostpixel/difference/

# Claude plugin state files (ralph-wiggum loop state)
.claude/ralph-loop.local.md
.claude/.hook-state.json
.claude/scheduled_tasks.lock
# Worktree-isolated agent sessions materialize transient checkouts here
.claude/worktrees/

# Local code-index cache only (MCP + Codex config are beep-sync managed/committed)
.code-index/

# SST
.sst/
sst-env.d.ts
/outputs/agent-reliability/worktrees/
/output/

# lsmcp cache
.lsmcp/cache
.beep/*
**/.beep/*

# Deterministic packet navigation projections; manifests are authoritative.
/goals/INDEX.md
/explorations/ATLAS.md

# subtrees
.repos/*

.claude/settings.local.json

# secrets / credential exports (never commit; public repo)
*cookies*.txt
openai.com_cookies.txt

# SkillOpt pilot local env (tools/skillopt is a uv project; lock is committed)
tools/skillopt/.venv/

# graph-3d-navigation reverse-engineering capture corpus: third-party reference
# artifacts only (proprietary graph.infranodus.com JS/CSS bundle, scraped vendor
# docs, copyrighted papers, their demo dataset). Reference-only working artifact —
# never commit (public repo). The packet's committed prose reports are the
# deliverable; the corpus is reproducible from the URLs in
# explorations/graph-3d-navigation/research/SEED-INVENTORY.md
explorations/graph-3d-navigation/research/seed/

# Agent-harness prompt scratch files must never be committed (public repo).
/codex-task-*.txt

# codegraph local index (shared-memory-code-kg-wiring)
.codegraph/
