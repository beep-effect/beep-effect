/**
 * Tagged union over all 113 JSDoc tag occurrence shapes.
 *
 * This module re-exports from `./tag-values/index.ts` where individual
 * `S.TaggedClass` members are defined and composed.
 *
 * @packageDocumentation
 * @category models
 * @since 0.0.0
 */
export * from "./tag-values/index.ts";
