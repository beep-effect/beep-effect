/**
 * Parity fixture dead code — DELIBERATELY DEAD (one-round-loop P0 row 2).
 *
 * @packageDocumentation
 * @since 0.0.0
 */

/**
 * Never imported anywhere; exists to trip the fallow dead-code gate.
 *
 * @returns A constant string.
 * @example
 * ```ts
 * console.log("dead")
 * ```
 * @category testing
 * @since 0.0.0
 */
export const parityFixtureDeadFunction = (): string => "dead";
