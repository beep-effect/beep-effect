{
  "$schema": "https://v2-11-3.turborepo.dev/schema.json",
  "extends": ["//"],
  "tasks": {
    "test": {
      "inputs": ["$TURBO_EXTENDS$", "$TURBO_ROOT$/turbo.json"]
    },
    "test:property": {
      "inputs": ["$TURBO_EXTENDS$", "$TURBO_ROOT$/turbo.json"]
    }
  }
}
