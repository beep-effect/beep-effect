/**
 * Candor record repository export surface.
 *
 * @packageDocumentation
 * @category repositories
 * @since 0.0.0
 */

export * from "./CandorRecord.ports.ts";
/**
 * Derivation of the candor gate's read seam from the durable repository.
 *
 * @category layers
 * @since 0.0.0
 */
export * from "./CandorRecord.reader.ts";
