/**
 * Package version constant exported by `@beep/ui`.
 *
 * **Example** (Log package version)
 *
 * ```ts
 * import { VERSION } from "@beep/ui/Version"
 *
 * console.log(VERSION)
 * ```
 *
 * @category configuration
 * @since 0.0.0
 */
export const VERSION = "0.0.0" as const;
