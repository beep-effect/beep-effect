/**
 * Public workspace tree surface: tab groups, splits, floating members, and
 * serialized snapshots.
 *
 * @packageDocumentation
 * @since 0.0.0
 */
export {
  /**
   * @category models
   * @since 0.0.0
   */
  DockNode,
  /**
   * @category models
   * @since 0.0.0
   */
  DockSnapshot,
  /**
   * @category models
   * @since 0.0.0
   */
  DockWorkspace,
  /**
   * @category models
   * @since 0.0.0
   */
  EmptyWorkspace,
  /**
   * @category models
   * @since 0.0.0
   */
  FloatingMember,
  /**
   * @category models
   * @since 0.0.0
   */
  HorizontalSplitLayout,
  /**
   * @category models
   * @since 0.0.0
   */
  PopulatedWorkspace,
  /**
   * @category models
   * @since 0.0.0
   */
  SplitLayout,
  /**
   * @category models
   * @since 0.0.0
   */
  SplitNode,
  /**
   * @category models
   * @since 0.0.0
   */
  TabsNode,
  /**
   * @category models
   * @since 0.0.0
   */
  VerticalSplitLayout,
} from "./Dock.models-tree.ts";
