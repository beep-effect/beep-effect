/**
 * Public panel and group model surface for the dock workspace kernel.
 *
 * @packageDocumentation
 * @since 0.0.0
 */
export {
  /**
   * @category models
   * @since 0.0.0
   */
  ComponentPanelView,
  /**
   * @category models
   * @since 0.0.0
   */
  GroupHeaderPosition,
  /**
   * @category models
   * @since 0.0.0
   */
  GroupLockedMode,
  /**
   * @category models
   * @since 0.0.0
   */
  GroupMetadata,
  /**
   * @category models
   * @since 0.0.0
   */
  GroupPatch,
  /**
   * @category models
   * @since 0.0.0
   */
  Panel,
  /**
   * @category models
   * @since 0.0.0
   */
  PanelParameters,
  /**
   * @category models
   * @since 0.0.0
   */
  PanelParameterValue,
  /**
   * @category models
   * @since 0.0.0
   */
  PanelPatch,
  /**
   * @category models
   * @since 0.0.0
   */
  PanelRenderMode,
  /**
   * @category models
   * @since 0.0.0
   */
  PanelView,
  /**
   * @category models
   * @since 0.0.0
   */
  TextPanelView,
} from "./Dock.models-tree.ts";
