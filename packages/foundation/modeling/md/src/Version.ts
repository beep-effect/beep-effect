/**
 * Current `@beep/md` package version.
 *
 * **Example** (Import and type version)
 *
 * ```ts
 * import { VERSION } from "@beep/md/Version"
 *
 * const version: "0.0.2" = VERSION
 * console.log(version)
 * ```
 *
 * @category configuration
 * @since 0.0.0
 */
export const VERSION = "0.0.2" as const;
