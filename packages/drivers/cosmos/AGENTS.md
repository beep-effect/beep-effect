# Agent Guide

Driver-level graph viewport package for the ontology workbench P3 spike. Keep
cosmos.gl and sigma.js runtime details inside this package; export browser-safe
capability detection, typed-array graph projections, and minimal render adapters.
