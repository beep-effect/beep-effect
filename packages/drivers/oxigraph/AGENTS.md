# Agent Guide

Driver-level SPARQL package backed by Oxigraph WASM. Keep Oxigraph and RDF/JS
runtime details inside this package; export the semantic-web SPARQL service
Layer and browser-safe helpers only.
