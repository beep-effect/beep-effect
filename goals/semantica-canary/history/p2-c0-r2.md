# P2 C0 full-W1 R2 result

Date: 2026-08-30

Status: C0 passed; Input and Extraction verdicts written.

## Gate

The R2 run selected all 25 committed W1 papers and all nine F1 fixtures. The
live command used the committed `fixtures/w1.manifest.json`, the ratified
evidence-quote candidate, and the existing `op://`-backed environment through
`op run`. Replay used the same selection with `SEMANTICA_OFFLINE=true`.

| Measurement | Live | Replay |
| --- | ---: | ---: |
| Report digest | `7d1918096fc2ae893b9257a0a84aeb7def1cad7bf8f4d5c8add13c660faf210a` | `7d1918096fc2ae893b9257a0a84aeb7def1cad7bf8f4d5c8add13c660faf210a` |
| Documents | 34 | 34 |
| W1 parsed and hosted-extracted | 25 | 25 |
| Unexpected degradation | 0 | 0 |
| Failed anchors | 0 | 0 |
| Wall clock | 1,906,490 ms | 580,206 ms |
| Cold start | 1,874 ms | 1,884 ms |
| p95 document duration | 227,190 ms | 148,696 ms |
| Peak RSS | 1,917,935,616 bytes | 2,041,348,096 bytes |
| Disk growth | 173,932,584 bytes | 173,932,584 bytes |

The two `eval-report.json` files were byte-identical. Telemetry sidecars were
not compared for identity and correctly reported `live` and `replay` modes.
The provider cache grew from 44 to 66 entries during the live run: the three
relation papers were already cached, and the remaining 22 W1 papers added one
entry each. It stayed at 66 throughout replay.

## Archived artifacts

The schema-decoded JSON artifacts are tracked under [`history/c0`](./c0/):

- first slice: `057e356e94f8.{live,replay}.{eval-report,eval-telemetry}.json`
- remaining relation slate:
  `05afbbf3e1e9.{live,replay}.{eval-report,eval-telemetry}.json` and
  `06c93f91ef3d.{live,replay}.{eval-report,eval-telemetry}.json`
- full R2 gate: `full-w1.{live,replay}.{eval-report,eval-telemetry}.json`
- byte-integrity witness: `SHA256SUMS`

The report files contain report accounting and content-addressed cache keys,
not hosted response text. Identical SHA-256 values for each live/replay report
pair provide the archived byte-equality witness.

## Schema-validated outcomes

- Every W1 paper had `parse: parsed` and hosted extraction `extracted`.
- All five hosted metric coordinates had status `scored`.
- The three frozen relation papers emitted 10, 9, and 9 hosted relation
  claims.
- F1 malformed specimens matched their declared typed degradation.
- Every document reported zero failed anchors.
- The report self-digest, selection coverage, model-family separation, metric
  completeness, and unexpected-degradation arithmetic passed the `EvalReport`
  schema.

## Breaker and verdicts

The evidence-quote candidate passed its first probe, the complete
three-relation-paper extension, and R2. Its retry was never used.

The C0 pass permits the dated family verdicts recorded in
`explorations/semantica-lab/DECISIONS.md`:

- Input: `pick-one` on the proven C0 input stack, with `@beep/doc-text` for
  born-digital W1 PDFs.
- Extraction: `pick-one` on the hybrid contract: hosted LangExtract-shaped
  extraction with exact evidence-quote grounding, plus the fail-closed Wink
  pattern comparator with declared unsupported losses.

C1 remains a separate phase and does not begin until the C0 evidence PR is
merge-ready and landed.
