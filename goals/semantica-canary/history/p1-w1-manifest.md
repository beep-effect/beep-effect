# P1 step 2 — W1 manifest and F1 fixtures receipt

Date: 2026-08-25. Branch: `feat/semantica-p1-fixtures` (after the mint, PR #819).

W1 is defined by the committed manifest only (B3): the first 25 of the 76
on-disk academia-corpus PDFs by corpus-id sort, one row per paper with id,
sha256, and byte length. The PDFs themselves are never committed. No private-corpus
reference anywhere (D14).

## W1 manifest

| Field | Value |
| --- | --- |
| Path | the lab's `fixtures/w1.manifest.json` (`w1-manifest/v1`) |
| Corpus | `academia-2026-07` (out-of-repo; `SEMANTICA_CORPUS_ROOT`) |
| Selection | `first-25-by-id`, `take: 25`, `onDisk: 76` |
| `corpusHash` | `cc0637e1ff947147bcb3d676d2d1e355d502276bfebc9d8de6134924c1741bb3` (sha256 over the canonical JSON of `rows`) |
| Proof | `canary manifest build` twice → byte-identical; `canary manifest check` → same hash, zero drift |

Selected ids, in manifest order: `057e356e94f8`, `05afbbf3e1e9`, `06c93f91ef3d`,
`06df406f321e`, `08e376be5ed2`, `0a4ab229f341`, `0a8de1437753`, `0d06c1a2189a`,
`0f75da2dbb9f`, `10828be135bf`, `145a460064d8`, `15cb15b598de`, `1689a2ff076d`,
`1735f97c31a2`, `1a3665b7d2ae`, `1a454c6a2f8d`, `1a72f7ffcd1c`, `1e9eb2c7cafa`,
`1f6f06759a86`, `26c14353f198`, `299eb5d0c0f3`, `2b2bc2e2a40e`, `2b2eef3b7fee`,
`2ebef49a0b91`, `393357d5bc4f`.

## F1 fixtures

Nine synthetic documents under the lab's `fixtures/f1/documents/`, indexed by
`fixtures/f1/index.json` (`f1-index/v1`): per media type two `parses`
specimens and one declared `degraded` specimen.

| Fixture | Bytes | Expectation |
| --- | ---: | --- |
| `md-structure.md` | 1,508 | parses |
| `md-unicode.md` | 525 | parses |
| `md-invalid-utf8.md` | 226 | degraded (`invalid-utf8`) |
| `html-article.html` | 1,302 | parses |
| `html-entities-tables.html` | 1,217 | parses |
| `html-truncated.html` | 631 | degraded (`truncated`) |
| `pdf-two-column.pdf` | 2,909 | parses |
| `pdf-multipage.pdf` | 3,980 | parses |
| `pdf-truncated.pdf` | 2,672 | degraded (`truncated`) |

PDFs are generated by the committed `pdf-lib` generator with pinned metadata
dates; the lab test regenerates them twice into temp dirs and asserts the
committed sha256s. `F1Catalog.load` verifies every byte length and digest at
the boundary.

`gold/v1` (PLAN P1 step 8) rides its own gold-proposer PR before the first C0
slice: the gold ids are frozen from the manifest order (first 10 / 5 / 3), the
proposer is a hosted model of a different provider family than the extractor,
and the spot-check pass only annotates (research/c0-design.md, D-C0-9).
