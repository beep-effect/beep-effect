---
goal: packet-control-plane-core
agent: claude
date: "2026-08-26"
trigger: closeout
confidence: high
findings:
  - category: tooling-friction
    confidence: high
    instruction: >-
      Make the changeset gate report which working-tree paths made a package
      count as changed, and have yeet repair either stage or report the files
      its repo-wide fixers write. A fixer write in an unrelated package
      currently surfaces as that package's own test lane failing.
    explanation: >-
      During rung-4 closeout, repair's terse-effect and lint fixers rewrote
      nine files across five packages nobody had touched (semantica lab,
      runpod driver, ui-system editor, codegen-kit, docgen internals). The
      only symptom was a red feedback:test lane blaming @beep/repo-cli, whose
      packet then routed to quality-review-fix-loop for a changeset-policy
      error. The tests were green the whole time; reverting the nine unrelated
      files cleared the gate with no other change. The failure named the wrong
      package and the wrong lane.
  - category: tooling-friction
    confidence: high
    instruction: >-
      Treat the codex companion's "Codex Task started in the background as
      task-..." message as an unverified claim. Confirm the job state file
      exists under the companion state directory before counting the lane as
      dispatched, and relaunch through a direct codex exec rather than
      resuming.
    explanation: >-
      One of four dispatched lanes returned a task id and then never appeared
      in status --all and never wrote a job state file. Nothing surfaced the
      failure; the lane was simply absent. Relaunching the identical prompt
      through a direct background codex exec worked first try. This is the
      same silent-instant-death class recorded in codex-fanout-operations, now
      seen without an auth revocation to explain it.
  - category: tooling-friction
    confidence: high
    instruction: >-
      When a codex lane must produce a report, put the report path inside the
      repository the lane is rooted in. The sandbox writable root is the
      lane's cwd, so an absolute path elsewhere is rejected and the lane
      invents a fallback the orchestrator has to go find.
    explanation: >-
      The rung-4 lane was told to write to a staging directory outside the
      checkout. Its first action was rejected with "writing outside of the
      project", it fell back to a temporary path, and it correctly reported
      the blocker in that fallback file. The three analysis lanes, pointed at
      a path inside the packet, wrote where they were told with no friction.
  - category: implementation-improvement
    confidence: medium
    instruction: >-
      Expect the packet event reader's identity checks to need extraction into
      a named predicate every time an amendment adds one. Keep them in a pure
      helper returning an optional chain issue rather than inline guard
      clauses in the reader.
    explanation: >-
      Amendment D's packet-mismatch check was the guard clause that pushed
      readEventFile over the fallow cognitive and CRAP thresholds (cognitive 9
      against a limit of 8, CRAP 31.6 against 30), and it was the only
      introduced blocking finding in the whole rung. Extracting digest,
      file-name, and locator identity into one helper cleared it without
      touching behavior, and leaves room for candidate 2's opaque-event check
      to land the same way.
todos:
  - >-
    Confirm whether the three inherited fallow complexity findings in
    Goals/Doctor.ts and Goals/SetStatus.ts belong to candidate 6's migration
    work or to a separate burn-down, since the campaign will touch both files.
---

# Closeout — packet-control-plane-core

The goal closed the way it said it would: the rung-4 hardening merged as #848
after a review cycle in which every finding was real, and the
`completed-retained` flip ran through the guarded writer as this stream's
fourth event — the machinery closing itself is the D9 acceptance in one line.

The findings above are the session's residue worth keeping: two fan-out
failure modes (a silently dead codex dispatch, a sandbox-rejected report
path), one gate-misattribution trap (repo-wide fixers dirtying five untouched
packages and the changeset gate blaming this branch's package), and one
structural note on the event reader's identity checks. The open todo hands the
inherited fallow complexity rows to whoever charters candidate 6, since the
fleet campaign touches both files anyway.
