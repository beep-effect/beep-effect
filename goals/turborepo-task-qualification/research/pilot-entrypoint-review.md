# Entrypoint source review after native pilot integration

This review binds the current Cache executor and current merged planner sources.
It grants source identity only. Native pilot observations, external verdicts,
and signed remote transport evidence have separate authority.

The fresh executable census is grouped by the same exact command-string recipe.
The command-group document changes only its census digest. Regenerated local
and hosted CI/Quality planners, local and hosted Yeet planners, and complete
workflow/action source documents are equal to their post-merge counterparts.
The current projections retain 69 CI plans, nine Quality modes, 15 partition
plans and four local dispatch shapes per context, 30 Yeet branch/mode plans
per context, and ten complete local workflow/action documents.

The attachment recipe now inventories every source role in Cache rather than
maintaining a partial list. It also binds the Cache test facade and the pilot
capture-boundary tests. This includes the request/receipt models, strict log
parser, read-only overlay executor and CLI command wiring introduced since
the previous attachment.

The pilot command verifies exact executable hashes, current activation bytes,
registered worktree identities, native dry-run input/configuration parity and
the selected task's command. It uses network-isolated Bubblewrap execution,
read-only source mounts and private writable cache/log mounts. Its receipt
keeps graph failures distinct from selected-task execution and never grants
signed-remote or promotion authority.

Remaining obligations include root/child configuration and lockfile controls,
absent-script controls, complete semantic read/write and capture-adversary
evidence, signed sibling receipts, dynamic entrypoint interpretation and fresh
external verdicts. Ten local shadow observations do not discharge those
obligations or promote the currently excluded tuple.
