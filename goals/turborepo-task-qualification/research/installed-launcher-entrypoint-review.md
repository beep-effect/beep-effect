# Entrypoints with installed dependencies and merged quality lanes

The refreshed population remains 142 workspaces, 2,840 configured nodes and
1,474 executable computations. The accepted main-integration baseline changes
123 command digests across 13 workspace script maps without changing task
commands, dependency edges, effective task configuration or the global task
configuration. Identity and types lint remain excluded and cache-disabled.

The regenerated `installed-v3` planner snapshots contain 72 CI plans, nine
Quality modes, 15 partition plans and four local dispatch shapes. Each Yeet
snapshot has 30 branch/mode scenarios and three hardware-profile examples.
These names identify source snapshots; they do not specify the native pilot
receipt version. The command-group snapshot binds the newly collected census.

Main adds config type-checking, Storybook selection and affected build
arguments to the relevant plans. The source snapshots also retain remote-read
arguments and the desktop Rust path gate. Four of the ten complete local
workflow/action documents changed: setup-monorepo-ci, check, heavy and
storybook. The review request now directly binds `scripts/ci-job-env.mjs`,
`scripts/ci-change-profile.sh` and `tsconfig.configs.json` alongside the
existing planner sources.

The hosted environment helper is a source boundary. It selects push, same-repo
pull-request or local-only credential posture and writes the runner environment
file using generated delimiters. The snapshots neither execute this helper
with credentials nor prove remote authorization. Hosted token permissions,
expanded workflows, external action code and required statuses retain their
existing proof owners.

The pilot now requires a verified installed-tree receipt and pinned Node,
mounts the retained dependency tree read-only, and records direct native Turbo
selection with `--skip-infer`. The toolchain fingerprint includes the installed
tree and the observed system launcher helpers. The request also binds the
snapshot/fingerprint tests and lint's tsconfig overlay/workspace walk helpers.

Complete semantic reads, writes and captures still need evidence. Shared
libraries and ambient host inputs are not established by these source
snapshots. Native installed-view and local replay receipts retain their exact
activation, client and toolchain bindings; they do not establish signed remote
reuse. Dynamic interpreter coverage, accepted sibling artifacts, adoption and
final Yeet proof remain required. This review grants no qualification or live
cache activation.
