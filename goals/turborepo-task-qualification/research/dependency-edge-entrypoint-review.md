# Entrypoints after the scoped dependency repair

The current census has 142 workspaces, 2,840 configured nodes and 1,474
executable computations. The scoped input and dependency reviews are recorded
separately from the inherited main integration. Identity now declares 46 file
inputs and a `^lint` edge to types lint. Both live tasks remain cache-disabled
and excluded. The qualification ledger is unchanged.

Regenerated local and hosted planner snapshots retain 69 CI plans, nine
Quality modes, 15 partition plans and four local dispatch shapes. Each Yeet
snapshot retains 30 illustrative scenarios. The ten local workflow/action
documents retain their existing authority. These pure planner and source
snapshots do not execute lanes or establish hosted runtime outcomes.

The observer validates the source-bound two-task closure and requires every
observed types-lint dependency execution to be fresh. Removing the child
configuration in its named negative control can remove the child-owned edge;
the resulting missing dependency is not counted as an execution. Unexpected
dependencies and reuse of an unqualified dependency are still rejected.

The durable mutation matrix now includes the types-source deprecation case.
The retained direct/native probes establish its semantic effect and reproduce
stale replay before the scoped repair. Full comparison/shadow receipts must
bind this observer and the v5 activation preview before they are reused as
local evidence. Earlier receipts remain specific to their earlier bindings.

The source-binding recipe covers the current census sources, existing planner
dependencies and all Cache source roles. Remaining obligations include
complete semantic read/write/capture coverage, installed dependency resolution,
signed sibling integration, dynamic interpreter coverage and fresh external
verdicts. Nothing in this review activates live caching or qualifies a tuple.
