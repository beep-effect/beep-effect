# Git local-exclusion identity repair

The retained paired control at `028262e8c0` changed only Git `info/exclude`.
Identical invalid identity source changed from lint exit one to zero, while
stable Turbo retained the same identity/types input maps and task hashes.
`postMainGitExcludeCounterexample` in the runtime boundary record binds the
retained observations and independent review. This is direct-execution and
native-selection evidence, not a demonstrated cache replay.

The successor source attachment includes the repair in
`Cache.fingerprint.ts`, its test-kit export and `cache-runtime.test.ts`.
`collectCacheGitExclusions` resolves Git's absolute common directory, including
linked worktrees, then uses the existing bounded no-follow file reader for
`info/exclude`. The observation contributes a SHA-256 entry labeled
`.git/info/exclude` to the existing toolchain snapshot. That label is logical:
the physical file may live in the clone's common directory outside a worktree.
No exclusion contents or machine-specific path enter the snapshot.

Missing and empty files have the same inert identity. Non-regular files,
symlinks and reads exceeding one MiB fail closed. The collector uses the
existing typed Cache error boundary; it adds no independent state store or
qualification writer. Changes to exclusion bytes change the runtime digest
that the existing Cache execution wrapper injects for governed cached tasks.

The focused runtime suite passed ten tests. Its real Git fixture verifies
clone/worktree equivalence, changed runtime identity, absent/empty equivalence,
and directory/symlink/oversize rejection. One scoped EV010 inventory exception
documents why the fixture requires native platform services. Full CLI package
verification is tracked separately; this source review grants no package or
hosted acceptance.

Prior pilot identities do not cover this added input. Fresh activation and
stable/canary evidence are required before qualification. Other Git
configuration and concurrent mutations remain outside this repair's proof.
The pilot remains excluded with cache disabled. Signed replay, accepted
sibling contracts and final hosted proof remain required.
