---
goal: model-routing-sync
agent: codex
date: 2026-09-24
trigger: closeout
confidence: high
findings:
  - category: implementation-improvement
    confidence: high
    instruction: Verify external-schema round trips against complete payloads and preserve unknown fields.
    explanation: The merged schemas deliberately discarded metadata despite the lossless acceptance criterion; live deep-equality checks and nested-field regressions now cover it.
  - category: tooling-friction
    confidence: high
    instruction: Test complete CLI listings, including headings and usage tips.
    explanation: The original Cursor parser admitted two human-readable lines as model IDs while all 31 existing Models tests passed.
  - category: goal-critique
    confidence: high
    instruction: Date live conflict examples and distinguish required categories from historical literal values.
    explanation: Grok and JetBrains Air had changed since the census, and the Codex cache refreshed during verification; all seven categories still require evidence.
todos:
  - Require a category-by-category acceptance receipt before closing catalog or routing goals.
---

# Reflection — model-routing-sync (2026-09-24, codex)

## Summary

The acceptance audit found missing reporting coverage in an already-merged
slice. The repair preserves external payloads, inventories existing doctrine
and examples, exposes unbound candidates, and keeps projection files unchanged.
The dated acceptance receipt separates local proof from the remaining hosted
closeout gate.

## Tooling experience

- **Worked:** live payload round trips, captured Cursor output, package quick
  verification, and hashes for all 36 targets gave direct evidence.
- **Didn't:** the original fixture-only suite did not exercise CLI headings,
  unknown-field preservation, or the complete seed target census.
- **Frustrating:** a concurrent cache refresh changed availability while the
  check was running. Catalog errors also hid the actual target values.
- **Wished existed:** a packet acceptance check mapping every numbered conflict
  category to a report assertion.

## Implementation improvement opportunities

- Retain per-source effort ladders; a Codex-only level must not be lost when
  the upstream model also exists (R10).
- Keep file observations even when catalog validity fails. Both facts matter
  when a cache changes between checks.
- Keep future write support separate: reading literal-delimited examples does
  not authorize rewriting them or adopting generated blocks (R12).

## Goal & prompt critique

The launcher's slice boundary was useful. The plan mixed that boundary with
later write/timer work, while the README called slice 1 landed without proving
all seven categories. Keep the historical census, but place the dated
acceptance receipt beside the status so it is clear what was actually checked.

## TODOs worth codifying

- Add complete CLI-output samples to boundary tests without modifying frozen
  historical parsing fixtures.
- Make lossless claims executable with decode/encode equality assertions.

## Lessons (confidence-tiered)

- **HIGH — Critical:** a merged implementation and green tests do not prove a
  packet's acceptance criteria. Audit each requirement against current state.
- **MEDIUM — Best practice:** use a scratch seed plus target hashes to prove
  read-only checks without modifying operator configuration.
- **LOW — Consideration:** future reports could expose per-source observation
  timestamps to make concurrent cache refreshes easier to recognize.
