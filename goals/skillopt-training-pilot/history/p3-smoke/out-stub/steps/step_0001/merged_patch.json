{
  "reasoning": "The single trajectory shows the agent correctly modeled the input as an S.Class (schema law followed) but had no rule telling it that when a task asks for an 'executable function contract' over a schema input, the repo convention is a schema-derived Fn(...) definition wired to the implementation via .implementSync(...)/.implement(...), with the plain function re-exported as a thin call-through. Because this rule is entirely absent, the agent invented its own plain-function shape instead. Adding one Fast Rule (plus a workflow classification hook) closes this gap without hardcoding the fixture's specific names.",
  "edits": [
    {
      "op": "insert_after",
      "target": "- Prefer `MappedLiteralKit` when a protocol/code mapping needs both encoded and\n  decoded literal helper surfaces.",
      "content": "\n- When a task asks for a schema-owned input model plus an \"executable function\n  contract\" (not just a plain helper function), model the contract itself as a\n  schema-derived `Fn(...)` value built from the input/output schemas, and wire\n  the real implementation with `.implementSync(...)` (or the async variant) —\n  do not hand-write a bare function as the sole export when the task names a\n  distinct input schema and a distinct function-contract export.",
      "source_type": "failure",
      "support_count": 1
    }
  ]
}