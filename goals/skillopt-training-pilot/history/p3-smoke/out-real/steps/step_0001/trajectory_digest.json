{
  "step": 1,
  "action": "reject",
  "n_total": 1,
  "n_fail": 1,
  "failure_patterns": [
    {
      "pattern": "rule_missing: Skill has no guidance on the repo's executable function-contract pattern (`export const X = Fn(...)` with `X.implementSync(...)`), so the agent invented an ad-hoc S.Class-based 'schema transform' instead of the required Fn contract, failing two required-pattern completion checks.",
      "count": 1,
      "task_ids": [
        "sfv4-fn-schema-001"
      ]
    }
  ],
  "score_before": 0.444445,
  "score_after": 0.3,
  "rejected_edits": [
    {
      "op": "insert_after",
      "content": "\n- When a task asks for an executable function contract alongside a schema-owned\n  input model (e.g. \"executable function contract\", \"function contract\",\n  \"implement/execute this against the schema\"), export it as\n  `export const <Name> = Fn(...)` and implement it with\n  `<Name>.implementSync(...)` (or the async/effectful `implement` variant for\n  effectful bodies) instead of writing an ad-hoc schema transform or plain\n  function — check `references/pattern-catalog.md` or local repo examples for\n  the exact `Fn(...)` signature before writing one from memory.\n- Before finishing, run the repo's formatter/linter (e.g. `biome check`) on\n  every edited file, not just a standalone type-check — a passing `tsc`/`tsgo`\n  run does not imply the file matches repo formatting, and formatting\n  violations are scored independently of type and schema-law checks.",
      "target": "- Prefer built-in schema constructors and checks before `S.makeFilter(...)`."
    }
  ]
}