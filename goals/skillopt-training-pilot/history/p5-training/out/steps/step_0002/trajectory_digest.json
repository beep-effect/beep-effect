{
  "step": 2,
  "action": "reject",
  "n_total": 2,
  "n_fail": 2,
  "failure_patterns": [
    {
      "pattern": "rule_ignored: Skill already documents `O.getSomesStruct({...})` from `@beep/utils` for shape-preserving optional compaction, but both agents invented their own substitute (a mapped-type/cast, and a bespoke `compactOptionRecord` helper) instead of writing the literal required call/import.",
      "count": 1,
      "task_ids": [
        "sfv4-getsomes-struct-002"
      ]
    },
    {
      "pattern": "scorer_contract: Both agents left the exported pure-data type as a plain `type`/`interface` (or a cast) instead of also converting the schema-owned model itself into `S.Class`, so the 'no exported interface/type literal remains' verification step was not actually satisfied even though an Option-based helper was added.",
      "count": 1,
      "task_ids": [
        "sfv4-getsomes-struct-001"
      ]
    }
  ],
  "score_before": 0.4714125,
  "score_after": 0.4089125,
  "rejected_edits": [
    {
      "op": "insert_after",
      "content": "  - When refactoring an existing optional-field compaction site (a `R.getSomes(...)` call, an `as Partial<...>`/mapped-type cast, or any hand-rolled local helper that reimplements shape-preserving compaction), do not invent a new bespoke utility or type-level workaround as a substitute — write the literal call `O.getSomesStruct({...})` together with `import { O } from \"@beep/utils\"` directly in the entrypoint file. A type-safe alternative that avoids this exact call/import still fails review even when it typechecks cleanly.\n  - When the object being compacted is itself the task's named exported data model (not just a helper's return value), the model's own declaration must also become `S.Class` (or the otherwise-preferred schema construct) in the same edit — e.g. `export class Foo extends S.Class<Foo>(\"Foo\")({...})`. Adding an `O.getSomesStruct`/Option-based helper without also converting the exported `type`/`interface` itself leaves the 'no exported interface/type literal remains' check unsatisfied.",
      "target": "  - Use `S.OptionFrom*` when the wire/schema field is optional or nullish. If\n    runtime `Option` values are already being shaped into an object, prefer\n    `O.getSomesStruct({...})` (`@beep/utils`) for heterogeneous struct spreads\n    with known keys — runtime-identical to `R.getSomes` but type-preserving per\n    key; reserve `R.getSomes({...})` for homogeneous dynamic-key dictionaries.\n    Use `O.all({...})` for all-or-nothing fixed-shape composition. (Amended\n    2026-07-05 — see `standards/architecture/DECISIONS.md`.)"
    }
  ]
}