# P1 summary

## Changed surfaces

- `.patterns/jsdoc-documentation.md`: rewrote the binding law around the Effect
  v4 section grammar, transitional titled-Example carrier, kind-split Example
  requirement, `@remarks` retirement, described `@see`, revised tag order,
  namespace imports, and `@since 0.0.0`. Preserved category vocabulary, custom
  agent tags, TSDoc rules, and compile validation. Added an MIT semantic-port
  attribution and fixed the `@module`/`@packageDocumentation` copy-paste bug.
- `.claude/skills/jsdoc-annotation-specialist/SKILL.md`: changed the workflow to
  classify symbol kind, author or upgrade section-shaped blocks, migrate touched
  legacy carriers, and validate described links and compilation. Corrected all
  three stale Source Reference paths.
- `references/conventions.md`: replaced the universal-`@example` posture with the
  section grammar, kind split, tag order, described links, and an Option/none-style
  schema-helper before/after exemplar.
- `references/annotation-patterns.md`: made the same-name schema type companion a
  prose-only type-level example with a described `@see`.
- `references/agent-lifting-and-greps.md`: routed semantic context through Details
  and Gotchas instead of `@remarks`, and updated audits for sections, carriers,
  described links, kind-aware presence, and annotation gaps.
- `tsdoc.json`: removed the banned `@module` and `@template` definitions and
  support registrations.

## Decisions and deferrals

- Applied all P1-relevant grilled decisions without re-mining Effect or changing
  package source.
- Current enforcement is described accurately: the repo-wide aggregate ratchet
  prevents net growth but cannot prove cleanup in each touched file.
- P2 remains responsible for the changed-files cleanup gate, grammar inventory
  rules, kind-aware dual-carrier presence, docgen regression fixture, and baseline
  attribution/update.
- P3 remains responsible for pilot JSDoc conversion and before/after hover
  evidence.
- Used the P0-corrected live facts: 532 existing `@remarks`, dual-carrier docgen
  harvest already exists, and `.agents/skills` mirrors `.claude/skills` by symlink.
