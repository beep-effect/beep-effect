Found 8 actionable defects. No files were modified.

| id | severity | file:line | summary | failure scenario | fix |
|---|---|---|---|---|---|
| R01-01 | P1 | `packages/foundation/ui-system/editor/src/viewer.tsx:197` | Viewer does not react to `state` prop changes | A message component remains mounted while streamed or refreshed content supplies a new serialized state. `LexicalComposer` only consumes `initialConfig.editorState` during initialization, so the viewer continues displaying the original state. | Add a plugin that watches the decoded state and calls `editor.setEditorState(...)`, or deliberately key/remount the composer by a stable state revision. |
| R01-02 | P1 | `packages/foundation/ui-system/editor/src/chat/atoms.ts:243` | Throwing attachment callback leaks every newly created blob URL | Valid files reach `fileToAttachment`, which creates object URLs. If consumer `onAttach` throws at line 260, execution never appends them to `attachmentsAtom`; removal, send cleanup, and the unmount sweep therefore cannot discover or revoke them. The mutation failure is also not represented in the UI. | Put callback invocation behind a typed Effect boundary and use acquire/release semantics: revoke all newly created attachments on callback failure. Append before notification only if rollback is guaranteed. Surface a capture error state. |
| R01-03 | P2 | `packages/foundation/ui-system/editor/src/chat/typeahead.tsx:332` | Mention lookup failures are silently converted to “no results” | A network-backed mention source rejects, times out, or throws synchronously. The catch at lines 348–350 clears the list, making a service outage indistinguishable from a valid empty search and providing no diagnostic or retry state. | Model lookup as an async atom/Effect with pending, success, and typed failure states. Render an unavailable/retry row and log/report the cause at the boundary while retaining the stale-response guard. |
| R01-04 | P2 | `packages/foundation/ui-system/editor/src/chat/typeahead.tsx:192` | Open menus are not repositioned on viewport movement | Open a mention menu, then scroll an enclosing desktop pane or resize the window without changing the query/highlight. Position is calculated only during React render, so the fixed menu remains at its old viewport coordinates and detaches from the caret. | Subscribe to scroll and resize while open, preferably through a scoped atom finalizer, and recompute position. A positioning primitive such as Floating UI would also handle transformed ancestors and collision updates. |
| R01-05 | P2 | `packages/foundation/ui-system/editor/src/chat/typeahead.tsx:203` | Typeahead option IDs collide across menus and composers | Two composers have typeahead menus mounted/open, or slash and mention lifecycles overlap. Both emit IDs such as `typeahead-item-0`; `aria-activedescendant` then references a non-unique document ID and assistive technology may announce the option from the wrong composer. | Prefix option and listbox IDs with a stable per-editor/per-menu identifier and ensure Lexical’s active-descendant value uses that exact ID. |
| R01-06 | P2 | `packages/foundation/ui-system/editor/src/chat/typeahead.tsx:408` | Combobox attributes survive plugin teardown | Feature configuration removes `ComboboxAriaPlugin` while the Lexical root remains mounted. Unregistering the root listener does not remove `role="combobox"`, `aria-haspopup`, `aria-autocomplete`, or the last `aria-expanded` value, leaving the editor exposed as an expanded combobox with no popup. | Add a finalizer that removes/restores every attribute applied by the binding. Prefer updating `aria-expanded` directly without repeatedly re-registering the root listener. |
| R01-07 | P2 | `packages/foundation/ui-system/editor/src/youtube-embed.tsx:31` | Third-party iframe is unsandboxed | LLM-controlled persisted content contains a valid YouTube node. Loading it gives the third-party frame the normal unsandboxed iframe capability set; the DOM export path in `youtube-node.tsx:109` has the same issue. This increases exposure to top-level navigation, popups, and future provider compromise. | Add an explicit least-privilege `sandbox` policy to both React and exported-DOM iframe paths, plus a restrictive `referrerPolicy`. Keep only capabilities required for playback/fullscreen and test them against `youtube-nocookie.com`. |
| R01-08 | P2 | `packages/foundation/ui-system/editor/src/mermaid-view.tsx:771` | Unbounded LLM Mermaid input can block the UI thread | An assistant emits a very large or pathologically complex Mermaid graph. `mermaid.render` parses and lays it out on the main thread with no source/node/edge budget; cancellation only prevents the later React state write and cannot stop the work, so the desktop UI can freeze. | Enforce source-size and graph-complexity limits before rendering. Move parsing/rendering to an interruptible worker or isolated frame and apply a timeout that visibly falls back to escaped source. |

Clean areas:

- Enter-to-send correctly guards modern and legacy IME composition signals.
- The high-priority keybinding returns `false` while typeahead is open, allowing Lexical’s menu command handling to win.
- Shift+Enter dispatches `INSERT_PARAGRAPH_COMMAND` without recursively redispatching `KEY_ENTER_COMMAND`; no direct priority loop was found.
- Lexical update and command registrations in character count, send, toolbar, drag/drop, and ARIA bindings use atom finalizers.
- Attachment rejection occurs before object-URL creation, and normal removal, successful send, and composer teardown revoke tracked URLs.
- Attachment size limits are finite, clamped, schema-checked, and MIME values are decoded before capture.
- React text rendering for mentions, filenames, artifact labels, and Mermaid failures escapes content rather than injecting HTML.
- Mermaid uses `securityLevel: "strict"` and ignores stale async completion after unmount/source replacement.
- YouTube IDs entering through the serialized-state schema are normalized and constrained to canonical IDs.
- Toolbar controls have accessible labels and expose pressed state.
- `ContentEditable` supplies an accessible placeholder and keeps visual placeholder content non-interactive.

Codex session ID: 019f5491-79c3-7152-817c-86e806aa6b45
Resume in Codex: codex resume 019f5491-79c3-7152-817c-86e806aa6b45
