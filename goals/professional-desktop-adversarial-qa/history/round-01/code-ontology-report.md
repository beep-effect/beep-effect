Found 8 actionable defects. No files were changed.

| id | severity | file:line | summary | failure scenario | fix |
|---|---|---|---|---|---|
| R01 | P0 | `packages/ontology/client/src/aggregates/Session/Session.atoms.ts:1508` | Concurrent edits can overwrite each other. Each mutation captures the same session before awaiting RPC and later replaces the entire session with its response. | Double-click Apply, or trigger Connect while Apply is pending. Both requests start from session S; responses contain S+A and S+B. Whichever completes last becomes current, silently discarding the other authored change. | Serialize session mutations, disable editing while one is pending, or add a revision/CAS field and reject/rebase stale responses. Prefer sending operations to a single client-side mutation queue. |
| R02 | P1 | `packages/ontology/use-cases/src/aggregates/Session/Session.sparql.ts:407` | LIMIT detection is lexical rather than SPARQL-aware, allowing comments to defeat the result safeguard. | Run `SELECT ... WHERE { ... }\n# LIMIT 1`. The regex sees `LIMIT`, skips injection, and Oxigraph materializes the unbounded result before `truncateResult` runs. A large graph can freeze the sidecar or exhaust memory. | Parse the query and inspect the actual solution modifier, or wrap SELECT/CONSTRUCT safely with an enforced outer limit. Do not search raw query text with a regex. |
| R03 | P1 | `packages/drivers/oxigraph/src/Oxigraph.sparql.ts:196` | SPARQL execution has no effective cancellation or timeout. `store.query()` runs synchronously inside `Effect.try`; fiber interruption cannot preempt it. | Submit a pathological property-path/join query, navigate away, or submit another query. The original synchronous query continues monopolizing the sidecar thread; the UI has no cancel control and later actions remain blocked. | Run queries in a terminable worker/process with a deadline and interrupt hook. Add `Effect.timeoutOrElse` at orchestration level, but ensure timeout terminates the underlying worker rather than merely abandoning its result. |
| R04 | P1 | `packages/ontology/client/src/aggregates/Session/Session.atoms.ts:1244` | Failed SPARQL RPCs are never copied into `ontologySparqlErrorAtom`; the old result remains visible. | Run a successful query, then submit a syntax-invalid or profile-mismatched query. The atom fails before lines 1268–1269, so the panel continues showing the previous query’s rows with no failure indication. | Clear the previous result and set a running state before execution; handle `catchCause` by storing a rendered error. Track a request token so only the latest query can publish success or failure. |
| R05 | P1 | `packages/ontology/client/src/aggregates/Session/Session.atoms.ts:1335` | Repair state is committed before inference/revalidation, but downstream failure is neither caught nor reflected in validation state. | Apply a verified repair; batch application succeeds, then inference or validation RPC fails. The ontology becomes dirty and changed, while the panel retains the pre-repair violation/result and still offers the repair button. | Set validation to running and clear the stale result before applying. Catch downstream failure and publish `failed`. Either make apply+revalidate transactional or explicitly show “repair applied; revalidation failed.” |
| R06 | P2 | `packages/ontology/client/src/aggregates/Session/Session.atoms.ts:1122` | Removing the graph container does not invalidate an in-flight Cosmos mount. | Switch/unmount the workbench while `renderCosmosGraph` is pending. The callback destroys the current handle, which is still empty, but does not increment `renderToken`; the pending mount subsequently resolves into a detached element and retains its renderer/listeners. | Increment `renderToken` whenever container or projection becomes absent, and preferably run mounting as a scoped/interruption-aware Effect whose finalizer destroys the handle. |
| R07 | P1 | `packages/ontology/ui/src/aggregates/Session/Session.workbench.tsx:506` | Add Triple accepts arbitrary non-empty “IRIs” and trims literal values, permitting invalid RDF terms and changing valid data. | Enter `not an iri` as subject/predicate, or enter the literal ` Pizza `. Apply accepts it; invalid terms can later break serialization/save, while significant literal whitespace is silently removed immediately. | Decode subject, predicate, and IRI objects through an absolute-IRI schema before enabling Apply and show field errors. Preserve literal text verbatim; only use trimming to decide whether an IRI field is blank. |
| R08 | P1 | `packages/ontology/client/src/aggregates/Session/Session.atoms.ts:1419` | Open, save, preview, and edit failures have no visible error state; invalid paths and rejected RPCs appear inert. | Enter `/etc/passwd`, `../outside.ttl`, or a missing file and click Open. The server correctly rejects it, but the mutation failure is not rendered anywhere. Save can likewise fail while the badge and source remain unchanged, leaving the user unable to tell whether data reached disk. | Add operation-specific pending/error atoms or consume the mutation `AsyncResult` in the workbench. Disable duplicate submissions, render failures beside the initiating control, and only show “Saved” when a session exists and the latest save completed successfully. |

Clean areas:

- File access is confined by the server store: it requires traversal-safe, root-relative, lower-case `.ttl` paths, canonicalizes targets, and checks resolved paths against the configured workspace root.
- Dirty-state comparison uses the complete ordered change-log signature, so undoing exactly back to the saved log correctly restores “Saved.”
- The basic undo/redo ordering—prepend undone entries and consume from the head—is internally consistent when actions are sequential.
- New edits correctly clear the redo stack.
- View mode and fold level feed the same graph projection options atom; no static inconsistency was found between their controls and worker requests.
- The projection worker is terminated by an atom finalizer and explicitly handles both `error` and `messageerror`.
- Rendered SPARQL, validation, resource, and Turtle values use React text nodes; no `dangerouslySetInnerHTML`, unsafe iframe, or direct LLM-content XSS surface was found.
- Form fields and selects in the reviewed workbench generally have accessible labels. The terse Undo/Redo buttons are tooltip-backed, though explicit `aria-label`s would still be preferable.
- Desktop orchestration registers all ontology RPC handlers and maps/logs server failures rather than swallowing them server-side; the visibility gap is in client mutation state.


Codex session ID: 019f5491-7989-76e2-b350-c53e76f6b236
Resume in Codex: codex resume 019f5491-7989-76e2-b350-c53e76f6b236
