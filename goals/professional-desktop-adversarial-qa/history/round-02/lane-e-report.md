# Lane E — Vault sync + intake

| id | severity | summary | repro | location | recommended fix | screenshot |
| --- | --- | --- | --- | --- | --- | --- |
| E-01 | medium | Zero-byte documents are accepted, classified, and written to the inbox instead of being refused. This creates content-free Box objects and presents an invented classification rationale for an empty payload. | Open `http://127.0.0.1:1421` → Vault sync. Dispatch `dragenter`, `dragover`, and `drop` on `[data-testid="document-intake-target"]` with `new File([], "qa-empty.txt", { type: "text/plain" })`. Wait for intake to finish. The result is labeled `inbox` with path `00-inbox/batch-1-qa-empty-txt/qa-empty--e3b0c44298fc.txt` and a classification rationale. | `apps/professional-desktop/src/intake/DocumentIntakeTarget.tsx:309` accepts every non-empty file list; the only per-file guard at `apps/professional-desktop/src/intake/DocumentIntakeTarget.tsx:321` is the upper size bound, after which line 331 reads and submits even a zero-byte file. | Add a pre-read `file.size === 0` rejection beside the 25 MB guard, with explicit copy such as `Empty files cannot be filed.` Do not invoke classification or persistence for empty content. | `goals/professional-desktop-adversarial-qa/history/round-02/screenshots/lane-e-01-empty-file-accepted.png` |
| E-02 | low | A correctly rejected oversized file is rendered inside a panel titled “Filed documents,” making the failure look like a filed result at panel level. The row is red, but the group heading contradicts it. | In Vault sync, drop `new File([new Uint8Array(26*1024*1024)], "qa-huge.bin")`. Observe the exact refusal, `26 MB exceeds the 25 MB intake limit.`, inside the “Filed documents” panel. | `apps/professional-desktop/src/intake/DocumentIntakeTarget.tsx:123` renders both failures and documents as result rows, while `apps/professional-desktop/src/intake/DocumentIntakeTarget.tsx:200` unconditionally titles the mixed result collection “Filed documents.” | Rename the panel to “Intake results,” or split failures into a clearly labeled “Not filed” group and successes into “Filed documents.” Preserve the excellent explicit 25 MB refusal copy. | `goals/professional-desktop-adversarial-qa/history/round-02/screenshots/lane-e-02-rejection-under-filed-heading.png` |

## Clean checks and blocked coverage

- Box reported `connected`; `Sync now` was enabled. A normal pass showed `Syncing` with the control disabled, then `Sync complete.` Counters stayed at Pending 0, Current 8, Errors 0, Conflicts 0, Queued ops 0, Failed ops 0, Open conflicts 0.
- A rapid double-click did not produce duplicate counts, overlapping spinners, or a stuck state. The control disabled during the pass and returned to one clean completion.
- No conflict rows existed before or after either pass. Therefore “mark reviewed” and next-pass conflict persistence were blocked by the live dataset; no claim is made for that behavior.
- `qa-small.txt` was clearly reported as `inbox` with its destination path.
- The oversized-file refusal was correct and named the limit verbatim: `26 MB exceeds the 25 MB intake limit.`
- `qa résumé 你好 (final) #1.txt` was accepted, visibly preserved in the result label, safely normalized in the destination path, and inboxed without an error.
- No Box object was deleted. Intake created only the requested QA artifacts in the configured dedicated QA vault.
