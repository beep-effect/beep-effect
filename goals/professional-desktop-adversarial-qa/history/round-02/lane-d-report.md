# Lane D — Ontology workbench

| id | severity | summary | repro | location | recommended fix | screenshot |
| --- | --- | --- | --- | --- | --- | --- |
| D-01 | P2 | A query with `LIMIT 2` returns two rows but the result badge says `LIMIT 100`, misreporting the executed bound. | Open `tmp/ontology-workbench/pizza-tutorial.ttl`; enter `SELECT ?s ?p ?o WHERE { ?s ?p ?o } LIMIT 2`; click **Run**; observe `2/2` beside `LIMIT 100`. | `packages/ontology/use-cases/src/aggregates/Session/Session.sparql.ts:583` always assigns `effectiveLimit` from the default safeguard; `packages/ontology/ui/src/aggregates/Session/Session.workbench.tsx:230` renders it verbatim. | Parse and return the query's top-level explicit limit when one exists; reserve the configured default for injected limits. Add a UI/use-case test for `LIMIT 2`. | [lane-d-01-limit-badge-mismatch.png](screenshots/lane-d-01-limit-badge-mismatch.png) |
| D-02 | P2 | Invalid SPARQL exposes a long internal Effect/Vite stack trace instead of an actionable parse error. | Open the pizza fixture; enter `SELECT WHERE { ?s ?p ?o }`; click **Run**; read the red Result panel. | `packages/ontology/ui/src/aggregates/Session/Session.workbench.tsx:225` renders the raw error string; the RPC boundary is collapsed in `apps/professional-desktop/src/ontology/OntologyOrchestrator.ts:54`. | Preserve the structured SPARQL reason/message across the RPC boundary and render only a concise message plus optional diagnostic ID; never render stack text in the panel. | [lane-d-02-invalid-query-stacktrace.png](screenshots/lane-d-02-invalid-query-stacktrace.png) |
| D-03 | P1 | A valid ASK query cannot run; the workbench has only SELECT/CONSTRUCT profiles and rejects ASK as a profile mismatch, again showing the internal stack. | Open the pizza fixture; enter `ASK WHERE { ?s ?p ?o }`; leave the only available SELECT profile selected; click **Run**. | `packages/ontology/ui/src/aggregates/Session/Session.workbench.tsx:947` exposes only SELECT and CONSTRUCT; `packages/ontology/use-cases/src/aggregates/Session/Session.sparql.ts:388` rejects every other query form. | Add an ASK profile and boolean result model/rendering, or infer the query form safely and dispatch it; cover true and false ASK results. | [lane-d-03-ask-query-rejected.png](screenshots/lane-d-03-ask-query-rejected.png) |
| D-04 | P1 | The cosmos graph never becomes available: after opening, editing, undo/redo, saving, and Preview, it remains `pending` with “Worker projection pending,” so canvas gestures cannot be exercised. | Open the pizza fixture; wait; click **Preview**; add a valid triple; undo, redo, and save; wait again. The Graph badge and overlay never leave pending. | Worker bridges mount at `packages/ontology/ui/src/aggregates/Session/Session.workbench.tsx:471`; pending is rendered indefinitely at `packages/ontology/ui/src/aggregates/Session/Session.workbench.tsx:491`. | Add a bounded initialization timeout and surfaced diagnostic/retry action; fix the bridge startup so an open session always produces a projection or a typed failure. | [lane-d-04-graph-worker-pending.png](screenshots/lane-d-04-graph-worker-pending.png) |

Verified without a finding: file open/tree/source, malformed-IRI feedback, Add Triple, undo/redo,
dirty/saved transition, Save, a valid SELECT, an empty SELECT result, and validation with no SHACL
shapes. Repair application and provenance export could not be meaningfully exercised because the
fixture has no SHACL shapes; huge-document behavior was not conclusive in this pass.
