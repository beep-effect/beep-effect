# P0 Verdict — Storage, Concurrency, and Provenance Spike

**Date:** 2026-07-25
**Packet:** `goals/epistemic-bitemporal-edge-core`, phase P0
**Contract:** [`ops/handoffs/p0-spike-contract.md`](../ops/handoffs/p0-spike-contract.md)
**Branch:** `feat/epistemic-bitemporal-p0-spike` (base `18c36b5dc5`)

## Verdict

**PROCEED TO P1.**

Every ratified invariant has an evidenced production-Postgres backstop AND an evidenced
PGlite proof-lane backstop — the production mechanism itself achieved full parity in the
proof lane (no substitution required for the exclusion constraint), and the two places a
mechanism genuinely cannot cross lanes (true parallel races on a single-connection
in-process database; dataDir restart on a durable server) carry named, documented
substitutions, not weakened semantics. No stop condition fired. The hard gate clears.

The live rejection no-op this packet exists to close is re-cited at the time of this
verdict: `packages/epistemic/use-cases/src/ClaimLifecycle/ClaimLifecycle.service.ts:116`
— `rejected: () => Effect.succeed(claim)` (prior citations `:73` and `:113-121` have
both drifted).

## Invariant → backstop → evidence matrix

Evidence lanes: **PG** = PostgreSQL 17.10 (`pgvector/pgvector:pg17`),
[`p0/s5-concurrency/vitest-pg-external.log`](./p0/s5-concurrency/vitest-pg-external.log) (4/4);
**PGlite** = 0.5.4 / PostgreSQL 18.3 wasm,
[`p0/s3-postgres-ddl/vitest-pglite.log`](./p0/s3-postgres-ddl/vitest-pglite.log) (22/22,
incl. the S1-derived out-of-order-ingestion fixture added by contract addendum);
**pure** = [`p0/s2-identity-endpoints/vitest-identity.log`](./p0/s2-identity-endpoints/vitest-identity.log) (9/9).

| Ratified invariant | Production Postgres evidence | PGlite proof-lane evidence |
| --- | --- | --- |
| Half-open `[validFrom,validTo)` / `[recordedAt,expiredAt)`, NULL open ends, no magic dates | S3 DDL (BIGINT pairs + `int8range(...,'[)')` expressions); constraint behavior on PG (S5 test 1) | S3 range-semantics fixture: NULL-upper unbounded, adjacency disjoint, tstzrange parity |
| Canonical `asOf(validAt, knownAt)` reads | predicate shape identical (core SQL) | S3 retroactive-correction + boundary-instant fixtures, exact answers |
| Retroactive correction: same `validAt`, two `knownAt`s → former vs corrected fact | core SQL, PGlite-proven | S3 fixture, exact (`"100"` / `"150"`) |
| Immutable fact, close-and-insert supersession, valid-time closes at invalidating `valid_from` | S5 race A winner path (metadata-only close, idempotent guard) | S3 fact-became-false + out-of-order-ingestion fixtures (donor semantics per S1 `edge_operations.py:569-570`, `:820-839`) |
| Derived "latest", no persisted `isLatest` | no such column in DDL (both lanes) | S3 derived-open-head assertions |
| Ordered-interval checks (both axes) | same DDL applied on PG | S3 probes: `spike_edge_valid_ordered`, `spike_edge_txn_ordered` |
| Unique logical-version identity | same DDL applied on PG | S3 probe: `spike_edge_logical_version_unique` |
| Lineage foreign keys + no self-supersession | same DDL applied on PG | S3 probes: `spike_edge_supersedes_fk` domain, `spike_edge_no_self_supersede` |
| No-overlap exclusion over authoritative versions | S5 test 1 + race C: `spike_edge_no_overlap` enforces on PG 17.10 | S3 smoke + closed-overlap probes: full L1 parity on 18.3 wasm; L2 substitution (`spike_edge_open_head_idx`) proven with residual named (S4) |
| Safe no-overlap partition (logical edge identity) | DB enforces per `logical_key` (S3/S5) | pure digest fixtures: symmetric collapse, asymmetric distinctness, qualifier permutation collapse, scope partition incl. none/some (S2) |
| Bounded endpoints, no arbitrary/dangling kinds | same DDL applied on PG | S3 probes: FK + bounded CHECKs (S2 §endpoints) |
| Atomic one-transaction supersession, deterministic races, no last-writer-wins | S5 races A/B/C with two real connections: deterministic winner/loser, one open head, intact lineage | sequential loser-path equivalents (documented single-connection substitution) |
| Migration shape through the repo-owned db-admin path | S5 test 1 (real `migrate()`, isolated journal schema) | S3: apply + journal idempotence + catalog probes |
| Restart/migration survival | durable server (substitution: n/a by nature) | S4 dataDir close/reopen: data, lineage, dispositions, as-of answers, live constraint |
| Durable rejected-verdict disposition | same DDL applied on PG | S3 disposition fixture + vocabulary CHECK |
| Cycle prevention application-side unless simple DB mechanism | none exists (S6, both lanes' DDL identical) | S6 probe: cycle constructible via raw UPDATE, detectable via recursive CTE only |
| Pre-code provenance/license inventory, no donor runtime dependency | — (not lane-bound) | S1: PASS, live pin `v0.29.2` @ `ff7e29cc`, no upstream NOTICE, zero mined-location drift, zero-hit runtime-dependency proof |

## Spike evidence index

- [S1 — provenance/license inventory](./p0/s1-provenance/NOTES.md) — PASS
- [S2 — logical identity + bounded endpoints](./p0/s2-identity-endpoints/NOTES.md) — PASS
- [S3 — DDL prototype + migration shape](./p0/s3-postgres-ddl/NOTES.md) — PASS
- [S4 — PGlite parity + substitution + restart](./p0/s4-pglite-parity/NOTES.md) — PASS
- [S5 — concurrent supersession determinism](./p0/s5-concurrency/NOTES.md) — PASS
- [S6 — cycle-enforcement decision](./p0/s6-cycles/NOTES.md) — DECIDED (app-side)

Spike artifacts (disposable, committed for review): suite + fixtures under
`packages/drivers/pglite/test/integration/spike/` and
`.../test/integration/fixtures/epistemic-bitemporal-spike{,-noext}/`; the only
production-file wiring change is two `@beep/pglite` devDependencies
(`@beep/postgres`, `@electric-sql/pglite`). The suites deliberately do NOT live under
`packages/_internal/db-admin/test/` — that entire tree is an architecture-proof
generated surface (the repo-cli operation-plan test sweeps db-admin `src/`, `test/`,
`dtslint/`, and `drizzle/` and requires every file to be generator-reproducible), which
CI proved by failing on the first placement.

## P1 handoff

The ratified enforcement design and every named P1 decision (kit-extension vs Exception
Ledger, test-utils extensions seam, disposition vocabulary, endpoint FK completion,
three-place migration registration, notices entry landing, retirement-record surfaces)
are recorded in [`ops/handoffs/p0-to-p1-handoff.md`](../ops/handoffs/p0-to-p1-handoff.md).
