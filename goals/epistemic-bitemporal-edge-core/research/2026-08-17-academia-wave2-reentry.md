# Academia wave-2 re-entry notes — edge substrate successors

> Routed 2026-08-17 from `explorations/academia-corpus-mining` wave-2 synthesis
> (`synthesis/wave2-synthesis.md` in the machine-local academia-2026-07 research corpus; paper IDs below are
> `id`-keyed into that corpus's `text/`, `meta/`, and `notes/` directories,
> now also carrying DOIs/OA PDFs where recoverable via `resolved-index.jsonl`).
> Routing ratified by the operator 2026-08-17; proposals never auto-enter packets.

This goal is completed-retained; these findings are **re-entry material for
successor work**, not reopened requirements.

## 1. Edge provenance distinctness (high)

Keep observed, extracted, and inferred edges distinct; gate completion by
predicate and topology; retain derivation DAGs. Logical expansion raises
recall but can reduce precision and recursively amplify prior predictions.
Evidence: `68833ada82df`, `6b8bc18b69e7`. Successor candidate:
`explorations/legal-inference-policy` (spawned from this same routing).

## 2. Source-local graphs, reversible fusion (high)

Preserve source-local graphs and entities; make alignment candidates explicit
and fusion reversible. Early fusion under incomplete identity truth
propagates false links. Evidence: `2754739530de`, `0a8de1437753`,
`e80ae01d401d`. Shared with `goals/identity-iri-fibered`.

## 3. Confidence-signal separation (high)

Declare source trust, source-assertion confidence, extractor confidence,
validator score, and reviewer disposition as separate fields — they arise at
different stages and are not mutually calibrated. Evidence: `0a8de1437753`,
`b7ab12db479a`. Successor candidate:
`explorations/evidence-source-policy-calibration`.

## 4. Literal preservation (medium)

Preserve literal lexical forms, units, coordinates, statement IDs,
qualifiers, references, and transformation lineage beneath normalized
features. Evidence: `24346fc59983`, `393357d5bc4f`. Shared with
`goals/file-processing-capability`.
