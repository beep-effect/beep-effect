# P0 research evidence

Date: 2026-08-27

## Upstream state

- Root catalog: `@effect/ai-openai` `4.0.0-rc.112`.
- Effect reference commit: `dd99ab007e3352761187dae330d52f65feeff7c0`.
- `OpenAiEmbeddingModel.model(model, { dimensions })` merges the embedding
  Layer with `EmbeddingModel.Dimensions`.
- `OpenAiEmbeddingModel.layer(...)` provides only `EmbeddingModel` and requires
  `OpenAiClient`.
- `OpenAiLanguageModel.layer(...)` provides `LanguageModel` and requires
  `OpenAiClient`.
- `OpenAiClient.layerConfig(...)` returns a config-error Layer requiring
  `HttpClient`; the driver passes only `apiKey`.
- The client posts to `/responses` and `/embeddings` beneath the default OpenAI
  API URL.

The rc.112 source preserves the packet's original rc.111 composition contract.

## Defaults

- Language model: `gpt-4o-mini`.
- Embedding model: `text-embedding-3-small`.

Both ids appear in the current upstream `Model` domains and in upstream model
tests.

## Scaffold proof

The required dry run and write command both succeeded:

```sh
bun run beep create-package openai --family drivers --description "Product-neutral Effect driver for OpenAI."
```

The generator created `packages/drivers/openai`, registered `$OpenaiId`, added
the root workspace and TypeScript entries, refreshed `bun.lock`, and ran the
repo sync steps.
