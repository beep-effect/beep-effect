# P0 storage cutover — operational runbook log

Date: 2026-08-07 (01:43–02:10 CDT)
Operator: Claude (Fable), supervised session; design doc runbook §9 followed stepwise.
Code: branch `feat/evidence-loop-p0-and-fixes` (this PR).

## Result

The canonical ai-metrics store moved atomically from
`/home/elpresidank/YeeBois/projects/beep-effect/.beep/ai-metrics` (clone-owned) to
`/home/elpresidank/.local/state/beep/ai-metrics` (XDG state root). All census gates
passed byte-identical, the store serves through both old and new code paths, and all
three P0 bullets are demonstrated live against the moved store.

## Census (hard gate: byte-identical pre/post)

| Metric | Pre (01:43:26) | Post (01:43:43) |
| --- | --- | --- |
| Files | 8,433 | 8,433 |
| Exact bytes (`du -sb`) | 21,378,995,358 | 21,378,995,358 |
| `raw/` files | 8,364 | 8,364 |
| DuckDB bytes | 3,373,805,568 | 3,373,805,568 |
| config-snapshot JSONs | 28 | 28 |

Same-filesystem rename: both parents on device 53 (`/dev/nvme0n1p2`, btrfs on
`/home`), 2.3T free — single atomic `rename(2)` via `mv -n`. The design's historical
expectation of 29 snapshot JSONs read 28 at execution time (one manifest aged out
between design and execution); the pre/post identity is the gate, and it held.

## Secrets: preserved, proven

`~/.config/beep/ai-metrics.env` was backed up (`.pre-p0`) before anything else, the
rendered install commands that truncate it (`install -m 0600 /dev/null …` + two
`op read` lines) were **skipped**, and `cmp` against the backup proved
`BEEP_AI_METRICS_HASH_SALT` and `BEEP_AI_METRICS_RAW_ARCHIVE_KEY` byte-untouched.
`BEEP_AI_METRICS_DATA_ROOT=/home/elpresidank/.local/state/beep/ai-metrics` was
**appended** (rung-2 belt for every clone; the unit also passes `--data-root`).

## Units: re-rendered from this branch, argv-mirrored

`bun run beep ai-metrics forwarder timer` rendered both units with the installed
unit's exact argv (including `--otlp`, which the design's mirror command had
omitted — mirroring the live `ExecStart` caught it) plus the absolute data root.
Verified before enable: `WorkingDirectory=/home/elpresidank/YeeBois/projects/beep-effect`
kept (it is the canonical repoRoot), zero `.beep/ai-metrics` occurrences, timer
content-identical to pre-p0. Unit backups taken alongside the env backup.

## Verification runs (never dual live stores; old root confirmed absent throughout)

1. **DuckDB open check** — `retention list --data-root <new root>` from this branch:
   exit 0, decodable inventory (derived exports + raw archive objects).
2. **Supervised unit run** (old code in the beep-effect clone, new root):
   `Result=success`, status `ok`, ingestRunId `forwarder-1786085130491`,
   100 source files / 51,191 turns / 28 archive objects. Proves the store serves
   pre-P0 code via the explicit `--data-root` flag.
3. **Branch-code run** (new code, same canonical `--repo-root`): status `ok`,
   51,224 turns. This run activated bullets 2 and 3:
   - `identity/registry.json` created — 1 canonical root, 2 source instances.
   - Bounded config snapshot: **fileCount 25,741 → 256**, totalBytes 740,014,
     `stageTimings` 5, `excludedNestedRootPaths` = the two `.claude/worktrees/*`
     linked worktrees, `skippedOversizeFileCount` 0, and
     `diff.removedPaths` = **25,485 — exactly the design's predicted first-run
     number. This is the bounding fix landing, not data loss.**
   - `bounds.truncated: true, truncationReason: "max-depth"`: one config tree runs
     deeper than the depth budget (8); the bound recorded itself instead of walking
     it. Working as designed — a bounded walk that says so beats an unbounded one.

Timer restarted and enabled; next run 07:50 CDT.

## Residual (deployment propagation, not cutover work)

The installed unit executes code from the beep-effect clone's checkout. Until that
clone pulls a `main` containing this PR, timer runs use pre-P0 code against the new
root: correct ingestion (proven above), but **unbounded** snapshots and no registry
upserts, alternating with bounded ones from any branch-code run. One operator step
clears it after merge: `git -C ~/YeeBois/projects/beep-effect pull`. No compatibility
symlink exists and none is needed; legacy evidence is preserved in place inside the
moved store.

## Rollback boundary

Passed. A run has been written into the new root, so per runbook §9.2 the new root
is the only complete store — fix forward. (Before that point, rollback was a single
reverse `mv` plus the `.pre-p0` unit/env restores, which remain on disk.)
