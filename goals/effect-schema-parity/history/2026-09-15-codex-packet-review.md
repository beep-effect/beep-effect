# Codex packet review — 2026-09-15 (read-only, gpt-6-astra medium)

Adversarial fidelity review of the freshly graduated packet against `explorations/effect-schema-parity/{BRIEF,MAP,DECISIONS}.md`. Every finding below was addressed the same day; see the re-review file for per-finding resolution evidence.

Verdict: **Not faithful enough to execute unchanged.** The packet contains a wire-format contradiction, weakens per-retirement performance acceptance, and omits or leaves unresolved several implementation rulings.

| Severity | File:line | Finding | Suggested fix |
| --- | --- | --- | --- |
| blocking | `goals/effect-schema-parity/SPEC.md:131` | **The boundary table prescribes an encoding change on a persisted/HTTP boundary.** The DateTimeUtcFromValid row says “tagged ISO form becomes plain ISO or millis.” This directly contradicts the byte-identity constraint at `SPEC.md:84–86`, its own table rule at `SPEC.md:123–124`, and the source ruling at `explorations/effect-schema-parity/DECISIONS.md:280–287`. The working implementation really does encode a tagged value: `packages/foundation/modeling/schema/src/DateTimeUtcFromValid/DateTimeUtcFromValid.schema.ts:451–452`, documented at `:459–461`. This is an inherited research migration recipe that the later wire-shape ruling forbids applying to persisted or served data. | Remove the prescribed tagged-to-primitive conversion from the persisted/HTTP row. Require a consumer-specific composition preserving the existing encoded representation; if that cannot be done within the retirement contract, record the boundary as excluded migration work. Confine any primitive-shape conversion to verified in-memory boundaries. |
| blocking | `goals/effect-schema-parity/SPEC.md:117`; `goals/effect-schema-parity/SPEC.md:166`; `goals/effect-schema-parity/SPEC.md:173`; `goals/effect-schema-parity/GOAL.md:50` | **The executable phase contract weakens performance acceptance from every retirement PR to P2/P5.** The source explicitly requires no regression in instantiations **or check time on any of the three packages for each retirement PR** (`explorations/effect-schema-parity/DECISIONS.md:334–340`). The decision table binds “every change carries a number” only to P2/P5 and the three-package acceptance only to P5. P3’s done-signal omits performance, its per-PR checklist omits it (`PLAN.md:103–109`), and the launcher explicitly requests receipts only for P2/P5. The general constraint at `SPEC.md:89–90` does not restore the missing per-PR comparison and rejection criterion. | Add before/after diagnostics and three-package non-regression to every P3 retirement PR’s acceptance and bounce conditions. Bind the performance decisions to P3 as well, and replace the launcher’s P2/P5-only instruction with a reference to the complete per-PR requirement. |
| should-fix | `goals/effect-schema-parity/SPEC.md:115`; `goals/effect-schema-parity/SPEC.md:163–178`; `goals/effect-schema-parity/PLAN.md:48–63` | **The Knowledge layer ruling is neither back-linked nor fully implemented by the phase contract.** That ruling requires prompt context containing signatures, JSDoc sections and examples, plus graft-led upstream context (`explorations/effect-schema-parity/DECISIONS.md:50–57`); the orchestration ruling further requires inventory rows plus graft output (`:133–136`). The plan moves the prototype unchanged, preserves its current contract, and generates prompts from its rows. But that prototype explicitly contains truncated signatures, a summary and an example-presence flag (`explorations/effect-schema-parity/research/inventory/README.md:14–16`), and explicitly says it omits full JSDoc sections and example bodies and is **not** the full prompt context required by the Knowledge layer decision (`:42`). There is no phase requirement to close that gap. | Back-link “Knowledge layer” and specify how lane prompts obtain the missing documentation, examples and upstream structural context. Either extend the extraction/prompt pipeline or record a source-backed superseding decision; relocating the bounded prototype alone must not satisfy the original deliverable. |
| should-fix | `goals/effect-schema-parity/SPEC.md:115`; `goals/effect-schema-parity/PLAN.md:19`; `goals/effect-schema-parity/PLAN.md:62–63` | **The chosen first slice loses an observable exit requirement.** MAP defines P0 plus P1 as the first vertical slice and requires that a Codex prompt for one Role A module can be generated from the rows (`explorations/effect-schema-parity/MAP.md:143–157`). PLAN mentions prompt templates as work, but neither its P1 exit criteria nor SPEC’s P1 done-signal requires generating or verifying a prompt. P1 can therefore be declared complete with fixture/verifier tests alone. | Add the one-module generated-prompt proof to P1’s done-signal and verification evidence, and identify P0/P1 as the first vertical slice. Include the context requirements from the Knowledge layer ruling in that proof. |
| should-fix | `goals/effect-schema-parity/SPEC.md:195–197`; `goals/effect-schema-parity/PLAN.md:101` | **The unconditional Role B deletion contract does not resolve MAP’s HttpStatus exception.** SPEC requires all six Role B concepts to be deleted, and PLAN puts HttpStatus in the retirement group. However, `explorations/effect-schema-parity/MAP.md:85` explicitly says “HttpStatus keeps its MappedLiteralKit named codes after the P2 trim.” The earlier Role B decision requires all six to retire and HttpStatus codecs to become value-module lookups (`DECISIONS.md:241–246`). The source surfaces conflict here, and graduation silently selects deletion without explaining whether the named-code API survives elsewhere. | Resolve this specific source conflict in the goal decision log. State whether HttpStatus’s named codes are removed, migrated into consumer-owned domains, or retained under an explicitly amended disposition. Align group G and the deletion acceptance criterion with that ruling. |
| should-fix | `goals/effect-schema-parity/SPEC.md:126–137`; `goals/effect-schema-parity/SPEC.md:198–199` | **The boundary-table scaffold omits two concepts covered by its own completion requirement.** Acceptance requires rows for every group D/E concept, but PLAN includes Timezone in D (`PLAN.md:98`) and ArrayOf in E (`PLAN.md:99`), neither of which appears in the table. MAP includes both and places those groups behind the boundary-table gate (`explorations/effect-schema-parity/MAP.md:82–83`). Filling every existing `TO FILL` cell would still leave the declared gate incomplete. | Add Timezone and ArrayOf rows, or explicit evidence-backed “no persisted/served boundary” dispositions. Require the table’s coverage to reconcile against the complete D/E concept lists before either PR opens. |
| should-fix | `goals/effect-schema-parity/SPEC.md:163–178`; `goals/effect-schema-parity/PLAN.md:28–44` | **The Doctrine surface ruling disappears without an explicit disposition.** The source requires an entry in `standards/architecture/DECISIONS.md` as well as the schema README rule (`explorations/effect-schema-parity/DECISIONS.md:182–190`). P0 only treats that decision log as a precedent and ships the §11 entry and standing-rule prose. The later doctrine-ahead ruling specifies the §11 entry and sequencing (`DECISIONS.md:348–360`), but does not explicitly cancel the architecture decision-record requirement. Neither the missing ruling nor its supersession is back-linked in SPEC. | Back-link “Doctrine surface” and explicitly reconcile it with “Doctrine lands ahead.” Either include the architecture decision entry in P0 or record that the later §11 ruling replaces that deliverable. |
| should-fix | `goals/effect-schema-parity/SPEC.md:163–178`; `goals/effect-schema-parity/research/SOURCES.md:98` | **The Evidence retention ruling is omitted from both the decision log and phase contract.** It prescribes retention of reports, generators, inventory and small proofs, and deletion of three named large generated receipts (`explorations/effect-schema-parity/DECISIONS.md:317–321`). No SPEC row or phase carries it. The working tree already has those three deletions staged, but the packet does not preserve the policy governing what lands with it. SOURCES’ claim that SPEC back-links “every ruling by heading” is therefore false, independently of the other omitted rulings. | Add the Evidence retention back-link and identify its application to packet publication and P1’s inventory move. Correct the “every ruling” claim unless the decision log becomes exhaustive, including explicit supersession or historical-only dispositions where appropriate. |
| should-fix | `goals/effect-schema-parity/SPEC.md:107–110`; `goals/effect-schema-parity/SPEC.md:163–178`; `goals/effect-schema-parity/PLAN.md:14–23` | **The approved appetite is dropped.** Six phases survive, but the roughly six-week bound and intention to land before the next RC bump forces re-audit are absent from the goal’s contract and plan. They are explicit in `explorations/effect-schema-parity/BRIEF.md:39–48` and `DECISIONS.md:432–440`. SPEC preserves the mid-train bump procedure at `:236–237`, but that is only the contingency, not the approved appetite. “Appetite and shape sign-off” is also absent from the decision table. | Carry the appetite into SPEC as a planning constraint and back-link the ruling. Preserve the existing bump contingency without presenting it as a substitute for the agreed time/scope bound. |
| should-fix | `goals/effect-schema-parity/ops/manifest.json:7`; `goals/effect-schema-parity/ops/manifest.json:13`; `goals/effect-schema-parity/README.md:5` | **The lifecycle contradicts the packet’s explicit execution hold.** The manifest and README say `active`, but `PLAN.md:7–10` says execution opens only after the packet and source exploration land on main. `README.md:37–41` nevertheless directs the next session to start P0. The standard defines `active` as execution open and `paused` as including authored-but-not-started packets with explicit resume conditions (`goals/README.md:104–109`). This is not a complaint about phase status `pending`, which the template permits; it is a conflicting lifecycle declaration. | Use `paused` with the main-landing resume condition until that condition is met, then activate the packet; alternatively, remove the execution hold only if execution is actually authorized to begin. Make README’s next action agree with the selected state. |
| nit | `goals/effect-schema-parity/PLAN.md:83–85`; `goals/effect-schema-parity/PLAN.md:133–135` | **The performance receipt pointer identifies a different measurement sample from the baseline being committed.** PLAN points measurement readers to `research/performance-baseline.md`, whose check times are 0.427, 3.628 and 0.645 seconds (`explorations/effect-schema-parity/research/performance-baseline.md:60–64`). The packet’s selected 0.529, 3.717 and 0.659 seconds come from `performance-verification-supplement.md:34–38`. The selected numbers agree with DECISIONS, but their provenance is ambiguous at the execution point. Both reports also require fresh build-info for comparable runs (`performance-baseline.md:52`; `performance-verification-supplement.md:40`). | Point the committed baseline explicitly to the verification supplement’s selected sample and commands. Preserve the same-compiler/fresh-build-info measurement conditions beside the before/after instructions. |

Check 1 — No findings: all seven BRIEF no-gos are represented in SPEC’s non-goals; the rabbit-hole controls are carried into constraints, gates, sequencing or explicit supersession references.

Check 2 — No additional findings concerning the reopened LiteralKit ruling: no instruction was found to retire LiteralKit or MappedLiteralKit wholesale, implement F01 as a detector, or replace the AGENTS.md LiteralKit rule. The omissions and conflicts are listed above.

Check 3 — No additional findings concerning the P0 → P5 ordering or selective-statics merge gate. The facet-census gate is present; boundary-table coverage and first-slice acceptance findings are listed above.

Check 4 — No additional manifest findings: JSON parses; template top-level fields are present; P0–P5 IDs match PLAN; every `currentSourceOfTruth` and packet-relative `researchReports` path exists; provenance matches `explorations/effect-schema-parity/ops/manifest.json:12–15`.

Check 5 — No additional launcher-form findings: GOAL.md is **3,382 characters**, references the packet documents, and delegates detailed acceptance to SPEC. Its narrowed performance instruction is covered above.

Check 6 — No additional disproved counts or inspected source-line references: DECISIONS contains **25 entries**, and the prototype inventory contains **2,105 rows across 14 JSONL files**. The false “every ruling” claim and performance-provenance mismatch are listed above.
