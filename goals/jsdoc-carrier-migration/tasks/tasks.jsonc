// JSDoc carrier migration task graph.
//
// Ordering is load-bearing, not stylistic:
//   P0 legalises P3 (the law currently forbids mass migration at
//      .patterns/jsdoc-documentation.md:58),
//   P1 builds what P3 runs and produces the residue count P3's zero claim depends on,
//   P2 keeps P3 from planting a codegen mine.
{
  "schemaVersion": "jsdoc-carrier-migration/tasks/v1",
  "updated": "2026-08-06",
  "rankingMethod": "P0 first because it makes P3 legal and is docs-only (no gate exposure). P1 before P2 because the codemod's dry run is the only way to learn the true residue count. P2 before P3 so P3 touches hand-authored code only. P3 is atomic. P4 lands with P3.",
  "tasks": [
    {
      "id": "jcm-001",
      "rank": 1,
      "phase": "P0",
      "status": "done",
      "title": "Fix the law contradiction in .patterns/jsdoc-documentation.md",
      "detail": "Rewrite the Transitional carrier policy section (:54-68). Delete the stale Phase-P2 claim — that changed-files check shipped and goals/effect-jsdoc-quality is completed-retained. Replace 'grandfathered' (:56) with the real enforcement semantics: a legacy carrier anywhere in a changed non-generated source file is a hard CI failure today. Remove the ':58' prohibition on mass-migrating untouched files and explicitly authorize this initiative's one-time migration. State the post-migration target so P3's final flip executes written law. Keep AGENTS.md and this law in agreement."
    },
    {
      "id": "jcm-002",
      "rank": 2,
      "phase": "P0",
      "status": "done",
      "title": "Land the goal packet",
      "detail": "goals/jsdoc-carrier-migration/ with SPEC.md as anchor, research/{SOURCES,decisions-locked,corpus-census}.md, ops/{manifest,progress}.json, this task graph. Docs-only PR together with jcm-001 so the cleanup-on-touch gate is not provoked."
    },
    {
      "id": "jcm-003",
      "rank": 3,
      "phase": "P1",
      "status": "pending",
      "blockedBy": ["jcm-001"],
      "title": "beep quality jsdoc-migrate extract",
      "detail": "New subcommand in packages/tooling/tool/cli/src/commands/Quality/internal/, registered in the existing jsdoc-* family. ts-morph walks packages/**/src/**/*.{ts,tsx}, emitting one extract.jsonl record per affected doc block: path#symbol anchor, symbol kind (value-level vs pure type-level per the kind-split Example law), lead paragraphs, fence bodies, existing tags, and byte offsets for the block span. Anchors are path#symbol#ordinal, NEVER content hashes and never line numbers. path#symbol alone collides: overloads, declaration merging, default exports, and same-name type companions for runtime schemas (documented law in .patterns/jsdoc-documentation.md) all share a name. ordinal is the 0-based index among blocks sharing an anchor in source order, 0 in the unique case. extract MUST fail loudly on a duplicate anchor — a title applied to the wrong block is well-formed and the conservation law cannot catch it."
    },
    {
      "id": "jcm-004",
      "rank": 4,
      "phase": "P1",
      "status": "pending",
      "blockedBy": ["jcm-003"],
      "title": "Conservation law + proof manifest",
      "detail": "Export documentationShapeViolations within the package (not across package boundaries). Implement BOTH clauses of SPEC 5.3. (a) Content: every fence's code bytes identical, prose tokens a subset of output, added prose limited to section markers plus data-sourced strings from titles.jsonl (title, @see purpose phrase) — the codemod never invents prose. (b) Tags: only the closed allowlist may be rewritten (@template->@typeParam, @module->@packageDocumentation, @default->@defaultValue, {type} removal, @returns/@throws hyphen, canonical order, @see purpose phrase, and the two carrier consumptions); every other tag is bytes-identical, every rewrite must match its normal form exactly, and no tag may be dropped without a consuming rule. Do NOT implement this as blanket 'tags bytes-identical' — that contradicts the D4 grammar fixes and would quarantine every free win. Violations quarantine rather than write. Pair it with the shape oracle: accept a rewrite only when documentationShapeViolations' finding set shrinks or stays equal. Record results in a schema-versioned proof manifest following the DocgenProofManifest / AcceptedProofManifest idiom. Conservation is computed on POST-FORMAT bytes — biome first, then verify, or reflow reads as content mutation."
    },
    {
      "id": "jcm-005",
      "rank": 5,
      "phase": "P1",
      "status": "pending",
      "blockedBy": ["jcm-004"],
      "title": "beep quality jsdoc-migrate apply",
      "detail": "Text-surgical rewrite by byte offset. ts-morph is for analysis only and must never reformat a block — byte conservation becomes unprovable if it does. Consumes titles.jsonl and overrides.jsonl. Idempotent: re-running over already-migrated files is a no-op. Transform set per SPEC 5: carrier swap, @template->@typeParam, @module->@packageDocumentation, @default->@defaultValue, {type} blob removal, @returns/@throws hyphen, canonical tag order, lead splitting at leadEnd, @see purpose phrases."
    },
    {
      "id": "jcm-006",
      "rank": 6,
      "phase": "P1",
      "status": "pending",
      "blockedBy": ["jcm-004"],
      "title": "beep quality jsdoc-migrate titles",
      "detail": "effect/unstable/http against http://127.0.0.1:8317 with model grok-4.5 — never node:http, and never the xAI API directly (that bills credits instead of the plan). Returns data only: { anchor, title, remarks: details|gotchas, leadEnd }. Append-only titles.jsonl with per-anchor resume (skip anchors already present) and retry only on schema-invalid returns. Not a Workflow: the 1,000-agent cap is below the 1,935 files."
    },
    {
      "id": "jcm-007",
      "rank": 7,
      "phase": "P1",
      "status": "pending",
      "blockedBy": ["jcm-005"],
      "title": "Fixture corpus + full-corpus dry run",
      "detail": "Fixtures covering every recognised block shape, including the pathological ones: unfenced example (expect quarantine — a fence is a third addition), multi-example block (19 exist, max 5, the only place title uniqueness binds), @remarks + @example together (464 blocks), multi-paragraph lead (872 blocks), block already carrying **Example** sections (expect no-op), and ANCHOR COLLISIONS — a runtime schema plus its same-name type companion in one file, an overload set, and a merged declaration, each expecting distinct ordinals and a loud failure if extract would emit a duplicate anchor. Also cover each allowlisted tag rewrite and at least one off-normal-form rewrite (expect quarantine). Then dry-run the full corpus and record the true residue count into ops/progress.json measurements.residueCount. P3's zero claim is not committable until this number exists."
    },
    {
      "id": "jcm-008",
      "rank": 8,
      "phase": "P2",
      "status": "pending",
      "blockedBy": ["jcm-001"],
      "title": "Fix the 9 repo-owned generator templates",
      "detail": "Emit **Example** (Title) sections instead of @example. Mechanical titles are acceptable in generated output. Targets: drivers/{box,acp,runpod,ecfr,gov-legal-mcp} (bun run generate), foundation/primitive/data and foundation/modeling/rdf (beep sync-data-to-ts), foundation/modeling/html (bun run generate), tooling/library/ai-sync (package-local emitter). Then regenerate the 18 files. Independent of P1 — can run in parallel."
    },
    {
      "id": "jcm-009",
      "rank": 9,
      "phase": "P3",
      "status": "pending",
      "blockedBy": ["jcm-006", "jcm-007", "jcm-008"],
      "title": "Run the title pass and freeze titles.jsonl",
      "detail": "All 13,265 blocks. Spot-check title quality across families before freezing — once P3 opens, titles.jsonl is an input, not a working file."
    },
    {
      "id": "jcm-010",
      "rank": 10,
      "phase": "P3",
      "status": "pending",
      "blockedBy": ["jcm-009"],
      "title": "Resolve residue into overrides.jsonl until apply is clean",
      "detail": "Starts with the 114 unfenced examples, which auto-quarantine by construction, plus whatever jcm-007 measured. Each override carries full replacement block text. NO HAND-EDITS ON THE BRANCH — residue fixes are data, or the branch stops being f(main, codemod, titles.jsonl, overrides.jsonl) and D3's regenerability is forfeited."
    },
    {
      "id": "jcm-011",
      "rank": 11,
      "phase": "P3",
      "status": "pending",
      "blockedBy": ["jcm-010"],
      "title": "Swap the gate to a repo-wide zero-legacy check",
      "detail": "Replace enforceTouchedFileCleanup with a check that fails on any @example or @remarks in packages/**/src. Once the corpus is zero, 'touched' is a meaningless qualifier: the repo-wide form needs no git diff work, is faster, and cannot be evaded. Ship TWO scopes: default non-generated (SPEC 7 row 4) and --include-generated (SPEC 7 row 6). The generated scope is not optional — without it, 'generated output stays compliant' would be proved by re-running a check that skips generated files, which passes vacuously. Same PR as the migration."
    },
    {
      "id": "jcm-012",
      "rank": 12,
      "phase": "P3",
      "status": "pending",
      "blockedBy": ["jcm-010"],
      "title": "Rewrite the totals baseline and delete the transitional law section",
      "detail": "bun run beep quality jsdoc-inventory then jsdoc-ratchet --write-baseline for standards/jsdoc-totals.regression-baseline.jsonc. Delete the .patterns/jsdoc-documentation.md transitional carrier section — after migration it has no remaining subjects. Regenerate the docgen proof manifest, which goes stale on a change this size."
    },
    {
      "id": "jcm-013",
      "rank": 13,
      "phase": "P4",
      "status": "pending",
      "blockedBy": ["jcm-011", "jcm-012"],
      "title": "Close the packet",
      "detail": "Reflection under history/reflections/ via the reflect skill, manifest lifecycle flipped to completed-retained, progress.json finalised — all in the same PR as P3 per the same-PR packet-state-flip law. The completion gate clears only if the merged commit cites the packet slug."
    }
  ]
}
