# S4 operator move — 2026-09-25

Executed by the orchestrating Fable session with the operator present (PLAN P1 S4), after S1–S3
landed on `feat/refs-workspace-impl` (`7da31d1620`, `17b24c9e1d`, `0e957c47b0`, `b3aca46988`).

## Preconditions (all held)

- `~/YeeBois/dev/effect` on `main` at `2a8f5c748d`, clean once `graft/` and `.graft/` were
  excluded; linked worktree `~/YeeBois/dev/effect-worktrees/docgen-enforce-examples` clean, 0/0
  against its upstream.
- `~/YeeBois/dev/effect-tsgo` on `main` at `d6758e71`, clean.
- `~/YeeBois/references` absent; same filesystem, so the move is a rename.
- CLIProxyAPI `/v1/models` lists `claude-opus-5`; `mise trust --show` clean for the owner;
  `~/.config/beep-graft/env` present.

## Steps and evidence

1. `printf 'graft/\n.graft/\n' >> <member>/.git/info/exclude` for both members.
2. `mv ~/YeeBois/dev/effect ~/YeeBois/references/effect/effect`;
   `mv ~/YeeBois/dev/effect-tsgo ~/YeeBois/references/effect/effect-tsgo`.
   A session-only symlink `~/YeeBois/dev/effect -> references/effect/effect` was left so the
   orchestrating shell (whose working directory was the old path) kept working; it is removed at
   session end (R1).
3. `git -C ~/YeeBois/references/effect/effect worktree repair ~/YeeBois/dev/effect-worktrees/docgen-enforce-examples`
   → `git worktree list` shows both; the linked worktree is clean and its history intact.
4. Pruned the stale 2026-09-12 pair `graft/.cache/extract.496c9d83e6eb3668.json` and
   `fingerprint.496c9d83e6eb3668.json`.
5. `GRAFT_NO_GITIGNORE=1 graft build ~/YeeBois/references/effect` (structural):
   `effect/: 19937 nodes, 40629 edges, 1679 cards`; `effect-tsgo/: 5908 nodes, 11192 edges,
   1153 cards`; `workspace: 2 repos federated → graft/workspace.json`.
   `graft check` → `effect/: OK`, `effect-tsgo/: OK`. Neither member's `.gitignore` changed.
   graft also wrote `.ignore` into `effect-tsgo` (ripgrep re-admit); added to that member's
   exclude, and the refresh/provisioner now write all three excludes themselves (`b3aca46988`).
6. Fleet relink with `scripts/setup-effect-ref.sh` over every `beep-effect*` clone and
   `*-worktrees/*` checkout: 152 checkouts, 0 script failures. Census after relink:
   140 checkouts carry all three links; 12 keep a pre-existing **vendored** `.repos/effect`
   directory (≈45 MB, no `.git`) that the provisioner warns about and skips by design. They got
   `.repos/effect-tsgo` and `.repos/effect-workspace` only. See "Follow-ups".
7. `bun run beep refs install-timer --owner <refs-workspace-impl worktree>` rendered
   `beep-refs-refresh.{service,timer}` (`OnCalendar=*-*-* 03:30:00`, next run 2026-09-26
   03:32 CDT, `EnvironmentFile=~/.config/beep-graft/env`, `ExecStart=… beep refs refresh --root
   ~/YeeBois/references/effect --jobs 16`). `systemctl --user start beep-refs-refresh.service`
   was issued once at 07:39:56 CDT; the journal shows the frozen-lockfile install and
   `graft build --deep --allow-partial -j 16` running under the unit. Deep-tier evidence is
   recorded the next morning from `~/.local/state/beep/refs/last-refresh.json`.

## Verification through the beep links

- From `~/YeeBois/projects/beep-effect2`: `graft ask "Effect.fn vs fnUntraced"
  .repos/effect-workspace` → federated hits labeled `[effect-tsgo/]` / `[effect/]`;
  `graft ask "Schema.Class extend" .repos/effect` → `packages/effect/src/Schema.ts:L3609`.
- `bun run beep worktree new refs-link-probe` → summary lists the three `reference:` links;
  `graft ask` via the probe's `.repos/effect-workspace` answers; probe removed afterwards.

## Follow-ups

- **Owner re-point after merge.** The timer's owner is the implementation worktree because only
  it carried `beep refs` at install time. After the PR merges and `beep-effect0` pulls `main`
  (its 02:30 graft nightly does this), run a fresh
  `bun run beep refs install-timer --owner ~/YeeBois/projects/beep-effect0` before retiring the
  worktree. This is the operator's one sanctioned fresh install.
- **12 vendored `.repos/effect` directories** in stale lane worktrees
  (`beep-effect19-scratchpad-type-repairs`, `beep-effect2-goalshot2`, `beep-effect2-proof`,
  `beep-effect7-gitleaks-beta105`, `beep-effect9-ceiling`,
  `beep-effect18-worktrees/lint-policy-close-proof`, `beep-effect2-worktrees/p1-hook-writer`,
  `beep-effect2-worktrees/pr-531-lint-oom`, `beep-effect-worktrees/{p1-a-verdict,
  p1-deps-audit-fix, p3-semantic-delta-gate, recorded-qa-followup}`). Deleting a vendored tree
  is the operator's call; `rm -rf <checkout>/.repos/effect && bash scripts/setup-effect-ref.sh
  <checkout>` converts one.
- **Session symlink** at `~/YeeBois/dev/effect` is removed as the final step of the session.
