# CSF-003: Codex sessions now run unsandboxed without approval

- Severity: Low
- Codex ID: `0ba9252f73048191bc143b5732736004`
- Source commit: `6041ec475aee0c82baa39548e04ada39fa8d9cb6`
- Owner: repository Codex permission configuration
- Status: already fixed on current `main`

## Public Summary

An earlier repository configuration pinned Codex sessions to unrestricted
filesystem access without approval, overriding each contributor's own policy.

## Current-HEAD Validation

Already fixed by current-main commit `4ac6aa5cd6` (#876). The repository config
omits both permission keys, and the AI-sync safety gate rejects any future
repository pin, including a superficially safer pin that still overrides the
user's choice.

## Remediation

No change was required in this branch. The existing mainline fix is the proof
of record.

Changed files:

- `.codex/config.toml`
- `packages/tooling/library/ai-sync/src/validation.ts`
- `packages/tooling/library/ai-sync/test/ai-sync.test.ts`

## Verification

- `git merge-base --is-ancestor 4ac6aa5cd6 HEAD`
- The canonical Yeet proof covers the existing AI-sync regression tests.
