# Absorbed Constraints

## Agent-effectiveness sequencing

Do not add CI or scheduled enforcement for agent-effectiveness reporting until
the manual workflow exposed by the existing CLI doctor, dataset, experiment,
and sync commands has proved useful and repeatable.

## Deferred Yeet UX

The following optional improvements are **recorded-not-planned**:

- show unresolved review-thread counts and Greptile gate state in
  `yeet status --remote`;
- expose a canonical latest-run artifact pointer from status;
- define a repo-wide structured-command-summary convention beyond Yeet.
