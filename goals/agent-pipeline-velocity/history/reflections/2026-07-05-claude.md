---
goal: agent-pipeline-velocity
agent: claude
date: "2026-07-05"
trigger: closeout
confidence: high
findings:
  - category: tooling-friction
    confidence: high
    instruction: Make yeet verdict.json carry the failing step labels verbatim instead of a routed first-repair-path hint.
    explanation: Two verify failures were attributed to dual-arity while the real red steps were quality:lint/test/changeset-status (run 1) and quality:test (run 2); the runner output had the true labels all along. Cost two 7-14min runs of misdirected diagnosis.
  - category: tooling-friction
    confidence: high
    instruction: Treat architecture-lab package files (including AGENTS.md) as generation fixtures, never as deletable boilerplate; the PROOF ORACLE header now marks them.
    explanation: The nested-instruction surgery deleted lab AGENTS.md files and 8 operation-plan tests failed with ENOENT because the beep architecture planner reads the live lab tree as its accepted-proof source.
  - category: implementation-improvement
    confidence: high
    instruction: When measuring pipeline levers, benchmark before changing constants; task-level turbo concurrency was the wrong lever.
    explanation: Forced-cold check measured c=16 SLOWER than c=3 (27.3s vs 25.0s, +70% user time) because tsgo tasks are internally multi-threaded; the real win was structural - lint-policy steps grouped-concurrent went 142.4s sum to 32.3s wall (4.4x).
  - category: implementation-improvement
    confidence: medium
    instruction: Content-contract tests that pin skill prose (repo-configs effect-steering-guidance) should reference an extractable law block, not arbitrary body sentences.
    explanation: The effect-first-development restructure broke the test by condensing two pinned sentences; restoring them into the triage block worked, but the coupling between test and prose location is fragile by design.
  - category: goal-critique
    confidence: medium
    instruction: Budget an explicit verify-fix-verify loop when a goal rewrites agent-facing files at scale.
    explanation: Four verify iterations were consumed by consequences of the surgery itself (cspell words, steering contract, oracle fixtures, dual-arity on new code) - predictable classes of breakage for this goal shape, none present in the plan's time model.
  - category: codification-todo
    confidence: high
    instruction: Add a lint rule or fallow zone protecting files matched by accepted-proof manifests from deletion.
    explanation: Nothing machine-blocks deleting oracle fixtures today; the failure mode recurs the next time an audit hunts boilerplate.
todos:
  - Fix QualityIssueIndex verdict attribution to quote failing step labels (bug filed in history/rqt-ledger.md).
  - Successor goal - SkillOpt training pilot on one skill atop a scored repo-task eval set (Phoenix loop; absorbed phoenix-enrichment direction).
  - dual-arity check is now the lint-lane floor (30.6s); optimize only if the lane needs to drop below ~32s.
  - Execute incremental retirement of beep-effect2..7 clones via beep worktree as branches merge.
---

# Reflection — agent-pipeline-velocity (2026-07-05, claude)

## Summary

One exploration packet graduated into one goal and one PR that landed all five phases: greptile-only review lineup, read-only PR turbo cache, the last main-red fix (Release PR token), a ~150KB agent-context reduction with machine drift-gates, measured pipeline wins (lint-policy 4.4x), and the `beep worktree` command. Verdict: the plan's structure held; the cost model under-counted the self-inflicted breakage of rewriting agent-facing files, and instrumentation (rqt-013) paid for itself within the same session by naming the real failures.

## Tooling experience

- **Worked:** the grouped step runner already existed (parallelizing lint was a 3-line composition change); `beep skills update` self-healed the crispen lock drift; the architecture operation-plan tests caught fixture deletion precisely; per-step timing named real failures on first read after two misdirected runs.
- **Didn't:** verdict.json repair hints (misattributed twice); `tail`-piped background runs destroyed diagnostics until switched to `tee`.
- **Frustrating:** changeset-status only sees committed changesets (known gotcha, still cost a cycle); fractional-millis broke naive log extraction.
- **Wished existed:** a machine guard on accepted-proof fixture files; a single flag to rerun exactly the failed steps from a verify verdict.

## Implementation improvement opportunities

- Lint lane is now floor-bound by dual-arity (30.6s of a 32.3s wall) — the checker itself is the next lever if ever needed.
- The law-pulse hook is period-based (every 5th edit); a smarter version would key on files-touched-since-last-pulse.
- Worktree doctor could offer `--fix` for missing bootstrap files (it already knows the copy plan).

## Goal & prompt critique

The GOAL.md sequencing gate (PR #291 first) and Fable-direct mandate on Phase D were both load-bearing and correctly placed. Revision I'd make: add an explicit acceptance item "run verify after any change to agent-facing files consumed by tests/fixtures (skills, AGENTS.md, templates)" — the four verify iterations were all instances of that one missing loop.

## TODOs worth codifying

See frontmatter `todos`; the oracle-protection lint is the highest-value item.

## Lessons (confidence-tiered)

- **HIGH — Critical:** Before deleting "boilerplate" consumed by tooling, grep test fixtures and manifests for the paths — the architecture-lab AGENTS.md files were byte-compared generation oracles.
- **HIGH — Critical:** Trust runner step labels over routed repair hints, and always `tee` full logs from long background runs.
- **MEDIUM — Best practice:** Measure before moving concurrency knobs; internally-parallel tools (tsgo) invert the intuition. Structural sum-to-max conversions are where lane wall-time actually lives.
- **LOW — Consideration:** Skill-content contract tests are a hidden coupling class; inventory them (`rg -l 'skills/' packages/*/test`) before restructuring skills.
