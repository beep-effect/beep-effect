# P0d pinned primitives graph lane

P0c has passed. Continue the existing Codex session for P0d at explicitly medium
effort using gpt-daybreak-blue-latest. The P0c xhigh correction allowance was
specific to that phase; this phase starts at the requested medium setting.

You are not alone. Preserve all concurrent and prior edits. The orchestrator
owns packet status, decisions, all git operations, package verification and
publication. P0a lanes are finished and have no ownership. You are the only
source lane currently assigned to repo-cli. Never run git commands, including
read-only git, and never spawn agents. Create your report in the first actions
and append as you work. Final response is only its path. No work in P0e or later.

Read SPEC.md sections 0, 5.3 and 7; PLAN.md P0d; and the completed P0c verification
receipt before implementation. Refresh the local schema-first and Effect-first
skills and JSDoc rules as needed. Search live source and barrels before writing
helpers, including the existing EffectVitestPrimitive S.Class and artifact
decoder/formatter. Run the architecture command before adding a new role file.
Keep domain invariants in schemas, use typed errors and tagged unions, Effect
helpers and terse equivalent forms. Test imports use @beep aliases. New exported
multi-argument helpers need pipeable overloads. JSDoc examples must use supported
public facades, never private internal package subpaths.

Owned paths:

- standards/effect-vitest.primitives.jsonc
- packages/tooling/tool/cli/src/commands/Lint/Lint.schemas.ts
- packages/tooling/tool/cli/src/commands/Lint/Lint.errors.ts
- packages/tooling/tool/cli/src/commands/Lint/index.ts
- packages/tooling/tool/cli/src/commands/Lint/EffectVitest.ts
- packages/tooling/tool/cli/src/commands/Lint/internal/EffectVitestPrimitives.ts
- packages/tooling/tool/cli/src/commands/Lint/internal/EffectVitestPolicy.ts
- packages/tooling/tool/cli/src/commands/Lint/internal/EffectVitestScan.ts
- packages/tooling/tool/cli/src/commands/Lint/internal/EffectVitestStore.ts
- packages/tooling/tool/cli/test/effect-vitest-primitives.test.ts
- Existing packages/tooling/tool/cli/test/effect-vitest*.test.ts only for affected
  graph/pin/hint behavior; preserve all current expectations and rule fixtures.
- packages/tooling/tool/cli/test/fixtures/effect-vitest-rc112/** for the minimal
  portable pinned-source coverage fixture, with upstream license preserved.
- goals/effect-vitest-canon/history/lanes/p0d-primitives.md (your report).

The baseline, census and detector rows may be regenerated only through the exact
lint command after the graph is complete. Never hand-edit those artifacts. No
other packet docs, source packages, dependencies, lockfiles, global config,
timeouts, property floors, coverage baselines or existing tests are yours to
remove or weaken. Request a concrete ownership extension in your report if one
is necessary; continue independent owned work.

Authoritative pinned files are supplied in the absolute cache path in the launch
prompt. The orchestrator has compared their bytes to git show at the exact tag.
Use those files, not .repos/effect HEAD, installed dist anchors, or online HEAD.
Do not introduce absolute workstation/cache paths into production or CI tests.

Deliver the complete P0d contract:

1. The graph header pins package @effect/vitest, version 4.0.0-rc.112, tag
   @effect/vitest@4.0.0-rc.112 and SHA
   2600f62f4532026928454dcea8d1c48557b3f942. Reuse EffectVitestPrimitive S.Class
   for every entry and a schema-first complete document boundary. Apply meaningful
   invariants: unique IDs, valid line ranges, no dangling policy replacement IDs,
   and a reason in whenToUse when replaces is empty. Every nonempty replaces row
   names the corresponding repository candidate class and its applicability.
2. Cover every direct index.ts export, its Vitest namespace types/members,
   tester methods skip/skipIf/runIf/only/each/fails/prop, MethodsNonLive versus
   Methods, both layer signatures and options, every utils.ts export, and every
   README section. Handle export-star deliberately; do not invent a list of
   hundreds of upstream Vitest exports as if they were declared in this file.
   Record the re-export boundary and coverage method explicitly.
3. Also cover every charter API named in P0d: Effect.timeoutOrElse, raceFirst,
   scopedWith, fnUntraced, annotateLogs; Logger.layer and consolePretty;
   TestClock.adjust and withLive; Layer.mock. Anchor at the same tag. Existing
   policy IDs must resolve, including module.@effect/vitest, readme.testclock,
   readme.resource-safety and layer option IDs. No placeholder entries, missing
   signatures, invented line numbers, or partial graph presented as complete.
4. Each entry supplies every field in SPEC 5.3. Examples are self-contained and
   actually compile at rc.112, including type-only and option entries. Validate
   them using a reusable existing facility or a focused portable fixture; do not
   execute skip/only/fails examples as migration tests. Preserve upstream license
   when copying pinned source into a fixture. No local-reference dependency in CI.
5. Lint decodes the graph and uses its replaces edges to render remediation hints.
   A changed graph hint must visibly affect the command output or emitted finding
   replacement sketch. Keep judgment/mechanical classification, evidence and
   membership identity unchanged. Reject missing graph data or unknown policy
   IDs clearly. The installed version is checked against the graph header; a
   mismatch explains the regenerate-anchors/review-semantic-diff/update-pin
   procedure. Avoid two drifting hardcoded pin sources.
6. Add meaningful tests that derive the pinned export/member/README inventory
   independently of the graph entries and compare for full coverage. Assert
   real missing-entry and malformed/pin-mismatch failures, graph-backed hints,
   and compilation of all examples. Do not compare the graph to a second list
   copied from the same graph. Ship minimal portable tag source fixtures and
   hash provenance if needed, with tested source anchors.

Protect P0c proof: syntax-only Project, schema-owned D9 globs, keyed membership,
positive JSONL ownership, import/shadowing/live-clock/Context/Drizzle fixtures,
under-10-second full command. Do not edit shared Glob or FsUtils. New filesystem
tests use it.layer with inner scoped temp resources; shorter scopes asserting
cleanup remain legitimate. Plain synchronous tests may use plain it and expect
on plain values; Option/Result/Exit use canonical utils assertions.

Verification you own: the complete focused effect-vitest suites and the affected
routing tests, direct package check when possible, changed-file Biome, package
docgen, exact writer twice with artifact determinism, and default ratchet. Record
runtime versions, commands, exits, file/finding counts, scan time and full wall
time. The current post-build starting census is 1,072 paths (967 tests and 105
support) / 139 owners, with 5,012 findings. Literal D9 includes generated
declarations; explain additions instead of forcing a fixed historical count.

Use installed Bun 1.4.1 via command-scoped PATH supplied in the launch prompt.
The workstation latest alias now selects 1.4.2; do not change workstation config.
If the managed sandbox denies tsgo's Node launcher, record the exact failure and
run the same Effect compiler through Bun as supporting proof. The orchestrator
runs the mandatory full package-verify after source handoff. No broad repository
check/build/coverage command and no blind rerun of attributed environment failures.

Report a distilled coverage table and any ambiguities early, before treating
fixtures as acceptance. Append your changed paths and evidence as you go; keep
completed P0c receipts intact. Final response: report path only.
