# P0c lane contract

The orchestrator owns the packet, decisions, all git operations, publication and
package verification. P0a and P0b passed; P0c is authorized. No later phase starts
inside this lane. You are not alone; preserve all other edits and stay within
owned paths. Never run git commands, including read-only git. Never spawn agents.
Create the report first and append progress/evidence as you work. Final response
is only the report path. Use gpt-daybreak-blue-latest at medium effort. No broad
check/build/coverage runs; the orchestrator runs package-verify. You may run the
focused new Vitest test and the package's direct typecheck/lint when no other
lane owns the package. Record exact commands and outcomes. Never weaken tests,
change global timeouts, property floors, or coverage baselines.

Full lane map: detector implementation owns the paths below. Orchestrator owns
all other paths, including packet docs and source aggregation. Previous P0a
Codex/Grok lanes are complete and have no current source ownership.
