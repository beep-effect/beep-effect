# @beep/ontology-use-cases — four-lens P1 digest

Browser-condition graph test invokes real esbuild and file-URL import in Node, with scoped disk output and exact graph exclusions. WorkerImportGraph dynamically imports real module surfaces without DOM globals; neither case starts a Worker/browser. The retained toolkit import is the slowest file here but is not an isolated resource-build measurement. Layout/tutorial/schema/error files are in-memory contracts. Session has pure per-case codec/FileStore/SPARQL values alongside actual reasoner layer execution; validation combines real SHACL/runner with fake ports. Public outer ownership must preserve per-case overrides, repairs, undo and provenance, not hoist every stub or delete every inner scope. The describe-level validation writes Map has only one export user in this file, so no current concurrent-map race is proved. Sixteen parity schemas retain fcRuns25 and explicit high coherence discard budgets; Session laws retain fcRuns10. No mapped hosted failures does not establish absent failures.

8 files; 32 rows: 3 review items and 29 coverage-only rows.

| Lens | Rows |
| --- | ---: |
| resource | 8 |
| flake | 8 |
| property | 8 |
| observability | 8 |

Severity: 30 info, 2 minor.


## Review items

- L-RES-04 / info: packages/ontology/use-cases/test/BrowserWorkerImportGraph.test.ts:55-108. Real esbuild resolution/metafile plus file-URL evaluation is the subject; retain scoped disk bundle and DynamicModuleLoader binding. MemoryFS cannot substitute for the loader filesystem. Preserve browser conditions/no-tree-shaking/forbidden imports and exported function assertions. Public outer registration and an explicit appropriate acquisition timeout must not change the120-second body limit or claim actual browser/worker execution. Coordinate EV010/EV014 without classifying this as proven container startup.
- L-RES-05 / minor: packages/ontology/use-cases/test/Session.test.ts:83-86. The private wrapper at83-86 is used for real OntologyReasonerLive and the SPARQL runner with scripted query service. Move effectful outer ownership to public layer blocks while retaining per-case fixture identity and exact inference add/remove and query/profile assertions. Direct provideService for pure FileStore/TurtleCodec values remains legal. Do not treat every provide as allocation or replace the actual reasoner with a fake. Coordinate EV002/EV003 without an automatic scope deletion.
- L-RES-05 / minor: packages/ontology/use-cases/test/Session.validation.test.ts:117-134. runWithLayer builds the real validation/SHACL layer plus pure codec/FileStore stubs inside a private scope. In P2 register the effectful outer layer publicly, retain per-case substituted SHACL behavior and test-owned stub state, and account for teardown/context before deleting either wrapper. Real engine repair/undo/export outcomes must remain. Current single writes-map user is not proof of a race; do not hoist that mutable map as shared concurrent state. Coordinate EV004 with this actual layer topology.

## Retained timing and history

accepted-node-command-baseline: 59 cases across 8 files. Reporter span 4650.236083984375ms; whole command 4.972863508000046s. Raw reporter SHA256 fd41b2028fb37b19299cc033da459638740351b8ceb9fdb48f20f2c914fd60da. Node22.22.3/Bun1.4.2/Vitest4.1.11. Complete configured file representation is retained; this is neither compiler nor coverage nor full package proof. Exact source/runtime identities and worker configuration are retained in the public package timing context. Durations overlap under concurrency and cannot be summed as rebuild cost.

0 mapped historical observations across 0 jobs. Historical observations only; no current-source or flaky classification. Zero mapped observations does not establish absence of failures.


P2 sequence, only after Benjamin authorization: scope/layer/native-boundary review, exact assertion-family migration, property-law/floor witnesses, evidence-driven flake review, public instrumentation. Keep every operand, polarity, invalid boundary, seed/floor, native subject and inner cleanup; no timeout increase, retry, skip or baseline exception proposed. Confidence covers cited source observations, not unexecuted runtime correctness. All rows remain open P1 judgments; P2 remains gated.

Root accepted these P1 rows after full report/digest review, source and artifact hash checks, and fresh combined strict validation. Original failed validation remains preserved. Full P1 completeness, Grok review and Benjamin acknowledgement remain required before P2.

Evidence: [timing index](../timings/baseline-index.json), [failed timing attempts](../timings/baseline-failures.json), and [hosted history](../hosted-history-summary.json).
