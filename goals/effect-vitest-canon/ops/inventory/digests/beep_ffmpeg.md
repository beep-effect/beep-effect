# @beep/ffmpeg four-lens digest

Pure argument/schema/equivalence cases coexist with three scripted process suites and one true native integration. Scripted spawners capture commands in case-local arrays and write real Node files. Temp directories use acquireUseRelease; staged-file/overwrite/error/event assertions inspect real outputs. EV provider judgments do not authorize hoisting mutable recorders or blanket MemoryFS substitution. Any later MemoryFS proposal must preserve the precise staging subject and retain separate native proof.

The no-partial-commit timestamp case fails every encode, hence never reaches a second-frame failure after first success. Add that sequence and final/staging absence witnesses without deleting the original first-failure control. Preserve all argv order, frame names, manifest values, numeric/string metadata, malformed luma and missing-output negatives. fcRuns25 and intentional opaque-cause equivalence constraints remain.

The integration invokes actual ffmpeg/ffprobe, drains stdout/stderr/exit concurrently and closes each runTool scope before continuing. It generates media, checks luma-derived timestamp accuracy within the unchanged0.15s tolerance, sizes, clip duration0.8–1.2 and actual container tags. Do not replace it with scripted outputs. However each test returns logInfo without assertions when ffmpegAvailable is false, and tool errors are collapsed into unavailable. A passed reporter record therefore does not alone prove all media work executed. Record prerequisite availability distinctly in later authorized work; no skip/gate modification is authorized now. The retained baseline reports3 integration cases and duration, not an assertion-path trace. Native setup includes generated clips/processes/temp files; no separate setup cost or binary version measurement was taken.

| Lens | Rows |
| --- | ---: |
| resource | 6 |
| flake | 6 |
| property | 6 |
| observability | 6 |

Severity: 22 info, 2 minor. 6 review items and 18 coverage-only rows.

Retained accepted configured Node baseline: 45 cases, reporter span 17746.7158203125 ms, whole command 19.495264141999996 seconds. Node22.22.3/Bun1.4.2/Vitest4.1.11; no load adjustment. Passing runs are not race absence, coverage, browser QA or full package proof.

## Top ten files by retained reporter duration

- packages/drivers/ffmpeg/test/integration/FFmpeg.capture.integration.test.ts: 2421.7158203125 ms, 3 tests.
- packages/drivers/ffmpeg/test/FFmpeg.capture.test.ts: 187.63427734375 ms, 17 tests.
- packages/drivers/ffmpeg/test/FFmpeg.service.test.ts: 153.43798828125 ms, 7 tests.
- packages/drivers/ffmpeg/test/FFmpeg.capture.failures.test.ts: 119.670166015625 ms, 11 tests.
- packages/drivers/ffmpeg/test/FFmpeg.extract-args.test.ts: 4.45556640625 ms, 6 tests.
- packages/drivers/ffmpeg/test/FFmpeg.equivalence.test.ts: 1.619384765625 ms, 1 tests.

Hosted history: 0 observations across 0 jobs; categories {}. Zero mappings does not prove no failures; coverage-ratchet observations are not unique flakes or test failures. Exact provenance/config/host context is retained in the public timing context and hosted history summary.

Proposed P2 order: scope, assertions, property, flake, observability. Preserve all assertions, operands, run minima, negative controls and native subjects. P2 remains gated; Root-reviewed P1 inventory; P2 remains gated.

Root reviewed and accepted this package’s P1 rows after source hash, artifact and combined strict-schema validation. Full P1 remains incomplete; Benjamin’s acknowledgement after completeness and Grok review is required before P2.

Evidence: [timing index](../timings/baseline-index.json), [failed timing attempts](../timings/baseline-failures.json), and [hosted history](../hosted-history-summary.json).
