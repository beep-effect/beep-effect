# Law Practice use-cases — P1 source audit digest

Five census tests are fully reviewed through all four lenses: 20 open judgment rows, two minor review proposals and 18 info coverage rows. Resource has one review/four coverage; flake zero/five; property zero/five; observability one/four. Root-reviewed P1 inventory; P2 remains gated.

All five files tie at four human rows each (the complete top-file list): CandorPolicy.test.ts (714 lines, one review), LegalPositionRelatorPolicy.test.ts (471, coverage only), PatentClaimCandidate.test.ts (129, coverage only), SchemaParity.test.ts (166, one review), TaggedError.equivalence.test.ts (80, coverage only). The 36 scoped detector rows remain open: EV001 one, EV006 four, EV007 one, EV014 thirty. Independent human coverage is recorded separately.

Candor scenario constructs distinct readerLayer(build) and resolverLayer(build) Layer.effect nodes, so the fixture Effect is evaluated for each port. L-RES-01 at line 215 proposes reviewing one fixture construction per isolated scenario, not sharing scenarios or changing source custody. Preserve the real Web Crypto digest and all human-author, retired-disposition, replay, supersession, foreign/empty-reference and unresolved/drifted-anchor cases. Existing anonymous layer blocks already scope each scenario. The policy itself is Layer.succeed; runtime capabilities are requirements of evaluate. Rebuild cost is source-visible duplicate hashing/schema decoding, not a measured performance defect. The accepted Candor reporter file duration is 50.0322265625 ms; no speedup is promised.

LegalPositionRelatorPolicyLive is a pure Layer.succeed service: admission decodes schemas, views derive values, and opposition screens supplied records. Its three EV014 candidates are suspected overly broad resource-timeout judgments, not evidence of missing container hooks. Candor's 27 EV014 judgments involve effectful in-memory fixture creation and real hashing, but no database/container/HTTP acquisition. Do not automatically add a 30-second container timeout. Keep every detector row open for Root. None of the five tests needs MemoryFileSystem: source filenames and repository error names are data, and actual text alignment/hash behavior must remain the subject.

SchemaParity's local encode/decode round-trip helper projects the native check result to _tag (line 55), losing counterexample/replay details in its tag-only assertion. L-OBS-01 adds that diagnostic reason beyond EV001/EV007 syntax. Use a separately named native property registration or retain complete formatted failure during P2. Keep equivalent(decode(encode(value)), value), both explicit 25/10 call-site floors, fallback 50 and fcRuns env-max/seed policy. Imported decode-to-self tests already use the shared helper and must retain their distinct law and 10/25 floors. Its helper asserts against the complete result object, unlike the local projection. No reduced run counts, new retry or weaker test-only schema is proposed.

Legal-position algebra exhausts all eight kinds for closure, involution, commutation and two four-member orbits; preserve full candidate equality, all five overlap axes, polarity, required-field and empty/singleton/order controls. Patent candidate tests preserve quote whitespace, escaped parentheses, exact section bounds around earlier/later duplicates, full-digest identity, dependent references and typed absence errors. Tagged-error tests deliberately compare declared diagnostics while varying incidental cause; they do not swallow a running repository failure. D5 permits plain reasons, encoded wire objects and other plain-value equality. Do not invent Option/Result expected payloads for branch-only assertions.

The accepted first-attempt baseline recorded 60 passed tests across all five files, exit 0, whole command 6.969501525000396 seconds. Reporter file durations are Candor 50.0322265625 ms/27 tests, SchemaParity 37.436279296875 ms/6, LegalPosition 19.996826171875 ms/18, PatentClaimCandidate 11.2998046875 ms/4, TaggedError 2.340087890625 ms/5. Runtime: Node22.22.3, Bun1.4.2, Vitest4.1.11. Raw reporter SHA256 39b209bd536edec26a132dfbc3fe66298ce0d235ade3edbeb064e172f1d2b8f2. Wall time includes startup/shutdown; file times are not interchangeable. No load normalization or rerun occurred, and this is neither compiler, coverage nor full package proof.

Hosted history maps eight coverage-ratchet observations across three jobs, not test failures or unique flakes: IrToLaw.ports.ts functions/statements on Aug17 (job95265008664), and new PatentClaimCandidate uncovered lines/statements plus package function floor on Aug27 (jobs98571841230 and98591375444). Exact source paths and observed numbers are retained in the hosted-history evidence. Those production coverage paths are not test failure locations, and no present-source causal claim follows. Global collection includes 527 failed runs, 21 unavailable relevant logs and one unresolved cause. Rare failures cannot be ruled out. The completed timing campaign retains 132 full-file-representation baselines, four configured subsets and three failures (CIops, Effect Drizzle, QA Capture).

Proposed P2 order remains scope, assertions, property, flake, observability: first review Candor duplicate fixture construction and EV014 provenance without collapsing scenarios; then adjudicate SchemaParity's direct tagged branches while preserving encoded/diagnostic plain equality; migrate the native property registration with all floors/laws and useful failure context; retain deterministic behavior unless actual flake evidence requires a repair; finally adopt the accepted instrumented runner without moving tests to live or dropping type-level controls. Root owns package verification, timings, canonical assembly and approval. No provider, store, process or native integration was executed by this audit.

Root accepted these P1 rows after full report/digest, source-read and artifact review and combined strict validation without input drift. The initial prerequisite-check sequencing error is retained in the private audit receipt; all prerequisite hashes were verified before subsequent source reads. Full P1 completeness, Grok review and Benjamin acknowledgement remain required before P2.

Evidence: [timing index](../timings/baseline-index.json), [failed timing attempts](../timings/baseline-failures.json), and [hosted history](../hosted-history-summary.json).
