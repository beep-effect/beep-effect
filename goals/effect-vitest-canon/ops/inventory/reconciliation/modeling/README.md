# Modeling inventory reconciliation candidate

Reviewed resource, flake, property and observability rows for types, identity
and utils. All source hashes match the publication base in `manifest.json`.
The source review remains pinned to its original revision.

These are candidate rows, not canonical inventory replacements. Frozen census,
timings and historical dispositions remain intact. This batch preserves earlier
fixed and exception statuses and records the missing Struct raw-path fallback
coverage as a separate open finding. No new runtime or remediation credit is
claimed.

Schema and data are deferred because their merged remediation changed sources
after the review pin. Their existing receipts will be reconciled separately.
The full goal scope and remaining census obligations are unchanged.

The manifest hashes every source and candidate row file. `validation.json`
records the strict four-lens completeness check for this batch only.
