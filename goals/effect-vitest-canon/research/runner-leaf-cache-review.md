# Runner extraction cache configuration review

This review records the task graph changes required to extract the existing
Vitest harness into `@beep/test-runner`. It grants no qualification state,
remote-cache trust, or performance claim.

The executable projection comparison contains thirteen new test-runner tasks,
no removed tasks, and eleven changed existing tasks. Ten test-utils tasks add
the corresponding test-runner dependency edge; their commands and effective
configuration are unchanged. The remaining change adds the new package manifest
and source glob to the policy-fingerprint task inputs, as generated by
`beep lint policy-fingerprint --write`. Global configuration is unchanged.
The new tasks use the canonical package scaffold and inherited task settings.
Eight inherit enabled caching; they remain unassessed, like the existing
unqualified package tasks. No tuple is promoted or activated by this review.

The configuration-source notices outside root `turbo.json` are inherited from
the branch base: this extraction does not edit those package Turbo files.
The writer refreshes their source digests alongside the current projection;
no effective task change outside the eleven entries above was observed.

The qualification scope, profile, epoch, and qualification ledger are retained.
The request includes the exact previous baseline digest, and the canonical
`beep cache baseline` writer verifies this review file digest before updating.
This is a prerequisite configuration review, not a refreshed Effect test inventory.

After merging PR #1185, the base includes the reviewed `@beep/ciops#build`
outputs declaration from PR #1180. That independent change is retained. The
base comparison found no other task or global configuration changes.
