# NLP final local evidence

Final Node and Bun runs both passed 175 tests, with zero failed or pending
tests. Source hashes remained unchanged during each run. Whole command times
were 4.186 seconds for Node and 1.899 seconds for Bun. The reports include
runtime versions, system load, CPU/memory/I/O pressure, process limits, and
source manifests. These shared-workstation measurements do not prove a speedup;
the suite now includes seven additional generated-law registrations.

The NLP detector baseline removal is limited to its 102 current entries.
All 7,934 other finding objects are preserved verbatim. Historical inventory
identities and evidence are retained. No-findings spans were refreshed for the
current ten files. The strict five-lens inventory validator passes with zero
missing rows; this validates coverage and shape, not P1 acceptance or closure.

The remaining 80 actionable rows credit signed implementation commit
b8152b533f6f90aa69c4e75282de8a775f51361f. Earlier fix SHAs remain intact.
Committed-tree reference checks, publication, review closure, and hosted proof
remain outstanding.

The preserved preparation reports are also copied byte-for-byte into the
canonical before-report and before-context locations for both Node and Bun.
Each baseline passed 168 tests; each final report passed 175. The seven added
registrations are the graph mapping property and six Composable properties.
Runtime-specific before/after reports must be compared separately. Increased
coverage and varying workstation load prevent interpreting elapsed-time ratios
as a controlled performance regression measurement.

Final timing reports were refreshed after the runner dependency was corrected
to workspace:^. Syncpack passed, followed by full package verification
(audit 8.1 seconds, docgen 3.1 seconds). The earlier full proof stopped because
it lacked an in-range NLP changeset; this wave now includes one.
