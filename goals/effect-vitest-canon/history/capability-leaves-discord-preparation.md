# Discord current-source preparation

Both existing inventory files were read in full and compared with frozen commit
662823dd960367046ba7d73dd8fd25d15782865a. Only the Effect Arbitrary and HTTP import
paths changed upstream. The service fixture remains an injected HTTP client
with a Ref-backed capture; no provider account is used.

Configured Node and Bun baselines pass four tests with stable source hashes.
Whole-command times are 4.681139 and 2.390165 seconds; load, pressure and runtime
context accompany the reports. These are shared-workstation observations.

The existing it.prop registration retains its six native generators and
fifty-case floor. Exact wire-shape, Option-field and error-equivalence assertions
remain. The existing property-lens finding requires the captured POST method and
submitted content to be asserted alongside URLs, authorization, response ids and
mention suppression. The local layer receives an explicit five-second budget.

## Ordered remediation and local proof

Scope: the existing injected-client layer now uses it.layer with an explicit
five-second acquisition budget. No service fixture or test body changed in this
phase. Package verification passed (audit 7.5 seconds; docgen 3.2 seconds).
Assertion review retains existing exact encoded-shape, Option-field, response
and equivalence expectations. Property review adds only the captured POST method
and exact submitted message content beside the existing transport assertions.
Package verification passed again (audit 7.3 seconds; docgen 3.0 seconds).

Flake review preserves the reset Ref and sequential requests; no new timing,
retry or concurrency behavior is introduced. Both files now use the instrumented
runner, retaining the six native generators and fifty-case property registration.
The workspace:^ development dependency, lockfile, two generated TypeScript
references and Fallow boundaries are updated. The cache receipt accepts only
nine Discord dependency lists, preserving all previous wave entries and cache
configuration. Cache audit reports zero blocking findings; unassessed cache
computations are not claimed as qualified. Fallow checks pass. Final package
verification passed (audit 7.7 seconds; docgen 2.9 seconds).

Final Node and Bun runs pass all four tests with stable source hashes, taking
4.178119 and 1.386847 seconds respectively. No detector findings remain for
Discord. The one resolved baseline entry is removed; all 8001 unrelated records
are unchanged byte-for-byte. Canonical discovery still contains the same two
files. Scoped ledger attribution and validation follow the source commit.

Source commit 80c4d73cb50273e555261533cd56d40b2d5bac11 is the fix attribution
for both actionable inventory entries. Both final timing manifests match the
committed source after hooks. The ledger retains one detector and eight human
lens rows. Scoped strict inventory validation is valid and complete with zero
missing lens coverage. Hosted wave proof remains outstanding.
