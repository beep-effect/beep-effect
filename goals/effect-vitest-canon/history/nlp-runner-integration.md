# NLP runner integration and final native boundaries

The two remaining native Effect test boundaries now use it.effect: four
runSync codec calls in the core wire-shape example and one runPromise wrapper
in the pattern element example. AST comparison confirms preserved bodies
apart from those runtime lifts. Full package verification passed (audit 7.5
seconds, docgen 3.7 seconds).

A fresh full detector scan then emitted no NLP finding rows. This is detector
evidence, not closure of the human inventory or the repository-wide goal.
No detector baseline entries have yet been removed for this package.

All ten NLP test files now import it from the instrumented @beep/test-runner;
describe and expect remain from @effect/vitest. The declared development
dependency, lockfile, generated TypeScript references and Fallow boundaries
are updated. The cache review records nine narrowly changed dependency lists.

Full package verification with the runner passed (audit 20.5 seconds, docgen
8.6 seconds). Cache audit reported zero blocking findings; generated Fallow
boundaries passed their check. Existing unassessed cache computations remain
unassessed. Final timing evidence, human-ledger reconciliation, baseline
removal, publication and hosted proof are still outstanding.

The preceding signing failure remains unresolved. Verified edits are staged
and backed up privately; no unsigned commit or authentication change is used.
