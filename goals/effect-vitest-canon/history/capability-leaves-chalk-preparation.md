# Chalk preparation within the capability backlog wave

Both originally inventoried Chalk files were read in full against current main.
The tagged-error equivalence test is unchanged from the frozen source. The main
test changed its codec boundaries to Effect and Result and moved Arbitrary's
module import, but retains the eleven-schema manual property loop and 50-case
floor. Those current boundaries are the starting point for this wave.

Fresh configured Node and Bun baselines each passed all 18 tests, with no failed
or pending tests and stable source hashes. Whole-command times were 11.558316
and 9.732754 seconds. Runtime versions, limits, load and pressure are recorded.
These are shared-workstation measurements, not controlled performance claims.

Scope review requires no edit: singleton level changes and restoration stay
inside synchronous callbacks with finally; detection inputs are injected values.
The private tagged-error test acquires no external resources. Preserve its exact
declared-equivalence checks and the existing ANSI output and identity assertions.

Assertion remediation replaces the invalid-options Result branch check with a
complete failure Cause assertion. Typed failure reasons are projected to their
SchemaError tag; defects, interruptions and multiplicity remain observable. The
original integer-range message substring assertion is retained on the original
failure. Full package verification passed: audit 11.8 seconds and docgen 5.2 seconds.

The other ten files in Brand, Box, Ciops and Discord await their current-source
full reads and baselines. No package-wide or wave-wide completion is claimed.

The property phase names all eleven schemas through public it.effect.prop
registrations. Each retains the same Arbitrary.schema input, encode/decode
sequence, Equal.equals predicate and 50-case floor. Per-schema codecs are
compiled at registration time. Full package verification passed: audit 12.8 seconds, docgen 6.1 seconds.
Schema order and complete predicate-body parity were verified after accounting
for whitespace and the hoisted codec references.

Flake review found no required change: synchronous restoration and injected
detection facts remain intact. Instrumented runner adoption and its generated
metadata remain pending; the ledger and baseline have not been closed.

## Runner integration

Both files now use @beep/test-runner. The generated TypeScript references and
Fallow boundaries include the dependency; only nine Chalk cache dependency
lists changed. A dedicated review receipt and matching hash identify those
edges without claiming inherited Graph3D changes.

Bun normalized the requested workspace:^ dependency to workspace:* on add.
The manifest was corrected to workspace:^; Syncpack and regenerated lockfile
checks passed. Full package verification then passed (audit 7.9 seconds,
docgen 3.4 seconds). Cache audit has zero blocking findings; Fallow boundaries
check passed. Final timings, detector/ledger reconciliation and the other
packages remain pending.

## Final Chalk local evidence

Final Node and Bun reports each pass 28 tests with no failed or pending tests
and stable source hashes (4.444750 and 1.380356 seconds respectively). The
authoritative census contains the same two Chalk files. All five lens row
files validate as complete with zero missing coverage.

The detector emits no Chalk findings. Three current baseline rows were removed
while 8033 unrelated raw records remain unchanged. Historical detector row IDs
are retained alongside the current baseline rows. The historical runSync row
is credited to upstream PR #1200; the remaining 5 rows credit 2477457561f3a6613b443399918831ff6c48e2d8.
The root ratchet passes with introduced=0 and resolved=0. These results do not
close the remaining capability packages or the repository-wide goal.
