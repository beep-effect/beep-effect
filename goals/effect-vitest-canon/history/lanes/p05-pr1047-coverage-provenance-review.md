# PR #1047 coverage provenance review

Status: complete — conditional provenance recommendation; final coverage not approved. Independent read-only review of the baseline model/writer, new-file comparator, applicable coverage laws and immutable before-coverage evidence. No final source/coverage acceptance is implied.

Ownership is this report and new private `~/.cache/beep/effect-vitest-canon/p05-pr1047-coverage-provenance-review-evidence/` files. Commands use the filesystem worktree and non-login shells; actual permissions remain danger-full-access / approval never. No source/test/config/baseline, git or inbox mutation, delegation or package check is authorized. The live core/characterization/coverage writers remain outside this review.

## Established facts

Captured policy/baseline/runner/config and immutable before-coverage inputs are retained in `~/.cache/beep/effect-vitest-canon/p05-pr1047-coverage-provenance-review-evidence/` (`P` below). Baseline SHA-256 is `0fbace2118dc31a6bb2ec08f20d51cd4d537f6fafdc13d11dff54712020a35ec`; CoverageRegression.ts is `d2332d334480350f38cfa2773449453da5dc16254c0cbc6da7cb5b5f5ec4e9a7`; preserved summary is `31f05d6efe99af1db0c5e3a742d6ab1b1ae5069dbe12a46276e4f3a90761e1d9`. No moving core/Characterization/Coverage source is used as immutable proof.

The baseline contains 134 packages and 11 existing @beep/test-utils file identities; the preserved run reports 14 files, all 11 existing identities retained. The three new identities are FileSystemConformance.ts, MemoryFileSystem/MemoryFileSystem.test-kit.ts and MemoryFileSystem/index.ts. The index has zero uncovered units; the helper has one uncovered branch; the core has 146 uncovered lines, 195 statements, 179 branches and 40 functions. Core LCOV independently agrees on 1,155/1,009 total/covered lines, 565/386 branches and 326/286 functions. Statement counts come from coverage-summary.json, not LCOV.

| Package metric | Existing percentage floor | Before measured percentage | Existing uncovered count | Before measured uncovered count |
| --- | ---: | ---: | ---: | ---: |
| Lines | 78.88 | 89.06 | 95 | 204 |
| Statements | 79.37 | 87.35 | 99 | 256 |
| Branches | 71.95 | 70.39 | 23 | 193 |
| Functions | 66.98 | 85.57 | 70 | 87 |

The only changed surviving file row in the before measurement is SqlTest.ts: percentages rise from 66.30/65.73/58.49/46.51 to 79.56/79.02/77.35/64.34 (lines/statements/branches/functions), while uncovered counts fall from 94/98/22/69 to 57/60/12/46. A scoped writer would adopt those higher existing-file floors too; it is not a file-identity-only append operation. This potentially environment-sensitive change needs explicit review and verifier-topology evidence.

The before measurement is **not eligible** for the requested non-weakening regeneration: its 70.39 branch percentage is below 71.95. Merely raising package branches later will not automatically clear new-file provenance failures while the new core/helper retain uncovered units. No final test/coverage result is assumed.

## Recommendation and authority

**Conditional yes: a reviewed, measured identity for genuinely new promoted source is supported by the existing policy.** After meaningful final tests and preservation of existing percentage floors/file identities are proved, Root can use the existing scoped writer for @beep/test-utils. This is adoption of explicit coverage data through the existing schema/writer, not a coverage-policy exception. It is neither approval of today's red measurement nor pre-approval of unknown R2/coverage-lane output.

The strongest source evidence is `CoverageRegression.ts:1635–1686`: base-pinned comparison keeps the base package metrics and the base metrics of surviving file identities, while the **reviewed workspace baseline determines the file identity set** and supplies metrics for genuinely new identities. The existing test at `packages/tooling/tool/cli/test/quality-tasks.test.ts:4270–4328` explicitly expects an old file to keep its base floor and a new file to take its workspace measurement. No comparator alteration, broad waiver, severity change or implicit lab-to-package identity transfer is necessary. The supplied lab-promotion provenance must still be verified against the final PR: the scratchpad source remains and was not an old covered identity moved/deleted to evade its floor. The captured current baseline has no MemoryFileSystem identity to transfer.

Authority is narrow. Primary `SPEC.md:266–268` names “coverage ratchet policy” as a non-goal. `SPEC.md:270–273` says to ask before “writing any coverage baseline row for a package the PR did not touch”; `GOAL.md:31–34` repeats that untouched-package boundary. @beep/test-utils is explicitly touched by this promotion, so that particular Benjamin-confirmation condition does not apply to its measured row. It would apply to an unintended other package. The instruction that reviewer acceptance precedes scoped regeneration is also explicit in this task briefing; this report does not treat a provisional provenance review as final acceptance of unfinished source/tests. I did not locate an additional coverage-specific permission clause in the inspected skill files and do not attribute one to an unverified SKILL.md. Root's prohibition on **lane** baseline writes remains binding; this reviewer has no write authorization.

Fast publication is already authorized once all issues are addressed locally. That does not erase final measurement/review checks, authorize weakening or merge, or require another permission/queue round for a correctly scoped touched-package update. Root owns the final local issue audit, regeneration, push and subsequent hosted proof.

## What the writer and comparator actually enforce

- **Data model:** `CoverageRegression.ts:246–316,336–351` requires schema v2, all four percentages, uncovered counts and normalized repo-relative file keys. Counts are measured data, not optional annotations. `:790–813,887–901` recomputes percentages from covered/total counts using the existing two-decimal truncation and derives uncovered as total minus covered; it does not trust a rounded display string or invent a source-history identity. `:846–885` rejects invalid/duplicate normalized paths.
- **Default rejection stays meaningful:** `:2093–2255` catches package/file regressions, vanished covered identities and uncovered new identities even when aggregates hide their gaps. Restoring the package branch floor only changes which witness is printed; remaining new-file gaps still require reviewer adjudication/adoption or meaningful coverage that removes those gaps. Fully covered new files can pass without a prior identity; no blanket 100% rule is imposed on every future accepted baseline file.
- **Scoped merge is at package granularity:** `:1205–1220,1569–1623,1908–1972` replaces the entire measured @beep/test-utils row, including every existing file row and its counters. It does not merge each metric with max(old,new), preserve every old file automatically, or restrict adoption to the new Memory file. Unmeasured package rows are preserved. Existing minimum/exemption/follow-up records and scoped document provenance are inherited; command/schema/epsilon use canonical values. `:1882–1889` explicitly warns that changed-package rows may include environment-dependent file measurements.
- **Completeness is not exclusivity:** `:1772–1795` requires each expected package to emit a summary. `:1051–1061` collects all existing workspace summaries, and the scoped merge adopts every collected entry. `Quality/Tasks.ts:3004` first clears stale coverage directories, but a concurrent coverage run can repopulate another package before collection. The writer does not enforce “only @beep/test-utils emitted a summary”; Root must serialize authoritative coverage and verify the exact measured-owner set.
- **A successful write is not a ratchet pass:** `Quality/Tasks.ts:2992–3018` runs coverage and then chooses either write or compare; the write branch returns without the comparator. Baseline mode sets the existing VITEST_COVERAGE_REPORT_ONLY variable (`:2133,2165`), which disables this package's local Vitest thresholds through `packages/tooling/test-kit/test-utils/vitest.config.ts:4–16`. This is the existing generation mechanism, not a proposed suppression, and makes independent pre/post acceptance essential. Normal gate runs reject report-only mode (`Tasks.ts:2993–2997`).
- **Worker/environment reproducibility matters:** `Tasks.ts:154–174,2137–2166,2813–2847` gives the scoped writer a selected weighted shard with `--fileParallelism=true --maxWorkers=1` for a sole @beep/test-utils selection. An ordinary explicit-filter non-write run need not take that same branch. The package config documents PGLite/environment-sensitive coverage (`vitest.config.ts:8–11`); the measured SqlTest increase must not be assumed reproducible on hosted runners. Capture actual argv/runtime/environment disposition and compare the writer's own fresh summary as well as the independent gate result.
- **Base-pinned checks do not excuse identity deletion:** the base-reader intentionally iterates workspace file keys. A writer-generated omission can therefore remove an old identity from the base-pinned comparison too; its existence must be protected independently. `coverageBaselineRowDelta` at `:1344–1390` scopes row-only updates but does not judge whether each new row is justified or monotonic. Global field changes force broader planning. The existing named @beep/test-utils follow-up skips only the tier minimum, never its regression comparison (`:2281–2331`); preserve it unchanged and do not use it as a waiver.

Percentages and uncovered counts must not be conflated. Adding legitimate partial new source can increase measured **package** uncovered totals even while old files improve and all old percentage floors survive. In the before receipt, all package count increases reconcile exactly to the three new identities minus improved old-file gaps: old-file uncovered totals become 58/61/13/47 and new identities contribute 146/195/180/40 (lines/statements/branches/functions). No old-file uncovered count increased. An additive adoption may establish measured counts for the expanded package and new files; it must not be described as preserving the old package uncovered-count budget. If Root additionally requires those totals never to increase, tests must first bring them to at most 95/99/23/70. There is no truthful clamp/max writer option that can claim both the higher measured counts and unchanged old budgets. The recommendation preserves existing percentage floors and old-file gap budgets, while requiring explicit review of each new file's residual gaps.

## Required acceptance sequence for Root

1. **Freeze and adjudicate the actual change.** Wait for R2 and coverage writers to exit. Capture final source/test/config/lock/runtime/policy hashes and resolve all locally actionable comments. Review the added public-API tests for behavior/assertion substance and residual LCOV gaps. Preserve the existing regression/characterization/conformance assertions; no test deletion/skip, source-exclusion trick, coverage-floor/config change, source rename or policy edit is part of this path. This review supplies policy judgment only; it does not supply that final source/test acceptance.
2. **Retain the authoritative old baseline.** Save its full bytes/hash before any writer, and retain Root's actual PR-base comparison document for hosted/base-pinned checks. Verify schema v2; package path stays `packages/tooling/test-kit/test-utils`; all 134 package identities and all 11 previous file identities remain. The reviewed addition set is exactly the three paths listed above unless final evidence justifies a separately reviewed change. No scratchpad row is subtracted or reused.
3. **Measure final bytes before regeneration.** Run the canonical selected coverage gate with fresh outputs and full selected tests, preserving its exit and raw summary/LCOV. Also establish the writer's actual worker topology and repeatable verifier/hosted environment behavior before accepting any tighter existing-file floor. The default comparator may still exit 1 solely for reviewed new identities; record that honestly, never relabel it green. A package/file drop, missing summary, unexpected identity, assertion failure or unrelated regression is not an adoption-only failure.
4. **Check every preserved floor, not branches alone.** Final measured package lines/statements/branches/functions must be at least 78.88/79.37/71.95/66.98. Every previous file's four percentage floors must be preserved or raised, with no increased uncovered count for a surviving old file in this bounded correction. All normalized keys must be present and unique, all counts valid and reconciled with raw coverage, and every residual new-file gap reviewed. Explicitly review SqlTest's row; path preservation alone does not make a newly tightened, environment-sensitive floor safe.
5. **Root alone regenerates the exact package.** After the preceding review gates, the supported command is `bun run coverage -- --filter=@beep/test-utils --write-baseline`. No full writer, --replace-all, manually fabricated metric, baseline-side clamping, explicit suppression, exemption/follow-up change or unrelated-package adoption is needed. Ensure no concurrent coverage emitter; the collected summary package set must be exactly @beep/test-utils. Preserve the fresh writer summary and run the same numeric/identity guards on it: its output can differ from the pre-write run.
6. **Inspect the generated delta before accepting it.** Deep-compare all 133 other package rows, the global schema/epsilon/minimum/exemption/follow-up fields and inherited metadata; they must remain unchanged. No package or existing file path may disappear. The target row must equal the fresh measured normalized summary, including truthful uncovered counts and the three new identities. Require `coverageBaselineRowDelta` equivalent to present=[@beep/test-utils], removed=[], with no global-field delta. If “unrelated files preserved” means their entire metric records must stay byte-for-byte unchanged, the current writer provides no per-file selector; today's SqlTest delta demonstrates why an unguarded write does not meet that stronger requirement.
7. **Re-run normal proof and complete publication duties.** With report-only/write mode absent, run the canonical selected ratchet against the generated workspace baseline and Root's actual base-pinned comparison context; both must pass on the accepted source. A same-run comparison against newly written data alone is insufficient evidence of old-floor preservation. Root must combine this with required package/local tests and resolve all local issues before the already-authorized push, then retain actual hosted/base-pinned receipts. No merge or P0.5 acceptance is conferred by this report.

The read-only `P/adoption-guards.mjs` implements strict percentage preservation, old-file uncovered-count preservation, the exact 11+3 file set and count reconciliation. It optionally checks a supplied generated baseline for unchanged other packages/global fields and exact agreement with the measured target row. It never generates or writes a baseline and never runs coverage. Running it against the preserved before summary exits **1**, correctly rejecting the sole numeric-floor violation, package branches 70.39 < 71.95; identity retention, old-file gap preservation and accounting pass. Passing these data guards later would still require the review and runtime/provenance checks above.

## Remaining evidence and terminal integrity

The supplied 96 passing Node/Bun tests do not establish coverage acceptance; the preserved branch failure demonstrates the distinction. Final R2/coverage source, final measured rows, final reviewer acceptance and Root's actual PR-base document are still missing by design. If that comparison base differs from the captured current baseline, Root must reconcile its applicable floors/identities too; the private data guard is anchored to the captured baseline and cannot certify an unseen base. A green comparator against newly written rows is not a substitute for these checks.

`P/commands.md` records commands/exits. The private data guard exited 1 for the expected before-state branch-floor rejection; no canonical coverage/package command was run by this reviewer. `python3 "$P/verify-inputs.py"` exited 0: all seven primary input records, six policy/config/packet support records and the captured existing policy-test file are unchanged. The committed baseline remains SHA-256 `0fbace2118dc31a6bb2ec08f20d51cd4d537f6fafdc13d11dff54712020a35ec`; no baseline was regenerated or edited.

Graft was consulted first: two caller queries lacked indexed symbols; the CoverageRegression skeleton succeeded and reported 21,699 estimated tokens saved (three calls total). Targeted source fallback supplied the Effect.fn definitions. Some discovery aliases were absent and personal skill links contained a loop; no trust/configuration change, graph rebuild or link repair followed. The primary packet and captured live schema/writer are the decision authority, not historical comparator behavior. Permissions remained danger-full-access / approval never. This lane wrote only the owned report and new private evidence, with no moving-source proof, delegation, git/inbox action, package check, baseline/rule edit or publication.
