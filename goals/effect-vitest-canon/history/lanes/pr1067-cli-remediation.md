# PR1067 CLI remediation

Bounded detector complexity/duplication and Windows path repair. Root owns integration, canonical artifacts and aggregate proof. Prior census reconciliation remains interrupted and unchanged. No Git, publication, package proof or baseline writes.

## Progress

- Saved the three source and four test preimages in the private `before/` snapshot; `before-hashes.json` records their identities. Graft callers lookup returned no detector symbol (exit 1); targeted live source fallback used, no graph rebuild. No Graft token-saving estimate is available from this miss.
- Split rule dispatch into private rule-specific functions in the existing Detectors role; retained full-token hashing and reverse-child traversal without new token arrays. Consolidated shared piped-wait containment and lexical parameter-owner walks. Separated syntax binding, callback/reference graph construction and propagation responsibilities, preserving per-analysis caches and declaration order.
- Scanner uses `Path.relative(root, absolute)` followed by existing `toPosixPath`; Project/addSourceFilesAtPaths and D9 globs remain unchanged. Added a bounded public-scanner regression for drive/UNC path normalization. No new public export or role file.
- First focused four-suite observation: 98/98 passed. Intermediate compiler errors in the new regression were retained and corrected using pinned NodePath.layerWin32, scoped Layer.build, and schema-decoded rows; no diagnostic suppression. Final proof pending.
- Higher-priority live P0/P1 reminders required four inbox acknowledgments outside normal lane ownership. Each used the existing coordinating thread URL, with no fixed/environment-only/wontfix/waiver claim. Exact commands and exit-0 logs are retained in private `inbox-required.log`. Root retains disposition.

## Stable handoff

The owned remediation is stable. The final focused suites pass **99/99**, focused tsgo passes with inherited Effect diagnostics, and read-only Biome passes all seven inputs. No package, full scanner, benchmark, census, canonical writer or publication command ran.

The Windows correction is at `EffectVitestScan.ts:363`: `toPosixPath(path.relative(root, absolute))`. The regression at `effect-vitest-contract.test.ts:63` invokes the actual public scanner with a single retained test input, uses pinned `NodePath.layerWin32` through scoped `Layer.build`, verifies the relative-path service is consulted, validates emitted rows and compares complete drive/UNC payloads. It writes only scoped temporary row output, never canonical artifacts. No new public API or role file was introduced.

The dispatcher retains EV001–EV015 call order, then definition/import/CJS/loop order. Provider lookup now completes in the shared call-inspection record before rule emission; it is syntax/cache-only and does not mutate findings. Syntax bindings retain first declarations, destructuring, TDZ/shadows, loop/catch ownership, cached None versus absent entries, and per-analysis WeakMaps. Helper edges and fixed-point queue order are unchanged. Token children still use the original reverse-index push order and complete statement/named-context digest; no anchor truncation or extra token traversal. Binding extraction helpers are module-level, avoiding new per-scope closures. Performance was not measured in this lane; Root retains timing acceptance.

The native 12 assigned complexity sites were decomposed by rule or syntax responsibility. The isolated seven-input Fallow snapshot uses unchanged repository ceilings (cognitive 8, cyclomatic 20, CRAP 30, unit size 60). It reports **zero cognitive/cyclomatic exceedances** and **zero duplicate groups** across the three production inputs. Representative cyclomatic/cognitive measurements:

| Boundary | Native before | Scoped after |
| --- | --- | --- |
| detector dispatcher | 105 / 136 | 8 / 8 |
| helper reachability | 55 / 70 | 6 / 5 |
| scanner orchestration | 16 / 17 | 10 / 8 |
| harness index | 9 / 14 | 5 / 7 |

**Remaining qualification:** isolated `fallow health` exits 1 with 57 CRAP-only findings because its copied subset has no resolved test-dependency graph and is assigned coverage tier `none`. This is not a passing native health/audit receipt, an attributed waiver, or evidence that actual hosted CRAP is green. The raw failure is retained. Root must run the real integration audit/health with full graph context and address any actual remaining findings. Bounded `fallow dupes` exits 0; repository duplication settings use defaults on these paths. No ceilings, exclusions, assertions or suppressions were changed.

The assertion ledger captures exact expressions plus trivia-free token identity: contract 44→53, detectors 136→136, primitives 48→48, store 5→5. **All 233 original assertions remain; nine were added.** Method-option and charter-anchor extraction preserves the original assertions and collection order. Detector/store test bytes remain unchanged. Public export JSDoc was not changed; no docgen run was needed for new exports.

## Commands and evidence

All commands used the primary worktree and non-login shell. Runtime receipt: Bun 1.4.2, Node v24.20.0, Vitest 4.1.11, rc.112, tsgo `7.0.2+effect-tsgo.0.39.1`. Exact argv/cwd, exits, log hashes and before/after input hashes are in the following private receipts under `~/.cache/beep/effect-vitest-canon/pr1067-resume/cli-remediation/`:

| Receipt | Command/result | Log SHA256 |
| --- | --- | --- |
| `stable-tests-receipt.json` | Node Vitest run: four effect-vitest suites, runner config loader, no cache; exit 0 | `187f52a319f9d6fceb9f678c17f1c87b285cedb0817a362d18637b3364cdf3a0` |
| `stable-compiler-receipt.json` | tsgo --project private tsconfig.focused.json --pretty false; exit 0 | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| `stable-biome-receipt.json` | biome check: all seven owned inputs, read-only; exit 0 | `756c303bb7a25cddc6813582e20badbb4ec43e896986d6e240ee48b0767bf760` |
| `stable-fallow-health-receipt.json` | fallow health --complexity --no-cache --root private/fallow-scope --format json; exit 1 (limit above) | `dc236df0624661dfdf5846fdd39d836447773077072ec937ede8f00e8a387b0d` |
| `stable-fallow-dupes-receipt.json` | fallow dupes --no-cache --root private/fallow-scope --format json; exit 0 | `b9689bb86ca3cd920a2815542682f1ab6256f6f58b894e414822dfe0299fc101` |

Intermediate misses and successful checks remain retained; final commands ran on the same final bytes. `terminal-integrity.json` records empty unexpected drift across the seven owned inputs and the listed protected schemas, graph, facade, source-only seam, policies/store/primitives, manifest, lockfile and configs during final validation. This is a scoped hash proof, not a whole-checkout claim; Root's concurrent packet work is outside it. `owned.diff`, `before/`, `after/`, `stable-assertion-ledger.json`, `complexity-summary.json`, and `manifest.json` provide the reviewable delta and complete evidence identities.

## Final input hashes

Changed: Scan, Detectors, Syntax, contract test, primitives test. Unchanged: detectors test and store test.

| Input (CLI-relative) | SHA256 |
| --- | --- |
| `test/effect-vitest-contract.test.ts` | `981fb2249898b82aa6730d8077fb48e2b77c738c241137506f5edc544e44b794` |
| `test/effect-vitest-detectors.test.ts` | `82649b552f90eeef4cd14b8ecff0fd031a8c048b0dbcf6adf8904a0572e21ce2` |
| `test/effect-vitest-primitives.test.ts` | `52a7b8249ccb19755be5d8ae9a75ae9fa03b7976635ce058279fad75fb2ba154` |
| `test/effect-vitest-store.test.ts` | `957ad4c0674712faa69640b7292760369716efbe7a8ebb377e35a904b6868fda` |
| `src/commands/Lint/internal/EffectVitestScan.ts` | `e942589b1b4eb3724f237eb96244c5fd366a29e46302d51b9061a442ee393333` |
| `src/commands/Lint/internal/EffectVitestDetectors.ts` | `010b42e1e534a02b3f4f9285cb003c60f5451ddecd515fb9e6c8b8569fb1e084` |
| `src/commands/Lint/internal/EffectVitestSyntax.ts` | `0f5f85d742ce240897a44c7ec982a4b96cc275b2b9fcf550e9c197eb3c37a67f` |

Frozen graph SHA256 remains `faf7b04f240aa4f909c89c49bec473d7b6f2b518df80ef15825401f52e7ea6a8`. Root retains native aggregate/package proof, timing, Git/GitHub and mergeable-state judgment. No merge, P1/P2, phase completion or canonical-write approval is asserted.
