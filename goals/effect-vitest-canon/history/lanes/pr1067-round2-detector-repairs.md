# PR #1067: round-two detector repairs

Root accepted the twelve-finding adversarial review and assigned the confirmed
detector defects to the existing CLI lane. The graph remains pinned to rc.113;
all 100 API entries, signatures, source coordinates and prior edges are preserved.
Conditional guidance adds four edges among existing clock and assertion helpers.

The detector now distinguishes deferred service/local-function waits from direct
test execution while retaining uncertain waits as judgment. Test-clock reachability
is separate from live-only reachability, including helpers reached through both
modes. Direct stalls are not exempted merely because a callback also forks or
adjusts a clock. Exact repository Option-facade provenance is recognized; projected
plain values remain legal assertions.

Filesystem candidates require actual filesystem provenance through the supported
ESM/CommonJS imports or resolved barrel references. Path, Spawner, shadowed bindings
and lookalikes remain negative. Opaque resource references are explicitly unproven
review candidates. For-of and for-in loops are indexed and independently checked;
ordinary iteration remains negative and retry candidates remain judgment.

Shared clock guidance calls for ownership and concurrency review. Neither a
setTime hook nor a single nested test proves isolation. Unknown module mocks remain
visible judgment candidates whose replacement requires a real service key.
The three broader reviewer proposals waived in DECISIONS.md do not waive the
confirmed defects.

Assertion routing preserves family, matcher polarity, negation, supplied operands
and compound expressions. Root's follow-up caught a remaining non-progress route:
a Boolean-only helper supplies no expected payload, error or Cause and would itself
be detected again. Such fallbacks are judgment. Exact Option-None routes remain
mechanical; no expected payload is invented. Twelve parameterized cases cover
24 direct-predicate and existing-helper forms with explicit expectations.

## Verification

- The initial round-two repair passes 124 focused cases and full repo-cli package
  audit/docgen in 425.790 seconds.
- The final polarity follow-up passes 136 focused cases. All 171 preceding detector
  and 90 contract assertion expressions remain; four incorrect table expectations
  change classification from mechanical to judgment. No tests are removed.
- All seven integration files pass 167 cases from the package directory, including
  the new main-branch law scanner tests and lint-worker dispatch tests. An earlier
  run from the repository root failed two package-relative argument expectations;
  correcting the launch cwd proves the cause without editing those expectations.
- Final compiler, scoped lint, canonical Fallow audit and baseline health pass.
  Initial complexity and tuple-inference failures in the new regression were
  repaired without new suppressions or policy changes.
- Final full repo-cli package verification passes in 455.366 seconds: audit
  436.6 seconds and docgen 17.0 seconds.
- Root verifies all 124 initial and 82 follow-up evidence artifacts. Source hashes
  remain fixed throughout the final package proof.

The separate Knowledge.refs change replaces TypeScript's deprecated getTextPos
alias with getTokenEnd. The actual TypeScript 6 implementation and 23 token
positions agree, all 51 focused knowledge tests pass, and the deprecated-API
ESLint profile passes. This source is present in the final repo-cli package proof.

Private evidence is retained under
`~/.cache/beep/effect-vitest-canon/pr1067-resume/round2-detector-repair/`,
`round2-polarity-followthrough/` and `b862-compiler-api-repair/`.
The package proof used the preceding 8,026-row inventory. Complete row
reconciliation, canonical adoption, final CLI coverage, normal-command timing,
round three and exact-head hosted closeout are separate remaining gates.

## Services provenance and assertion boundaries

The complete 8,026-to-7,998 comparison found two further defects before adoption:
23 Services-subpath resource candidates disappeared, and seven new EV006 findings
checked plain values with incidental Option expressions deeper inside them. Root
rejected that candidate. The subsequent bounded repair collects members for only
the verified NodeServices and BunServices subpaths and recognizes their actual
layer export as an unproven resource judgment. Type-only imports, shadowed names,
lookalikes, Path and Spawner remain negative.

Assertion traversal stops at unrelated calls, plain projections and function
bodies. Direct Option/Result/Exit values and predicates, Boolean compounds and
proven Effect Array predicate compositions remain visible. The prior A.every
fixture gains its missing effect/Array import; its expected route is unchanged.

All 148 focused cases pass. The compiler, scoped lint and canonical Fallow
audit/health pass. All 175 preceding detector and 90 contract assertion
expressions are preserved. Only the two owned files changed among 5,408 captured
code/configuration inputs. Full repo-cli package verification passes in
458.082 seconds, with audit at 436.2 seconds and docgen at 20.0 seconds. Root
verifies all 59 evidence artifacts and unchanged final proof hashes.

Private evidence is retained in services-subpath-followthrough under the same
cache root. These proofs do not establish the full live finding count: the fresh
comparison must confirm all 23 restored findings, all seven removed false
positives and every additional delta before the canonical writer runs.

The fresh preview restored all 23 Services rows, retained 7,661 full finding
payloads and removed 337 EV006 rows, producing 7,684 candidates. Root rejected
adoption because at least one removed assertion directly compares an Option
returned by Option.map. The passing package result above remains valid for its
source hashes, but does not close this additional detector defect. Return-family
provenance and complete removal attribution are under review before a further
bounded repair. No candidate inventory or coverage floor has been written.

## Accepted tagged-value correction

The final bounded correction recognizes complete applications of the verified
tagged-value operations, the final supported pipeline stage, returned predicates
for exact Array APIs and delegated yield of Effect.exit/result. An unexecuted
Effect, a partial application, a terminal getter or an unrelated callback does
not establish an asserted Option/Result/Exit. Exact repository Array namespace
provenance was fixed after the first expanded run exposed the Utils.A.every form.

All 207 focused cases pass, with all 182 preceding detector and 90 contract
assertion expressions preserved. The actual-source matrix restores all 20
confirmed assertions and excludes the seven original plain-value negatives.
Compiler, scoped lint and Fallow audit/health pass. Full repo-cli package
verification passes in 423.690 seconds: audit 406.4 seconds and docgen 15.5 seconds.
Root verifies all 82 artifacts and the full 6,539-input source snapshot before
and after the proof. Initial namespace-test and private harness diagnostics remain
in the retained evidence; they are not relabeled as passing runs.

The independent final comparison is accepted in
pr1067-round2-current-delta-review.md. The canonical writer passes in 9.827 seconds
and reproduces all 7,775 reviewed rows and the 1,110-record census exactly, with
no source drift. Final normal timing, CLI coverage, round three and exact-head
aggregate/hosted closure remain separate gates. Private implementation evidence
is retained in tagged-value-followthrough under the same cache root.
