# P0.5 core-preservation implementation review

Status: complete; one introduced performance finding (R1) remains for Root/source-writer disposition. Read-only Codex review; not a P0f adversarial round.
Scope: this report and new private `p05-core-preservation-review-astra*` evidence only.
Permissions: unrestricted filesystem and approval never; no managed-profile mismatch.
Source: `~/YeeBois/projects/beep-effect2-worktrees/effect-vitest-filesystem`.

## Progress

- Created the report before investigation. Will snapshot inputs, compare promoted implementation and tests with accepted originals, and qualify terminal drift. No source/config/git/package-proof operations.

## Captured input and review progress
- Retained 33 input files and SHA-256 records in `~/.cache/beep/effect-vitest-canon/p05-core-preservation-review-astra-r35ottt1/inputs.before.json`; `snapshot/` is the review source and `packet/` retains the prior decisions/readiness evidence.
- Promoted core snapshot SHA-256: `ecdc7da0cfc649d2e1b5cf54687cf9a4afac1446a1eb0f547cfd1043a9f2424f`. Terminal comparison retained the same core hash.
- One Graft skeleton query automatically refreshed one indexed file before answering; no graph rebuild was requested. Its omissions around `fnUntraced` are resolved from captured live source.
- Reviewed the graduated test diffs: all 17 original regression names and all 12 characterization names remain in order. Failure checks now using `Effect.flip` still fail on unexpected success; error metadata, byte/state assertions and watcher assertions remain. The two intentionally shared-volume cases are enclosed in `describe.sequential`.
- Initial candidate: a suspected overescaped temporary-fragment pattern at `MemoryFileSystem.test-kit.ts:179`; original `volume.ts:2131–2134` rejected only slash and NUL. A private public-operation probe will determine the concrete behavior difference.
- Temporary-fragment candidate **disproved**: byte-level inspection finds one regex backslash, not two; the apparent double escape was tool serialization. The private Bun probe executed 72 observations (36 original/promoted pairs) across all four temporary APIs, prefix/suffix as applicable. Plain, `u`, `0` and backslash succeed; slash/NUL fail identically, including error metadata and visible `/tmp` contents. No temporary-name regression found.

## R1 — introduced lookup complexity regression (P2)
- Captured `src/MemoryFileSystem/MemoryFileSystem.test-kit.ts:389–395`: `getDirectory` now uses `InodeEntry.guards.Directory(entry)` where old `scratchpad/memfs/internal/volume.ts:289–295` checked only `_tag`. `resolve` invokes it for each ordinary path component (`:681`).
- This is a full schema check: installed rc.112 `effect/src/Schema.ts:6353` constructs `.guards` with `is(toType(schema))`; the directory model includes every entry in `S.HashMap(S.String, Inode)` (`MemoryFileSystem.test-kit.ts:207`). It introduces work proportional to sibling count into ordinary lookup.
- Bounded captured-source Bun probe: five measured rounds of 100 `stat("/width/f0")` operations after warmup, with 32 and 1,024 sibling files; every stat asserted size `1n`. Median old/promoted times: 32 siblings **0.645 / 2.690 ms**; 1,024 siblings **0.364 / 56.564 ms** (about **156×** at the larger width). Exact samples: private `directory-lookup-probe.json`. Timings are local observations, not a portable performance threshold; the full-map validation explains the scaling change independently.
- Recommended scoped repair: use schema-derived tag discrimination for already typed inode variants (`InodeEntry.isAnyOf(["Directory"])`, and equivalent hot variant dispatch), retaining schema models and validation at actual construction/input boundaries. rc.112 `Schema.ts:6326` implements `.isAnyOf` with only discriminant membership. No new export, resolver work, source role or global law change is needed. Root should resolve this implementation finding before accepting core preservation; this is not a claimed failure of a package gate.

## Additional preservation evidence
- The entire 25-property `FileSystem.make` wiring object is structurally unchanged (`:2901–2928`); `makeReadyVolume` is unchanged apart from location.
- All 12 characterization call trees are identical ignoring comments/formatting; 10/17 regression call trees are identical. The seven changed regression bodies retain the same failure, metadata, concurrency and no-mutation assertions after the Effect helper adaptations. Exact per-case mapping is in private `case-structure.json`; printed diffs were reviewed, not just names/counts.
- Full promoted MIT notice (`:22–42`) exactly matches the retained installed rc.112 `LICENSE` after stripping comment prefixes; upstream PR, pinned head, both author credits and source path remain at `:16–20`.

## Core preservation assessment
All abbreviated source locations below refer to `packages/tooling/test-kit/test-utils/src/MemoryFileSystem/MemoryFileSystem.test-kit.ts` in the captured filesystem worktree.
- **Stored data and absence:** inode identities/metadata/variants (`:125–216`), descriptors/state (`:247–278`), resolution/clone policies (`:280–329`), open modes (`:714–748`) and parser models (`:2398–2455`) are schema-derived. Inode and descriptor lookups and optional parser results use `Option`; technical volume interfaces, handles, worklists, watch queues and transition envelopes retain their technical roles. Full signed cursor values remain accepted. Mode/owner checks retain uint32 bounds and size checks retain nonnegative safe integers: rc.112 `Schema.isInt` is `Number.isSafeInteger` (`effect/src/Schema.ts:8298–8300`), and `isUint32` adds inclusive `0..4294967295` (`:8430–8434`). No accepted-value narrowing was found. R1 concerns the cost of full internal guards, not a demand to discard these schemas.
- **Bytes and cursor:** file creation/copy retain `slice()` ownership (`:496–523`, `:1387–1429`); descriptor reads retain internal `subarray()` followed by copying into the caller's buffer (`:1771–1817`); `readFile` returns a copy (`:2083–2098`). Writes preserve allocation-before-mutation, append positioning, zero-length early return and the in-place commit-region write (`:1819–1862`). Signed seek and zero-length IO ordering remain at `:1908–1967`; truncate retains bounded subarray copy and cursor adjustment (`:1957–1987`, `:2155–2176`).
- **Ordering/parser:** the five code-unit sorts use `Order.String`, while three directory clone/removal sorts retain `localeCompare` through `localeEntryOrder` (`:137–140`). Escapes, classes/ranges/negation, dotfile handling, root-relative restrictions and globstar matching retain the original branches (`:2459–2790`). Brace expansion remains capped at 256 alternatives and nesting at the existing 256 levels; no parser limit was raised or removed.
- **Transactions/resources/errors:** the permit still encloses transition, state assignment and ordered event publication; acquisition remains interruptible, normal mutations uninterruptible, and interruptible mutations restore only the computation before commit (`:2796–2851`). Watch registration, pending-event handoff, publication and scoped deregistration retain their ordering (`:428–455`, `:2857–2895`). File descriptor `acquireRelease`, temp finalizers and error-path/method mapping remain; `fileSystemError`, `withSystemErrorPath` and `withOperationError` have identical parsed tokens. No host fallback, inspection facade, `currentState` hook or eager global volume is present.
- **Fresh volumes:** `makeReadyVolume` allocates and prepares `/tmp` on each evaluation, `make` maps that effect, and `layer` remains `Layer.effect(FileSystem.FileSystem, make)` (`:2930–2997`). Separate acquisitions/builds allocate independently; a shared `it.layer` block deliberately shares one volume.

## Graduated tests and boundary
- The 17 regressions still cover independent layer blocks; normalized paths; final symlinks; missing removal; dangling-link exclusive create; same-inode rename; agreed copy-error metadata; serialized appends/exclusive races; invalid-size byte preservation; exact POSIX metadata; non-traversed glob symlinks; bounded recursion; committed event payload/order; hard-link update paths; recursive versus direct-child watching. The seven edited bodies preserve these assertions: failure `Result` checks become `Effect.flip`, concurrent result counts use Effect Array helpers, and Option metadata assertions remain explicit.
- The 12 unchanged characterization bodies retain shared-block ordering, independent `make`, buffer ownership, inode/link/copy identity, exact modes/owners, signed seek/error metadata/zero IO, validation bounds without mutation, code-unit versus locale ordering, escaped glob syntax/classes, braces/dotfiles, and 256/257 expansion/nesting boundaries. The removed runtime `console.log` was telemetry, not an assertion.
- All three promoted test modules import `@beep/test-utils/MemoryFileSystem`; both exported constructor/layer examples use it. The conformance registration still passes the real layer to the shared helper (`test/MemoryFileSystem/Conformance.test.ts:1–6`). This review does not repeat the writer's 50-test runs.
- Curated `src/MemoryFileSystem/index.ts:8` reexports exactly `layer, make`; the role exports exactly those two values with explicit Effect/Layer signatures and meaningful examples. The package root has no new Memory reexport. Source manifest entries are exactly `"./MemoryFileSystem": "./src/MemoryFileSystem/index.ts"`, `"./MemoryFileSystem/*": null` (`package.json:19–20`); publish entries are exactly `"./MemoryFileSystem": "./dist/MemoryFileSystem/index.js"`, `"./MemoryFileSystem/*": null` (`:86–87`).
- This matches the accepted explicit-boundary decision and test-kit anchor shape (`standards/ARCHITECTURE.md:681–696`). Inherited workspace aliases still permit source routing around null exports; nothing here claims otherwise. No global alias/law/Vitest rewrite prerequisite is introduced. The writer's final actual consumer/package receipts remain Root's separate acceptance evidence.

## Focused commands and limits
`P` below denotes `~/.cache/beep/effect-vitest-canon/p05-core-preservation-review-astra-r35ottt1`; source commands used the filesystem worktree and non-login shells.
| Command | Exit / evidence |
| --- | --- |
| `graft skeleton packages/tooling/test-kit/test-utils/src/MemoryFileSystem/MemoryFileSystem.test-kit.ts` | 0; one automatic indexed-file refresh; 24,046 estimated tokens saved. |
| `node "$P/compare.mjs"` | 0; syntax inventory and normalized source/test diffs. |
| `node "$P/probe-setup.mjs"`; `bun --version` | Each 0; Node v24.20.0, Bun 1.4.2, Effect and @effect/vitest 4.0.0-rc.112. |
| `bun --tsconfig-override "$P/probe.tsconfig.json" "$P/temporary-fragment-probe.ts" > "$P/temporary-fragment-probe.json"` | 0; 72 observations; subsequent Python assertions passed all 36 outcome/content pairs and all valid/invalid expectations. |
| `bun --tsconfig-override "$P/probe.tsconfig.json" "$P/directory-lookup-probe.ts" > "$P/directory-lookup-probe.json" 2> "$P/directory-lookup-probe.stderr"` | 0; R1 samples and size assertions completed. |
| `node "$P/assertion-structure.mjs"` | 0; all 29 cases mapped, 12/12 characterization and 10/17 regression call trees identical. |
- Both Bun probes emitted a nonfatal `Internal error: directory mismatch` diagnostic naming the explicit private tsconfig; they completed with exit 0 and full captured results. This harness warning is retained as a limitation, not relabeled as a clean runtime log. Probe imports point directly to captured cores; private aliases/dependency links supply the installed unchanged rc.112 dependencies.
- Supplemental dependency capture initially attempted an obsolete flat `FilePath.ts` path and failed; the exact captured aliases resolved `FilePath/index.ts` and `LiteralKit/index.ts`, then the relevant source/guards and eight dependency files were retained and hashed. No package/source repair was performed.
- No package-wide verification, full suite rerun, hosted proof, publication, repository fixture/config edit, inbox action or git operation was performed. This read-only comparison and two bounded probes do not prove every behavior, interleaving, platform or performance characteristic.

## Terminal drift and disposition
- `inputs.after2.json` compares **41 captured inputs** (33 original inputs plus eight supplemental dependency files): **40 unchanged**. Every captured package source/test/config/manifest, lock/runtime pin and original scratchpad/characterization input is byte-identical. No new file appeared in the promoted source/test directories (`added.after.json`).
- The sole drift is Root/writer-owned `history/lanes/p05-core-promotion.md`: initially 18 appended receipt lines, then revised status/history wording and an appended final implementation handoff during terminal recheck. Both versions are retained; the aggregate captured-to-terminal difference is `terminal-final-drift.diff`. These are documentary receipt/status edits, not source-code or source-comment changes. Before SHA-256 `62d95331500dd7ba722d9c9f861f9b95c43eab115d7fcbc39c55a2da8b3b8a1b`; terminal `9820f9271fee837c0fa21c4c7d01cc96dc2da673b9778997da06386f17f3ae24`. The first final-receipt assertion correctly failed on this additional report drift; it was recaptured, not ignored.
- **Disposition:** one concrete introduced performance regression, R1, remains on the unchanged current core. No additional functional preservation failure, weakened graduated assertion or scoped public-boundary violation was found. Root should combine this review and R1 disposition with the writer's actual final runtime/package/consumer receipts; no failed mandatory gate has been converted to a pass here.

## Conformance Fallow review — complete
- Independent P0.5 implementation review of `packages/tooling/test-kit/test-utils/src/FileSystemConformance.ts` only. The active MemoryFileSystem refactor and combined package acceptance are outside this review.
- Ownership is this appended section and new private `p05-fallow-conformance-review-astra*` evidence. Permissions remain unrestricted filesystem / approval never. Source commands use the filesystem worktree and non-login shells. No Graft initialization, inbox disposition or repository source/config/test write is authorized.
- Captured helper/preimage/upstream hashes match all three supplied pins: `30fbdb2a4f344aa8bc62c9c9577dd2b68a23b51374b9a5a60a8d059aeecdb552` / `7a088666d2734dbc27089e8d5a4faac36c79787b01f069c20baab7a3d824922e` / `8725010039e5ef2cee8b9b4fbcdb076f4099e8a44fe393a8e032c5fd89808abe`. Immutable review copies and writer-proof copies are under `~/.cache/beep/effect-vitest-canon/p05-fallow-conformance-review-astra-qqbl_2op`.
- Graft `callers testLayer --depth all --in packages/tooling/test-kit/test-utils/` exited 1 with the known missing dual-bound symbol; no initialization/refresh was reported. Targeted source fallback located the real dual declaration and stable Node/Bun consumers.
- Independent AST checks now confirm 21 same ordered cases, all 46 assertion expressions equal to the preimage and pinned upstream (only the already accepted trim adaptation normalized), 23 explicit scopes, and one direct subject provision in each case. Option schema/public contract/header and 27-byte fixture are unchanged; all six added operations are private and contain no assertions, registrations or scope-closing helper. The initial private selector missed upstream plain `it(...)`; v2 corrected that selector and passed without relaxing comparisons.
- Examined the writer's final compiler/Biome, Node21/Bun21 and isolated Fallow receipts; their before/after helper hashes match the reviewed helper. Exact isolated scan inputs match both pinned helper versions. The seven-to-zero groups result is within one helper file, not the aggregate cross-file gate. No writer command was rerun here.

**Scoped verdict: no findings.** No introduced correctness regression, weakened assertion, resource-lifetime change or binding-law violation was found in this one completed file. This verdict neither closes the earlier MemoryFileSystem finding nor accepts its changing refactor or the combined package.

| Reviewed boundary | Concrete evidence in current `FileSystemConformance.ts` |
| --- | --- |
| Public API/defaults | `:58–91`, `:196–202`: the annotated settings schema, both constructor defaults of true, structural options type and both generic dual signatures are byte-identical to the preimage. `Layer.isLayer(args[0])` still distinguishes data-first from curried registration; omitted options use `{}`. Explicit false is preserved: `:254` skips only writable-directory access; `:287–291` always checks file removal and conditionally checks its directory. No extra public export or option. |
| Fixture/license/directive | `:9–39`, `:93–101`: full MIT text matches the pinned checkout license; attribution/source hash and the sole canonical `strictEffectProvide:skip-file` directive remain unchanged at line 39 with the D14 lifetime reason. The exact UTF-8 fixture is `lorem ipsum dolar sit amet\n`, 27 bytes, written through the subject in a caller-owned scoped directory. |
| Subject/scope topology | All 21 cases retain one direct `Effect.provide(layer)`; all 23 existing explicit scopes and assertion depths remain. `:214–236` retains the unscoped directory acquisition inside the short scope, its uninterruptible outer-scope finalizer registration, and both survival assertions. `:238–296` still closes the relevant shorter scope before NotFound checks; no lifetime-test body was flattened. |
| Setup helpers | `:105–131`: `temporaryDirectory` retains default unscoped acquisition plus recursive `orDie` cleanup; `writtenFile` retains scoped parent → unscoped child → seed write; `openTextFixture` retains subject lookup → fixture → default open; `openTemporaryFile` retains scoped temp acquisition → flag-specific open. No helper closes a scope, memoizes a resource or replaces the subject. The returned path/handle pair is technical wiring. |
| I/O helpers | `:135–144`: the 13 extracted reads retain `readAlloc` → typed `Effect.fromOption` failure → fresh decoder. The 13 extracted writes retain one `File.write` and a fresh encoder, including its returned progress; no `writeAll`, retry or error conversion is introduced. All invocation sites remain inside an executed generator. |
| Flattened generators | `:433–456`, `:513–552`: exactly five redundant outer generators/service lookups are folded into the existing resource-scoped generator. The same scoped operation remains inside subject provision; lookup still receives that subject context. Acquisitions, assertions and descriptor operations keep their order, and resources still release before the layer. |
| Error and mutation assertions | All 46 full assertion expressions match the preimage and upstream. Read-only writes retain the reason exclusion, method and path checks (`:340–352`); exclusive writes preserve their failed-effect check (`:365–375`); copy preserves AlreadyExists/method/source-path and both content checks (`:378–396`). The conditional copy-metadata check is inherited; this refactor does not strengthen it into an unconditional failure assertion. |
| Descriptor semantics | `:399–569`: seek offsets/modes, byte counts, overwrite/append flags, stream offset 6 and limit 5, sequential/backward reads, independent append/read cursors and truncation/clamping expectations retain their exact values and operation order. Path access stays explicit where stream or path writes require it. |

The six private operations each consolidate a repeated resource or text-I/O sequence (3 seeded-file setups, 2 explicit directory lifecycles, 2 text-fixture opens, 6 temporary-file opens, 13 reads and 13 writes). They introduce no registration/assertion wrapper, schema weakening, helper service, source role, timeout, floor, baseline or suppression. The source diff contains only these extractions and the five reviewed generator simplifications.

**Proof scrutiny and supplemental probes.** The writer's `verify-v3.mjs` compares the ordered selected `fs.*`/`file.*` call ASTs after helper expansion, assertion ASTs and integer scope depths. It does not compare every Effect control-flow node, service lookup, return value, decoder callback or cancellation interleaving; equal counts/depths alone would not prove cleanup timing. Direct diff review checked the omitted wrappers/finalizers. No changed operation or scope hidden by its normalization was found. Its sole assertion normalization is the already accepted `text.trim()` → `Str.trim(text)` adaptation.

A new private Node probe uses the exact extracted helper declarations from the captured source, with instrumented services/codecs; it does not import the helper's Vitest registration module, any platform/core implementation, or repo tests. Nine I/O scenarios compare the current helper with the exact preimage inline expression for success, missing chunks, typed failure, method-construction defects and effect defects as applicable. Each program executes twice: construction stays suspended, codec instances are fresh, writes remain single calls and partial progress `2n` is returned unchanged. Six resource scenarios verify helper return before cleanup, descriptor release before temporary-resource release, subject release last, typed open failure cleanup, and cleanup failure remaining a defect. These probes passed; they supplement source review and do not constitute platform or full-conformance proof.

Private command receipts for this continuation (`C` = `~/.cache/beep/effect-vitest-canon/p05-fallow-conformance-review-astra-qqbl_2op`):
- `graft callers testLayer --depth all --in packages/tooling/test-kit/test-utils/`: exit 1, expected dual-binding miss, zero reported token savings. No Graft initialization/repair followed.
- `node "$C/review-structure.mjs"`: exit 1 in the private upstream registration selector; retained. `node "$C/review-structure-v2.mjs"`: exit 0, independent 21/46/23/21 and immutable-header/fixture checks (`independent-structure.json`).
- `node "$C/helper-boundaries.mjs"`: exit 0 under Node v24.20.0 and Effect 4.0.0-rc.112; nine I/O and six resource scenarios (`helper-boundaries.json`). Only declarations extracted into this new private harness are evaluated; source snapshots remain immutable.
- Discovery-only mistakes were confined to reads: a nonexistent generic compatibility-test filename made the targeted fallback exit 2 while the actual Node/Bun consumers were found; an API-introspection one-liner had an extra parenthesis and failed before import, then its corrected form exited 0. No source fix or acceptance check was changed in response.
- Writer evidence examined, not rerun: final helper-pinned compiler/Biome exits 0, Node21/Bun21 exits 0, and Fallow 3.23.0 before 7 groups/16 instances versus after 0 on one exact file with the unchanged repository config. The missing private-root node_modules warning limits that result to the reproduced within-file duplication; Root retains the aggregate gate.

**Terminal integrity.** All four primary inputs, 31 captured writer/upstream proof inputs and 33 guards of existing tests/config/manifests/lock remain byte-identical (`inputs.after.json`, `proof.after.json`, `guards.after.json`). The helper is still SHA-256 `30fbdb2a4f344aa8bc62c9c9577dd2b68a23b51374b9a5a60a8d059aeecdb552`; preimage and upstream pins also remain unchanged. Existing report content before this section is preserved. This lane made no repository source/test/config/platform-patch write and did not inspect or execute the active core refactor. No inbox action/claim, waiver, aggregate/package check, phase decision or publication occurred. Root retains all combined runtime/package/consumer acceptance decisions.

## Final core Fallow delta review — complete
- Independent P0.5 review against Root's accepted R1 preimage. The unchanged conformance verdict will carry forward after hash verification. This review does not ratify a phase or substitute green aggregate checks for semantic review.
- Source commands use the filesystem worktree with unrestricted filesystem / approval never. Ownership is this report and new private `p05-fallow-preservation-review-astra*` evidence; no source/config/test/git/inbox operation is authorized.

- Immutable inputs match the supplied pins: accepted R1 core `c2bf1cfe1e5e082123800f45791fed8ea008ef405f98d22c7dc98422e083eccd` → final core `48548936e9a8c9ef35ba48aedad318ae3458818c6d8653ea9a08608ef4ccfe63`; helper remains `30fbdb2a4f344aa8bc62c9c9577dd2b68a23b51374b9a5a60a8d059aeecdb552`. The completed conformance review above carries forward without repeating its runtime/AST review. New immutable copies, raw diff and receipts are under `~/.cache/beep/effect-vitest-canon/p05-fallow-preservation-review-astra-iim9f2ls` (`P` below).
- Protected-input qualification: 47/48 files exactly match Root's pre-repair manifest. The sole difference is `Quality.command.ts:3458`, the reported scheduler `Flag.withDefault(false)`. Removing precisely that line in a private in-memory comparison reproduces Root's full-file SHA `879a14a16279dbcb5401d2d6fd3786e0f014e3faca8aff6b7d4362729d5ba95d`; current SHA is `a533808247f087a484c4f351a25c7a354acb510833feeda7c58b1998f835ac02`. This proves the directive policy and every other byte in that file are unchanged; no source was edited.
- Independent AST inventory (`review-ast.json`) finds 153 unchanged declarations, 28 changed declarations, 55 additions (including the separately keyed OpenTarget type alias), and no removals. All 25 exact primitive-to-volume bindings, the complete exported make/layer declarations and MIT header match R1. This comparison removes only comments/formatting; it does not expand helpers, rename values or normalize operation/scope differences. All 1,989 raw diff lines have been read.

**Scoped verdict: no findings.** No introduced semantic regression or binding-law violation was found in the final core Fallow delta against accepted R1. The conformance verdict above still applies to its identical bytes. This is an implementation-review conclusion, not full D8 behavior proof, aggregate-gate acceptance, P0.5 ratification or merge readiness.

All locations below are in `packages/tooling/test-kit/test-utils/src/MemoryFileSystem/MemoryFileSystem.test-kit.ts` at the final core hash.

| Reviewed delta | Preservation evidence and reasoning |
| --- | --- |
| Models, validation and surface | `:131–340`, `:712–855`, `:2236–2358`, `:2491–2550`, `:3062–3158`: all 28 original schema/guard declarations and 13 retained contract declarations independently match R1. Stored inode/descriptor/state/policy/parser models, Option absence, mode/owner/name/size/temporary-fragment/open-flag validation and bounds are unchanged. R1's three hoisted schema-derived tag predicates remain at `:221–223`. New ResolvedParent/OpenTarget/BraceGroup/GlobMatchCursor/GlobSelection shapes describe private intermediate results; they do not replace boundary guards or validate whole directory maps on lookup. All 25 exact primitive bindings and only public make/layer remain. Full MIT/header and public JSDoc remain; no inspection facade, currentState hook, host fallback or singleton. |
| Allocation and resolution | `:516–709`, `:814–838`: common allocation still reads the clock before assigning the same inode/counter/metadata; file seeds still use `data.slice()`. Attachment validates name → containing directory → collision → inode. The extracted resolution walk is created inside each execution, retains empty-component/dot/parent directory checks, root clamping, final-link policy, link traversal bounds, absolute-target reset and original error path. No truthiness check substitutes for Option membership or numeric position. |
| Directory creation and traversal | `:922–994`, `:1094–1111`, `:2108–2148`: recursive/default/mode checks precede mutations as before; existing-final-directory behavior and event accumulation order are retained. Worklists/frames remain local technical arrays with the same push/pop discipline and sorting. `Option.map` is used eagerly for the same conditional enqueue operations, without retaining a lazy callback or sharing a walk across calls. |
| Rename | `:1113–1210`: same-entry/same-inode returns remain before mutation; descendant and destination type/nonempty checks retain their ordering and paths. Clock read → optional destination detach → refreshed parent/moved inode lookup → relocation → Remove/Create events stays inside the same atomic transition. The refreshed post-detach parents preserve same-parent replacements and cross-parent directory link counts. |
| Copy | `:1217–1634`: overwrite rejection, source-path error metadata, recursive conflict prevalidation, sorting, metadata/timestamp policy and depth increments remain. Directory merging still detaches an overwritten child before cloning; top-level replacement still clones before detaching. Each merge refreshes the current destination. Fresh clones retain their own depth bound, file bytes are copied, symbolic-link targets stay literal, and clone attachment selects the same directory/link operation. No partial state or event publishes on a failed transition. |
| Open and file bytes | `:1640–1793`, unchanged `:1796–2088`, `:2192–2234`: supported flag and mode validation still precede resolution; exclusive rejection precedes link following. Retry/Ready explicitly carries the next path or file without a truthy sentinel. Creation, permission mask, failed-link reclaim/retry and truncation clock/metadata ordering are retained. Descriptor allocation still starts at cursor zero and updates the same open count/counter. Read/write copies and subarrays, signed seek/clamping, append/read/write flags, partial progress, safe-size/overflow rejection and descriptor errors remain in unchanged declarations. writeFile still closes its descriptor before deriving the same Create/Update/no-event result. |
| Glob grammar and matching | `:2555–2949`: the extracted scanners retain code-unit indexing, escaped bracket/brace handling and the inherited distinction between the outer brace search and scanning inside a selected group. Unrecognized escapes retain the literal backslash and reprocess the next code unit; trailing escapes, initial literal `]`, escaped hyphens, descending ranges and negated classes retain their errors/meaning. Position zero and the minus-one star sentinel remain explicit values. Brace bounds/expansion/compile validation are unchanged. Greedy star fallback, explicit-dot matching and right-to-left dynamic-programming rows preserve the original transitions; closures use the preceding row before assignment. Excluded directories still prune traversal; result and directory-order sorting are unchanged. |
| Suspension, resources and commits | New Effect helpers are invoked from the same executed generators, with explicit state/path/policy dependencies; mutable state is per volume or per traversal, never a lifted shared walk. Pure helper evaluation remains inside those executed effects. Unchanged `:2957–3012` keeps permit acquisition interruptible, transition/commit/publication ordering masked as before, and interruptible-use commit behavior intact. Unchanged MemoryFile/open/temp/watch acquire-release wrappers retain descriptor and temporary cleanup, queue handoff and failure propagation. `:3091–3158` still allocates a fresh volume for each make/layer construction. |

The extractions have concrete responsibilities: path-component traversal, target selection, copy conflict/attachment/merge, token scanning/matching and event routing. They keep critical branch decisions visible in their orchestrating operation and pass dependencies explicitly. No helper wall conceals a validation or resource boundary; no schema weakening, additional public service/source role, timeout/floor/config change, baseline or suppression appears in this delta. Existing tests, manifest/index/export entries, aliases, compiler/Vitest config and platform patch match their protected inputs; the accepted workspace-source-alias limitation is unchanged.

**Supporting evidence examined, with limits.** Writer `source-integrity-complete.mjs` compares printed AST declarations, not transitive behavior; its corrected runtime-first map avoids the earlier type-alias shadowing. This review independently keys declarations by syntax kind/name and reads every changed body/helper. The writer's 2,400-pattern corpus includes short brace/class/escape combinations, explicit 256/257 nesting and 256/512 expansion boundaries, six exclusions and one ordering comparison against byte-identical R1. It compares exact result arrays and error tag/module/method/description/path, not every error field, mutation trace or interruption schedule. No equivalence claim is inferred solely from its counts.

The valid directory comparison imports a relocated original volume whose bytes independently match the surviving original (`3263694ece7deb17136ac0602e2b93d4cade06393e1d29cac1cadc56701c6084`); its Vitest config changes execution plumbing only. It performs warmup plus five rounds of 100 stat/size checks at widths 32 and 1024 for both subjects. Those receipts and retained R1 predicates support preservation of the R1 repair, without a timing SLA. The old direct-Bun GlobalValue initialization failure remains unqualified and was not replayed. Writer Node50/Bun50, compiler/Biome and consumer/package evidence remain separate supporting/Root-owned proof. In particular, the writer's source-only Fallow health command exited **1** with 38 CRAP-only rows and no test graph; its zero complexity/duplication violations do not make that command a passing aggregate gate.

No new runtime probe was needed after the changed paths were resolved by direct preimage comparison. No package, platform, conformance or blanket differential suite was run by this continuation. A read-only delta review cannot prove all filesystem behaviors or all interruption/interleaving outcomes; Root must combine this result with the frozen-source canonical runtime/package/compiler/consumer receipts.

**Commands and terminal integrity.** `graft callers toFileSystem --depth all --in packages/tooling/test-kit/test-utils/` exited 0 with no indexed incoming edges (known fnUntraced coverage limitation); targeted live bindings and immutable diffs supplied the missing details. It reported 29,404 estimated tokens saved in one call. `node "$P/review-ast.mjs"` and `python3 "$P/terminal-verification.py"` each exited 0; runtime version reads exited 0 for Node v24.20.0 and Bun 1.4.2. Scoped discovery/snapshot/hash reads also exited 0; exact reproducible proof commands and attribution are retained in `P/commands.md`.

Terminal comparison found no drift in nine primary input records, seven captured writer-proof inputs, four retained baseline-proof inputs, all 48 protected files during this review, or all 33 prior conformance guards. Relative to the original protected manifest, the only difference remains the independently reproduced scheduler default line described above. Current core/helper hashes remain `48548936e9a8c9ef35ba48aedad318ae3458818c6d8653ea9a08608ef4ccfe63` / `30fbdb2a4f344aa8bc62c9c9577dd2b68a23b51374b9a5a60a8d059aeecdb552`; both preimages and original-volume baseline remain unchanged. Earlier report sections are preserved byte-for-byte. No source/test/config/manifest/patch or old evidence was written, no inbox claim/waiver was made, and no packet phase was changed. Root retains the combined frozen-source acceptance decision.
