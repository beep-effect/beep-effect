# PR #1047 closed-cursor preservation review

Status: complete — scoped no-findings verdict. Independent read-only review of the supplied immutable core/test snapshots, with the published `5eef61179500a7550508b8e60bd500a93e4d1844` preimage identified by Root. Acceptance is limited to the captured bytes.

Commands use the filesystem worktree and non-login shells. Actual permissions remain unrestricted filesystem / approval never. Ownership is this report and new private evidence under `~/.cache/beep/effect-vitest-canon/p05-pr1047-closed-cursor-review-evidence/`. The active source lane and all git/inbox/integration/verification/publication remain outside this review.

## Snapshot qualification and initial review

All four manifest SHA-256 values match: core before `820c35e92308f20f790f2acd7cd932166c3d18b28d30eec44fb847713809fa80`, core after `63a7b5902dcdb8219de50f56ce8c0dbbd3e2628cc74dc8340c7dbdc641799b33`, test before `1661738a890ec04b7dae15e3ca6d22ce3e8fc0b6f346b84d81fb517366a9d12b`, test after `b0a340125d63cc8d963c962330425fa7433e9cbfdf4dff9ba62c865cb804a03c`. The manifest, supplied diffs, four source/test files and two pinned API/platform references are copied into the new private evidence directory and hashed in `inputs.before.json`. No mutable live core/helper/test is read or executed.

Independent AST comparison confirms only the old closeDescriptor wrapper is removed and MemoryFile/open change. Within MemoryFile, only seek changes; closedPosition and close are added. Its constructor, type id, existing fields and all seven other File operations are identical. Every other top-level declaration, imports and exports are unchanged, including schemas/guards, R1 tag predicates, validation, descriptor deletion/reclamation, volume permit/commit functions, 25 primitives and make/layer. All 12 existing characterization case ASTs are unchanged; case 13 is added (`structure.json`, Node exit 0). The copy conformance edit is absent from these snapshots and outside the verdict.

Pinned rc.112 `packages/effect/src/FileSystem.ts:1040–1049` specifies infallible seek, and shared `NodeFileSystem.ts:195–214,253–287` keeps its cursor on the handle after the descriptor finalizer closes. A BadResource seek failure would invent an error channel. Snapshot review instead checks cursor preservation plus unchanged failures for actual descriptor operations. The supplied diff needs no new live symbol discovery; no Graft startup, trust change or live caller query is needed.

## Verdict and checked invariants

**No actionable findings in the supplied closed-cursor snapshots.** The implementation preserves the last live cursor without reopening the descriptor or changing rc.112's public error channel. This verdict excludes all later live bytes, the two possible guard hoists, the intermediate copy-test change, package/full/hosted checks and PR acceptance.

`core.after.ts` and `test.after.ts` below mean the immutable files in `~/.cache/beep/effect-vitest-canon/p05-pr1047-closed-cursor-review-input/`, not the changing checkout.

| Boundary | Exact snapshot evidence and reasoning |
| --- | --- |
| Acquisition and finalizer | `core.after.ts:2100–2104` moves the non-suspending MemoryFile constructor into acquisition and registers release against that exact instance. It still uses Effect.acquireRelease. Pinned `packages/effect/src/internal/effect.ts:3971–3987` masks acquisition plus finalizer registration by default and restores the captured context for release. There is no interruptible gap between descriptor acquisition, construction and registration. Constructor changes add only the valid scalar `Size(0)` initializer; no fallible service/resource acquisition is introduced. Reexecuting open constructs a new descriptor/handle through the same existing acquisition effect. |
| Atomic close and reclamation | `core.after.ts:1995–2003` reads the live position and invokes unchanged `closeDescriptorUnlocked` within one Effect.sync under volume.mutate. The unchanged permit/commit implementation at `:3000–3015` assigns the transition state before event publication and masks the transition after acquiring the permit. `:1825–1839` deletes the descriptor, decrements openCount once, and delegates to unchanged reclamation (`:502–508`), which removes an unlinked inode when its last handle closes. Close creates no watch event. The only new retained value is closedPosition, a bigint Size; existing fd/volume fields remain, with no new descriptor, inode or byte-buffer reference. |
| Repeated close | `core.after.ts:1999` updates the saved position only while the descriptor exists; `:1827` makes later closes identity transitions. A closed seek followed by repeated close therefore preserves that seek's result and cannot decrement openCount again. The finalizer receives the same MemoryFile that the caller used. The only other direct closeDescriptorUnlocked call (`:2234`) still belongs to writeFile's separately allocated internal descriptor; it does not bypass this handle finalizer. |
| Live/closed seek and concurrency | `core.after.ts:2015–2040` retains the same live-descriptor calculation and HashMap update. Closed seek uses the saved scalar, returns unchanged volume state and never restores a descriptor. Both branches use the same mutation permit as close and actual reads/writes (`:1898–1975`): operations before close contribute their committed position; later I/O observes absence, and later seeks use the captured position. No operation sees a half-deleted descriptor or a partly captured cursor. Zero, negative and beyond-EOF positions are not mistaken for absence; closed seeks retain the same Size conversion/arithmetic used by live seek. |
| Other File operations | Stat/sync/read/readAlloc/truncate/write/writeAll are unchanged. The descriptor check in `getOpenFile` (`core.after.ts:758–777`) still rejects closed handles before permission/position/data access; read/write check it even for empty buffers. Existing argument validation still precedes lookup where it did before, so invalid arguments may retain their original argument error rather than BadResource. No operation is converted into successful closed I/O. The new test checks all seven methods using valid arguments and exact BadResource metadata, even after a negative closed cursor. |
| Public contract and existing invariants | `core.after.ts:2015`, `:3135`, `:3169` retain infallible seek, public make/layer and fresh volume construction. The unexported class's close accessor is internal finalizer wiring, not a new public File interface method or inspection API. All original schema declarations/guards, Option representations, 25 primitive bindings, resource wrappers, byte-copy/subarray behavior, validation bounds, glob logic and exported declarations match the before snapshot. The three R1 predicates remain hoisted at `:221–223`; closing adds a descriptor lookup and scalar copy, not whole-inode/schema validation. Full MIT/header bytes are unchanged. |
| Added regression | `test.after.ts:336–346` uses a fresh public Memory volume, writes six bytes, seeks to 1 and reads two bytes before the inner scope closes. Thus expected 3 is the last **I/O** cursor, not merely the last seek input. `:350–354` checks relative/absolute closed seeking `[3,5,7,3]`, beyond EOF and negative start. `:358–372` requires failure and exact tag/module/method/descriptor/description for all seven actual descriptor operations. `:374–378` preserves the caller buffer, closed cursor, new handle's independent cursor and file bytes. The fresh volume makes expected descriptor 3 deterministic without reading a private fd field. All 12 earlier case bodies/assertions remain identical. |

The committed case is sequential; it does not directly force repeated internal close, inode reclamation after unlink, or finalizer contention under interruption. These are explicit coverage limits, not an observed source failure or a reason to expose a public close/inspection hook. The focused probes below cover those internal lifecycle paths, and the unchanged permit/reclamation code supplies the remaining source argument. No missing regression that blocks this bounded correction was identified; durable cancellation/contention coverage could be added through existing public scope operations without changing the API.

## Focused private evidence and limits

`lifecycle-probe.mjs` extracts the exact captured MemoryFile, open/close/reclamation primitives and the five unchanged volume commit/permit declarations, then erases TypeScript types. It runs with actual Node v24.20.0 and installed Effect 4.0.0-rc.112. Initial state, descriptor acquisition and empty-event publication are controlled fixtures; the probe does **not** import the live core/helper/tests or pretend to execute the complete filesystem. It retains both extracted sources and hashes.

Six bounded scenarios passed: (1) exact before-code control returns `[0,0,0,0]`; (2–3) after-code returns `[3,5,7,3]` for unlinked and linked inodes, removes the descriptor, reclaims the unlinked inode, decrements the linked inode to zero open references, and preserves -1 across repeated close; (4) 32 concurrent live relative seeks serialize and close captures 35; (5) interruption before permit acquisition leaves the descriptor intact, and a later close captures 3; (6) an interrupted owner scope waits for a held permit and its new file finalizer completes descriptor removal while retaining 3. Effect construction remains suspended. Direct field inspection in this private extracted fixture confirms only a scalar field was added; it is not a garbage-collection proof or a production inspection facade.

The archived Node and Bun observation logs independently contain platform positions `[3n,5n,7n,3n]` and old Memory `[0n,0n,0n,0n]`. Their platform closed reads report Unknown/native EBADF while Memory reports BadResource; the correction does not homogenize unrelated platform errors. Those logs were examined and captured, not rerun. The implementation report's intermediate strict copy assertion is superseded by Root's explicit scope direction; neither that assertion nor a platform copy patch is accepted here.

Read-only review and these extracted-path probes do not establish every scheduler interleaving, complete public acquisition/I/O behavior, heap reachability, later guard-hoist correctness or package/hosted acceptance. Root retains actual full-core tests, compiler/package checks, combined conformance/copy proof, integration and publication.

## Commands and terminal integrity

`P` denotes `~/.cache/beep/effect-vitest-canon/p05-pr1047-closed-cursor-review-evidence/`. Exact commands and scope qualifications are retained in `P/commands.md`.

- `~/.nvm/versions/node/v24.20.0/bin/node "$P/review-structure.mjs"`: exit 0, independent declaration/member/case preservation checks.
- `~/.nvm/versions/node/v24.20.0/bin/node "$P/lifecycle-probe.mjs"`: exit 0, the six bounded snapshot-extracted scenarios above; Node v24.20.0 and Effect 4.0.0-rc.112.
- `python3 "$P/verify-inputs.py"`: exit 0; all nine snapshot/manifest/diff/API records and all three additional reference/control records unchanged before/after (`inputs.after.json`, `support.after.json`, `terminal-integrity.json`). Initial snapshot/hash and targeted read commands also exited 0.

Final reviewed core/test hashes remain `63a7b5902dcdb8219de50f56ce8c0dbbd3e2628cc74dc8340c7dbdc641799b33` / `b0a340125d63cc8d963c962330425fa7433e9cbfdf4dff9ba62c865cb804a03c`. Both before hashes still match the supplied manifest. Full access / Never ask remained the actual permission configuration throughout. This lane wrote only this report and its private evidence; it performed no git, source/config/test/inbox/baseline/rule mutation, delegation, network research or test execution against the mutable live checkout. No later live source byte or copy correction is included in this verdict.
