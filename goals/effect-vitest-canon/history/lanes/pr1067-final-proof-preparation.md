# PR #1067 rc113 final proof preparation

Status: in progress. Preparation only; Root owns acceptance and execution after package/coverage proof and source freeze. No collection, source copying, scanner, timing, or canonical writes are authorized in this lane.

2026-09-10 completion: driver preparation is complete; execution and acceptance remain pending with Root. Existing drivers/evidence were preserved. The rc113 graph and historical 7417-to-7580 reconciliation were not reopened. No collector, source inventory, source snapshot operation, scanner, benchmark, package proof, coverage command, Git, dependency operation, scheduler mutation or model launch was executed.

The new bundle is `~/.cache/beep/effect-vitest-canon/pr1067-resume/rc113-final-preparation/`. Its `README.md` documents the exact gate shape, commands, path policy, outputs and failure semantics. It contains adapted `prepare-final-freeze.py`, `run-final-scan.py`, `run-final-variability.py`, a render-only `run-current-delta-review.py`, shared `rc113_final_common.py`, a byte-identical collector, the retained resource helper with added host CPU counters, and the same-context delta prompt.

Root must write `~/.cache/beep/effect-vitest-canon/pr1067-resume/rc113-final-input-acceptance.json` after joining authors, tests, coverage/Fallow and fixture producers. No example acceptance file was created. The documented `rc113-final-input-acceptance/v1` gate requires:

- Root acceptance, writers joined, and source-freeze authorization.
- Five reviewed, hash-bound proof references: current runner/CLI package results, both scoped coverage receipts, and full Fallow acceptance. Each names a real successful terminal receipt and its proof source-hash map. Package identities are `@beep/test-utils` and `@beep/repo-cli`; coverage must use those scopes, Fallow must be full. Missing aggregate exit codes require a genuine terminal wrapper receipt; an interrupted process cannot be promoted to success.
- The complete accepted source map, adopted graph hash and rc113 pin, exact discovery globs, and driver-bundle hashes.
- Node 24.20.0, Bun 1.4.2, Effect/adapter 4.0.0-rc.113, an explicit installed Vitest version, Root's compatibility decision and hashed evidence. Installed package manifests and adapter index/utils/README hashes are mandatory and belong to the frozen input map. Vitest 4-versus-5 remains a user/Root scope decision; these drivers neither assume peer support nor change dependencies.
- The immutable historical acceptance reference, fixed 7580-row payload hash, and complete pre-resumption source-manifest hash.

The drivers reject missing/stale inputs before output creation. Gates, proof references, source sets and installed identities are rechecked around commands. The complete inventory is deliberately more conservative than the old Git-based inventory: every file under apps/packages/infra/scripts/standards, root files, and explicit runtime/other extras. The exact excluded cache/dependency directory names are documented. **dist is not excluded.** Every discovered census file must already be included in the accepted hashes; D9 generated test declarations therefore cannot disappear through Git-ignore filtering. Root must review this broader map after source freeze; old smaller maps will fail closed.

Root's command sequence, from the worktree, is:

```sh
python ~/.cache/beep/effect-vitest-canon/pr1067-resume/rc113-final-preparation/prepare-final-freeze.py
python ~/.cache/beep/effect-vitest-canon/pr1067-resume/rc113-final-preparation/run-final-scan.py
python ~/.cache/beep/effect-vitest-canon/pr1067-resume/rc113-final-preparation/run-final-variability.py
python ~/.cache/beep/effect-vitest-canon/pr1067-resume/rc113-final-preparation/run-current-delta-review.py
```

Execution creates fresh sibling `rc113-final-freeze/`, `rc113-final-scan/`, and `rc113-final-variability/` directories. Existing output directories are fatal, including failed attempts; no automatic overwrite/retry is prepared. Freeze retains all accepted input bytes, including detector implementation and graph. Scan retains every census source byte, with copy hashes checked before/after, complete census before/after, full historical/current row arrays, per-file rows, and exact-id whole-payload deltas with **no omitted fields**. It verifies the accepted 7580-row payload and all 1101 retained historical sources; this is input identity verification, not a redo of historical attribution. The final reviewer must account for every residual against immutable bytes without fuzzy pairing, expression-only guesses or reason transfer. The two earlier zero-row historical declaration limitations remain historical.

Each stage records all-source and canonical before/after maps, command logs/receipts, terminal status and an output manifest. Canonical inventory, census and detector JSONL membership/hashes are guarded. None of the prepared commands writes canonical data or creates runtime fixtures. A `failure.json` invalidates the stage. Root must still establish quiescence: an attestation and before/after hashes are not a writer lock and cannot detect a write followed by exact restoration between observations.

Timing retains exactly three ordered raw observations, each including full-command wall time, child CPU time, reported scanner scanMs when present, host CPU counters/load/affinity, memory/PSI, swap/page counters and deltas, and child/ancestor cgroup context. No normalization, best-of selection, cache clearing or scheduling changes occur. All three full commands must be **<=10 seconds**; a slow run is retained and the other ordered observations still run, then the gate fails. Command failure or row/source/canonical drift stops immediately with incomplete evidence. The initial scan also enforces <=10 seconds. Root alone grants timing acceptance.

The delta launcher is now strictly an offline prompt renderer. After a successful scan exists, it verifies the scan manifest and emits `current-delta-review.ready.prompt.md` plus a receipt bound to its exact row/manifest/acceptance identities. It launches no model, embeds no session identifier and performs no live-source collection. Root sends that prompt to the same existing mapper context after sealing the new evidence. The prepared prompt covers every row field, exception status, owner/kind, support-only EV003, declaration scope, implementation transitions and residual blockers; canonical-write approval remains pending.

Unsafe old dependencies removed: preupgrade lane/package/coverage success paths, imported helper coupling to historical status files, Git-based discovery/head checks, self-asserted writer acceptance, session-bound Codex launching, and known-path-only timing hashes. The old variability script recorded the 10-second bound without failing it; the replacement enforces the full-command bound. The collector still imports the current CLI discovery/owner/kind/line-count exports when Root executes it. Those imports were preserved, not runtime-tested while their owners are active; a changed export or dependency fails the future run and needs attribution, never a fabricated green result.

Validation was syntax only: six Python modules compiled without import/execution and the collector passed `node --check` (seven successful checks). A final syntax receipt supersedes the initial syntax receipt after small failure-path and snapshot-coverage refinements; both remain. `preparation-inputs-before.json` records the retained scripts first hashed after initial inspection; `preparation-inputs-after.json` checks those same identities at completion. No pre-inspection timestamp claim is made. The supplementary historical-manifest read is separately identified. `preparation-terminal.json` and `preparation-outputs-manifest.json` seal the result and public report hash; the manifest excludes itself. This is a reviewable preparation bundle, not functional proof or permission to execute before Root's gate.
