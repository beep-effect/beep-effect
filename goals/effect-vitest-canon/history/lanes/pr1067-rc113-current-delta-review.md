# PR #1067 rc113 current delta review

Status: in progress. Offline reconciliation of the accepted pre-resumption 7580-row cohort against the bound final scan. Historical 7417-to-7580 conclusions remain unchanged. Root owns timing and canonical-write approval.

2026-09-10 terminal result: **reconciliation acceptance recommended**, limited to the bound immutable 7580-to-8023 cohort. Every finding and census path is accounted for; remaining rows **0**, remaining census paths **0**, duplicate assignments **0**, unexplained finding losses **0**, unexplained scope shrinkage **0**. Root's canonical-write approval, timing judgment and phase/publication decisions remain pending.

The verified final row payload is `05263b2d6d15fef625c621bf6878b97e0353f8a5d574ff2980d20cfe55d5c121`; the accepted starting payload remains `8a065f795274868f33d0eae90d313c158f57e47f50475350b5a1dd7f68769d0f`. The scan manifest, status and acceptance hashes exactly match the supplied binding. The scan bundle's listed files, all 1101 historical census preimages, all 1106 final census preimages, and the frozen implementation/graph/runtime files read were independently hash-verified. No live worktree source was substituted.

Row accounting is:

| Classification | Before | After |
| --- | ---: | ---: |
| One-to-one proven correspondences | 7435 | 7435 |
| Named test/helper property-transition groups | 140 | 89 |
| Separately explained removals/additions | 5 | 499 |
| **Total** | **7580** | **8023** |

The 7435 correspondences comprise 4015 entirely identical payloads; 2970 unique full-payload matches omitting only ID/line/endLine; 12 additional repeated-payload matches disambiguated by exact equal-line mapping and context; 64 unchanged expressions with changed enclosing-statement anchors; 126 property transitions with matching named contexts and explicit schema targets; 205 unique named-registration property transitions; one formatting-only expression; six verified local schema-guard hoists; six equal-start/context body transitions; 16 retained layer calls with changed suite bodies; and 14 individually reviewed source transitions. The 126 property groups comprise 75 native-check groups and 51 conversions to native property registrations. These groups retain every complete row and source context; they make no claim of whole-payload or generator-semantic equality.

Exact textual IDs alone were insufficient: 144 IDs exist on both sides with changed payloads. Shifted adjacent assertions can reuse an ID. Those candidates were withheld from automatic pairing and resolved through the subsequent context/source evidence. Their before/after assignments are recorded separately. No nearest-line or cross-context expression matching was accepted.

All **733 byte-identical source files**, carrying **3070 findings**, preserve their full row lists exactly. The census is **1101 → 1106**, **991 → 996 tests**, and **110 → 110 support**. There are five added test paths, no removed paths, and no owner/kind changes. **368** existing files changed bytes; **358** changed census metadata, while ten changed source bytes without changing the recorded byte/line counts. Every census path has complete before/after rows, hashes and exact source hunks, including explicit import and property-call changes.

The five new paths are `packages/_internal/db-admin/test/migrate-on-boot.pglite.test.ts`, `packages/foundation/ui-system/editor/test/decorator-node-contracts.test.ts`, `packages/tooling/library/ai-metrics/test/FlightRecordGeneration.test.ts`, and CLI tests `policy-fingerprint-turbo-inputs.test.ts` and `schema-first-arbitrary-coverage.test.ts`. They contribute respectively **0, 0, 1, 2, 0** findings. The other additions occur in existing files.

The **499** standalone new findings are 415 runtime wrappers around native property checks, 18 runtime wrappers around native sampling, one runtime wrapper around the AgentSession property helper, one new scheduler runtime boundary, 14 added provider candidates, one scoped socket wrapper, 22 outer property-result shape assertions, ten other shape assertions, ten native property checks, two live registrations, one platform import, three layer-timeout candidates and one shared-clock candidate. Each record includes the actual emitted expression, registration/helper context, imports and source changes. In particular, the 22 outer EV006 findings arise because the existing `containsDataShape` predicate traverses into their nested property callbacks; their syntactic origin is explained, and they remain open review candidates.

The five standalone retirements are explicit: OIP's combined four-generator assertion becomes four named `it.prop` codec round trips; HTML's `fc.constantFrom` over twelve concrete grammar fixtures becomes exhaustive iteration over those same fixtures; graft's `fc.asyncProperty` and its nested `Effect.runPromise` become one `it.effect.prop` returning the Effect; and fc-runs removes its direct `vitest` import in favor of `@effect/vitest`. Together with the 51 grouped registration conversions, these explain every retired candidate.

Actual semantic changes are retained rather than called preservation. CSV's property now expects null-byte normalization in three text fields. TSConfig's hostile `__proto__` case now expects strict-record failure rather than dropped keys. CI's cache test changes its dispatch expectations, and runner bake now discards inherited dependency caches. CLI shard tests extract named helpers and add `--full`; their runtime calls persist. The runtime fixture keeps `Effect.scoped` while extracting evidence finalization into `retainFixtureEvidence`. The proof-coordinator helper adds lock-directory creation and an explicit test runtime root. An added release-desktop apt-pruning test reuses a textual provider closing line that a diff aligned to an older test; its new named context proves an addition, not a match to the older provider.

Rule totals are EV001 **1467→1901**, EV002 **1204→1218**, EV003 **370→370**, EV004 **134→135**, EV005 **27→27**, EV006 **2873→2905**, EV007 **476→432**, EV008 **3→3**, EV009 **197→199**, EV010 **355→356**, EV011 **169→168**, EV012 **4→4**, EV013 **5→5**, EV014 **290→293**, EV015 **6→7**. Gross exact-ID scanner deltas—3864 added, 3421 removed, 144 changed—were independently recomputed and reconciled to the correspondence accounting above.

All **29 declaration inputs** remain present and emit zero findings on both sides. Twenty-seven are byte-identical. `Vitest.test-kit.d.ts` adds package documentation and updates example imports; `PackageScripts.test-kit.d.ts` adds the `PolicyFingerprintTurboConfiguration` reexport. The two formerly irrecoverable **7417-cohort** declaration preimages remain historical limitations; both have exact, unchanged bytes in this separate 7580-to-final comparison. The two support-source findings are byte-for-byte preserved EV003 rows; all other support inputs emit none. The scanner's support-only EV003 filter remains explicit in the frozen implementation.

The every-field matrix covers **19 encoded leaf/optional fields**, including `replacement.primitive`, `replacement.sketch`, and explicit absence of `reason`/`fixSha`. Full rows are retained beside every comparison, with exact structural omission lists. Both cohorts contain **zero exceptions** and **zero reason-bearing rows**; all 8023 final findings are open. No status, reason or fix attribution was transferred or manufactured. Every one of the **15603** row occurrences also matches its normalized evidence text within its exact retained source range.

The implementation comparison is bound back to the 7580 input hashes. It records native Arbitrary provenance/dispatch and EV007 class additions, EV007 option/sketch changes, detector/syntax helper extraction and harness indexing, unchanged v2 context/token digest construction, schema compiler hoists, canonical-key fallback removal, POSIX normalization of platform-relative paths, and rc113 flag constructors. The unchanged-source payload proof establishes no observed detector drift on those 733 inputs; it is not a universal equivalence proof for every possible program.

The adopted 100-entry graph remains pinned to rc113 with hash `e848ebeaf7ab5691a16a3a2ae450c2b1d2016bfebf1f06aac76d9a0447028bbb`. Root's bound acceptance retains Node 24.20.0, Bun 1.4.2, Effect/adapter rc113 and Vitest 4.1.11 with bounded behavioral compatibility evidence; the adapter's declared Vitest 5 peer remains unsatisfied. This review preserves that explicit limitation and does not reassess the graph audit or runtime compatibility proof.

Detailed evidence is under `~/.cache/beep/effect-vitest-canon/pr1067-resume/rc113-current-delta-review/`. Authoritative outputs are the `*-reviewed.json` row/census ledgers, `property-groups.json`, `row-removals.json`, `row-assignment-index.json`, final field/count/coverage/blocker receipts, source hunks, implementation comparisons and before/after input hashes. Earlier intermediate ledgers are exploratory and superseded by those final outputs; the initially unproven provider addition is explicitly resolved. `terminal-manifest.json` and `outputs-manifest.json` seal this report and the private evidence. Only offline Python/JSON/text reconciliation ran; no scanner, compiler, test, benchmark, Git, network, agent, canonical write or phase change occurred. Root must keep the accepted source/graph/runtime identities frozen through any adoption; subsequent source edits require their own delta review and applicable proof refresh.
