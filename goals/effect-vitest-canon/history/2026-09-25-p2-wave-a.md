# Wave A: fc-runs and ontology-config

Started from main after the identity wave merged in PR #1216. Reuse the
existing inventory; the moving-main census delta remains deferred under the
backlog-first authorization.

The fc-runs child boot probe keeps its command, environment override and exact
stdout assertion. Its success and output assertions now include the child exit
code, signal and stderr bounded to 2,000 characters. It does not adopt the
instrumented runner because the runner already depends on fc-runs; the reverse
edge would create a development dependency cycle.

Ontology success fixtures now use it.layer. Deliberate acquisition failures
use Layer.build inside the Effect test scope, preserving typed failure and
defect polarity. The generic scoped-provider wrapper was removed. Tests now
import the instrumented runner; generated references and Fallow boundaries
were synchronized for the new development dependency.
Implementation commit e8df201fd4 fixes eight inventory rows. The ledger links
those rows to that commit; two existing EV006 rows have reasoned exceptions.

Full fc-runs package verification passed: audit 4.8 seconds and docgen 1.8
seconds. Evidence: `~/.cache/beep/effect-vitest-canon/wave-a-fc-runs-proof.log`.

Ontology package verification passed: audit 6.1 seconds, docgen 2.2 seconds.
The independent Node run passed all six tests. The fc-runs Node reporter and
workstation context were captured, and normalized summaries are tracked.

The initial detector check identified three layer-hook budget candidates and
two rekeyed assertion judgments. Their dispositions below produce a final
full-scan ratchet result of zero introduced findings across 1,159 files.

## Detector disposition

The three pure configuration fixtures now have explicit five-second hook
budgets. No hang was observed and no global timeout changed. The two existing
EV006 polarity checks are reviewed exceptions: their contract intentionally
asserts typed failure and absence of defects, without specifying a full Cause.
The helper conversion rekeys those same two candidates. Their reasons are
recorded in both the baseline and frozen inventory; this does not claim that
the final empty-baseline goal has been achieved.

Exactly six ontology baseline rows were removed (five EV002 providers and the
EV003 wrapper). Every unrelated baseline row remains semantically identical
and in its original order. Full ontology package verification passed again
(audit 4.7 seconds, Docgen 2.0 seconds) after the hook budgets were added.
Fresh Node timing runs passed all eight fc-runs and six ontology tests. All
ten selected inventory rows passed strict schema decoding.

Normalized after timing summaries and pressure context are now recorded for
both packages. Raw reporters remain private. No local full-repository coverage
run is requested; the P2 contract assigns that proof to hosted checks.

## Publication

PR #1235 is open. Commit 55df5c011c records ledger dispositions and
271bb848c0 accepts the ontology runner dependency in the reviewed cache
projection. Cache policy reports zero blocking findings. Generated changes
alter only ontology dependency lists; global configuration, commands and
outputs remain unchanged. This grants no runtime cache qualification.

Hosted checks and review closeout remain required. Apply ready-for-heavy only
after non-heavy code checks pass and review threads are clear. Benjamin retains
merge authority. The next wave is not started before this prerequisite lands.
