# P1 inventory contract proof

The P1 schema correction accepts the charter's four `L-*-NONE` coverage IDs
and enforces their lens, coverage class, severity, confidence, status and absence
of a fabricated fix. Existing EV detector/resource routing remains valid.

Normal `bun run beep quality package-verify @beep/repo-cli` passed its audit
and docgen on command-scoped Node 22.22.3 and Bun 1.4.2. The final focused suite
passed 237 tests. The source comparison preserved all 90 original assertions,
14 registrations and 28 non-import statements, adding seven assertions and
three registrations. Earlier introduced proposal failures remain in private
receipts; their passing replacements use the installed rc.113 APIs.

The versioned private validator passes 25 controlled checks. It explicitly
rejects excess finding fields and keeps detector-origin resource candidates
separate from independent human lens coverage. It never grants inventory or
baseline acceptance automatically.

Root verified 73 repair artifacts and 339 validator artifacts against their
manifests. Root's later partial validation used the refreshed 1,122-file census
and all 8,549 supplied rows: 8,228 detector candidates and 321 human rows from
76 fully audited files. All rows passed; no input drift or coverage contradiction
was found. The 321 human rows contain 65 proposed findings and 256 coverage
rows. There are still 4,184 missing file/lens reviews. Schema's first 40 files
remain private chunks until the entire 82-file package is assembled.

The first Root validation attempt omitted the copied helper's module-resolution
symlink and failed before decoding. Restoring that private symlink produced the
reported result; no source or validator logic changed. Prior failed evidence was
retained. Generated support declaration metadata remains included in the census.

All benchmark attempts and normalization preceded the source correction; their
original identities remain attached. This proof does not complete P1, authorize
P2 remediation, mark detector findings fixed or raise a baseline.
