# MCP kit scope reconciliation

All seven previously inventoried files were read at the current source state.
The nine round-trip properties are already public it.effect.prop registrations
with fcRuns(50), inherited from landed main changes. Their historical inventory
rows must retain provenance instead of crediting this wave for those migrations.
The field-value, refused-handler-execution, and hard-gate absence assertions
remain actionable. Five added files remain pending separate reconciliation.

Nine suite registrations in the inventoried files now use it.layer with an
explicit five-second hook budget. The exact layer expressions and test bodies
are preserved. Hard-gate configuration remains upstream of layer construction;
soft credentials remain per-call reads; recording tracers and caller overrides
remain case-local. The real HTTP conformance fixture is retained.

Inverse comparison passed after removing the registration-only edits and
normalizing formatting/imports. Full package verification passed (audit 8.1
seconds, docgen 3.1 seconds); all 95 tests passed on Bun. No production code
changed. Assertions, property oracles, flake review, instrumentation, final
inventory, and hosted proof remain outstanding.
