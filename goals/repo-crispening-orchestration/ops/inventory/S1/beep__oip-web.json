[
  {
    "ruleId": "SFV4-fn-schema",
    "file": "apps/oip-web/src/content/OipSeo.ts",
    "line": 38,
    "symbol": "makeJsonLdGraph",
    "smell": "Exported function builds a machine-readable JSON-LD wire payload with an inferred object return shape rather than a schema-carried contract.",
    "proposedTarget": "Introduce an `OipJsonLdGraph`/`OipJsonLdNode` boundary schema near `OipSeo.ts` and make the helper return that schema type, or wrap the projection with `Fn({ input: OipSiteContent, output: OipJsonLdGraph })`. In-repo exemplars: HTTP wire response schemas in `apps/oip-web/src/contact/ContactSubmission.http.ts:38` and `apps/oip-web/src/contact/ContactSubmission.http.ts:66`, plus `Fn` usage in `packages/foundation/capability/colors/src/internal/ColorsSchema.ts:66`.",
    "confidence": 0.56,
    "mechanization": "judgment",
    "roiRank": 0.28,
    "exception": {
      "reason": "JSON-LD is an external schema.org projection assembled from already-decoded OipSiteContent and escaped at the Next.js script boundary; a local partial schema would duplicate schema.org terms without strengthening the trusted content boundary.",
      "boundary": "schema.org JSON-LD projection boundary over decoded OipSiteContent"
    }
  },
  {
    "ruleId": "SFV4-fn-schema",
    "file": "apps/oip-web/src/contact/ContactSubmission.service.ts",
    "line": 278,
    "symbol": "submitContact",
    "smell": "Exported workflow function in a schema-modeled contact module accepts `unknown` and returns an Effect.",
    "proposedTarget": "Hypothetical `Fn({ input: ContactSubmission, output: EffectSchema<ContactSubmissionResponse, never, never>() })` or a narrowed `ContactSubmission` parameter - audited and rejected for this exported boundary.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 0.08,
    "exception": {
      "reason": "`submitContact` is the untrusted server workflow boundary: it immediately decodes `unknown` through `decodeContactSubmission` and maps decode failure to the public rejection response. Narrowing the public parameter would move the decode obligation to callers and weaken this boundary.",
      "boundary": "server contact workflow boundary (unknown request payload -> ContactSubmission schema)"
    }
  },
  {
    "ruleId": "SFV4-fn-schema",
    "file": "apps/oip-web/src/contact/ContactSubmission.model.ts",
    "line": 278,
    "symbol": "contactSubmissionPayloadFromFormDataEffect",
    "smell": "Exported function in a schema-modeled file takes browser `FormData` instead of a schema-modeled object.",
    "proposedTarget": "Hypothetical DOM `FormData` schema or `Fn` wrapper - audited and rejected.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.05,
    "exception": {
      "reason": "The parameter is a browser/platform boundary object. The helper projects it into a plain input object and immediately decodes with `ContactSubmissionFormPayload.decodeUnknownEffect`, so the payload contract is carried by the schema at the right boundary.",
      "boundary": "browser FormData boundary -> ContactSubmissionFormPayload"
    }
  },
  {
    "ruleId": "SFV4-fn-schema",
    "file": "apps/oip-web/src/contact/ContactSubmission.model.ts",
    "line": 301,
    "symbol": "contactSubmissionPayloadFromFormData",
    "smell": "Exported synchronous helper in a schema-modeled file takes browser `FormData` and returns a schema class.",
    "proposedTarget": "Hypothetical DOM `FormData` schema or `Fn` wrapper - audited and rejected.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.05,
    "exception": {
      "reason": "This is the sync browser form adapter. It decodes the derived object with `ContactSubmissionFormPayload.decodeUnknownResult` and falls back to `ContactSubmissionFormPayload.make(...)`; the domain payload remains schema-carried.",
      "boundary": "browser FormData boundary -> ContactSubmissionFormPayload"
    }
  },
  {
    "ruleId": "SFV4-boundary-codec",
    "file": "apps/oip-web/src/app/api/contact/route.ts",
    "line": 18,
    "symbol": "decodeJsonContactSubmissionPayload",
    "smell": "Route module has an explicit JSON payload decoder next to `request.json()` handling.",
    "proposedTarget": "Inline `S.decodeUnknownEffect(ContactSubmissionPayload)` at the single route boundary use, or leave the named boundary decoder - audited as not actionable.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 0.06,
    "exception": {
      "reason": "`request.json()` is the Next/Web API JSON parse boundary, and the result is immediately decoded through `ContactSubmissionPayload`. There is no ad-hoc `JSON.parse` and no schema bypass.",
      "boundary": "Next JSON request boundary -> ContactSubmissionPayload"
    }
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "apps/oip-web/src/app/page.tsx",
    "line": 18,
    "symbol": "HomeProps",
    "smell": "Local object type alias includes a Promise-wrapped search-param record.",
    "proposedTarget": "Hypothetical search-param schema - audited and rejected.",
    "confidence": 0.15,
    "mechanization": "judgment",
    "roiRank": 0.03,
    "exception": {
      "reason": "This is the Next.js page props surface. Framework-facing props and route signatures are a declared app carve-out for this batch; the actual `contact` value is checked with `S.is(ContactSubmissionStatus)` before use.",
      "boundary": "Next.js page props/searchParams surface"
    }
  },
  {
    "ruleId": "SFV4-fn-schema",
    "file": "apps/oip-web/src/content/OipContent.runtime.ts",
    "line": 173,
    "symbol": "getOipSiteContent",
    "smell": "Exported function returns `Promise<OipSiteContent>` from a schema-modeled content runtime.",
    "proposedTarget": "Hypothetical `PromiseSchema` or `Fn` wrapper - audited and rejected.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.05,
    "exception": {
      "reason": "This is the Promise boundary consumed by Next.js server components. The resolved content is already `OipSiteContent`, and the runtime loader decodes Sanity results through `decodeOipSiteContent` before fallback.",
      "boundary": "Next.js server-component Promise boundary"
    }
  },
  {
    "ruleId": "SFV4-fn-schema",
    "file": "apps/oip-web/src/app/api/contact/ContactHttpApiRoute.ts",
    "line": 14,
    "symbol": "SubmitContact",
    "smell": "Function type alias for an injected HTTP API submit workflow.",
    "proposedTarget": "Hypothetical `Fn({ input: ContactSubmissionPayload, output: EffectSchema<ContactSubmissionResponse, never, never>() })` - audited and rejected for this adapter-local alias.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 0.06,
    "exception": {
      "reason": "Unlike the form-post route alias, this adapter-local alias already uses the `ContactSubmissionPayload` schema-derived type, and the surrounding `HttpApiEndpoint.post` declares the payload, success, and error schemas. A local `Fn` wrapper would duplicate the `HttpApi` contract.",
      "boundary": "Effect HttpApi adapter injection surface"
    }
  }
]
