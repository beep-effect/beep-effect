[
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/drivers/ecfr/src/Ecfr.service.ts",
    "line": 47,
    "symbol": "EcfrShape",
    "smell": "Exported interface with Effect-returning service fields matches the schema-first-inventory surface.",
    "proposedTarget": "None -- fence 1 service-contract carve-out. Keep `EcfrShape` as the `Context.Service` shape used by `Ecfr extends Context.Service<Ecfr, EcfrShape>()` at line 131; response payloads come from the generated schemas and are decoded at the service boundary.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.04,
    "exception": {
      "reason": "Service shapes and Context.Service contracts are explicitly carved out by SPEC Non-Goals fence 1.",
      "boundary": "Context.Service shape for the eCFR driver"
    }
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/drivers/ecfr/src/Ecfr.service.ts",
    "line": 79,
    "symbol": "runJson",
    "smell": "Generic HTTP helper accepts a schema-derived decoder callback and can look like a decode abstraction wall.",
    "proposedTarget": "None -- keep this transport helper as the single eCFR JSON response boundary. The call sites pass `S.decodeUnknownEffect(AgenciesResponse)` and `S.decodeUnknownEffect(TitlesResponse)` at lines 101-102; Effect v4 `S.decodeUnknownEffect` was verified at `.repos/effect-v4/packages/effect/src/Schema.ts:1325`.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 0.05,
    "exception": {
      "reason": "This helper centralizes HTTP status, JSON extraction, schema decode, and EcfrError mapping. It does not replace schema validation; schemas remain the decoder arguments.",
      "boundary": "eCFR REST JSON response boundary"
    }
  }
]
