[
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/drivers/onepassword-cli/src/OnePasswordCli.service.ts",
    "line": 43,
    "symbol": "OnePasswordCliRunner",
    "smell": "Exported function type alias returning `Effect.Effect<OnePasswordCliProcessResult, OnePasswordCliError>` matches the function-contract surface.",
    "proposedTarget": "None while excepted. A hypothetical `Fn({ input: OnePasswordCliRunnerInput, output: EffectSchema<OnePasswordCliProcessResult, OnePasswordCliError, never>() })` was audited and rejected; `EffectSchema` is verified as a factory at `packages/foundation/modeling/schema/src/EffectSchema.ts:83`, and Fn `.implementEffect` / `.implementSync` are verified in `packages/foundation/modeling/schema/src/Fn/Fn.schema.ts:224` and `:403`.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 0.04,
    "exception": {
      "reason": "Runtime runner port for injected native `op` execution. Stable process results are schema-backed as `OnePasswordCliProcessResult`; `command` and `args` are service-supplied argv runtime details. Fn-wrapping would change runner injection semantics rather than move data truth into Schema.",
      "boundary": "native 1Password CLI runner port"
    }
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/drivers/onepassword-cli/src/OnePasswordCli.service.ts",
    "line": 54,
    "symbol": "OnePasswordCliShape",
    "smell": "Service interface with Effect-returning members over schema-modeled 1Password probe/account payloads matches the schema-first-inventory surface.",
    "proposedTarget": "None -- fence 1 service-contract carve-out. Keep `OnePasswordCliShape` as the `Context.Service` shape used by `OnePasswordCli extends Context.Service<OnePasswordCli, OnePasswordCliShape>()` at line 156.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.04,
    "exception": {
      "reason": "Context.Service shapes stay interfaces under SPEC Non-Goals fence 1; account/probe/process result payloads are schema-modeled in `OnePasswordCli.models.ts`.",
      "boundary": "Context.Service shape for the 1Password CLI driver"
    }
  }
]
