[
  {
    "ruleId": "SFV4-fn-schema",
    "file": "packages/architecture-lab/ui/src/aggregates/WorkItem/WorkItem.view-model.ts",
    "line": 175,
    "symbol": "toWorkItemSummaryViewModel",
    "smell": "Exported overloaded `dual` projection in a schema-modeled file has an explicit function type surface, which can look like an SFV4-fn-schema hit.",
    "proposedTarget": "Hypothetical `Fn({ input: ..., output: WorkItemSummaryViewModel })` wrapper - audited and rejected. The parameters are already schema-backed (`DomainWorkItem.WorkItem`, `WorkItemPublicConfig`) and the output is constructed via the `WorkItemSummaryViewModel` S.Class at line 110; `Fn` overloads verified at `packages/foundation/modeling/schema/src/Fn/Fn.schema.ts:637`, but they do not preserve this data-first/config-first `dual` overload surface.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.04,
    "exception": {
      "reason": "SPEC fence 1 preserves overload surfaces. This helper is a pure projection over schema-backed data, and the overload is the public ergonomic API.",
      "boundary": "dual overload projection from domain/config schemas to UI view-model schema"
    }
  }
]
