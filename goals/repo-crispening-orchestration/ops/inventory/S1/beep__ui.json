[
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/ui-system/ui/src/components/phone-input.tsx",
    "line": 86,
    "symbol": "PhoneNumberE164",
    "smell": "Exported E.164 phone-number value is modeled as a bare `string` alias, with the runtime invariant split into the separate `isValidPhoneNumberE164` guard at line 146.",
    "proposedTarget": "Promote the value to an annotated schema, e.g. `export const PhoneNumberE164 = S.String.check(S.makeFilterGroup([S.isPattern(/^\\+[1-9]\\d{1,14}$/, ...), S.makeFilter(isValidPhoneNumber, ...)])).pipe($I.annoteSchema(\"PhoneNumberE164\", ...))` and `export type PhoneNumberE164 = typeof PhoneNumberE164.Type`; split any UI-empty sentinel into a separate input/draft boundary if the public component must continue emitting `\"\"`. In-repo exemplars: the Storybook phone field already defines an E.164 `S.String.check(S.isPattern(...))` schema at `packages/foundation/ui-system/form/stories/fields/Phone.stories.tsx:6`; `HexColorInput` / `HexColor` show branded string-schema value exports with runtime aliases at `packages/foundation/modeling/schema/src/Color/Color.hex.ts:114` and `:143`.",
    "confidence": 0.82,
    "mechanization": "assisted",
    "roiRank": 0.62
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/ui-system/ui/src/components/country-select.tsx",
    "line": 401,
    "symbol": "isCountryCode",
    "smell": "Exported country-code guard re-derives membership by scanning `countryOptions` even though `CountryCode` is already the schema-owned `LiteralKit` source of truth.",
    "proposedTarget": "Replace the helper with a schema-derived guard, e.g. `export const isCountryCode: (value: string) => value is CountryCode = S.is(CountryCode)`, or document an exception if the intended domain is the `countryOptions` subset rather than the `CountryCode` schema. In-repo exemplar: `CurrencyCode` is a `LiteralKit(...)` schema at `packages/foundation/modeling/schema/src/CurrencyCode.ts:43` and exports `isCurrencyCode = S.is(CurrencyCode)` at `packages/foundation/modeling/schema/src/CurrencyCode.ts:118`; `LiteralKit` helper statics are defined at `packages/foundation/modeling/schema/src/LiteralKit/LiteralKit.schema.ts:657`.",
    "confidence": 0.88,
    "mechanization": "assisted",
    "roiRank": 0.41
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/ui-system/ui/src/components/link-preview.tsx",
    "line": 15,
    "symbol": "UrlMetadata",
    "smell": "Fetched/merged link-preview metadata is a structural interface with nullable fields instead of schema-owned boundary data; the metadata crosses a network-derived HTML boundary before being cached and merged into component state.",
    "proposedTarget": "Model `UrlMetadata` as an annotated `S.Class`/`S.Struct` with `S.NullOr(S.String)` fields and a deliberately chosen `url` field (`S.String` to preserve current fallback behavior, or `URLStr` if the same PR tightens invalid-url handling). Then derive `type UrlMetadata = typeof UrlMetadata.Type` and use the schema at the fetch/fallback merge boundary. In-repo exemplars: `ToastData` uses `S.Class` for UI-carried data at `packages/foundation/ui-system/ui/src/components/toast.tsx:135`; `CountryOption` uses `S.Class` for rendered option data at `packages/foundation/ui-system/ui/src/components/country-select.tsx:324`; `URLStr` is the shared URL string schema at `packages/foundation/modeling/schema/src/URL.ts:44`.",
    "confidence": 0.67,
    "mechanization": "assisted",
    "roiRank": 0.33
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/ui-system/ui/src/hooks/useNumberInput.ts",
    "line": 499,
    "symbol": "UseNumberInputOptions",
    "smell": "Exported hook options type extends schema-backed `BoundaryParams` and `SpinParams` with an inline object of booleans and callbacks, which looks like a schema-first inventory hit.",
    "proposedTarget": "Hypothetical `S.Class` for the full hook options object; audited and rejected for this S1 pass.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.04,
    "exception": {
      "reason": "`BoundaryParams` and `SpinParams` already carry the durable numeric data as `S.Class` models; the remaining fields are React hook behavior toggles and callback ports (`formatter`, `parser`, `onChange`) whose accepted functions are component/hook boundary contracts. S2 may later move defaults, but S1 should not schema-ify this full JSX-facing hook options bag.",
      "boundary": "React hook options and callback port"
    }
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/ui-system/ui/src/hooks/useNumberInput.ts",
    "line": 93,
    "symbol": "isNumberInputText (+ isNumberInputEventKey)",
    "smell": "Top-of-file `S.is(...)` guard aliases textually match the S1 guard-helper-wall smell.",
    "proposedTarget": "Hypothetical inlining or helper-static colocation; audited and rejected as not actionable for this package inventory.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.04,
    "exception": {
      "reason": "Both predicates are local, schema-derived, and single-use (`normalizeEventKey` and `handleChange`). The invariants are already carried by `NumberInputEventKey` (`LiteralKit`) and `NumberInputText` (`S.String.check(S.isPattern(...))`); inlining `S.is(...)` would be a terseness cleanup with no schema-as-truth gain.",
      "boundary": "local schema-derived guards inside a React input hook"
    }
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/ui-system/ui/src/hooks/useSpinner.ts",
    "line": 23,
    "symbol": "decodeSpinnerSchedule",
    "smell": "Top-of-file decode helper over `SpinnerSchedule` textually matches the decode-helper-wall smell.",
    "proposedTarget": "Hypothetical inline `S.decodeUnknownResult(SpinnerSchedule)` at the single construction site; audited and rejected.",
    "confidence": 0.18,
    "mechanization": "judgment",
    "roiRank": 0.03,
    "exception": {
      "reason": "The helper decodes one hard-coded spinner timing object immediately below its definition, and the truth is already the private `SpinnerSchedule` S.Class. It is not exported, not reused as a parallel validation wall, and does not hide a schema-worthy payload.",
      "boundary": "single-use local decode for static React timing configuration"
    }
  },
  {
    "ruleId": "SFV4-fn-schema",
    "file": "packages/foundation/ui-system/ui/src/lib/react-invariant.ts",
    "line": 79,
    "symbol": "requireReactContext",
    "smell": "Exported function in a schema-modeled file has overloads and generic parameters, which can look like an `SFV4-fn-schema` hit.",
    "proposedTarget": "Hypothetical `Fn` wrapper around `requireReactContext`; audited and rejected.",
    "confidence": 0.18,
    "mechanization": "judgment",
    "roiRank": 0.03,
    "exception": {
      "reason": "This is a generic React context invariant helper and overload surface. Its only durable input payload is already schema-modeled as `ReactContextInvariantOptions`, and failures are already modeled with `ReactContextInvariantError`; wrapping the generic React context value in `Fn` would not preserve the generic `Value` relationship or JSX hook ergonomics.",
      "boundary": "generic React context utility / overload surface"
    }
  },
  {
    "ruleId": "SFV4-fn-schema",
    "file": "packages/foundation/ui-system/ui/src/components/editor/editor-ui/content-editable.tsx",
    "line": 58,
    "symbol": "ContentEditable",
    "smell": "Exported component function lives in a schema-modeled file and takes a schema class as props, so a naive `SFV4-fn-schema` detector can flag the component contract.",
    "proposedTarget": "Hypothetical `Fn({ input: Props, output: ... })` wrapper; audited and rejected.",
    "confidence": 0.16,
    "mechanization": "judgment",
    "roiRank": 0.03,
    "exception": {
      "reason": "This is a React component boundary. The prop payload is already modeled by the local `Props` S.Class, while React itself expects a callable component returning JSX; an `Fn` wrapper would add invocation semantics that do not belong on JSX component exports.",
      "boundary": "React component props / JSX component callable"
    }
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/ui-system/ui/src/components/notification-card.tsx",
    "line": 175,
    "symbol": "NotificationCardProps",
    "smell": "Component prop interface includes notification display data, actions, and callbacks in a schema-modeled file, which can look like an exported data-payload interface.",
    "proposedTarget": "Hypothetical `S.Class` for all notification-card props; audited and rejected for S1.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.04,
    "exception": {
      "reason": "The schema-relevant finite domains and action payload are already modeled with `NotificationStatus`, `ActionType`, `ActionStyle`, and `NotificationAction`. The remaining interface is a JSX prop surface with `className`, loading UI state, and callback ports (`onAction`, `onMarkAsRead`), explicitly covered by the React carve-out.",
      "boundary": "React component props / callback ports"
    }
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/ui-system/ui/src/components/upload.tsx",
    "line": 78,
    "symbol": "UploadBoxProps (+ UploadBoxRenderProps, UploadProps, UploadAvatarProps)",
    "smell": "Upload component prop interfaces sit in a file that also defines the `UploadImagePreview` S.Class, so broad schema-first inventory scans can flag them as data payloads.",
    "proposedTarget": "Hypothetical `S.Class` props models for the upload primitives; audited and rejected.",
    "confidence": 0.14,
    "mechanization": "judgment",
    "roiRank": 0.02,
    "exception": {
      "reason": "These props are a React/dropzone adapter surface: they extend DOM props, expose `ReactNode` render callbacks, carry `File` arrays from browser APIs, and forward selected `react-dropzone` options. The durable local data (`UploadImagePreview`) is already schema-modeled; the rest belongs to the third-party/UI boundary.",
      "boundary": "React component props / react-dropzone adapter"
    }
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/ui-system/ui/src/components/knowledge-graph.tsx",
    "line": 30,
    "symbol": "GraphNode (+ GraphLink)",
    "smell": "Exported graph node/link interfaces describe render data and may look like schema-worthy payload models.",
    "proposedTarget": "Hypothetical `S.Class`/`S.Struct` graph data models; audited and rejected for S1.",
    "confidence": 0.18,
    "mechanization": "judgment",
    "roiRank": 0.03,
    "exception": {
      "reason": "These interfaces extend D3 simulation datum/link surfaces, which D3 mutates with runtime layout coordinates and accepts node-reference unions. They are public JSX component data props tied to a third-party renderer, not Effect `Graph` models and not stable wire/persisted payloads for this wave.",
      "boundary": "D3 simulation adapter / React component props"
    }
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/ui-system/ui/src/hooks/use-scribe.ts",
    "line": 66,
    "symbol": "ScribeStatus (+ UseScribeOptions, UseScribeResult)",
    "smell": "Scribe hook status/options/result types include finite states, callbacks, and SDK message payloads without local schemas.",
    "proposedTarget": "Hypothetical `LiteralKit`/`S.Class` wrappers for the hook state and options; audited and rejected for this S1 pass.",
    "confidence": 0.18,
    "mechanization": "judgment",
    "roiRank": 0.03,
    "exception": {
      "reason": "This file is a React adapter over `@elevenlabs/client`; event payloads and errors are typed by the SDK, while the local options/result shapes are hook/callback contracts that include `Promise`-returning connection functions and React state. A schema wrapper here would duplicate third-party contracts rather than move a first-party invariant into schema truth.",
      "boundary": "third-party realtime SDK adapter / React hook result"
    }
  }
]
