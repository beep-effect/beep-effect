[
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/capability/colors/src/Colors.ts",
    "line": 91,
    "symbol": "decodeProcessLike",
    "smell": "Top-of-file `const decodeProcessLike = S.decodeUnknownOption(ProcessLike)` textually matches the decode-helper-wall smell.",
    "proposedTarget": "Inline `S.decodeUnknownOption(ProcessLike)(runtimeProcess)` at the single use site (line 93) — audited and rejected as not actionable.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.04,
    "exception": {
      "reason": "Single non-exported decode used exactly once, immediately below its definition, to decode `globalThis.process` at the runtime boundary. This is the correct boundary-codec pattern (schema IS the truth for ProcessLike), not a helper wall; inlining is a terseness nit with no schema-as-truth gain.",
      "boundary": "runtime-boundary codec (globalThis.process → ProcessLike)"
    }
  },
  {
    "ruleId": "SFV4-fn-schema",
    "file": "packages/foundation/capability/colors/src/Colors.ts",
    "line": 198,
    "symbol": "supportsColor",
    "smell": "Exported function in a schema-modeled file (naive SFV4-fn-schema detector hit: file exports S.Class + Fn schemas).",
    "proposedTarget": "Hypothetical `Fn({ input: ProcessLike, output: S.Boolean }).implementSync(...)` — audited and rejected.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.05,
    "exception": {
      "reason": "The parameter contract is already carried by the `ProcessLike` S.Class (schema-typed parameter with schema-constructed default `runtimeProcessLike`); the return is a primitive boolean. An Fn wrapper via `implementSync` would add per-call input decode with throw-on-violation semantics (verified: Fn.schema.ts:403 `decodeInputSync` → `Result.getOrThrowWith`) — a behavior change with no truth gain. Functions whose parameter types are already schema-class instances do not bypass a schema; they use it.",
      "boundary": "schema-typed function contract (parameter is an S.Class instance)"
    }
  },
  {
    "ruleId": "SFV4-fn-schema",
    "file": "packages/foundation/capability/colors/src/Colors.ts",
    "line": 296,
    "symbol": "createColors",
    "smell": "Exported constructor-style function in a schema-modeled file (naive SFV4-fn-schema detector hit).",
    "proposedTarget": "Hypothetical `Fn({ input: S.Boolean, output: Colors })` wrapper — audited and rejected.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.05,
    "exception": {
      "reason": "Returns a schema-constructed `Colors.make({...})` instance; the return contract IS the `Colors` S.Class and the input is a primitive enable flag defaulted from `isColorSupported`. Every produced field is typed by the Fn-based `Formatter` schema. Fn-wrapping the factory adds decode-throwing invocation semantics without moving any invariant into a schema.",
      "boundary": "schema-typed function contract (return is an S.Class instance built via .make)"
    }
  },
  {
    "ruleId": "SFV4-fn-schema",
    "file": "packages/foundation/capability/colors/src/Colors.browser.ts",
    "line": 76,
    "symbol": "supportsColor",
    "smell": "Exported function value (`thunkFalse`) in a schema-modeled file (naive SFV4-fn-schema detector hit).",
    "proposedTarget": "Hypothetical `ThunkOf(S.Boolean).implementSync(thunkFalse)` — audited and rejected.",
    "confidence": 0.15,
    "mechanization": "judgment",
    "roiRank": 0.03,
    "exception": {
      "reason": "Shared `thunkFalse` helper from @beep/utils kept for API parity with the Node entrypoint (CLAUDE.md prefers shared thunk helpers already in scope). Wrapping a constant-false thunk in a ThunkOf schema validates nothing new and would break referential reuse of the shared helper.",
      "boundary": "shared thunk helper / API-parity surface"
    }
  },
  {
    "ruleId": "SFV4-fn-schema",
    "file": "packages/foundation/capability/colors/src/Colors.browser.ts",
    "line": 97,
    "symbol": "createColors",
    "smell": "Exported constructor-style function in a schema-modeled file (naive SFV4-fn-schema detector hit).",
    "proposedTarget": "Hypothetical `Fn({ input: S.UndefinedOr(S.Boolean), output: Colors })` wrapper — audited and rejected.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.04,
    "exception": {
      "reason": "Same shape as the Node `createColors`: returns a schema-constructed `Colors.make({...})` (browser Colors S.Class over the shared `ColorsFields`), parameter is an ignored parity flag. Contract carried by the schema; Fn-wrapping adds nothing. (The `undefined | boolean` parameter union is S2 territory, out of S1 scope.)",
      "boundary": "schema-typed function contract (return is an S.Class instance built via .make)"
    }
  }
]
