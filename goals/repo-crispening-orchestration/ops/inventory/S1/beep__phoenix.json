[
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/drivers/phoenix/src/Phoenix.service.ts",
    "line": 81,
    "symbol": "PhoenixSdkShape",
    "smell": "Exported interface with Promise-returning SDK adapter methods matches the schema-first-inventory detector surface.",
    "proposedTarget": "Hypothetical schema/Fn map for every SDK method -- audited and rejected. `PromiseSchema` is verified as a value at `packages/foundation/modeling/schema/src/PromiseSchema.ts:91`, but S1 fence 1 keeps SDK/client ports as interfaces while request/response payloads remain schema-modeled.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.05,
    "exception": {
      "reason": "This is the injected third-party Phoenix SDK adapter port. Its methods consume and return schema-modeled package payloads, but the live Promise-returning SDK handle is a service boundary, not a first-party data payload.",
      "boundary": "third-party Phoenix SDK adapter port"
    }
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/drivers/phoenix/src/Phoenix.service.ts",
    "line": 109,
    "symbol": "PhoenixShape",
    "smell": "Exported interface with Effect-returning service methods matches the schema-first-inventory detector surface.",
    "proposedTarget": "None -- fence 1 service-contract carve-out. Keep request/response models as the `Phoenix*.models.ts` schemas and keep this as the `Context.Service` shape used by `Phoenix` at line 661; Effect v4 class-style `Context.Service<Self, Shape>()(\"Key\")` verified at `.repos/effect-v4/packages/effect/src/Context.ts:162-163`.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.05,
    "exception": {
      "reason": "Service shapes are explicitly carved out by S1. The interface groups runtime operations; the operation payloads are schema classes.",
      "boundary": "Context.Service shape for the Phoenix driver"
    }
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/drivers/phoenix/src/Phoenix.errors.ts",
    "line": 58,
    "symbol": "isPhoenixOperation",
    "smell": "Local `const isPhoenixOperation = S.is(PhoenixOperation)` textually matches the guard-helper-wall smell.",
    "proposedTarget": "Inline `S.is(PhoenixOperation)` into the `dual` overload discriminator at line 160 -- audited and rejected. Effect v4 `S.is` verified at `.repos/effect-v4/packages/effect/src/Schema.ts:1256`.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.04,
    "exception": {
      "reason": "The guard is schema-derived and used only as the overload discriminator for `PhoenixError.operation`; it does not duplicate validation logic and keeps `PhoenixOperation` as the source of truth.",
      "boundary": "dual overload discriminator over a LiteralKit domain"
    }
  }
]
