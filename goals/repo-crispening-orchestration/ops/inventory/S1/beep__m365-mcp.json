[
  {
    "ruleId": "SFV4-fn-schema",
    "file": "packages/drivers/m365-mcp/src/Server.ts",
    "line": 46,
    "symbol": "makeServerLayer",
    "smell": "Exported function in a schema-modeled file can look like an SFV4-fn-schema hit because it accepts `M365McpServerConfig` and returns a Layer runtime handle.",
    "proposedTarget": "None -- a hypothetical `Fn({ input: M365McpServerConfig, output: Layer.Layer<never, never, M365 | Stdio> })` wrapper was audited and rejected.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.04,
    "exception": {
      "reason": "The config input is already an S.Class. The output is stdio MCP server runtime wiring over Layer/Toolkit handles, which is a service/port boundary, not data.",
      "boundary": "MCP stdio server layer factory"
    }
  }
]
