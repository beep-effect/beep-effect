[
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/architecture-lab/tables/src/entities/Worker/Worker.table.ts",
    "line": 121,
    "symbol": "encodeWorker / decodeWorker",
    "smell": "Module-level `S.encodeResult(DomainWorker.Worker)` and `S.decodeUnknownResult(DomainWorker.Worker)` helpers textually match the codec-helper-wall smell.",
    "proposedTarget": "Inline the schema codecs into `toWorkerInsert` and `fromWorkerRow` - audited and rejected as not actionable. Effect v4 `decodeUnknownResult` is in the current decode family adjacent to `S.decodeUnknownEffect` verified at `.repos/effect-v4/packages/effect/src/Schema.ts:1325`.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.04,
    "exception": {
      "reason": "These are direct schema codecs at the SQL row boundary; `DomainWorker.Worker` remains the executable source of truth for Worker row projection.",
      "boundary": "Worker entity <-> Drizzle row codec"
    }
  },
  {
    "ruleId": "SFV4-boundary-codec",
    "file": "packages/architecture-lab/tables/src/aggregates/WorkItem/WorkItem.table.ts",
    "line": 214,
    "symbol": "fromWorkItemRow / toWorkItemInsert",
    "smell": "Persisted WorkItem row conversion is hand-mapped between Drizzle row aliases and the domain aggregate, including SQL `null` to `Option` conversion, without an executable row codec carrying the boundary contract.",
    "proposedTarget": "Introduce a named WorkItem SQL row/insert codec near `workItemTable` and route `toWorkItemInsert`/`fromWorkItemRow` through it, preserving SQL `null` at the row boundary per SPEC fence 2. Use `S.NullOr(...)` for nullable row fields (Effect v4 `S.NullOr` verified at `.repos/effect-v4/packages/effect/src/Schema.ts:4829`-`:4844`) and keep the domain aggregate as `DomainWorkItem.WorkItem.make(...)`; `Worker.table.ts:121`-`:122` is the local exemplar for table-boundary schema codecs.",
    "confidence": 0.68,
    "mechanization": "assisted",
    "roiRank": 0.58
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/architecture-lab/tables/src/tables.ts",
    "line": 59,
    "symbol": "DbSchema",
    "smell": "Exported type alias over a local object shape can look like a schema-first data-model alias.",
    "proposedTarget": "None - Drizzle metadata/type-level projection carve-out. Keep `DbSchema` as the typed object of live table handles, similar to the Drizzle metadata exceptions recorded for `packages/drivers/drizzle/src/EntityTable.models.ts` in `goals/repo-crispening-orchestration/ops/inventory/S1/beep__drizzle.json`.",
    "confidence": 0.18,
    "mechanization": "judgment",
    "roiRank": 0.03,
    "exception": {
      "reason": "The alias describes a map of Drizzle table runtime handles, not a decoded payload. The row/domain boundaries are audited separately.",
      "boundary": "Drizzle table metadata registry"
    }
  }
]
