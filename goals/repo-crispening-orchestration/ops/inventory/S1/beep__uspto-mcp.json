[
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/drivers/uspto-mcp/src/UsptoDocumentTiers.ts",
    "line": 53,
    "symbol": "usptoDocumentFieldTiers",
    "smell": "Exported `defineFieldTiers({ minimal: S.Struct(...), balanced: S.Struct(...), complete: S.Struct(...) })` can look like an exported struct inventory hit.",
    "proposedTarget": "None -- `defineFieldTiers` deliberately accepts a `FieldTierSet` of `S.Struct` schema values. Verified in `packages/foundation/capability/mcp-kit/src/FieldTier.ts:75` (`FieldTierSet`) and `:108` (`defineFieldTiers`).",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.04,
    "exception": {
      "reason": "This is a schema-container contract: the tier members are already schemas and the helper anchors the three-tier constraint expected by mcp-kit.",
      "boundary": "mcp-kit field-tier schema set"
    }
  },
  {
    "ruleId": "SFV4-fn-schema",
    "file": "packages/drivers/uspto-mcp/src/Server.ts",
    "line": 83,
    "symbol": "makeServerLayer",
    "smell": "Exported function in a schema-modeled file can look like an SFV4-fn-schema hit because it accepts `UsptoMcpServerConfig` and returns a Layer runtime handle.",
    "proposedTarget": "None -- a hypothetical `Fn({ input: UsptoMcpServerConfig, output: Layer.Layer<never, never, Stdio> })` wrapper was audited and rejected.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.04,
    "exception": {
      "reason": "The config input is already an S.Class. The output is stdio MCP server runtime wiring over Layer/Toolkit handles, which is a service/port boundary, not data.",
      "boundary": "MCP stdio server layer factory"
    }
  }
]
