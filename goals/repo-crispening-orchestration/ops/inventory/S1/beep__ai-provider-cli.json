[
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/drivers/ai-provider-cli/src/AiProviderCli.service.ts",
    "line": 55,
    "symbol": "AiProviderCliRunner",
    "smell": "Exported function type alias returning `Effect.Effect<AiProviderCliProcessResult, AiProviderCliError>` matches the function-contract surface.",
    "proposedTarget": "None while excepted. A hypothetical `Fn({ input: AiProviderCliRunnerInput, output: EffectSchema<AiProviderCliProcessResult, AiProviderCliError, never>() })` was audited and rejected; `EffectSchema` is verified as a factory at `packages/foundation/modeling/schema/src/EffectSchema.ts:83`, and Fn `.implementEffect` / `.implementSync` are verified in `packages/foundation/modeling/schema/src/Fn/Fn.schema.ts:224` and `:403`.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 0.04,
    "exception": {
      "reason": "Runtime runner port for injected native CLI execution. Stable payloads are already schema-backed (`AiProviderCliProvider`, `AiProviderCliProcessResult`, `AiProviderCliAuthProbe`), while `command` and `args` are service-supplied argv runtime details. Fn-wrapping would change runner injection semantics rather than move data truth into Schema.",
      "boundary": "native provider CLI runner port"
    }
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/drivers/ai-provider-cli/src/AiProviderCli.service.ts",
    "line": 67,
    "symbol": "AiProviderCliShape",
    "smell": "Service interface with an Effect-returning method over schema-modeled provider/auth payloads matches the schema-first-inventory surface.",
    "proposedTarget": "None -- fence 1 service-contract carve-out. Keep `AiProviderCliShape` as the `Context.Service` shape used by `AiProviderCli extends Context.Service<AiProviderCli, AiProviderCliShape>()` at line 183.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.04,
    "exception": {
      "reason": "Context.Service shapes stay interfaces under SPEC Non-Goals fence 1; provider and auth result payloads are schema-modeled in `AiProviderCli.models.ts`.",
      "boundary": "Context.Service shape for the AI provider CLI driver"
    }
  }
]
