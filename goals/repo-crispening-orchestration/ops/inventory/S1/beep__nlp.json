[
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/modeling/nlp/src/Ontology/Kind.ts",
    "line": 380,
    "symbol": "KindContainment.containment",
    "smell": "`KindContainment` is an `S.Class` for the containment poset, but runtime behavior ignores that schema and reads a separate static `containment` record. The two sources disagree: the schema defaults put `Document` at `[\"Character\", \"POS\", \"Lemma\"]` (lines 393-397), while the runtime record puts `Document` at `[\"Paragraph\", \"Sentence\"]` (line 412); the schema gives `Character` children (lines 382-386), while the runtime record makes `Character` a leaf (line 416).",
    "proposedTarget": "Make the poset a single schema-owned data value. Correct `KindContainment` defaults to the behavior proven by `test/Ontology/Kind.test.ts:52-62`, then derive `KindContainment.containment` from `KindContainment.make({})`, or remove the static record and have `canContain` / `getValidChildren` read the schema-built instance. Keep `SchemaUtils.withKeyDefaults(...)` as the default carrier; this turns the existing schema from decorative metadata into the runtime source of truth.",
    "confidence": 0.78,
    "mechanization": "assisted",
    "roiRank": 0.72
  },
  {
    "ruleId": "SFV4-fn-schema",
    "file": "packages/foundation/modeling/nlp/src/Ontology/Kind.ts",
    "line": 166,
    "symbol": "makeTyped / Document / Paragraph / Sentence / Token / Character / Entity / Relation / Embedding / Dependency / Chunk / POS / Lemma",
    "smell": "`TypedTextSchema` exists (line 151), but the shared constructor returns plain object literals and the exported kind constructors expose inline function contracts rather than schema-owned construction.",
    "proposedTarget": "Introduce per-kind schemas such as `DocumentText = TypedTextSchema(S.Literal(\"Document\"))` (or cache them behind a schema-backed `makeTyped`) and implement the exported constructors with the relevant schema `.make({ content, metadata })`, preserving the public constructor names. `Fn` is not required unless decode-on-call semantics are wanted; the main target is that `TypedText` values are constructed through the available schema.",
    "confidence": 0.72,
    "mechanization": "assisted",
    "roiRank": 0.5
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/modeling/nlp/src/Core/PatternOperations.ts",
    "line": 27,
    "symbol": "isPOSElement / isEntityElement / isLiteralElement",
    "smell": "Exported guard helpers hand-roll discriminator checks with `P.isTagged(...)` even though the corresponding element schemas already expose colocated schema-derived statics on `Pattern.POS`, `Pattern.Entity`, and `Pattern.Literal`.",
    "proposedTarget": "Replace the three helper bodies with compatibility aliases to the schema statics (`Pattern.POS.is`, `Pattern.Entity.is`, `Pattern.Literal.is`) from `packages/foundation/modeling/nlp/src/Core/Pattern.ts:526-563`. The statics are built with `SchemaUtils.withStatics`; canonical codec-statics helper precedent is `packages/foundation/modeling/schema/src/SchemaUtils/withCodecStatics.ts:90`, and v4 `S.is` is verified at `.repos/effect-v4/packages/effect/src/Schema.ts:1256`.",
    "confidence": 0.93,
    "mechanization": "codemod",
    "roiRank": 0.6
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/modeling/nlp/src/Core/Token.ts",
    "line": 52,
    "symbol": "isTokenIndex / isCharPosition",
    "smell": "Exported guards validate against `NonNegativeInt` directly while the branded schemas `TokenIndex` and `CharPosition` are declared later in the same file. The guard behavior is detached from the branded schemas that should carry the public contract.",
    "proposedTarget": "Pipe `TokenIndex` and `CharPosition` through `SchemaUtils.withCodecStatics` after `$I.annoteSchema(...)`, then keep `isTokenIndex` / `isCharPosition` as compatibility aliases to `TokenIndex.is` / `CharPosition.is`. Verified support: `S.fromBrand` at `.repos/effect-v4/packages/effect/src/Schema.ts:5083` and `SchemaUtils.withCodecStatics` at `packages/foundation/modeling/schema/src/SchemaUtils/withCodecStatics.ts:90`; in-repo branded schema statics precedent: `packages/foundation/modeling/md/src/Md.model.ts:36`.",
    "confidence": 0.78,
    "mechanization": "assisted",
    "roiRank": 0.46
  },
  {
    "ruleId": "SFV4-fn-schema",
    "file": "packages/foundation/modeling/nlp/src/Core/PatternParsers.ts",
    "line": 242,
    "symbol": "PatternFromString",
    "smell": "Exported decode function takes `unknown` and returns `S.NonEmptyArray(BracketStringToPatternElement)` by calling an inline schema expression, but the function contract itself is not named as a schema.",
    "proposedTarget": "Name the contract as a schema, e.g. `PatternElementsFromString = S.NonEmptyArray(BracketStringToPatternElement).pipe($I.annoteSchema(...), SchemaUtils.withCodecStatics)`, then preserve `PatternFromString` as a compatibility alias to the schema-derived decoder. If keeping Result-style behavior, v4 `S.decodeUnknownResult` is verified at `.repos/effect-v4/packages/effect/src/Schema.ts:1564`; codec statics helper is `packages/foundation/modeling/schema/src/SchemaUtils/withCodecStatics.ts:90`.",
    "confidence": 0.84,
    "mechanization": "assisted",
    "roiRank": 0.48
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/modeling/nlp/src/Operations/Definition.ts",
    "line": 48,
    "symbol": "OperationDefinition",
    "smell": "Exported interface carries metadata, schemas, and an Effect-returning implementation callback; a naive schema-first inventory pass sees an exported object shape and function member.",
    "proposedTarget": "Hypothetical `S.Class`/`Fn` wrapper was audited and rejected. `Fn` exists for concrete unary contracts (`packages/foundation/modeling/schema/src/Fn/Fn.schema.ts:637`, `implementSync` line 403), but this surface is a generic operation-definition contract with input/output schemas already carried as fields and `R`/`E` inferred from the implementation.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.05,
    "exception": {
      "reason": "This is an operation/port definition, not a serializable domain payload. The schema truth for values is explicit in `inputSchema` and `outputSchema`; schema-ifying the interface itself would erase the generic Effect environment and error channels rather than add runtime evidence.",
      "boundary": "operation definition / Effect callback contract"
    }
  },
  {
    "ruleId": "SFV4-fn-schema",
    "file": "packages/foundation/modeling/nlp/src/Operations/Composable.ts",
    "line": 53,
    "symbol": "NLPOperation",
    "smell": "Exported function type alias `A -> Effect<B, E, R>` textually matches the S1 function-contract smell.",
    "proposedTarget": "Hypothetical `Fn({ input, output, error })` was audited and rejected for this generic Kleisli-arrow alias. The concrete `OperationBuilder` carries `inputSchema` and `outputSchema` next to the arrow, and its docs intentionally avoid re-decoding already typed values because that would leak schema decoding services into the requirements channel.",
    "confidence": 0.18,
    "mechanization": "judgment",
    "roiRank": 0.04,
    "exception": {
      "reason": "`NLPOperation` is a type-level algebraic arrow over Effect, not a concrete exported function implementation. `Fn` is useful for concrete call contracts; this alias must preserve generic `R`/`E` composition across map/flatMap/product.",
      "boundary": "type-level Kleisli arrow / operation algebra"
    }
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/modeling/nlp/src/Graph/GraphOps.ts",
    "line": 50,
    "symbol": "DirectedGraph / NodeIndex / NodeWalker / SearchIndex",
    "smell": "Graph aliases and `SearchIndex` are exported type/interface contracts near schema-modeled graph utilities; `SearchIndex` includes a `HashMap` plus a key-extraction callback.",
    "proposedTarget": "No schema target. Keep these as graph/algorithm contracts. The S1 fence explicitly says not to schema-ify `Graph` / `MutableHash*`, and `SearchIndex` is an in-memory index with a callback, not a persisted or wire payload.",
    "confidence": 0.18,
    "mechanization": "judgment",
    "roiRank": 0.04,
    "exception": {
      "reason": "Graph-native data structures and callback-bearing indexes are carved out. The actual graph payload schemas live separately in `packages/foundation/modeling/nlp/src/Graph/Schema.ts`.",
      "boundary": "effect/Graph and HashMap-backed algorithm surface"
    }
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/modeling/nlp/src/Algebra/Monoid.ts",
    "line": 40,
    "symbol": "Monoid",
    "smell": "Exported interface carries the monoid type-class shape, including overloaded `combine` function signatures.",
    "proposedTarget": "Hypothetical schema was audited and rejected. This is a generic algebraic type class over arbitrary carrier `A`; law tests, not `S.Class`, are the meaningful proof surface.",
    "confidence": 0.15,
    "mechanization": "judgment",
    "roiRank": 0.03,
    "exception": {
      "reason": "Type-class dictionaries are behavior contracts with functions and a generic carrier, not pure data payloads. The S1 service/port/type-level-utility carve-out applies.",
      "boundary": "generic algebraic type class"
    }
  },
  {
    "ruleId": "SFV4-fn-schema",
    "file": "packages/foundation/modeling/nlp/src/Core/PatternBuilders.ts",
    "line": 93,
    "symbol": "pos / entity / literal / optionalPos / optionalEntity / optionalLiteral",
    "smell": "Public overloaded builder functions in a schema-modeled package textually match exported function-contract detection.",
    "proposedTarget": "Hypothetical `Fn` wrappers were audited and rejected. These overloads are builder DSL shorthands that normalize inputs into `POSPatternElement.make`, `EntityPatternElement.make`, and `LiteralPatternElement.make`; the durable values are the schema classes.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.05,
    "exception": {
      "reason": "Overload ergonomics accept rest arguments and array shorthands before constructing schema nodes. Wrapping them in `Fn` would change invocation semantics and does not move a new invariant into the Pattern schemas.",
      "boundary": "public builder overload surface normalized into schema classes"
    }
  },
  {
    "ruleId": "SFV4-fn-schema",
    "file": "packages/foundation/modeling/nlp/src/Handoff/Contract.ts",
    "line": 430,
    "symbol": "makeProvenance",
    "smell": "Exported dual constructor in a schema-modeled file accepts primitive fields and returns a `Provenance` schema class.",
    "proposedTarget": "Hypothetical `Fn({ input: S.Struct({ source, generatedBy, timestamp, confidence }), output: Provenance })` was audited and rejected. Keep the dual constructor; it already returns `Provenance.make(...)` and validates `confidence` through `UnitInterval.make`.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.05,
    "exception": {
      "reason": "This is a constructor convenience over a schema class with dual call ergonomics. The returned data is schema-constructed; `Fn` would add decode-on-call behavior and collapse the data-first/data-last overload shape.",
      "boundary": "schema-class constructor helper / dual overload surface"
    }
  }
]
