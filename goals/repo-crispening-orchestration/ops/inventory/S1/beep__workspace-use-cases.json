[
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/workspace/use-cases/src/aggregates/Thread/ThreadStore.ts",
    "line": 183,
    "symbol": "ThreadStoreShape",
    "smell": "Exported interface contains Effect-returning function members and can look like a schema-first function-contract candidate.",
    "proposedTarget": "None -- fence 1 service-contract carve-out. Keep `ThreadStoreShape` as the `Context.Service` shape consumed by `ThreadStore` at line 229. The payloads on the service methods are schema-modeled (`CreateThreadInput`, `AppendTurnInput`, `AppendTurnResult`, `SetThreadTitleIfEmptyInput`, `ThreadTimeline`, and the tagged error classes). Effect v4 `Context.Service<Self, Shape>()(\"Key\")` verified at `.repos/effect-v4/packages/effect/src/Context.ts:163`.",
    "confidence": 0.18,
    "mechanization": "judgment",
    "roiRank": 0.03,
    "exception": {
      "reason": "This is a cross-concept use-case port, not a wire or domain payload. SPEC fence 1 explicitly preserves service contracts, Context.Service shapes, and port interfaces.",
      "boundary": "Context.Service shape / cross-concept ThreadStore port"
    }
  }
]
