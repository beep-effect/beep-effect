[
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/drivers/hubspot/src/HubSpot.service.ts",
    "line": 264,
    "symbol": "HubSpotShape",
    "smell": "Exported type literal with Effect-returning service methods matches the schema-first-inventory detector surface.",
    "proposedTarget": "None -- fence 1 service-contract carve-out. Keep request/response payloads as the HubSpot S.Class models and keep this as the `Context.Service` shape used by `HubSpot` at line 480; Effect v4 class-style `Context.Service<Self, Shape>()(\"Key\")` verified at `.repos/effect-v4/packages/effect/src/Context.ts:162-163`.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.05,
    "exception": {
      "reason": "Service shapes are explicitly carved out by S1. This type groups runtime API operations; the operation payloads are schema-modeled in the same file.",
      "boundary": "Context.Service shape for the HubSpot driver"
    }
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/drivers/hubspot/src/HubSpot.service.ts",
    "line": 285,
    "symbol": "decodeSubmitFormRequest / decodeSubmitFormResponse / decodeUpsertContactRequest / decodeUpsertContactResponse",
    "smell": "Cluster of module-level `S.decodeUnknownEffect(...)` bindings textually matches the decode-helper-wall smell.",
    "proposedTarget": "Inline each `S.decodeUnknownEffect(Model)` call at the HTTP request/response boundary -- audited and rejected. Effect v4 `S.decodeUnknownEffect` verified at `.repos/effect-v4/packages/effect/src/Schema.ts:1325`.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 0.06,
    "exception": {
      "reason": "The helpers are the HubSpot HTTP boundary codecs and every use maps schema failures into `HubSpotError` with request-specific context (`formGuid`, `email`, `url`). The schemas remain the validation source of truth.",
      "boundary": "HubSpot HTTP request/response boundary codecs"
    }
  },
  {
    "ruleId": "SFV4-fn-schema",
    "file": "packages/drivers/hubspot/src/HubSpot.errors.ts",
    "line": 108,
    "symbol": "HubSpotError.fromReason",
    "smell": "Exported static constructor in a schema-modeled error file can look like an SFV4-fn-schema hit.",
    "proposedTarget": "Hypothetical `Fn({ input: HubSpotErrorFromReasonInput, output: HubSpotError })` wrapper -- audited and rejected. `Fn(...).implementSync` verified at `packages/foundation/modeling/schema/src/Fn/Fn.schema.ts:403`.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.04,
    "exception": {
      "reason": "The constructor parameters are already schema-carried (`reason` is the `HubSpotErrorReason` LiteralKit domain and `options` is the `HubSpotErrorOptions` S.Class). The body builds a `HubSpotError` schema instance; Fn-wrapping would add decode/throw invocation semantics without moving a new invariant into Schema.",
      "boundary": "schema-typed tagged-error constructor"
    }
  }
]
