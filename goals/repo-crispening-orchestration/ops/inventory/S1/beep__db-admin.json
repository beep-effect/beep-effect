[
  {
    "ruleId": "SFV4-fn-schema",
    "file": "packages/_internal/db-admin/src/migrate.ts",
    "line": 76,
    "symbol": "migrateOnBoot",
    "smell": "Exported `Effect.Effect<undefined, PostgresError, PostgresDrizzle>` workflow value can look like the S1 Effect-typed-value target.",
    "proposedTarget": "Hypothetical `EffectSchema<undefined, PostgresError, PostgresDrizzle>()` wrapper is rejected. Keep this as an executable migration workflow over the `PostgresDrizzle` service; `EffectSchema` was verified as a factory at `packages/foundation/modeling/schema/src/EffectSchema.ts:83`.",
    "confidence": 0.15,
    "mechanization": "judgment",
    "roiRank": 0.03,
    "exception": {
      "reason": "This is an operational migration workflow requiring a database service and filesystem-backed Drizzle migration folder, not a data field or wire payload. Schema-as-truth applies to the migration target metadata, which is `DbAdminMigrationTarget`.",
      "boundary": "db-admin migration workflow Effect"
    }
  },
  {
    "ruleId": "SFV4-fn-schema",
    "file": "packages/_internal/db-admin/src/targets.ts",
    "line": 78,
    "symbol": "listDbAdminMigrationTargets",
    "smell": "Exported `Effect.Effect<ReadonlyArray<DbAdminMigrationTarget>>` value can look like the S1 Effect-typed-value target.",
    "proposedTarget": "Hypothetical `EffectSchema<ReadonlyArray<DbAdminMigrationTarget>, never, never>()` wrapper is rejected. Keep this as a pure Effect query over the in-memory migration target registry; the registry elements are already constructed through the `DbAdminMigrationTarget` S.Class.",
    "confidence": 0.16,
    "mechanization": "judgment",
    "roiRank": 0.03,
    "exception": {
      "reason": "This is a small operational query effect, not a persisted or wire payload. Wrapping the Effect value would not add schema truth beyond the existing `DbAdminMigrationTarget` class.",
      "boundary": "db-admin migration-target registry query Effect"
    }
  }
]
