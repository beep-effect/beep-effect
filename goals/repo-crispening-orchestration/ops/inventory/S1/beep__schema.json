[
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/modeling/schema/src/Semver.ts",
    "line": 107,
    "symbol": "isSemverNumberSegmentString",
    "smell": "Top-of-file cluster of schema-derived guards/decoders (`S.is(...)`, `S.decodeUnknownOption(...)`) textually matches the S1 helper-wall smell.",
    "proposedTarget": "Hypothetical inlining or codec-static colocation on the private SemVer token schemas; audited and rejected as not actionable for S1.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0.05,
    "exception": {
      "reason": "These helpers are parser-local aliases over private schema tokens (`SemverNumberSegmentString`, `SemverNumberSegment`, prerelease/build identifiers, comparator operator). The runtime truth is already the `Semver` S.Class at line 367 and the `SemverFromString` codec at line 626; the aliases do not validate a parallel unmodeled payload or bypass the schema.",
      "boundary": "schema-internal parser token guards for a named S.Class/S.decodeTo codec"
    }
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/modeling/schema/src/Csv/Csv.schema.ts",
    "line": 47,
    "symbol": "RowSchemaWithFields",
    "smell": "Exported structural type alias (`S.Top & { readonly fields: S.Struct.Fields }`) looks like a data-payload type alias to the reused schema-first inventory detector.",
    "proposedTarget": "Hypothetical `S.Class`/`S.Struct` wrapper for row-schema arguments; audited and rejected.",
    "confidence": 0.18,
    "mechanization": "judgment",
    "roiRank": 0.04,
    "exception": {
      "reason": "`RowSchemaWithFields` is a type-level schema factory constraint for the public `Csv` schema constructor, not a wire/domain payload. The actual CSV rows are validated by the caller-supplied row schema and the returned `CsvDocument<RowSchema>` codec.",
      "boundary": "schema-constructor type constraint over `S.Top`"
    }
  },
  {
    "ruleId": "SFV4-fn-schema",
    "file": "packages/foundation/modeling/schema/src/Fn/Fn.schema.ts",
    "line": 218,
    "symbol": "FnSchemaUnary",
    "smell": "Exported interface describes callable implementation helpers and can look like a function contract that should itself be wrapped in `Fn`.",
    "proposedTarget": "Hypothetical `Fn` wrapper around the `Fn` primitive's own implementation surface; audited and rejected.",
    "confidence": 0.12,
    "mechanization": "judgment",
    "roiRank": 0.03,
    "exception": {
      "reason": "This is the package's function-schema primitive, extending `S.Codec` and exposing `implement`, `implementEffect`, and `implementSync` for downstream schema-backed functions. Schema-ifying it with `Fn` would be recursive toolbox code, not an application contract bypass.",
      "boundary": "schema package primitive / type-level codec surface"
    }
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/modeling/schema/src/Graph/Graph.from-self.ts",
    "line": 40,
    "symbol": "GraphFromSelf",
    "smell": "Exported interface over graph runtime values can look like a schema-first inventory interface hit.",
    "proposedTarget": "Hypothetical data-model schema for `Graph` values; audited and rejected by the explicit S1 fence.",
    "confidence": 0.1,
    "mechanization": "judgment",
    "roiRank": 0.02,
    "exception": {
      "reason": "The S1 prompt and SPEC fence explicitly prohibit `Graph` schema-ification. This interface is already a schema constructor surface (`S.declareConstructor`) for Effect Graph runtime values and carries the node/edge schemas as parameters.",
      "boundary": "SPEC fence 6: Graph schema-constructor surface"
    }
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/modeling/schema/src/MutableHashMap.ts",
    "line": 119,
    "symbol": "MutableHashMap",
    "smell": "Exported interface over a mutable Effect collection can look like a data-payload interface hit.",
    "proposedTarget": "Hypothetical `S.Class` for `MutableHashMap` values; audited and rejected by the explicit S1 fence.",
    "confidence": 0.1,
    "mechanization": "judgment",
    "roiRank": 0.02,
    "exception": {
      "reason": "The S1 prompt and SPEC fence explicitly prohibit `MutableHash*` schema-ification. This interface is already the schema constructor returned by the package's `MutableHashMap` codec factory, transforming entry arrays into Effect `MutableHashMap` instances.",
      "boundary": "SPEC fence 6: MutableHash* schema-constructor surface"
    }
  },
  {
    "ruleId": "SFV4-boundary-codec",
    "file": "packages/foundation/modeling/schema/src/SafeRemoteHost.ts",
    "line": 366,
    "symbol": "assertAllowedRemoteUrl",
    "smell": "Exported schema-modeled security helper parses a URL with `new URL(url).hostname` and uses inline option-object/function contracts, so a naive S1 detector can flag both boundary-codec and function-contract smells.",
    "proposedTarget": "Hypothetical `S.URLFromString` decode plus a schema-modeled options payload; audited and rejected for this S1 inventory.",
    "confidence": 0.24,
    "mechanization": "judgment",
    "roiRank": 0.06,
    "exception": {
      "reason": "This is an explicit SSRF trust boundary. The module intentionally keeps host normalization, allowlisting, resolver injection, and fail-closed `BlockedHostError` construction visible for auditability; the `resolve` callback is an injected port hook. Automatic schema wrapping could change error/cause semantics without moving a domain invariant into a schema.",
      "boundary": "security trust boundary / resolver port callback"
    }
  },
  {
    "ruleId": "SFV4-fn-schema",
    "file": "packages/foundation/modeling/schema/src/StatusCauseError.ts",
    "line": 193,
    "symbol": "makeStatusCauseError",
    "smell": "Exported overloaded builder in a schema-modeled file accepts object-shaped context inputs (`StatusCauseContext`, `StatusCauseErrorInput`) that can look like unmodeled function contracts.",
    "proposedTarget": "Hypothetical `Fn({ input: StatusCauseInput, output: ... })` wrapper or schema class for every overload arm; audited and rejected.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 0.06,
    "exception": {
      "reason": "This is an overload surface for typed-error constructors. The durable payloads already exist as `StatusCauseInputOptions` and `StatusCauseInput` S.Class models at lines 62 and 87; the remaining object types are call-shape conveniences for partial application and constructor plumbing.",
      "boundary": "overload surface for schema-backed tagged-error constructors"
    }
  }
]
