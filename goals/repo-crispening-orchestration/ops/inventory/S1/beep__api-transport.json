[
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/capability/api-transport/src/Transport.ts",
    "line": 54,
    "symbol": "ApiAuth",
    "smell": "Exported tagged-union data payload modeled as `Data.TaggedEnum<{...}>` with a `Data.taggedEnum` constructor bundle (Transport.ts:74) instead of a schema tagged union; this is the only Data.taggedEnum usage in the repo. Invisible to the current lint: the exported-type-alias detector skips non-TypeLiteral aliases (SchemaFirst.ts:1621-1624 `continue`s when the type node is a TypeReference).",
    "proposedTarget": "S.TaggedUnion({ NoAuth: {}, ApiKeyQueryAuth: { param: S.String, key: S.Redacted(S.String) }, TokenHeaderAuth: { key: S.Redacted(S.String) }, ApiKeyHeaderAuth: { header: S.String, key: S.Redacted(S.String) } }) with $I.annoteSchema, then `type ApiAuth = typeof ApiAuth.Type`. Verified v4: S.TaggedUnion (.repos/effect-v4/packages/effect/src/Schema.ts:6204) supplies .cases/.guards/.isAnyOf and dual .match; S.Redacted (Schema.ts:8910) types keys as Redacted<string>; constructors via ApiAuth.cases.NoAuth.make({}) (Bottom.make, Schema.ts:211). applyAuth's ApiAuth.$match(auth, {...}) becomes ApiAuth.match(auth, {...}). §5.4 ripple: constructor call sites in @beep/govinfo (Govinfo.service.ts:79-80) and @beep/ecfr (Ecfr.service.ts:68) must ship in the same PR — crosses the foundation→drivers family boundary, so the wave plan must schedule this alongside the drivers wave or preserve the public constructor shape via colocated statics (fence 8). Note Data.taggedEnum values carry Equal/Hash structural semantics that plain schema structs do not; no consumer relies on ApiAuth equality today (construct-and-match only).",
    "confidence": 0.7,
    "mechanization": "assisted",
    "roiRank": 4.2,
    "exception": {
      "reason": "§5.4 public-form conversion from Data.taggedEnum constructors to schema tagged-union cases would require same-PR edits in @beep/govinfo and @beep/ecfr constructor call sites; this package writer cannot edit driver packages. Keep the existing Data.taggedEnum surface until the family-close sweep can own the cross-package migration.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/capability/api-transport/src/Transport.ts",
    "line": 168,
    "symbol": "ApiTransportOptions",
    "smell": "Exported interface describing the makeApiTransport options payload — surface-level schema-first-inventory match (exported data-shaped interface).",
    "proposedTarget": "None while excepted. Revisit only if ApiAuth is schema-ified (see the ApiAuth finding): auth would then have a schema, leaving window: Duration.Input and retryBaseDelay?: Duration.Input as the remaining effect-native constructor-ergonomics members.",
    "confidence": 0.4,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "Durably ledgered exception at standards/schema-first.inventory.jsonc:555-560: constructor-options bag for makeApiTransport with effect-native members — an ApiAuth Data.TaggedEnum carrying Redacted secrets and a Duration.Input timing input — not a decode-from-wire boundary model. In-process config, never decoded from wire.",
      "boundary": "Constructor-options ergonomics surface for makeApiTransport (foundation capability consumed by @beep/govinfo and @beep/ecfr)."
    }
  },
  {
    "ruleId": "schema-first-inventory",
    "file": "packages/foundation/capability/api-transport/src/Transport.ts",
    "line": 206,
    "symbol": "ApiTransport",
    "smell": "Exported interface — surface-level schema-first-inventory match, but its members are runtime handles: rateLimit is an Effect accessor and transformClient is an HttpClient -> HttpClient function.",
    "proposedTarget": "None — fence 1 (service-contract/port-interface carve-out). Stays an interface.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "Port-interface carve-out (SPEC Non-Goals fence 1): capability handle with function members and an Effect-typed accessor over HttpClient runtime state; already durably ledgered at standards/schema-first.inventory.jsonc:547-553 ('Interface contains non-schema signals such as function members or runtime handles'), matching detectInterfaceReason's typeLiteralMembersUnsafe screen.",
      "boundary": "Capability handle returned by makeApiTransport over effect/unstable/http HttpClient."
    }
  },
  {
    "ruleId": "SFV4-fn-schema",
    "file": "packages/foundation/capability/api-transport/src/Transport.ts",
    "line": 241,
    "symbol": "makeApiTransport",
    "smell": "Exported function in a schema-modeled file (file exports the RateLimitSnapshot S.Class) whose parameter contract is a named non-schema interface (ApiTransportOptions) — the SFV4-fn-schema surface smell. Currently invisible to the landed detector twice over: it is an Effect.fnUntraced call-expression initializer (sourceExportedArrowFunctions harvests only Node.isArrowFunction initializers, SchemaFirst.ts:1000-1010) and its contract is a named interface, not an inline type literal (fnSchemaEntryFromFunctionLike, SchemaFirst.ts:1055-1060).",
    "proposedTarget": "None — legitimate escape hatch. Fn({ input, output }) from @beep/schema (packages/foundation/modeling/schema/src/Fn/Fn.schema.ts:558) is not warranted: the return value { rateLimit: Effect<Option<RateLimitSnapshot>>, transformClient: (HttpClient) => HttpClient } is a capability object over runtime handles with no schema-representable output, and the input options bag is itself a durably ledgered exception.",
    "confidence": 0.35,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "Capability-constructor / 3rd-party-boundary-adapter escape hatch for SFV4-fn-schema: an Effect-returning constructor whose output is runtime handles (HttpClient transformer + Ref-backed Effect accessor) cannot carry an Fn/EffectSchema contract; schema-as-truth applies to the RateLimitSnapshot data it exposes, which is already an S.Class.",
      "boundary": "effect/unstable/http HttpClient + effect/unstable/persistence RateLimiter adapter seam; requires RateLimiterStore context."
    }
  }
]
