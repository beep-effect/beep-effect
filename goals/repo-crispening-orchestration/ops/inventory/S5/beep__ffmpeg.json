[
  {
    "ruleId": "SFV4-precision-audit",
    "file": "packages/drivers/ffmpeg/src/FFmpeg.models.ts",
    "line": 322,
    "symbol": "ExtractFramesRequest.manifestPath/outDir/videoPath",
    "smell": "Detector candidate: caller-provided filesystem paths are broad S.String / S.Option(S.String) fields instead of branded path schemas.",
    "proposedTarget": "None — keep as is.",
    "confidence": 0.9,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "These are operating-system filesystem boundary strings, and the service performs the real safety checks with path.resolve, ensureFile/ensureDirectory, and resolvePathWithinRoot containment for manifest writes at FFmpeg.service.ts:886-915; a static string brand would be platform-dependent and would not replace those runtime checks.",
      "boundary": "caller-provided filesystem paths and manifest containment boundary"
    }
  },
  {
    "ruleId": "SFV4-precision-audit",
    "file": "packages/drivers/ffmpeg/src/FFmpeg.service.ts",
    "line": 40,
    "symbol": "NumberOrString / FfprobeOutput",
    "smell": "Detector candidate: ffprobe JSON accepts broad strings/numbers and an unbounded streams array.",
    "proposedTarget": "None — keep as is.",
    "confidence": 0.9,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "This schema mirrors a third-party process JSON boundary where ffprobe reports numeric fields as either numbers, rationals, strings, or absent values; parseNumber/rationalToNumber then normalize defensively to Option before constructing the public VideoProbe.",
      "boundary": "ffprobe process JSON compatibility boundary"
    }
  },
  {
    "ruleId": "SFV4-arbitrary-tests",
    "file": "packages/drivers/ffmpeg/test/FFmpeg.service.test.ts",
    "line": 114,
    "symbol": "FFmpeg service fixtures",
    "smell": "Detector candidate: command arguments, fake ffprobe JSON, emitted events, manifest rendering, and failure behavior are covered by hand-written fixtures.",
    "proposedTarget": "None — keep as is; the schema-derived law above is additive coverage, not a replacement.",
    "confidence": 0.9,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "These are golden/regression tests for process command construction, filesystem side effects, manifest bytes, and error normalization, which the S5 carve-out explicitly treats as legitimate static fixtures.",
      "boundary": "native ffmpeg service behavior and manifest golden fixtures"
    }
  }
]
