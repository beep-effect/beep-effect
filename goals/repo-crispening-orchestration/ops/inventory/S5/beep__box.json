[
  {
    "ruleId": "SFV4-numeric-domain",
    "file": "packages/drivers/box/src/Box.errors.ts",
    "line": 111,
    "symbol": "BoxErrorOptions.status / BoxError.status",
    "smell": "SDK HTTP status values are sanitized for non-finite numbers, but the schema still accepts any finite number, including negative and fractional status codes.",
    "proposedTarget": "Narrow the two status fields to the repo HTTP status schema used by `packages/drivers/box/src/experimental/Box.schemas.ts:249` (`HttpStatus` from `@beep/schema/HttpStatus`), or to an annotated local SDK-status integer if Box can surface non-standard codes.",
    "confidence": 0.74,
    "mechanization": "assisted",
    "roiRank": 4
  },
  {
    "ruleId": "SFV4-numeric-domain",
    "file": "packages/drivers/box/src/Box.streaming.ts",
    "line": 105,
    "symbol": "BoxPartAccumulator.fileSize / BoxPartAccumulator.lastIndex / BoxUploadBigFilePayload.fileSize",
    "smell": "Byte sizes and upload-part indexes are modeled as `S.Finite`; this accepts fractional and negative values even though the reducer and upload session logic treat them as counts/indexes.",
    "proposedTarget": "Use `NonNegativeInt` from `@beep/schema/Number` for byte sizes, and a local `LastPartIndex = S.Int.check(S.isGreaterThanOrEqualTo(-1))` for the sentinel index. Effect v4 exposes `S.Int` and `S.isGreaterThanOrEqualTo`; `packages/foundation/modeling/schema/src/Number.ts:182` is the in-repo non-negative integer exemplar.",
    "confidence": 0.7,
    "mechanization": "assisted",
    "roiRank": 4
  },
  {
    "ruleId": "SFV4-precision-audit",
    "file": "packages/drivers/box/src/Box.streaming.ts",
    "line": 407,
    "symbol": "BoxUploadFilePartByUrlPayload.url / BoxGetZipDownloadContentPayload.downloadUrl",
    "smell": "Caller-supplied remote URLs decode as plain `S.String`; invalid URLs only fail later in `assertBoxUrlAllowed`, after the payload schema has accepted them.",
    "proposedTarget": "Introduce an annotated URL-string schema for these fields, reusing `URLStr` or `HttpsUrl` from `packages/foundation/modeling/schema/src/URL.ts:44` and `packages/foundation/modeling/schema/src/URL.ts:103` where the encoded type must remain a string. Keep `assertBoxUrlAllowed` as the trust-boundary SSRF guard.",
    "confidence": 0.68,
    "mechanization": "assisted",
    "roiRank": 4
  },
  {
    "ruleId": "SFV4-arbitrary-tests",
    "file": "packages/drivers/box/test/Box.service.test.ts",
    "line": 154,
    "symbol": "Box handwritten config, error, and streaming schemas",
    "smell": "The package has fixture coverage for generated SDK wrapping, streaming paths, and error sanitization, but no schema-derived arbitrary/equivalence laws for the handwritten schemas outside `src/_generated/**`.",
    "proposedTarget": "Add a focused property test file for `BoxCcgConfig`, `BoxDeveloperTokenConfig`, `BoxError`, `BoxPartAccumulator`, and the handwritten streaming payload schemas using Effect v4 `S.toArbitrary` and `S.toEquivalence` (`.repos/effect-v4/packages/effect/src/Schema.ts:12970` and `:13201`). Skip opaque byte-stream/readable/form-data fields or provide custom arbitraries for them.",
    "confidence": 0.64,
    "mechanization": "assisted",
    "roiRank": 4
  },
  {
    "ruleId": "SFV4-precision-audit",
    "file": "packages/drivers/box/scripts/generate.ts",
    "line": 1072,
    "symbol": "Box SDK generator fallback schemas",
    "smell": "Detector candidate: the generator emits broad SDK fallback schemas such as `BoxSerializedData = S.Unknown` and maps many SDK `number` declarations to `S.Finite`.",
    "proposedTarget": "No change for this detector pass. If a generated field's semantics are known, tighten the durable generator/shared schema concept rather than editing `src/_generated/**`; prior Box digest planning identified `packages/drivers/box/scripts/generate.ts` as the stable edit point for generated-schema refinements.",
    "confidence": 0.9,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "The generator mirrors the evolving `box-node-sdk` declaration surface and deliberately preserves compatibility for unknown SDK payload shapes.",
      "boundary": "Box Node SDK generated-model boundary"
    }
  },
  {
    "ruleId": "SFV4-precision-audit",
    "file": "packages/drivers/box/src/experimental/domain/entities/ZipDownload/ZipDownload.model.ts",
    "line": 16,
    "symbol": "experimental Box empty domain scaffolds",
    "smell": "Detector candidate: many experimental Box domain anchors declare an empty schema shape instead of first-party field invariants.",
    "proposedTarget": "No immediate S5 rewrite. Promote fields into these domain classes only when the experimental domain replaces the generated SDK payload as the canonical model.",
    "confidence": 0.9,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "The file explicitly states that generated Box SDK payload schemas remain the field-level source until fields are promoted here.",
      "boundary": "experimental Box domain scaffold"
    }
  },
  {
    "ruleId": "SFV4-arbitrary-tests",
    "file": "packages/drivers/box/test/Box.service.test.ts",
    "line": 155,
    "symbol": "Box SDK compatibility fixtures",
    "smell": "Detector candidate: fixture-style tests cover generated enum openness, SDK response decoding, stream bridging, and sanitized error behavior without using schema arbitraries.",
    "proposedTarget": "Keep these fixtures. Add schema-law tests separately for handwritten schemas instead of replacing SDK compatibility coverage.",
    "confidence": 0.9,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "These tests pin SDK compatibility and trust-boundary behavior that arbitrary schema laws would not exercise.",
      "boundary": "Box SDK compatibility and streaming fixture tests"
    }
  }
]
