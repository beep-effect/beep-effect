[
  {
    "ruleId": "SFV4-precision-audit",
    "file": "packages/drivers/firecrawl/src/Firecrawl.models.ts",
    "line": 2030,
    "symbol": "Firecrawl request URL payload fields",
    "smell": "FirecrawlScrapePayload/MapPayload/StartCrawlPayload/CrawlPayload/CrawlParamsPreviewPayload use url: S.String, and batch scrape payloads use urls: S.Array(S.String), even though the operations require URL strings and batch requests are meaningless with an empty URL list.",
    "proposedTarget": "Introduce a local FirecrawlUrlInput = S.String.check(URLStr.filter) or HttpsUrl-compatible schema and reuse it for scalar url fields; use S.NonEmptyArray(FirecrawlUrlInput) for urls fields. URLStr.filter is exposed at packages/foundation/modeling/schema/src/URL.ts:44-53; S.NonEmptyArray is verified in Effect v4 at .repos/effect-v4/packages/effect/src/Schema.ts:4533.",
    "confidence": 0.68,
    "mechanization": "assisted",
    "roiRank": 4,
    "exception": {
      "reason": "Tightening the public request URL payload schemas requires a consumer sweep outside packages/drivers/firecrawl: packages/tooling/tool/cli/src/commands/Research/Research.service.ts constructs FirecrawlScrapePayload from a plain URL string. Per SPEC §5.4 and fence 8, this ripple is deferred to the family-close sweep instead of widening this package wave.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "SFV4-precision-audit",
    "file": "packages/drivers/firecrawl/src/Firecrawl.models.ts",
    "line": 61,
    "symbol": "typedUnknown Firecrawl SDK mirrors",
    "smell": "Detector candidate: many exported Firecrawl SDK request/response mirrors are S.declare(...P.isUnknown...), which is intentionally broader than the imported SDK TypeScript types.",
    "proposedTarget": "None - keep as is.",
    "confidence": 0.9,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "The schemas deliberately preserve Firecrawl SDK compatibility for opaque upstream shapes imported from the firecrawl package; duplicating the SDK's evolving models locally would create a stale second contract. The driver wraps these opaque values in typed payload/success/error classes at its own boundary.",
      "boundary": "Firecrawl SDK request/response passthrough boundary"
    }
  },
  {
    "ruleId": "SFV4-precision-audit",
    "file": "packages/drivers/firecrawl/src/Firecrawl.models.ts",
    "line": 4557,
    "symbol": "FirecrawlWatcherSnapshotEvent.snapshot",
    "smell": "Detector candidate: watcher snapshot payload is S.Unknown.",
    "proposedTarget": "None - keep as is.",
    "confidence": 0.9,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "Snapshot events are delivered by the SDK watcher and are not decoded by this driver beyond preserving the event tag; no in-repo schema owns the SDK snapshot structure today.",
      "boundary": "Firecrawl watcher event passthrough boundary"
    }
  },
  {
    "ruleId": "SFV4-arbitrary-tests",
    "file": "packages/drivers/firecrawl/test/Firecrawl.service.test.ts",
    "line": 61,
    "symbol": "Firecrawl fake SDK response fixtures",
    "smell": "Detector candidate: tests rely on static fake SDK response objects instead of generated arbitrary data.",
    "proposedTarget": "None - keep as is (schema-derived laws should be additive coverage, not a replacement for these SDK compatibility fixtures).",
    "confidence": 0.9,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "The fixtures pin exact SDK method routing, wrapper class construction, watcher translation, and sanitized error mapping; these are regression fixtures for integration behavior rather than schema law tests.",
      "boundary": "Firecrawl SDK compatibility unit fixtures"
    }
  }
]
