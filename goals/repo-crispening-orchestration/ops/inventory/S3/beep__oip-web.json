[
  {
    "ruleId": "SFV4-getsomes-struct",
    "file": "apps/oip-web/src/contact/ContactSubmission.model.ts",
    "line": 233,
    "symbol": "contactSubmissionPayloadInputFromFormData",
    "smell": "R.getSomes is used for fixed optional form fields at lines 233-240 and again in contactSubmissionPayloadFallback at lines 249-255; a broad inline-object detector will see both.",
    "proposedTarget": "None -- keep under this S3 card.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "Every option in these calls is produced by formTextOption and carries the same Option<string> value domain before ContactSubmissionFormPayload decodes submittedAt. The user instruction says R.getSomes is only a finding for heterogeneous Option-structs; this is a homogeneous form-text compaction, not a mixed per-key payload.",
      "boundary": "browser FormData text normalization before ContactSubmissionFormPayload decoding"
    }
  },
  {
    "ruleId": "SFV4-getsomes-struct",
    "file": "apps/oip-web/src/content/OipContent.runtime.ts",
    "line": 125,
    "symbol": "loadFromSanity",
    "smell": "R.getSomes({ status: O.fromUndefinedOr(error.status) }) is a single-key optional spread inside the provider error options object; the current detector flags inline object literals.",
    "proposedTarget": "None -- keep under this S3 card.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "This call contains only one optional value and therefore is not a heterogeneous Option struct. The surrounding provider/providerReason fields are required literals in this object, so there is no mixed optional struct for O.getSomesStruct to preserve.",
      "boundary": "Sanity provider error status option at the catch boundary"
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "apps/oip-web/src/content/OipSeo.ts",
    "line": 20,
    "symbol": "profileUrls",
    "smell": "SEO projections compare SocialLink.platform to literal SocialPlatform values at line 20 and line 96; a broad discriminator detector could suggest SocialPlatform.$match or a tagged union.",
    "proposedTarget": "None -- keep as simple predicates.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "SocialPlatform is a literal domain for filtering links, not a variant family with per-case payload or required exhaustive behavior. A nine-case match would add noise without proving a domain invariant.",
      "boundary": "SEO/read-model projection over SocialLink.platform"
    }
  }
]
