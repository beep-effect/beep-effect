[
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/tooling/policy-pack/lint-rules/src/rules/utils.ts",
    "line": 156,
    "symbol": "unwrapMemberExpression",
    "smell": "The lint-rule helpers and rules contain many checks against ESTree node.type fields (for example MemberExpression at utils.ts:158, Identifier/Literal at utils.ts:214 and 237, ImportSpecifier at utils.ts:320, and CallExpression/MethodDefinition/ImportSpecifier checks in the rule modules). A broad S3 detector for .type discriminator branches would flag these as manual tagged-union dispatch.",
    "proposedTarget": "None -- keep these external AST shape checks as-is.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "These are discriminants from @oxlint/plugins' ESTree visitor API, not repo-owned schema-modeled unions with generated match/guards. The package is itself an AST lint layer; local predicates such as unwrapExpression/getPropertyName intentionally narrow external parser nodes without weakening the trust boundary.",
      "boundary": "external oxlint ESTree AST API boundary"
    }
  }
]
