[
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/shared/domain/src/identity/index.ts",
    "line": 48,
    "symbol": "passesIdentityComposerSmokeTest",
    "smell": "The AnyIdentityComposer runtime guard uses value as IdentityComposerType<string>, an as unknown as TemplateStringsArray probe, and value as IdentityComposerCandidate while validating a callable object decorated with identity-composer methods. A broad no-as detector will flag this cluster.",
    "proposedTarget": "None -- keep as-is unless @beep/identity exposes a first-class assertion-free runtime probe for callable IdentityComposer values.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "This S.declare guard is the trust boundary that proves the callable composer shape before a schema exists for it. Existing S.is/P narrowing can prove Function and member presence, but cannot express the tagged-template call signature or decorated identity-composer methods without entering this smoke-test boundary.",
      "boundary": "runtime schema guard for callable @beep/identity IdentityComposer values"
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/shared/domain/src/entity/EntityId.ts",
    "line": 395,
    "symbol": "literalDefinition",
    "smell": "definition as DefinitionFor<...> pins runtime-computed resource, tableName, entityType, and brand strings to their conditional/template-literal type result. A broad no-as detector will flag the type witness.",
    "proposedTarget": "None -- keep as-is unless the EntityId definition builder is redesigned so the computed defaults are constructed by type-level-preserving helpers all the way through Options overrides.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "The factory computes default strings from const generic inputs and optional runtime overrides; TypeScript cannot prove those values equal the Resolved* conditional/template-literal types after Options.make. The cast is internal to the EntityId factory and is covered by the exported statics and dtslint expectations.",
      "boundary": "EntityId.factory type-level statics bridge"
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/shared/domain/src/entity/EntityId.ts",
    "line": 494,
    "symbol": "factory",
    "smell": "schema as S.Codec<EntityIdValueFor<ResolvedBrand<...>>, number> pins S.brand(definition.brand) plus identity annotations to the resolved entity-id brand. A broad no-as detector will flag the assertion.",
    "proposedTarget": "None -- keep as-is unless Effect Schema brand inference or the local EntityId factory API can preserve the runtime definition.brand literal through S.brand and identity.annoteSchema without a type witness.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "The brand name is derived from the runtime definition object; the current Effect v4 Schema.brand/annotation chain does not carry the conditional generic brand through the factory. The cast is an internal type witness before attachEntityIdStatics and S.toEquivalence expose the public schema surface.",
      "boundary": "EntityId.factory schema-brand bridge"
    }
  }
]
