[
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/capability/api-transport/src/Transport.ts",
    "line": 54,
    "symbol": "ApiAuth",
    "smell": "Finite 4-variant auth family (NoAuth | ApiKeyQueryAuth | TokenHeaderAuth | ApiKeyHeaderAuth) modeled as Data.TaggedEnum rather than a schema tagged union (S.TaggedUnion / LiteralKit .toTaggedUnion); a naive S3 heuristic surfaces it as a non-schema union candidate.",
    "proposedTarget": "None — keep as-is. Branching already goes through the derived exhaustive matcher ApiAuth.$match (Transport.ts:79); there is no manual discriminator branching (no switch, no _tag if-chains) to remediate.",
    "confidence": 0.95,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "Legitimate carve-out: already a discriminated union with derived exhaustive $match from Data.taggedEnum (verified .repos/effect-v4/packages/effect/src/Data.ts:590 taggedEnum factory, :366-408 $match exhaustive matcher); variants carry Redacted.Redacted<string> secrets that must not enter a serializable schema value model; ApiAuth is transformer-input config on the ApiTransportOptions port surface, covered by SPEC fence 1 (service shapes and port interfaces stay interfaces — crispening does not schema-ify service contracts), and the module doc pins this transformer to never touch value-model decoding. Not a REQUIRED_TAGGED_UNIONS family (packages/tooling/tool/cli/src/commands/Lint/Lint.command.ts:65).",
      "boundary": "packages/foundation/capability/api-transport/src/Transport.ts — transport-transformer options/port surface (SPEC fence 1), not a value model"
    }
  },
  {
    "ruleId": "literal-kit-const-assertion",
    "file": "packages/foundation/capability/api-transport/src/index.ts",
    "line": 29,
    "symbol": "VERSION",
    "smell": "`\"0.0.0\" as const` — const assertion surfaced by the no-`as` detector pass over the package entrypoint.",
    "proposedTarget": "None — keep as-is. Ordinary scalar `as const` is the allowed repo carve-out; the assertion is not applied to a LiteralKit([...]) inline array, so the redundancy the literal-kit-const-assertion card targets is absent.",
    "confidence": 0.98,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "`as const` on a scalar version literal is the explicitly allowed carve-out (the repo bans every other as-form); no LiteralKit inline array is involved, so the card does not fire. Recorded per the false-positive-audit rule that legitimate uses are ledgered via exception, not silently omitted.",
      "boundary": "packages/foundation/capability/api-transport/src/index.ts — package entrypoint VERSION constant"
    }
  }
]
