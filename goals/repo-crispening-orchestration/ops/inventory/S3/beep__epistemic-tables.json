[
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/epistemic/tables/src/entities/UsageRecord/UsageRecord.converters.ts",
    "line": 141,
    "symbol": "toUsageRecordInsert",
    "smell": "return rest as UsageRecordInsert is an `as` cast candidate surfaced by the S3 no-`as` detector pass after dropping the database-managed id from the encoded entity row.",
    "proposedTarget": "None -- keep as-is unless the table codec grows a schema-derived insert encoder.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "Persistence-boundary projection: encodeUsageRecord returns the full selected row shape and object rest drops the SERIAL id, but TypeScript does not infer Drizzle's $inferInsert projection from that destructuring. This is not discriminator narrowing and no S3 match/guard primitive removes the cast without changing the table converter API.",
      "boundary": "epistemic UsageRecord Drizzle insert-row converter"
    }
  }
]
