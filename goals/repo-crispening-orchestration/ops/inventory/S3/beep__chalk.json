[
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/capability/chalk/src/internal/ChalkRuntime.ts",
    "line": 148,
    "symbol": "setChalkStateLevel",
    "smell": "(state as MutableChalkState).level = normalizeColorSupportLevel(level) uses a repo-banned as cast (only as const is allowed) to strip readonly from the S.toTaggedUnion-derived ChalkState and mutate the discriminator in place, bypassing the schema-owned variant model.",
    "proposedTarget": "Model the shared-mutable builder level explicitly instead of casting: hold state in a mutable box ({ state: ChalkState } whose property is reassigned to a freshly-made variant) or give BuilderMeta an intentionally mutable level field, keeping normalizeColorSupportLevel's Result-based decode at the boundary. Requires care: derived builders share the state object by reference, so the box must remain the shared identity.",
    "confidence": 0.65,
    "mechanization": "assisted",
    "roiRank": 2.6,
    "exception": {
      "reason": "The localized mutable cast preserves shared level propagation across child builders (covered by tests). Replacing it requires a shared mutable state-box redesign rather than a safe schema-static colocation.",
      "boundary": "internal shared Chalk builder state identity"
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/capability/chalk/src/internal/ChalkSchema.ts",
    "line": 221,
    "symbol": "ColorInfo",
    "smell": "ColorInfo = S.Union([ColorSupport, S.Literal(false)]) is an undiscriminated union (struct | false) that a naive undiscriminated-union detector would flag as a tagged-union candidate.",
    "proposedTarget": "None -- keep as-is.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "Upstream chalk API compatibility: supportsColor must be `false | ColorSupport` so the ecosystem idiom `supportsColor === false` keeps working (package docs and public aliases explicitly preserve chalk API compatibility). The union is trivially discriminable by the false literal; tagging it would break the drop-in surface.",
      "boundary": "public chalk-compatible API surface (@beep/chalk supportsColor / supportsColorStderr / ColorInfo)"
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/capability/chalk/src/internal/PublicSurface.ts",
    "line": 235,
    "symbol": "makeChalkConstructor",
    "smell": "new Proxy(ConstructorBase, { construct... }) as unknown as ChalkConstructorFor<Base> -- a repo-banned double cast retyping a Proxy whose construct trap changes the constructor signature.",
    "proposedTarget": "None -- keep as-is.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "TypeScript types new Proxy(target, handler) as typeof target; no Effect narrowing primitive (P.*, S.is, Match) can express a construct-trap signature change, and a class-extends restructuring fails because create() returns object, which is not assignable to InstanceType<Base>. The cast is the minimal honest boundary for the chalk callable-constructor architecture mirrored from upstream.",
      "boundary": "type-system limit at the Proxy construct trap in PublicSurface.makeChalkConstructor"
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/capability/chalk/src/internal/ChalkRuntime.ts",
    "line": 215,
    "symbol": "applyStyle",
    "smell": "if (state.level <= 0 || text.length === 0) branches on the ChalkState discriminator field where the S.toTaggedUnion-derived ChalkState.match/.guards exist -- an if-over-discriminator detector would flag it.",
    "proposedTarget": "None -- keep as-is.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "The check is a numeric threshold/ordering comparison (<= 0) combined with an unrelated emptiness test, not variant dispatch; a derived 4-case match adds noise without an exhaustiveness benefit on this hot string-render path. ChalkState's derived match remains the right tool where cases actually diverge.",
      "boundary": "internal hot render path (ChalkRuntime.applyStyle); no public contract"
    }
  },
  {
    "ruleId": "literal-kit-const-assertion",
    "file": "packages/foundation/capability/chalk/src/internal/ChalkSchema.ts",
    "line": 44,
    "symbol": "modifierNameValues",
    "smell": "Named as-const literal arrays feeding LiteralKit by identifier (modifierNameValues:44, colorSupportLevelValues:28, foregroundColorNameValues:70, backgroundColorNameValues:105, colorNameValues:140) -- a loose literal-kit-const-assertion heuristic keyed on 'LiteralKit(x) where x's declaration has as const' would flag them.",
    "proposedTarget": "None -- keep as-is.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "The card only covers inline LiteralKit array arguments (detector at packages/tooling/tool/cli/src/commands/Lint/SchemaFirst.ts:476-486: 'Inline LiteralKit array arguments do not need as const'). These arrays are separate named bindings where as const is required for tuple inference, they are the single source the kits derive from (no parallel duplicate family), and they double as documented public API exports (modifierNames, colorNames, etc. in Chalk.ts).",
      "boundary": "named public-API literal tuples consumed by LiteralKit by identifier, outside the inline-argument scope of the card"
    }
  }
]
