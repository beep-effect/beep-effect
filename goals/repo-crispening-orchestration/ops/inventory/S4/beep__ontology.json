[
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/modeling/ontology/src/Ontology.models.ts",
    "line": 653,
    "symbol": "OWLSearchScore / ValidationErrorLocationSegment",
    "smell": "Two exported union consts (`S.Union([S.Int, S.Finite])` at line 653, `S.Union([S.String, S.Int])` at line 767) without `SchemaUtils.withCodecStatics` — literal match for the S4 'union consts missing withCodecStatics' smell.",
    "proposedTarget": "No action. If either gains guard/decode call sites, append `SchemaUtils.withCodecStatics` to the existing `.pipe($I.annoteSchema(...))` chain (SchemaUtils is already imported at line 10).",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 0.4,
    "exception": {
      "reason": "Zero decode/guard/fromUnknown call sites anywhere in the repo (rg over packages/** and apps/** finds no consumers of either union outside this file; the package test/dtslint suites hoist no decode helpers for them). Both exist solely as field building blocks inside the OpenAPI-mirror classes (OWLSearchResult tuple, ValidationError.loc). Attaching withCodecStatics would add speculative surface with no free-helper wall to delete — the crispen doctrine stops at the first rung that removes noise, and there is no noise. The rest of the package is a pure schema mirror (classes + LiteralKits with Option/defaults already in codecs) with no free-floating behavior at all.",
      "boundary": "packages/foundation/modeling/ontology/src/Ontology.models.ts (@beep/ontology OpenAPI component mirror). Revisit when the FOLIO client wiring starts decoding these shapes directly."
    }
  }
]
