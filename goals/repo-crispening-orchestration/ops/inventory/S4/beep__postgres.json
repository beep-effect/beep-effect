[
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/drivers/postgres/src/PostgresSqlState.models.ts",
    "line": 1503,
    "symbol": "isPgErrorCode",
    "smell": "Detector hit: exported free-floating schema guard `S.is(PgErrorCode)` beside a LiteralKit domain.",
    "proposedTarget": "None - leave as-is in this S4 pass.",
    "confidence": 0.35,
    "mechanization": "judgment",
    "roiRank": 5,
    "exception": {
      "reason": "`PgErrorCode` is a LiteralKit; `$I.annoteSchema` already preserves LiteralKit statics (packages/foundation/modeling/identity/src/Id.ts:72 and :1386), while `SchemaUtils.withCodecStatics` would collide with LiteralKit's non-configurable `is` object. The exported full-domain guard is a compatibility API until a non-colliding LiteralKit codec-static convention exists.",
      "boundary": "Verified current SchemaUtils/withStatics.ts:31-43 redefinition behavior; do not propose `withCodecStatics` on LiteralKit `is` surfaces."
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/drivers/postgres/src/PostgresDiagnostics.service.ts",
    "line": 313,
    "symbol": "formatSql",
    "smell": "Detector hit: exported 3-arg helper is not `dual`.",
    "proposedTarget": "None - leave as-is.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 6,
    "exception": {
      "reason": "`formatSql(statement, parameters = [], palette = colors)` intentionally supports 1-, 2-, and 3-argument calls. `dual(3, ...)` would reinterpret the existing one-argument call as data-last instead of returning formatted SQL.",
      "boundary": "Optional-trailing-argument public utility; preserving current call semantics outranks pipeability."
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/drivers/postgres/src/PostgresDiagnostics.service.ts",
    "line": 344,
    "symbol": "formatPostgresError",
    "smell": "Detector hit: exported 2-arg helper is not `dual`.",
    "proposedTarget": "None - leave as-is.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 7,
    "exception": {
      "reason": "`formatPostgresError(error, palette = colors)` intentionally supports a one-argument formatting call. `dual(2, ...)` would turn that call into a data-last function and break the public API.",
      "boundary": "Optional-trailing-argument public utility; no public-form change without same-PR consumer sweep."
    }
  }
]
