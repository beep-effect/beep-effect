[
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/drivers/m365/src/M365.service.ts",
    "line": 755,
    "symbol": "decodeRequest",
    "smell": "Detector hit: generic boundary decoder calls `S.decodeUnknownEffect(schema)` instead of schema-local statics.",
    "proposedTarget": "None - leave as-is in S4.",
    "confidence": 0.4,
    "mechanization": "judgment",
    "roiRank": 4,
    "exception": {
      "reason": "This helper maps request decode failures into resource-specific `M365Error.fromReason(\"request encoding\", ...)`; `withCodecStatics` would not preserve that typed error channel or resource context.",
      "boundary": "SPEC Non-Goal 4: no trust-boundary weakening; request-boundary error mapping stays explicit."
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/drivers/m365/src/M365.service.ts",
    "line": 877,
    "symbol": "executeJson",
    "smell": "Detector hit: generic response decoder calls `S.decodeUnknownEffect(schema)` instead of schema-local statics.",
    "proposedTarget": "None - leave as-is in S4.",
    "confidence": 0.4,
    "mechanization": "judgment",
    "roiRank": 5,
    "exception": {
      "reason": "This is the HTTP response trust boundary and maps schema failures into `M365Error.fromReason(\"response decoding\", ...)` with status, URL, and resource context. Replacing it with schema statics would erase driver-specific diagnostics.",
      "boundary": "SPEC Non-Goal 4: no trust-boundary weakening; response-boundary error mapping stays explicit."
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/drivers/m365/src/M365.auth.ts",
    "line": 332,
    "symbol": "M365Auth.makeLayer",
    "smell": "Detector hit: public 2-arg static helper is not `dual`.",
    "proposedTarget": "None - leave as-is.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 6,
    "exception": {
      "reason": "`makeLayer(config, options = {})` intentionally supports the one-argument layer constructor used by callers. `dual(2, ...)` would reinterpret that existing call as data-last.",
      "boundary": "Optional-trailing-argument layer constructor; preserving public call semantics outranks pipeability."
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/drivers/m365/src/M365.service.ts",
    "line": 1156,
    "symbol": "M365.makeLiveLayer",
    "smell": "Detector hit: public 2-arg static helper is not `dual`.",
    "proposedTarget": "None - leave as-is.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 7,
    "exception": {
      "reason": "`makeLiveLayer(config, options = {})` intentionally supports the one-argument live-layer constructor. `dual(2, ...)` would break that public form.",
      "boundary": "Optional-trailing-argument layer constructor; no public-form change without same-PR consumer sweep."
    }
  }
]
