[
  {
    "ruleId": "SFV4-static-api",
    "file": "apps/architecture-lab-proof/src/index.ts",
    "line": 19,
    "symbol": "decodeWorkItemId",
    "smell": "Package entry point builds a free-floating decoder `S.decodeUnknownEffect(DomainWorkItem.WorkItemId)` for the proof WorkItem id and consumes it once at line 66; the branded schema is defined in `packages/architecture-lab/domain/src/aggregates/WorkItem/WorkItem.values.ts:33` without codec statics.",
    "proposedTarget": "Attach `SchemaUtils.withCodecStatics` (or a `withStatics` decode effect if the async decoder is required) to `WorkItemId` in its owning domain value module, then replace this helper with `DomainWorkItem.WorkItemId.fromUnknown(...)` or `DomainWorkItem.WorkItemId.decodeUnknownEffect(...)` depending on the chosen static. `withCodecStatics` is verified at `packages/foundation/modeling/schema/src/SchemaUtils/withCodecStatics.ts:90` and `S.decodeUnknownEffect` at `.repos/effect-v4/packages/effect/src/Schema.ts:1325`.",
    "confidence": 0.7,
    "mechanization": "assisted",
    "roiRank": 1
  }
]
