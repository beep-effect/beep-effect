[
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/modeling/pandoc-ast/src/Pandoc.model.ts",
    "line": 959,
    "symbol": "PandocInline",
    "smell": "Exported tagged-union const (`S.Union([Str, Space, ...UnknownInline]).pipe(S.toTaggedUnion(\"_tag\"), $I.annoteSchema(...))`) missing `SchemaUtils.withCodecStatics` — no colocated is/fromUnknown/decodeOption (toTaggedUnion supplies only match/guards/cases/isAnyOf).",
    "proposedTarget": "Append `SchemaUtils.withCodecStatics` as the final pipe step — exemplar-identical to the sibling Md union (packages/foundation/modeling/md/src/Md.model.ts:608-614: `S.Union([...]).pipe(S.toTaggedUnion(\"_tag\"), $I.annoteSchema(...), SchemaUtils.withCodecStatics)`). Purely additive (no encoded-form change, no §5.4 sweep); import SchemaUtils from @beep/schema.",
    "confidence": 0.9,
    "mechanization": "codemod",
    "roiRank": 1.8
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/modeling/pandoc-ast/src/Pandoc.model.ts",
    "line": 1532,
    "symbol": "PandocBlock",
    "smell": "Exported tagged-union const (`S.Union([Plain, Para, ...UnknownBlock]).pipe(S.toTaggedUnion(\"_tag\"), $I.annoteSchema(...))`) missing `SchemaUtils.withCodecStatics` — same gap as PandocInline; the package's block union has no colocated codec statics while its Md twin does.",
    "proposedTarget": "Append `SchemaUtils.withCodecStatics` as the final pipe step (Md Block exemplar, packages/foundation/modeling/md/src/Md.model.ts:1612-1618). Purely additive.",
    "confidence": 0.9,
    "mechanization": "codemod",
    "roiRank": 1.8
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/modeling/pandoc-ast/src/Pandoc.mapping.ts",
    "line": 98,
    "symbol": "isMdInline",
    "smell": "Free-floating guard helper `const isMdInline = S.is(Md.Inline)` duplicating an already-colocated schema-derived static: Md.Inline carries `SchemaUtils.withCodecStatics` (packages/foundation/modeling/md/src/Md.model.ts:613), so `Md.Inline.is` exists — the literal SFV4-static-api reuse case (duplicate guard helper where a schema-derived static exists).",
    "proposedTarget": "Replace the two call sites (`isMdInline(child)` at Pandoc.mapping.ts:405 and :444) with `Md.Inline.is(child)` and delete the module-private helper. `withCodecStatics` `is` is a type guard `(u: unknown) => u is Type` (packages/foundation/modeling/schema/src/SchemaUtils/withCodecStatics.ts:64-70), so the Md.ListItemChild → Md.Inline narrowing is preserved. Behavior-identical, zero public-form change.",
    "confidence": 0.92,
    "mechanization": "codemod",
    "roiRank": 2.8
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/modeling/pandoc-ast/src/Pandoc.report.ts",
    "line": 160,
    "symbol": "jsonPointerFromPath",
    "smell": "Pure JSON-Pointer rendering behavior for the JsonPath schema lives as a free exported function (plus private `escapePointerSegment` at line 144) instead of a static colocated on the JsonPath const; PandocMappingIssue.fromPath (line 209) consumes it free-floating.",
    "proposedTarget": "Attach via `SchemaUtils.withStatics(() => ({ toPointer: ... }))` on the JsonPath const (packages/foundation/modeling/schema/src/SchemaUtils/withStatics.ts — piping a plain S.Array const is safe, unlike classes) so callers use `JsonPath.toPointer(path)`; PandocMappingIssue.fromPath and the doc examples follow. No external consumers exist (rg: only in-file uses + JSDoc examples), so dropping the free export is an in-package §5.4 sweep; alternatively keep the export as a thin alias. Modest ROI — one helper, one in-file consumer.",
    "confidence": 0.55,
    "mechanization": "judgment",
    "roiRank": 1.1
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/modeling/pandoc-ast/src/Pandoc.report.ts",
    "line": 250,
    "symbol": "profileFromIssues",
    "smell": "Pure profile derivation for the PandocMappingProfile kit lives as a free exported function whose only real consumer is the colocated static `PandocCompatibilityReport.fromIssues` (line 281) in the same file — behavior separated from its data with an exported surface nobody else imports (rg: no consumers outside this file and its JSDoc example).",
    "proposedTarget": "Fold into the data: either inline the ternary into `PandocCompatibilityReport.fromIssues` and delete the export, or attach as `SchemaUtils.withStatics(() => ({ fromIssues: ... }))` on the PandocMappingProfile kit const so callers use `PandocMappingProfile.fromIssues(issues)`. In-package-only §5.4 sweep (doc example update).",
    "confidence": 0.5,
    "mechanization": "judgment",
    "roiRank": 1.0
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/modeling/pandoc-ast/src/Pandoc.mapping.ts",
    "line": 68,
    "symbol": "hasPandocAttr",
    "smell": "Generic non-emptiness predicate over PandocAttr data (`attr.id.length > 0 || attr.classes.length > 0 || attr.keyValues.length > 0`) lives as a free module-private helper in the mapping module, separated from the PandocAttr S.Class in Pandoc.model.ts — which already demonstrates the colocation pattern with its `static readonly empty`.",
    "proposedTarget": "Colocate as an in-body static on PandocAttr (`static readonly isNonEmpty = (self: PandocAttr.Type): boolean => ...`; in-body statics only — piping the class loses constructor identity per packages/foundation/modeling/schema/src/SchemaUtils/withCodecStatics.ts:10-12) and call `PandocAttr.isNonEmpty(node.attr)` at the ~6 mapping call sites. The sibling `hasCodeBlockDroppedAttr` (line 71) stays put — it encodes mapping-specific loss policy (classes.length > 1), not a data invariant. Small, private, one consumer module — judgment on ROI.",
    "confidence": 0.5,
    "mechanization": "judgment",
    "roiRank": 1.0
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/modeling/pandoc-ast/src/Pandoc.codec.ts",
    "line": 187,
    "symbol": "decodeConstructor ... decodeListNumberDelimiter (19-const decode hoist block)",
    "smell": "Nineteen hoisted `S.decodeUnknownEffect(...)` / `S.encodeEffect(...)` consts at lines 187-205 look like the canonical top-of-file decode wall.",
    "proposedTarget": "None — legitimate carve-out. All are module-private hoisted-once effectful codecs inside the package's dedicated wire-codec module, directly above their consumers; most target unexported file-local wire tuples (AttrWire, TargetWire, ...PayloadWire, lines 166-185) that have no other call sites anywhere. CodecStatics offers no Effect-returning decoder ({is, fromUnknown, decodeOption} only, withCodecStatics.ts:50-63), so no schema static can absorb them, and attaching per-schema `decodeEffect` statics to 14 private single-consumer wire consts adds surface without deleting any noise.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 0.3,
    "exception": {
      "reason": "Hoisted-once effectful decoders, module-private and colocated with both their schemas and their single consumers in the codec module whose entire purpose is wire conversion; identical in kind to the accepted file-processing Artifact hoist exception, only larger. The effectful form is outside CodecStatics, so withCodecStatics cannot replace them.",
      "boundary": "packages/foundation/modeling/pandoc-ast/src/Pandoc.codec.ts (wire-codec module). Revisit only if a decodeEffect-bearing statics combinator lands in SchemaUtils and the wire consts gain external consumers."
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/modeling/pandoc-ast/src/Pandoc.mapping.ts",
    "line": 100,
    "symbol": "mdInlineText / pandocInlineText / pandocBlockText / mdBlockText",
    "smell": "Pure Match.tagsExhaustive text-degradation folds over Md.Inline/Md.Block and PandocInline/PandocBlock data live in the mapping module, superficially matching 'pure fns detached from their schema' (the Pandoc folds' schemas live in Pandoc.model.ts, the Md folds' schemas in another package).",
    "proposedTarget": "None — legitimate carve-out. These encode this projection's loss semantics (notes/math degrade to text, tables render as '[table]' captions, YouTube embeds become watch URLs), not general data behavior: @beep/md exports no plainText/inlineText fold (rg over packages/foundation/modeling/md/src is empty), so nothing is duplicated, and colocating mapping-specific degradation policy onto the Md or Pandoc models would smear compatibility policy into the data packages (and cross into @beep/md's wave, fence 8). All four are module-private.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 0.3,
    "exception": {
      "reason": "Projection-specific degradation policy, module-private, with no schema-derived or in-repo equivalent to reuse; colocation on the data classes would relocate mapping policy into model packages and violate the touch-scoped wave fence for @beep/md.",
      "boundary": "packages/foundation/modeling/pandoc-ast/src/Pandoc.mapping.ts. Revisit if @beep/md grows a canonical plain-text fold these could delegate to."
    }
  }
]
