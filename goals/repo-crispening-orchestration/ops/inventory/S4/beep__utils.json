[
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/modeling/utils/src/Glob.ts",
    "line": 233,
    "symbol": "toGlobError",
    "smell": "Module-private constructor helper duplicating the already-colocated schema-derived static: its Match.orElse branch hand-builds `GlobError.make({ pattern, cause: S.decodeUnknownOption(S.Defect({ includeStack: true }))(error) })` (line 238-241), which re-implements `normalizeGlobErrorCause` (line 164-165) and therefore the colocated dual static `GlobError.new(pattern, cause)` (line 151-155) — a duplicate constructor helper where a schema-derived static exists, plus the `S.Defect({ includeStack: true })` schema and its decoder are rebuilt on every invocation of the returned closure.",
    "proposedTarget": "Collapse the orElse branch to `Match.orElse((error) => GlobError.new(pattern, error))` — `GlobError.new` already routes through `normalizeGlobErrorCause`, which performs the identical `S.decodeUnknownOption(S.Defect({ includeStack: true }))` on non-Option input. Type note: `GlobErrorCauseInput` is `GlobError[\"cause\"] | GlobError.Encoded[\"cause\"]`, so the `unknown` from Match.orElse needs either a widening of GlobErrorCauseInput to accept unknown (normalizeGlobErrorCause already handles arbitrary input at runtime) or a decode-first step — verify at the call site. Also hoist the repeated `S.decodeUnknownOption(S.Defect({ includeStack: true }))` (lines 165 and 240) to a single module const so the schema is built once. Module-private, zero public-form change.",
    "confidence": 0.65,
    "mechanization": "assisted",
    "roiRank": 1.3
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/modeling/utils/src/Glob.ts",
    "line": 37,
    "symbol": "Pattern / PathInput",
    "smell": "Two union consts without `SchemaUtils.withCodecStatics`: `Pattern = S.Union([S.String, S.Array(S.String)])` (Glob.ts:37) and the internal `PathInput = S.Union([S.String, S.Array(S.String)])` (src/internal/StructPath.ts:31) — literal match for the 'union consts missing withCodecStatics' smell.",
    "proposedTarget": "No action. If either gains decode/guard call sites, append `SchemaUtils.withCodecStatics` — but note @beep/utils sits below @beep/schema in the foundation dependency graph (it imports only effect + @beep/identity/@beep/types), so SchemaUtils may not even be importable here without an inversion; the attachment would have to be plain in-file statics instead.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 0.3,
    "exception": {
      "reason": "Both unions are used purely as type carriers for dual helper signatures (Struct.ts/Option.ts path helpers, Glob service inputs); rg finds zero decode/guard/fromUnknown call sites for either anywhere in the repo. PathInput is module-internal. Attaching codec statics would add speculative surface with no helper wall to delete, and @beep/utils is itself the dual/flow helper home whose own primitives are out of S4 scope by packet instruction. The rest of the package is the S4 exemplar surface: dual-arity throughout (Str/Struct/Array/Stream/GlobalValue/Event), flow for passthroughs (Stream.ts:60), and statics already colocated on its schema classes (GlobError.new/newThunk).",
      "boundary": "packages/foundation/modeling/utils (@beep/utils, the dual/flow helper home). Revisit only if the unions gain codec call sites."
    }
  }
]
