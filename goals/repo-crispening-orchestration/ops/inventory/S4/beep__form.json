[
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/ui-system/form/src/internal/FieldBinding.tsx",
    "line": 428,
    "symbol": "decodeDateTimePickerValue",
    "smell": "Detector hit: module-local `const decodeDateTimePickerValue = S.decodeUnknownOption(DateTimeUtcFromValid)` looks like a decode-wall entry.",
    "proposedTarget": "None — leave as-is in @beep/form.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 0.3,
    "exception": {
      "reason": "`DateTimeUtcFromValid` is @beep/schema-owned and has no decodeOption static today; this UI adapter specifically converts MUI picker input into `DateTime | null`, so the soft Option decode is the boundary behavior. A real cleanup belongs to the schema package adding a non-throwing static, then this helper can delegate.",
      "boundary": "MUI DateTimePicker adapter boundary in packages/foundation/ui-system/form/src/internal/FieldBinding.tsx plus upstream @beep/schema/DateTimeUtcFromValid ownership."
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/ui-system/form/src/core/FormOptions.ts",
    "line": 178,
    "symbol": "formOptionsWithSubmit / formOptionsWithSubmitEffect",
    "smell": "Detector hit: generic form builders create `const decode = S.decodeUnknownPromise(params.schema)` at line 178 and `const decode = S.decodeUnknownEffect(params.schema)` at line 221.",
    "proposedTarget": "None — leave generic schema-parameter decoders as local closures.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 0.3,
    "exception": {
      "reason": "The schema is caller-supplied and generic (`params.schema`), not a package-owned const that can receive colocated statics. The local closure captures the selected submitted schema once so TanStack Form can pass encoded field values into either Promise or Effect submit callbacks.",
      "boundary": "Generic form option builder boundary; no concrete schema data object exists in @beep/form to receive statics."
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/ui-system/form/src/core/Defaults.ts",
    "line": 70,
    "symbol": "getEncodedDefaultFormValues",
    "smell": "Detector hit: local `const encode = schema.pipe(S.encodeSync)` resembles an encode helper.",
    "proposedTarget": "None — leave as-is.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 0.3,
    "exception": {
      "reason": "This is a generic helper over any caller-provided codec; there is no known schema const or reusable package model to colocate an encode static onto. The local encoder is scoped to converting constructor defaults into TanStack Form's encoded defaultValues shape.",
      "boundary": "Generic default-values bridge from Effect Schema codecs to TanStack Form encoded field values."
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/ui-system/form/src/internal/FieldShell.tsx",
    "line": 38,
    "symbol": "FieldShellProps / field component props interfaces",
    "smell": "Detector hit: @beep/form exports many `*FieldProps` / component props interfaces across `src/fields/*`, `src/components/*`, and `src/internal/*` rather than schema/static-backed models.",
    "proposedTarget": "None — leave as React component props/interfaces.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 0.3,
    "exception": {
      "reason": "React field component props are an explicit UI boundary carve-out for @beep/form. These interfaces describe component surfaces over TanStack Form and UI controls; schema-ifying them would add runtime modeling to React props without deleting schema helper duplication.",
      "boundary": "@beep/form React UI component props boundary."
    }
  }
]
