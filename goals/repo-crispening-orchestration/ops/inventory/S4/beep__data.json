[
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/primitive/data/src/internal/data/mime-types/index.ts",
    "line": 265,
    "symbol": "lookup",
    "smell": "Exported one-arg MIME lookup helper is a passthrough `return pipe(path, O.liftPredicate(...), O.map(...), O.map(...), O.getOrElse(...))` pipeline. It also wraps `!Str.isEmpty(value)` instead of using the verified direct predicate `Str.isNonEmpty`.",
    "proposedTarget": "Hoist the pipeline to `Fn.flow(O.liftPredicate(Str.isNonEmpty), O.map(normalizeLookupExtension), O.map(lookupNormalizedExtension), O.getOrElse(returnFalse))`, then either export that const directly or keep the existing function shell as `return lookupPath(path)` if the internal public form must stay a function. In-repo exemplar: the same module already uses `Fn.flow` for `normalizeLookupExtension` at line 179. Effect v4 APIs verified at `.repos/effect-v4/packages/effect/src/Function.ts:1162`, `.repos/effect-v4/packages/effect/src/Option.ts:617`, `.repos/effect-v4/packages/effect/src/Option.ts:1159`, `.repos/effect-v4/packages/effect/src/Option.ts:2178`, and `.repos/effect-v4/packages/effect/src/String.ts:420`.",
    "confidence": 0.85,
    "mechanization": "assisted",
    "roiRank": 1
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/primitive/data/src/internal/data/utils.ts",
    "line": 4,
    "symbol": "formalizeValues",
    "smell": "Internal exported helper is a trivial data-first wrapper around dual `A.map`: `<T>(values: T) => A.map(values, Str.capitalize)`. The only consumers are the month and weekday value derivations, so the helper can keep its semantic name while delegating through the data-last helper form.",
    "proposedTarget": "Prefer the direct helper ref `export const formalizeValues = A.map(Str.capitalize)` if the inferred tuple/non-empty return type remains acceptable for `calendar/month.ts:18` and `calendar/weekday.ts:5`; otherwise keep the current signature and treat it as an explicit type-preservation exception. Effect v4 `Array.map` dual form verified at `.repos/effect-v4/packages/effect/src/Array.ts:3493`; `Str.capitalize` verified at `.repos/effect-v4/packages/effect/src/String.ts:211`.",
    "confidence": 0.65,
    "mechanization": "assisted",
    "roiRank": 2
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/primitive/data/src/internal/data/mime-types/index.ts",
    "line": 182,
    "symbol": "hasTypeForExtension",
    "smell": "False-positive audit for non-dual-helper detector: private 2-arg type predicate `hasTypeForExtension(extension, types)` is not `dual`.",
    "proposedTarget": "None - leave as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 3,
    "exception": {
      "reason": "This is a private TypeScript refinement used to narrow `extension is FileExtension` inside `lookupNormalizedExtension`; converting it to `dual` would add no public pipeability and risks weakening the refinement readability.",
      "boundary": "Private MIME lookup refinement, not a public 2-3 arg helper."
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/primitive/data/src/internal/data/mime-types/index.ts",
    "line": 186,
    "symbol": "lookupNormalizedExtension",
    "smell": "False-positive audit for passthrough-pipe detector: private helper contains `return pipe(extension, ...)`, but first snapshots the lazily populated `getTypes()` table and threads that local table through a refinement.",
    "proposedTarget": "None - leave as-is.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 4,
    "exception": {
      "reason": "The pipeline is coupled to a per-call lazy lookup-table snapshot and an inline `extension is FileExtension` refinement; hoisting to `flow` would hide the cache boundary rather than reduce a trivial wrapper.",
      "boundary": "Private MIME lookup helper with local cache/refinement state."
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/primitive/data/src/internal/data/mime-types/index.ts",
    "line": 280,
    "symbol": "preferenceOf",
    "smell": "False-positive audit for passthrough-pipe detector: private helper returns `pipe(preference, A.findFirstIndex(...), O.getOrElse(...))`, but the piped value is the fixed source-preference table, not the function argument.",
    "proposedTarget": "None - leave as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 5,
    "exception": {
      "reason": "This is a small private table lookup over a closed preference array; `flow` would have to capture `source` anyway and would not improve call-site pipeability.",
      "boundary": "Private MIME source-preference lookup, not a passthrough argument pipeline."
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/primitive/data/src/internal/data/mime-types/index.ts",
    "line": 288,
    "symbol": "shouldKeepByPreference",
    "smell": "False-positive audit for non-dual-helper detector: private 2-arg predicate `shouldKeepByPreference(currentType, mimeSource)` is not `dual`.",
    "proposedTarget": "None - leave as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 6,
    "exception": {
      "reason": "The helper is private to the MIME map-population algorithm and has no receiver-like `self` parameter or public call surface; dual arity would only add ceremony.",
      "boundary": "Private MIME source-preference predicate, not a public 2-3 arg helper."
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/primitive/data/src/internal/data/mime-types/index.ts",
    "line": 298,
    "symbol": "shouldKeepCurrentMapping",
    "smell": "False-positive audit for non-dual-helper detector: private 3-arg predicate `shouldKeepCurrentMapping(extension, mimeSource, types)` is not `dual`.",
    "proposedTarget": "None - leave as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 7,
    "exception": {
      "reason": "This is an internal decision helper for mutating the extension-to-type cache; parameters are algorithm inputs, not a public data-first API.",
      "boundary": "Private MIME cache-population predicate, not a public 2-3 arg helper."
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/primitive/data/src/internal/data/mime-types/index.ts",
    "line": 341,
    "symbol": "populateMaps",
    "smell": "False-positive audit for non-dual-helper detector: private 2-arg cache initializer `populateMaps(extensions, types)` is not `dual`.",
    "proposedTarget": "None - leave as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 8,
    "exception": {
      "reason": "Both parameters are mutable output caches populated together under the `inittedMaps` guard; a dual receiver shape would imply a reusable public transformation that this side-effectful initializer is not.",
      "boundary": "Private lazy-cache initializer, not a public 2-3 arg helper."
    }
  }
]
