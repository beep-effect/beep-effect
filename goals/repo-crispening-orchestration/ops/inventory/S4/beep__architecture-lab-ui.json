[
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/architecture-lab/ui/src/aggregates/WorkItem/WorkItem.view-model.ts",
    "line": 178,
    "symbol": "toWorkItemSummaryViewModel",
    "smell": "Detector scan hit: public 2-arg helper with explicit dual-call overload signature.",
    "proposedTarget": "None — leave as-is; already implemented as `dual(2, (workItem, config) => ...)` with a matching data-first/data-last overload type, which is exactly the S4 target pattern.",
    "confidence": 0.05,
    "mechanization": "judgment",
    "roiRank": 10,
    "exception": {
      "reason": "Already the sanctioned `dual`-based pipe-ability form the rule asks for; recorded only for false-positive-audit completeness since the detector's 2-arg-helper heuristic would otherwise flag it.",
      "boundary": "Not a smell — matches the SPEC's own prescribed fix shape (`dual(2, ...)`)."
    }
  }
]
