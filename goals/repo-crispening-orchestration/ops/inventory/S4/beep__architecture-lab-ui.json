[
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/architecture-lab/ui/src/aggregates/WorkItem/WorkItem.view-model.ts",
    "line": 44,
    "symbol": "WorkItemVisibleAction",
    "smell": "LiteralKit const `LiteralKit([\"assign\", \"complete\", \"reopen\", \"archive\"]).pipe($I.annoteSchema(...))` has no `withLiteralKitStatics`/`withCodecStatics` attached; the file's own doc comment (line 34) falls back to bare `S.decodeUnknownSync(WorkItemVisibleAction)(\"archive\")` instead of a colocated `.fromUnknown`/`.decodeOption`, and `makeVisibleActions` (line 127) compares raw string literals (`action !== \"assign\"`) rather than using `WorkItemVisibleAction.Enum.assign`-style colocated members through a guard.",
    "proposedTarget": "Capture the kit in a base const and compose both statics, mirroring packages/foundation/modeling/schema/src/HttpMethod/HttpMethod.schema.ts:30-35: `const WorkItemVisibleActionBase = LiteralKit([\"assign\", \"complete\", \"reopen\", \"archive\"]); export const WorkItemVisibleAction = WorkItemVisibleActionBase.pipe($I.annoteSchema(\"WorkItemVisibleAction\", {...}), SchemaUtils.withLiteralKitStatics(WorkItemVisibleActionBase), SchemaUtils.withCodecStatics)` (statics verified at packages/foundation/modeling/schema/src/SchemaUtils/withLiteralKitStatics.ts:37 and withCodecStatics.ts:90).",
    "confidence": 0.8,
    "mechanization": "assisted",
    "roiRank": 3
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/architecture-lab/ui/src/aggregates/WorkItem/WorkItem.view-model.ts",
    "line": 178,
    "symbol": "toWorkItemSummaryViewModel",
    "smell": "Detector scan hit: public 2-arg helper with explicit dual-call overload signature.",
    "proposedTarget": "None — leave as-is; already implemented as `dual(2, (workItem, config) => ...)` with a matching data-first/data-last overload type, which is exactly the S4 target pattern.",
    "confidence": 0.05,
    "mechanization": "judgment",
    "roiRank": 10,
    "exception": {
      "reason": "Already the sanctioned `dual`-based pipe-ability form the rule asks for; recorded only for false-positive-audit completeness since the detector's 2-arg-helper heuristic would otherwise flag it.",
      "boundary": "Not a smell — matches the SPEC's own prescribed fix shape (`dual(2, ...)`)."
    }
  }
]
