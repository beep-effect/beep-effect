[
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/epistemic/use-cases/src/ClaimGate/ClaimGate.service.ts",
    "line": 32,
    "symbol": "decodeClaimGateResult",
    "smell": "Top-of-file decode-wall const `const decodeClaimGateResult = S.decodeUnknownSync(ClaimGateResult)` duplicates the `fromUnknown` static that `SchemaUtils.withCodecStatics` would put directly on `ClaimGateResult` (see beep__epistemic-domain.json finding on ClaimGateResult.model.ts:114); both call sites (lines 72-73, throw-on-decode-failure of known-good literals) are `fromUnknown` semantics.",
    "proposedTarget": "Delete the module-level const; call `ClaimGateResult.fromUnknown({...})` directly at both call sites once `withCodecStatics` lands upstream on `ClaimGateResult`.",
    "confidence": 0.75,
    "mechanization": "assisted",
    "roiRank": 1,
    "exception": {
      "reason": "`ClaimGateResult` is defined in `@beep/epistemic-domain`; adding `fromUnknown` there would require a cross-package schema-static ripple outside this writer's package. The file-scoped static-api-colocation codemod proposer was run and returned unchanged because the owner schema is imported.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/epistemic/use-cases/src/ClaimGate/ClaimGate.service.ts",
    "line": 33,
    "symbol": "decodeShaclValidationRequest",
    "smell": "Top-of-file decode-wall const `const decodeShaclValidationRequest = S.decodeUnknownSync(ShaclValidationRequest)` duplicates decode behavior that colocated in-body statics on `ShaclValidationRequest` (an `S.Class` with no attached statics today — verified at packages/foundation/capability/semantic-web/src/services/shacl-validation.ts:183) would provide.",
    "proposedTarget": "Cannot be fixed inside this package: `ShaclValidationRequest` is owned by `@beep/semantic-web`, outside this scan's package boundary (fence 8, touch-scoped waves). Flagging the consumer-side duplication here; the actual fix (in-body `static readonly fromUnknown` on `ShaclValidationRequest`) belongs to a `@beep/semantic-web` wave.",
    "confidence": 0.4,
    "mechanization": "judgment",
    "roiRank": 4,
    "exception": {
      "reason": "The schema needing statics (`ShaclValidationRequest`) is defined in `@beep/semantic-web`, not in `@beep/epistemic-use-cases`; fence 8 (touch-scoped waves) means this package's wave cannot edit another package's schema file. Recorded as a cross-package dependency, not omitted.",
      "boundary": "SPEC fence 8 — touch-scoped waves; fix owner is @beep/semantic-web, not this package."
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/epistemic/use-cases/src/ClaimGate/ClaimGate.service.ts",
    "line": 34,
    "symbol": "encodeDataset",
    "smell": "Detector scan hit: module-level `const encodeDataset = S.encodeSync(Dataset)` — `Dataset` is owned by `@beep/semantic-web/rdf`, another package.",
    "proposedTarget": "None in this package — same cross-package boundary as decodeShaclValidationRequest.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 6,
    "exception": {
      "reason": "`Dataset` is defined in `@beep/semantic-web/rdf`, outside this package; attaching statics there is out of this wave's scope (fence 8).",
      "boundary": "SPEC fence 8 — touch-scoped waves; fix owner is @beep/semantic-web, not this package."
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/epistemic/use-cases/src/ClaimGate/ClaimGate.ports.ts",
    "line": 42,
    "symbol": "ClaimGateShape",
    "smell": "Detector hit: `interface ClaimGateShape` service-contract shape sitting apart from any schema — superficially a colocation candidate.",
    "proposedTarget": "None — leave as-is.",
    "confidence": 0.15,
    "mechanization": "judgment",
    "roiRank": 8,
    "exception": {
      "reason": "Service-shape interface consumed by `Context.Service<ClaimGate, ClaimGateShape>()`; service contracts stay interfaces per fence 1.",
      "boundary": "SPEC fence 1 — service-contract carve-out."
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/epistemic/use-cases/src/ClaimLifecycle/ClaimLifecycle.service.ts",
    "line": 39,
    "symbol": "ClaimTransitionShape",
    "smell": "Detector hit: `interface ClaimTransitionShape` service-contract shape sitting apart from any schema.",
    "proposedTarget": "None — leave as-is.",
    "confidence": 0.15,
    "mechanization": "judgment",
    "roiRank": 9,
    "exception": {
      "reason": "Service-shape interface consumed by `Context.Service<ClaimTransition, ClaimTransitionShape>()`; service contracts stay interfaces per fence 1.",
      "boundary": "SPEC fence 1 — service-contract carve-out."
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/epistemic/use-cases/src/ClaimLifecycle/ClaimLifecycle.service.ts",
    "line": 114,
    "symbol": "makeClaimTransition",
    "smell": "Detector scan hit: exported factory with an internal 2-arg `advance` callback that branches on `gateResult.verdict`/`claim.lifecycle` via nested ternaries rather than `Match`.",
    "proposedTarget": "None proposed under S4 (ternary-vs-Match style is S3's discrimination/exhaustiveness scope, not S4 colocation/pipe-ability); no dual/flow opportunity here since `advance` is consumed only as a service method, never piped.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 10,
    "exception": {
      "reason": "The `advance` method is a 2-arg Effect-returning service method invoked directly (`transition.advance(claim, gateResult)`), never piped or composed — it is not a public pipeable helper in the dual/flow sense. Ternary-chain-vs-Match.tagsExhaustive is the S3 rule card's scope, out of S4's reuse-only card.",
      "boundary": "Out of S4 scope — belongs to S3 (Discrimination & exhaustiveness) if actionable at all; not a colocation/pipe-ability smell."
    }
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/epistemic/use-cases/src/ClaimProjection/ClaimProjection.ts",
    "line": 65,
    "symbol": "projectClaims",
    "smell": "Detector scan hit: in-body call `S.decodeUnknownSync(ClaimProjectionView)({...})` builds the read-model from plain-number folds (not a module-level wall const, but a decode call embedded in the projection function).",
    "proposedTarget": "None — leave as-is; single in-body call site, not a duplicated top-of-file wall, and the decode is genuinely brand-checking freshly-computed plain numbers into `NonNegativeInt`, which `ClaimProjectionView.make` alone would not validate.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 11,
    "exception": {
      "reason": "This is a single inline decode call (not a free-floating top-of-file const), used exactly for its brand-checking purpose (folding plain `number` counts into `NonNegativeInt` fields) — `.make()` would skip that validation. Not the duplicate-guard-wall smell SFV4-static-api targets.",
      "boundary": "Legitimate single-use decode call, not a colocation violation."
    }
  }
]
