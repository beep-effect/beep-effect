[
  {
    "ruleId": "SFV4-static-api",
    "file": "infra/src/Storybook.ts",
    "line": 60,
    "symbol": "StorybookVercelAuthenticationDeploymentType",
    "smell": "LiteralKit schema behavior is split into a module-level decoder (`decodeStorybookVercelAuthenticationDeploymentType` at line 93) solely to feed the Pulumi config normalizer at lines 97-105.",
    "proposedTarget": "Attach a schema-owned result decoder with `SchemaUtils.withStatics((schema) => ({ decodeResult: S.decodeUnknownResult(schema) }))` after `$I.annoteSchema(...)`, then replace the free helper with `StorybookVercelAuthenticationDeploymentType.decodeResult(value)`. This preserves the current `Result.getOrThrowWith(..., schemaIssueToPulumiConfigError(...))` path; `withStatics` is verified at `packages/foundation/modeling/schema/src/SchemaUtils/withStatics.ts:103` and `S.decodeUnknownResult` at `.repos/effect-v4/packages/effect/src/Schema.ts:1564`.",
    "confidence": 0.85,
    "mechanization": "assisted",
    "roiRank": 1
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "infra/src/AIMetrics.ts",
    "line": 39,
    "symbol": "AiMetricsDeployTarget / AiMetricsTool decoders",
    "smell": "Pulumi config enum decoding is rebuilt in infra as `decodeAiMetricsDeployTarget` and `decodeAiMetricsTool`, even though the schemas are first-party LiteralKit values defined in `packages/tooling/library/ai-metrics/src/models.ts:26,57`.",
    "proposedTarget": "Move the result decoders onto the owning `@beep/repo-ai-metrics` schemas with `SchemaUtils.withStatics((schema) => ({ decodeResult: S.decodeUnknownResult(schema) }))`, then replace infra's helpers with `AiMetricsDeployTarget.decodeResult(value)` and `AiMetricsTool.decodeResult(value)`. This keeps infra's custom `pulumi.RunError` mapping while colocating the codec behavior with the data.",
    "confidence": 0.7,
    "mechanization": "assisted",
    "roiRank": 2
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "infra/src/AIMetrics.ts",
    "line": 233,
    "symbol": "AIMetricsPulumiConfigValues",
    "smell": "The public Pulumi config value schema has no colocated decoder, so tests rebuild `S.decodeUnknownEffect(AIMetricsPulumiConfigValues)` at `infra/test/AIMetrics.test.ts:106,114`.",
    "proposedTarget": "After the `.mapFields(Struct.map(S.optionalKey))` schema construction, attach `SchemaUtils.withStatics((schema) => ({ decodeEffect: S.decodeUnknownEffect(schema) }))`; tests and consumers can call `AIMetricsPulumiConfigValues.decodeEffect(input)`. `S.decodeUnknownEffect` is verified in Effect v4 at `.repos/effect-v4/packages/effect/src/Schema.ts:1325`.",
    "confidence": 0.75,
    "mechanization": "assisted",
    "roiRank": 3
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "infra/src/Storybook.ts",
    "line": 120,
    "symbol": "StorybookPulumiConfigValues",
    "smell": "The public optional Pulumi config shape has no schema-owned decoder; the package test reconstructs `S.decodeUnknownEffect(StorybookPulumiConfigValues)` at `infra/test/Storybook.test.ts:61`.",
    "proposedTarget": "Attach `decodeEffect` with `SchemaUtils.withStatics((schema) => ({ decodeEffect: S.decodeUnknownEffect(schema) }))` after the mapped optional-key schema is built, then replace test/local callers with `StorybookPulumiConfigValues.decodeEffect(input)`.",
    "confidence": 0.75,
    "mechanization": "assisted",
    "roiRank": 4
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "infra/src/OipWeb.ts",
    "line": 102,
    "symbol": "OipWebPulumiConfigValues",
    "smell": "The public OIP Pulumi config value schema leaves decoding at call sites; `infra/test/OipWeb.test.ts:101` rebuilds `S.decodeUnknownEffect(OipWebPulumiConfigValues)` directly.",
    "proposedTarget": "Attach `decodeEffect` with `SchemaUtils.withStatics((schema) => ({ decodeEffect: S.decodeUnknownEffect(schema) }))` after `.mapFields(Struct.map(S.optionalKey))`, mirroring the Storybook and AIMetrics config schema target.",
    "confidence": 0.75,
    "mechanization": "assisted",
    "roiRank": 5
  }
]
