[
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/capability/colors/src/Colors.ts",
    "line": 91,
    "symbol": "decodeProcessLike",
    "smell": "Free-floating decode helper `const decodeProcessLike = S.decodeUnknownOption(ProcessLike)` at module top instead of a colocated class static; the identical helper is re-instantiated in test/index.test.ts:100 (`const decode = S.decodeUnknownOption(ProcessLike)`), i.e. duplicate decode helpers for the same schema.",
    "proposedTarget": "Attach in-body `static readonly decodeOption = S.decodeUnknownOption(ProcessLike)` on the ProcessLike class (S.Class cannot take SchemaUtils.withCodecStatics via .pipe without losing constructor identity — in-body statics are the documented class-side pattern, see packages/foundation/modeling/schema/src/SchemaUtils/withCodecStatics.ts:10-12 and exemplars Md.model.ts:873 / Lexical.model.ts:1282). Use `ProcessLike.decodeOption(runtimeProcess)` at Colors.ts:93 and in test/index.test.ts:100; delete the free helper.",
    "confidence": 0.75,
    "mechanization": "assisted",
    "roiRank": 3.0
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/capability/colors/src/Colors.ts",
    "line": 198,
    "symbol": "supportsColor",
    "smell": "Pure color-support detection behavior over the ProcessLike schema class lives as a free exported function, together with three private predicate helpers over ProcessLike fields (hasNoColorFlag/hasNoColorEnv/hasForceColorEnv, Colors.ts:98-103) — behavior separated from its data.",
    "proposedTarget": "Colocate detection onto the data: in-body `static readonly supportsColor = (self: ProcessLike = runtimeProcessLike): boolean => ...` on ProcessLike, absorbing the three private predicates; keep the exported `supportsColor` as a thin alias to the static so module-level export parity with the browser entrypoint (Colors.browser.ts:76 `supportsColor = thunkFalse`, which has no ProcessLike class at all) is preserved. Public-form is unchanged if the alias stays; if the alias is dropped, the section 5.4 consumer sweep covers only this package's test and dtslint files (no external consumer imports supportsColor).",
    "confidence": 0.5,
    "mechanization": "judgment",
    "roiRank": 2.0
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/capability/colors/src/Colors.ts",
    "line": 297,
    "symbol": "makeFormatter",
    "smell": "Trivial wrapper lambda `() => stringIdentity` in `enabled ? formatter : () => stringIdentity` instead of the shared thunk helper — the repo-preferred direct-helper-ref form (the sibling entrypoint Colors.browser.ts:12 already imports thunkFalse from the same helper family).",
    "proposedTarget": "Replace with `thunk(stringIdentity)` using the generic `thunk` from @beep/utils (packages/foundation/modeling/utils/src/thunk.ts:31, `<A>(value: A): LazyArg<A>`); a zero-arg LazyArg<Formatter> is assignable to `typeof formatter`. Behavior-identical, single call site.",
    "confidence": 0.85,
    "mechanization": "assisted",
    "roiRank": 1.7
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/capability/colors/src/Colors.ts",
    "line": 96,
    "symbol": "stringIdentity",
    "smell": "The plain String-coercion fallback formatter is hand-rolled per entrypoint (`const stringIdentity: FormatterType = String` here; duplicate `const identity: FormatterType = String` at Colors.browser.ts:18) instead of living with the Formatter schema it implements — behavior separated from its data, duplicated across the Node and browser entrypoints.",
    "proposedTarget": "Colocate the fallback with the schema in src/internal/ColorsSchema.ts: either attach it via SchemaUtils.withStatics on the Formatter const (pipeable, packages/foundation/modeling/schema/src/SchemaUtils/withStatics.ts) e.g. `Formatter.plain`, or export a shared `plainFormatter: Formatter = String` from the internal module; both entrypoints then reference the single definition. Marginal value (two one-line consts) — remediation agent may decline as churn.",
    "confidence": 0.55,
    "mechanization": "judgment",
    "roiRank": 1.2
  },
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/foundation/capability/colors/src/internal/ColorsSchema.ts",
    "line": 29,
    "symbol": "FormatterInput",
    "smell": "Union const `S.Union([S.String, S.Finite]).pipe(S.UndefinedOr, ...)` without SchemaUtils.withCodecStatics — literal match for the S4 'branded/union consts missing withCodecStatics' smell.",
    "proposedTarget": "No action. If it ever gains codec/guard call sites, append `SchemaUtils.withCodecStatics` to the existing .pipe chain (bound `S.Top & S.ConstraintDecoder<unknown>` is satisfied).",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 0.5,
    "exception": {
      "reason": "Internal-only schema (not re-exported from Colors.ts, Colors.browser.ts, or the package barrel) with zero decode/guard/fromUnknown call sites anywhere in the package or repo; it exists solely as the input leg of the Fn Formatter signature. Attaching withCodecStatics would add speculative surface with no free-helper wall to delete — the crispen doctrine says stop at the first rung that removes noise, and there is no noise here.",
      "boundary": "packages/foundation/capability/colors/src/internal/ColorsSchema.ts (internal schema module of @beep/colors)"
    }
  }
]
