[
  {
    "ruleId": "SFV4-static-api",
    "file": "packages/drivers/pglite/src/Pglite.errors.ts",
    "line": 64,
    "symbol": "PgliteError.fromUnknown",
    "smell": "Detector hit: public two-argument static constructor is not `dual`.",
    "proposedTarget": "None - leave as-is.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 1,
    "exception": {
      "reason": "The second argument is optional (`cause?`). Arity-based `dual(2, ...)` would turn the valid `PgliteError.fromUnknown(operation)` call into data-last partial application instead of constructing an error.",
      "boundary": "Public error-constructor API with optional trailing args; dual dispatch is incompatible."
    }
  }
]
