[
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/law-practice/use-cases/src/IrToLaw/IrToLaw.service.ts",
    "line": 72,
    "symbol": "requiredExtraction",
    "smell": "`extraction.text.trim().length === 0` enforces the nonblank-after-trim invariant in use-case logic even though `GroundedExtraction.text` is schema-owned upstream as `S.NonEmptyString` (packages/foundation/capability/langextract/src/Extraction/index.ts:244).",
    "proposedTarget": "Move the invariant to the extraction schema: replace `ExtractionCandidate.text` / `GroundedExtraction.text` with a named nonblank text schema. Existing direct exemplar: `TrimmedNonEmptyText` at packages/foundation/modeling/schema/src/CommonTextSchemas.ts:36-43 uses `S.decodeTo` + `SchemaTransformation.transform`; Effect v4 `S.Trim` / `SchemaTransformation.trim()` are verified at .repos/effect-v4/packages/effect/src/Schema.ts:11544 and .repos/effect-v4/packages/effect/src/SchemaTransformation.ts:428. Design choice for the fixer: trim-on-decode (`TrimmedNonEmptyText`) vs reject whitespace-only while preserving raw text.",
    "confidence": 0.65,
    "mechanization": "assisted",
    "roiRank": 1
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/law-practice/use-cases/src/IrToLaw/IrToLaw.errors.ts",
    "line": 89,
    "symbol": "IrToLawExtractionError",
    "smell": "`alignmentStatus: S.optionalKey(S.String)` plus `readonly alignmentStatus?: string` at line 107 keeps absence as an optional property on a TaggedErrorClass instead of an Option field.",
    "proposedTarget": "Model the decoded field as Option: `alignmentStatus: S.OptionFromOptionalKey(S.String).pipe(SchemaUtils.withNoneDefault)` (v4 `S.OptionFromOptionalKey` verified at .repos/effect-v4/packages/effect/src/Schema.ts:8668; `withNoneDefault` exemplar at packages/foundation/modeling/schema/src/SchemaUtils/withConstructorDefaults.ts:33-51). Then update `fromReason` to build the optional field intentionally, e.g. with `O.fromUndefinedOr(options.alignmentStatus)` / `O.getSomesStruct`, while preserving the encoded optional-key wire shape.",
    "confidence": 0.7,
    "mechanization": "assisted",
    "roiRank": 2
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/law-practice/use-cases/src/IrToLaw/IrToLaw.service.ts",
    "line": 104,
    "symbol": "anchorOf",
    "smell": "`O.fromUndefinedOr(extraction.span)` lifts `GroundedExtraction.span?: Contract.Span` from the upstream @beep/langextract DTO into Option before producing a typed alignment failure.",
    "proposedTarget": "None in this package. If @beep/langextract is crispened later, `GroundedExtraction.span` can become `S.OptionFromOptionalKey(Contract.Span)` and this adapter can consume the Option directly.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 3,
    "exception": {
      "reason": "Cross-package DTO adapter immediately lifts undefined into Option and fails typed; this package does not own the source schema.",
      "boundary": "upstream @beep/langextract optional field boundary"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/law-practice/use-cases/src/IrToLaw/IrToLaw.service.ts",
    "line": 112,
    "symbol": "anchorOf",
    "smell": "`O.getOrElse(() => extraction.text)` defaults missing `matchedText` to sibling `text` while building the required legal TextAnchor quote.",
    "proposedTarget": "None - keep as-is unless a future whole-object GroundedExtraction codec can derive `matchedText` from `text` without changing alignment semantics.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 4,
    "exception": {
      "reason": "Sibling-derived fallback for an anchor quote; field-level constructor/default helpers cannot see sibling `text`, and the code stays Option-first at the adapter point.",
      "boundary": "source-grounding adapter with sibling-derived fallback"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/law-practice/use-cases/src/OfficeActionReview/OfficeActionReview.service.ts",
    "line": 118,
    "symbol": "sourceTextFrom",
    "smell": "`O.match(O.fromUndefinedOr(result.extraction.text), ...)` adapts `ExtractionResult.text?: string` from @beep/file-processing into a typed file-extraction failure.",
    "proposedTarget": "None in this package. A future @beep/file-processing S2 pass can model `ExtractionResult.text` as `S.OptionFromOptionalKey(S.String)` (current source: packages/foundation/capability/file-processing/src/Extraction/index.ts:136), after which this helper consumes the Option directly.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 5,
    "exception": {
      "reason": "Adapter boundary to an upstream optional extraction result; absence is immediately converted into a typed error, not defaulted in business logic.",
      "boundary": "upstream @beep/file-processing optional field boundary"
    }
  }
]
