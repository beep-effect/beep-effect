[
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/wink/src/WinkTools.service.ts",
    "line": 633,
    "symbol": "WinkNlpToolkitLive",
    "smell": "The toolkit handlers repeatedly default upstream tool parameters in bodies: `topN ?? 10` (ExtractKeywords), `groupName ?? \"custom-entities\"` / `mode ?? \"append\"`, `mode ?? \"bag\"`, `algorithm ?? \"soundex\"`, `minTokenLength ?? 2`, and include flags like `params.includeText ?? false`.",
    "proposedTarget": "Root cause is mostly upstream: move stable defaults onto the `@beep/nlp-processing` tool parameter schemas (e.g. `Tools/PhoneticMatch.ts:29-32`, `Tools/NGrams.ts:29-40`, `Tools/LearnCustomEntities.ts:50-53`, `Tools/QueryCorpus.ts:21-27`) using `SchemaUtils.withKeyDefaults`; then this driver consumes already-defaulted params. Keep dynamic defaults such as `topN ?? result.uniqueNGrams` in handler logic.",
    "confidence": 0.55,
    "mechanization": "judgment",
    "roiRank": 9,
    "exception": {
      "reason": "Stable defaults for these agent-facing tool parameters are owned by @beep/nlp-processing tool schemas; changing them in the driver would leave duplicated public contracts. Dynamic result-size defaults remain handler behavior.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/drivers/wink/src/WinkCorpus.service.ts",
    "line": 224,
    "symbol": "sanitizeLimit",
    "smell": "`sanitizeLimit` floors and clamps optional numeric limits with `Math.max(1, Math.min(fallback, Math.floor(limit)))`; callers feed optional `topN` / `topIdfTerms` values from schemas that currently accept undefined.",
    "proposedTarget": "Split the invariant: use a positive-int schema for the lower bound and integer normalization, but keep the dynamic upper-bound clamp against the current result size in service code. If clamp-on-decode is wanted for fixed bounds, use a schema transformation; Effect v4 verification: `.repos/effect-v4/packages/effect/src/SchemaTransformation.ts:334` defines `SchemaTransformation.transform`.",
    "confidence": 0.58,
    "mechanization": "judgment",
    "roiRank": 10,
    "exception": {
      "reason": "The remaining clamp depends on live result sizes such as scored documents, decoded IDF terms, and generated n-gram counts; only the static positive-integer lower-bound moved into schemas.",
      "boundary": "dynamic runtime limit clamp"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/drivers/wink/src/WinkTools.service.ts",
    "line": 58,
    "symbol": "normalizeTerm",
    "smell": "`normalizeTerm = flow(normalizedTokenText, Str.trim)` and related term-normalization uses (`Str.toLowerCase` in phonetic matching at line 814) look like generic string normalization.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 11,
    "exception": {
      "reason": "This is NLP algorithm behavior for term canonicalization, not normalization of a schema field. Moving it into a schema would hide a ranking/token-processing choice.",
      "boundary": "domain algorithm: token/term canonicalization"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/wink/src/WinkTools.service.ts",
    "line": 115,
    "symbol": "tokenBagOfWords",
    "smell": "`[term]: (bag[term] ?? 0) + 1` defaults a missing frequency counter to zero.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 12,
    "exception": {
      "reason": "Accumulator lookup default inside a bag-of-words algorithm; there is no schema field whose constructor/default semantics apply to a transient frequency map.",
      "boundary": "algorithmic counter initialization"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/drivers/wink/src/WinkTools.service.ts",
    "line": 220,
    "symbol": "resolveTokenIndex",
    "smell": "`Math.max(0, Math.min(A.length(tokens) - 1, Math.floor(span[0])))` clamps external entity spans to token bounds.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 13,
    "exception": {
      "reason": "Defensive bounds repair over `wink-nlp` entity span output; the upper bound depends on the current token array and cannot be a static field default.",
      "boundary": "3rd-party SDK span alignment"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/wink/src/WinkCorpus.service.ts",
    "line": 522,
    "symbol": "createCorpus",
    "smell": "`const corpusId = requestedId ?? (yield* makeGeneratedId)` defaults an absent corpus id by allocating a timestamp/counter id.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 14,
    "exception": {
      "reason": "The default is effectful identity allocation using a service-local counter and current time; moving it into a plain schema default would change lifecycle/state semantics.",
      "boundary": "effectful service identity generation"
    }
  },
  {
    "ruleId": "SFV4-null-return",
    "file": "packages/drivers/wink/src/WinkTokenization.service.ts",
    "line": 74,
    "symbol": "getSentenceSpan",
    "smell": "`getSentenceSpan` returns `readonly [number, number] | undefined` when the wink sentence span is malformed or absent.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 15,
    "exception": {
      "reason": "Private adapter around `wink-nlp` sentence output; the value is immediately lifted into `Option` in `resolveSentenceSpan` and combined with a fallback span derivation.",
      "boundary": "3rd-party SDK sentence-span boundary"
    }
  },
  {
    "ruleId": "SFV4-null-return",
    "file": "packages/drivers/wink/src/Wink.models.ts",
    "line": 128,
    "symbol": "CustomEntityExample.toWinkExample",
    "smell": "`toWinkExample` returns a wink-compatible object type whose `mark` property is optional (`readonly mark?: ... | undefined`).",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 16,
    "exception": {
      "reason": "This is the encoder edge from the repo's `Option` model to `wink-nlp.learnCustomEntities`, whose input shape uses an omitted optional property.",
      "boundary": "3rd-party SDK input shape"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/wink/src/WinkObservability.ts",
    "line": 42,
    "symbol": "errorMessage",
    "smell": "`stringProperty(error, \"message\").pipe(O.getOrElse(() => Inspectable.toStringUnknown(error)))` defaults a missing message field to a sanitized unknown-value rendering.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 17,
    "exception": {
      "reason": "Diagnostic fallback for unknown thrown values; not a domain model default, and keeping it explicit preserves the error-boundary sanitization behavior.",
      "boundary": "unknown error diagnostic boundary"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/wink/src/WinkBackend.service.ts",
    "line": 84,
    "symbol": "posTag",
    "smell": "`O.getOrElse(A.get(tags, index), () => \"UNKNOWN\")` and the lemma fallback at line 102 repair missing parallel `wink-nlp` output arrays.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 18,
    "exception": {
      "reason": "Adapter fallback for mismatched third-party token/tag/lemma arrays before building backend graph nodes; the fallback depends on array alignment, not a schema-field default.",
      "boundary": "3rd-party SDK output alignment"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/drivers/wink/src/WinkUtils.service.ts",
    "line": 287,
    "symbol": "WinkUtils.trim",
    "smell": "`trim` exposes `wink-nlp-utils` string trimming as a service operation.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 19,
    "exception": {
      "reason": "This is the explicit user-facing utility operation being provided by the driver, not incidental normalization before schema construction.",
      "boundary": "external utility operation wrapper"
    }
  }
]
