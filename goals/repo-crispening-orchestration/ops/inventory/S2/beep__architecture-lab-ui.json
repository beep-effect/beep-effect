[
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/architecture-lab/ui/src/aggregates/WorkItem/WorkItem.view-model.ts",
    "line": 125,
    "symbol": "makeStatusLabel",
    "smell": "`const makeStatusLabel: (status: DomainWorkItem.WorkItemStatus) => string = Str.toUpperCase;` looks like case normalization outside a codec and flows into `statusLabel`.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 2,
    "exception": {
      "reason": "Presentation label formatting in a UI read-model projection, not normalization of a domain or wire field. Effect v4 has `SchemaTransformation.toUpperCase()` and `SchemaGetter.toUpperCase()` (verified .repos/effect-v4/packages/effect/src/SchemaTransformation.ts:521-543 and .repos/effect-v4/packages/effect/src/SchemaGetter.ts:919-954), but this projection derives display text from an enum status.",
      "boundary": "UI presentation/read-model formatting"
    }
  }
]
