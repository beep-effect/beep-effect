[
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/file-processing/src/Artifact/index.ts",
    "line": 298,
    "symbol": "SourceArtifact",
    "smell": "Four `S.optionalKey` fields on the central domain class — bytes (line 300), extension (302), mediaType (305), text (309) — surface as `T | undefined` reads in domain code instead of Option. Downstream wrap-at-call-site tells: `operation.source.text ?? \"synthetic extraction\"` (src/test.ts:169), `mediaType: O.fromUndefinedOr(operation.source.mediaType)` (drivers/tika/src/Tika.service.ts:135), `operation.source.extension === \"pst\" ? 1 : 0` undefined-tolerant equality dispatch (drivers/libpff/src/Libpff.service.ts:169,171), and the conditional-spread dance `...(text === undefined ? {} : { text })` in the package's own test fixture (test/FileProcessing.test.ts:63).",
    "proposedTarget": "Per field: `S.OptionFromOptionalKey(X).pipe(SchemaUtils.withNoneDefault)` (effect-v4 Schema.ts:8668; @beep/schema SchemaUtils/withConstructorDefaults.ts:46). Wire/JSON contract unchanged (absence stays a missing key); domain side becomes `Option<T>`; make call sites may omit the field thanks to withNoneDefault. Public-form change with wide fan-out (drivers/tika, drivers/libpff, drivers/ffmpeg, tooling/cli Files+Corpus, law-practice) — fence 9 same-PR consumer sweep crosses family boundaries (fence 8), so remediation-wave sequencing must handle it; assisted, not codemod.",
    "confidence": 0.6,
    "mechanization": "assisted",
    "roiRank": 1.8,
    "exception": {
      "reason": "Changing this optionalKey field to Option changes the package public domain type and requires driver/tooling/law-practice consumer edits in the same sweep.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/file-processing/src/Operation/index.ts",
    "line": 219,
    "symbol": "ExtractFileOperation.maxMaterializedBytes",
    "smell": "`maxMaterializedBytes: S.optionalKey(S.Finite)` yields `number | undefined` on the domain side, forcing the exactOptionalPropertyTypes construction dance downstream: `...R.getSomes({ maxMaterializedBytes: O.fromUndefinedOr(operation.maxMaterializedBytes) })` (src/Service/index.ts:331-333) and `...(O.isNone(maxMaterializedBytes) ? {} : { maxMaterializedBytes: maxMaterializedBytes.value })` (tooling/cli Files.command.ts:450, where the CLI already holds an Option and must unwrap it).",
    "proposedTarget": "`S.OptionFromOptionalKey(S.Finite).pipe(SchemaUtils.withNoneDefault)` — wire stays a missing key; domain side becomes `Option<number>`; both spread dances delete (the CLI Option flows straight through). Shared fix with the ExportArchiveOperation (line 264) and ProcessFileOperation (line 310) findings; land as one change.",
    "confidence": 0.7,
    "mechanization": "assisted",
    "roiRank": 1.4,
    "exception": {
      "reason": "Changing this optionalKey field to Option changes the package public domain type and requires driver/tooling/law-practice consumer edits in the same sweep.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/file-processing/src/Operation/index.ts",
    "line": 264,
    "symbol": "ExportArchiveOperation.maxMaterializedBytes",
    "smell": "`maxMaterializedBytes: S.optionalKey(S.Finite)` — same nullish-instead-of-Option field as ExtractFileOperation line 219; its Service consumer re-wraps via `...R.getSomes({ maxMaterializedBytes: O.fromUndefinedOr(...) })` (src/Service/index.ts:302-304).",
    "proposedTarget": "Same as the line-219 finding: `S.OptionFromOptionalKey(S.Finite).pipe(SchemaUtils.withNoneDefault)`; one change collapses all three operation classes and both Service spread sites.",
    "confidence": 0.7,
    "mechanization": "assisted",
    "roiRank": 1.4,
    "exception": {
      "reason": "Changing this optionalKey field to Option changes the package public domain type and requires driver/tooling/law-practice consumer edits in the same sweep.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/file-processing/src/Operation/index.ts",
    "line": 310,
    "symbol": "ProcessFileOperation.maxMaterializedBytes",
    "smell": "`maxMaterializedBytes: S.optionalKey(S.Finite)` — third copy of the nullish-instead-of-Option operation field; ProcessFileOperation is the source the Service reads before re-wrapping into the other two operations (src/Service/index.ts:303,332).",
    "proposedTarget": "Same as the line-219 finding: `S.OptionFromOptionalKey(S.Finite).pipe(SchemaUtils.withNoneDefault)`; after the change `operation.maxMaterializedBytes` passes through as Option with no `O.fromUndefinedOr`.",
    "confidence": 0.7,
    "mechanization": "assisted",
    "roiRank": 1.4,
    "exception": {
      "reason": "Changing this optionalKey field to Option changes the package public domain type and requires driver/tooling/law-practice consumer edits in the same sweep.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/file-processing/src/Extraction/index.ts",
    "line": 129,
    "symbol": "ExtractionResult",
    "smell": "`text: S.optionalKey(S.String)` (line 136) and `textArtifact: S.optionalKey(TextArtifactReference)` (line 137) read as `T | undefined` in domain code; downstream consumers immediately Option-wrap: `O.match(O.fromUndefinedOr(result.extraction.text), ...)` (law-practice/use-cases OfficeActionReview.service.ts:118) and drivers/tika builds `text` from `O.fromUndefinedOr(record[tikaContentKey])` only to unwrap it back (Tika.tikaapp.ts:192).",
    "proposedTarget": "`S.OptionFromOptionalKey(S.String).pipe(SchemaUtils.withNoneDefault)` and `S.OptionFromOptionalKey(TextArtifactReference).pipe(SchemaUtils.withNoneDefault)` — JSONL wire unchanged, domain side Option; the law-practice and tika Option shims delete. Fence 9 consumer sweep spans drivers + law-practice + tooling.",
    "confidence": 0.65,
    "mechanization": "assisted",
    "roiRank": 1.3,
    "exception": {
      "reason": "Changing this optionalKey field to Option changes the package public domain type and requires driver/tooling/law-practice consumer edits in the same sweep.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/file-processing/src/Operation/index.ts",
    "line": 69,
    "symbol": "FileProcessingOperationError",
    "smell": "Five `S.optionalKey` context fields — artifactId (line 74), details (75), engine (76), format (77), operationId (79) — while the sibling error class in the same package, PathSafetyError (src/PathSafety/index.ts:96,99), already models optional context as `S.OptionFromOptionalKey`. In-package inconsistency; driver error builders feed these via `...R.getSomes({ details: O.map(O.fromUndefinedOr(...), ...) })` dances (drivers/tika Tika.tikaapp.ts:83, drivers/libpff Libpff.pffexport.ts:138).",
    "proposedTarget": "Align with PathSafetyError: `S.OptionFromOptionalKey(X).pipe(SchemaUtils.withNoneDefault)` per field; `fromReason`'s options bag then takes `Option` values (or keeps optional props and wraps once inside the static), and driver-side R.getSomes spreads collapse to direct Option passes.",
    "confidence": 0.7,
    "mechanization": "assisted",
    "roiRank": 1.3,
    "exception": {
      "reason": "Changing this optionalKey field to Option changes the package public domain type and requires driver/tooling/law-practice consumer edits in the same sweep.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/file-processing/src/Operation/index.ts",
    "line": 170,
    "symbol": "DetectionResult",
    "smell": "`confidence: S.optionalKey(S.Finite)` (line 172) and `mediaType: S.optionalKey(MimeType)` (line 175) are `T | undefined` domain reads; produced by every detection engine (drivers/tika, drivers/libpff, the synthetic test engine).",
    "proposedTarget": "`S.OptionFromOptionalKey(...).pipe(SchemaUtils.withNoneDefault)` for both fields; engine constructors that currently omit the keys keep omitting them (withNoneDefault), engines that set them pass `O.some`.",
    "confidence": 0.65,
    "mechanization": "assisted",
    "roiRank": 1.1,
    "exception": {
      "reason": "Changing this optionalKey field to Option changes the package public domain type and requires driver/tooling/law-practice consumer edits in the same sweep.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/file-processing/src/Artifact/index.ts",
    "line": 344,
    "symbol": "ArtifactReference",
    "smell": "`digest` (line 346), `mediaType` (348), and `sizeBytes` (350) are `S.optionalKey` fields reading as `T | undefined` in domain code; referenced from ArchiveExportResult.children, TextArtifactReference, and ChildArtifactRecord rows.",
    "proposedTarget": "`S.OptionFromOptionalKey(...).pipe(SchemaUtils.withNoneDefault)` per field; JSONL rows keep missing-key absence, constructors keep omission ergonomics.",
    "confidence": 0.65,
    "mechanization": "assisted",
    "roiRank": 1.1,
    "exception": {
      "reason": "Changing this optionalKey field to Option changes the package public domain type and requires driver/tooling/law-practice consumer edits in the same sweep.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/file-processing/src/Extraction/index.ts",
    "line": 426,
    "symbol": "SucceededSourceProcessingRecord",
    "smell": "The three SourceProcessingRecord members carry `engine: S.optionalKey(S.String)` (lines 432, 488, 542) and `textPath: S.optionalKey(PosixPath)` (line 438) — `T | undefined` domain reads on the JSONL row models (sources.jsonl).",
    "proposedTarget": "`S.OptionFromOptionalKey(...).pipe(SchemaUtils.withNoneDefault)` on all four field sites across SucceededSourceProcessingRecord, SkippedSourceProcessingRecord, and FailedSourceProcessingRecord; the JSONL wire (missing key) and the `S.toTaggedUnion(\"status\")` derivation are unchanged.",
    "confidence": 0.7,
    "mechanization": "assisted",
    "roiRank": 1.1,
    "exception": {
      "reason": "Changing this optionalKey field to Option changes the package public domain type and requires driver/tooling/law-practice consumer edits in the same sweep.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/file-processing/src/Extraction/index.ts",
    "line": 661,
    "symbol": "SkippedFileProcessingFailureRecord",
    "smell": "Both FileProcessingFailureRecord members carry `engine: S.optionalKey(S.String)` and `format: S.optionalKey(FileFormatFamily)` (lines 666-667 and 717-718) — `T | undefined` domain reads on the failures.jsonl row models.",
    "proposedTarget": "`S.OptionFromOptionalKey(...).pipe(SchemaUtils.withNoneDefault)` on all four field sites across SkippedFileProcessingFailureRecord and FailedFileProcessingFailureRecord; wire and tagged-union derivation unchanged.",
    "confidence": 0.7,
    "mechanization": "assisted",
    "roiRank": 1.1,
    "exception": {
      "reason": "Changing this optionalKey field to Option changes the package public domain type and requires driver/tooling/law-practice consumer edits in the same sweep.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/file-processing/src/Strategy/index.ts",
    "line": 400,
    "symbol": "FileProcessingEngineDescriptor.version",
    "smell": "`version: S.optionalKey(S.String)` — `string | undefined` domain read on the engine descriptor; smallest instance of the package-wide optionalKey-instead-of-Option family.",
    "proposedTarget": "`S.OptionFromOptionalKey(S.String).pipe(SchemaUtils.withNoneDefault)`; descriptor literals that set version pass `O.some(\"...\")`, ones that omit it stay unchanged.",
    "confidence": 0.7,
    "mechanization": "assisted",
    "roiRank": 0.8,
    "exception": {
      "reason": "Changing this optionalKey field to Option changes the package public domain type and requires driver/tooling/law-practice consumer edits in the same sweep.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/file-processing/src/Strategy/index.ts",
    "line": 425,
    "symbol": "classifyFormatFromExtension",
    "smell": "Exported domain classifier typed `(extension: string | undefined) => FileFormatFamily` — the `| undefined` leaks from SourceArtifact.extension's optionalKey shape into a public signature, and the Match pipeline's `Match.orElse(() => \"unknown\")` doubles as the absent-value default.",
    "proposedTarget": "If SourceArtifact.extension becomes `Option<ArtifactExtension>` (Artifact/index.ts:298 finding), retype as `(extension: O.Option<string>) => FileFormatFamily` via `O.match`/`O.getOrElse` feeding the existing Match, or model the mapping as a decode-side transformation schema (`S.decodeTo(FileFormatFamily, SchemaTransformation.transform(...))`) so classification travels with the schema. Many-to-one mapping (htm/html, txt/text, 8 image extensions) rules out MappedLiteralKit's reversible code map; design call, hence judgment.",
    "confidence": 0.55,
    "mechanization": "judgment",
    "roiRank": 0.6,
    "exception": {
      "reason": "The undefined input comes from the deferred SourceArtifact.extension Option migration; the public alias keeps the current absent-extension fallback until that cross-family migration lands.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/file-processing/src/Service/index.ts",
    "line": 138,
    "symbol": "selectEngine",
    "smell": "Module-private selector takes optional parameters `capability?: FileProcessingCapability` and `format?: FileFormatFamily` and branches on `capability === undefined || ...` / `format === undefined || ...` (lines 149-150) — undefined-checks standing in for Option in domain code.",
    "proposedTarget": "Take `O.Option<FileProcessingCapability>` / `O.Option<FileFormatFamily>` parameters and fold the guards with `O.match`/`O.forall` (predicate holds on None), or model the selection criteria as a small criteria struct. Internal-only, tiny blast radius, and plain optional-parameter ergonomics are defensible — low confidence, judgment.",
    "confidence": 0.5,
    "mechanization": "judgment",
    "roiRank": 0.4,
    "exception": {
      "reason": "Private selector optional parameters are filter ergonomics, not a wire/domain model; converting them to Option adds call-site ceremony without moving a schema-owned invariant.",
      "boundary": "private service helper; manual low-ROI Option carve-out"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/foundation/capability/file-processing/src/PathSafety/index.ts",
    "line": 173,
    "symbol": "normalizeForComparison",
    "smell": "String normalization in bodies — separator collapsing `Str.replaceAll(\"\\\\\", \"/\")` + trailing-slash strip ternary (lines 173-175), empty-segment filtering (line 180), `..`-segment scan (line 210), and root-prefix ternary (line 218) — audited as normalization-outside-codec candidates.",
    "proposedTarget": "None: do not absorb into a schema transform. This is the path-traversal trust-boundary guard; fence 4 requires escaping/sanitization/traversal guards to stay explicit and testable, and the containment comparison is deliberately pure and lossless.",
    "confidence": 0.9,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "PathSafety is a fail-closed path-traversal guard; its normalization exists to make the containment comparison sound (symlink/`..`/separator attacks), not to shape domain data. Moving it into a schema transformation would weaken an explicit, property-testable trust boundary.",
      "boundary": "trust boundary: fence 4 (no trust-boundary weakening) — path-traversal sanitization guard"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/file-processing/src/test.ts",
    "line": 169,
    "symbol": "TestFileProcessingEngine.extract",
    "smell": "`const text = operation.source.text ?? \"synthetic extraction\"` — a `??` default fallback in a body, inside the synthetic test-engine fixture shipped at the `/test` subpath.",
    "proposedTarget": "None standalone: the default is deliberate fixture behavior (the synthetic engine fabricates text when the source has none), not a schema invariant — SourceArtifact.text is legitimately absent for byte-only sources. If SourceArtifact.text becomes Option (Artifact/index.ts:298 finding), this becomes `O.getOrElse(() => \"synthetic extraction\")` mechanically.",
    "confidence": 0.85,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "Synthetic test-engine fixture default: the fabricated fallback text is the fixture's contract for deterministic proofs, not a leaked domain default; the field's absence is a valid domain state.",
      "boundary": "test fixture: `@beep/file-processing/test` synthetic engine module"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/file-processing/test/FileProcessing.test.ts",
    "line": 62,
    "symbol": "makeSource",
    "smell": "Test fixture helper: `sizeBytes: NonNegativeInt.make(text?.length ?? 11)` (line 62) and the conditional spread `...(text === undefined ? {} : { text })` (line 63) — `??` default plus undefined-dance in a vitest fixture builder.",
    "proposedTarget": "None standalone: test-fixture defaults are a listed carve-out. Note the line-63 conditional spread is forced by SourceArtifact.text's `S.optionalKey` shape and would collapse to `text: O.fromUndefinedOr(text)` (or omission via withNoneDefault) under the Artifact/index.ts:298 finding — corroborating evidence, not an independent action.",
    "confidence": 0.85,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "Fixture-builder default sizing inside a test file; the `?? 11` picks a deterministic fixture byte size and encodes no domain invariant.",
      "boundary": "test fixture: vitest test helper in test/FileProcessing.test.ts"
    }
  }
]
