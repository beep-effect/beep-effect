[
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/foundation/primitive/types/src/TString.types.ts",
    "line": 30,
    "symbol": "NonEmpty",
    "smell": "`export type NonEmpty<T extends string = string> = T extends \"\" ? never : T` - empty-string check detector hit in a source package.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.1,
    "mechanization": "judgment",
    "roiRank": 1,
    "exception": {
      "reason": "`@beep/types` is a type-level utility package; this conditional type is the package's compile-time vocabulary for rejecting the empty string, not runtime business logic or a schema-field normalization leak.",
      "boundary": "type-level utility package (compile-time conditional type)"
    }
  }
]
