[
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/shared/domain/src/entity/EntityId.ts",
    "line": 434,
    "symbol": "buildDefinition",
    "smell": "`const overrides = Options.make(input ?? {});` and the following `??` fallbacks derive entity-id metadata defaults from `slice`, `name`, and `entityType`.",
    "proposedTarget": "None as a standalone default move; this is handled only if the `Options` schema finding at line 105 is remediated together with a same-PR consumer/type sweep.",
    "confidence": 0.35,
    "mechanization": "judgment",
    "roiRank": 4,
    "exception": {
      "reason": "The defaults are sibling/parameter-derived (`slice`, `name`, and derived `entityType`), so field-level schema defaults cannot represent them directly. The actionable part is the `Options` absence modeling at line 105, not these derived fallback expressions in isolation.",
      "boundary": "parameter-derived identity metadata factory"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/shared/domain/test/Organization.test.ts",
    "line": 34,
    "symbol": "Organization decode fixture",
    "smell": "`parentOrgId: null` appears in the organization decode fixture.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 5,
    "exception": {
      "reason": "Fixture proves the persisted/wire nullable organization parent id decodes through `M.FieldOption` to domain Option.",
      "boundary": "SQL/wire decode fixture"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/shared/domain/test/EntityKernel.test.ts",
    "line": 72,
    "symbol": "Document optionalNote fixture",
    "smell": "`optionalNote: null` appears in the BaseEntity kernel test fixture.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 6,
    "exception": {
      "reason": "Fixture exercises a nullable persisted field (`S.OptionFromNullOr`) and verifies it decodes to the domain Option shape.",
      "boundary": "SQL/wire decode fixture"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/shared/domain/src/values/LocalDate/LocalDate.behavior.ts",
    "line": 141,
    "symbol": "toCalendarParts",
    "smell": "`globalThis.Number.parseInt(...)` appears in the ISO date parser used by `LocalDateFromString`.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 7,
    "exception": {
      "reason": "The parsing is already inside the implementation of a schema transformation: `LocalDateFromString` wires `decodeLocalDateFromString` through `SchemaGetter.transformOrFail` at lines 703-706. The actionable duplication is the public `fromString` helper at line 216.",
      "boundary": "schema transformation implementation"
    }
  }
]
