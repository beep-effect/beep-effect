[
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/tooling/policy-pack/repo-configs/src/next/SharedNextConfig.model.ts",
    "line": 415,
    "symbol": "makeBaseConfig",
    "smell": "`const next = withDefault(options.next, {})`, plus `experimental`, `turbopack`, and `typescript` object fallbacks at lines 416-418, are detector hits for body-level object defaults.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 8,
    "exception": {
      "reason": "These defaults are adapter merge scaffolding for the external Next.js config object; nested absence belongs to Next's public optional config contract, while the shared preset overlays repo defaults afterward.",
      "boundary": "3rd-party Next.js config merge adapter"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/tooling/policy-pack/repo-configs/src/next/NextConfig.model.ts",
    "line": 193,
    "symbol": "NextConfig",
    "smell": "`S.NullOr` / `S.UndefinedOr` appear on fields such as `i18n`, `webpack`, `env`, `cacheHandler`, and `reactStrictMode`, which are detector hits for nullish schema surfaces.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 9,
    "exception": {
      "reason": "`NextConfig` explicitly models the public `next` package configuration declaration, including its null/undefined wire shapes; replacing these with domain `Option` would change the external adapter contract.",
      "boundary": "3rd-party Next.js public config schema"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/tooling/policy-pack/repo-configs/src/next/models/ImageConfig.schema.ts",
    "line": 383,
    "symbol": "ImageConfig.default",
    "smell": "`maximumDiskCacheSize: undefined` and `localPatterns: undefined` are detector hits inside the default image config static.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 10,
    "exception": {
      "reason": "The static mirrors Next.js image-config semantics: `undefined` means auto-detect maximum disk cache size and allow all local image patterns. This is external config shape preservation, not repo-domain absence modeling.",
      "boundary": "3rd-party Next.js image config default contract"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/tooling/policy-pack/repo-configs/src/internal/eslint/EffectLawsAllowlistSnapshotCodegen.ts",
    "line": 73,
    "symbol": "renderAllowlistSnapshotModule",
    "smell": "`Effect.map(O.getOrElse(thunkEmptyStr))` is a getOrElse-as-default detector hit after `SchemaGetter.stringifyJson(...).run(O.some(encodedSnapshot), {})`.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 11,
    "exception": {
      "reason": "The Option is produced by the SchemaGetter API while rendering a generated snapshot module; the input is always `O.some(encodedSnapshot)`, so this is a terminal render adapter fallback rather than a schema-field default.",
      "boundary": "schema getter / generated snapshot rendering adapter"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/tooling/policy-pack/repo-configs/src/internal/eslint/EffectLawsAllowlistSchemas.ts",
    "line": 142,
    "symbol": "formatSchemaDiagnostics",
    "smell": "`O.getOrElse(() => '<root>')` defaults a missing Standard Schema issue path while formatting diagnostics.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 12,
    "exception": {
      "reason": "This is presentation-only diagnostic labeling for a missing schema-issue path, not a domain data default.",
      "boundary": "diagnostic formatting"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/tooling/policy-pack/repo-configs/src/eslint/RequireCategoryTagRule.ts",
    "line": 74,
    "symbol": "getNodeName",
    "smell": "`O.getOrElse(() => 'exported symbol')` defaults an absent ESTree node name for ESLint reporting.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 13,
    "exception": {
      "reason": "Fallback text is an ESLint report label for anonymous/undecodable syntax, not a schema-modeled domain value.",
      "boundary": "ESLint diagnostic reporting"
    }
  }
]
