[
  {
    "ruleId": "SFV4-defaults",
    "file": "infra/src/Storybook.ts",
    "line": 120,
    "symbol": "StorybookPulumiConfigValues",
    "smell": "`StorybookPulumiConfigValues` maps every raw config field through `S.optionalKey` and the loader fills the object from `pulumi.Config.get(...)`, so the detector sees a large optional-key surface.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 6,
    "exception": {
      "reason": "This schema intentionally represents raw Pulumi config before deployment defaults are applied; `pulumi.Config.get` returns `string | undefined`, and the resolved `StorybookVercelProjectConfig` carries the repo defaults.",
      "boundary": "Pulumi config loader adapter"
    }
  },
  {
    "ruleId": "SFV4-null-return",
    "file": "infra/src/Storybook.ts",
    "line": 97,
    "symbol": "storybookVercelAuthenticationDeploymentTypeFromPulumiConfig",
    "smell": "Private parser returns `StorybookVercelAuthenticationDeploymentType | undefined`, with absent raw Pulumi config represented by `undefined`.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 7,
    "exception": {
      "reason": "The helper is a narrow Pulumi string parser; `undefined` is only used to omit the downstream constructor field so the resolved schema default (`defaultVercelAuthenticationDeploymentType`) applies.",
      "boundary": "Pulumi config parser adapter"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "infra/src/OipWeb.ts",
    "line": 102,
    "symbol": "OipWebPulumiConfigValues",
    "smell": "`OipWebPulumiConfigValues` maps a full raw Pulumi config struct through `S.optionalKey`, mirroring many `pulumi.Config.get*` values that may be absent.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 8,
    "exception": {
      "reason": "This is the raw Pulumi config boundary before the package resolves defaults into `OipWebStackArgs`; provider configuration absence is intentionally still `undefined` at this layer.",
      "boundary": "Pulumi config loader adapter"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "infra/src/OipWeb.ts",
    "line": 165,
    "symbol": "OipPulumiStateBackendConfig.make",
    "smell": "`const normalizedInput = input ?? {}` and `normalizedInput.lockTableName ?? `${normalizedInput.bucketName ?? defaultPulumiStateBucketName}-locks`` are detector hits for body-level defaults inside a schema class.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.35,
    "mechanization": "judgment",
    "roiRank": 9,
    "exception": {
      "reason": "The lock-table name is sibling-derived from `bucketName`; simple field defaults cannot see sibling values, and the derivation is colocated in the schema class's canonical constructor.",
      "boundary": "sibling-derived default in schema smart constructor"
    }
  },
  {
    "ruleId": "SFV4-null-return",
    "file": "infra/src/OipWeb.ts",
    "line": 597,
    "symbol": "makeRuntimeEnvironmentVariable",
    "smell": "`makeRuntimeEnvironmentVariable` returns `undefined` when an optional secret or integration value is absent, and otherwise constructs a `vercel.ProjectEnvironmentVariable` resource.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 10,
    "exception": {
      "reason": "This is a Pulumi provider resource factory; absence means no provider resource should be registered. Any Option modeling should terminate before this third-party resource-construction boundary.",
      "boundary": "Pulumi/Vercel optional resource creation"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "infra/src/OipWeb.ts",
    "line": 609,
    "symbol": "makeRuntimeEnvironmentVariable",
    "smell": "`pipe(key, Str.toLowerCase, Str.replaceAll(\"_\", \"-\"))` normalizes environment-variable keys into Pulumi resource names.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 11,
    "exception": {
      "reason": "The case and separator conversion is provider resource-name derivation from a constant environment-variable key, not normalization of schema-modeled input data.",
      "boundary": "Pulumi resource naming"
    }
  },
  {
    "ruleId": "SFV4-null-return",
    "file": "infra/src/OipWeb.ts",
    "line": 621,
    "symbol": "makeDnsRecord",
    "smell": "`makeDnsRecord` returns `undefined` when no Cloudflare zone id is configured, and callers filter out `undefined` provider resources before wiring dependencies.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 12,
    "exception": {
      "reason": "This is side-effecting Pulumi resource construction gated by provider configuration; a missing zone means no Cloudflare resource should be created.",
      "boundary": "Pulumi/Cloudflare optional resource creation"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "infra/src/OipWeb.ts",
    "line": 701,
    "symbol": "OipWebStack.stateLockTableName",
    "smell": "`stateLockTableName` is exposed as `pulumi.Output<string | undefined>` and assigned from `pulumi.output(lockTable?.name)` after optional lock-table creation.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 13,
    "exception": {
      "reason": "The optional output mirrors a conditionally-created Pulumi resource; this is provider output shape preservation rather than repo-domain absence modeling.",
      "boundary": "Pulumi conditional resource output"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "infra/src/AIMetrics.ts",
    "line": 203,
    "symbol": "AIMetricsPulumiConfigValues",
    "smell": "`AIMetricsPulumiConfigValues` models every raw `aiMetrics:*` Pulumi setting as an optional key before the stack args resolver applies target-aware defaults.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 14,
    "exception": {
      "reason": "The class documents raw Pulumi config values and is populated by `pulumi.Config.get*`, whose absence contract is `undefined`; resolved domain defaults happen after this boundary.",
      "boundary": "Pulumi config loader adapter"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "infra/src/AIMetrics.ts",
    "line": 394,
    "symbol": "makeAIMetricsStackArgsFromConfigValues",
    "smell": "`publicBaseUrl ?? (resolvedTarget === dankserver ? remote-derived URL : undefined)` is a target-aware fallback in the Pulumi config resolver.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 15,
    "exception": {
      "reason": "The default is derived from sibling remote deployment settings (`tailnetFqdn` and `phoenixTailnetHttpsPort`) after Pulumi config has been loaded. Simple field defaults cannot express this without introducing a separate resolved-config transformation.",
      "boundary": "target-aware Pulumi config resolution"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "infra/src/AIMetrics.ts",
    "line": 68,
    "symbol": "shellQuote",
    "smell": "`shellQuote` uses `Str.replace(/'/gu, ...)`, which is a normalization detector hit over command strings.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.1,
    "mechanization": "judgment",
    "roiRank": 16,
    "exception": {
      "reason": "This is explicit shell escaping at a trust boundary. SPEC fence 4 says escaping/sanitization guards stay explicit and must not be absorbed into schema transforms.",
      "boundary": "trust-boundary shell escaping"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "infra/src/AIMetrics.ts",
    "line": 577,
    "symbol": "AIMetricsStack.remoteResources",
    "smell": "`remoteResources` is `undefined` for local installs, and the remote stdout outputs default to empty Pulumi outputs at lines 656-658.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 17,
    "exception": {
      "reason": "The optional value mirrors conditional Pulumi remote resources; the empty stdout outputs preserve the component output contract when those provider resources are intentionally absent.",
      "boundary": "Pulumi conditional resource output"
    }
  }
]
