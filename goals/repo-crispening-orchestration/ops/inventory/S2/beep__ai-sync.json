[
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/tooling/library/ai-sync/src/transforms.ts",
    "line": 65,
    "symbol": "codexMcpServersToClaudeMcpJson",
    "smell": "`config.mcp_servers ?? {}` defaults absent Codex TOML MCP server config to an empty record.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 8,
    "exception": {
      "reason": "This is an interop adapter over the generated Codex native config schema; Codex's TOML surface treats `mcp_servers` as an optional table, and the transform's empty-record fallback is the adapter contract.",
      "boundary": "external Codex TOML config adapter"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/tooling/library/ai-sync/src/transforms.ts",
    "line": 15,
    "symbol": "codexServerToMcpJsonServer",
    "smell": "The transform accepts a server object with `command`, `args`, `env`, `url`, `headers`, and `timeout_ms` typed as optional/undefined, then rebuilds output through `O.fromUndefinedOr` and `O.getSomesStruct`.",
    "proposedTarget": "None — keep the native config adapter shape here.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 9,
    "exception": {
      "reason": "The undefined fields mirror Codex/Claude native config wire schemas generated from external documentation; the adapter is exactly the boundary that omits absent keys in the other wire shape.",
      "boundary": "external Codex/Claude MCP config interop boundary"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/tooling/library/ai-sync/src/generator.ts",
    "line": 153,
    "symbol": "renderSchemasFile",
    "smell": "Generated schema templates emit many `S.optionalKey` fields for Codex, Claude MCP JSON, and Claude settings (`CodexMcpServer`, `CodexConfig`, `McpJsonServer`, `ClaudeSettings`).",
    "proposedTarget": "None — keep as-is for the native generated schema layer.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 10,
    "exception": {
      "reason": "These templates intentionally preserve third-party/native configuration wire optionality. Repo-owned wrapper/domain models can Option-ify absence, but the generated native schema layer should stay faithful to upstream config shapes.",
      "boundary": "generated native agent config schema boundary"
    }
  },
  {
    "ruleId": "SFV4-null-return",
    "file": "packages/tooling/library/ai-sync/scripts/ai-sync.ts",
    "line": 13,
    "symbol": "readFlag",
    "smell": "`readFlag` returns `string | undefined` and returns `undefined` when a CLI flag is absent.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 11,
    "exception": {
      "reason": "This is a tiny executable CLI boundary over `process.argv`; the following command code immediately supplies CLI defaults, and changing it to `Option` would not move a domain invariant into a schema.",
      "boundary": "CLI argument parsing boundary"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/tooling/library/ai-sync/scripts/ai-sync.ts",
    "line": 24,
    "symbol": "validateCommand",
    "smell": "`readFlag('--repo-root') ?? defaultRepoRoot()` and `readFlag('--config') ?? '.codex/config.toml'` are nullish CLI defaults.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 12,
    "exception": {
      "reason": "CLI flag defaults belong at the executable boundary; they are not persisted/domain model defaults and cannot be moved into the package schemas without adding a separate CLI option schema.",
      "boundary": "CLI executable flag boundary"
    }
  }
]
