[
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/architecture-lab/domain/src/aggregates/WorkItem/WorkItem.model.ts",
    "line": 128,
    "symbol": "create",
    "smell": "`priority: O.some(O.getOrElse(input.priority, () => defaultWorkPriority))` applies the new-WorkItem priority default in the aggregate factory body even though `CreateWorkItemInput.priority` is already schema-modeled.",
    "proposedTarget": "If omitted creation priority semantically means normal, move that default onto `CreateWorkItemInput.priority` with `S.withConstructorDefault(Effect.succeed(O.some(defaultWorkPriority)))` or a small SchemaUtils default helper, then have `create` pass `priority: input.priority`. Verified v4 API: .repos/effect-v4/packages/effect/src/Schema.ts:5646-5652; local default value: packages/architecture-lab/domain/src/values/WorkPriority/WorkPriority.behavior.ts:33. Needs a design check because current docs assert constructor-time absence before `create` applies the default.",
    "confidence": 0.55,
    "mechanization": "judgment",
    "roiRank": 3,
    "exception": {
      "reason": "Keeping the normal-priority default in the aggregate factory preserves the public CreateWorkItemInput.make({ id, title }) constructor semantics, where omitted priority remains Option.none before create applies the aggregate default.",
      "boundary": "public CreateWorkItemInput constructor semantics"
    }
  }
]
