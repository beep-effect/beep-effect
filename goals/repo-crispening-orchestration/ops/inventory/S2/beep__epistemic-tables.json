[
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/epistemic/tables/test/EpistemicTables.test.ts",
    "line": 15,
    "symbol": "usageRecordInput",
    "smell": "`costUsdApproxMicros: null` and sibling nullable UsageRecord row fields are fixture-level nulls for persistence shape.",
    "proposedTarget": "None - keep as-is. SQL row absence must encode as null; the domain counterpart decodes through `S.OptionFromNullOr` at packages/epistemic/domain/src/entities/UsageRecord/UsageRecord.model.ts:65-74.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 1,
    "exception": {
      "reason": "Persistence-row fixture keeps SQL nullable columns as null while the domain model decodes them to Option.",
      "boundary": "SQL row absence fence plus table test fixture"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/epistemic/tables/test/EpistemicTables.test.ts",
    "line": 78,
    "symbol": "UsageRecord round-trip row",
    "smell": "`insert.costUsdApproxMicros ?? null` and sibling fallbacks normalize Drizzle `$inferInsert` optional nullable columns into the concrete select-row test shape.",
    "proposedTarget": "None - keep as-is. This is a round-trip proof of the Drizzle insert/select SQL boundary, not a domain default; the converter itself delegates to `S.encodeSync(UsageRecord)` / `S.decodeUnknownSync(UsageRecord)` at UsageRecord.converters.ts:92-93.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 2,
    "exception": {
      "reason": "Test bridges Drizzle's optional insert typing to the selected row contract where nullable columns are concrete nulls.",
      "boundary": "SQL row absence fence plus Drizzle test adapter"
    }
  }
]
