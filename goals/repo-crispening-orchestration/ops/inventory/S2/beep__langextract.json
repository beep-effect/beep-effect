[
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/langextract/src/Extraction/index.ts",
    "line": 236,
    "symbol": "GroundedExtraction",
    "smell": "Four `S.optionalKey` fields (attributes line 239, confidence 240, matchedText 242, span 243) model absence as exact-optional instead of Option, leaking undefined-plumbing everywhere downstream: makeGrounded's four-branch `if (x !== undefined) input.x = x` conditional-build block (Alignment/index.ts:56-67), the `extraction.span !== undefined` type-predicate filter in definedExtractions (Handoff/index.ts:52-57), `extraction.confidence !== undefined` (Handoff/index.ts:81), and — the wrap-at-call-site tell — cross-package consumers re-wrapping with `O.fromUndefinedOr(extraction.span)` / `O.fromUndefinedOr(extraction.matchedText)` (packages/law-practice/use-cases/src/IrToLaw/IrToLaw.service.ts:104,111).",
    "proposedTarget": "`S.OptionFromOptionalKey(...)` (effect-v4 Schema.ts:8668; encoded side stays optionalKey per the interface at Schema.ts:8653, so the wire contract is unchanged) piped through `SchemaUtils.withNoneDefault` (SchemaUtils/withConstructorDefaults.ts:46) to erase `O.none()` at each make. makeGrounded then collapses to a single `GroundedExtraction.make({...})` passing Options through; definedExtractions becomes an `A.filterMap` (effect-v4 Array.ts:3710) pairing each extraction with its span; the law-practice `O.fromUndefinedOr` shims delete. Judgment: public-form change embedded in an out-of-family port schema (`S.Array(GroundedExtraction)` at law-practice IrToLaw.ports.ts:96) — the §5.4 same-PR consumer sweep crosses the foundation-family fence-8 boundary, and the span type-predicate rework is a design change, so this needs a coordination decision, not a mechanical rewrite.",
    "confidence": 0.55,
    "mechanization": "judgment",
    "roiRank": 1.4,
    "exception": {
      "reason": "Option-ifying GroundedExtraction optional keys changes the exported Type used by @beep/law-practice IrToLaw ports and services; it must ship with that consumer sweep.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/foundation/capability/langextract/src/Extraction/index.ts",
    "line": 395,
    "symbol": "stripJsonFence",
    "smell": "Normalization in a body feeding a schema decode: `text.trim()` (line 396), regex code-fence stripping, and `fenced?.[1]?.trim() ?? trimmed` (line 398) pre-process untrusted model text immediately before `decodeModelOutputJson(stripJsonFence(text))` (line 423) — the fence-strip+trim invariant belongs in the decode pipeline, not a free helper. Not a fence-4 trust-boundary guard: it normalizes formatting, it does not escape or sanitize.",
    "proposedTarget": "Absorb into a transformation codec composed in front of `S.UnknownFromJsonString`: `S.String.pipe(S.decodeTo(S.String, SchemaTransformation.transform({...fence-strip + trim...})))` (S.decodeTo at effect-v4 Schema.ts:5408; SchemaTransformation.transform at SchemaTransformation.ts:334; built-in trim() transformation at SchemaTransformation.ts:428), then the existing ModelOutput decode. Assisted, not codemod: parseModelOutput maps the two decode stages to distinct error reasons (`model-output-parse-failed` line 425 vs `model-output-schema-invalid` line 434), so either keep two-stage decoding or discriminate the composed codec's issue — a small design choice.",
    "confidence": 0.7,
    "mechanization": "assisted",
    "roiRank": 1.2,
    "exception": {
      "reason": "Formatting normalization remains a pre-parse step to preserve separate parse-failed and schema-invalid errors; moving it into one codec is deferred to a parser redesign with equivalent error taxonomy.",
      "boundary": "manual-audit parser boundary, deferred codec redesign"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/langextract/src/Alignment/index.ts",
    "line": 242,
    "symbol": "alignCandidate",
    "smell": "`options?.fuzzyThreshold ?? DEFAULT_FUZZY_THRESHOLD` constructor-default fallback in the body, paired with `const DEFAULT_FUZZY_THRESHOLD = 0.82` (line 18) and the `fuzzyThreshold: S.optionalKey(UnitInterval)` field on LangExtractOptions (Extraction/index.ts:168) — the default lives in business logic instead of the options schema.",
    "proposedTarget": "Carry the default on the schema field: `fuzzyThreshold: UnitInterval.pipe(SchemaUtils.withKeyDefaults(0.82))` (SchemaUtils/withKeyDefaults.ts:50, make + missing-key decode) so a constructed/decoded LangExtractOptions always has a threshold; UnitInterval is a constrained schema (packages/foundation/modeling/schema/src/UnitInterval.ts:32) so the withConstantDefault explicit-type-arg gotcha may apply. The residual `options?.` optional-parameter case collapses with the companion LangExtractRequest.options finding (Extraction/index.ts:270) by defaulting the options field itself; both `??` sites in this file go in one options-schema change.",
    "confidence": 0.7,
    "mechanization": "assisted",
    "roiRank": 1.2,
    "exception": {
      "reason": "Moving the fuzzy threshold default into LangExtractOptions changes public defaulting and encoded snapshot behavior; defer to a coordinated options-default sweep with parity fixtures.",
      "boundary": "public-form defaulting, deferred schema-default sweep"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/langextract/src/Alignment/index.ts",
    "line": 284,
    "symbol": "alignCandidates",
    "smell": "`options?.maxExtractions ?? Math.min(candidates.length, DEFAULT_MAX_EXTRACTIONS)` default fallback in the body, paired with `const DEFAULT_MAX_EXTRACTIONS = 256` (line 32) and the `maxExtractions: S.optionalKey(NonNegativeInt)` field (Extraction/index.ts:169). The `Math.min(candidates.length, ...)` wrapper is redundant — `A.take` already clamps to array length — so a constant schema default of 256 is behavior-equivalent.",
    "proposedTarget": "Same options-schema refactor as the line-242 finding: `maxExtractions: NonNegativeInt.pipe(SchemaUtils.withKeyDefaults(256))` (SchemaUtils/withKeyDefaults.ts:50; NonNegativeInt is branded, so `withConstantDefault` would need the explicit type argument if chosen instead), then `A.take(candidates, options.maxExtractions)` with no `??` and no Math.min. Behavior-parity note for §5.3: verify take-clamping equivalence in the wave PR.",
    "confidence": 0.7,
    "mechanization": "assisted",
    "roiRank": 1.2,
    "exception": {
      "reason": "Moving the extraction cap default into LangExtractOptions changes public defaulting and encoded snapshot behavior; defer with the companion fuzzyThreshold default sweep.",
      "boundary": "public-form defaulting, deferred schema-default sweep"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/langextract/src/Extraction/index.ts",
    "line": 194,
    "symbol": "ExtractionCandidate",
    "smell": "`attributes: S.optionalKey(...)` (line 196) and `confidence: S.optionalKey(UnitInterval)` (line 203) model absence as exact-optional; the fallout is makeGrounded's `if (candidate.attributes !== undefined)` / `if (candidate.confidence !== undefined)` conditional-assign pair (Alignment/index.ts:56-61).",
    "proposedTarget": "`S.OptionFromOptionalKey(...)` (effect-v4 Schema.ts:8668) + `SchemaUtils.withNoneDefault` (SchemaUtils/withConstructorDefaults.ts:46). Wire contract unchanged: the decode boundary is model-emitted JSON where the keys stay optional, and existing make call sites omit both fields (in-package tests; law-practice OfficeActionReview.candidates.ts:29-32 `ExtractionCandidate.make({ label, text })`) so withNoneDefault keeps every constructor call byte-identical. makeGrounded then passes `candidate.attributes` / `candidate.confidence` straight through as Options. Couple with the GroundedExtraction finding so the two classes' field forms move together.",
    "confidence": 0.7,
    "mechanization": "assisted",
    "roiRank": 1.1,
    "exception": {
      "reason": "ExtractionCandidate optional-key Optionification needs to stay coupled to GroundedExtraction optional-form changes; the complete sweep crosses into @beep/law-practice consumers.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/langextract/src/Extraction/index.ts",
    "line": 270,
    "symbol": "LangExtractRequest",
    "smell": "`examples` (lines 273-281) and `options` (line 282) are `S.optionalKey`, producing renderExamples' `request.examples === undefined || request.examples.length === 0` double-check (Service/index.ts:107) and every `options?.field ?? default` site in Alignment — absent-and-empty are treated identically, so the schema should own the default.",
    "proposedTarget": "`examples: S.Array(ExtractionExample).pipe(...max-length check..., SchemaUtils.withEmptyArrayDefaults<ExtractionExample>())` (SchemaUtils/withKeyDefaults.ts:114, make + decode default `[]`) so the guard collapses to a single emptiness check; `options: LangExtractOptions.pipe(SchemaUtils.withKeyDefaults(LangExtractOptions.make({})))`-style defaulting so `request.options` is always present and the Alignment `??` sites die with the companion findings. §5.3 wire note: with a required Type side, encode emits `examples: []` where the key was previously omitted — LangExtractRequest is an in-process service input (no persisted encoded form found), but the wave PR must include the encoded-snapshot parity check.",
    "confidence": 0.65,
    "mechanization": "assisted",
    "roiRank": 1.1,
    "exception": {
      "reason": "Defaulting examples/options changes the exported request Type and encoded shape used by law-practice request constructors and server tests; defer to the consumer sweep.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/langextract/src/Target/index.ts",
    "line": 62,
    "symbol": "ExtractionTarget",
    "smell": "`attributes: S.NonEmptyString.pipe(S.Array, S.optionalKey)` (line 64) and `description: S.optionalKey(S.String)` (line 65) — plus `ExtractionExampleItem.attributes` (line 89) — model absence as exact-optional; fallout is renderTarget's `target.attributes === undefined || target.attributes.length === 0` empty-check and `target.description === undefined ? \"\" : ...` ternary (Service/index.ts:99-102), the `v === undefined ? d : v` tell.",
    "proposedTarget": "`S.OptionFromOptionalKey` (effect-v4 Schema.ts:8668) + `SchemaUtils.withNoneDefault` (withConstructorDefaults.ts:46); renderTarget becomes two `O.match` arms. Since absent and empty attributes render identically, consider `S.NonEmptyArray(S.NonEmptyString).pipe(S.OptionFromOptionalKey)` to make the empty-array state unrepresentable (decode-tightening — needs a §5.3 decision). Ripple: four cross-family `ExtractionTarget.make({ description: \"...\" })` sites pass raw strings (law-practice OfficeActionReview.service.ts:51-70) and would need `O.some(...)` wraps in the same PR per §5.4; ExtractionExampleItem instances also flow through raw `JSON.stringify` at Service/index.ts:112, which must switch to `S.encodeSync(ExtractionExample)` (effect-v4 Schema.ts:2154) in the same change or the few-shot prompt JSON would serialize Option internals.",
    "confidence": 0.6,
    "mechanization": "assisted",
    "roiRank": 1.1,
    "exception": {
      "reason": "Option-ifying target description/attributes changes the exported target Type constructed in @beep/law-practice with raw strings; it must ship with that sweep.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/foundation/capability/langextract/src/Alignment/index.ts",
    "line": 34,
    "symbol": "lower (lowerWithSourceOffsets / similarity)",
    "smell": "`const lower = Str.toLowerCase` applied inside alignment internals (lines 97, 154-155) — audited as a normalization-outside-codec candidate.",
    "proposedTarget": "None: do not absorb into a schema transform. The lowercasing is algorithmic case-folding for case-insensitive matching whose spans MUST index the original, un-normalized source text — `lowerWithSourceOffsets` (lines 83-107) exists precisely to map normalized offsets back to original offsets, and `similarity` folds case only to score candidate windows. A schema-level lowercase would destroy the span invariant and the stored field values are deliberately un-normalized.",
    "confidence": 0.9,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "Comparison-only derivation: the case-folded values feed offset lookups and similarity scores and are never stored or returned as field values; the schema-side invariant is that text/matchedText stay original-cased so spans remain valid.",
      "boundary": "deterministic alignment algorithm internals (derived comparison values, not schema-field normalization)"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/foundation/capability/langextract/src/Alignment/index.ts",
    "line": 178,
    "symbol": "findFuzzy",
    "smell": "`query.trim().split(/\\s+/u).filter(Boolean).length` — a `.trim()` in a body, audited as a normalization-outside-codec candidate.",
    "proposedTarget": "None: the trim+split computes a word count used solely to size the sliding fuzzy-match window; the un-trimmed query is still what gets matched and reported. Not a schema-field normalization — same comparison-only-derivation carve-out as the case-folding family at line 34.",
    "confidence": 0.9,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "Derived metric (word-window size) for the fuzzy algorithm; the trimmed value never flows into or out of a schema field.",
      "boundary": "deterministic alignment algorithm internals (derived comparison values, not schema-field normalization)"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/langextract/src/Alignment/index.ts",
    "line": 133,
    "symbol": "levenshtein / wordsWithOffsets",
    "smell": "`previous[j] ?? 0` / `current[j] ?? 0` fallbacks in the DP inner loop (lines 141-142, 145, 150) and the `RegExpExecArray | null` / `match !== null` loop in wordsWithOffsets (lines 163-165) — audited as `??`-default and nullish-union candidates.",
    "proposedTarget": "None: the `?? 0` sites are noUncheckedIndexedAccess artifacts on always-in-range DP-table indices inside a performance-critical O(n*m) loop (Option-wrapping the hot path would be a regression, and native-collection choices are fence 7 / effect-native-migration's seam), and `RegExp.exec` returning `null` is the native platform contract.",
    "confidence": 0.85,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "Index-access safety artifacts in a perf-critical algorithmic kernel plus the native RegExp.exec null contract; no schema invariant leaked.",
      "boundary": "3rd-party/native: RegExp.exec API + noUncheckedIndexedAccess array indexing in an internal DP loop"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/langextract/src/Handoff/index.ts",
    "line": 134,
    "symbol": "toAnnotatedDocument",
    "smell": "`text: extraction.matchedText ?? extraction.text` — audited as a schema-default candidate.",
    "proposedTarget": "None standalone: the fallback is a cross-field derivation (prefer the aligned matchedText, else the model-emitted text), not a constant default a schema can carry. After the GroundedExtraction Option-ification (Extraction/index.ts:236 finding) it becomes `O.getOrElse(extraction.matchedText, () => extraction.text)` (effect-v4 Option.ts:617) and is then idiomatic; do not flag independently.",
    "confidence": 0.8,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "Cross-field fallback whose default value is another field of the same record; no schema-absorbable constant exists. Resolves to O.getOrElse once the field is Option-typed.",
      "boundary": "in-domain derived mapping (mention text selection), not a boundary default"
    }
  }
]
