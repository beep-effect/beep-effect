[
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/anthropic/src/Anthropic.service.ts",
    "line": 49,
    "symbol": "normalizeOptions",
    "smell": "`normalizeOptions` materializes `maxTokens` and `model` with `??` from `AnthropicLanguageModelOptions`, whose fields are `S.optionalKey(...)` at `Anthropic.config.ts:205-206`.",
    "proposedTarget": "Move the main language-model defaults onto `AnthropicLanguageModelOptions`: `maxTokens: S.Finite.pipe(SchemaUtils.withKeyDefaults(ANTHROPIC_DEFAULT_MAX_TOKENS))` and `model: S.String.pipe(SchemaUtils.withKeyDefaults(ANTHROPIC_DEFAULT_MODEL))`. The repair plan can keep passing explicit repair defaults. Local exemplars: `packages/drivers/m365/src/M365.config.ts:185` for numeric defaults and `packages/drivers/uspto/src/Uspto.config.ts:47` for string defaults.",
    "confidence": 0.85,
    "mechanization": "assisted",
    "roiRank": 1
  }
]
