[
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/agents/client/src/Chat.atoms.ts",
    "line": 436,
    "symbol": "messageFromUnknownError",
    "smell": "`Str.trim(error.message)` plus the empty-string fallback at lines 436-437 normalizes a user-facing `ChatActionError.message` in the client body while the target error schema is `message: S.String` (`packages/agents/use-cases/src/processes/Chat/Chat.errors.ts:31-40`).",
    "proposedTarget": "Promote a `ChatActionErrorMessage` schema/static in `@beep/agents-use-cases` that trims and falls back to the canonical non-empty message before `ChatActionError.make`; then the client only extracts a raw candidate message from `unknown`. Use v4 `S.decodeTo` + `SchemaGetter.transform` (in-repo transformation exemplar: `packages/foundation/modeling/md/src/Md.model.ts:1295-1301`) targeting `S.NonEmptyString` (verified at `.repos/effect-v4/packages/effect/src/Schema.ts:8409-8420`); v4 `S.Trim`/trim transformation verified at `.repos/effect-v4/packages/effect/src/Schema.ts:11526-11546` and `SchemaTransformation.ts:428-430`.",
    "confidence": 0.62,
    "mechanization": "assisted",
    "roiRank": 2,
    "exception": {
      "reason": "The normalization belongs on the exported ChatActionError schema in @beep/agents-use-cases; moving it there would change a public use-cases package schema/static outside this package writer scope.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/agents/client/src/Chat.atoms.ts",
    "line": 723,
    "symbol": "runTurnAtom",
    "smell": "`A.isReadonlyArrayEmpty(blocks)` is an empty-array detector hit before recording first-block perceived latency.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 4,
    "exception": {
      "reason": "Runtime control flow over the in-memory streamed-block accumulator; the empty/non-empty distinction drives a first-block metric, not a schema/default invariant.",
      "boundary": "client streaming metric control flow"
    }
  }
]
