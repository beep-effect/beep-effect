[
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/foundation/primitive/data/src/internal/data/mime-types/index.ts",
    "line": 179,
    "symbol": "normalizeLookupExtension",
    "smell": "`Fn.flow((input: string) => extname(`x.${input}`), Str.toLowerCase, Str.substring(1))` extracts and lowercases a caller-provided path/extension in the MIME lookup body; the public `lookup` surface accepts bare extensions, dotted extensions, and paths, so this is boundary normalization rather than static data.",
    "proposedTarget": "Introduce a small lookup-input transformation schema near the MIME lookup utilities, e.g. `MimeLookupExtensionInput = S.String.pipe(S.decodeTo(S.String, SchemaTransformation.transform({ decode: normalize extension + lowercase, encode: identity })))`, then have `lookup` decode once before `lookupNormalizedExtension`. Verified v4 surface: `S.decodeTo` and `SchemaTransformation.transform`; in-repo exemplars: `packages/foundation/modeling/schema/src/CommonTextSchemas.ts:36-43` and `:81-88` for text normalization, and `packages/foundation/modeling/schema/src/internal/email.ts:36-39` for trim/lowercase in-schema.",
    "confidence": 0.68,
    "mechanization": "assisted",
    "roiRank": 1,
    "exception": {
      "reason": "Remediation audit: declined adding a private Effect Schema to the vendored MIME data package. `@beep/data` owns raw compatibility data and the public `mime-types.lookup`-style facade; executable MIME/FileExtension schemas already live downstream in `@beep/schema`. The package-local remediation kept the lookup shell byte-compatible and removed the trivial passthrough/empty-string helper smell without introducing an unexported schema that cannot carry a package-local S.toArbitrary parity surface.",
      "boundary": "primitive data-source package; MIME lookup compatibility normalization remains a vendored facade"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/primitive/data/src/internal/data/mime-types/index.ts",
    "line": 172,
    "symbol": "extname",
    "smell": "`O.getOrElse(thunkEmptyStr)` returns the empty string when no dot exists while implementing a local `extname` helper.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 2,
    "exception": {
      "reason": "This is a private edge-compatible replacement for Node `path.extname`; the empty-string fallback is path API semantics, not a schema-field default.",
      "boundary": "vendored path/mime utility semantics"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/primitive/data/src/internal/data/mime-types/index.ts",
    "line": 195,
    "symbol": "lookupNormalizedExtension",
    "smell": "`O.getOrElse(returnFalse)` converts an unmatched normalized extension into the `false` sentinel used by MIME lookup.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 3,
    "exception": {
      "reason": "The public package mirrors the vendored `mime-types.lookup` contract (`false | MimeType`), and this branch is the adapter boundary from Option lookup miss to that external sentinel.",
      "boundary": "vendored third-party API compatibility"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/primitive/data/src/internal/data/mime-types/index.ts",
    "line": 271,
    "symbol": "lookup",
    "smell": "`O.getOrElse(returnFalse)` converts an empty input path into the public `false` lookup result.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 4,
    "exception": {
      "reason": "Same vendored MIME lookup boundary as `lookupNormalizedExtension`: `false` is the stable public miss value, not a nullish/defaulted domain field.",
      "boundary": "vendored third-party API compatibility"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/primitive/data/src/internal/data/mime-types/index.ts",
    "line": 278,
    "symbol": "preference",
    "smell": "`ReadonlyArray<string | undefined>` keeps `undefined` as one source-preference bucket.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 5,
    "exception": {
      "reason": "This list is copied from the vendored mime-db preference algorithm where missing `source` has an ordered slot; it is not a decoded domain field or persistence row that should become `Option`.",
      "boundary": "vendored mime-db source-precedence compatibility"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/primitive/data/src/internal/data/mime-types/index.ts",
    "line": 284,
    "symbol": "preferenceOf",
    "smell": "`O.getOrElse(() => -1)` assigns an algorithmic rank to an unknown source.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 6,
    "exception": {
      "reason": "Lookup-miss ranking fallback inside the MIME preference algorithm; this is not an object construction default and does not affect wire/domain absence modeling.",
      "boundary": "algorithmic sort/ranking fallback"
    }
  },
  {
    "ruleId": "SFV4-null-return",
    "file": "packages/foundation/primitive/data/src/internal/data/mime-types/index.ts",
    "line": 316,
    "symbol": "setTypeMapping",
    "smell": "`Match.when(true, () => undefined)` uses `undefined` as the no-op branch for a private mutating helper.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 7,
    "exception": {
      "reason": "Private side-effect helper with an inferred void result; no exported domain API returns `undefined` to callers.",
      "boundary": "private mutation/no-op branch"
    }
  },
  {
    "ruleId": "SFV4-null-return",
    "file": "packages/foundation/primitive/data/src/internal/data/mime-types/index.ts",
    "line": 343,
    "symbol": "populateMaps",
    "smell": "`Match.when(true, () => undefined)` uses `undefined` as the already-initialized no-op branch.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 8,
    "exception": {
      "reason": "Private lazy-cache initialization guard with an inferred void result; the branch communicates no domain absence and is not part of an exported contract.",
      "boundary": "private cache-initialization no-op branch"
    }
  },
  {
    "ruleId": "SFV4-null-return",
    "file": "packages/foundation/primitive/data/src/internal/data/mime-types/index.ts",
    "line": 350,
    "symbol": "populateMaps",
    "smell": "`onEmpty: () => undefined` uses `undefined` as the empty-extension no-op branch while populating lookup maps.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 9,
    "exception": {
      "reason": "Private mutation loop over static MIME data; empty arrays mean there is no mapping to populate, not that a domain helper returned absence.",
      "boundary": "private static-data population no-op branch"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/foundation/primitive/data/src/internal/data/utils.ts",
    "line": 4,
    "symbol": "formalizeValues",
    "smell": "`A.map(values, Str.capitalize)` derives formal month/weekday labels from lowercase literal arrays.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 10,
    "exception": {
      "reason": "Static constant-data construction for calendar literal exports, not normalization of caller input flowing into or out of a schema field.",
      "boundary": "constant-data derivation"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/foundation/primitive/data/test/keyboard-shortcuts.test.ts",
    "line": 56,
    "symbol": "keeps each row inside the exported literal domains",
    "smell": "`expect(entry.label).toBe(Str.capitalize(entry.name))` repeats label normalization in a test body.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 11,
    "exception": {
      "reason": "Test invariant over curated fixture data; it is an audit assertion, not production normalization logic.",
      "boundary": "test/fixture assertion"
    }
  }
]
