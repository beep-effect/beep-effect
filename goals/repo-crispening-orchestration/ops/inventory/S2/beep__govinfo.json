[
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/govinfo/src/Govinfo.config.ts",
    "line": 121,
    "symbol": "GovinfoConfigInput",
    "smell": "`apiKey` and `apiUrl` are `S.optionalKey(...)`, then `resolveConfig` re-models absence with `O.fromNullishOr(input.apiKey)` and `input.apiUrl ?? GOVINFO_API_URL` at service lines 72-75.",
    "proposedTarget": "Move fixed config defaults and absence into the schema: `apiKey: S.OptionFromOptionalKey(S.String.pipe(S.RedactedFromValue)).pipe(SchemaUtils.withNoneDefault)` and a defaulted string URL input such as `apiUrl: S.String.pipe(SchemaUtils.withKeyDefaults(GOVINFO_API_URL))` before the existing `URLStr.make` boundary (or a named `GovinfoApiUrl` codec if the URL brand should decode in the input schema). In-repo exemplars: `packages/drivers/m365/src/M365.config.ts:182-200` for key defaults and `packages/foundation/modeling/schema/src/SchemaUtils/withConstructorDefaults.ts:46-51` for Option constructor defaults.",
    "confidence": 0.72,
    "mechanization": "assisted",
    "roiRank": 1
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/govinfo/src/Govinfo.errors.ts",
    "line": 72,
    "symbol": "GovinfoErrorOptions",
    "smell": "`cause` and `status` are `S.optionalKey(...)`, then `GovinfoError.of` re-wraps them with `O.fromUndefinedOr(...)` / `O.getSomesStruct(...)` at lines 117-120.",
    "proposedTarget": "Use Option fields on the options schema: `cause: S.OptionFromOptionalKey(S.Defect({ includeStack: true })).pipe(SchemaUtils.withNoneDefault)` and `status: S.OptionFromOptionalKey(S.Finite).pipe(SchemaUtils.withNoneDefault)`. The package already uses Option error fields in search-contract failures at `domain/contracts/Search/Search.contract.ts:113,142,173`.",
    "confidence": 0.82,
    "mechanization": "assisted",
    "roiRank": 2
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/govinfo/src/Govinfo.errors.ts",
    "line": 97,
    "symbol": "GovinfoError",
    "smell": "`GovinfoError` itself keeps `cause` and `status` as `S.optionalKey(...)`, so error instances expose absent diagnostics as `undefined` instead of Option.",
    "proposedTarget": "Mirror the options target on the error class: `cause: S.OptionFromOptionalKey(S.Defect({ includeStack: true })).pipe(SchemaUtils.withNoneDefault)` and `status: S.OptionFromOptionalKey(S.Finite).pipe(SchemaUtils.withNoneDefault)`, then constructors pass the Option values directly instead of sparse spreading.",
    "confidence": 0.82,
    "mechanization": "assisted",
    "roiRank": 3
  },
  {
    "ruleId": "SFV4-null-return",
    "file": "packages/drivers/govinfo/src/Govinfo.service.ts",
    "line": 83,
    "symbol": "readStatus",
    "smell": "`readStatus(cause): number | undefined` returns bare `undefined` on non-object or non-number status, and `mapClientError` branches on `status === undefined`.",
    "proposedTarget": "Return `O.Option<number>` from `readStatus`, using `O.fromUndefinedOr` / `O.filter(P.isNumber)`, then have `mapClientError` use `O.match` to choose the transport vs response-status error. This is a private helper and should be a small assisted edit.",
    "confidence": 0.72,
    "mechanization": "assisted",
    "roiRank": 4
  }
]
