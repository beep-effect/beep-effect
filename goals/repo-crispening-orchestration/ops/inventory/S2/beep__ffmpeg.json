[
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/ffmpeg/src/FFmpeg.service.ts",
    "line": 222,
    "symbol": "defaultConfig",
    "smell": "`input?.ffmpegPath ?? \"ffmpeg\"`, `input?.ffprobePath ?? \"ffprobe\"`, and `input?.forceKillAfterMillis ?? 2000` are fixed constructor defaults in service code while `FFmpegConfigInput` already owns those fields as optional schema keys (FFmpeg.models.ts:208-210).",
    "proposedTarget": "Move the defaults onto `FFmpegConfigInput`: `ffmpegPath: S.String.pipe(S.optionalKey, SchemaUtils.withKeyDefaults(\"ffmpeg\"))`, `ffprobePath: ...`, and `forceKillAfterMillis: PositiveMilliseconds.pipe(S.optionalKey, SchemaUtils.withKeyDefaults(2000))`; then collapse `defaultConfig` to schema construction/decoding. In-repo exemplars: `packages/drivers/phoenix/src/Phoenix.config.ts:52-53` and `packages/drivers/face-detection/src/FaceDetection.models.ts:268-270`.",
    "confidence": 0.85,
    "mechanization": "assisted",
    "roiRank": 1
  },
  {
    "ruleId": "SFV4-null-return",
    "file": "packages/drivers/ffmpeg/src/FFmpeg.service.ts",
    "line": 260,
    "symbol": "optionToOptional",
    "smell": "`optionToOptional` returns `A | undefined`, and `maybe` spreads omitted fields so parsed ffprobe/progress absence can be stuffed back into domain models with `S.optionalKey` fields (`VideoProbe` at FFmpeg.models.ts:286-290 and `FFmpegProgressEvent` at FFmpeg.models.ts:548/551).",
    "proposedTarget": "Model decoded/domain absence as `Option`: `VideoProbe.durationSeconds/fps/frameCount/height/width` and `FFmpegProgressEvent.outTimeSeconds/speed` should use `S.OptionFromOptionalKey(...).pipe(SchemaUtils.withNoneDefault)`. That lets `probeFromOutput` and `parseProgressEvent` pass Options directly and delete `optionToOptional`/`maybe`. In-repo exemplar: `packages/foundation/modeling/ontology/src/Ontology.models.ts:152-160`; v4 API verified at `.repos/effect-v4/packages/effect/src/Schema.ts:8648-8669`.",
    "confidence": 0.75,
    "mechanization": "assisted",
    "roiRank": 2
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/drivers/ffmpeg/src/FFmpeg.service.ts",
    "line": 437,
    "symbol": "parseProgressEvent",
    "smell": "`Math.min(100, Math.max(0, (frameCount.value / expected) * 100))` clamps progress into the percent range in the parser body, while `FFmpegProgressEvent.percent` is a bare `S.Finite` field (FFmpeg.models.ts:549).",
    "proposedTarget": "Carry the 0..100 invariant on the field: use the shared `Percentage` schema (`packages/foundation/modeling/schema/src/Percentage.ts:32-33`) or a local `FFmpegProgressPercent` schema, and put the clamp behind a decode-side `S.decodeTo(..., SchemaTransformation.transform(...))` if clamp-on-construction is required. Otherwise use a range check and keep only the raw progress calculation in the body.",
    "confidence": 0.65,
    "mechanization": "assisted",
    "roiRank": 3
  },
  {
    "ruleId": "SFV4-null-return",
    "file": "packages/drivers/ffmpeg/src/FFmpeg.errors.ts",
    "line": 43,
    "symbol": "causeFromUnknown",
    "smell": "`causeFromUnknown` returns `unknown | undefined` and `FFmpegError.fromUnknown` passes that undefined into the schema-backed `FFmpegError.cause` optional key at line 133.",
    "proposedTarget": "Model error cause absence as `Option`: `cause: S.OptionFromOptionalKey(S.Defect({ includeStack: true })).pipe(SchemaUtils.withNoneDefault)` on `FFmpegErrorFromUnknownOptions` and `FFmpegError`; make `causeFromUnknown` return `O.Option<unknown>` and construct via Option. In-repo exemplar: `packages/foundation/modeling/ontology/src/Ontology.models.ts:152-160`; nearby package precedent from the exemplar inventory is `ToolExport.ts` cause modeling.",
    "confidence": 0.7,
    "mechanization": "assisted",
    "roiRank": 4
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/ffmpeg/src/FFmpeg.service.ts",
    "line": 115,
    "symbol": "FFmpegShape.extractFrames",
    "smell": "`onEvent?: FFmpegEventSink | undefined` is a detector hit for optional/undefined service input.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 5,
    "exception": {
      "reason": "Service-contract/interface carve-out: `FFmpegShape` is the service port shape and `onEvent` is an optional callback, not schema-modeled domain data.",
      "boundary": "service contract / callback boundary"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/ffmpeg/src/FFmpeg.service.ts",
    "line": 308,
    "symbol": "expectedFrameCount",
    "smell": "`probe.durationSeconds ?? 0` is a detector hit for nullish fallback.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 6,
    "exception": {
      "reason": "Algorithmic missing-metadata fallback: when ffprobe has no duration, expected frame count intentionally fails closed to zero. This is not a schema-field default; the domain absence should still become Option in the separate `VideoProbe` finding.",
      "boundary": "derived computation over external ffprobe metadata"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/ffmpeg/src/FFmpeg.service.ts",
    "line": 432,
    "symbol": "parseProgressEvent",
    "smell": "`block.out_time_ms ?? block.out_time_us` is a detector hit for nullish fallback.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 7,
    "exception": {
      "reason": "External ffmpeg progress output uses either key; the fallback is an adapter over 3rd-party process text, not a default for a schema field.",
      "boundary": "3rd-party ffmpeg progress key alias"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/ffmpeg/src/FFmpeg.service.ts",
    "line": 506,
    "symbol": "collectProgressText",
    "smell": "`O.getOrElse(thunkEmptyStr)` defaults an absent trailing line while buffering streamed process output.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 8,
    "exception": {
      "reason": "Streaming parser state fallback: the empty string represents no partial trailing line after splitting the current chunk, not a domain default that belongs in a schema.",
      "boundary": "stream parser buffer management"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/ffmpeg/src/FFmpeg.service.ts",
    "line": 642,
    "symbol": "makeExtractContext",
    "smell": "`request.prefix` falls back to `${sourceStem}_frame` with `O.getOrElse`.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 9,
    "exception": {
      "reason": "Sibling/environment-derived default: the prefix depends on the resolved input video path and basename, so a field-level schema default cannot express it without changing the request contract.",
      "boundary": "derived filename default"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/drivers/ffmpeg/src/FFmpeg.service.ts",
    "line": 862,
    "symbol": "probeVideo",
    "smell": "`Str.trim(result.stderr)` and `Str.trim(result.stdout)` normalize process output before constructing an `FFmpegError`.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 10,
    "exception": {
      "reason": "External process diagnostic formatting: trimming ffprobe stderr/stdout removes transport noise for an error message and does not define a reusable domain field invariant.",
      "boundary": "external process diagnostics"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/drivers/ffmpeg/src/FFmpeg.service.ts",
    "line": 971,
    "symbol": "extractFrames",
    "smell": "`Str.trim(result.stderr)` and `Str.trim(result.stdout)` normalize process output before constructing an `FFmpegError`.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 11,
    "exception": {
      "reason": "Same carve-out as `probeVideo`: external ffmpeg diagnostics are presentation cleanup at the process boundary, not schema-owned normalization.",
      "boundary": "external process diagnostics"
    }
  }
]
