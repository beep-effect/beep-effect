[
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/ffmpeg/src/FFmpeg.service.ts",
    "line": 115,
    "symbol": "FFmpegShape.extractFrames",
    "smell": "`onEvent?: FFmpegEventSink | undefined` is a detector hit for optional/undefined service input.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 5,
    "exception": {
      "reason": "Service-contract/interface carve-out: `FFmpegShape` is the service port shape and `onEvent` is an optional callback, not schema-modeled domain data.",
      "boundary": "service contract / callback boundary"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/ffmpeg/src/FFmpeg.service.ts",
    "line": 308,
    "symbol": "expectedFrameCount",
    "smell": "`probe.durationSeconds ?? 0` is a detector hit for nullish fallback.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 6,
    "exception": {
      "reason": "Algorithmic missing-metadata fallback: when ffprobe has no duration, expected frame count intentionally fails closed to zero. This is not a schema-field default; the domain absence should still become Option in the separate `VideoProbe` finding.",
      "boundary": "derived computation over external ffprobe metadata"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/ffmpeg/src/FFmpeg.service.ts",
    "line": 432,
    "symbol": "parseProgressEvent",
    "smell": "`block.out_time_ms ?? block.out_time_us` is a detector hit for nullish fallback.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 7,
    "exception": {
      "reason": "External ffmpeg progress output uses either key; the fallback is an adapter over 3rd-party process text, not a default for a schema field.",
      "boundary": "3rd-party ffmpeg progress key alias"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/ffmpeg/src/FFmpeg.service.ts",
    "line": 506,
    "symbol": "collectProgressText",
    "smell": "`O.getOrElse(thunkEmptyStr)` defaults an absent trailing line while buffering streamed process output.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 8,
    "exception": {
      "reason": "Streaming parser state fallback: the empty string represents no partial trailing line after splitting the current chunk, not a domain default that belongs in a schema.",
      "boundary": "stream parser buffer management"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/ffmpeg/src/FFmpeg.service.ts",
    "line": 642,
    "symbol": "makeExtractContext",
    "smell": "`request.prefix` falls back to `${sourceStem}_frame` with `O.getOrElse`.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.3,
    "mechanization": "judgment",
    "roiRank": 9,
    "exception": {
      "reason": "Sibling/environment-derived default: the prefix depends on the resolved input video path and basename, so a field-level schema default cannot express it without changing the request contract.",
      "boundary": "derived filename default"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/drivers/ffmpeg/src/FFmpeg.service.ts",
    "line": 862,
    "symbol": "probeVideo",
    "smell": "`Str.trim(result.stderr)` and `Str.trim(result.stdout)` normalize process output before constructing an `FFmpegError`.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 10,
    "exception": {
      "reason": "External process diagnostic formatting: trimming ffprobe stderr/stdout removes transport noise for an error message and does not define a reusable domain field invariant.",
      "boundary": "external process diagnostics"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/drivers/ffmpeg/src/FFmpeg.service.ts",
    "line": 971,
    "symbol": "extractFrames",
    "smell": "`Str.trim(result.stderr)` and `Str.trim(result.stdout)` normalize process output before constructing an `FFmpegError`.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 11,
    "exception": {
      "reason": "Same carve-out as `probeVideo`: external ffmpeg diagnostics are presentation cleanup at the process boundary, not schema-owned normalization.",
      "boundary": "external process diagnostics"
    }
  }
]
