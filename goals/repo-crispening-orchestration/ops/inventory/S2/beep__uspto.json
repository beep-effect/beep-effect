[
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/uspto/src/Uspto.models.ts",
    "line": 210,
    "symbol": "UsptoDocumentReference",
    "smell": "`documentCode`, `documentCodeDescriptionText`, `downloadUrl`, and `officialDate` are `S.optionalKey(S.String)` on the decoded document-reference model, forcing downstream `undefined` handling.",
    "proposedTarget": "Use `S.OptionFromOptionalKey(S.String).pipe(SchemaUtils.withNoneDefault)` for the optional document fields; this should also simplify `packages/drivers/uspto/src/Uspto.service.ts:186` where `R.getSomes` rebuilds optional properties.",
    "confidence": 0.65,
    "mechanization": "assisted",
    "roiRank": 4,
    "exception": {
      "reason": "Changing UsptoDocumentReference optional fields to Option would require updating @beep/uspto-mcp document projection consumers that read those fields directly. Field annotations and decoder colocation were fixed in-package; the public Type change is deferred.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/uspto/src/Uspto.service.ts",
    "line": 144,
    "symbol": "metadataFromWrapper",
    "smell": "`wrapper.applicationNumberText ?? fallbackApplicationNumber` is a detector hit for a body fallback.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 9,
    "exception": {
      "reason": "The fallback is derived from the request application number, not a constant or field-local schema default; the response projection needs that request context to synthesize a required domain identifier when the upstream wrapper omits it.",
      "boundary": "response projection with request-derived fallback"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/drivers/uspto/src/Uspto.service.ts",
    "line": 209,
    "symbol": "hostOf",
    "smell": "`Str.toLowerCase(new URL(url).hostname)` is normalization in a helper body.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 10,
    "exception": {
      "reason": "This is part of the explicit credential-scoping and SSRF guard path; fence 4 says URL/security guards stay explicit and property-testable instead of being hidden inside a schema transform.",
      "boundary": "trust-boundary URL host guard"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/uspto/src/Uspto.service.ts",
    "line": 399,
    "symbol": "Uspto.layer",
    "smell": "`Config.string(\"USPTO_API_URL\").pipe(Config.withDefault(USPTO_API_URL))` is a defaulting detector hit.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 11,
    "exception": {
      "reason": "This is the ambient Effect Config acquisition boundary; explicit caller config is already schema-defaulted by `UsptoConfigInput.apiUrl` at `Uspto.config.ts:47`, while the live layer must choose an environment fallback before constructing the service.",
      "boundary": "Effect Config environment boundary"
    }
  }
]
