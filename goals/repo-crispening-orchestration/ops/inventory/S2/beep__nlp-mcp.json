[
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/nlp-mcp/src/StreamingTools.ts",
    "line": 428,
    "symbol": "ReadLinesParameters",
    "smell": "The read-lines tool parameter schema repeats optional `encoding`, `maxLines`, `skip`, `skipEmpty`, and `trim` fields, then the handler re-defaults them with `??` / `O.fromUndefinedOr` (`maxLines ?? 1000`, `encoding ?? \"utf-8\"`, `skip ?? 0`, lines 345-363).",
    "proposedTarget": "Extract a named `ReadLinesOptions` schema with `SchemaUtils.withKeyDefaults` for `encoding`, `maxLines`, `skip`, `skipEmpty`, and `trim`; keep `tail` as `S.OptionFromOptionalKey(S.Finite).pipe(SchemaUtils.withNoneDefault)` because it is a real mode switch, not a default. Handler code then consumes decoded values directly. Exemplar for Option absence: `packages/foundation/modeling/lexical/src/Lexical.model.ts:686-688`.",
    "confidence": 0.78,
    "mechanization": "assisted",
    "roiRank": 2,
    "exception": {
      "reason": "The public TextStream option defaults were moved onto TextReadOptions/TextStreamOptions. This remaining site is a local MCP Tool.make adapter with head/tail result caps and an existing nested-struct boundary exception; extracting it would be the deferred tool-parameter class pass, not this helper-contract remediation.",
      "boundary": "MCP Tool.make parameter adapter and head/tail response cap"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/nlp-mcp/src/StreamingTools.ts",
    "line": 563,
    "symbol": "LoadLinesParameters",
    "smell": "Dataset-loading tool parameter schemas use optional `maxLines` / `maxRecords` / `timeout` / `skip*` fields, while handlers and loaders re-apply defaults later: `options?.maxRecords === undefined ? ...` at StreamingHandlers.ts:247, `options?.maxLines === undefined ? ...` at line 262, and `options.timeout ?? DEFAULT_TIMEOUT_MS` in DatasetLoader.ts:402,454,525.",
    "proposedTarget": "Introduce named load option schemas (`LoadTextOptions`, `LoadLinesOptions`, `LoadJsonlOptions`, `LoadJsonOptions`) with `timeout: S.Finite.pipe(SchemaUtils.withKeyDefaults(DEFAULT_TIMEOUT_MS))`, boolean defaults via `withKeyDefaults(false)`, and `maxLines` / `maxRecords` defaults where the tool caps results. Apply the same schemas at the public loader boundary so handlers stop rebuilding partial option objects.",
    "confidence": 0.7,
    "mechanization": "assisted",
    "roiRank": 3,
    "exception": {
      "reason": "Dataset loader timeout and boolean defaults now live on DatasetLoad*Options. The remaining maxLines/maxRecords handling is a tool-level response cap applied after loading and belongs to the MCP adapter boundary.",
      "boundary": "MCP dataset-loading tool adapter response cap"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/nlp-mcp/src/StreamingTools.ts",
    "line": 632,
    "symbol": "FilterLinesParameters",
    "smell": "Search/validation parameter schemas leave boolean and limit defaults in handlers: `maxMatches ?? 1000` at StreamingHandlers.ts:151, `maxLines ?? 1000` at line 196, and `maxErrors ?? 100` / `maxRecords ?? 1000` at lines 426-427.",
    "proposedTarget": "Extract named schemas for `FilterLinesOptions`, `ExtractMatchesOptions`, `ReadJsonlOptions`, and `ValidateJsonlOptions` with `SchemaUtils.withKeyDefaults` for `caseInsensitive`, `invert`, `fullLines`, `collectErrors`, `skipInvalid`, and the max-count defaults. In-repo exemplar: `packages/foundation/modeling/schema/src/SchemaUtils/withKeyDefaults.ts:50-69` defines the helper used by existing config schemas.",
    "confidence": 0.7,
    "mechanization": "assisted",
    "roiRank": 4,
    "exception": {
      "reason": "Regex search limits and booleans are local MCP tool execution caps guarded by compileRegex and stream take limits. Moving these into a shared helper schema would cross the settled Tool.make adapter carve-out without deleting package-owned helper defaults.",
      "boundary": "MCP regex/search tool adapter cap"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/nlp-mcp/src/StreamingTools.ts",
    "line": 104,
    "symbol": "FileInfoOutput",
    "smell": "Tool output schemas use optional keys for semantically absent fields (`lineCount?:`, `sizeBytes?:` on FileInfoOutput, `errors?:` on JsonlOutput line 192, and `errors?:` on CountWithErrorsOutput line 402), while handlers omit fields conditionally (`return { exists: false }`, conditional `errors` spread, etc.).",
    "proposedTarget": "Consider `S.OptionFromOptionalKey(...).pipe(SchemaUtils.withNoneDefault)` for decoded output values while preserving omitted-key encoding for MCP clients; array-valued `errors` may alternatively use `SchemaUtils.withEmptyArrayDefaults` if an empty array is the desired public contract. In-repo exemplar for empty arrays: `packages/tooling/tool/cli/src/commands/Architecture/OperationPlan.ts:765-769`.",
    "confidence": 0.55,
    "mechanization": "judgment",
    "roiRank": 7,
    "exception": {
      "reason": "Optional output keys intentionally preserve omitted-key MCP wire payloads for absent file metadata and collected errors. Numeric precision was tightened, but Option-decoding these success objects would change the local Tool success shape without removing runtime default logic.",
      "boundary": "MCP tool success wire shape"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/drivers/nlp-mcp/src/Streaming/DatasetLoader.ts",
    "line": 305,
    "symbol": "isBlockedRemoteHost",
    "smell": "`const host = pipe(Str.toLowerCase(hostname), Str.replace(/^\\[|\\]$/g, \"\"))` normalizes hostnames before the internal-network checks.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 10,
    "exception": {
      "reason": "This is a fail-closed SSRF guard for attacker-controlled URL loaders; normalization is part of the explicit trust-boundary check and must remain auditable.",
      "boundary": "trust boundary: remote URL / SSRF protection"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/drivers/nlp-mcp/src/Streaming/TextStream.ts",
    "line": 329,
    "symbol": "streamLines",
    "smell": "`Stream.map((line) => (trim ? Str.trim(line) : line))` normalizes line text in a stream body.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 11,
    "exception": {
      "reason": "Trimming is the user-selected behavior of the streaming text tool, not hidden normalization of a schema field; the schema default work is on the `trim` option, not this transform.",
      "boundary": "user-requested text-processing operation"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/drivers/nlp-mcp/src/Streaming/Pipeline.ts",
    "line": 144,
    "symbol": "stageTransform",
    "smell": "Pipeline stages map literal user commands to `Str.toLowerCase`, `Str.toUpperCase`, `Str.trim`, and whitespace normalization functions.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 12,
    "exception": {
      "reason": "These are the explicit operations exposed by the pipeline tool (`lowercase`, `uppercase`, `trim`, `normalizeWhitespace`), not accidental normalization that leaked out of a codec.",
      "boundary": "user-requested line-transform pipeline"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/drivers/nlp-mcp/src/Streaming/Jsonl.ts",
    "line": 111,
    "symbol": "streamIndexedLines",
    "smell": "`Str.trim(line)` is applied before filtering blank JSONL lines and parsing records.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 13,
    "exception": {
      "reason": "JSONL parsing intentionally ignores surrounding whitespace and blank lines while preserving the physical line number; this is parser/framing behavior rather than schema-field normalization.",
      "boundary": "JSONL parser framing"
    }
  }
]
