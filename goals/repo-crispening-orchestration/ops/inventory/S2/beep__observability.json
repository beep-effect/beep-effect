[
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/foundation/capability/observability/src/CauseDiagnostics.ts",
    "line": 340,
    "symbol": "fingerprintChunk",
    "smell": "`flow(Str.trim, Str.replaceAll(/\\s+/g, \" \"), Str.slice(0, 80), Str.toLowerCase)` normalizes the chunk that flows into `CauseFingerprint.value`, while `CauseFingerprint.value` is modeled as bare `S.String` at CauseDiagnostics.ts:129-132.",
    "proposedTarget": "Introduce a `CauseFingerprintChunk` or `CauseFingerprintValue` transformation schema carrying the trim/collapse/truncate/lowercase invariant, then build `CauseFingerprint.make` from decoded normalized chunks. In-repo normalization-transform exemplar: `Pre.language` uses `S.decodeTo(..., SchemaGetter.transform(...))` at packages/foundation/modeling/md/src/Md.model.ts:1295-1300.",
    "confidence": 0.6,
    "mechanization": "assisted",
    "roiRank": 9,
    "exception": {
      "reason": "fingerprintChunk is producer-specific canonicalization of only the message chunk inside a composite fingerprint. Moving it onto CauseFingerprint.value would normalize the full fingerprint and change the algorithm boundary.",
      "boundary": "algorithm-specific fingerprint canonicalization"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/observability/src/server/NodeSdk.ts",
    "line": 180,
    "symbol": "makeNodeSdkServerConfig processor fallbacks",
    "smell": "`options?.spanProcessor ?? ...`, `options?.metricReader ?? ...`, and `options?.logRecordProcessor ?? ...` choose processors from `config.otlpEnabled`; the default depends on sibling server config and third-party OpenTelemetry constructors.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 10,
    "exception": {
      "reason": "These are config-dependent adapter constructions for Effect/OpenTelemetry's optional `NodeSdk.Configuration` processors (verified optional upstream at .repos/effect-v4/packages/opentelemetry/src/NodeSdk.ts:35-48), not pure field defaults that a schema can own.",
      "boundary": "third-party OpenTelemetry NodeSdk adapter; sibling-derived runtime default"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/observability/src/server/Prometheus.ts",
    "line": 52,
    "symbol": "layerPrometheusMetricsHttp",
    "smell": "`const { path: routePath, ...formatOptions } = options ?? {}` and `routePath ?? \"/metrics\"` mirror a nullable third-party options bag.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 11,
    "exception": {
      "reason": "This is a thin sanitized wrapper over Effect's own `PrometheusMetrics.layerHttp` contract, whose `HttpOptions.path` and whole options argument are optional and default `/metrics` upstream (verified .repos/effect-v4/packages/effect/src/unstable/observability/PrometheusMetrics.ts:59-64 and :175-190).",
      "boundary": "third-party Effect PrometheusMetrics.HttpOptions adapter"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/foundation/capability/observability/src/CauseRedaction.ts",
    "line": 190,
    "symbol": "sanitizeSensitiveText",
    "smell": "`sanitizeSensitiveText` normalizes whitespace and trims via `Str.replaceAll(/[ \\t]+/g, \" \")` and `Str.trim`; detector hit for normalization outside codecs.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.15,
    "mechanization": "judgment",
    "roiRank": 12,
    "exception": {
      "reason": "This is the package's redaction/sanitization trust boundary for secrets, tokens, and home paths; fence 4 says escaping/sanitization guards stay explicit and property-tested.",
      "boundary": "trust-boundary guard (redaction/sanitization)"
    }
  },
  {
    "ruleId": "SFV4-null-return",
    "file": "packages/foundation/capability/observability/src/experimental/server/OtlpPacketLab.ts",
    "line": 161,
    "symbol": "contentTypeFromBody",
    "smell": "`contentTypeFromBody` returns `string | undefined`, and packet preview/metadata use `?? \"application/octet-stream\"` fallbacks at OtlpPacketLab.ts:174 and :206.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 13,
    "exception": {
      "reason": "Adapter over Effect HTTP body variants where missing content type is a transport fact; the fallback is packet-lab presentation metadata, not a domain field invariant. `OtlpPacket.contentType` itself stays total.",
      "boundary": "experimental HTTP body adapter / packet-lab diagnostics"
    }
  }
]
