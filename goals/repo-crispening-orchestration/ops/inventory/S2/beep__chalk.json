[
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/foundation/capability/chalk/src/internal/AnsiStyles.ts",
    "line": 339,
    "symbol": "hexToRgb",
    "smell": "Hex-string-to-RGB-tuple parsing lives in body helpers (parseHexMatch, canonicalizeHex with its `length === 3` check, toRgbTuple) and silently defaults invalid hex to `[0, 0, 0]` via O.match onNone; the whole pipeline is a string-to-parts transformation.",
    "proposedTarget": "An `RgbFromHex` transformation schema (`S.String.pipe(S.decodeTo(RgbTuple, SchemaTransformation.transform(...)))`) capturing short-hex expansion and the chalk-parity invalid-hex-to-black policy in the codec; hexToRgb becomes a decode call.",
    "confidence": 0.6,
    "mechanization": "assisted",
    "roiRank": 5,
    "exception": {
      "reason": "Same public compatibility boundary as the S1 hexToRgb finding: malformed hex strings intentionally normalize to [0, 0, 0]. Moving this into a schema transform is a public color-argument codec design, not a safe package-local crispening substitution.",
      "boundary": "public chalk-compatible hex/bgHex color argument behavior"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/foundation/capability/chalk/src/internal/SupportsColor.ts",
    "line": 270,
    "symbol": "parseNumericForceColorLevel",
    "smell": "`Str.trim` normalization plus regex gate plus Number.parseInt in a body to turn the FORCE_COLOR env string into a ColorSupportLevel; trim/coerce normalization belongs in a schema transformation.",
    "proposedTarget": "A `ColorSupportLevelFromEnvString` transformation schema (S.String -> ColorSupportLevel via `S.decodeTo` + `SchemaTransformation.transformOrFail`, trim inside the codec) so envForceColor becomes `S.decodeUnknownOption` of that schema.",
    "confidence": 0.6,
    "mechanization": "assisted",
    "roiRank": 5,
    "exception": {
      "reason": "Numeric FORCE_COLOR normalization is part of the same env heuristic as envForceColor; preserving chalk-compatible trim, regex, parseInt, and clamp semantics in a schema transform needs a dedicated supports-color codec sweep.",
      "boundary": "Node.js FORCE_COLOR compatibility heuristic"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/chalk/src/internal/ChalkRuntime.ts",
    "line": 359,
    "symbol": "makeCreateChalk",
    "smell": "`P.isUndefined(options?.level) ? defaultLevel : normalizeColorSupportLevel(options.level)` is a `v === undefined ? default : v` fallback in the body; the options schema (ChalkConstructorOptions, S.optionalKey level) never sees the default.",
    "proposedTarget": "At minimum `pipe(options?.level, O.fromNullishOr, O.match({ onNone: () => defaultLevel, onSome: normalizeColorSupportLevel }))`; the default is dynamic (per-stream detection) so a constant withKeyDefaults does not fit -- a decode-with-context or factory-scoped schema would be needed to fully absorb it.",
    "confidence": 0.55,
    "mechanization": "judgment",
    "roiRank": 3,
    "exception": {
      "reason": "The fallback level is dynamic per detected stdout/stderr color support, not a schema constant or missing-key default. Keeping the dynamic factory default at the builder boundary preserves chalk instance behavior.",
      "boundary": "runtime-detected Chalk builder factory default"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/foundation/capability/chalk/src/internal/ChalkRuntime.ts",
    "line": 193,
    "symbol": "capitalizeModelName",
    "smell": "`Str.toUpperCase(Str.slice(0, 1)(modelName))` case normalization in a body to derive the `bgRgb`/`bgHex`/`bgAnsi256` property names from the (`rgb`|`hex`|`ansi256`) literal family.",
    "proposedTarget": "Model the fg->bg name pairing as a literal mapping (MappedLiteralKit or an S.TemplateLiteral `bg${Capitalize}` family) so the property-name domain is schema-owned rather than string-mangled; borderline S2 -- the value feeds a computed property key, not a schema field (see rule-card note).",
    "confidence": 0.5,
    "mechanization": "judgment",
    "roiRank": 2,
    "exception": {
      "reason": "@beep/utils/Str does not expose a current capitalize helper, and replacing the dynamic bgRgb/bgHex/bgAnsi256 property-name derivation with a mapped literal surface would be a larger builder-prototype redesign.",
      "boundary": "dynamic chalk-compatible builder property names"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/chalk/src/internal/ChalkRuntime.ts",
    "line": 84,
    "symbol": "Styler",
    "smell": "`parent: S.UndefinedOr(S.suspend(...))` models absence as `| undefined` in a domain schema instead of Option (S.OptionFromOptionalKey / S.OptionFromNullOr).",
    "proposedTarget": "`parent: S.OptionFromOptionalKey(S.suspend(...))` (verified at .repos/effect-v4/packages/effect/src/Schema.ts:8668) with `SchemaUtils.withNoneDefault`; caution: applyStyle walks the parent chain in a hot do/while loop matching upstream chalk, so the Option-wrapping cost and loop rewrite need judgment.",
    "confidence": 0.5,
    "mechanization": "judgment",
    "roiRank": 2,
    "exception": {
      "reason": "The recursive Styler parent chain mirrors upstream chalk and is walked in a hot loop; changing parent to Option would rewrite the recursive schema type and traversal semantics beyond a package-local crispening substitution.",
      "boundary": "internal recursive styler chain hot path"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/chalk/src/internal/ChalkRuntime.ts",
    "line": 105,
    "symbol": "BuilderMeta",
    "smell": "`styler: S.UndefinedOr(Styler)` models absence as `| undefined` in a domain schema instead of Option; consumers branch with `styler === undefined` (line 219).",
    "proposedTarget": "`styler: S.OptionFromOptionalKey(Styler)` + `SchemaUtils.withNoneDefault`, with O.match at the applyStyle branch; same hot-path caveat as Styler.parent.",
    "confidence": 0.5,
    "mechanization": "judgment",
    "roiRank": 2,
    "exception": {
      "reason": "BuilderMeta.styler shares the same upstream-style undefined absence boundary as Styler.parent; Option-wrapping it would require a hot-path traversal rewrite and broader state-model proof.",
      "boundary": "internal builder metadata hot path"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/chalk/src/internal/Utilities.ts",
    "line": 21,
    "symbol": "indexOfOrNotFound",
    "smell": "`O.getOrElse(() => -1)` converts Str.indexOf's Option into a -1 sentinel that drives the do/while loop guards (`index !== -1`, lines 46/138); Option collapsed to a magic number in domain helpers.",
    "proposedTarget": "Keep Option through the scan (O.match-driven recursion or a loop over `O.Option<number>` state) so no -1 sentinel exists; judgment because the imperative loops are perf-parity ports of upstream chalk string encasing.",
    "confidence": 0.5,
    "mechanization": "judgment",
    "roiRank": 2,
    "exception": {
      "reason": "This sentinel drives a small upstream-parity string replacement loop; replacing the loop state with Option would be a behavior/performance rewrite rather than schema absorption. The duplicated runtime call site in ChalkRuntime was removed separately.",
      "boundary": "internal string replacement loop ported for chalk rendering parity"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/chalk/src/internal/Utilities.ts",
    "line": 32,
    "symbol": "indexOfFromOrNotFound",
    "smell": "Same Option-to-sentinel collapse as indexOfOrNotFound: `O.getOrElse(() => -1)` feeding `!== -1` loop conditions.",
    "proposedTarget": "Same as indexOfOrNotFound: carry O.Option<number> through the loop state instead of a -1 sentinel.",
    "confidence": 0.5,
    "mechanization": "judgment",
    "roiRank": 2,
    "exception": {
      "reason": "This sentinel drives the same upstream-parity string replacement loop as indexOfOrNotFound; replacing the loop state with Option is deferred to a focused renderer-loop rewrite.",
      "boundary": "internal string replacement loop ported for chalk rendering parity"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/chalk/src/internal/AnsiStyles.ts",
    "line": 455,
    "symbol": "renderRgbModel",
    "smell": "Positional destructuring defaults `[red = 0, green = 0, blue = 0]` on the loosely-typed variadic `arguments_` plus `Number(...)` coercion in the body; argument defaults/coercion living outside a codec.",
    "proposedTarget": "Decode the variadic model arguments through a tuple schema (e.g. S.Tuple of channels with key defaults / FiniteFromString-style coercion) at the model-builder boundary so render helpers receive typed channels.",
    "confidence": 0.5,
    "mechanization": "judgment",
    "roiRank": 1,
    "exception": {
      "reason": "The loose variadic color-builder arguments intentionally preserve chalk-compatible coercion and missing-argument defaults. Tuple-decoding these arguments would change the public rgb/bgRgb builder boundary and needs a dedicated public API sweep.",
      "boundary": "public chalk-compatible rgb/bgRgb variadic argument behavior"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/chalk/src/internal/AnsiStyles.ts",
    "line": 471,
    "symbol": "renderHexModel",
    "smell": "`const [color = \"\"] = arguments_` empty-string default on variadic args plus template coercion `${color}`.",
    "proposedTarget": "Same tuple-schema decode as renderRgbModel: a single-element hex-string tuple codec feeding the RgbFromHex transformation.",
    "confidence": 0.5,
    "mechanization": "judgment",
    "roiRank": 1,
    "exception": {
      "reason": "The loose variadic hex-builder argument preserves chalk-compatible coercion and empty-string fallback before hexToRgb normalizes malformed input to black. A tuple codec would be part of the deferred color-argument codec sweep.",
      "boundary": "public chalk-compatible hex/bgHex variadic argument behavior"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/chalk/src/internal/AnsiStyles.ts",
    "line": 477,
    "symbol": "renderAnsi256Model",
    "smell": "`const [index = 0] = arguments_` zero default on variadic args plus `Number(index)` coercion.",
    "proposedTarget": "Same tuple-schema decode: single-element ANSI-256 index codec with the default carried by the schema key.",
    "confidence": 0.5,
    "mechanization": "judgment",
    "roiRank": 1,
    "exception": {
      "reason": "The loose variadic ansi256-builder argument intentionally preserves chalk-compatible Number(...) coercion and zero default. Tuple-decoding would alter the public ansi256/bgAnsi256 builder boundary.",
      "boundary": "public chalk-compatible ansi256/bgAnsi256 variadic argument behavior"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/chalk/src/internal/SupportsColor.ts",
    "line": 47,
    "symbol": "RuntimeProcessLikeModel",
    "smell": "`env: S.Record(S.String, S.UndefinedOr(S.String))` carries a `| undefined` union in a domain schema.",
    "proposedTarget": "None: keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "The record faithfully mirrors the actual shape of NodeJS process.env (values are string | undefined at the platform boundary); Option-wrapping every env value would misrepresent the 3rd-party contract, and use sites already lift individual keys via O.fromNullishOr.",
      "boundary": "3rd-party (node process.env / process.argv runtime boundary)"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/chalk/src/internal/SupportsColor.ts",
    "line": 505,
    "symbol": "supportsColorLevel",
    "smell": "`O.getOrElse(() => heuristicInput.minimumLevel)` (and the minimumLevel floor at line 485) look like O.getOrElse-as-default.",
    "proposedTarget": "None: keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "Terminal fold of an O.firstSomeOf heuristic chain: the fallback is derived state (the force-color floor), i.e. idiomatic Option control flow ending a match cascade, not a data default that a schema key could carry.",
      "boundary": "internal heuristic-chain fold (idiomatic Option control flow)"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/chalk/src/internal/ChalkRuntime.ts",
    "line": 173,
    "symbol": "renderJoinArgument",
    "smell": "`P.isNullish(argument) ? \"\" : ...` nullish-to-empty-string handling in a domain helper.",
    "proposedTarget": "None: keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "The chalk public API accepts arbitrary unknown variadic arguments (including null/undefined) and must join them the way upstream chalk does; this is boundary handling of untyped 3rd-party-compat input, not domain absence modeling.",
      "boundary": "3rd-party-compat (chalk public API variadic input contract)"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/chalk/src/internal/ChalkRuntime.ts",
    "line": 215,
    "symbol": "applyStyle",
    "smell": "`state.level <= 0 || text.length === 0` empty-string/zero check in the body.",
    "proposedTarget": "None: keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "Hot render-path early exit that reproduces upstream chalk semantics (empty input renders empty, disabled level passes text through); pushing this into a schema would add decode cost per styled string for no invariant gain.",
      "boundary": "3rd-party-compat (chalk render behavior parity, performance-sensitive hot path)"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/foundation/capability/chalk/src/internal/Utilities.ts",
    "line": 25,
    "symbol": "indexOfFromOrNotFound",
    "smell": "`Math.min(Math.max(fromIndex, 0), Str.length(text))` clamp-style coercion in a body.",
    "proposedTarget": "None: keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "The clamped value is an internal loop cursor for string scanning; it never flows into or out of a schema field, so a SchemaTransformation would carry no invariant. False-positive class for the clamp heuristic.",
      "boundary": "internal loop arithmetic (no schema-field flow)"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/chalk/src/internal/Utilities.ts",
    "line": 90,
    "symbol": "stringReplaceAll",
    "smell": "`O.getOrElse(() => text)` after O.liftPredicate looks like a default fallback.",
    "proposedTarget": "None: keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "Identity fallback used as control flow (no occurrence of the substring means the text is returned unchanged); there is no absent datum being defaulted, so no schema key could carry it.",
      "boundary": "internal control flow (identity fallback, not a data default)"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/chalk/src/internal/SupportsColor.browser.ts",
    "line": 83,
    "symbol": "browserColorSupportFromNavigator",
    "smell": "`O.getOrElse(() => disabledBrowserSupport)` folds to the literal `false`.",
    "proposedTarget": "None: keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "`false` is a first-class schema-modeled variant of the ColorInfo union (S.Union([ColorSupport, S.Literal(false)]) in ChalkSchema.ts:221) mandated by chalk/supports-color API parity; the fold selects a wire value already owned by the schema, it does not invent a default.",
      "boundary": "3rd-party-compat (chalk ColorInfo wire union with Literal(false))"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/capability/chalk/src/internal/SupportsColor.browser.ts",
    "line": 90,
    "symbol": "browserColorSupport",
    "smell": "`O.getOrElse(() => disabledBrowserSupport)` when globalThis.navigator is absent.",
    "proposedTarget": "None: keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 0,
    "exception": {
      "reason": "Absent navigator means a non-browser runtime, where the chalk-compat contract requires color support to report the schema-modeled `false` variant of ColorInfo; same carve-out as browserColorSupportFromNavigator.",
      "boundary": "3rd-party (globalThis.navigator runtime boundary) + chalk-compat ColorInfo union"
    }
  }
]
