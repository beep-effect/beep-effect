[
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/sanity/src/Sanity.errors.ts",
    "line": 82,
    "symbol": "SanityError",
    "smell": "`SanityError` and `SanityErrorOptions` use `S.optionalKey(...)` for cause/status/url context, then `fromReason` rebuilds omitted values through `O.getSomesStruct`.",
    "proposedTarget": "Model error context with `S.OptionFromOptionalKey(...).pipe(SchemaUtils.withNoneDefault)` on both `SanityError` and `SanityErrorOptions`; local driver exemplar: `packages/drivers/pglite/src/Pglite.errors.ts:43`.",
    "confidence": 0.75,
    "mechanization": "assisted",
    "roiRank": 5,
    "exception": {
      "reason": "Changing SanityError.status/cause/url and SanityErrorOptions.status/cause/url to Option-shaped public fields would alter the exported error constructor and consumers that currently read optional number/string fields directly.",
      "boundary": "cross-package ripple, deferred to family-close sweep"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/sanity/src/Sanity.errors.ts",
    "line": 156,
    "symbol": "readProperty",
    "smell": "`Result.getOrElse(..., thunkUndefined)` is a get-or-default detector hit while probing unknown error objects.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.25,
    "mechanization": "judgment",
    "roiRank": 6,
    "exception": {
      "reason": "The helper immediately wraps the unsafe reflective read in `O.fromUndefinedOr`; the default is an exception-safe unknown-object boundary fallback, not a schema-field default.",
      "boundary": "unknown error-cause reflection boundary"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/sanity/src/Sanity.errors.ts",
    "line": 167,
    "symbol": "safeBoolean",
    "smell": "`Result.getOrElse(Result.try(evaluate), thunkFalse)` defaults a failed predicate to false.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 7,
    "exception": {
      "reason": "This is a fail-closed guard around `HttpClientError.isHttpClientError` on unknown values; it does not represent a persisted or domain schema default.",
      "boundary": "unknown error-cause predicate guard"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/drivers/sanity/test/Sanity.service.test.ts",
    "line": 52,
    "symbol": "SanityTestHttp.handle",
    "smell": "`O.getOrElse(() => request.url)` defaults a captured test URL when `HttpClientRequest.toUrl` cannot parse it.",
    "proposedTarget": "None - keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 8,
    "exception": {
      "reason": "Test harness fallback for request capture assertions, not first-party domain behavior.",
      "boundary": "test fixture"
    }
  }
]
