[
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/epistemic/domain/test/EpistemicDomain.test.ts",
    "line": 57,
    "symbol": "UsageRecord turn-finalization fixture",
    "smell": "`unitCount: null` in the TurnFinalizationUsageAppend test fixture is a detector hit for nullish input.",
    "proposedTarget": "None - keep as-is. The domain schema already models these nullable wire values as `Option` via `S.OptionFromNullOr` on UsageRecord fields at packages/epistemic/domain/src/entities/UsageRecord/UsageRecord.model.ts:65-74.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 1,
    "exception": {
      "reason": "Test fixture intentionally exercises encoded null input decoding to Option on the domain side.",
      "boundary": "test fixture for nullable wire input"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/epistemic/domain/test/EpistemicDomain.test.ts",
    "line": 64,
    "symbol": "UsageRecord Option assertions",
    "smell": "`O.getOrElse(appended.credentialReference, () => \"\")` and the adjacent `unitCount` fallback assert decoded Option values in a test.",
    "proposedTarget": "None - keep as-is. These are test assertions over fields that are already Option-modeled by UsageRecord, not runtime defaults that belong in a schema.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 2,
    "exception": {
      "reason": "Test-only fallback used to assert Option contents; no business logic or schema-field default is leaking.",
      "boundary": "test assertion over Option-decoded fields"
    }
  }
]
