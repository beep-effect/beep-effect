[
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/modeling/md/src/Md.model.ts",
    "line": 1295,
    "symbol": "Pre.language",
    "smell": "`Pre.language` intentionally encodes as `string | null` via `S.OptionFromNullOr(S.String)` and the `Encoded` namespace records `readonly language: string | null` at Md.model.ts:1339, which is a nullish-union detector hit.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.1,
    "mechanization": "judgment",
    "roiRank": 1,
    "exception": {
      "reason": "The file documents the wire/JSON boundary explicitly: Markdown Pre language must round-trip through JSON-safe transport as `string | null`, while the decoded/domain side is `O.Option<CodeFenceLanguage>` via the codec at Md.model.ts:1295-1300. This matches the S2 SQL/wire null carve-out.",
      "boundary": "encoded/wire JSON boundary; decoded side is Option"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/foundation/modeling/md/src/Md.escape.ts",
    "line": 150,
    "symbol": "normalizeUrlProtocolCandidate",
    "smell": "`flow(Str.trim, Str.replace(...), Str.toLowerCase)` normalizes URLs in a body before unsafe protocol detection.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.1,
    "mechanization": "judgment",
    "roiRank": 2,
    "exception": {
      "reason": "This is the Markdown/HTML URL injection guard. Fence 4 explicitly says escaping, sanitization, and URL guards remain explicit and property-tested rather than being hidden in schemas.",
      "boundary": "trust-boundary guard (URL protocol sanitization)"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/foundation/modeling/md/src/Md.escape.ts",
    "line": 105,
    "symbol": "decodeHtmlCharacterReferences",
    "smell": "`Str.toLowerCase(value)` normalizes named HTML character references while decoding escaped URL protocol payloads.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.1,
    "mechanization": "judgment",
    "roiRank": 3,
    "exception": {
      "reason": "Entity decoding is part of the same URL/HTML trust-boundary sanitizer; moving this into a schema transform would weaken the visibility of the XSS defense.",
      "boundary": "trust-boundary guard (HTML character reference decoding)"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/foundation/modeling/md/src/Md.escape.ts",
    "line": 367,
    "symbol": "renderFencedCode",
    "smell": "`CodeFenceLanguage.decodeOption(Str.trim(language))` folds a raw code-fence info string to a safe token, then `O.getOrElse(..., thunkEmptyStr)` renders empty when invalid.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.2,
    "mechanization": "judgment",
    "roiRank": 4,
    "exception": {
      "reason": "`Pre.language` already carries the schema codec for modeled AST nodes; `renderFencedCode` is a public low-level escaping/render helper that accepts raw strings and must defensively sanitize its boundary input.",
      "boundary": "public Markdown escaping/render helper boundary"
    }
  },
  {
    "ruleId": "SFV4-normalization",
    "file": "packages/foundation/modeling/md/src/Md.ts",
    "line": 331,
    "symbol": "isBlockTemplateFormattingChunk",
    "smell": "`Str.trim(chunk)` is used to identify whitespace-only template-string formatting chunks.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.15,
    "mechanization": "judgment",
    "roiRank": 5,
    "exception": {
      "reason": "This is builder syntax cleanup for source-layout whitespace around template interpolations, not normalization of a domain field. The chunk either becomes a `Text` node unchanged or is dropped as formatting noise.",
      "boundary": "template-string builder formatting"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/modeling/md/src/Md.render.ts",
    "line": 268,
    "symbol": "languageToMarkdown",
    "smell": "`const languageToMarkdown: (language: O.Option<string>) => string = O.getOrElse(thunkEmptyStr)` renders absent `Pre.language` as an empty Markdown info string.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.15,
    "mechanization": "judgment",
    "roiRank": 6,
    "exception": {
      "reason": "The data is already Option-modeled by `Pre.language`; empty string is a renderer projection because Markdown syntax represents an absent code-fence language by emitting no info token.",
      "boundary": "Markdown presentation rendering"
    }
  },
  {
    "ruleId": "SFV4-defaults",
    "file": "packages/foundation/modeling/md/src/Md.escape.ts",
    "line": 306,
    "symbol": "maxBackticks",
    "smell": "`O.getOrElse(() => 0)` maps no backtick runs to a length of zero.",
    "proposedTarget": "None — keep as-is.",
    "confidence": 0.15,
    "mechanization": "judgment",
    "roiRank": 7,
    "exception": {
      "reason": "Algorithmic aggregate fallback for a regex scan result; it is not a schema-field default, and the helper output is the computed maximum run length.",
      "boundary": "Markdown fence-width algorithm"
    }
  }
]
