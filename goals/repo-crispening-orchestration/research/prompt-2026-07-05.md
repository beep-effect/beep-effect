<!-- pending verbatim import from local scratchpad -->
<!--
  PROVENANCE NOTE: The authoring spec for this packet (§0–§8 + Appendix A)
  arrived in a local Claude Code session whose scratchpad holds the verbatim
  original at <scratchpad>/original-prompt.md. This packet was authored from a
  remote container where that scratchpad is not mounted, so this file is a
  faithful reconstruction from the approved refinement plan. Replace this file
  with the verbatim original when working from the local session; keep the
  filename.
-->

# Original authoring prompt — repo-wide crispening orchestration (2026-07-05)

## Intent (§0)

Run a repo-wide "crispening": push every invariant and pure behavior into
`effect/Schema` and onto the data — schema-as-truth, defaults/normalization/
Option in schemas, tagged-union discrimination, colocated statics, precision +
annotations — so business-logic modules read as pure intent. This session's
deliverable is **packet authoring only**: plan and scaffold a goal packet under
`goals/` that orchestrates the crispening via specialist sub-agents and drives
it to a durable, self-enforcing end state. The actual sweeps run later, from
the packet, in bounded waves gated by `bun run beep yeet verify`.

## Settled decisions (§1)

Five decisions arrived pre-settled; see `decisions-locked.md` D1–D5
(orchestrate + add novel; specialists find / package-agents fix; enforce-first
then bounded waves; five specialist domains S1–S5; amend effect-first Law 20 &
47 before the `R.getSomes` → `O.getSomesStruct` sweep).

## Verified-API corrections (§2)

A correction table for Effect-v4-vs-training-data drift accompanied the spec;
preserved in full in `decisions-locked.md` (§2 table). Key entries: EffectSchema
is a factory, PromiseSchema is a value, the `.implementSync`/`.implement`/
`.implementEffect` split, the LiteralKit vs MappedLiteralKit API split,
`O.getSomesStruct` is repo-added (`Option.getSomes` does not exist),
`S.TaggedUnion` vs `S.toTaggedUnion`, `annotations` needs `?.`.

## Specialist domains (§4 + Appendix A)

Five read-only discovery specialists, each with a sub-brief (Appendix A):

- **S1 Schema-as-truth** — invariants belong in schemas; find decode/guard
  helper walls, fn contracts not carried by schemas, hand-rolled validation.
- **S2 Defaults, normalization, Option** — find `*Defaults` spreads, `?? d`
  fallbacks, nullish unions in domain code, normalization living in business
  logic instead of codecs, `null`-returning helpers.
- **S3 Discrimination & exhaustiveness** — find undiscriminated unions,
  `switch`/if-chains over tags, duplicate literal families, heterogeneous
  Option-struct spreads eligible for `O.getSomesStruct`.
- **S4 Colocation & pipeability** — find behavior separated from its data,
  missing `withCodecStatics`, statics lost by piping classes, non-dual public
  helpers, passthrough lambdas.
- **S5 Precision, testing, annotations** — find unbranded primitives on domain
  fields, schemas without `$I.annote*`, absorptions without a `S.toArbitrary`
  round-trip law, missing equivalence usage.

## Execution disciplines (§5)

§5.1 Definition of Done, §5.3 behavior-parity proofs, §5.4 breaking-change
ripple protocol, §5.5 inventory record shape — preserved in full in
`decisions-locked.md`.

## Fences (§6)

Nine non-goals fences — preserved verbatim in `decisions-locked.md` and
mirrored into `SPEC.md` Non-Goals.

## Grill outcomes (§7–§8)

A follow-up grill closed the leaf choices: slug, wave order + pilot, the four
novel lint cards (with `SFV4-type-alias` dropped), the per-owner blocking
policy ratchet, the 0.9/0.6 codemod threshold, the consolidated
`standards/architecture/DECISIONS.md` sanction, and the tracked catalog path.
See `decisions-locked.md` G1–G7.
