# FINAL-B — cross-package conversions + docs

Lane: FINAL-B (dual-arity residue, schema-first candidate/exception conversions,
JSDoc live re-audits across 13 packages). Branch `standards-remediation`.
Packages processed strictly sequentially, one writer at a time, full verify
(`tsgo -b`, `vitest run`, `turbo run docgen --force`, `biome check`) before
moving on. No commits made. Never touched `standards/*.jsonc` or any
`*.regression-baseline.jsonc`.

**Shared-tree hazard.** This is not an isolated worktree — other lanes/agents
are writing to the same checkout concurrently. Twice, files I had already
fixed and verified (`identity/src/Id.ts`, `identity/src/Vocab.ts`, and later
`uspto/src/Uspto.service.ts`) were silently reverted to their pre-fix (or a
different, broken) state mid-session by something else touching the tree.
Caught both via re-checks, reapplied, re-verified. Flagged live to team-lead;
recommend the driver confirm final on-disk state independently before commit
given this volatility.

## Per-entry disposition table

| # | Package | Entry | Disposition | Notes |
|---|---|---|---|---|
| 1 | `@beep/mcp-kit` | `projectWithinBudget` dual-arity (regressed to `dual(4)`, 4 positional) | **fixed** | Collapsed `tiers`/`budgetBytes`/`mintFetchableHandle` into one options object, `dual(2, (value, options) => ...)`. Swept the 2 test call sites, doc example, `AGENTS.md` surface-map row (`CLAUDE.md` is a symlink). |
| 2 | `@beep/identity` | `expandOption`/`contractOption` missing-dual | **fixed** | `dual(2, ...)` via private `*Impl` + public overload pair (mirrors the existing `mergeVocab` pattern in `Vocab.ts`); doc examples now show both call forms. All real call sites were already 2-arg data-first, so no ripple sweep needed beyond the doc examples. |
| 2b | `@beep/identity` | `Id.ts` `anonymous@117`/`@148` (IdentityInterpolationError/IdentitySegmentCountError), `Vocab.ts` `anonymous@57` (VocabEntry) | **fixed, true elimination** | All three were `S.Struct({...}).annotate({...})`/`S.TaggedErrorClass(...)("Tag", S.Struct({}).annotate({...}))` feeding a class/error-class. Converted to the bare-fields-plus-separate-annotations-argument form (no `S.Struct` call at all), confirmed via live `beep lint schema-first` re-scan: both entries flip to `stale` (no longer live). §5.3 parity: `VocabEntry`'s shape is already exercised by the existing `VocabRegistry` round-trip test (unchanged, still passing); the two error classes are empty-field structs where `TaggedErrorClass`'s own type signature accepts a `Fields` object or a pre-built `Struct` interchangeably (verified against `.repos/effect-v4/.../Schema.ts:12872`), so the encoded shape is provably unchanged. |
| 3 | `@beep/box` | `Box.config.ts` `anonymous@63` (`BoxCcgConfig`'s cross-field check) | **fixed (partial): candidate → exception, needs FINAL-A curation for true zero** | This was a real regression: HEAD had the struct on a named `BoxCcgConfigShape` const, but the working tree had it inlined back to an anonymous `S.Struct({...}).check(...)` fed directly into the `extends S.Class<...>(...)` clause. Restored the named-const form. **Real attempt at true elimination, reverted**: tried `class BoxCcgConfigBase extends S.Class(...)( {bare fields} )` + `export const BoxCcgConfig = BoxCcgConfigBase.check(filter)` (proven empirically that `Class.check()` *does* preserve `.make()` and enforces the check, unlike my first assumption from reading source) — but its **declared** `.make()` parameter type drops the `withNoneDefault` constructor-default optionality (verified: a real `TS2741`/docgen failure requiring `userId` in `.make()` calls that previously could omit it). That's a genuine public-contract regression, so reverted to the named-struct fix. Residual: still an `object-struct-schema` **exception** entry with reason "feeds an S.Class constructor" — same bucket as `LocalDateFields`, which R22 already lists for FINAL-A curation; this one needs the same treatment. |
| 4 | `@beep/file-processing` | `Extraction/index.ts` `anonymous@111` (`TextSpan`'s cross-field check) | **fixed, true elimination** | Same shape as box's `BoxCcgConfig` but with **no constructor-defaulted fields** (all three fields required), so the `class TextSpanBase extends S.Class(...)({bare fields})` + `export const TextSpan = TextSpanBase.check(filter)` + `export type TextSpan = InstanceType<typeof TextSpanBase>` technique has no typing regression. Confirmed via live schema-first re-scan (`anonymous@111` → `stale`) and the package's own decode-rejection test still passing. |
| 5 | `@beep/m365` | `GraphCollection` (generic factory, `@odata.*` non-identifier keys) | **attempted, reverted — unconvertible in a bounded diff** | Converting the factory to return a local class (P2's "local-class technique") requires either (a) re-annotating post-construction via `.pipe($I.annoteSchema(...))` at all 6 `M365.service.ts` call sites — but `.annotate()`/`.check()` on a `Class` both drop the `.make()` constructor-default optionality for the `opt(...)`-defaulted `@odata.*` fields (real docgen `TS2345` failures at all 6 call sites), or (b) restructuring `GraphCollection` to take `identifier`/`annotations` params baked in at construction — attempted this, but the resulting `S.Annotations.Declaration<unknown, ...>` typing cascaded into 10+ unrelated type errors across `M365.service.ts` (circular self-reference on the local class, `unknown` leaking into Effect's requirements channel). Reverted both files to the committed baseline (`git checkout --`), verified `tsgo -b`/`vitest` clean, zero diff. Disposition: **unconvertible** without either accepting a `.make()` ergonomics regression across 6 exported collection types or a larger `GraphCollection` redesign than fits this lane's scope. |
| 6 | `@beep/uspto-mcp` | `usptoDocumentFieldTiers` (3 inline nested tiers) | **unconvertible, not attempted as code** | `defineFieldTiers`/`FieldTierSet` (mcp-kit) type-constrains each tier to a literal `S.Struct<Fields>` — an `S.Class` instance is not structurally assignable to that type, so these three tiers cannot become classes without redesigning `FieldTierSet`'s own type contract in a different package (cross-cutting, out of this lane's scope; the same constraint that makes #5 hard). |
| 7 | `@beep/agents-domain` | `Agent.model.ts` `anonymous@65` (`toTagged()`'s inline member struct) | **fixed, true elimination** | Local-class technique: `() => S.Struct({...Agent.fields, mode: S.tag(...)})` → `() => { class AgentDeterministicFixtureMember extends S.Class(...)({...Agent.fields, mode: S.tag(...)}) {}; return AgentDeterministicFixtureMember; }` inside the existing `Tuple.evolve([...])` callback. `Union.mapMembers`'s type constraint is a generic `Constraint` (not a literal `Struct`), so this is safe — confirmed via `tsgo -b` and a manual runtime smoke test (`Agent.toTagged()` has zero existing test/call-site coverage anywhere in the repo; decode correctly rejected an incomplete fixture with "Missing key [createdAt]", proving the schema machinery still validates). Live schema-first re-scan: `stale`. |
| 7b | `@beep/agents-domain` | `AssistantContent.model.ts` `anonymous@316` (`ListBlock.items`'s inline nested struct) | **fixed, true elimination** | Extracted to a named `ListItem` class, referenced as `S.Array(ListItem)`. `ListBlock` is consumed by `@beep/agents-server`'s `AnthropicTurnCodec.ts`; verified that package's `tsgo -b` still compiles clean. Live re-scan: `stale`. |
| 8 | `@beep/agents-client` | `Chat.atoms.ts` `CreateThreadAtomInput` | **fixed, true elimination** | `S.Struct({...}).annotate({...})` + separate `export type X = typeof X.Type` → single `class CreateThreadAtomInput extends S.Class<CreateThreadAtomInput>($I\`CreateThreadAtomInput\`)({...}, $I.annote(...)) {}` (the `$AgentsClientId`/`.annote` composer already exists per R21). Verified against the package's real `Chat.schema-parity.test.ts` (`.make()` + `S.encodeResult` round-trip) and `dtslint/Chat.tst.ts`. Live re-scan: `stale`. |
| 9 | `@beep/lint-rules` | `index.ts` `anonymous@155` (`RuleRegistrySchema`, hyphenated dictionary keys) | **fixed, true elimination** | Same bare-fields-plus-annotations mechanical fix as #2b. Live re-scan: `stale`. The package's 12 jsdoc findings mentioned in scope were **already `status: "clean"`** in the current committed inventory (resolved by a parallel lane, presumably the repo-cli 58-finding pass referenced in R20) — verified via the tracked jsdoc inventory; no action needed. |
| 10 | `@beep/ecfr` + `@beep/html` | `scripts/generate.ts` `.toUpperCase()` advisories | **fixed, true elimination, byte-stable** | `method.toUpperCase()` → `Str.toUpperCase(method)` (ecfr); `tag.charAt(0).toUpperCase() + tag.slice(1)` → `Str.toUpperCase(tag.charAt(0)) + tag.slice(1)` (html). Ran `bun run generate` for both packages post-fix: `git diff --stat` on every generated output file is **empty** (byte-identical), satisfying "no unintended changes." Live re-scan: both `stale`. |
| 11 | `@beep/uspto` | `Uspto.service.ts:144` `optionalField`'s `R.getSomes` | **verified correct as-is, no change** | Attempted the swap to `O.getSomesStruct` as instructed; it does not compile (`TS2322`: `getSomesStruct`'s return type is always `Partial<{...}>` per-key, which cannot satisfy `optionalField`'s declared `Record<string, string>` return used by 13 different `...spread` call sites with different literal keys through a non-generic `key: string` parameter). This is exactly the "R.getSomes remains for homogeneous dynamic-key dictionaries" carve-out already written into the advisory's own recorded reason (Law 20/47 amendment). Reverted to `R.getSomes`, confirmed `tsgo -b`/`vitest` clean and zero diff against HEAD. A real fix (if desired) would require making `optionalField` generic over the key (`<const K extends string>(record, key: K): Partial<Record<K, string>>`) and re-auditing all 13 call sites — bigger than this lane's scope; flagged to team-lead. |
| 12 | `@beep/repo-utils` | `JSDoc/JSDoc.ts`: 12 missing `@example` (all `Runtime type for {@link X}` aliases over big `S.toTaggedUnion("_tag")` unions) | **fixed** | Added a uniform, minimal, compiling type-only example per export (`const tagName = (tag: X): string => tag._tag`) for `StructuralJSDoc`, `AccessModifierJSDoc`, `DocumentationContentJSDoc`, `TSDocSpecificJSDoc`, `InlineJSDoc`, `OrganizationalJSDoc`, `EventDependencyJSDoc`, `RemainingJSDoc`, `ClosureSpecificJSDoc`, `TypeDocSpecificJSDoc`, `TypeScriptSpecificJSDoc`, `JSDocTag`. Docgen example count went from 595 → 607 (+12), all typecheck. |
| 13 | `@beep/schema` | 17 missing `@example` / 8 missing `@category` / 8 missing `@since` / 8 missing summary (33 counted findings, 17 distinct export sites) | **fixed** | `CardinalDirection.schema.ts` (`Schema`, `Abbrev` type aliases), `Duration/Duration.input.ts` (`Input`), `Http/Http.headers.shared.ts` (`ArrayOfStrOrStr`, `StringOrUrl`, `HeaderMaxAgeSeconds`, `EncodedStrictURIFromStrOrURL` — 4 type aliases), `HttpProtocol.schema.ts` (`Schema`), `Sex/Sex.schema.ts` (`Schema`) — all missing-`@example`-only type aliases, fixed with `satisfies`-style value examples. `Model/Model.variants.ts` — the real finding: 8 re-exports (`Class`, `Field`, `FieldExcept`, `FieldOnly`, `Struct`, `Union`, `extract`, `fieldEvolve`) documented richly at their destructuring `BindingElement` site but docgen attributes findings to the separate, sparse `export { ... }` re-export statement at the bottom of the file (the two doc sites are not the same node to the tool, despite the file's own comment claiming otherwise) — enriched the 8 export-statement doc blocks with matching summary + `@example` + existing `@category`/`@since`. Docgen example count 1243 → 1260 (+17), all typecheck; 617/617 tests still pass. |

## Verification commands run (every row above, per package)

- `<pkgroot>/../../../[../]node_modules/.bin/tsgo -b tsconfig.json` (or `bunx tsgo -b <pkg>/tsconfig.json` from repo root) — clean on every touched package, re-run at the end as a full sweep across all 13.
- `npx vitest run` inside each package dir — all green, re-run at the end as a full sweep (mcp-kit 23/23, identity 58/58, box 19/19, file-processing 10/10, agents-domain 11/11, agents-client 2/2, lint-rules 44/44, ecfr 3/3, html 12/12, uspto 11/11, repo-utils 187/187, schema 617/617, m365 12/12 unaffected-baseline check).
- `bunx turbo run docgen --filter=<pkg> --concurrency=1 --force` — succeeded for every touched package (used `--force` + direct turbo invocation rather than `docgen:local`, since this branch's huge diff against `origin/main` makes `docgen:local`'s changed-file scoping pull in unrelated packages).
- `npx biome check <files>` — clean, no fixes needed, on every touched file.
- `bun run packages/tooling/tool/cli/src/bin.ts -- lint schema-first` (read-only, no `--write`) — used repeatedly as a live litmus check to confirm each schema-first entry actually goes `stale` (eliminated) vs. merely changing status; **not** used as a substitute for the driver's own gate run, and never given `--write`.
- `bun run generate` in `@beep/ecfr` and `@beep/html` — regenerated outputs, `git diff --stat` empty on every generated file (byte-stable).

## Ripple / blocked items for the driver

1. **FINAL-A companion curation needed** for `packages/drivers/box/src/Box.config.ts` :: `BoxCcgConfigShape` (`object-struct-schema`, "feeds an S.Class constructor" reason) — same bucket as the already-planned `LocalDateFields` curation; cannot reach true zero without a `PERMANENT_SCHEMA_FIRST_EXCLUSIONS` entry.
2. **`@beep/m365` `GraphCollection`** and **`@beep/uspto-mcp` `usptoDocumentFieldTiers`** — both verified unconvertible within a bounded diff (see disposition table rows 5–6); recommend the driver either accept them as permanent `object-struct-schema` exceptions or scope a dedicated cross-package redesign lane (would touch `@beep/mcp-kit`'s `FieldTierSet` type contract and/or `GraphCollection`'s call sites).
3. **`packages/drivers/uspto/src/Uspto.service.ts` `optionalField`** — confirmed `R.getSomes` is correct as-is; the `SFV4-getsomes-struct` advisory's own recorded reason already carves this case out. No further action needed unless the driver wants the generic-key redesign described in row 11.

## Files touched (all verified individually + in a final cross-package re-sweep)

`packages/foundation/capability/mcp-kit/{src/FieldTier.ts,test/FieldTier.test.ts,AGENTS.md}`,
`packages/foundation/modeling/identity/src/{Curie.ts,Id.ts,Vocab.ts}`,
`packages/drivers/box/src/Box.config.ts`,
`packages/foundation/capability/file-processing/src/Extraction/index.ts`,
`packages/agents/domain/src/{entities/Agent/Agent.model.ts,values/AssistantContent/AssistantContent.model.ts}`,
`packages/agents/client/src/Chat.atoms.ts`,
`packages/tooling/policy-pack/lint-rules/src/index.ts`,
`packages/drivers/ecfr/scripts/generate.ts`,
`packages/foundation/modeling/html/scripts/generate.ts`,
`packages/drivers/uspto/src/Uspto.service.ts` (net: no diff vs. HEAD — see hazard note),
`packages/tooling/library/repo-utils/src/JSDoc/JSDoc.ts`,
`packages/foundation/modeling/schema/src/{CardinalDirection/CardinalDirection.schema.ts,Duration/Duration.input.ts,Http/Http.headers.shared.ts,HttpProtocol/HttpProtocol.schema.ts,Model/Model.variants.ts,Sex/Sex.schema.ts}`.

`packages/drivers/m365/src/{M365.schemas.ts,M365.service.ts}` — touched, then reverted to HEAD (zero net diff); confirmed via `git diff`.
