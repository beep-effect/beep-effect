---
browserControl: yes
tool: Chrome extension bridge
checksRun: 3
---

- `https://example.com` loaded successfully with page title `Example Domain`.
- The main heading rendered visibly as `Example Domain` (one matching level-1 heading).
- `http://localhost:3000` responded successfully and rendered Grafana; the page title was `Grafana`.
