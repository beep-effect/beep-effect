---
goal: box-typecheck-cost
agent: claude
date: 2026-08-01
trigger: on-demand
confidence: high
findings:
  - category: implementation-improvement
    confidence: high
    instruction: "Measure the fixed import floor before setting an absolute type-instantiation budget."
    explanation: "A probe file importing only @beep/identity, @beep/schema, and effect/Schema with one trivial schema costs 1,649,265 instantiations. The packet's original <=1.5M absolute per-file budget sat below that floor and was unachievable by construction; post-prune Box.models.gen.ts is 83% floor, so the absolute number barely responds to work on the package."
  - category: goal-critique
    confidence: high
    instruction: "Prefer demand-scoping a generated vendor surface over optimizing how the full surface is emitted."
    explanation: "Wrapping 9 of 85 Box SDK managers cut package instantiations 66.5% and the generated file's marginal cost 89.3%, so levers 2 and 3 (annotation emission, per-manager split) were never needed. The prior grill had ranked annotation emission as the mass-reducing lever; scope reduction dominated it by an order of magnitude."
  - category: implementation-improvement
    confidence: high
    instruction: "Attribute consumer typecheck failures by rerunning against restored pre-change artifacts, not by reading error text for relevance."
    explanation: "apps/professional-desktop reported 168 errors after the prune. Restoring src/_generated from HEAD produced a byte-identical 168-error set, proving inheritance rather than introduction. Judging by 'none of these mention Box' would have reached the same verdict without evidence."
  - category: tooling-friction
    confidence: high
    instruction: "Do not assume a repo script runs in a git worktree; node_modules is not there."
    explanation: "resolveBoxPaths hardcoded repoRoot/node_modules/box-node-sdk. Worktrees under .claude/worktrees have no node_modules and resolve against the primary checkout, so bun run generate died on ENOENT. Fixed by walking up for the installed SDK."
  - category: codification-todo
    confidence: medium
    instruction: "Derive @beep/ui's own import floor before setting any budget for it."
    explanation: "@beep/ui's ~3.5M is dominated by React and component generics rather than effect/Schema, so the Box floor does not transfer. A per-package probe is a prerequisite for a meaningful target."
  - category: tooling-friction
    confidence: high
    instruction: "Run the changed-file gates against a staged tree; several of them cannot see untracked files."
    explanation: "changeset status --since=origin/main reported no changesets while the changeset file was untracked, and markdownlint reported 'Linting: 0 files' for a packet of new markdown. Both passed or failed for the wrong reason until the paths were staged. Adding a goal packet also requires regenerating goals/INDEX.md via `bun run beep goals index --write`."
  - category: implementation-improvement
    confidence: high
    instruction: "Expect fallow's audit gate to block ANY newly introduced complexity finding, not just severe ones."
    explanation: "Two new functions at cyclomatic 8 and 7 blocked the gate; after decomposing them a replacement at cyclomatic 5 (severity 'moderate') still blocked it. New helpers in a changed file need to stay under the reporting threshold entirely, which meant splitting predicates into single-purpose functions."
  - category: goal-critique
    confidence: high
    instruction: "Read the proof log, not the summary signal, before calling a run green."
    explanation: "A background task notification reported exit code 0 for a run that had actually failed, because the command was piped through `tail` and the pipeline's exit code was reported instead. Separately the yeet verdict's repairCommand pointed at dual-arity when the real failure was the changeset gate."
todos:
  - "Derive the @beep/ui import floor and restate its ~3.5M figure in marginal terms before opening that packet."
  - "Revisit the retained lever-3 assumption (per-manager split vs tsgo parallel checker) only if generated mass grows again."
---

# Reflection - box-typecheck-cost (2026-08-01, claude)

## Summary

This packet scoped `@beep/box`'s generated SDK surface to declared demand: 9 of
85 `BoxClient` managers, 34 of 333 operations, 294 of 2,763 model schemas, via
an explicit manager manifest plus a transitive model-closure prune. Package
instantiations fell 7,472,755 → 2,503,112 (−66.5%); `Box.models.gen.ts` fell
88,709 → 9,822 lines and 6.797s → 0.816s to check.

Lever 1 met budget alone. The staged levers 2 (generator-emitted explicit type
annotations) and 3 (per-manager file split) were never fired and are retained
as designed-but-unbuilt.

## What the measurement changed

The packet's most useful output was not the prune — it was discovering that the
budget metric was wrong. The original per-file budget (≤1.5M instantiations,
absolute) was set from the observed 4.8M without asking what a *floor* file
costs. It costs 1,649,265. The budget was therefore below the floor: no file in
this repo importing `effect/Schema` could ever satisfy it, and the metric it
gated was ~83% constant after the prune.

Restating the per-file budget as marginal cost (`total − floor`) makes it
respond to work on the package. The package-wide ≤3M absolute budget needed no
amendment and was met.

The general lesson: an absolute instantiation count for a single file in a
schema-heavy repo is mostly a measure of its imports. Budget the delta.

## Where the defects actually came from

Seven defects reached a gate in this packet, and every one was introduced by
this change rather than inherited: a nested `O.match` (terse-effect law), TSDoc
parsing `<=` as an HTML tag, a native `Error` in `Effect.die` (native-runtime
law), a stale `goals/INDEX.md`, an unlanguaged fenced code block (MD040), and
two introduced complexity findings (fallow audit). None were caught by
self-review; all were caught by repo gates.

The most instructive one was not on that list. After amending the instantiation
budget mid-flight, the old `≤1.5M` figure survived in four files
(`box.surface.ts`, `GOAL.md`, `PLAN.md`, and the manifest's `keyDecisions`)
because the amendment was written in `SPEC.md` and not propagated. A decision
recorded in several places has to be swept when it changes; no gate checks that.

## Tooling experience

- `bun run beep goals doctor` caught four real manifest schema violations while
  the packet was being authored (`"open"` is not a `GoalStatus`; `"conditional"`
  is not a `GoalPhaseStatus` — conditionality belongs in a phase's `exit` field;
  `supersededBy` is a slug string, not an object). Running it immediately after
  authoring a packet is cheaper than discovering these at closeout.
- The Effect LSP rejected `Effect.dieMessage` as a v3 API and named the v4
  replacement inline. Separately, `A.filterMap` in v4 takes a `Filter`
  (`Result`-returning), not an `Option` — the file's existing
  `A.getSomes(A.map(...))` idiom was the correct local precedent. Both confirm
  the standing rule: validate Effect APIs against v4 source rather than writing
  from priors.
- 76 dropped managers are logged by name on every generation, so the reduced
  surface is self-documenting rather than a silent cap.

## Carry-forward

The manifest at `packages/drivers/box/scripts/box.surface.ts` is the single
chokepoint through which generated mass can regrow, which is why the budget is
enforced as a review obligation on manifest edits rather than a CI lane. If a
future manager addition pushes marginal cost past 750K, lever 2 is the
pre-agreed next step — and `SPEC.md` §4 records why a file split must not be
tried first.
