# ADHD divergence pools — backpressure (2026-08-13)

Raw grok-4.6 branch outputs (5 isolated divergent frames + 3 FOCUS deepen passes).
Synthesis lives in SPEC items A2-A4. Banned-obvious ideas: webhook, poll-faster, notification.

## hardware

```json
[{"text":"Treat each checkout as a cache-coherent NUMA node and poison the work-item cache line so the next load of that path takes a machine-check instead of completing.","rationale":"A red check or sibling race is a poisoned MESI line: stamp the local work artifact (lockfile, PR lease, dirty path) so the owning agent traps on its next memory reference to that job, not on a later poll of a remote status register."},{"text":"Give every agent session an NMI vector that cannot be masked by a mid-tool critical section.","rationale":"Busy agents are CPUs with IF=0; ordinary hooks are maskable IRQs. A non-maskable path (ptrace/PTY inject, forced SessionStart, synthetic UserPromptSubmit) preempts the firmware loop even while a long tool is in flight."},{"text":"DMA the failure packet into a pre-mapped per-session doorbell page the agent already owns.","rationale":"Stop asking the CPU to fetch status. The interrupt fabric writes the 16-check bitmap plus reviewer thread into an mmap’d ring; the agent notices on the next syscall/futex wake with zero GitHub round-trip in its timing budget."},{"text":"Pin MSI-X vectors one-per-checkout so the APIC never broadcasts a failure to the wrong core.","rationale":"A shared ntfy/webhook bus is a wired-OR interrupt line; 32-core sibling fleets need destination-ID routing. eventfd/signalfd bound to the owning PID is IRQ affinity, not a workstation-wide bell."},{"text":"Arm a hardware watchdog on every in-flight lane; missing ACK is a forced context switch, not a sticky LED.","rationale":"Quality-lane fail or required-check red starts a TSC-deadline timer. If the agent does not EOI the mailbox before the budget, the supervisor preempts the current tool and vectors into the fix handler."},{"text":"Make sibling races a locked bus cycle: CAS the work lease or take a #MC, never a late merge.","rationale":"Two checkouts touching one PR is two masters writing the same address. LOCK XCHG on a lease file turns the conflict into an immediate failed atomic, which is cheaper than discovering it after both pipelines have already run."}]
```

## speedrunner

```json
[{"text":"Pre-arm a parked `claude -p` / `codex exec` fixer on a named pipe; the check-run GraphQL subscription writes the failing SHA+job into the pipe so the agent process is already warm and starts the fix in the same kernel wakeup as the status flip.","rationale":"Delivery is the unbounded part; a parked process plus a fifo makes the failure itself the stdin of the fixer, not a notification a busy session might miss."},{"text":"Hijack GitHub required-check *pending* as the signal: a local inotify on `.git/refs` plus a checkSuites watch starts the owning agent the moment the check is *queued*, not when it goes red, so the fixer is already mid-repro when CI finishes failing.","rationale":"The failure is predictable from the pending suite; starting before the red status is a frame skip on GitHub's own state machine."},{"text":"Give each checkout a SIGUSR1 handler in the Claude SessionStart hook that dumps context and switches the live REPL onto the failing PR; systemd path units on `~/.claude/inbox/<agent-id>` fire the signal into the exact pid, so a mid-tool-use agent is preempted instead of finishing the now-wrong task.","rationale":"The busy-agent gap is preemption, not awareness; a POSIX signal into the harness is how you interrupt without waiting for the next UserPromptSubmit."},{"text":"Lease work via `git worktree lock` + a `.fleet/owners` file written atomically; PostToolUse on Edit/Bash refuses the tool if another checkout holds the same path, converting sibling races into an immediate deny that the agent treats as the failure to fix.","rationale":"Conflicting sibling work is a failure that currently lands after the commit; making the tool call itself the detector collapses detect-and-fix into one hook."},{"text":"Mirror required-check conclusions into a local bare repo's notes (`refs/notes/ci`) from a machine-user that is a required reviewer; the owning agent's Stop hook cannot exit while a note on HEAD is `failure`, so the session is physically unable to go idle until the red is gone.","rationale":"Forcing the session to stay alive turns late discovery into an impossibility; the agent cannot leave the scene of the crime."},{"text":"Predict the next failing check from the last `yeet verify` shard that already failed locally and open a draft review comment as the *agent itself* on the still-green PR, so Greptile/openclaw never get first-mover and the human relay is a no-op.","rationale":"Local quality lanes know the red before GitHub does; posting the comment first makes the AI-reviewer thread a confirmation, not news."}]
```

## oncall

```json
[{"text":"PreToolUse/Stop/UserPromptSubmit become a hard mutex: the next tool, session end, or new prompt is denied until the checkout’s failure-inbox lease is ACKed.","rationale":"A busy agent cannot keep working past a failure because the harness itself is the interrupt; delivery is the blocked syscall, not a message someone might read later."},{"text":"Every claimed PR has a heartbeat lease file; expired lease auto-spawns a new incident-mode session on a fresh worktree of that PR and steals ownership.","rationale":"If the owning agent is mid-task, crashed, or between sessions, the default path is a new fixer process, not a waiting human."},{"text":"A per-checkout systemd unit holds a flock and must keep a GitHub required “fleet-watchdog” check green; unit death or unclaimed red check is the only pageable state.","rationale":"The dead-man switch is inverted: silence from the conductor is the alarm, so an unwatched failure cannot stay invisible."},{"text":"Local quality-lane failure writes a poison-pill git hook so commit/rebase/yeet publish refuse until the same checkout remediates the exact failing shard.","rationale":"The agent that caused the break cannot proceed, publish, or context-switch; the stall is at the VCS boundary, not in a notification inbox."},{"text":"A conductor freezes non-owner agent cgroups (SIGSTOP/cgroup freezer) when a sibling dirties an overlapping path, and only the lease-holder’s TTY is left runnable.","rationale":"Race detection is structural exclusion of the other processes, so conflicting work cannot continue long enough to need a late merge cleanup."},{"text":"tmux/PTY injection: the conductor types a synthetic user turn (“drop current work, you own incident X”) into the live agent session instead of waiting for the next poll loop.","rationale":"The agent’s input stream is the delivery channel; a mid-task session learns by being spoken to as the user, which is the only interrupt those CLIs already honor."}]
```

## inversion

```json
[{"text": "Inject every failure into the owning checkout’s next PreToolUse/PostToolUse result so the busy agent cannot take another tool step without reading it.","rationale": "Inversion of ‘never interrupt mid-task / leave signals in a place the agent is not looking’: hijack the hook the agent is already about to fire."},{"text": "Persist an unacked per-worktree failure ledger that SessionStart and UserPromptSubmit always splice into context, and keep re-injecting until a receipt file is written.","rationale": "Inversion of ‘signals die with the session and fire-and-forget is enough’: the inbox lives in the checkout and survives restarts with mandatory ACK."},{"text": "Bind each required check and review thread to the checkout’s branch and refuse the next Edit/Bash/git call there until the owning agent acknowledges.","rationale": "Inversion of ‘broadcast to everyone or no one and let work continue’: ownership is the worktree, and the next local mutation is the delivery channel."},{"text": "On CI-red or base-drift, fail the agent’s current tool and put the failure payload in the error text so discovery is the error path.","rationale": "Inversion of ‘queue until the 40-minute task finishes’: the in-flight tool becomes the courier."},{"text": "Auto-resume the dead or idle session that owns that branch with the failure as a synthetic user turn instead of waiting for a human to reopen it.","rationale": "Inversion of ‘sleeping sessions stay asleep and a human relays the news’: the owning harness is woken with the signal as the next prompt."},{"text": "Flock overlapping paths across sibling checkouts and intercept the loser’s next write with ‘sibling owns this and their lane just failed’.","rationale": "Inversion of ‘siblings race silently and failures stay on the other desk’: the conflict lock carries the failure to the agent about to collide."}]
```

## antcolony

```json
[{"text":"Every PreToolUse sniffs a per-PR scent file in a shared colony dir; a fresh 'red' mtime turns the next tool into a hard denial whose reason is the failing check log.","rationale":"The agent does not get messaged — the trail itself becomes impassable ground, so work naturally veers into the failure without a router."},{"text":"A red check or reviewer thread hatches as an untracked file inside the owning worktree; the next git/status/read surfaces it as dirt the agent must clean before continuing.","rationale":"Stigmergy via working-tree mess: the failure is indistinguishable from local residue, so existing 'clean the tree' instincts fire."},{"text":"Ownership is an flock on a named lock whose body is the live incident; SIGUSR1 only pokes the holder, and SessionStart reads the lockfile as the new task.","rationale":"The nest, not a broker, knows who is on the brood — the signal rides the same object that already marks territory."},{"text":"Agents lay path-crumbs on every write; a sibling finding a foreign crumb on the same path makes both PreToolUse hooks refuse the overlapping edit until one trail evaporates.","rationale":"Collision is sensed as crossed pheromone, not as a merge later — racing work dies at the leaf, not at PR time."},{"text":"UserPromptSubmit and SessionStart swallow any unread egg from the checkout inbox and splice it into the prompt as if the human just said 'fix this now', then eat the egg.","rationale":"The human-relay job is replaced by a local consume-on-wake rule; between-session latency collapses to the next breath."},{"text":"yeet monitor pins a failing required-check as a git note on HEAD; every later git/gh tool in that checkout returns a synthetic extra error until a green check clears the note.","rationale":"The repo itself becomes sticky: tools the agent already uses keep tasting bitter until the trail fades."}]
```

## deepen1

```json
{"sketch":"Writers (yeet monitor, local lane runner, sibling-collision watcher) append a typed NDJSON row `{id, kind, severity, checkout, payload, ts}` to the owning checkout's `.beep/inbox/failures.ndjson` and never talk to GitHub from a hook. Claude PreToolUse reads inbox + `.beep/inbox/acks/<id>` on every tool boundary; an unacked row DENYs the next call with the failure capsule as the reason, while SessionStart/UserPromptSubmit inject unread rows as additionalContext as if the operator typed 'fix this now'. Codex hooks splice the same capsule at the next tool boundary; a Stop hook can REFUSE to end while unacked P0 rows remain, so headless `claude -p --resume` / `codex exec resume` re-enters the same mutex. Acknowledgment is an explicit receipt file write (fix SHA, wontfix+reason, or PR-comment URL) — until that file exists the harness keeps re-presenting the row. The hot path is stat+read of git-ignored local files only; GitHub stays in the writer/publisher process. Investigation and receipt writes must be allow-listed or the DENY becomes a deadlock.","risk":"Hard DENY of every tool (including the reads/writes needed to diagnose and ACK) deadlocks the session; equally, if writers re-key the same failure or the model never learns the receipt protocol, the mutex never releases.","first_step":"Define the NDJSON row schema and receipt path in `.beep/inbox/` plus a tiny `yeet inbox append|list|ack` CLI that both writers and hooks share — no hook wiring until that contract exists.","children":[{"text":"Severity-gated mutex: P0 (required-check red, sibling collision) DENYs tools; P1 (review thread) injects context only; P2 (base drift) is SessionStart-only — same inbox, different enforcement."},{"text":"Stop-hook veto + headless resume: session cannot end while unacked P0 rows exist; the next `claude -p --resume` / `codex exec resume` consumes the same inbox as a fresh 'fix this now'."},{"text":"Cross-checkout collision bus: the detector writes the capsule into the colliding sibling's `.beep/inbox` (or a pointer in XDG_RUNTIME_DIR) so the offender is interrupted, not the victim."},{"text":"Receipt-as-work-log: ACK files carry fix SHA / PR comment URL / wontfix+reason so yeet monitor can close the GitHub thread later — still zero GitHub in the hook hot path."},{"text":"Harness adapters on one inbox: Claude DENYs, Codex injects at the next tool boundary, Grok `monitor` watches the NDJSON file and wakes the session — same rows, three enforcement styles."}]}
```

## deepen2

```json
{"sketch":"The checkout is the scene. When yeet verify fails a local shard, or yeet monitor sees a required GitHub check go red on a PR this checkout published, a failure capsule is written to a git-ignored inbox at .beep/scene.json (or XDG_RUNTIME_DIR keyed by checkout path). Yeet then poison-pills commit/publish/new-work: those commands refuse until that exact shard is re-run green or an explicit, attributed waive is recorded. Independently, a Claude Stop hook reads the same inbox and REFUSES session end while unacked failures exist for a PR this session published, stuffing the capsule and the precise fix command into the refusal so the agent is forced to keep working the red. Codex and Grok cannot refuse Stop, so they get the same capsule injected at the next tool boundary plus yeet-level refusal, which is enough to make new work taste bitter. A SessionStart/UserPromptSubmit hook in a later agent that lands in the same checkout injects the still-red capsule so a replacement session inherits the scene instead of wandering off.","risk":"A flake, an unfixable-from-this-checkout required check, or ambiguous PR ownership (multiple sessions or sibling checkouts published into the same red) deadlocks the checkout and, on Claude, can burn a headless -p session in a refuse-Stop loop.","first_step":"Define a tiny shared failure-capsule schema and have yeet write/read .beep/scene.json so commit/publish/new-work already refuse before any harness hook exists.","children":[{"text":"Headless yank-back: if the session already ended, a monitor watcher resumes it (claude -p --resume / codex exec resume) with the capsule as the prompt instead of only blocking a still-live Stop."},{"text":"Transferable scene: another sibling checkout can claim the red PR, which acks the original inbox and moves the poison-pill so a human or a different agent can leave without a waive."},{"text":"Attributed, expiring waives: waive is (shard, reason, actor, expiry), not a boolean, so flakes can be temporarily unpinned without permanently silencing the gate."},{"text":"SessionStart inheritance: any new agent that opens the poisoned checkout gets the capsule injected immediately, so spawning a fresh session cannot be used as an escape hatch."},{"text":"Required-check-only poison: the pill arms only for the 16 required GitHub checks (plus named local quality shards), never for informational or optional status, to keep the bitter taste scoped to merge blockers."}]}
```

## deepen3

```json
{"sketch":"On yeet publish, the opening session writes a machine-local lease (XDG_RUNTIME_DIR or git-ignored .beep/) keyed by PR: owner session id, pid+starttime, agent kind, worktree path, and a freshness timestamp. A PostToolUse (Claude) or next-tool-boundary (Codex) hook plus a cheap pid liveness probe refresh that timestamp; yeet monitor already surfaces the 16 required checks as failure capsules the supervisor can treat as unacked incidents. A workstation daemon (Grok monitor or a tiny lease watcher) treats “freshness past deadline AND unacked failure AND pid/starttime gone or frozen” as a dead-owner event, then CAS-steals the lease and either headless-resumes the owner (claude -p --resume / codex exec resume) or spawns an incident-mode fixer on a fresh worktree of that PR branch with the packed capsules as the first prompt. Claude PreToolUse DENYs mutating tools when this session is not the current lease holder, so a zombie owner that wakes cannot race the fixer; Stop can REFUSE to end until the incident is acked or the lease is handed back. Optionally a pre-warmed fixer already sitting on a named pipe receives the stolen-lease payload and starts inside the same second the failure row lands.","risk":"False-dead detection: an owner that is alive but quiet (waiting on the 16 checks, blocked on a YubiKey sudo touch, laptop sleep, long compile) loses the lease, and two writers then mutate the same PR branch. The whole design hangs on the CAS + fence being correct; a stale heartbeat is not the same as a dead session.","first_step":"Define the lease record and three local operations—write on publish, refresh from a hook, atomic CAS steal—plus a unit test that two processes cannot both hold the same PR lease.","children":[{"text":"Zombie fence: PreToolUse/tool-boundary hook DENYs git write, yeet publish, and worktree-mutating calls unless this session id still owns the lease, turning takeover into a hard exclusive lock rather than a polite convention."},{"text":"Resume-first policy: prefer claude -p --resume / codex exec resume when the owner transcript and agent kind are still valid; only spawn a fresh incident session if resume is impossible, so context is not thrown away on every flap."},{"text":"Warm fixer pool: one parked process per busy repo, blocked on $XDG_RUNTIME_DIR/beep/fixer.fifo with the monorepo already indexed, so remediation starts in the same second the failure capsule is written."},{"text":"Stop-hook hold: the fixer (or resumed owner) cannot end the session while it holds a stolen lease and unacked failures remain—Stop REFUSE forces it to either ack, hand the lease back, or park itself as the next warm fixer."},{"text":"Cross-checkout supervisor: one machine-wide watcher over sibling checkouts, stealing into a free worktree instead of always creating a new one, and refusing takeover if any live sibling already holds a lease on that PR."}]}
```
