# GOAL: Ship the Effect-native legal eval framework

Outcome: ship schema-first legal evaluation contracts and evidence integrated
with the repo's judge/QA surfaces, plus one operator-approved upstream C&H
baseline for comparison.

Read this packet and the Harvey exploration first. In P0, pin the on-demand
synthetic corpus, map the live judge seams, and obtain explicit approval and a
cost/cleanup envelope before the podman + metered-key baseline.

Build the durable framework in Effect: typed rubrics and judge services,
criterion isolation, all-pass plus diagnostic scoring, closure/precision
criteria, and neutral-band outcomes.

Do not retain the Python harness, make the baseline routine CI, add the bulk
corpus to normal gates, implement tracked changes, or use client/OIP material.
Any later real data room stays out of the repo, out of telemetry, and out of
shared C&H mounts.

Prove a small C&H fixture and record the one comparable baseline; then publish
through Yeet and close with a reflection.
