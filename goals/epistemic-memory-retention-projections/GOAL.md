# GOAL: Ship rebuildable retention projections

Outcome: accepted epistemic authority records produce a standalone
tier/memory-pressure report with as-of and disposition-aware views, inert
schema-validated policy data, and byte-identical delete/rebuild proof.

Read this packet, the source exploration, bitemporal-core contracts, and repo
instructions first.

Scope:

- In: repo-native projection mechanism, operator report, policy schemas,
  calibration gate, deterministic rebuild fixtures.
- Out: authority mutation, donor code/constants, external graph authority,
  operator-memory coupling, and RRF arithmetic/integration.

Start by fixing the authority/projection seam. Ship the mechanism without
activating uncalibrated policy, prove delete/rebuild identity, then publish via
Yeet and close with a reflection.
