# Research Placeholder

This directory intentionally contains no corpus inventory, concrete data-home
paths, local usernames, source filenames, file counts, sizes, document excerpts,
or other PII.

The redacted inventory for this packet is limited to four labels:

- `source-a`: new email-export directory.
- `source-b`: second new email-export directory.
- `source-c`: standalone PST file.
- `source-d`: June-era recovery directory.

The concrete label-to-path mapping lives outside the repo at
`<CORPUS_ROOT>/ops/refresh-source-map.json`. Future research notes may record
only aggregate, non-identifying counts and verification results unless the
packet spec is changed by the user.
