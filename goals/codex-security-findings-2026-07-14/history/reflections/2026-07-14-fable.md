---
goal: codex-security-findings-2026-07-14
agent: fable
date: 2026-07-14
trigger: closeout
confidence: high
findings:
  - category: tooling-friction
    confidence: high
    instruction: For security packets, land Greptile-review follow-up fixes (or hold the merge) before the base PR is merged, because the operator may merge as soon as checks are green.
    explanation: PR #410 was merged while the review-hardening commit was still proving, so a macOS-breaking Linux-only /proc/self/fd vault check shipped to main and had to be corrected in follow-up PR #413.
  - category: tooling-friction
    confidence: high
    instruction: Always re-verify codex-authored tests in the real environment; codex's sandbox cannot start bunx --bun vitest fork workers (they time out before import), so its green comes only from the node vitest fallback.
    explanation: Every codex remediation lane reported bunx vitest fork-worker timeouts and passed only via node node_modules/vitest/vitest.mjs; Fable's env ran bunx vitest fine and confirmed the tests.
  - category: implementation-improvement
    confidence: high
    instruction: Default security identity/containment checks to fail-closed, and compare paths after canonical resolution against a canonical root rather than lexically.
    explanation: Greptile P1 rounds showed a fail-open inode default (constTrue) and a lexical Path.normalize alias check were both bypassable; the definitive fixes were fail-closed inode identity and realPath-canonicalized comparison.
  - category: tooling-friction
    confidence: medium
    instruction: Anticipate two recurring Yeet gates on any code PR touching private packages - an empty changeset (bunx changeset add --empty, staged) and the borderline 10-minute JSDoc Ratchet lane (re-run to clear).
    explanation: Both PRs failed changeset-status until an empty changeset was staged, and JSDoc Ratchet (local inventory ~3m10s) timed out twice on #413 before a re-run passed.
todos:
  - Consider raising the JSDoc Ratchet CI job timeout or giving it an affected-only fast path; it sits at the 10-minute cap.
  - Consider a portable no-follow FileSystem primitive so vault descriptor verification does not need per-platform /proc/self/fd + dev/inode fallbacks.
---

# Reflection — codex-security-findings-2026-07-14 (2026-07-14, fable)

## Summary

Fourth entry in the codex-security-findings series: captured 9 findings via
codex Chrome, remediated all 9 across four codex GPT-5.6 Sol (medium) lanes plus
a Fable-owned lockfile dedupe, merged via PR #410, and closed every finding on
the dashboard as "Already fixed". A Greptile P1 review drove a hardening
follow-up (PR #413) that made the CSF-003 vault check cross-platform and the
CSF-004 alias check canonical.

## What worked

- Disjoint codex lanes with Fable owning ledgers, bun.lock, browser, and Yeet
  kept the token-heavy work off the Fable limit while preserving one reviewer of
  record for the shared state.
- Grill-first decisions (remove grafana MCP from repo config; dedupe lockfile;
  minimal-scoped fixes) held through implementation without rework.

## What didn't

- The base PR merged before review fixes were in, briefly shipping a macOS
  regression to main.
- Codex sandbox could not run vitest workers; and the JSDoc Ratchet lane is
  chronically borderline on the CI time cap.

## Lessons (confidence-tiered)

- **HIGH — Critical:** Sequence review-hardening before merge on security work;
  fail-closed and canonicalize in security-critical checks.
- **HIGH — Best practice:** Re-verify codex tests in the real environment.
- **MEDIUM — Consideration:** Pre-stage an empty changeset for private-package
  PRs and expect to re-run the JSDoc Ratchet lane.
