---
goal: shared-memory-code-kg-wiring
agent: claude
date: 2026-08-06
trigger: closeout
confidence: high
findings:
  - category: tooling-friction
    confidence: high
    instruction: Document (in 07-shared-memory-adoption.md or a skill) that grok's stdio MCP servers silently do not attach in untrusted directories, and that new .mcp.json servers need enabledMcpjsonServers entries to auto-start.
    explanation: Both bit this run. The grok recall leg failed with no error surfaced in-session — the model just saw no tools — until ~/.grok/trusted_folders.toml gained the checkout; and .claude/settings.json shipped enabledMcpjsonServers [] so the two new servers would have prompted rather than started in fresh sessions. Both are one-line fixes that cost real diagnosis time.
  - category: tooling-friction
    confidence: medium
    instruction: Give the store a post-write auto-commit path (hook or small daemon) instead of relying on agents/humans to git-commit ~/YeeBois/memory/beep-shared.
    explanation: Temporality-via-git only works if commits actually happen; this run committed manually twice. An uncommitted store window loses the history guarantees 07 promises.
  - category: goal-critique
    confidence: medium
    instruction: When an acceptance criterion depends on an external quota (live Codex tool call), write the fallback into the criterion itself, not only the exception ledger.
    explanation: Codex's weekly limit was exhausted mid-goal (reset 2026-08-08 ~06:50); acceptance criterion 3 was satisfiable only at registration level. The exception ledger covered it, but the criterion text still reads as unmet on a literal reading.
todos:
  - Add a memory-write skill that enforces the author frontmatter convention and one-fact-per-note shape on write_note calls.
  - Wire a store auto-commit mechanism (post-write hook or scheduled commit) for ~/YeeBois/memory/beep-shared.
  - Codex live MCP tool-call evidence after quota reset 2026-08-08; archive to this packet's history/.
  - Cursor GUI session check that basic-memory + codegraph attach; archive evidence.
  - Pilot review 2026-08-20 → route outcome to 04-decision-log.md (standardize vs rollback).
---

# Reflection — shared-memory-code-kg-wiring (2026-08-06, claude)

## Summary

Wired basic-memory (project `beep-shared`) and codegraph into all four coding
agents, retired cognee from the durable dev-memory role across five standards
docs plus AGENTS.md, and proved the plane live: claude wrote the adoption
note through repo `.mcp.json` wiring, grok recalled it verbatim over real MCP
tools, codegraph answered symbol queries in both sessions, all keyless.
Verdict: the wiring works and the packet closes clean, with two deferred
evidence items (codex live call — quota; cursor live call — GUI-only).

## Tooling experience

- **Worked:** the packet template discipline (P0 inventory before edits caught
  five traps that would have shipped contradictions — two AGENTS.md law
  sites, 06's cognee-centric runbook, the enabledMcpjsonServers gate, the
  installer's missing env support); `grok mcp doctor` for MCP diagnosis;
  `bun run beep yeet verify` all-green first try; `codegraph init` at 2.1s
  for 180 packages.
- **Didn't:** grok stdio MCP in untrusted dirs — silent no-tools failure mode;
  `codegraph install` cannot emit env blocks, so the no-telemetry rule forced
  hand-written entries in all four configs.
- **Frustrating:** basic-memory's `-p`-session cold-start race (first grok
  attempt saw no tools; doctor-warmed retry worked); claude `-p` variadic
  `--allowedTools` swallowing a trailing prompt argument (prompt must go via
  stdin).
- **Wished existed:** a `grok mcp trust <dir>` command; a codegraph target
  for grok in its installer.

## Implementation improvement opportunities

- Store auto-commit (see todos) — the git-temporality convention currently
  depends on discipline.
- The `author:` frontmatter convention is enforced only by prose; a
  memory-write skill could validate it mechanically.

## Goal & prompt critique

- GOAL.md's "current working directory" assumption didn't hold (the driving
  session ran from the research collection dir); absolute-path discipline in
  the packet files made it a non-issue, but the launcher could say "resolve
  the repo root from this file's location" instead.
- Acceptance criterion 3's "live tool call per CLI" should carry its own
  quota/GUI fallback language (see finding).

## TODOs worth codifying

- See frontmatter `todos` — all five are concrete and dated.

## Lessons (confidence-tiered)

- **HIGH — Critical:** Inventory before wiring — the P0 sweep found the law
  in more places than the SPEC assumed (AGENTS.md:118, 06's whole routing
  section); editing only the named section would have shipped a
  contradiction that Greptile or a future agent would trip over.
- **MEDIUM — Best practice:** Prove multi-agent claims with two genuinely
  different CLIs and models in fresh sessions; the trust-gate failure only
  surfaced because the proof used real sessions, not the CLI bridge.
- **LOW — Consideration:** basic-memory materializes frontmatter into files
  on first sync; commit the store after wiring so the diff noise lands in an
  init commit, not a content commit.
