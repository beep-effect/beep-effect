# goals-packet-migration-kind

Source `0be1f13d62fa00cb65e34ff69ec99043380f8d81`,2026-09-22.
Proposed disposition: withdraw the current qualified design into a contract hold.
This is not a disqualified independence finding or a completed replacement P2.

## Current shape

GoalPacketMigration at packages/tooling/tool/cli/src/commands/Goals/Migration.ts
343-355 exports slug:String, edits:Array(String), optional manifestText/readmeText/
parked:String, and optional isBackfill:Boolean. It is available from the Goals
barrel98 and the module's documented public import. Documentation329-331 says
outputs are present only when their surface changes and parked records a question
blocking mechanical migration. It does not state the exact relation among parked,
backfill and outputs, nor prohibit explicit false or empty strings/arrays.

## Cardinality gap

The finite representable domain is24: absent/false/true flag times presence of
three strings. The producer emits six presence tuples: parked, backfill manifest,
and four mechanical output-presence combinations. That is measured producer
coverage, not established public legal cardinality. Current24/6 overclaims.

SetStatus116-117 treats absent and false identically with !==true. Neither schema
nor documentation excludes explicit false, so absence of that producer does not
prove illegality. Backfill with a README output is also consumed without a special
rejection: it writes both and increments backfill plus changedReadmes. Parked is
checked before all writes/counters, but ignored accompanying data is not by itself
proof that constructing such a value violates the contract. No legal count is
asserted until these public contract ambiguities are adjudicated.

## Target schema

No target union is admitted yet. The old six-case design is withdrawn because
it excluded false, backfill+README, arbitrary edits and empty strings using only
producer absence. Do not replace it with another narrowed union or an always-valid
flat schema called a completed solution. Preserve the broader accepted payloads
until normative relations are established. Any later design must distinguish raw
representation aliases from semantic operation state and identify its actual
constructor/encoding compatibility obligations.

## Migration inventory

This audit proposes no source migration. Evidence surfaces are Migration343-355
(public schema), parkedPlan365-366, planBackfill378-391 and exported planner547-608;
SetStatus.applyMigrationPlan63-82 and processGoalMigration104-122; Goals/index.ts98;
and test/goals-command.test.ts108 onward. Three construction sites produce six
patterns, but tests call the planner rather than exhaust arbitrary public schema
construction. The documented schema constructor example336 uses absent fields.

The planner first handles missing manifest/backfill, then parse failure, absent
status, unmapped status, and ordered status/supersession/phase/mission/README edits.
No contract repair may silently change that sequence or generated bytes. Requests,
record schemas, command flags and their diagnostics remain separate owners.

## Guard-deletion accounting

No deletion is authorized by this audit. Current SetStatus parks before logging
edit descriptions or writes; nonparked processing logs ordered edits, writes the
manifest after ensuring its directory, then writes README. Counters distinguish
isBackfill===true versus !==true only when a manifest output is present. These
branches establish current behavior for broader combinations but do not prove all
are normative valid inputs. Do not claim them removable before settling ownership.

## Encoded-side impact

The exported schema presently accepts omitted and explicitfalse flags and full
String/Array payloads. No dedicated persisted-result wire transport was found,
but that alone cannot justify silently narrowing the documented exported schema.
Preserve optional omissions, explicitfalse and empty payload behavior during any
future compatibility decision. There is no default on optional fields. No codec
or normalization is introduced in this audit.

## Test impact

The existing tests exercise mechanical rewriting, idempotence, bare manifests,
supersession normalization, unmapped-status parking and recorded backfills. They
do not prove the public constructor forbids unproduced combinations. The private
finite-table enumerates all24 schema presence strata and labels only six as
producer-observed; it intentionally supplies no legal Boolean verdict.

Before returning this owner to qualified, obtain documented exclusions or an
owner decision for parked+outputs/backfill and backfill without a manifest, then
exercise approved aliases and full payload domains. No migration, filesystem
write, runtime schema/procedure execution or package test was performed here.

## Risk

The dominant risk is substituting planner reachability for the exported contract.
A six-case producer union may eventually be appropriate for a private planner
return boundary, but moving the existing public constructor to that domain is an
additional decision rather than proven equivalence. Parent should retain immutable
old row/design history and track this owner outside inventory/v1 while unresolved;
do not label it D1 or claim a dry round/P3 approval from withdrawal.
