# Final bounded adjudication of two Sweep neighbors

This native P2 handoff owns only its private directory. It neither reopens the
accepted worktreeDirty/statusProbeUnreliable 4/3 design nor asserts a complete
SweepGitState legal-state product. No source, canonical row/design, prior
artifact, test, ref or dependency is edited.

## Dispositions

| Cluster | Source projection | Public-owner disposition | Proposed action |
| --- | --- | --- | --- |
| branchMergedIntoBase / localTip | Observer output has 3 of 4 Boolean/presence tuples | Class-wide qualification not justified by current contract evidence; not D1 | Keep fields/guards/output unchanged; preserve exact source finding, with no row or design. |
| worktreeProbeUnreliable / mainWorktreePath | 4 representable, 3 supported tuples under the documented observation/address contract | Propose separate E4 qualification | Add r29-yeet-sweep-worktree-probe-address and its complete P2 design only after parent review. |

The initial net is satisfied by the real seven-Boolean SweepGitState class at
`packages/tooling/tool/cli/src/commands/Yeet/internal/Sweep.ts:177-200`.
Both selected payloads are real Option fields with None constructor defaults;
no required-payload predicate is invented. Neither minimal finding admits a
new whole-owner count or silently treats remaining relations as independent.

The different dispositions follow different contracts. For ancestry, the
local SHA read (:724) precedes a distinct ancestry command (:726-731), and
observeSha(:590-596) conflates absent/unreadable output. The guard at :753
proves a conservative observer image, but the exported constructed-state seam
(:124-127), blocked plan branches (:319-347) and revalidator(:885-910) do not
establish whether ancestry without captured SHA is an invalid observation or
a supported diagnostic request. No D1 witness or false certainty is invented.

For the worktree address, both the producer's explicit :757-762 prohibition
and exported handoff's :1003-1009 contract say an unreadable worktree list may
not name a holding path. Direct fixtures at test:773-821 corroborate the three
meanings. Reliable/None remains legal, including main-held/no-path helper
inputs; the unreliable/None fixture also preserves a false branch-occupancy
Boolean. Thus the proposed three-case model changes no supported partial
fixture and never interprets absence as unoccupied.

## Evidence and output files

- `ancestry-local-tip-disposition.md` contains the complete non-admission
  reasoning, all four projected tuples and the unresolved contract distinction.
- `proposals/r29-yeet-sweep-worktree-probe-address.md` contains the complete
  eight-section P2 design: one required derived observation with unreliable,
  unaddressed and addressed(full NonEmptyString path) cases; explicit default,
  payload, fixture, consumer, codec and guard accounting.
- `proposed-rows.jsonl` contains exactly one complete proposed designed row.
  It is schema-contract-shaped but not installed; the parent must validate its
  combined canonical projection before admission. There is no pretend D1 row
  for ancestry.
- `source-bindings.start.json` and `source-bindings.end.json` bind source,
  fixtures, existing encoded consumers, metadata context and original private
  status-design/unresolved-note bytes.
- `projection-check.json` contains eight private equation cases. They check
  documented same-probe behavior and handoff branch equivalence; they do not
  establish a class contract alone or execute TypeScript/package tests.

## Source pins and expected concurrent parent work

The first read recorded HEAD `1c07c15495aaa42f521b887b01e943e68804606c` and
origin/main `3657f8f97f7135c53c3c0b9fa99aa19093c3e5ee`. The parent then reported
a fetch advancing the tracking ref to
`c12c6ae651a3facc3f6e6a5190fd84670ef8a014`, with Sweep sources unchanged.
The private snapshot records that new ref separately. Equality to pinned main
means immutable3657 source bytes, not an exact-current-main claim.

The parent separately announced installation of its 697-record/140-qualified
R29 projection, including the accepted status pair and a public neighbor note.
Any observed inventory difference is recorded as expected parent context;
this bundle never overwrites that inventory, reuses its broader former D1
claim, or changes the accepted status design. Source/ref ending checks remain
explicit and must be inspected before integration after the next merge.

The prior unresolved note and accepted private status design remain frozen at
their captured hashes. They are retained as historical inputs; this handoff
records the two later neighbor dispositions separately, without rewriting the
older uncertainty into a purported earlier conclusion.

## Parent actions and limits

Review the new worktree pair's constructor-domain evidence and target taxonomy.
If admitted, add its distinct row/design in a separate attributed transaction,
keeping the status pair untouched. The conditional change is +1 record/+1
qualified; no existing neighbor record is withdrawn. Keep the ancestry
non-admission finding accessible alongside the parent neighbor note, without
claiming all four tuples legitimate or treating type permissiveness as proof.

Rebind source after main changes, validate the combined inventory/designs and
retain independent review as pending. Neither current sparse evidence nor a
fresh test authored solely to encode a desired conclusion supplies independent
P3. No implementation, census, dry-round or approval credit is asserted.
