# Knowledge candidate and classifier sequencing

The 2026-09-22 owner ruling in `../DECISIONS.md` resolves the previously held
classifier contract. The active classifier design now qualifies the kind/grammar
projection at 16/6 while retaining every independent field.

The earlier exact-source `knowledge-ref-candidate` design correctly required
its adapter to preserve the public raw flag bag while that owner was unresolved.
That instruction is superseded **only for the joint, reviewed implementation**
by `../designs/knowledge-ref-classification-input.md`: land both owners atomically
after replacement independent P3 and GATE 2. Match each private candidate case
to the relevant classifier schema case, without reconstructing the public pair
of grammar flags. No temporary unchecked adapter or duplicate deletion credit.

All other candidate obligations remain: complete nested reference payloads,
manifest-read ordering, report bytes, raw strings and refId preimages. The
classifier keeps common anchor/token Options on every kind, every resolution
status, independent pattern context, host fallbacks and reserved upstream.
Private producer reachability must not narrow that public input domain.

Both original audit artifacts retain their original hashes and source bindings.
This reconciliation supplies sequencing, not independent review, implementation,
or new census credit.
