# Round 25 reconciliation (complete)

Source `7440cb8c4302ce64b87860069a464bafbf65f576`; corpus main
`9b7553f618b2b3ee10e11a3d6ee93606f3e40ce1`. The full census has 25 primary
partitions over 3,060 included files. All primary lane receipts, source-backed reconciliation, and the nine
requested correction receipts are complete.

This round is WET. It admitted a link image-sizes record and expanded the
existing image-sizes cluster, so it receives no dry-round credit even if the
remaining partitions add nothing. Two later complete current-source dry rounds
remain required before P3.

## Decisions during the round

- Admitted independent loading-lazy/exact-auto facts and three schema-helper
  census suspects from the completed modeling and N-Z reports. The semver
  comparator explicitly handles all four numeric/nonnumeric pairs, slug edge
  filters can fail separately, and the Percentage predicates are independently
  callable helpers rather than a stored sibling bit vector.
- Superseded two modeling D1 decisions with the single correction report
  `r25-modeling-presence-correction1.jsonl`. An Option-derived implication
  still excludes a boolean pair; calling it a tautology does not establish D1
  independence. The resumed scanner exited zero with an end-turn event.
- Expanded `html-img-sizes-disposition` to include `sizesIsExactlyAuto` alongside
  presence, missing, and incompatible facts: 16 representable / 6 legal source
  tuples. Reopened its design status to confirmed until all four locals are
  accounted for. Exact-auto detail has no separate output consumer and can be
  consumed inside the present issue-classification arm.
- Admitted canonical `html-link-imagesizes-disposition` from the correction's
  longer link id. Incompatibility implies imagesizes presence; 4 representable
  / 3 legal pairs. Its separate inline missing-imagesizes branch belongs in the
  same issue-disposition design; icon sizes remain independent.

- Admitted ten driver census records after checking their current source
  declarations: independent diagnostic/quota products and separate callable
  guards, plus external Firecrawl failure/execute wire shapes. None establishes
  a new qualified domain-state invariant.

- Admitted thirteen source-checked UI/driver census records from the completed
  foundation-UI and drivers-N-R reports. Asset failures, hydration versus
  tooltip presence, finish emission versus finish-reason payload, truncation
  versus collected errors, and HTTP authentication versus body are independent.
  External request/envelope fields remain D2. Several UI predicate pairs are
  callable helpers, not a two-bit value carrier; the canonical notes correct
  the raw report's unjustified same-input independence language.
- Held the UI tab-strip implication and mention settled-alias D1 rows for the
  lane's one evidence-gate correction. `unmeasured => allFit` excludes a pair;
  `settled = !pending` may expand the existing mention cluster. Neither raw
  D1 row has been admitted to the canonical inventory.

- Admitted two schema-derived callable guard-toolkit census records from the
  architecture/ecosystem partition. They already query tagged unions; they do
  not store sibling boolean values.
- Withdrew `generated-file-drift-mode-flags` from the live census. Its entire
  signature consists of function flag parameters, which the binding scope
  excludes and the scanner prompt explicitly says to skip. Its former design
  changed the unrelated `syncGeneratedFile` helper. The record and design are
  retained under history; no artificial D1 classification or source edit is
  used to remove the out-of-scope declaration.

- The single UI correction completed with exit zero/end-turn. Admitted
  `tabstrip-overflow-disposition` (unmeasured implies allFit, 4/3) and expanded
  the existing mention cluster to pending/settled/failed (8/3). Its current
  design already deletes all three locals, so only the cardinality/member
  metadata changed and its historical review was reset to designed. The new
  tab-strip design is assigned to the native Sol design lane.

- Withdrew `docgen-quality-scope-flags` for the same explicit function-parameter
  exclusion. `resolveDocgenQualityTargets` owns only an inline parameter bag;
  callers invoke it without persisting that bag, and all four raw boolean
  inputs have defined route/error behavior. The historical record/design are
  archived, and its resolver and exact orphan-audit/error precedence stay
  unchanged.

- Withdrew `goals-portfolio-index-mode`, whose recorded declaration is only
  `runGoalsIndex`'s anonymous flag parameter object. Upstream CLI parser
  definitions are a separate surface and cannot substitute for this excluded
  declaration without their own qualified census record. The old record and
  design are archived; generated Goals-index files remain unchanged.

- Expanded DMS to connected/disconnectReason/rootRemoteId (8/3). The verified
  design already carries root only on connected; the held root-id D1 was based
  on disproving the converse implication and is superseded. Timestamp remains
  independent. Corrected XAI message cardinality to 12/3 and preserved the
  documented text-without-data case, plus unchanged close/error schemas.
- The epistemic correction completed with exit zero/end-turn. It withdrew the
  parameter-only document badge candidate and the callable-query/one-output
  injected pair. It expanded and qualified `epistemic-review-dialog-reason-gates`
  (16/4), superseding the old D1 arithmetic-local dismissal. Its design is
  assigned; supersession and separately observed waiting remain independent.
- Admitted nineteen source-checked census records from epistemic, shared
  documents, law-practice runtime, and Docgen reports. Held all three law
  domain presence candidates for their one contract-based correction; the
  blank-page annotation explicitly contradicts its raw D1 note.
- Withdrew the parameter-only `codex-findings-ingest-modes` validator record.
  Its two separate schema/CLI-option qualified carriers remain in scope.
- All three HTML/tab-strip designs are complete and source-checked; advanced
  those confirmed records to designed. No independent P3 pass is claimed.

- Admitted the source-checked composer edit-content implication and four
  CLI internal outcomes: contained file read, admission attempt, promotion tick,
  and dangling-stub exactness. Their native design lanes are active.
- The one apps correction completed with exit zero/end-turn and disqualified
  the proposed IPC/token relation. Exact transport parity and missing-token
  send tests prove the fourth combination meaningful. Admitted ten additional
  source-checked app/internal census records. Omitted the Rust OpenOptions
  record because src-tauri is supporting producer evidence, outside the corpus.
- Held the transcript empty/streaming D1 for a full existing-cluster audit,
  rather than accepting an AND-based independence assertion. The five new
  AI-metrics candidates await their single cardinality/exposure correction.
- The contradiction-review reason design is complete; its record advances
  to designed with independent P3 still pending.

## Completion and later admissions

All 25 primary execution receipts are complete with exit zero and an end-turn
event. `execution-summary.json` records 47 raw qualified candidates; that is
an execution count, not the canonical admission count or a dry-round verdict.
All nine requested correction services also completed. Raw reports and their
corrections remain preserved; source and native design audits decide canonical
metadata when a raw correction still misses the contract.

- The CLI A-C correction withdrew `addFamilyTerminal.terminal` as an excluded
  anonymous function-parameter shape. It confirmed `corpus-pst-terminal` and
  promoted `ci-lane-timings-render-mode`: only window plus both formats is
  rejected, so seven of eight raw triples remain accepted.
- Admitted scheduler protocol intent, coverage baseline mode, and prepared
  Yeet publish commit. Their designs must preserve flag-error precedence,
  affected coverage's later scope selection, and staged-only stash ownership.
- The AI-metrics correction and native audit admitted five persisted/wire
  cases: mirror privacy proof, config bounds, redaction safety, canonical root,
  and Phoenix sync policy. Synthetic detector inputs proved every supported
  redaction counter subset. The separate scorecard readiness/count cluster is adjudicated below;
  coverage-gap D1 rows remain separate.
- The law correction and native audit admitted three Tier 2 cases. Pincite
  expanded to all five locator/presence axes (32/5); citation and pincite public
  recursive Encoded contracts require compatibility codecs. IDS retains the
  unknown-type present state.
- The Files audit admitted six cases. Face presence includes the ordered faces
  vector and exact count (32/2). Rounded border score/percentage can be zero
  even when matched (16/5). Match/review tolerance flags form one four-local
  cluster (16/6), including overlapping bands. The repository-owned Python
  person worker is an application protocol and its persisted report is Tier 2;
  the raw correction's external-driver D2 ruling is superseded.
- Admitted the corrected Docgen generation, package quality, subject collection,
  cleanup, and worker-candidate outcomes, plus Goals snapshot, migration, and
  transition-plan cases. Cleanup additionally includes correlated error
  presence (36/5). The rubric functions are callable predicates, not sibling
  boolean values; their proposed qualification is superseded by a D1 record.
- Reconciled the former transition plan/commit aggregate record onto the actual
  `PacketTransitionOutcome` disposition/trace-written carrier. A separate new
  record owns the plan's stream-presence restatement. Their coupled design
  refresh must finish before another census is seeded.
- Admitted twelve corrected Research/Skills/SyncData/Worktree outcomes. Branch
  diff now includes paths presence (8/3) and preserves a measured zero count.
  The WorkspaceDescriptor candidate is D1: its documented project-present/owner-absent
  construction is consumed by the exported sorter. This supported fourth tuple
  defeats the producer-only qualification; no design is created.
- The transcript audit supersedes the raw empty/streaming D1 and expands the
  existing record to six load/content axes (64/21). Loading requires empty
  persisted turns; failure can retain previous turns; ready can be entirely
  empty before a request starts. Its completed design preserves all cases.
- Archived three obsolete census entries: the curation alias now points to the
  already inventoried StagedFileCommitRecord; the old three-subscription
  immediate cluster no longer exists; the portfolio helper is an excluded
  anonymous parameter shape. Removed retired `runExamples` and thread
  `isDraft` members, refreshed the current eight Yeet criteria, and recorded
  46 declaration-anchor updates in `declaration-anchor-refresh.json`.
- The final packet audit expands migration to all five kind/text/edit axes
  (48/6), transition plan to stream/disposition/revision/tip/event/derived axes
  (192/4), and transition outcome to disposition/trace/count/tip (24/4).
  An existing empty stream remains a legal append source: planning seeds genesis
  before the transition events. Internal schema exports alone do not create a
  wire contract; these transient values migrate atomically without extra codecs.
- The public Codex findings decoder is Tier 2. Its command mode lands first in
  Tier 1E, introducing only the shared mode vocabulary; the later singleton
  changes the public decoded schema behind a compatibility codec. Source
  confirms the command does not call this decoder, so serial tiers are viable.

The final audit admitted both Scorecard readiness/count clusters at 16/6, with
separate false/111 and true/111 judgments. The public constructor documents
false/111 through its missing-key default; the all-zero case had no supported
writer or example and was withdrawn from the draft. Invalid SQL rows preserve
the existing typed decode failure, while only no row becomes None/null.

Reconciliation is complete. Against the immutable round seed, this round adds
49 qualified records, expands five existing qualified clusters, and withdraws
four excluded parameter-only records. All 149 current qualified records have
designs. Round 25 is wet and receives no dry credit; two later complete dry
rounds remain required before independent P3 review.

The reconciled canonical inventory is 930 records: 149 qualified and 781
disqualified (D1 619 / D2 162). Qualified tiers are 117 Tier 1 and 32 Tier 2;
statuses are 36 historically `reviewed`, 113 `designed`, and 0 `confirmed`. No source
implementation or formal P3 review has occurred.
