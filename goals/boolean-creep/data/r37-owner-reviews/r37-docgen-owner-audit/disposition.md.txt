# R37 Docgen complete-owner and request audit

Withdraw the two current designed worker-source rows and archive their two designs. Admit one distinct D1 target-selection request. Correct one goals-index locator/note; retain docgen-quality's existing first-field anchor. Net−1row/−2qualified/+1disqualified/−2designs. No new qualified design.

Complete local worker source object at Docgen.command.ts887-892 and Runpod source object at1035-1040 each has exactly four own fields: all:Boolean, input:Option<string>, packageSelector:Option<string>, packetLimit:number. There are no spreads or inherited fields on either concrete object. Nearby provider/runtime Boolean flags belong to separate raw handlers or later evaluator requests; they cannot be flattened into these owners. Both call the same resolver whose input signature confirms those four types at Targets.ts270-280. Exact-one validation before construction establishes a selection relation but cannot waive the ≥2 actual Boolean recall gate. Nullable/Option string presence is not another actual Boolean. Remove both8/3 qualified rows without fabricated D replacements. The paired historical designs explicitly coordinate their shared selector/resolver and have no remaining external live design dependency; archive both together and perform no runtime migration.

The raw new D1 is a different actual object at Targets.ts292-296, downstream of input-file early return. It contains all:Boolean, changedFiles:false(Boolean) and packageSelector:Option<string>; two actual Boolean fields qualify for recall. The inline callback parameter declaration at Quality.scope.ts107-115 is excluded as a standalone candidate, but the real constructed object is eligible. Canonical inventory has no prior Targets.ts owner; existing r26 docgen-quality-command-scope belongs to Docgen.command.ts792-796 and is a distinct writer.

Retain D1 for this request object. It forwards all, pins changedFiles false and forwards the selector to a raw resolver. The resolver first runs orphan-config validation, then rejects more than one selected scope118-122 with a specified DomainError, handles a package, handles all, or derives changed-files/affected. The public source resolver also has input-file precedence and an affected fallback; its body does not enforce the CLI-only sourceCount rule. A caller pinning false does not establish a legal-domain requirement forbidding the other Boolean tuples. Conflicting selection has a supported diagnostic, not an inferred post-validation state gap. No enum/source-mode rewrite or narrowing of accepted request inputs is justified. New row anchors the actual object start292 rather than its second Boolean294.

Metadata: goalsIndexCommand is a Command.make handle declared329, which is not the census owner. Its actual runGoalsIndex({write,check}) object is333; canonical325 now falls in JSDoc. Change this row to333 and its stale conflict citation282-284 to288-290. The raw function parameters284-287 and command flag descriptors remain excluded. docgenQualityCommand's actual request starts792, but canonical793 validly anchors first all Boolean793 and its note already names the full792-796 span. Do not replace793 merely with the call start or Command.make declaration778.

Source hashes match frozen R37 HEAD/main. Primary transcript matches its execution receipt. Exact old rows/design bytes, new D1 row and metadata replacement are private; disposition.json contains input hashes, dependencies and deltas. No tracked edits/ref changes, launches, implementation, P3 or dry-round credit.
