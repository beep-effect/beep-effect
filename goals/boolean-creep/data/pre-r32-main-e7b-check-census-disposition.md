> Parent integration: the initial local-pair omission rationale below is superseded by [the D1 supplement](./pre-r32-main-e7b-check-census-local-pair-disposition.md). Both the compiler-options D2 correction and the independent local-pair D1 record are installed. All original source evidence and the no-new-qualification result remain intact.

# CheckCensus current-main owner audit

This bounded audit supports **zero new qualifications and one same-id D2 metadata update**. It is source-forward P2 preparation, not a census round, dry-round receipt, P3 review or implementation. No eight-section implementation design is authored because no owner in the audited module passes the Boolean net plus evidence gate. No existing qualification, status, cardinality, tier or exposure changes.

The exact source is HEAD `a942d7dab3a962963912247d4699b7986bcb9c03` over main `e7b7d03e61bd5cddd74e89bb03ae10dbe06e067b`, compared with main `284294ee24177f13f6d3a763c5d987206d351c51`. Inventory input is SHA-256 `fc4a37c261318d37cc276315d7014c61410277c7802b38806b72ae3a9437fc13` (751 rows / 145 qualified). All source and dependency citations resolve against full copies under `frozen/`; previous source/test bytes live under `previous-main284/`. `bindings.json`, `source-anchor-map.json` and `API-SPANS.json` bind their complete contents and exact spans.

For readable citations below, **C** means `packages/tooling/tool/cli/src/commands/Quality/CheckCensus.ts`; **T** means `packages/tooling/tool/cli/test/check-census.test.ts`. Citation numbers always refer to the current source, except when explicitly marked previous. The complete 868-line C and the complete test were inspected, including JSDoc examples. Graft retrieval preceded source discovery; full tracked-file and barrel searches supplement its name-resolved edges.

## Existing inventory and previous-main change

The complete live inventory has one CheckCensus record: `r30-cli-commands-l-q-check-census-overlay-compiler-options`, line 706 of the inventory, D2. It currently cites C:411 and lists five compiler flags. The full row's exact original bytes are preserved in `original-r30-cli-commands-l-q-check-census-overlay-compiler-options.jsonl`, SHA-256 `37859b785997bc63b52edf90a8693aaaf380aa05607a550ca4dca09e247fff69`. No current design refers to CheckCensus or this owner; the corresponding `designs/<id>.md` does not exist. The tracked reference inventory also identifies historical R30 rows, inventory archives and source-forward receipts; these are historical projections of this same owner, not independent qualifications.

`proposed-row.jsonl` changes only the source locator, member list and D2 explanatory note. The current compilerOptions object begins at C:584; its first Boolean is C:585. Its complete six-member cluster is `composite`, `incremental`, `declaration`, `declarationMap`, `emitDeclarationOnly`, `noEmit`. The sibling `rootDir` remains a string. Preserve the same id, object-literal kind, symbol, status and D2 class. The proposal is a replacement for the old row, never an append. `proposed-inventory.jsonl` demonstrates the replacement against the exact input inventory; row totals and qualified totals remain 751 / 145.

The six pre-existing class bodies are byte-identical to previous main; `retained-class-bodies.json` proves this independently of changed line positions. The delta adds `CheckCensusMissingOutput`, optional `compilerOptions.outDir` extraction, referenced-config resolution and the filesystem-only built-tree gate; calls the gate before timestamping and compiler measurement; adds the external `emitDeclarationOnly: false` override; substitutes shared `thunkFalse` for two identical fallback callbacks; updates documentation/command description; and adds the unbuilt-tree test. Full delta bytes and old/new declaration mappings are included. The old C:410-417 compiler object maps to current C:584-592. Adding one externally specified flag does not turn this driver configuration into an internal state variable.

## Complete owner disposition

| Owner / field group | Actual complete field types | Disposition and evidence |
| --- | --- | --- |
| `censusProgramFields`, `CheckCensusProgram` | `files`, `upstreamSrc`, `upstreamDist`, `wallMs`, `diagnostics`: five `CensusCount` values | Zero Boolean members. `CensusCount` is annotated `S.Int`, not a Boolean wrapper. Shared-field inheritance is fully included, C:58-100. |
| `CheckCensusDelta` | Same five names, all signed `CensusDelta` integers | Zero Boolean members; arithmetic differences preserve measurements, C:64-68, 118-129, 623-629. |
| `CheckCensusRow` | package string; full overlay and referenceKeeping programs; full delta; integer buildOverlapFiles | The nested models above also have zero Boolean members. No success/error marker is hidden in counts, C:154-165. |
| `CheckCensusReport` | generatedAt string, concurrency integer, array of complete rows | Zero Boolean members; an empty row array is explicitly shown in its public example, C:173-191. |
| `CheckCensusPackage` | name string, dir string | Zero Boolean members, C:208-216. |
| `CheckCensusOptions` | repoRoot string, tsgoPath string, packages array, integer concurrency with constructor default 4 | Zero Boolean members, including nested packages; no request flag pair, C:238-248. |
| New `CheckCensusMissingOutput` | package, reference, declarationFile: three strings | Zero Boolean members. The original reference string and relative declaration path are both carried, C:270-279, 466-473. |
| `TsconfigReference` / `TsconfigReferences` | path string; optional array of those records | External JSONC selection, no Boolean fields; absent references becomes an empty array, C:281-291, 386-404. |
| New `TsconfigOutDir`, including its inline nested Struct | optional compilerOptions object with optional outDir string | No Boolean fields; the name compilerOptions does not import TypeScript's entire shape into this narrow schema, C:406-413. |
| `TsconfigSelection` | optional include and exclude arrays of strings | No Boolean fields, C:544-563. |
| `measureDiagnostics` result | wallMs and diagnostics numbers | Numeric measurements, not flags. Nonzero/zero observations do not create Boolean members, C:319-340. |
| `measureProgram` result | complete CheckCensusProgram plus HashSet of strings | The set and nested counts contain no Boolean cluster, C:356-384. |
| `withReferenceKeepingOverlay` outer serialized object | extends string, include/exclude arrays from selection, references array, compilerOptions object | Whole object considered. Only its six compilerOptions flags meet the net; they mirror external TypeScript configuration and remain D2, C:570-600. |
| `hasCheckOverlay` local destructured pair | two independent filesystem `exists` Booleans | Both paths may be present, absent, or present independently: representable 4, legal 4. The pair is transient and returns only a conjunction. There is no eligible correlated state or cardinality gap, C:747-758; installed FileSystem.ts:140-145. |
| `missingReferenceOutputs` local `exists` | one filesystem Boolean, then an empty array or one three-string record | No returned `{exists, payload}` bag or sibling Boolean. The record array already carries the result. Do not invent a second flag or a readiness owner, C:446-477. |
| Standalone predicates / inline conditions | diagnostic regex, isSrcFile, isDistFile, nameMatches, signed-number rendering, Option/directory test, collection non-emptiness | Separate predicates across different scopes are not one state owner. Conditions involving counts or Option tags do not Boolean-type their inputs, C:295-298, 345-354, 423-426, 522, 704, 760-764. |
| Process / filesystem option objects | capture calls provide only trim:true; cleanup only force:true; output directory creation only recursive:true | Each actual call object has at most one Boolean. Imported RunCapturedOptions supports extendEnv/trim/tee, but its optional fields are owned by StepExec, not inherited into C's call literals or report. CapturedStep has one truncated Boolean that is not copied into any C result, C:300-338, 599, 825-827; StepExec.ts:133-143, 436-443, 884-892. |
| Command flags and formatting arguments | output path string, optional filter string; column widths numbers | No Boolean command input owner or correlated status model, C:684-702, 804-831, 851-868. |

The independent filesystem table is in `independent-filesystem-table.json`. It proves the four cases as ordinary API observations, not as four accepted package selections: selection intentionally requires both files. Error-to-false fallback means a failed existence check also produces a negative observation; this does not establish an implication between the two paths. No new canonical D1 row is proposed for transient local predicates absent a scanner row needing reconciliation.

For the D2 record, a `2^6` syntactic product is not an internal legal-state claim. The single fixed writer is not evidence that the compiler domain has exactly one legal state. The external `CompilerOptions` declaration explicitly defines all six Boolean keys (installed TypeScript typescript.d.ts:7014-7102), and C serializes those exact property names/values to a tsconfig passed to the compiler. D2 does not require inventing a finite legal count for an external compiler's diagnostic input domain.

## Producers, consumers and behavior that remains intact

`selectCheckCensusPackages` obtains workspace packages, applies the optional name filter, sorts names and filters for both tsconfig files (C:783-802). Its `CheckCensusPackage.make` writes both name and directory. `runCheckCensusCli` constructs full options and supplies the installed repo `.bin/tsgo`; public `runCheckCensus` and `assertCheckCensusTreeBuilt` also accept callers' complete Options values (C:515-519, 658-674, 811-818). The report API is not restricted to the CLI constructor's one configuration.

The new gate reads every selected package's `tsconfig.json`, decodes reference path records and checks each reference sequentially within that package (C:386-404, 446-477). It handles either a reference naming a directory or a config file. `fs.stat` failure becomes None and falls through to the original resolved path (C:416-426). It reads the referenced file, decodes only explicit outDir, and uses the documented `dist/index.d.ts` fallback when outDir is absent; it does not traverse extends or prove all possible declaration outputs. Existence errors become false. These actual behaviors are preserved without strengthening the gate or replacing diagnostic failures.

Missing-output records retain package name, original reference path and repo-relative declaration file. The outer traversal uses options.concurrency. On nonempty missing results, the error message preserves total missing-reference count, distinct package count, ordered detail lines, the 40-entry truncation rule, and both full-build and filtered-upstream remediation text (C:479-537; ArtifactIo.ts:183-199). `QualityScriptCommandError` preserves its tag plus inherited message, optional command, optional finite exitCode and optional cause, with runtime exit default 1 (Quality.errors.ts:140-175; CommandErrorFields.ts:68-73). It does not contain a second Boolean hidden by shared fields.

`runCheckCensus` gates before timestamping, parallel package measurement and report creation (C:672-682). Per package, execution remains committed check-overlay measurement, canonical build file listing, then temporary reference-keeping overlay measurement (C:608-617). Each measured program lists files and then measures diagnostics. Temporary config serialization keeps extends, the full include/exclude selection, full references, six compiler flags and rootDir; acquire/release writes it with a newline and removes it even when use fails, ignoring cleanup failure as before (C:580-600).

Every CheckCensusRow retains both full five-count programs, all five arithmetic deltas and buildOverlapFiles (C:619-631). `runCheckCensus` preserves generatedAt, supplied concurrency and ordered rows (C:676-681). Human rendering retains its current columns and signs; the JSON writer retains the complete report even though some delta fields are omitted from the table. The CLI serializes the report first, prints its table, creates the output directory, writes pretty JSON plus newline, then prints the output path (C:819-831). No Boolean or numeric field is projected out and no operation is rescheduled.

## Public codecs, exports and encoded compatibility

The seven classes export actual Schema codecs through source deep imports. Their class codec has an encoded field shape derived from its Struct and a constructed Type (installed Schema.ts:14317-14344). `CheckCensusOptions`' default applies during make operations only, not decode/encode (Schema.ts:5798-5830). `CensusCount`'s prose says nonnegative, but the actual schema is only `S.Int`; it does not add a nonnegative refinement. No new positivity check, cross-counter coherence check, array nonempty constraint, timestamp parser, path refinement, or decoding default is proposed. Structural decoder acceptance is documented separately from a claim that every arbitrary number combination is meaningful census output.

The JSON report is currently serialized through `jsonStringifyPretty(report)`, not a dedicated `CheckCensusReport` encode call (C:819-822). JsonUtils.ts:17-18,39-46 uses `SchemaGetter.stringifyJson({space:2})`; installed SchemaGetter.ts:1057-1101 documents generic JSON serialization and its failure channel. The metadata-only proposal does not narrow that boundary, change field names, synthesize statuses or add a compatibility sidecar.

Local package exports include `./commands/*` → `./src/commands/*.ts`, enabling deep CheckCensus source imports; `src/test/Quality.test-kit.ts:22` re-exports the module for tests. `Quality.command.ts:60,3931` imports/registers the executable command; its help lists the command at 3903. The Quality facade and root CLI barrel do not re-export CheckCensus model names. The private CLI manifest's publishConfig has only the explicit Quality facade, excludes the commands wildcard and sets test exports to null; do not silently describe the source deep API as a separately published package API or add public exports. All source/test barrels and the complete manifest are frozen.

Tracked reference search found eight non-packet matching files: the owner, command registration, test barrel, test, changeset, coverage baseline, and two historical CI-fleet document/comment references to a similarly named build-mode census. Those last two are not CheckCensus imports or report readers. There is no additional tracked report decode/encode consumer, document example or caller in the searched authored extensions. This bounded search is evidence of the actual checkout, not proof that external source consumers cannot exist. Existing exported input/codec contracts remain untouched regardless.

## Guard accounting and test impact

**Guard-deletion credit is zero.** No correlated owner qualifies, and the proposal does not remove any source guard. The following are retained for their actual ownership and purpose:

| Current guard | Retained purpose |
| --- | --- |
| C:297,315 diagnostic-line test | Separate compiler diagnostics from file paths. |
| C:345-354,367-371 path and src/dist predicates | Classify measured paths without combining independent counters into state flags. |
| C:425 Option/directory test and C:464 existence branch | Resolve a reference path and create missing-output entries from observations. |
| C:522 missing array non-emptiness | Preserve the built-tree diagnostic boundary. |
| C:758 overlay && build | Select packages with both required configuration files. |
| C:760-764 optional name filter | Preserve raw optional filter semantics. |
| C:704 sign comparison | Format a number, not validate a Boolean state. |
| ArtifactIo.ts:193-195 omittedCount > 0 | Render a bounded error list's overflow line. |

Existing test T:81-145 builds a real referenced package, runs both measurements, asserts source-vs-declaration counts and deltas, verifies temporary-overlay cleanup and renders the report. New test T:147 onward runs the unbuilt fixture through the public API, checks the typed diagnostic, exact missing-output path, full-build remediation text and absence of the temporary overlay/upstream dist. The source and annotation establish that the gate precedes spawns; the new test does not directly count spawn invocations. These tests were read in full and **not run**. No product/package tests, compiler runs or code edits were authorized or performed. The separate private inventory validation is packet validation only.

Neither test proves directory-form references, absent outDir, failed stat/exists operations, >40 missing references, every raw Options/codec input, or universal external compiler flag combinations. Those limits do not require a new Boolean design and are not represented as fresh test credit. Fixture compiler-option objects remain excluded from corpus scanning and continue to exercise the raw external tsconfig contract.

## Handoff and limits

Install only the same-id D2 row replacement after the parent verifies the exact source/inventory binding. No design file is to be installed, archived or retired for this owner. No implementation prerequisite, helper, schema, service, new public carrier or approval is requested. Other agents own TsconfigSync and TsconfigOverlay/PackageShell; this audit only reads cross-file evidence. The Constitutional hold and every earlier private bundle stay sealed.

The source/dependency hold ends with the manifest's drift check. Concurrent parent packet edits are recorded as context rather than incorrectly treated as source drift. Advisory `.repos/effect` snapshots carry their own SHA and commit; installed APIs are bound separately. Installed and advisory packages both say rc.112 while their Schema source bytes/line numbers differ, so source claims use the exact installed spans and compare only the relevant advisory behavior. No advanced transformation API is proposed. `retained-evidence-map.json` verifies 15 byte-identical artifacts against the sealed Constitutional bundle without importing its qualification conclusions.

The dependency snapshot also distinguishes installed TypeScript declarations (typescript 6.0.3) from the CLI runtime: repo `.bin/tsgo` is the frozen tsgo-shim, which delegates to installed `@effect/tsgo` 0.39.1; `@typescript/native` is the 7.0.2 package and has no `lib/typescript.d.ts`. No compiler binary was invoked. The six external keys are supported by the captured compiler-options declaration and the actual tsconfig writer/consumer, without claiming a compiler-version-specific universal legality table. The initially guessed native-preview package is absent; `binding-start.json` records that harmless read miss and the actual packages are frozen.

At the ending check, advisory SCHEMA.md and Schema.ts changed concurrently. `ADVISORY-MOVEMENT.json` binds full ending copies and compares the two advisory claims actually used: the integer implementation and make-only constructor default implementation/documentation remain exact. Original source citations still refer to the original frozen snapshots. All 50 protected bindings (including installed Effect) are unchanged; no stale advisory-source stability claim is made.

Both complete advisory spans used in API-SPANS.json are byte-identical in the ending Schema.ts snapshot, not just behaviorally similar: the constructor-default span and integer span have exact matches at the ending locations recorded in ADVISORY-MOVEMENT.json. The advisory file-level drift therefore does not alter either cited span. Installed source remains authoritative.
