# Entire foundation-modeling-rest report scope audit

All15 raw records are excluded generated surfaces, not eligible D1/D2 census
rows: one Html.meta.ts candidate and14 Html.model.ts D2 claims. Both paths are
explicit members of the actual R33 lane-map excludedFiles and absent from
includedFiles. See lane-scope-audit.json for exact IDs, membership and177 included
paths. Html.model.ts line2 GENERATED FILE, generator2398/2448 emits class templates,
generator2687 emits its banner and4187 writes its file. Html.meta template3991
and banner3068 likewise prove generation. README179-183 corroborates both.

This does not automatically invalidate every operation of the lane. Execution
receipt exit0/endTurnEvent/reportValidationExitCode0 and terminal footer are
retained as terminal execution/claimed root coverage evidence. The transcript
has broad field/local Boolean scans over all11 assigned roots plus seed checks.
However its initial enumeration only includes .ts/.tsx and lists184 paths,
of which175 match the177 included lane files. It omits legal-intake.jsonld and
legal-intake.ttl in ontology/src/seed, and no subsequent tool call names either.
Thus full exact177-file semantic coverage is not independently established by
the footer; parent must supplement the two omitted included inputs before
accepting that claim. Structural inventory validation cannot establish corpus
scope or semantic completeness. No dry credit is issued by this audit.

No files in the canonical packet/source were changed. No generator command,
fetch, remote change or proof restart ran. Out-of-corpus rows should be preserved
raw and excluded during parent reconciliation, not rewritten into D2 or deleted.
