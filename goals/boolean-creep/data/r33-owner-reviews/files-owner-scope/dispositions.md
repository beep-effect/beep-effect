# Files owner recall eligibility — R33 private audit

Frozen HEAD32f111f3707a63168b68ed04516af800ecc3a66c/main339da1562a2ed52f73a0a693c176fc52cca9ccb6. No source, canonical packet, git state, or implementation changes. Audit JSON binds source bytes to both frozen revisions. This is eligibility review, not independent P3 or runtime proof.

## Binding distinction

SPEC.md:33-38 requires >=2 actual Boolean members in one scope, separately from a cited E1-E4 precision proof. SPEC.md:58-60 permits independently adjudicated clusters within one declaration. DECISIONS.md:117-120 explicitly admits E3 flag/payload aliases and E4 ordered-flag implications. A qualifying cluster need not itself contain two Booleans when the real enclosing owner meets recall. Conversely, optional payload presence, enum variants, nested array elements and return values are not extra direct Boolean members. Full-owner eligibility does not ratify a producer-only public-domain restriction.

## Withdraw three historical qualified rows and designs

- `files-face-detection-presence`: complete `DetectFacesEntry`, DetectFaces.schemas.ts:169-189, has exactly one Boolean, `hasFace`175. `faceCount` is integer; `faces` and `flags` arrays; primary and moved fields optional payload. Analysis.ts:124-145 derives the primary payloads and bit; Apply.ts:755-780 constructs move-target payload. Those correlations do not supply a second Boolean. No D1/D2 census replacement: outside recall net.
- `files-border-classification`: complete `DetectBordersEntry`, Borders.schemas.ts:395-410, has exactly one Boolean, `hasBorder`400. Classification is a literal domain, borderCount integer, sides array. Analysis.ts:31-50 projects border count/classification/bit; enum alternatives and nested side flags are not siblings of `hasBorder`. No census replacement.
- `files-person-reference-disposition`: complete `PersonMatchReference`, MatchPerson.schemas.ts:1254-1266, has exactly one Boolean, `accepted`1258. Face count, optional score and optional reason are payload. MatchPerson.ts:1257-1332 validates path before accepted/rejected evidence and counts accepted names. The returned validation Boolean and loop-local `accepted` are different owners, not added members of the schema. Historical public/worker semantic-boundary tightening does not bypass recall. No census replacement.

`historical-rows.jsonl` preserves all five exact input rows; `inventory-removals.json` and `design-archive-list.json` identify the three removals. Private exact design copies are in design-archives/. This does not authorize deleting semantic checks or changing worker DTO compatibility.

## Retain threshold owner

`files-worker-score-thresholds` remains qualified16/6. The complete body of `isWorkerDispositionCoherent`, MatchPerson.ts:1384-1405, has six simultaneous Boolean locals: four score-threshold predicates1389-1392, `hasQualityFlags`1393 and `hasPartialAlignerRejection`1394. The latter two remain independent and must be preserved, not restricted to threshold cases. Ordered thresholds are enforced587-594 and invoked651; tolerance is0.000001 at70. The shared finite score produces four threshold bits with B=>A, A=>C, B=>!D and !C=>D. These implications give six admissible vectors. finite-evidence.json exhaustively enumerates16vectors and provides six concrete score/threshold witnesses. This is arithmetic evidence, not a newly executed product test.

The existing six-literal threshold-band design remains applicable to that minimal cluster. It must preserve each exact floating-point comparison, inclusive/exclusive endpoints, rounding tolerance, face-count checks, independent quality flags and partial-aligner bypass, and no-face/unreadable false cases. Preserve validation ordering: maximumValidatedFaceScore1363-1382, validateWorkerEntryDisposition1456-1466 and validateWorkerEntryEvidence1468-1483. No exported result shape migration or newly forbidden public construction follows from this private helper refactor. Prior historical probe numbers are not fresh verification credit.

## Retain orientation scope, flag design precision for correction

`files-image-orientation-state` is NOT a one-Boolean owner: `ImageAuditMetadataPresence`, ImageAudit.schemas.ts:45-57, has five actual Booleans (hasExif/hasIcc/hasIptc/hasXmp/orientationApplied). It passes recall. The four embedded-metadata flags remain independent. Do not archive it for a false scope failure.

Its historical qualified4/3 and compatibility narrowing are not independently reapproved. `orientation` is optional S.Int and `orientationApplied` unrestricted S.Boolean. All four bit/presence tuples are accepted by the actual public class; present orientation1 versus nonidentity adds a payload distinction omitted by the coarse4/3 account. ImageCuration.ts:438-456 emits absent/false, identity1/false, nonidentity/true, but this producer subset is not the exported construction/decoding contract. ImageAuditEntry112 embeds it in ImageAuditManifest207-223, whose public encode/decode functions231-245 accept that class contract. In particular true/absent, true/identity and false/nonidentity are schema-permitted, even though absent from the producer.

There is also no cited E4 implication between two ordered Boolean phases; the cited code relates one Boolean to a numeric payload value, while E3 specifically concerns presence. Therefore the current design cannot be treated as approved simply because recall is satisfied. Keep this item visible for parent precision adjudication and separate contract-safe correction or withdrawal; do not silently add a public restriction, claim the producer relation authoritative, or change the independent flags. This audit retains eligibility only, not current designed readiness. No proposed canonical replacement row is supplied for this unresolved precision disposition.

### Orientation domain-authority check

Targeted search of tracked docs and CLI source for orientationApplied, ImageAuditMetadataPresence and orientation/applied prose found the schema declaration and producer above; adjacent crop/dimension prose merely describes pixels after EXIF orientation and does not define manifest grammar. The metadata schema JSDoc says metadata-presence/orientation facts but does not specify an iff invariant. There is no cited domain assertion, reader assumption, phase implication, or user-ratified narrowing specific to this owner. The producer itself disproves an exact Boolean/presence E3 alias: orientation1 is present with orientationApplied=false, and nonidentity is present with true. A numeric-value relation is not the currently ratified presence-only E3 or ordered-flags E4. The failure therefore is not inferred solely from schema permissiveness: the actual cited precision evidence also does not establish the claimed evidence class.

Recommendation: HOLD existing qualified design for explicit precision adjudication; do not count it P2-ready. If integrating only dispositions established by this bounded audit, retain the scope census and withdraw qualification rather than implementing the old narrowing. A new public grammar would need explicit authority; this audit does not seek or fabricate it. This hold is separate from the three definite recall removals in inventory-removals.json. No design archive for orientation is requested until the parent adjudicates that precision disposition.
