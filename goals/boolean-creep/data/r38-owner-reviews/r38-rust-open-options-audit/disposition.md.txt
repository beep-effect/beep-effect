# R38 Rust OpenOptions owner audit

Reject r38-apps-webkit-extension-open-options as outside campaign recall; do not admit a D2 row. No replacement or design is needed.

Complete callsite: prepare_youtube_web_extension284–354 prepares a private revision directory, validates the existing extension path, compares bundled bytes, and if necessary writes a temporary file before rename. At329–333, std::fs::OpenOptions::new() is followed by create_new(true), write(true), Unix mode(0o600), and open(&temporary_path)?. These are Boolean arguments to two external builder methods. No Rust struct literal or repository-owned type with Boolean members is declared here. The local binding is named temporary, not temporaryFile, and receives std::fs::File after open succeeds. Later write_all, sync_all and rename consume that File. There is no actual object-literal declaration corresponding to the submitted symbol/kind.

The standard library builder maintains options internally, but that external implementation is outside packages/apps source. It does not turn this repository's method flag arguments into authored Boolean fields. SPEC39 and DECISIONS22 exclude function flag parameters; methods also take function arguments. A D2 external-contract disqualification is available after an eligible owner is identified; it does not waive recall. This is unlike a source object literal passed to an SDK with two actual named Boolean fields.

The installed Rust1.98.1 standard-library documentation confirms create_new(&mut self, bool) and write(&mut self, bool) mutate the builder and open returns a File result. The raw true/true call is not evidence that all option combinations are legal. open documents InvalidInput for unsupported combinations, including missing access mode and creation without write/append access. No cardinality or public-grammar narrowing is proposed: builder argument acceptance and successful file-open semantics are distinct. The external API details only corroborate call shape; rejection follows the explicit repository scope boundary.

Canonical inventory has no OpenOptions or proposed-ID duplicate. No existing row/design is withdrawn. Current source matches frozen HEADab77f10cb74ad96404b05d1ab5a1d4968cef847b/main9a1bd3805574bca087d4da09ece4a628038afb27; exact raw, primary transcript, execution receipt, policy, inventory, and local Rust reference hashes are bound in JSON. No tracked changes, ref moves, P3 or dry credit.
