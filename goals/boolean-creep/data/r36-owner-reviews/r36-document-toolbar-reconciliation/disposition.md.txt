# R36 document toolbar admission — technical proof resolved

Supersedes the provisional evidence-insufficiency disposition, retained verbatim under history-provisional-rejection. Admit one new qualified call-site cluster: sessionOpen/dirty,4 representable versus3 legal states. The complete actual object contains5 Boolean fields; factor out its3 independent action waits. The public function parameter remains excluded and accepts all32 tuples.

Installed @effect/atom-react4.0.0-rc.117 Hooks.ts:31-62 and170-177 routes each useAtomValue through the same RegistryContext and useSyncExternalStore snapshot calling registry.get. Effect rc.117 AtomRegistry.ts:488-490 reads node.value; value750-780 recomputes stale nodes; dependency registration983-991 links dirty to session; setValue805-837 invalidates children synchronously through861-889. React19.3.0 react-dom-client.development.js:8491-8572 reads snapshots anew during both mount and update, rather than returning unrelated cached previous hook results. The component function executes synchronously without yielding; only the pure workbench ontologyPathAtom read lies between dirty121 and session123. There is no intervening write. Therefore a prior session update cannot leave dirty stale in this object. The atom's own1531-1541 None=>false rule extends to this same registry observation. Neither atom has a server override; Atom.getServerValue3208-3245 falls back to the same registry. React consistency checks additionally prevent committing stale external-store renders. No blanket assertion about arbitrary cross-store atomicity is made.

The reference Effect checkout has the same Hooks snapshot architecture but newer import paths; installed hashed rc.117 artifacts are the authority, bound alongside bun.lock. No dependency was upgraded or fetched.

The precise owner is OntologyDocumentRegion.toolbarInput at146, not documentToolbarState.input's excluded formal parameter. The public helper's closed+dirty input remains supported and renders No document. The attached design coalesces session/dirty via one derived phase atom for the component, preserves the public adapter32-domain, and shares one projector with the existing14-state output design. A full replacement sibling design includes the coordination amendment. A finite proposed-algorithm table proves all32 public tuples preserve prior presentation semantics; it is not runtime implementation proof.

Artifacts: proposed-rows.jsonl; full new design; document-toolbar-busy-disabled.replacement.md; compatibility-table.json; exact input/source/dependency hashes in disposition.json. Delta+1Q/+1design. No tracked edits, runtime implementation, P3 or dry-round credit. No remaining source-coherence question in this bounded candidate.
