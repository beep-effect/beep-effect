---
goal: identity-iri-fibered
agent: claude
date: "2026-08-25"
trigger: closeout
confidence: high
findings:
  - category: goal-critique
    confidence: high
    instruction: >-
      Verify every target-surface path in a graduated SPEC with a live rg/ls at graduation
      time, and record donor provenance as a commit hash when the donor is scratch material.
    explanation: >-
      The SPEC placed JSDocTagDefinition.make in @beep/schema; it lives in @beep/repo-utils
      (packages/tooling/library/repo-utils/src/JSDoc/models/JSDocTagDefinition.model.ts). The
      MAP's donor scratchpad/identity/ no longer existed in any working tree, so the fibered
      slice had no donor and was authored fresh from handoff section 6 plus the live make body;
      the fold reflection had already flagged the same donor-provenance gap.
  - category: tooling-friction
    confidence: high
    instruction: >-
      Codex sandbox lanes must pass --pool=threads on the Vitest command line (or the prompt
      must forbid editing vitest.config.ts) instead of patching committed test config.
    explanation: >-
      Both lanes hit "[vitest-pool]: Failed to start forks worker / Timeout waiting for worker
      to respond" inside codex exec -s workspace-write and self-patched three package
      vitest.config.ts files to pool threads. Outside the sandbox the unchanged fork pool passed
      every package first try (identity 103/103, repo-utils 221/221, semantic-web 15/15,
      epistemic-server 32/32); the operator had to revert all three edits before verification.
      Receipt in research/OPPORTUNITIES.md.
  - category: tooling-friction
    confidence: medium
    instruction: >-
      Treat a dirty bun.lock in the working tree as a verify-path hazard and attribute it before
      launching lanes; bun run docgen:local refuses to run and prescribes --full.
    explanation: >-
      A pre-existing, unrelated lockfile drift (a stale @serwist/next/next block) made both lanes
      fall back to docgen:local --full (127/127, several minutes each) instead of the bounded
      loop, and each lane spent turns diagnosing a refusal that had nothing to do with its work.
  - category: implementation-improvement
    confidence: medium
    instruction: >-
      Widen IdentityEntry.fibers from Record<string, string> to S.Json fiber parts (rdf:JSON
      literals in the RDF codec) once a consumer needs structured sections; keep the registry's
      single resolve method.
    explanation: >-
      v1 fibers are string-valued so the dataset codec is exact with plain literals and the SHACL
      projection can express a required fiber as minCount 1 per bound predicate. Fibered
      sections are structured records, so bridging them into the registry today means projecting
      a policy's keys to strings (the bridging test shows the pattern).
  - category: implementation-improvement
    confidence: medium
    instruction: >-
      Use effect/Struct.pick for key-list projections over decoded records instead of
      Reflect.set or R.fromEntries accumulators.
    explanation: >-
      Codex's first Fibered.project mutated an R.empty() accumulator via Reflect.set; the pure
      R.fromEntries rewrite failed tsgo because keyof T includes number and fromEntries widens
      literal keys. Struct.pick(self, keys) is the data-first, Pick-typed helper the law already
      asks for.
  - category: codification-todo
    confidence: medium
    instruction: >-
      Add the Yeet staging rule to the lane-orchestration checklist; stage the reviewed intent
      (including packet flips, reflection, goals index --write) BEFORE the proof that
      publish --reuse-verified will reuse.
    explanation: >-
      The proof fingerprint hashes git status, diff HEAD, and diff --cached together, so staging
      after a green verify invalidates it; this session ran an exploratory verify first and paid
      a second full proof for the staged tree.
todos:
  - >-
    Add a --pool=threads (command-line, not config) instruction to the Codex lane prompt
    template used for beep-effect implementation lanes.
  - >-
    Widen IdentityEntry.fibers to S.Json with rdf:JSON literals in IdentityRdfBinding when the
    first structured-section consumer appears.
  - >-
    Wire Fibered as the second consumer over the vocab registry (base = prefixes, section =
    iri + terms) as handoff section 6 item 5 anticipated; out of this goal's slice.
---

# Reflection — identity-iri-fibered (2026-08-25, claude)

## Summary

The full ratified MAP row shipped in one branch: discrete `Fibered` kit and exact
`IdentityRegistry` (+ local Layer) in `@beep/identity`, `JSDocTagDefinition.make`
migrated onto a single-point `Fibered` with a 113-tag golden proving byte-identity,
and in `@beep/semantic-web` an explicit identity↔RDF binding, a dataset-backed
dev/test registry Layer, and a policy → `ShaclNodeShape` projection proven
end-to-end against the live bounded validator. P0 cleared both semantic-web
blockers with live-source evidence before any implementation began.

## Tooling experience

- **Worked:** design-first packet flow (P0 audit → binding design doc → two
  sequential Codex Sol xhigh lanes with XML operator prompts and file reports);
  the golden-harness-before-migration order; `@effect/vitest` `it.layer` over the
  live `BoundedShaclValidationServiceLive`; the Effect language-service rules in
  the root `bun run check` caught test-side diagnostics the package checks missed.
- **Didn't:** the Codex sandbox's Vitest fork pool (worker startup timeout) — lanes
  patched committed config to get green; `docgen:local` refused on a pre-existing
  dirty `bun.lock`.
- **Frustrating:** every lane spent turns diagnosing environment-only failures
  (forks pool, lockfile preflight) that the operator then had to revert/attribute.
- **Wished existed:** a Vitest startup probe that names the remediation; a lane
  prompt template with the sandbox rules baked in.

## Implementation improvement opportunities

- `IdentityEntry.fibers` → `S.Json` fiber parts with rdf:JSON literals.
- Second `Fibered` consumer over the vocab registry; third over RDF term
  definitions (handoff §6 item 5).
- `layerLocal` builds its indexes with an imperative loop; an `Effect.reduce`
  over `Result` would read closer to the law without changing behavior.

## Goal & prompt critique

- GOAL.md said "byte-identical JSDocTagDefinition migration" without naming the
  file; SPEC named the wrong package. Propose: "migrate
  `packages/tooling/library/repo-utils/src/JSDoc/models/JSDocTagDefinition.model.ts`
  `make` onto `Fibered` (golden harness first)".
- The blocker note asked for "landed-content evidence" without naming the PRs;
  the memory file `semantic-web-package-fate` already mapped PR2 → #695 and
  PR3 → #687 — the packet should have carried that mapping at graduation.

## TODOs worth codifying

- Sandbox lane rule: `--pool=threads` on the command line, never in config.
- Lane-orchestration checklist: stage intent → verify → `publish --reuse-verified`.

## Lessons (confidence-tiered)

- **HIGH — Critical:** Write the golden harness against the unmodified
  implementation and never regenerate it — the migration then has a mechanical
  byte-identity proof instead of an argument.
- **MEDIUM — Best practice:** Keep the registry's fiber parts exact and boring
  (strings) until a consumer forces structure; the RDF codec and SHACL projection
  stay total and round-trippable.
- **LOW — Consideration:** Effect LS rejects duplicated segments in
  `Context.Service` keys (`@beep/identity/IdentityRegistry/IdentityRegistry`);
  design docs should write the shorter deterministic key.
