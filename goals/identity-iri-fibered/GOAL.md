# GOAL: Ship discrete Fibered identity retrieval

Outcome: the full ratified identity-iri-fibered MAP row ships: discrete
Fibered, byte-identical JSDocTagDefinition migration, exact IdentityRegistry,
post-move SHACL projection, and test/dev store Layers.

Read this packet, the identity exploration, shipped core/fold contracts, and
repo instructions first.

P0 is blocked until both semantic-web PR2 and PR3 cleanups are proven landed;
they have no goal packets, so use live source/content evidence and update the
blockedBy note before implementation.

Do not add version fibers, fuzzy retrieval, old ontology APIs, runtime IRI
computation, general JSON-LD import, or production store choices.

After blockers clear, implement the smallest full-row slice, verify
byte-identical migration and layer behavior, then publish through Yeet and
close with a reflection.
