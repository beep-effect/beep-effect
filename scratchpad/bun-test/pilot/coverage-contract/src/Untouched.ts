/**
 * Provide an intentionally unimported fixture module for coverage denominator checks.
 *
 * **Example** (Observe untouched)
 *
 * ```ts
 * import { untouched } from "@beep/scratchpad/bun-test/pilot/coverage-contract/src/Untouched"
 *
 * console.log(untouched()) // 42
 * ```
 *
 * @category testing
 * @since 0.0.0
 */
export const untouched = (): number => 42;
