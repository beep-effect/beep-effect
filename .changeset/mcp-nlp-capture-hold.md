---
"@beep/mcp-kit": patch
---

Record the nlp-mcp protocol hold in the consumer table: the D-cli-contract capture shows Claude
Code opens stdio with a legacy `initialize` by default, so nlp-mcp stays on `2025-06-18` behind
a re-entry gate.
