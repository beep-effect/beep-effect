---
"@beep/lexical-schema": patch
---

Crispen `@beep/lexical-schema` for the P2 repo-crispening wave: move artifact link URI grammar and Lexical link URL sanitization into schemas, derive text bit domains and list dispatch from schema kits, colocate Lexical decode statics, and add schema-derived parity laws. Public Type/Encoded shapes remain string-compatible while unsafe link URLs now normalize at the Lexical schema boundary.
