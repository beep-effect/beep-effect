---
"@beep/rdf": patch
---

Crispen `@beep/rdf` for the P2 repo-crispening wave: add schema-owned Web Annotation boundary codecs, move language-tag normalization and optional-field constructor defaults into schemas, colocate codec statics on public RDF/IRI/URI/JSON-LD/PROV schemas, and add encoded-shape plus `S.toArbitrary` parity coverage while preserving public helper signatures and deferring cross-package public-form ripples.
