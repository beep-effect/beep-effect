---
"@beep/nlp-mcp": patch
---

Crispen `@beep/nlp-mcp` for the P2 repo-crispening wave: move streaming, dataset, JSONL, and pipeline helper defaults into schema-owned option models, model optional dataset metadata and load-error causes as `Option` fields while preserving omitted-key encoded payloads, colocate literal/output codec statics, tighten streaming numeric domains, and add package-local encoded-shape plus `S.toArbitrary` parity laws for the touched schemas.
