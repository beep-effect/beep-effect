---
{}
---

commitlint-subtree-ignore: version-neutral CI config fix — a second, line-anchored ignore predicate for GitHub-squash-merged `git subtree --squash` bodies (marker line AND `git-subtree-dir:` trailer), plus a repo-cli regression test and its `@commitlint/is-ignored` devDependency.
