---
"@beep/test-utils": patch
---

Crispen `@beep/test-utils` for the P2 repo-crispening wave: move the PGLite layer mode into a `LiteralKit` schema, carry the external PostgreSQL URI invariant and field annotations on the SQL harness schemas, colocate config/error statics, and add encoded-shape snapshots plus `S.toArbitrary` parity laws while preserving public helper signatures and encoded wire shapes.
