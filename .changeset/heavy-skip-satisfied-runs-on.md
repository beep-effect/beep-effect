---
"@beep/repo-cli": patch
---

Heavy lanes pass without work on a hosted runner for docs-only pull requests instead of a job-level skip, which reported a single `Heavy / matrix.name` context and left the required lane contexts "Expected". The settle detail now says `lanes pass without work`.
