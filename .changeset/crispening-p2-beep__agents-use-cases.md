---
"@beep/agents-use-cases": patch
---

Crispen `@beep/agents-use-cases` for the P2 repo-crispening wave: move runtime evidence/activity absence to schema-owned `Option` fields while preserving encoded optional keys, model deterministic runtime scenario ids with `LiteralKit`, colocate the candidate output-set decoder, tighten assistant block indices and runtime DTO ids/dates/version/title fields, add field annotations, and add package-local encoded-shape plus `S.toArbitrary` parity laws. Public encoded wire shapes remain unchanged; the `AssistantBlock.fromUnknown` static-colocation ripple is deferred to the family-close sweep.
