---
"@beep/ai-provider-cli": patch
---

Crispen `@beep/ai-provider-cli` for the P2 repo-crispening wave: move provider CLI defaults into the path schema, model optional diagnostics as `Option` fields while preserving encoded error payloads, reattach LiteralKit statics after annotation, tighten provider exit-code and command fields, add field-level schema annotations, and add package-local wire-shape plus `S.toArbitrary` parity laws. Public encoded wire shapes remain unchanged.
