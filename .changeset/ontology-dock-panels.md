---
"@beep/professional-desktop": patch
---

The ontology surface splits into nine dock panels with a nav-rail panel
menu, a core-cluster default layout, and a v2 snapshot key (stale v1 keys
are deleted at boot).
