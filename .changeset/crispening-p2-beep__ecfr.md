---
"@beep/ecfr": patch
---

Crispen `@beep/ecfr` for the P2 repo-crispening wave: move explicit config defaults into the schema boundary, colocate eCFR error literal/statics and Option-backed error context, tighten HTTP status context to non-negative integers while preserving numeric encoding, add schema-derived encoded-shape laws, and update the package generator to emit Match-based schema selection plus class guards.
