---
"@beep/data": patch
---

Crispen `@beep/data` for the P2 repo-crispening wave: derive keyboard shortcut command labels from the command-name source, reduce the label formalizer to the Effect array helper, hoist MIME lookup into a point-free pipeline, and tighten internal MIME metadata to finite source values plus non-empty extension arrays while preserving the public MIME facade type and lookup signature.
