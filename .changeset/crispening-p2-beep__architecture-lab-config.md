---
"@beep/architecture-lab-config": patch
---

Crispen `@beep/architecture-lab-config` for the P2 repo-crispening wave: move WorkItem configuration defaults and non-empty identifier constraints onto the schemas, add field annotations for the exported config classes, and add schema-derived encoded-shape parity tests for the resolved WorkItem configuration values. Public runtime config object keys and default encoded output remain unchanged.
