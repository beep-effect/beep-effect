---
"@beep/form": patch
---

Crispen `@beep/form` for the P2 repo-crispening wave: move the `ValidateOn` validation-slot domain from a type-only union into a schema-owned `LiteralKit`, route validator selection through the derived match helper, and add encoded-shape plus `S.toArbitrary` parity coverage. Public string values and type imports remain unchanged.
