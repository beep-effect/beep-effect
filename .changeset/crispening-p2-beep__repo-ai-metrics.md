---
"@beep/repo-ai-metrics": patch
---

Crispen `@beep/repo-ai-metrics` for the P2 repo-crispening wave: move transcript and private metadata codecs onto schema-owned models, replace manual literal-domain dispatch with exhaustive kit matches, colocate codec statics on public union schemas, normalize private Codex attribution metadata through schemas while preserving public wire shapes, and add package-local encoded-shape plus `S.toArbitrary` parity laws. Public-form numeric, annotation, archive-key, and exported Option/default migrations that require broader CLI/infra or persistence sweeps are recorded as deferred exceptions.
