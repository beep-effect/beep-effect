---
"@beep/skill-contract": patch
---

Add the deterministic SKILL.md projection (`projectSkillDocument`, `renderSkillMarkdown`) with the narrow frontmatter decoder and the two-check `verifySkillArtifact` verdict; `@beep/md` becomes a dependency.
