---
"@beep/repo-ai-metrics": patch
---

Admit `cursor-cli` as a `HookPulseAgentKind` so Cursor agent lanes can write hook-pulse ledger rows
through the new `.cursor/hooks/hook-pulse.sh` adapter (goals/agent-pool-doctrine).
