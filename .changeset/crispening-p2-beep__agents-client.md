---
"@beep/agents-client": patch
---

Crispen `@beep/agents-client` for the P2 repo-crispening wave: model the thread-creation atom write payload as a schema-owned structural contract, move streaming-turn edit absence into a constructor default, annotate client request/edit schema fields, replace the OTLP empty-string sentinel with `Option`, and add package-local encoded-shape plus `S.toArbitrary` parity laws. Public atom payload and encoded schema wire shapes remain unchanged; cross-package error-normalization/static-colocation ripples are deferred to the family-close sweep.
