---
{}
---

No release: land the ci-fleet-endgame P1 bridge — the github-aws-runners
v7.10.1 module consumed via Pulumi's terraform-module SDK, the
CiFleetController component (shadow label, ephemeral JIT, spot diversified,
reaper-tag propagation), packet secret references, and the shadow probe
workflow. The controller is deployed; cutover waits on shadow acceptance.
