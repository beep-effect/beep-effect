---
"@beep/face-detection": patch
---

Crispen `@beep/face-detection` for the P2 repo-crispening wave: colocate codec statics and request decoders on schema-owned models, move raw confidence normalization into a schema codec, model error context absence with `Option` defaults and a finite operation domain, tighten image geometry/preprocessing numeric schemas, preserve encoded-shape snapshots, and add package-local `S.toArbitrary` encoded round-trip laws.
