---
"@beep/anthropic": patch
---

Crispen `@beep/anthropic` for the P2 repo-crispening wave: move language-model defaults into the schema, tighten Anthropic pricing and repair-error fields with annotations and numeric/non-empty constraints, preserve encoded-shape snapshots, and add `S.toArbitrary` encoded round-trip laws for the package-owned schemas.
