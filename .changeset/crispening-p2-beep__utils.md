---
"@beep/utils": patch
---

Crispen `@beep/utils` for the P2 repo-crispening wave: reconcile already-applied defaults, Option, static-branching, annotation, and numeric-domain fixes in the package inventory, and add encoded-shape snapshots plus `S.toArbitrary` round-trip parity laws for the reusable glob, filesystem option, and path-input schemas. Public helper signatures and encoded wire shapes remain unchanged.
