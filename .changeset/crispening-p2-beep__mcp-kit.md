---
"@beep/mcp-kit": patch
---

Crispen `@beep/mcp-kit` for the P2 repo-crispening wave: move projection outcomes and dispatch decisions onto schema/Data-derived constructors, colocate class statics and canonical helper values, absorb safe constructor defaults, add pipeable dual helper forms where the sweep is package-local, and add `S.toArbitrary` round-trip parity laws for the touched schemas. Public wire shapes stay stable; cross-family precision and signature ripples are recorded for the family-close sweep.
