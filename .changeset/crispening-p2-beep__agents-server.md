---
"@beep/agents-server": patch
---

Crispen `@beep/agents-server` for the P2 repo-crispening wave: model the incremental scan transition with schema-backed input/result contracts while preserving the public tuple adapter, derive the mermaid declaration allowlist from `LiteralKit`, tighten scanner depth, repair indices, patch-operation counts, and redacted JSON Pointer paths with schema-owned precision, add missing field annotations, and add package-local encoded-shape plus `S.toArbitrary` parity laws. Public encoded wire shapes remain unchanged.
