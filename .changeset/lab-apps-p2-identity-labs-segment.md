---
"@beep/identity": patch
---

Add the marker-fenced generated labs composer segment (empty until the first
lab app registers) so `apps/labs/*` identity composers are machine-synced
without hand edits.
