---
{}
---

Add the schema-first Tailscale driver, including typed process failures,
normalized status models, Serve and HTTPS helpers, documentation, and a
deterministic Effect Vitest layer. No package release is required because the
driver is private.
