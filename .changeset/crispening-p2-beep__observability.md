---
"@beep/observability": patch
---

Crispen `@beep/observability` for the P2 repo-crispening wave: move local option/default/status invariants onto schemas, colocate guards/codecs on owned schemas, replace null-returning HTTP API status resolution with `Option`, add dual helper forms, and add package-local parity/property tests. Cross-package `NonNegativeInt` statics and exported config-brand ripples are recorded as deferred exceptions for the family-close sweep.
