---
"@beep/pglite": patch
---

Crispen `@beep/pglite` for the P2 repo-crispening wave: tighten the schema-backed `PgliteError` fields with key annotations, non-empty operation/message validation, constructor defaults for optional Option fields, and an S.toArbitrary encoded-shape round-trip law while preserving the encoded wire shape.
