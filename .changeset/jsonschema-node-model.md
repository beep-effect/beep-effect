---
"@beep/schema": minor
---

Add the `JSONSchema` module: a structured, self-recursive draft-2020-12 node
model (`Node`, `NodeCodec`, `SubSchema`, `Document`) with lossless
unknown-keyword preservation, spec-MUST invariants, single-hop local `$ref`
resolvers, depth-gated schema-derived arbitraries, and full property-based
runtime and type tests.
