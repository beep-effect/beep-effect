---
"@beep/editor": patch
---

Crispen `@beep/editor` for the P2 repo-crispening wave: derive Lexical node factory inputs from serialized schema contracts, colocate attachment MIME/rejection statics, move byte-count and capture-limit policy into schemas, annotate chat config fields, tighten menu ids/labels to non-empty strings, route Mermaid rendering through an exhaustive matcher, reuse `SerializedEditorState.decodeOption`, and add encoded-shape plus `S.toArbitrary` parity coverage.
