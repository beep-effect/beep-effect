---
"@beep/repo-cli": patch
---

Follow up on stream console handling with best-effort teardown writes, a JSON stdout writer that honors callback errors and synchronous write failures, and a refs listing that omits verified rows by default with `--verbose` restoring the full listing.
