---
"@beep/identity": patch
---

Crispen `@beep/identity` for the P2 repo-crispening wave: move PN_LOCAL and
base-name normalization invariants into Effect schemas, add runtime vocabulary
schemas, specialize the core CURIE schema codec with finite CoreVocab guards,
and add schema-derived round-trip parity laws while preserving existing helper
signatures.
