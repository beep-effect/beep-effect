---
"@beep/hubspot": patch
---

Crispen `@beep/hubspot` for the P2 repo-crispening wave: move HubSpot base URL normalization and header defaults into schemas, colocate request/response decode statics, tighten HubSpot identifiers, URL fields, status and timestamp checks with annotations, preserve encoded-shape snapshots, and add `S.toArbitrary` encoded round-trip laws for the package-owned schemas.
