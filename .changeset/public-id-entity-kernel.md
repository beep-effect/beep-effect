---
---

No release required: adds private workspace public entity id plumbing and markdown rendering hardening across schema/domain/table proof packages. All touched packages are private; this changeset exists so branch quality can validate the version policy without publishing package releases.
