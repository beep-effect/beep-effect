---
"@beep/repo-cli": patch
---

Seed the `codex.plan` routing binding at `medium` reasoning effort, matching the ratified
`codex.heavy` default, so `beep models check` reports `plan_mode_reasoning_effort = "xhigh"`
copies as stale.
