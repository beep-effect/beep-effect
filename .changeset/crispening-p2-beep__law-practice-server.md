---
"@beep/law-practice-server": patch
---

Crispen `@beep/law-practice-server` for the P2 repo-crispening wave: replace the remaining test discriminator branch with the `DistinctionDetail` schema-derived guard and match helper, reconcile the package inventory, and keep the server package's layer wiring and fixture boundaries unchanged.
