---
"@beep/epistemic-domain": patch
---

Crispen `@beep/epistemic-domain` for the P2 repo-crispening wave: attach schema-owned decode statics to claim-gate and lifecycle unions, model epistemic fixture/model/provider keys as non-empty schemas, add field-level schema annotations, and add encoded-shape plus `S.toArbitrary` parity coverage. Public encoded wire shapes remain unchanged; opaque JSONB snapshot/metadata boundaries stay explicitly carved out.
