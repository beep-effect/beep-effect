---
"@beep/html": patch
---

Crispen `@beep/html` for repo-crispening-orchestration P2: decode optional HTML
attribute absence as `Option` while preserving optional encoded wire keys,
schema-validate generator JSON inputs, annotate generated metadata with the
package identity composer, and add schema-derived round-trip parity laws.
