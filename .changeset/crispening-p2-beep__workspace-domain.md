---
"@beep/workspace-domain": patch
---

Crispen `@beep/workspace-domain` for the P2 repo-crispening wave: tighten workspace fixture keys, imported email timestamp/source-span fields, and turn item lists; absorb the root-turn `parentTurnId` constructor default into the schema; annotate public entity fields; and add package-local encoded-shape plus `S.toArbitrary` parity laws. Persisted wire shapes and SQL-null root-turn semantics remain unchanged.
