---
"@beep/skill-contract": patch
"@beep/identity": patch
---

Add audited fail-closed gate contracts, unsigned evidence receipts, the `$SkillContractId` identity composer, and the first QA judge gate retrofit.
