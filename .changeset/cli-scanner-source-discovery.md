---
"@beep/repo-cli": patch
---

Load compiler options without traversing tsconfig include paths, then add only
the source files matched by each scanner. Reuse this project factory for native
runtime law checks so excluded generated documentation directories are not read.

Cover inaccessible excluded directories and inherited compiler options in the
native runtime regression tests.

Preserve Bun versions derived from the canonical version file in deployment
commands, and recognize quoted literal pins during version-sync checks and updates.
