---
"@beep/langextract": patch
---

Crispen `@beep/langextract` for the P2 repo-crispening wave: colocate grounded
extraction construction on the schema class, replace optional-key record
mutation with `O.getSomesStruct`, convert private alignment misses to `Option`,
remove service-test casts, and add schema-derived parser/alignment parity laws.
Public schema-form changes that require the law-practice consumer sweep are
recorded as deferred exceptions.
