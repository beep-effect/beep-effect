---
"@beep/repo-cli": patch
---

Add the read-only `beep skills provenance shadcn` pilot with the complete
`skills-lock/v2` schema, immutable pinned-content resolution, mode-aware tree
manifests, license verification, local drift and patch summaries, and JSON or
human output.
