---
"@beep/repo-cli": patch
---

Agent-pipeline-velocity operator changes: closeout review gates now default to a greptile-only bot lineup with gate emission following the `--bots` tokens (legacy CodeRabbit/ChatGPT gates behind explicit opt-in); CreatePackage AGENTS.md templates emit delta-only stubs instead of boilerplate; the turborepo skill mirror is repo-local (removed from remote skill sources).
