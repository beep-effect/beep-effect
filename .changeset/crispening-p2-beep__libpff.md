---
"@beep/libpff": patch
---

Crispen `@beep/libpff` for the P2 repo-crispening wave: move libpff error context and pffexport runtime defaults into schemas, preserve LiteralKit statics after annotation, model timeout absence with `Option`, add field-level schema annotations and `PosInt` timeout precision, collapse the error constructor helper to the class static, and add package-local encoded-shape plus `S.toArbitrary` parity laws. Public host-path tightening is deferred to the family-close sweep because it requires a cross-package consumer sweep.
