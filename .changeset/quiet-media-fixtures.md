---
"@beep/exiftool": patch
"@beep/face-detection": patch
---

Use canonical Effect test scopes and instrumented runners for media drivers.
Expose ExifTool schema laws as native properties and report missing native
executables as skips while preserving genuine process failures.
