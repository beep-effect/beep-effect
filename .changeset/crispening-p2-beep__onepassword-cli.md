---
"@beep/onepassword-cli": patch
---

Crispen `@beep/onepassword-cli` for the P2 repo-crispening wave: move diagnostic/account normalization, optional driver error context, exit-code and byte-count precision, and secret-reference validation into schemas, preserve raw secret stdout for reads, and add encoded-shape plus `S.toArbitrary` parity coverage for the package schema family.
