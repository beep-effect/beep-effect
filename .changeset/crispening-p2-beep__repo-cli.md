---
"@beep/repo-cli": patch
---

Crispen `@beep/repo-cli` for the P2 repo-crispening wave: replace heterogeneous `R.getSomes` option-struct spreads with schema-preserving `O.getSomesStruct`, colocate codec statics on create-package path/name schemas, move docgen and yeet command defaults into schema fields, narrow process-source MIME typing exposed by the option-struct helper, and add package-local encoded-shape plus optional-field parity tests. Larger public-form function, numeric, precision, and static API migrations are recorded as deferred family-close exceptions.
