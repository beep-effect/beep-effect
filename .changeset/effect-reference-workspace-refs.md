---
"@beep/ai-sync": patch
"@beep/repo-cli": minor
---

Add `beep refs plan | refresh | install-timer` for the Effect reference graft workspace, link `.repos/effect`, `.repos/effect-tsgo` and `.repos/effect-workspace` on `beep worktree new`, and admit the new agent permissions in the ai-sync policy.
