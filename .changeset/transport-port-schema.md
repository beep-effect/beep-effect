---
"@beep/schema": minor
"@beep/ui": patch
---

Add the transport port schema helpers and public subpath export.

Tighten schema default helper typing for decoded missing-key defaults and keep
UI editor/number-input prop defaults typed after the latest main merge.
